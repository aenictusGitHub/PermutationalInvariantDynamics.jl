var documenterSearchIndex = {"docs":
[{"category":"section","location":"cumulant_bridge/#Higher-order-cumulant-bridge","page":"Higher-order cumulant bridge","text":"The cumulant bridge supplies exact finite-N PI data for initializing, validating, and convergence-testing higher-order moment closures. It is an optional bridge rather than a second symbolic algebra system: the core package extracts exact moments and microscopic metadata, while an external package chooses symbolic operator and index spaces and performs the truncation.","title":"Higher-order cumulant bridge"},{"category":"section","location":"cumulant_bridge/#Exact-ordered-local-moments","page":"Higher-order cumulant bridge","text":"For local matrices A_a_1ldotsA_a_k on distinct particles, define\n\nM_a_1ldots a_k\n=mathrmtrleft(rho A_a_1^(1)cdots A_a_k^(k)right)\n\nFor a PI state this equals the expectation of the permutation-symmetrized k-site tensor summed over unordered subsets, divided by binomNk. ordered_local_moment performs that contraction directly in Schur blocks. ordered_local_moments evaluates all symmetry-canonical multisets from a labeled operator alphabet and reuses one PBodyGeometry per order.\n\nusing PermutationalInvariantDynamics, LinearAlgebra\n\nbasis = PIBasis(8, 2)\nrho = ghz_state(basis)\nsx = ComplexF64[0 1; 1 0]\nsz = ComplexF64[1 0; 0 -1]\n\nmoments = ordered_local_moments(rho, (x=sx, z=sz); order=3)\nm_xxz = moments[:x, :x, :z]\n\nLookup order is immaterial, but the moment always refers to distinct sites. Use an operator product as one alphabet entry when several operators act on the same site. The largest local tensor has d^(2k) entries. This is exponential in the deliberately selected closure order k, but it is independent of the exponentially larger full-system dimension d^N.","title":"Exact ordered local moments"},{"category":"section","location":"cumulant_bridge/#Neutral-model-schema","page":"Higher-order cumulant bridge","text":"cumulant_model_payload exports built-in or custom term metadata through the documented AbstractPITerm hooks. With time=t, schedules are evaluated using value_at(x,t,parameters); without a time, schedules and any available prototypes are retained. cumulant_bridge_payload combines this model description with an exact moment table.\n\nSchema 1.0.0 fixes the following adapter conventions:\n\nparticle one is the fastest tensor index;\na p-body operator is summed over unordered distinct subsets;\nordered local moments use distinct-particle/falling-factorial normalization;\nthe dissipator is Lrho L^dagger- L^dagger Lrho2;\nmicroscopic=false forbids automatic lowering of a direct PI term.\n\nAn adapter should reject a schema version it does not support. It must also ask the user for a local realization of direct PI terms instead of guessing one.","title":"Neutral model schema"},{"category":"section","location":"cumulant_bridge/#Comparing-a-closure","page":"Higher-order cumulant bridge","text":"compare_cumulant_closure accepts another moment table, a dictionary, or a callable. It reports per-moment and aggregate errors and raises when the closure omits a requested key.\n\nsigma = ComplexF64[0.6 0.1; 0.1 0.4]\nproduct = iid_state(basis, sigma)\nexact = ordered_local_moments(product, (x=sx, z=sz); order=3)\nclosure = Dict(key => prod(LinearAlgebra.tr(\n                   getproperty((x=sx, z=sz), label) * sigma)\n                   for label in key)\n               for key in keys(exact))\ncomparison = compare_cumulant_closure(exact, closure)\n@assert comparison.within_tolerance","title":"Comparing a closure"},{"category":"section","location":"cumulant_bridge/#Optional-QuantumCumulants.jl-adapter","page":"Higher-order cumulant bridge","text":"QuantumCumulants is a weak dependency with compatibility restricted to the supported 0.5 API line. After import QuantumCumulants, the package extension maps neutral keys to symbolic averages supplied by the user:\n\nimport QuantumCumulants\n\n# Construct these averages in the QuantumCumulants/SQA Hilbert and index\n# spaces chosen for the model.\nsymbolic_map = Dict((:x,) => symbolic_average_x,\n                    (:x, :z) => symbolic_average_xz)\nu0 = quantumcumulants_initial_values(exact, symbolic_map)\n\nThe extension also provides automatic indexed lowering for microscopic PI models:\n\nsymbolic = quantumcumulants_model(model; order=2,\n    complete=true, scale=true)\neqs = symbolic.equations\n\nThe adapter constructs an NLevelSpace, distinct symbolic indices for p-body operators, local or collective jump semantics, and the exact ordered-tuple to unordered-subset factor. Fixed correlated baths are lowered through their checked independent-channel factorization. Direct PI terms are rejected because their Schur blocks do not identify a unique microscopic operator; schedules require an explicit time and parameters. Custom seed operators can be supplied when a smaller symmetry-closed moment set is known. The resulting equations remain a selected-order cumulant approximation and should be checked against the exact PI moments above.\n\nThe complete runnable dependency-free example is examples/cumulant_bridge.jl.","title":"Optional QuantumCumulants.jl adapter"},{"category":"section","location":"cumulant_bridge/#API","page":"Higher-order cumulant bridge","text":"","title":"API"},{"category":"type","location":"cumulant_bridge/#PermutationalInvariantDynamics.OrderedLocalMoments","page":"Higher-order cumulant bridge","text":"OrderedLocalMoments\n\nExact distinct-particle local moments of a PIState, grouped by permutation symmetry.  table[:x, :y] represents tr(rho * X^(1) * Y^(2)); particle labels are distinct and the lookup order is immaterial for a PI state.  The stored dictionary therefore contains one canonical multiset key instead of all permutations of that key.\n\nThe table records moments only in the PI representation.  Its setup scales with the requested order through d^k local tensors and Appendix-D Schur geometry, not with the full Hilbert dimension d^N.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.OrderedLocalMoments"},{"category":"function","location":"cumulant_bridge/#PermutationalInvariantDynamics.ordered_local_moment","page":"Higher-order cumulant bridge","text":"ordered_local_moment(rho, operators; cache=nothing)\n\nReturn the exact PI expectation of local operators acting on an ordered tuple of distinct particles.  A single matrix computes a one-site moment; a tuple or vector (A, B, ...) computes tr(rho * A^(1) * B^(2) * ...).  Permutation invariance makes this value independent of how the distinct particle labels are assigned, but operators on the same particle are deliberately not combined by this API.\n\nThe implementation symmetrizes only the d^k local tensor, contracts Appendix-D Schur blocks, and divides by the exact binomial(N,k) subset count. It never constructs a d^N state.  Reuse a PBodyGeometry(basis,k) through cache for repeated moments of the same order.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.ordered_local_moment"},{"category":"function","location":"cumulant_bridge/#PermutationalInvariantDynamics.ordered_local_moments","page":"Higher-order cumulant bridge","text":"ordered_local_moments(rho, operators; order, include_lower=true)\n\nExtract all exact permutation-symmetric distinct-particle moments over a labeled local-operator alphabet.  operators may be a named tuple, dictionary, or iterable of Symbol => matrix pairs.  With include_lower=true, orders one through order are returned; otherwise only the selected order is returned.  For order=0, the sole empty-key value is trace(rho).\n\nOnly one canonical key is evaluated for each operator multiset.  Consequently an alphabet of size m requires binomial(m+k-1,k) contractions at exact order k, rather than m^k, and one PBodyGeometry is shared by every moment of that order.  The largest local scratch is d^order by d^order; there is no full-Hilbert reconstruction.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.ordered_local_moments"},{"category":"type","location":"cumulant_bridge/#PermutationalInvariantDynamics.CumulantTermPayload","page":"Higher-order cumulant bridge","text":"CumulantTermPayload\n\nDependency-neutral metadata for one microscopic AbstractPITerm.  process, scope, and order follow the public term hooks.  operator is a detached fixed operator, an in-place schedule prototype, or nothing when an unevaluated allocating schedule has no prototype; operator_schedule retains that schedule.  Direct PI terms have microscopic=false because their Schur blocks do not determine a unique local cumulant hierarchy.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.CumulantTermPayload"},{"category":"type","location":"cumulant_bridge/#PermutationalInvariantDynamics.CumulantModelPayload","page":"Higher-order cumulant bridge","text":"CumulantModelPayload\n\nVersioned, dependency-neutral description of a PIModel for an external cumulant-closure adapter.  It records N, d, copied microscopic operator prototypes, rate specifications, body orders, tensor-index ordering, and the package Hamiltonian/dissipator conventions.  No symbolic package types are stored in the core payload.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.CumulantModelPayload"},{"category":"type","location":"cumulant_bridge/#PermutationalInvariantDynamics.CumulantBridgePayload","page":"Higher-order cumulant bridge","text":"CumulantBridgePayload\n\nCombined versioned model metadata and exact OrderedLocalMoments for initialization, validation, or convergence checks of an order-k cumulant closure.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.CumulantBridgePayload"},{"category":"type","location":"cumulant_bridge/#PermutationalInvariantDynamics.CumulantComparison","page":"Higher-order cumulant bridge","text":"CumulantComparison\n\nError summary returned by compare_cumulant_closure.  rows retains the exact value, closure value, absolute error, relative error, and tolerance decision for every compared canonical moment key.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.CumulantComparison"},{"category":"function","location":"cumulant_bridge/#PermutationalInvariantDynamics.cumulant_model_payload","page":"Higher-order cumulant bridge","text":"cumulant_model_payload(model; time=nothing, parameters=nothing)\n\nExport the microscopic content of a PIModel into the versioned neutral CumulantModelPayload schema.  With no time, fixed operators are copied, InPlaceTimeOperator prototypes are copied while their schedules are retained, and allocating operator/rate schedules remain unevaluated.  Supplying time evaluates every schedule with the package value_at(x,time,parameters) convention and requires fixed numerical results.\n\nThe payload is intended for package-specific adapters which build automatic order-k closures.  A term with microscopic=false is a direct Schur-space term and must not be assigned a guessed local realization.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.cumulant_model_payload"},{"category":"function","location":"cumulant_bridge/#PermutationalInvariantDynamics.cumulant_bridge_payload","page":"Higher-order cumulant bridge","text":"cumulant_bridge_payload(model, rho, operators; order,\n                        include_lower=true, time=nothing,\n                        parameters=nothing)\n\nCreate one versioned object containing neutral microscopic model metadata and exact PI local moments through the requested closure order.  rho and model must own the identical PIBasis; this prevents a payload from mixing different sector restrictions or conventions.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.cumulant_bridge_payload"},{"category":"function","location":"cumulant_bridge/#PermutationalInvariantDynamics.compare_cumulant_closure","page":"Higher-order cumulant bridge","text":"compare_cumulant_closure(exact, closure; atol=nothing, rtol=nothing)\n\nCompare exact PI moments with a closure output.  closure may be another OrderedLocalMoments, a dictionary keyed by canonical label tuples (a single Symbol is also accepted for first order), or a callable.  Missing moments raise instead of being silently omitted.  Defaults are atol=0 and rtol=sqrt(eps(R)) in the promoted real scalar type R.\n\nThe per-row test is abs(closure-exact) <= atol + rtol*abs(exact).  Relative error is measured against abs(exact) and is Inf for a nonzero error at an exact zero.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.compare_cumulant_closure"},{"category":"function","location":"cumulant_bridge/#PermutationalInvariantDynamics.quantumcumulants_initial_values","page":"Higher-order cumulant bridge","text":"quantumcumulants_initial_values(moments, symbolic_map)\n\nMap neutral exact moment keys to user-supplied QuantumCumulants.jl symbolic averages.  This optional function becomes available after import QuantumCumulants; the package extension validates every symbolic order with QuantumCumulants' public get_order API and returns a dictionary suitable for assembling initial values.\n\nsymbolic_map maps a local label tuple (or a single first-order Symbol) to the corresponding symbolic average.  Symbolic Hamiltonian/jump construction is intentionally left to the adapter or research script because it depends on the selected QuantumCumulants Hilbert and index spaces.  The core package does not guess that mapping and does not depend on QuantumCumulants.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.quantumcumulants_initial_values"},{"category":"function","location":"cumulant_bridge/#PermutationalInvariantDynamics.quantumcumulants_model","page":"Higher-order cumulant bridge","text":"quantumcumulants_model(model; order=2, time=nothing, parameters=nothing,\n                       complete=false, scale=false, kwargs...)\n\nAutomatically lower the microscopic terms of a PIModel to indexed SecondQuantizedAlgebra operators and construct QuantumCumulants equations. This optional method becomes available after import QuantumCumulants; the core package retains no symbolic-algebra dependency.\n\nOne-body Hamiltonians and local or collective jumps are lowered directly. Permutation-symmetric p-body terms use pairwise-distinct symbolic indices and the exact 1/factorial(p) conversion from ordered tuples to the package's unordered-subset convention. Fixed correlated one-body baths are factorized into independent symbolic jump channels. Direct PI terms are rejected because Schur blocks do not specify a unique microscopic realization. Time-dependent operators, rates, or Kossakowski matrices require an explicit time (and optional parameters) at which they can be evaluated.\n\nThe returned named tuple retains the symbolic Hilbert space, summation and probe indices, Hamiltonian, jumps, rates, seed operators, equations, and lowering metadata. By default the equations contain the requested seed moments but are not recursively completed or permutation-scaled; set complete=true and/or scale=true explicitly. Extra keywords are forwarded to the optional adapter, including seed_operators, space_name, transition_name, and ground_state.\n\nThis is a cumulant closure, not exact finite-N PI dynamics. Validate its selected order against ordered_local_moments or exact PI evolution.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.quantumcumulants_model"},{"category":"section","location":"heom/#Permutationally-invariant-HEOM","page":"PI–HEOM non-Markovian dynamics","text":"The hierarchy equations of motion (HEOM) backend adds finite-memory bosonic environments to an otherwise time-local PI system model. Every auxiliary density operator (ADO) is stored in the same PI Schur coordinates as the physical density operator. Consequently, the method enlarges the calculation by the hierarchy size, but it never reconstructs a matrix of dimension d^N.\n\nThe backend is intended for bath correlations represented by finite sums of exponentials. HEOMBath remains the exact low-level convention. Validated physical constructors additionally prepare Drude–Lorentz Matsubara/Padé and underdamped-Brownian Matsubara expansions. Their truncation and white-noise residue are explicit metadata; the library never silently changes a user-supplied decomposition.\n\nFor a stochastic pure-state hierarchy of a shared Gaussian bath, see Permutationally invariant HOPS. PI–HOPS replaces every density ADO by a direct-sum Schur pseudo-ket and then averages independent unnormalized root outer products. It has a different pathwise symmetry requirement and statistical convergence limit; it is not an automatic drop-in replacement for independent local baths.","title":"Permutationally invariant HEOM"},{"category":"section","location":"heom/#Convention","page":"PI–HEOM non-Markovian dynamics","text":"For bath b, the coefficients supplied to HEOMBath define the left correlation\n\nC_b^L(t)=sum_kin bell_k e^-nu_k tqquad tgeq0\n\nThe physical conjugate correlation is\n\nC_b^R(t)=C_b^L(t)^*\n\nBoth are stored on one prepared pole list, C_b^R(t)=sum_k r_k exp(-nu_k*t). Real poles receive r_k=conj(ell_k). Exact complex-conjugate pole pairs are cross-paired. If a complex pole has no exact conjugate partner, the constructor appends its conjugate with zero left coefficient and the required right coefficient. Thus a single input complex exponential normally becomes two prepared HEOM poles while leaving C_b^L unchanged. This completion is exact rather than tolerance based.\n\nThe advanced right_coefficients keyword supplies r_k explicitly on the original pole list and disables completion. It is useful for already prepared left/right decompositions, but the library then does not certify C_b^R=C_b^{L*}; an inconsistent choice can destroy Hermiticity of the root ADO and is never repaired.\n\nThe coupling Q_b is a fixed Hermitian PI operator. All coefficients and nu_k may be complex, but Re(nu_k) must be strictly positive. Flatten all prepared exponentials from all baths into one index k, and let b(k) identify their bath. The conventional unscaled hierarchy implemented by HEOMPlan is\n\nbeginaligned\ndotrho_boldsymbol n=\nmathcal L_Srho_boldsymbol n\n-sum_k n_knu_krho_boldsymbol n\n-isum_kQ_b(k)rho_boldsymbol n+boldsymbol e_k\n-isum_k n_kleft(\n ell_k Q_b(k)rho_boldsymbol n-boldsymbol e_k\n -r_krho_boldsymbol n-boldsymbol e_kQ_b(k)\nright)\nendaligned\n\nL_S can itself contain Hamiltonian and Markovian dissipative terms. The root ADO rho_0 is the physical reduced state. Auxiliary ADOs generally have neither unit trace, Hermiticity, nor positivity and must not be interpreted as density matrices.","title":"Convention"},{"category":"section","location":"heom/#Exact-scaled-coordinates","page":"PI–HEOM non-Markovian dynamics","text":"The default scaling=:unscaled preserves those coordinates. The opt-in scaling=:scaled route stores\n\nwidehatrho_boldsymbol n=rho_boldsymbol ns_boldsymbol n\nqquad\ns_boldsymbol n=prod_ksqrtn_ka_k^n_k\n\nThe positive per-pole factor defaults to a_k=max(abs(ell_k),abs(r_k)), with exactly one used when both coefficients vanish. An explicit scaling_factors vector may instead be supplied; every entry must be positive, finite, and exactly representable in the plan's real scalar type. The transformed upward and downward factors are respectively\n\nsqrt(n_k+1)a_kqquad sqrtn_ka_k\n\nThus the scaled generator is exactly S^(-1) L_HEOM S, not a truncation or changed bath model. The root scale is one, and heom_ado multiplies an auxiliary coordinate by heom_coordinate_scale before returning the conventional unscaled ADO. At finite precision the constructor also requires each standalone s_n needed for public ADO extraction to remain positive and finite; it raises with depth/precision guidance rather than overflowing silently. scaling=:unscaled remains fully compatible with the original route.\n\nThe hierarchy retains sum(n_k) <= D, where D=max_depth. Upward ADOs beyond that boundary are set to zero. This is the terminator=:none hard truncation. With terminator=:residue, each bath's explicitly stored real discrepancy delta_b adds the time-local correction\n\n2delta_bmathcal DQ_brho=-delta_bQ_bQ_brho\n\nto every ADO. An arbitrary HEOMBath defaults to zero residue; none is inferred from its poles. Physical constructors calculate the documented zero-frequency discrepancy. The correction is matrix-free, and the exact adjoint uses its self-adjoint double-commutator form. A finite-depth and finite-pole convergence study remains necessary even when the residue is used.\n\nAn optional positive importance_cutoff applies an additional, explicitly heuristic approximation. For pole k, define\n\nw_k=fracmax(ell_kr_k)nu_k^2qquad\nq_k=sqrtfracw_k1+w_kqquad\nI_boldsymbol n=prod_kfracq_k^n_ksqrtn_k\n\nThe :normalized_coupling_decay policy retains I_n >= importance_cutoff and requires every immediate parent to be retained. The result is therefore a deterministic downward-closed order ideal; couplings to omitted upward ADOs are zero. This score is a ranking heuristic, not an error bound. Zero cutoff preserves the complete hierarchy and its original setup route. Inspect heom_hierarchy_metadata and heom_ado_importances, and repeat the calculation at lower cutoffs.\n\nFor K exponential terms, the number of retained ADOs is exactly\n\nM=binomK+DD\n\nand the coordinate dimension is M * length(basis). Setup checks this count with exact integer arithmetic before allocation.\n\nThe hierarchy state therefore stores M * n_PI complex coordinates. For positive K and D, the number of undirected nearest-hierarchy edges is\n\nE=KbinomK+D-1D-1\n\nThe plan stores each edge once, together with two compact incident-edge references and only K*D level-dependent scaling factors. It does not retain dense M-by-K upward/downward index and factor tables. Topology indices use 32 bits whenever the prepared counts permit. One dense physical coupling block is retained per selected PI sector and bath. PI compression removes the system's d^N scaling; it does not remove the combinatorial hierarchy growth or the dense cost of a large retained Schur block.","title":"Exact scaled coordinates"},{"category":"section","location":"heom/#Basic-workflow","page":"PI–HEOM non-Markovian dynamics","text":"The following is a one-qubit exponentially correlated dephasing bath. The same code works for a larger PI ensemble; only the PI basis and collective coupling operator change.\n\nusing PermutationalInvariantDynamics\n\nbasis = PIBasis(1, 2)\nspin = spin_matrices()\nQ = collective_operator(basis, spin.jz)\n\n# C(t) = 0.30 exp(-1.20t)\nbath = HEOMBath(Q, 0.30, 1.20)\nsystem = PIModel(basis, ())\nplan = HEOMPlan(system, bath; max_depth=4)\n\nrho0 = iid_pure_state(basis, ComplexF64[1, 1] / sqrt(2))\nhierarchy0 = heom_initial_state(plan, rho0)\nhierarchy_t = heom_evolve(plan, hierarchy0, (0.0, 0.7); steps=200)\nrho_t = heom_reduced_state(hierarchy_t)\n\nheom_initial_state constructs the conventional factorized initial condition: rho_0 is supplied by the user and every auxiliary ADO is zero. Use heom_ado to inspect a detached auxiliary operator by position or occupation vector.\n\nfirst_auxiliary = heom_ado(hierarchy_t, [1])\nindices = heom_multiindices(plan)\n\nFor several baths, pass a tuple. Each bath may contain several exponentials:\n\nbath_z = HEOMBath(Qz, [c1, c2], [nu1, nu2])\nbath_x = HEOMBath(Qx, [c3], [nu3])\nplan = HEOMPlan(system, (bath_z, bath_x); max_depth=4)\n\nThe occupation-vector entries follow the prepared flattened order: all terms of bath_z, including any automatically appended conjugate poles, followed by all prepared terms of bath_x.\n\nIf a bath decomposition already provides both actions on one common pole list, pass them explicitly:\n\nbath = HEOMBath(Q, left, poles; right_coefficients=right)\n\nHere left, right, and poles must have the same length. This route does not append poles or impose a conjugacy relation.","title":"Basic workflow"},{"category":"section","location":"heom/#Physical-bath-constructors","page":"PI–HEOM non-Markovian dynamics","text":"drude_lorentz_bath uses units hbar=k_B=1 and the convention\n\nJ(omega)=frac2lambdagammaomegagamma^2+omega^2\n\nFor decomposition=:matsubara, it retains\n\nbeginaligned\nnu_0=gamma\nc_0=lambdagammaleftcot(betagamma2)-iright\nnu_k=2pi kbeta\nc_k=frac4lambdagammabeta\n      fracnu_knu_k^2-gamma^2\nendaligned\n\nThe Padé option uses the Hu–Xu–Yan tridiagonal-eigenvalue construction (J. Chem. Phys. 134, 244106 (2011)). Its small symmetric eigensolves currently support Float32 and Float64 physical parameters; use Matsubara for BigFloat. A coincident Drude and thermal pole is rejected because the separate terms are singular and require a combined analytic limit. Padé setup has the package-wide 512 MiB memory_budget; use Inf only as an explicit opt-out after checking the quadratic eigensystem. The Padé parameter routine was adapted from QuTiP's BSD-3-Clause HEOM bath implementation and modified for the package's precision, validation, and memory contracts. The exact revision, affected routine, modifications, and complete upstream notice are recorded in THIRD_PARTY_NOTICES.md.\n\nbath = drude_lorentz_bath(\n    Q, 0.3, 0.8, 2.0;\n    matsubara_terms=4, decomposition=:pade)\nplan = HEOMPlan(system, bath; max_depth=5, terminator=:residue)\n\nunderdamped_brownian_bath implements the explicitly different spectral-density convention\n\nJ(omega)=fraclambda^2gammaomega\n(omega_0^2-omega^2)^2+gamma^2omega^2\n\nwith 0  gamma  2omega_0, its two conjugate damped-oscillator poles, and a finite Matsubara tail. Here coupling_strength=lambda enters quadratically; it is not the Drude reorganization-energy parameter. This distinction is recorded in heom_bath_metadata. Both physical constructors store the real finite-tail discrepancy returned by heom_bath_residue. The residue may be signed for a finite approximation and is never clipped. These bath and terminator conventions also follow the open-source HEOM formulation described by Lambert et al., Phys. Rev. Research 5, 013181 (2023).\n\nPhysical-parameter inputs preserve Float32, Float64, or BigFloat on the Matsubara route. Generated coefficients are rounded once in that selected precision. Increase the number of expansion terms independently of hierarchy depth and integration accuracy.","title":"Physical bath constructors"},{"category":"section","location":"heom/#Thermal-and-correlated-preparation","page":"PI–HEOM non-Markovian dynamics","text":"heom_thermal_state starts from the bare-system Gibbs state. Its three preparation modes make the approximation explicit:\n\nfactorized = heom_thermal_state(plan, H, beta)\n\nrelaxed = heom_thermal_state(\n    plan, H, beta;\n    preparation=:relaxation, relaxation_time=20.0, steps=4000)\n\nstationary = heom_thermal_state(\n    plan, H, beta;\n    preparation=:stationary,\n    preconditioner=:block, krylovdim=40)\n\nThe factorized route has zero auxiliary ADOs. Real-time relaxation produces a correlated hierarchy after a user-selected finite time. The stationary route uses the factorized hierarchy only as the initial GMRES guess. Neither route is imaginary-time HEOM, and a stationary root is thermal only if the supplied system generator and physical bath satisfy equilibrium assumptions. Known physical-bath inverse-temperature metadata is checked exactly, but arbitrary Markovian drives cannot be certified automatically.","title":"Thermal and correlated preparation"},{"category":"section","location":"heom/#Identical-independent-local-environments","page":"PI–HEOM non-Markovian dynamics","text":"A global HEOMBath coupled through Q=sum_i q_i describes a common bath and contains cross-correlations between different particles. It is not an implementation of N independent local baths. A true local-bath HEOM requires symmetry-adapted occupations carrying both site and hierarchy labels; those coordinates do not fit the current global-ADO HEOMPlan.\n\nFor a physically realizable collection of damped poles, use the exact finite-cutoff supersite embedding described in Local pseudomodes and PI supersites:\n\nmode = BosonicPseudomode(\n    3;\n    label=:local_bath,\n    frequency=omega_c,\n    damping=kappa,\n    thermal_occupation=nbar,\n)\n\ncoupling = PseudomodeCoupling(\n    Llocal;\n    mode=:local_bath,\n    strength=g,\n)\n\nembedding = pseudomode_model(\n    N, Hlocal, mode;\n    couplings=(coupling,),\n)\n\nbasis = embedding.basis\nmodel = embedding.model\n\nEvery system together with all of its local modes is one identical PI supersite. LocalJump creates independent damping on every copy of a mode. This is a time-local Markovian embedding in system ⊗ mode1 ⊗ mode2 ⊗ ... local order, not global-ADO HEOM. At zero occupation, one rotating-wave mode realizes the pole c=abs2(g) and nu=kappa/2 + im*frequency through g*L*a' + conj(g)*L'*a. Non-Hermitian L, Hermitian longitudinal couplings, several modes, and counter-rotating terms are supported.\n\npseudomode_model lifts fixed local, collective, and symmetric p-body system terms into the paired supersite ordering. Independent mode damping usually requires every Schur sector; a restricted basis is accepted only when PIModel validates it. The oscillator cutoffs are explicit approximations and must be converged separately. An arbitrary fitted complex pole set need not admit a positive damped-mode realization.\n\nThe historical single-mode convenience independent_local_pseudomode_model remains available, but the BosonicPseudomode/PseudomodeCoupling workflow is the general interface.","title":"Identical independent local environments"},{"category":"section","location":"heom/#Repeated-and-matrix-free-calculations","page":"PI–HEOM non-Markovian dynamics","text":"HEOMPlan is immutable prepared data. HEOMWorkspace owns only application scratch: a system-Liouvillian workspace and three PI-sized vectors for left/right and residue double-commutator actions. Reuse one workspace per task:\n\nwork = HEOMWorkspace(plan)\ny = similar(hierarchy0.data)\napply!(y, plan, hierarchy0.data, work)  # autonomous system\n\nOne application views the hierarchy as an n_PI-by-M matrix and evaluates the prepared system Liouvillian in bounded column batches. The packed edge incidences are ordered by source ADO and bath. Consequently, Q_b*rho_n and rho_n*Q_b are computed once for each nonempty (n,b) group and then scattered to every pole edge in that group. Forward and adjoint application share the same packed topology and use algebraically adjoint weights; this is only a contraction reordering and does not alter the hierarchy equations.\n\nMatrix right-hand sides are flattened as n_PI by (number of ADOs * number of right-hand sides). A driven system schedule is prepared once for that complete application even when the flattened matrix is processed in several bounded chunks. Prepare a wider bounded chunk up front when it is reused often:\n\nX = hcat(hierarchy0.data, hierarchy_probe_1, hierarchy_probe_2)\nY = similar(X)\nbatch_work = HEOMWorkspace(plan; batch_columns=size(X, 2))\napply!(Y, plan, X, 0.0, parameters, batch_work)\n\nThe system batch never exceeds the internal width bound. A default HEOMWorkspace(plan) remains valid for arbitrary matrix widths; it uses more chunks without growing its retained arrays. Forward and adjoint matrix applications use the same layout.\n\nHEOMEvolutionWorkspace additionally owns three hierarchy-sized vectors for low-storage fixed-step RK4: a stage state, one derivative, and a weighted stage accumulator. Passing it to heom_evolve! avoids repeated allocations across a parameter or convergence scan.\n\nevolution_work = HEOMEvolutionWorkspace(plan)\ndestination = copy(hierarchy0)\nheom_evolve!(destination, plan, hierarchy0, (0.0, 0.5);\n             steps=200, workspace=evolution_work)","title":"Repeated and matrix-free calculations"},{"category":"section","location":"heom/#Instantaneous-PI-pulses","page":"PI–HEOM non-Markovian dynamics","text":"An ideal system unitary can be applied to every ADO without restarting the hierarchy:\n\npulse = PIUnitaryPulse(basis, exp(-im * pi * spin.jx))\nsequence = HierarchyPulseSequence(pulse_times, pulse)\nstates = heom_time_evolution(\n    plan, rho0, times;\n    steps_per_interval=8, pulses=sequence)\n\nFor a local matrix, PIUnitaryPulse prepares the Schur blocks of U^otimes N directly; a compatible PI operator is also accepted. At an event, every ADO is mapped as rho_boldsymbol nmapsto Urho_boldsymbol nU^dagger. The root and all memory auxiliaries therefore receive the same physical system pulse.\n\nThe fixed-step driver splits an interval exactly at each event and uses the half-open convention (start, stop]. A pulse at a saved endpoint is applied before that hierarchy state is saved and cannot be applied twice by adjacent intervals. apply_hierarchy_pulse! exposes the same in-place operation for a caller-managed HEOMState and task-owned HEOMEvolutionWorkspace.\n\nPublished tetrahedral, octahedral, and icosahedral Eulerian sequences are available directly:\n\ntau0 = 0.01\ntedd = tetrahedral_pulse_sequence(basis, tau0)\noedd = octahedral_pulse_sequence(basis, tau0; cycles=2)\niedd = icosahedral_pulse_sequence(\n    basis, tau0; start_time=0.5)\n\nThese constructors reproduce the TEDD, OEDD, and IEDD Cayley-graph words of 24, 48, and 120 events from Read, Serrano-Ensástiga, and Martin, Quantum 9, 1661 (2025). Only two immutable axis–angle pulses are prepared for each schedule and shared by reference across every event. The positive tau0 is the free interval before each pulse, so one cycle ends after 24tau0, 48tau0, or 120tau0, with the final cyclic pulse included at that endpoint.\n\nThe constructors implement ideal global spin rotations for any local spin dimension. Their universal first-order single-spin ranges are respectively dleq3, dleq4, and dleq6. Claims about cancellation of interacting spin Hamiltonians additionally require the anisotropy conditions stated in the paper. A purely global rotation cannot remove a nontrivial rotationally invariant interaction.\n\nThe plan scalar type is promoted from the prepared system, couplings, both sets of bath coefficients, and frequencies. Sources, destinations, times, and step counts must be representable without narrowing; compiled matrix-free system scratch cannot be widened after preparation. Build the system and hierarchy at the wider precision instead. Plans may be shared read-only, but each concurrent application or evolution requires its own workspace.\n\nInteger bath coefficients and frequencies are accepted only when their exact values survive conversion to that prepared scalar type. Pass a suitably precise BigFloat value (and prepare the system at matching precision) when a large integer is not exactly representable; it is never silently rounded. Explicit scaled-coordinate factors obey the same no-narrowing rule.\n\nThe bath owns a detached copy of its coupling, and a raw matrix supplied as the system generator is copied into the plan. Subsequent mutation of either caller object therefore cannot change prepared HEOM dynamics.\n\nHierarchy truncation is independent of the RK4 error. Use heom_depth_convergence to compare successive depths in the Hilbert–Schmidt norm of the final reduced state:\n\nreport = heom_depth_convergence(\n    system, bath, rho0, (0.0, 0.7);\n    depths=(2, 4, 6), steps=200,\n    atol=1e-9, rtol=1e-7, consecutive=2)\n\ncollect(skipmissing(report.pairwise_errors))\nreport.pairwise_converged\nreport.converged\n\nThe system model is compiled once, and every truncated plan shares the same prepared system and coupling blocks. Intermediate results retain only the PI root states; last(report.results).hierarchy is the sole complete saved hierarchy, while each raw result exposes its ADO count, dimension, trace drift, and elapsed time. The standard ConvergenceStudyResult decision uses the final consecutive requested depth pairs and never normalizes or repairs either state. Repeat the report with a finer time step before attributing its entire difference to hierarchy depth. The reported trace error is drift from the supplied initial trace, not an assumption that the input was normalized. Pass scaling=:scaled (and optional scaling_factors) to the source-based overload, or use a scaled template plan; every prefix preserves the same per-pole similarity transform.\n\nheom_liouvillian adapts the plan to the package's synchronized MatrixFreeLiouvillian interface. Its physical trace vector is nonzero only on the root ADO:\n\nmathrmtr_mathrmHEOM(boldsymbolrho)\n=mathrmtr(rho_boldsymbol0)\n\nThis enables the existing matrix-free Krylov machinery without materializing the full hierarchy generator. Both its forward and adjoint callbacks are exact; explicit task-owned hot loops may call apply! and apply_adjoint! with one HEOMWorkspace.\n\nheom_steady_state is the direct trace-fixed GMRES convenience. For larger hierarchies, preconditioner=:block extracts the common system block once and represents every non-root ADO-diagonal n_PI-by-n_PI block by a scalar shift. For ComplexF32 and ComplexF64, one LAPACK Schur form normally serves every well-conditioned shift, so retained storage is O(n_PI^2 + M) even when all hierarchy decays differ. A reciprocal-condition guard assigns an ordinary LU factor to any unsafe shift. Unsupported scalar types use the generic LU route, sharing factors for exactly repeated shifts:\n\nstationary_hierarchy = heom_steady_state(\n    plan; preconditioner=:block,\n    krylovdim=40, maxiter=800, atol=1e-10, rtol=1e-8)\nrho_ss = heom_reduced_state(stationary_hierarchy)\n\nUse heom_block_preconditioner directly when its setup will be reused across solves. It omits only inter-ADO couplings from the preconditioning approximation; it does not modify the generator or convergence test. Inspect preconditioner_cost for the system right-hand sides, batched setup calls, Schur/LU factorizations, guarded and fallback shifts, retained coefficients, and per-application triangular solves. Every Schur application checks a transformed residual and finite output. Its two scratch vectors are lock-protected, so sharing one Schur preconditioner across concurrent solves is safe but serialized; construct one per concurrent solve for parallel throughput. shift_backend=:lu retains allocation-free concurrent application without shared numerical scratch. No precision-narrowing shortcut is used.\n\nStationary solving requires an autonomous system generator. As for any finite HEOM calculation, convergence of rho_ss with hierarchy depth remains the user's responsibility.\n\nFor adaptive or stiff integration, heom_problem returns an in-place SciMLBase.ODEProblem with its own captured HEOM workspace:\n\nproblem = heom_problem(plan, hierarchy0, (0.0, 10.0))\n\nChoose and import a compatible SciML solver separately. Construct independent problems for concurrent solves because each problem owns mutable scratch.","title":"Instantaneous PI pulses"},{"category":"section","location":"heom/#Reliability-checklist","page":"PI–HEOM non-Markovian dynamics","text":"Check separately:\n\nconvergence of the bath exponential decomposition;\nconsistency of the left/right correlation decomposition, especially when right_coefficients is explicit;\nconvergence in physical expansion terms and the effect of terminator=:residue;\nconvergence in max_depth and, when used, importance_cutoff;\nconvergence of the time step or Krylov residual;\npreservation of tr(heom_reduced_state(state));\nHermiticity and physicality of the root ADO.\n\nThe implementation never normalizes the hierarchy, clips eigenvalues, symmetrizes invalid couplings, changes explicit bath coefficients, or inserts a terminator without an explicit terminator=:residue. Default conjugate-pole completion is part of the documented correlation representation, not a state repair. The constructor rejects nonfinite coefficients, nondecaying frequencies, non-Hermitian fixed couplings, incompatible PI bases, and scalar types that would narrow the prepared system Liouvillian.","title":"Reliability checklist"},{"category":"section","location":"heom/#Current-limitations","page":"PI–HEOM non-Markovian dynamics","text":"The implemented hierarchy is the unscaled or exactly similarity-scaled bosonic Gaussian-bath HEOM for fixed Hermitian system couplings. Fermionic sign hierarchies and non-Hermitian coupling pairs require a different construction.\nEach declared environment couples through one global PI operator Q_b. A common bath coupled to a collective observable is therefore supported, but this is not the specialized global-ADO hierarchy for N independent local non-Markovian baths. Use the finite-cutoff pseudomode embedding for a realizable damped pole.\nFactorized, finite real-time relaxed, and stationary correlated preparation routes are provided. Imaginary-time HEOM and a certificate that an arbitrary driven generator thermalizes to the interacting equilibrium are not.\nThe residue terminator is a zero-frequency white-noise approximation. Low-temperature corrections beyond the retained physical expansion and Hamiltonian counterterms are not inferred.\nThe number of ADOs grows as binomial(K+D,D). PI compression removes the exponential system Hilbert space, not the combinatorial hierarchy growth caused by many bath poles and a large depth.\nFixed-step RK4 remains the dependency-free dedicated time-domain solver. heom_problem supplies a SciML problem but no adaptive/stiff algorithm; add and import the solver package appropriate to the calculation.\n\nThe complete executable dephasing comparison is in examples/pi_heom.jl, with discussion in examples/pi_heom.md. The ideal-pulse extension in examples/nonmarkovian_dynamical_decoupling.jl applies CPMG and UDD4 events to every ADO, compares the reduced state with PI–HOPS, and distinguishes the full-line one-pole Lorentzian correlation from the physical positive-frequency spectral-density integral.","title":"Current limitations"},{"category":"section","location":"heom/#API","page":"PI–HEOM non-Markovian dynamics","text":"","title":"API"},{"category":"type","location":"heom/#PermutationalInvariantDynamics.HEOMBath","page":"PI–HEOM non-Markovian dynamics","text":"HEOMBath(coupling, coefficients, frequencies;\n         right_coefficients=nothing, residue=0, metadata=(;),\n         check=true, atol=0, rtol=sqrt(eps(...)))\n\nFinite exponential representation of one bosonic bath correlation function,\n\nC(t)=sum_k c_kexp(-nu_k t)qquad tgeq0\n\ncoupling is the fixed Hermitian PI system operator Q in H_mathrmint=Qotimes B. coefficients[k] is the left coefficient c_k and frequencies[k] is nu_k. By default the constructor represents the conjugate correlation on the same pole list: real poles use conj(c_k), exact complex-conjugate pairs are cross-paired, and an absent conjugate pole is appended with zero left coefficient. Pass right_coefficients to specify the same-pole right coefficients explicitly and disable this completion. Every pole must have a finite strictly positive real part. A scalar coefficient/frequency pair is accepted as a convenience. Integer-valued bath data must be exactly representable in the prepared scalar type; otherwise preparation raises and requests an explicitly wider floating input instead of rounding it. residue is an explicit real coefficient for the white-noise approximation to omitted correlation terms. It is ignored by HEOMPlan(...; terminator=:none) and included as 2mathttresiduemathcal DQ only when terminator=:residue. metadata must be an immutable named tuple.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.HEOMBath"},{"category":"function","location":"heom/#PermutationalInvariantDynamics.drude_lorentz_bath","page":"PI–HEOM non-Markovian dynamics","text":"drude_lorentz_bath(coupling, reorganization_energy, cutoff,\n                   inverse_temperature; matsubara_terms=0,\n                   decomposition=:matsubara, memory_budget=512MiB)\n\nConstruct the finite Matsubara decomposition of the Drude–Lorentz spectral density\n\nJ(omega)=frac2lambdagammaomegagamma^2+omega^2\n\nUnits use hbar=k_B=1 and inverse_temperature is beta. The returned HEOMBath contains the Drude pole and exactly matsubara_terms positive thermal poles. decomposition=:matsubara supports generic floating precision. decomposition=:pade uses the Hu–Xu–Yan tridiagonal-eigenvalue construction and currently requires Float32 or Float64 physical parameters. Its residue is the real zero-frequency discrepancy of the omitted terms. Opt into the associated white-noise correction explicitly with HEOMPlan(...; terminator=:residue). Singular coincident Drude/Matsubara poles are rejected rather than evaluated as Inf - Inf. The Padé eigensolve obeys memory_budget; Inf is the explicit opt-out.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.drude_lorentz_bath"},{"category":"function","location":"heom/#PermutationalInvariantDynamics.underdamped_brownian_bath","page":"PI–HEOM non-Markovian dynamics","text":"underdamped_brownian_bath(coupling, coupling_strength, damping,\n                          resonance, inverse_temperature;\n                          matsubara_terms=0)\n\nConstruct a Matsubara decomposition of the underdamped Brownian spectral density\n\nJ(omega)=fraclambda^2gammaomega\n(omega_0^2-omega^2)^2+gamma^2omega^2\n\nThis exact convention matters: coupling_strength is lambda and enters quadratically. The constructor requires 0gamma2omega_0 and retains the conjugate damped-oscillator poles plus matsubara_terms thermal poles. The finite-tail residue is stored exactly as for drude_lorentz_bath.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.underdamped_brownian_bath"},{"category":"function","location":"heom/#PermutationalInvariantDynamics.heom_bath_metadata","page":"PI–HEOM non-Markovian dynamics","text":"Return the immutable physical/decomposition metadata attached to bath.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.heom_bath_metadata"},{"category":"function","location":"heom/#PermutationalInvariantDynamics.heom_bath_residue","page":"PI–HEOM non-Markovian dynamics","text":"Return the explicit real white-noise residue coefficient of bath.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.heom_bath_residue"},{"category":"type","location":"heom/#PermutationalInvariantDynamics.HEOMPlan","page":"PI–HEOM non-Markovian dynamics","text":"HEOMPlan(system, baths; max_depth, basis=nothing,\n         terminator=:none, scaling=:unscaled,\n         scaling_factors=nothing, importance_cutoff=0,\n         importance_metric=:normalized_coupling_decay)\n\nPrepared finite-exponential PI hierarchy of equations of motion. The root ADO is the physical reduced state, while all auxiliary ADOs use the same PI coefficient coordinates. max_depth applies a hard hierarchy truncation; convergence must be checked by increasing it. scaling=:scaled applies the documented exact diagonal similarity transform and does not approximate the hierarchy.\n\nThe plan owns immutable system, coupling, pole, multi-index, and packed-edge topology data. Numerical application scratch belongs to HEOMWorkspace or HEOMEvolutionWorkspace. Production application is matrix-free and never constructs a d^N system object. See HEOMBath for the finite-correlation convention and heom_depth_convergence for an explicit truncation study.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.HEOMPlan"},{"category":"type","location":"heom/#PermutationalInvariantDynamics.HEOMWorkspace","page":"PI–HEOM non-Markovian dynamics","text":"HEOMWorkspace(plan; batch_columns=1)\n\nTask-owned mutable storage for matrix-free HEOM generator application. A workspace may be reused sequentially but not concurrently. RK4 stage arrays live separately in HEOMEvolutionWorkspace, so spectral and steady-state calculations do not retain three hierarchy-sized evolution vectors. batch_columns prepares the bounded system-action scratch for that many hierarchy right-hand sides; the retained system batch width is capped by the implementation's fixed chunk width.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.HEOMWorkspace"},{"category":"type","location":"heom/#PermutationalInvariantDynamics.HEOMEvolutionWorkspace","page":"PI–HEOM non-Markovian dynamics","text":"HEOMEvolutionWorkspace(plan)\n\nTask-owned HEOM application scratch plus three full hierarchy vectors for the low-storage classical RK4 path: a stage state, one derivative, and a weighted stage accumulator. The legacy k3 and k4 fields remain empty compatibility placeholders in newly constructed workspaces; a field-constructed legacy workspace with full arrays remains accepted. Reuse the workspace for repeated fixed-step evolution, but do not share it between concurrent tasks.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.HEOMEvolutionWorkspace"},{"category":"type","location":"heom/#PermutationalInvariantDynamics.HEOMState","page":"PI–HEOM non-Markovian dynamics","text":"HEOMState(plan, data)\n\nState of a prepared hierarchy in ADO-major order. Each ADO stores one complete PI coefficient vector in plan.scaling; heom_ado converts selected auxiliaries back to conventional unscaled coordinates. Construction copies data and rejects scalar narrowing; it does not normalize or repair the reduced state.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.HEOMState"},{"category":"type","location":"heom/#PermutationalInvariantDynamics.HEOMBlockPreconditioner","page":"PI–HEOM non-Markovian dynamics","text":"HEOMBlockPreconditioner\n\nReusable guarded Schur-shift or LU factors for the ADO-diagonal blocks of the normalized, trace-fixed HEOM stationary operator. LAPACK complex precisions retain one Schur form and compact per-ADO shifts; ill-conditioned shifts and unsupported scalar types use exact block LU factors. Construct one with heom_block_preconditioner and apply it through ldiv!.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.HEOMBlockPreconditioner"},{"category":"function","location":"heom/#PermutationalInvariantDynamics.heom_block_preconditioner","page":"PI–HEOM non-Markovian dynamics","text":"heom_block_preconditioner(plan; regularization=0,\n                          operator_scale=nothing,\n                          shift_backend=:auto,\n                          schur_rcond_threshold=nothing,\n                          schur_residual_tolerance=nothing,\n                          expected_reuses=1,\n                          expected_solve_applications=30,\n                          warn_unamortized=true)\n\nConstruct a hierarchy-aware left preconditioner for trace-fixed HEOM GMRES. The common dense npi-by-npi system block is extracted once, after which every non-root diagonal block is formed by a scalar shift. With shift_backend=:auto, ComplexF32 and ComplexF64 plans retain one LAPACK Schur factorization. A triangular reciprocal-condition estimate guards every distinct shift; unsafe shifts receive a conventional LU factor. Other scalar types use duplicate-aware LU factors throughout. Pass shift_backend=:lu to force that generic route, or :schur to require a LAPACK-supported plan. schur_rcond_threshold and the application-time transformed-residual tolerance default to precision-aware values. All hierarchy couplings are omitted from the approximation. The root always has its own LU factor containing the trace-fixing rank-one term, and every block uses the same reproducible operator normalization as krylov_steady_state.\n\nSetup uses at most plan.npi system right-hand sides, evaluated in bounded batches, rather than heom_number_ados(plan) * plan.npi complete HEOM applications. Reuse the returned object across solves when that cost amortizes. A positive regularization is an explicit numerical modification of the preconditioner only; it never changes the HEOM generator. The guarded Schur route retains two scratch vectors protected by a lock, so concurrent applications of one preconditioner serialize; construct one preconditioner per concurrent solve when that matters. The LU-only route retains no shared numerical scratch and does not serialize.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.heom_block_preconditioner"},{"category":"function","location":"heom/#PermutationalInvariantDynamics.heom_number_ados","page":"PI–HEOM non-Markovian dynamics","text":"Return the number of auxiliary density operators retained by plan.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.heom_number_ados"},{"category":"function","location":"heom/#PermutationalInvariantDynamics.heom_multiindices","page":"PI–HEOM non-Markovian dynamics","text":"heom_multiindices(plan)\n\nReturn detached copies of all retained hierarchy multi-indices, ordered first by total depth and then by ascending lexicographic occupation order.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.heom_multiindices"},{"category":"function","location":"heom/#PermutationalInvariantDynamics.heom_ado_importances","page":"PI–HEOM non-Markovian dynamics","text":"heom_ado_importances(plan)\n\nReturn a detached vector of the dimensionless heuristic scores used by importance pruning. The root score is one. Scores are diagnostic estimates, not error bounds.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.heom_ado_importances"},{"category":"function","location":"heom/#PermutationalInvariantDynamics.heom_hierarchy_metadata","page":"PI–HEOM non-Markovian dynamics","text":"heom_hierarchy_metadata(plan)\n\nReturn immutable metadata describing full versus retained ADO counts, importance pruning, scaling, and the explicit residue terminator. The full_ados field is an exact BigInt.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.heom_hierarchy_metadata"},{"category":"function","location":"heom/#PermutationalInvariantDynamics.heom_coordinate_scale","page":"PI–HEOM non-Markovian dynamics","text":"heom_coordinate_scale(plan, label)\n\nReturn the positive scalar s_n relating the stored ADO coordinate to the conventional unscaled ADO, rho_n = s_n * rhohat_n. label may be the one-based ADO position or its occupation vector. The value is exactly one for an unscaled plan and for the root ADO.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.heom_coordinate_scale"},{"category":"function","location":"heom/#PermutationalInvariantDynamics.heom_initial_state","page":"PI–HEOM non-Markovian dynamics","text":"heom_initial_state(plan, rho)\n\nConstruct the standard factorized HEOM initial condition: the root ADO is rho and every auxiliary ADO is exactly zero.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.heom_initial_state"},{"category":"function","location":"heom/#PermutationalInvariantDynamics.heom_thermal_state","page":"PI–HEOM non-Markovian dynamics","text":"heom_thermal_state(plan, Hamiltonian, inverse_temperature;\n                   preparation=:factorized,\n                   relaxation_time=nothing, steps=256,\n                   parameters=nothing, return_info=false, kwargs...)\n\nPrepare a hierarchy from the bare-system Gibbs state. With preparation=:factorized, only the root ADO is populated. The explicit preparation=:relaxation route evolves that hierarchy for relaxation_time and therefore creates correlated auxiliary ADOs. preparation=:stationary uses the same Gibbs hierarchy only as the initial guess of heom_steady_state; kwargs are forwarded to that solver.\n\nThe latter two routes are real-time relaxation/stationary preparations, not imaginary-time HEOM. A stationary result represents thermal equilibrium only when the supplied system generator and physical bath decomposition satisfy the appropriate equilibrium assumptions. Known physical-bath temperature metadata is checked exactly, but the library cannot certify detailed balance of an arbitrary system Liouvillian.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.heom_thermal_state"},{"category":"function","location":"heom/#PermutationalInvariantDynamics.heom_ado","page":"PI–HEOM non-Markovian dynamics","text":"heom_ado(state, label)\n\nReturn a detached PIOperator holding the ADO selected by its one-based hierarchy position or occupation-vector label. Auxiliary ADOs are not density matrices and are therefore deliberately returned as operators.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.heom_ado"},{"category":"function","location":"heom/#PermutationalInvariantDynamics.heom_reduced_state","page":"PI–HEOM non-Markovian dynamics","text":"heom_reduced_state(state)\n\nReturn a detached PIState containing the physical reduced system density operator, i.e. the root ADO. No normalization or positivity repair is made.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.heom_reduced_state"},{"category":"function","location":"heom/#PermutationalInvariantDynamics.heom_evolve!","page":"PI–HEOM non-Markovian dynamics","text":"heom_evolve!(destination, plan, source, tspan;\n             steps=256, parameters=nothing, workspace=nothing,\n             pulses=nothing)\n\nPropagate a hierarchy with preallocated, three-scratch fixed-step RK4. destination may alias source, but it must not alias workspace scratch; one HEOMEvolutionWorkspace may be reused sequentially. Increase steps and plan.max_depth independently to check integration and hierarchy truncation errors. A HierarchyPulseSequence passed as pulses splits RK4 steps exactly at every event in (tspan[1], tspan[2]]; events at the final time are applied before return.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.heom_evolve!"},{"category":"function","location":"heom/#PermutationalInvariantDynamics.heom_evolve","page":"PI–HEOM non-Markovian dynamics","text":"Allocating convenience wrapper for heom_evolve!.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.heom_evolve"},{"category":"function","location":"heom/#PermutationalInvariantDynamics.heom_time_evolution","page":"PI–HEOM non-Markovian dynamics","text":"heom_time_evolution(plan, initial, times;\n                    steps_per_interval=64, parameters=nothing,\n                    pulses=nothing)\n\nReturn saved hierarchy states at ordered times, reusing one RK4 workspace. initial may be a PIState (factorized hierarchy) or an HEOMState. Pulse-time states follow the post-pulse convention of HierarchyPulseSequence.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.heom_time_evolution"},{"category":"function","location":"heom/#PermutationalInvariantDynamics.heom_problem","page":"PI–HEOM non-Markovian dynamics","text":"heom_problem(plan, initial, tspan; parameters=nothing)\n\nConstruct an in-place SciMLBase.ODEProblem for a prepared HEOM hierarchy. initial may be a compatible PIState, which creates the factorized root-only hierarchy, or a complete HEOMState. The problem owns one HEOMWorkspace, copies its initial coordinate vector, and supports a driven system Liouvillian through SciML's time and parameter arguments.\n\nThe state vectors use plan.scaling; use heom_reduced_state on an HEOMState(plan, solution.u[i]) to recover the physical reduced density operator. Construct a separate problem for every concurrent solve because its captured workspace is mutable. Both time-span endpoints must be exactly representable in the plan's real scalar type; the returned problem stores that precision rather than silently widening a fixed-precision plan.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.heom_problem"},{"category":"function","location":"heom/#PermutationalInvariantDynamics.heom_depth_convergence","page":"PI–HEOM non-Markovian dynamics","text":"heom_depth_convergence(system, baths, initial, tspan;\n                       depths, steps=256, parameters=nothing,\n                       scaling=:unscaled, scaling_factors=nothing,\n                       atol=0, rtol=nothing, consecutive=2,\n                       require_convergence=false)\nheom_depth_convergence(template, initial, tspan; depths, ...)\n\nPropagate the same factorized initial PI state at a strictly increasing set of hierarchy depths and compare successive final reduced states in the exact Hilbert–Schmidt norm of the PI coefficient basis. A pair converges when\n\nlVertrho_D-rho_DrVert_2leq\nmathrmatol+mathrmrtolmax(lVertrho_DrVert_2\n                                 lVertrho_DrVert_2)\n\nThe system is compiled once and all depths share the prepared system and coupling blocks; each evolution still owns dimension-matched scratch. An existing HEOMPlan may instead be passed as template, in which case every requested depth must not exceed template.max_depth.\n\nThe return value is the package-wide ConvergenceStudyResult. Intermediate raw results contain only a detached reduced state and plain diagnostics; only last(report.results).hierarchy retains a complete hierarchy. The shared convergence rule requires the final consecutive successive comparisons to pass (two by default). Set require_convergence=true to raise otherwise.\n\nThis report isolates hierarchy truncation only: every depth uses the same fixed RK4 steps, so time-step convergence must be checked separately. No state is normalized, clipped, or symmetrized for the comparison.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.heom_depth_convergence"},{"category":"function","location":"heom/#PermutationalInvariantDynamics.heom_liouvillian","page":"PI–HEOM non-Markovian dynamics","text":"heom_liouvillian(plan)\n\nReturn a synchronized MatrixFreeLiouvillian adapter for an HEOM plan. Its trace functional acts only on the root ADO, enabling use of the package's existing matrix-free Krylov routines without constructing the hierarchy matrix. The adapter publishes synchronized vector and matrix-RHS callbacks for both forward and adjoint application. In parallel hot loops, call apply! with one explicit HEOMWorkspace per task instead.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.heom_liouvillian"},{"category":"function","location":"heom/#PermutationalInvariantDynamics.heom_steady_state","page":"PI–HEOM non-Markovian dynamics","text":"heom_steady_state(plan; initial_state=nothing, return_info=false,\n                  preconditioner=nothing,\n                  preconditioner_regularization=0, kwargs...)\n\nCompute the trace-fixed stationary hierarchy with the existing restarted matrix-free GMRES solver. kwargs are forwarded to krylov_steady_state. The physical stationary density operator is heom_reduced_state(result). Set preconditioner=:block to construct and use one HEOMBlockPreconditioner, or pass a compatible reusable preconditioner directly.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.heom_steady_state"},{"category":"section","location":"krylov_extensions/#Block,-shifted,-recycled,-and-exponential-Krylov-methods","page":"Block, multi-shift, and recycled Krylov","text":"These advanced solvers reuse matrix-free applications of a prepared operator when a research calculation has more structure than one linear system. They accept an object implementing mul!(y, A, x). Their low-level mutating paths also accept either A(y, x) or an allocating A(x) callable. None of the methods materializes the PI Liouvillian.\n\nEvery solver returns convergence flags, residuals, iteration counts, and the number of vector operator applications. Nonconvergence raises by default. Use require_convergence=false only when the returned diagnostics will be checked explicitly.\n\nThe workspaces reuse the dominant full-coordinate bases and residual arrays; they do not promise a zero-allocation solve. Small projected QR/eigenvalue or matrix-exponential problems are formed as ordinary dense arrays. Size and reuse the workspace to bound the large memory term, and benchmark projected work separately when the block or Krylov dimension is itself large.","title":"Block, shifted, recycled, and exponential Krylov methods"},{"category":"section","location":"krylov_extensions/#Clustered-modes:-thick-restarted-block-Arnoldi","page":"Block, multi-shift, and recycled Krylov","text":"block_arnoldi_spectrum advances several search directions in one operator call. This is useful for degenerate or tightly clustered slow modes and for sized operators with a genuine matrix–matrix action. A bare callable must be wrapped in MatrixFreeLiouvillian so the solver has an explicit dimension:\n\nworkspace = BlockArnoldiWorkspace(ComplexF64, size(A, 1), 48, 4)\nresult = block_arnoldi_spectrum(A;\n    nev=6,\n    block_size=4,\n    krylovdim=48,\n    retained_dimension=12,\n    workspace,\n    vectors=true,\n)\n\nThe total search space, rather than the number of block steps, is bounded by krylovdim. Dependent directions are deflated, selected Ritz vectors are retained across a thick restart, and every reported mode is reapplied to the full operator before its residual is accepted. This is deliberately named a block/thick-restarted Arnoldi method; it is not exact-shift block IRAM. The default memory guard includes the reusable block arrays, projected dense eigensolve, predictable restart diagnostics, returned modes, source-action scratch, and the actual capacity of supplied workspaces. Set memory_budget=Inf only as an explicit opt-out.\n\nFor a matrix-free Floquet map, pair it with a fixed-capacity forward batch workspace so the four RK stages evaluate a drive once for the entire block:\n\nperiod_work = FloquetBatchWorkspace(period_map, 4; mode=:forward)\nresult = block_arnoldi_spectrum(period_map;\n    nev=4,\n    block_size=4,\n    operator_workspace=period_work,\n)\n\nselected_floquet_multipliers(period_map; method=:block_arnoldi, block_size=4) prepares this forward workspace automatically. Use mode=:full only when the same batch workspace must also apply the exact discrete adjoint. Its capacity is fixed and an oversized block raises instead of allocating hidden stage matrices.","title":"Clustered modes: thick-restarted block Arnoldi"},{"category":"section","location":"krylov_extensions/#Several-right-hand-sides:-block-GMRES","page":"Block, multi-shift, and recycled Krylov","text":"Block GMRES expands all residual directions together. This can reuse common spectral information when computing several response columns, parameter derivatives, or trace-fixed linear corrections:\n\nB = hcat(source_1, source_2, source_3)\nworkspace = BlockGMRESWorkspace(ComplexF64, length(source_1), 3, 12)\nX = zeros(ComplexF64, size(B))\n\nresult = block_gmres!(X, A, B, workspace;\n    maxiter=60,\n    atol=1e-11,\n    rtol=1e-9,\n)\n\nHere block_krylovdim=12 counts block Arnoldi steps, not scalar basis vectors. The largest stored basis has at most 3 * (12 + 1) columns. Dependent initial or generated directions are deflated deterministically. A fixed left preconditioner may be passed with preconditioner=P; the implementation checks both preconditioned and raw residuals for every column.\n\nUse the allocating wrapper for one-off work:\n\nresult = block_gmres(A, B; block_krylovdim=12)\nX = result.solution","title":"Several right-hand sides: block GMRES"},{"category":"section","location":"krylov_extensions/#Many-shifts-from-one-Arnoldi-factorization","page":"Block, multi-shift, and recycled Krylov","text":"Resolvents on a frequency grid involve systems\n\n(A-sigma_j I)x_j=b\n\nThe Krylov spaces of these shifted operators coincide when the initial guess is zero. multishift_gmres builds one Arnoldi factorization and solves every small shifted least-squares problem:\n\nshifts = im .* frequencies\nresult = multishift_gmres(A, source, shifts;\n    krylovdim=60,\n    atol=1e-11,\n    rtol=1e-9,\n)\nresponses = result.solutions\n\nThis implementation deliberately uses one unrestarted shared space. Generic preconditioners and nonzero initial guesses destroy shift invariance and are not accepted. If some shifts have not converged, increase krylovdim or solve those systems separately with an appropriate preconditioner. The diagnostic projected_residuals comes from the shared Arnoldi relation, while residuals is recomputed in the full space.\n\nOnly an exactly zero Arnoldi remainder is treated as a happy breakdown. A small representable remainder is retained because it can still be larger than the requested residual tolerance. Projected solves use rank-revealing QR; a singular or rank-deficient projected system therefore returns an honest full residual/nonconvergence result rather than manufacturing a solution.","title":"Many shifts from one Arnoldi factorization"},{"category":"section","location":"krylov_extensions/#Continuation-with-recycled-GMRES","page":"Block, multi-shift, and recycled Krylov","text":"RecycledGMRESWorkspace retains approximate invariant directions between solves. Before each new solve it applies the current operator to the retained space, so the operator may change along a parameter continuation:\n\nworkspace = RecycledGMRESWorkspace(A0, 40, 8)\n\nx = zeros(ComplexF64, size(A0, 1))\nfirst = recycled_gmres!(x, A0, b0, workspace)\n\n# A1 can be the matrix-free operator at the next parameter point.\nfill!(x, 0)\nsecond = recycled_gmres!(x, A1, b1, workspace)\n\nThe GCRO projection maintains a pair of spaces U and C satisfying M^-1 A U=C with orthonormal columns of C. Arnoldi then acts on the complement of C. Near-target Ritz directions from the final correction space replace U for the next call. The default target=0 is appropriate for trace-fixed steady-state and low-frequency response systems.\n\nRecycling is mutable algorithmic state. Use one workspace per continuation chain and never share it concurrently. The recycle_reset diagnostic is true if a changed operator made the retained image rank deficient and the solver safely discarded it.\n\nParameterScanPlan uses this mechanism when configured with RecycledGMRESAlgorithm; ordinary GMRESAlgorithm remains a state-only warm start. Use RecycledGMRESWorkspace directly when the sequence consists of custom linear systems.","title":"Continuation with recycled GMRES"},{"category":"section","location":"krylov_extensions/#Adaptive-exponential-action","page":"Block, multi-shift, and recycled Krylov","text":"krylov_expv computes exp(tA)b directly. It restarts in time and adapts the slice length from an augmented-Hessenberg defect estimate:\n\nworkspace = KrylovExpvWorkspace(A, 30)\nresult = krylov_expv(A, rho0, dt;\n    workspace=workspace,\n    atol=1e-11,\n    rtol=1e-9,\n)\nrho_dt = result.value\n\nThis is useful for autonomous propagation and exponential-integrator building blocks. It does not enforce trace, Hermiticity, or positivity after the action; these properties must follow from the supplied generator and the requested numerical tolerance. The returned estimated_error is the sum of accepted local defect estimates. reached_time makes a deliberately returned partial result unambiguous when require_convergence=false.\n\nWhen a proposed slice is rejected, its initial state has not changed. The implementation therefore retains that Arnoldi factorization and evaluates only the small projected exponential at the shorter trial step. The returned arnoldi_factorizations and trial_evaluations diagnostics expose this reuse; operator_applications counts only full-space actions.\n\nFor repeated calls, construct each workspace at the intended scalar type. Compiled matrix-free PI operators have fixed-precision scratch, so a wider workspace, time, initial/minimum step, or step-safety control must be rejected rather than silently narrowed. Accepted slices always advance by a representable amount in that precision. Shifts, recycling targets, preconditioners, and right-hand sides obey the same non-narrowing rule.","title":"Adaptive exponential action"},{"category":"section","location":"krylov_extensions/#API","page":"Block, multi-shift, and recycled Krylov","text":"","title":"API"},{"category":"type","location":"krylov_extensions/#PermutationalInvariantDynamics.BlockArnoldiWorkspace","page":"Block, multi-shift, and recycled Krylov","text":"BlockArnoldiWorkspace(T, n, krylovdim, block_size)\nBlockArnoldiWorkspace(A, krylovdim, block_size)\n\nReusable full-coordinate storage for block_arnoldi_spectrum. krylovdim is the maximum total search-space dimension, while block_size is the maximum number of directions advanced in one matrix–matrix operator application. The workspace is mutable and must be owned by one task at a time.\n\nThe implementation stores both the search basis and its operator image so a Rayleigh–Ritz extraction and thick restart do not rebuild already available actions. Candidate Ritz vectors are nevertheless reapplied in batches before their reported residuals are accepted; diagnostics are therefore full-space residuals rather than projected estimates.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.BlockArnoldiWorkspace"},{"category":"function","location":"krylov_extensions/#PermutationalInvariantDynamics.block_arnoldi_spectrum","page":"Block, multi-shift, and recycled Krylov","text":"block_arnoldi_spectrum(A; nev=6, block_size=min(nev, 4),\n    krylovdim=nothing, retained_dimension=nothing, maxrestarts=20,\n    which=:LR, target=nothing, initial_subspace=nothing,\n    operator_workspace=nothing, workspace=nothing,\n    memory_budget=512*1024^2, ...)\n\nCompute selected right eigenpairs with a thick-restarted block Arnoldi search. Several directions are advanced together, allowing a prepared matrix–matrix operator kernel (notably a FloquetBatchWorkspace) to reuse schedules and BLAS-3 Schur contractions. Dependent initial and generated directions are deflated by two-pass modified Gram–Schmidt.\n\nA must be a sized square operator. A bare mutating callable does not carry the dimension required to allocate the block basis; wrap it in a MatrixFreeLiouvillian (and provide a batched callback when available).\n\nThis routine is a block/thick-restarted Rayleigh–Ritz method. It is not an implicit-QR block IRAM implementation. Existing implicitly_restarted_arnoldi_spectrum remains the exact-shift scalar IRAM route.\n\nkrylovdim bounds the total number of retained full-coordinate basis vectors; retained_dimension controls the thick restart. At every extraction the selected Ritz vectors are reapplied to A in batches, and residuals stores norm(A*v-lambda*v) from those fresh full-space actions. Nonconvergence raises unless require_convergence=false.\n\noperator_workspace is optional caller-owned scratch for an operator with an explicit batched apply! method. It is reused but never retained by the BlockArnoldiWorkspace; both objects must be task-local.\n\nThe full block basis, projected eigensolve, predictable diagnostics, requested output vectors, source-action scratch, and supplied operator workspace are checked against memory_budget before an internal BlockArnoldiWorkspace is allocated. memory_budget=Inf is the explicit opt-out.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.block_arnoldi_spectrum"},{"category":"type","location":"krylov_extensions/#PermutationalInvariantDynamics.BlockGMRESWorkspace","page":"Block, multi-shift, and recycled Krylov","text":"BlockGMRESWorkspace(T, n, nrhs, block_krylovdim)\nBlockGMRESWorkspace(A, nrhs, block_krylovdim)\n\nReusable storage for block_gmres!. block_krylovdim counts block Arnoldi steps, so the dominant basis contains at most nrhs * (block_krylovdim + 1) vectors. A workspace is mutable and must be owned by one task at a time.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.BlockGMRESWorkspace"},{"category":"function","location":"krylov_extensions/#PermutationalInvariantDynamics.block_gmres!","page":"Block, multi-shift, and recycled Krylov","text":"block_gmres!(X, A, B, workspace; ...)\n\nSolve A*X = B with restarted block GMRES using only matrix-free vector applications. Linearly dependent residual columns are deflated rather than duplicated in the block basis. A fixed left preconditioner may be supplied through ldiv!; convergence is always checked against both the projected and the original residual of every right-hand side.\n\nThe returned named tuple contains solution, per-column residuals, projected_residuals, converged, block iterations, restarts, and the number of vector operator_applications. Failure raises unless require_convergence=false is explicitly requested.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.block_gmres!"},{"category":"function","location":"krylov_extensions/#PermutationalInvariantDynamics.block_gmres","page":"Block, multi-shift, and recycled Krylov","text":"block_gmres(A, B; initial_guess=nothing, workspace=nothing, ...)\n\nAllocating wrapper for block_gmres!. It returns the same diagnostic named tuple, with the solution available as result.solution.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.block_gmres"},{"category":"type","location":"krylov_extensions/#PermutationalInvariantDynamics.MultiShiftGMRESWorkspace","page":"Block, multi-shift, and recycled Krylov","text":"MultiShiftGMRESWorkspace(T, n, nshifts, krylovdim)\nMultiShiftGMRESWorkspace(A, nshifts, krylovdim)\n\nReusable Arnoldi and residual storage for multishift_gmres!.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.MultiShiftGMRESWorkspace"},{"category":"function","location":"krylov_extensions/#PermutationalInvariantDynamics.multishift_gmres!","page":"Block, multi-shift, and recycled Krylov","text":"multishift_gmres!(X, A, b, shifts, workspace; ...)\n\nSolve all systems (A - shifts[j]*I) * X[:,j] = b from one unrestarted Arnoldi factorization. Each shifted projected problem is the GMRES minimum-residual least-squares problem in the shared Krylov space.\n\nShift invariance requires a zero common initial guess and no generic left or right preconditioner; nonzero X is therefore rejected. Increase krylovdim when a requested shifted system is not converged. The returned tuple reports actual full-space residuals and vector-application counts, and failure raises unless require_convergence=false.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.multishift_gmres!"},{"category":"function","location":"krylov_extensions/#PermutationalInvariantDynamics.multishift_gmres","page":"Block, multi-shift, and recycled Krylov","text":"multishift_gmres(A, b, shifts; workspace=nothing, krylovdim=30, ...)\n\nAllocating wrapper for multishift_gmres!.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.multishift_gmres"},{"category":"type","location":"krylov_extensions/#PermutationalInvariantDynamics.RecycledGMRESWorkspace","page":"Block, multi-shift, and recycled Krylov","text":"RecycledGMRESWorkspace(T, n, krylovdim, recycle_dim)\nRecycledGMRESWorkspace(A, krylovdim, recycle_dim)\n\nReusable GCRO-style projected-GMRES storage. The workspace retains up to recycle_dim near-target Ritz directions after a solve and reuses them in subsequent calls to recycled_gmres!. It must not be shared between tasks.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.RecycledGMRESWorkspace"},{"category":"function","location":"krylov_extensions/#PermutationalInvariantDynamics.recycled_gmres!","page":"Block, multi-shift, and recycled Krylov","text":"recycled_gmres!(x, A, b, workspace; ...)\n\nSolve A*x=b with a GCRO-style projected restarted GMRES method. On later calls, the workspace rebuilds C=A*U for the current operator, projects the residual onto that image, and solves in the complementary Krylov space. At the end it retains near-target Ritz directions constructed from the GCRO correction basis. This makes the workspace suitable for continuation through a sequence of slowly varying operators.\n\nA fixed left preconditioner is supported. Full unpreconditioned residuals are always validated. The returned named tuple records whether recycling was used, the retained dimension, residuals, restarts, iterations, and operator applications. Failure raises unless require_convergence=false.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.recycled_gmres!"},{"category":"function","location":"krylov_extensions/#PermutationalInvariantDynamics.recycled_gmres","page":"Block, multi-shift, and recycled Krylov","text":"recycled_gmres(A, b; initial_guess=nothing, workspace=nothing,\n               krylovdim=30, recycle_dim=8, ...)\n\nAllocating wrapper for recycled_gmres!. Reuse the returned call's workspace explicitly on later systems to obtain recycling; the result itself contains only the solution and diagnostics.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.recycled_gmres"},{"category":"type","location":"krylov_extensions/#PermutationalInvariantDynamics.KrylovExpvWorkspace","page":"Block, multi-shift, and recycled Krylov","text":"KrylovExpvWorkspace(T, n, krylovdim)\nKrylovExpvWorkspace(A, krylovdim)\n\nReusable Arnoldi and vector storage for krylov_expv!.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.KrylovExpvWorkspace"},{"category":"function","location":"krylov_extensions/#PermutationalInvariantDynamics.krylov_expv!","page":"Block, multi-shift, and recycled Krylov","text":"krylov_expv!(y, A, b, t, workspace; ...)\n\nCompute y = exp(t*A)*b without materializing A or its exponential. Restarted Arnoldi time slices are accepted or rejected using the augmented Hessenberg defect estimate. initial_step, minimum_step, and maximum_step are expressed in the same units as the finite real time t. Rejected slices reuse their Arnoldi factorization because the current full-space state has not changed; only the projected matrix exponential is reevaluated at the shorter step.\n\nThe result reports accepted/rejected steps, total matrix-free operator applications, Arnoldi factorizations, projected trial evaluations, the accumulated local error estimate, and reached_time. Exhausting max_steps or the minimum step raises unless require_convergence=false, in which case converged=false and reached_time make the partial result explicit.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.krylov_expv!"},{"category":"function","location":"krylov_extensions/#PermutationalInvariantDynamics.krylov_expv","page":"Block, multi-shift, and recycled Krylov","text":"krylov_expv(A, b, t; workspace=nothing, krylovdim=30, ...)\n\nAllocating wrapper for krylov_expv!.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.krylov_expv"},{"category":"section","location":"api/analysis/#Observables-and-quantum-information","page":"Observables and quantum information","text":"","title":"Observables and quantum information"},{"category":"section","location":"api/analysis/#Observables,-correlations,-squeezing,-and-metrology","page":"Observables and quantum information","text":"","title":"Observables, correlations, squeezing, and metrology"},{"category":"section","location":"api/analysis/#Spin-phase-space","page":"Observables and quantum information","text":"","title":"Spin phase space"},{"category":"section","location":"api/analysis/#Entropies-and-distances","page":"Observables and quantum information","text":"","title":"Entropies and distances"},{"category":"section","location":"api/analysis/#Nonstabilizerness","page":"Observables and quantum information","text":"The paper definition, pure symmetric-state scope, prepared workflow, and scaling are explained in Nonstabilizerness of symmetric qubit states.","title":"Nonstabilizerness"},{"category":"section","location":"api/analysis/#Symmetry-resolved-information","page":"Observables and quantum information","text":"","title":"Symmetry-resolved information"},{"category":"section","location":"api/analysis/#Generator-diagnostics","page":"Observables and quantum information","text":"","title":"Generator diagnostics"},{"category":"section","location":"api/analysis/#Reduced-states-and-entanglement","page":"Observables and quantum information","text":"The solver-backed PI PPT-mixture criterion for genuine multipartite entanglement, including its one-sided interpretation for four or more qubits, is documented separately in Genuine multipartite entanglement.","title":"Reduced states and entanglement"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.expectation","page":"Observables and quantum information","text":"expectation(rho, A)\n\nReturn the Hilbert–Schmidt expectation tr(A' * rho) of a PIOperator on the exact same PIBasis. For a Hermitian observable this is its ordinary quantum expectation value.\n\n\n\n\n\nexpectation(rho, A)\n\nReturn the Hilbert–Schmidt expectation tr(A' * rho).  Since both the PI equation-(7) basis and finite matrix units are orthonormal, this is the direct coordinate dot product.  For Hermitian A it is the usual expectation value.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.expectation"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.variance","page":"Observables and quantum information","text":"Return real(expectation(rho, A*A) - expectation(rho, A)^2).\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.variance"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.covariance","page":"Observables and quantum information","text":"Return the symmetrized covariance of two PI observables.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.covariance"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.collective_expectation","page":"Observables and quantum information","text":"collective_expectation(rho, X)\n\nEfficiently return the expectation value of sum_n X^(n) directly from its physical Schur blocks.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.collective_expectation"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.collective_variance","page":"Observables and quantum information","text":"collective_variance(rho, X; atol=_analysis_atol(rho))\n\nEfficiently return the variance of the Hermitian collective observable sum_n X^(n). Tiny imaginary roundoff up to atol is discarded; a larger imaginary component raises an error.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.collective_variance"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.collective_moments","page":"Observables and quantum information","text":"collective_moments(rho, X)\n\nReturn (mean, second_moment) for the collective observable Xc = sum(n=1:N) X^(n), without assembling Xc as a flattened PIOperator. The equation-(31) physical blocks are constructed once and contracted as\n\nsum_nu f^nu * tr(rho_nu * Xc_nu) and sum_nu f^nu * tr(rho_nu * Xc_nu^2).\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.collective_moments"},{"category":"type","location":"api/analysis/#PermutationalInvariantDynamics.CollectiveObservablePlan","page":"Observables and quantum information","text":"CollectiveObservablePlan(basis, X; cache=nothing)\n\nPrepare the physical Schur blocks of the collective observable sum_n X^(n). Treat the prepared blocks as read-only and reuse the plan for many states on the exact same PIBasis, avoiding both representation-geometry construction and block assembly on every expectation, variance, covariance, or QFI call.\n\nFor a sole fully symmetric sector, preparation uses the lightweight occupation-number lift. When preparing several observables on general Schur sectors, construct one OneBodyGeometry and pass it through cache. The plan retains only one matrix per Schur sector, not the larger geometry. A one-off contraction can instead receive the geometry directly through cache= without retaining prepared observable blocks.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.CollectiveObservablePlan"},{"category":"type","location":"api/analysis/#PermutationalInvariantDynamics.OneBodyRDMWorkspace","page":"Observables and quantum information","text":"OneBodyRDMWorkspace(geometry; T=geometry_scalar_type(geometry),\n                    memory_budget=512*1024^2)\nOneBodyRDMWorkspace(geometry, rho)\n\nAllocate task-owned scratch for repeated one-particle reductions through a prepared OneBodyGeometry. The workspace owns one largest-sector multiplicity-weighted block and exact prepared Schur-multiplicity scales. It is tied to the exact geometry and must not be shared by concurrent tasks.\n\nThe keyword T is the real floating-point component type. The state constructor promotes the state and geometry scalar types. For BigFloat, the geometry and state must use one identical stored precision; the workspace captures that context and re-enters it during allocation and application. memory_budget guards the retained largest-sector scratch and prepared exact scales; pass Inf only as an explicit opt-out.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.OneBodyRDMWorkspace"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.one_body_rdm","page":"Observables and quantum information","text":"one_body_rdm(rho; cache=nothing, plan=nothing, workspace=nothing,\n             check=true,\n             atol=_analysis_atol(rho), rtol=_state_rtol(rho))\n\nReturn the one-particle density matrix in computational-label order. Passing a reusable OneBodyGeometry contracts all matrix units in one traversal. A matching OneBodyRDMWorkspace reuses the multiplicity-weighted sector scratch. Alternatively, a ReductionPlan(basis,1) and optional ReductionWorkspace reuse fixed-bipartition recoupling data. Geometry and reduction resources cannot be mixed.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.one_body_rdm"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.one_body_rdm!","page":"Observables and quantum information","text":"one_body_rdm!(destination, rho, workspace;\n              check=true,\n              atol=_analysis_atol(rho), rtol=_state_rtol(rho))\n\nWrite the one-particle density matrix in computational-label order into destination, reusing a task-owned OneBodyRDMWorkspace. With check=true the input state is validated first. Set check=false only when an enclosing prepared workflow has already performed equivalent validation. No normalization, symmetrization, or positivity repair is performed.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.one_body_rdm!"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.trace_error","page":"Observables and quantum information","text":"Return abs(trace(rho) - 1).\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.trace_error"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.collective_covariance","page":"Observables and quantum information","text":"Symmetrized covariance of two collective one-particle observables.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.collective_covariance"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.collective_covariance_matrix","page":"Observables and quantum information","text":"collective_covariance_matrix(rho, generators; cache=nothing, plans=nothing)\n\nReturn the real symmetric covariance matrix of Hermitian collective one-particle generators. Supply prepared CollectiveObservablePlan objects, or reuse shared one-body geometry through cache, to avoid rebuilding Schur blocks across repeated calls.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.collective_covariance_matrix"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.kitagawa_ueda_squeezing","page":"Observables and quantum information","text":"Kitagawa–Ueda qubit squeezing parameter 4*min(Delta J_perp^2)/N.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.kitagawa_ueda_squeezing"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.wineland_squeezing","page":"Observables and quantum information","text":"Wineland qubit squeezing parameter N*min(Delta J_perp^2)/|<J>|^2.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.wineland_squeezing"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.two_body_rdm","page":"Observables and quantum information","text":"Return the identical two-particle reduced state.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.two_body_rdm"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.two_body_expectation","page":"Observables and quantum information","text":"Expectation of X ⊗ Y on an ordered pair of distinct particles.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.two_body_expectation"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.connected_two_body_correlation","page":"Observables and quantum information","text":"connected_two_body_correlation(rho, X, Y; cache=nothing)\n\nReturn the connected correlation of X and Y on two distinct particles, expectation(X tensor Y) - expectation(X) * expectation(Y), using identical one-particle marginals implied by permutation invariance.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.connected_two_body_correlation"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.normalized_second_order_correlation","page":"Observables and quantum information","text":"Normalized identical-particle g^(2) for a local lowering operator L.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.normalized_second_order_correlation"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.qfi_entanglement_depth","page":"Observables and quantum information","text":"qfientanglementdepth(rho, X)\n\nLower bound on entanglement depth from collective-generator QFI. The standard k-producible bound s*k^2+r^2, multiplied by the squared spectral range of the local generator, is used.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.qfi_entanglement_depth"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.spin_squeezing_entangled","page":"Observables and quantum information","text":"Whether the Wineland parameter certifies particle entanglement.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.spin_squeezing_entangled"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.quantum_fisher_information","page":"Observables and quantum information","text":"quantum_fisher_information(rho, generator; atol=_analysis_atol(rho))\n\nCompute the quantum Fisher information for the unitary family exp(-im*theta*generator) * rho * exp(im*theta*generator).\n\ngenerator may be a PI operator or a local d×d matrix. A local matrix is interpreted as the collective observable sum_n generator^(n).  The implementation diagonalizes the multiplicity-weighted blocks rho_bar_nu=f^nu*rho_nu, whose eigenvalues q_a=f^nu*p_a remain on the sector-probability scale, and evaluates\n\nF_QrhoG = 2sum_nuabq_a+q_b0\nfrac(q_a-q_b)^2q_a+q_bG_ab^2\n\nThis is algebraically identical to the physical-eigenvalue formula, without forming an exponentially small p_a or converting f^nu to a machine float. No full-Hilbert-space matrix is constructed.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.quantum_fisher_information"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.qfi","page":"Observables and quantum information","text":"Alias for quantum_fisher_information.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.qfi"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.quantum_fisher_information_matrix","page":"Observables and quantum information","text":"quantum_fisher_information_matrix(rho, generators;\n                                  atol=_analysis_atol(rho))\n\nCompute the symmetric quantum Fisher information matrix (QFIM) for the multiparameter unitary model generated by generators:\n\nF_Q_munu=2sum_abp_a+p_b0\nfrac(p_a-p_b)^2p_a+p_b\nmathrmRe(G_mu)_ab(G_nu)_ba\n\nEach generator may be a local d×d matrix, interpreted as its collective sum, or a PIOperator. Multiplicity-weighted density blocks are diagonalized only once per sector, and local collective blocks share one representation-geometry cache.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.quantum_fisher_information_matrix"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.qfim","page":"Observables and quantum information","text":"Alias for quantum_fisher_information_matrix.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.qfim"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.symmetric_logarithmic_derivatives","page":"Observables and quantum information","text":"Return SLD PIOperators for unitary parameters generated by generators.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.symmetric_logarithmic_derivatives"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.sld_commutator_matrix","page":"Observables and quantum information","text":"Matrix (1/2i) tr(rho [L_mu,L_nu]) diagnosing SLD incompatibility.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.sld_commutator_matrix"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.mean_uhlmann_curvature","page":"Observables and quantum information","text":"Mean Uhlmann-curvature convention U = sld_commutator_matrix/2.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.mean_uhlmann_curvature"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.multiparameter_compatible","page":"Observables and quantum information","text":"Whether the weak SLD commutativity condition holds within tolerance.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.multiparameter_compatible"},{"category":"type","location":"api/analysis/#PermutationalInvariantDynamics.SpinPhaseSpaceData","page":"Observables and quantum information","text":"SpinPhaseSpaceData\n\nNumerical, sector-aware spin phase-space data for a qubit PIState.  The sample matrix values is indexed as values[phi_index, theta_index] and is the aggregate obtained by summing the selected Schur-sector distributions. sector_values is either nothing or a vector of matrices in the same order as sectors, depending on the resolved keyword used during construction.\n\nEvery sector distribution is normalized as a density with respect to the sphere measure sin(theta) dtheta dphi: its integral is the corresponding entry of populations.  Thus the aggregate integrates to sum(populations), which is one only when all sectors of a normalized state are selected.  Combining different total-spin sectors is explicitly a marginal over the discrete sector label; the sector-resolved spheres remain the primary representation.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.SpinPhaseSpaceData"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.spin_husimi_q","page":"Observables and quantum information","text":"spin_husimi_q(rho, theta, phi; sectors=:all, resolved=false,\n              normalization=:sphere_density, atol=..., rtol=...)\nspin_husimi_q(rho; ntheta=91, nphi=180, kwargs...)\n\nCompute the spin Husimi-Q distribution of a qubit PI state without forming a 2^N state.  For Schur sector nu with spin j, dimension n_j=2j+1, multiplicity f^nu, physical block rho_j, and coherent state |theta,phi;j>, the returned sector density is\n\nP_Q(jthetaphi) = n_j(4pi) *     thetaphij f^nu rho_j thetaphij.\n\nIt integrates over the sphere to that sector's physical population.  The aggregate values is the sum over the selected sector spheres and is therefore explicitly a marginal over j.  Use resolved=true to retain the individual matrices in the returned SpinPhaseSpaceData; the default streams them into the aggregate to bound retained memory.  theta must lie in [0,pi]; arbitrary finite azimuths are accepted and are not wrapped.\n\nThe state is validated but never normalized, clipped, or otherwise repaired. Only normalization=:sphere_density is currently defined.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.spin_husimi_q"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.spin_wigner","page":"Observables and quantum information","text":"spin_wigner(rho, theta, phi; sectors=:all, resolved=false,\n             normalization=:sphere_density, atol=..., rtol=...)\nspin_wigner(rho; ntheta=91, nphi=180, kwargs...)\n\nCompute the sector-resolved Agarwal spin-Wigner distribution of a qubit PI state.  In spin-j sector the implementation uses orthonormal Condon–Shortley polarization tensors T_kq, coefficients t_kq=tr(T_kq' * f^nu rho_j), and spherical harmonics Y_kq.  The returned sector density is\n\nP_W(jthetaphi) = sqrt((2j+1)(4pi)) *     sum(kq t_kq * Y_kq(thetaphi)).\n\nIts sphere integral is the sector population, and a maximally mixed conditional spin block contributes the uniform density p_j/(4pi). Wigner negativity is retained exactly; values are never clipped. The aggregate is the explicit marginal over the selected Schur sectors rather than a claim that a multi-sector state belongs to one spin irrep.\n\nresolved=true retains every selected sector matrix.  Tensor coefficients are generated from diagonal recurrences with call-local workspace; no dense 2^N object or global mutable cache is constructed.  The state and angle validation rules are the same as for spin_husimi_q.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.spin_wigner"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.von_neumann_entropy","page":"Observables and quantum information","text":"Return the von Neumann entropy of a PI state, with logarithm base base.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.von_neumann_entropy"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.renyi_entropy","page":"Observables and quantum information","text":"Return the order-alpha Rényi entropy of a PI state.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.renyi_entropy"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.reduced_entropy","page":"Observables and quantum information","text":"Entropy of the k-particle reduced state.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.reduced_entropy"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.mutual_information","page":"Observables and quantum information","text":"Mutual information across the k | N-k bipartition.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.mutual_information"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.conditional_entropy","page":"Observables and quantum information","text":"Conditional entropy S(A|B) for A containing k particles.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.conditional_entropy"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.trace_distance","page":"Observables and quantum information","text":"Trace distance between compatible PI states.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.trace_distance"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.fidelity","page":"Observables and quantum information","text":"fidelity(rho, sigma; atol, rtol)\n\nSquared Uhlmann fidelity between compatible PI states. Input and intermediate PSD spectra are validated before tolerance-sized negative roundoff is replaced by zero solely for matrix square roots. A final value outside [0,1] is corrected only within a scale-aware roundoff bound; a larger discrepancy throws.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.fidelity"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.bures_distance","page":"Observables and quantum information","text":"Bures distance corresponding to the squared-fidelity convention.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.bures_distance"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.quantum_relative_entropy","page":"Observables and quantum information","text":"quantum_relative_entropy(rho, sigma; base=2, atol, rtol)\n\nQuantum relative entropy D(rho || sigma) in the requested logarithm base. Support inclusion is decided once per Schur sector with the projector onto sigma's numerical nullspace. Positive rho weight above the scale-aware roundoff floor returns Inf; it is never discarded through separate eigenvalue or eigenvector-overlap cutoffs.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.quantum_relative_entropy"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.hilbert_schmidt_distance","page":"Observables and quantum information","text":"Hilbert–Schmidt distance between compatible PI states.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.hilbert_schmidt_distance"},{"category":"type","location":"api/analysis/#PermutationalInvariantDynamics.StabilizerRenyiPlan","page":"Observables and quantum information","text":"StabilizerRenyiPlan(basis; T=Float64, memory_budget=512*1024^2)\n\nPrepare the permutation-symmetric Pauli transform used by stabilizer_renyi_entropy.\n\nFor a pure symmetric N-qubit state the second stabilizer Rényi entropy is\n\nM_2=-logleft\n2^-Nsum_PinIXYZ^otimes N\nleft(mathrmtrrho Pright)^4\nright\n\nPermutation symmetry groups the 4^N strings into only binomN+33 representatives.  This plan stores bounded normalized Krawtchouk transforms, hypergeometric modes, and logarithmic binomial data. It is tied to the exact PIBasis, is read-only after construction, and can be shared across tasks.  The corresponding mutable scratch is owned by StabilizerRenyiWorkspace.\n\nThe prepared contraction takes O(N^4) arithmetic, the plan retains O(N^3) scalars, and a workspace retains O(N^2) scalars.  Neither setup nor evaluation constructs a 2^N state vector, a 4^N Pauli list, or the paper's collection of representative Pauli matrices.\n\nThe paper defines this magic measure for pure symmetric qubit states.  The plan therefore requires a qubit basis containing the fully symmetric sector; evaluation performs the remaining state and support checks.  memory_budget guards the conservative setup peak, including temporary exact binomial data; pass Inf only after checking available memory.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.StabilizerRenyiPlan"},{"category":"type","location":"api/analysis/#PermutationalInvariantDynamics.StabilizerRenyiWorkspace","page":"Observables and quantum information","text":"StabilizerRenyiWorkspace(plan; memory_budget=512*1024^2)\n\nAllocate the task-owned O(N^2) scratch for a StabilizerRenyiPlan.  A workspace contains the hypergeometric table, two complex Krawtchouk copies, and three transform buffers.  Reuse it sequentially for state scans, but never from concurrent tasks.  Concurrent use is detected and raises rather than corrupting the result.\n\nmemory_budget guards this allocation.  The plan records both its retained size estimate and this workspace estimate for reproducible memory planning.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.StabilizerRenyiWorkspace"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.stabilizer_renyi_entropy","page":"Observables and quantum information","text":"stabilizer_renyi_entropy(rho; base=\\mathit{e}, plan=nothing,\n                         workspace=nothing,\n                         atol=_analysis_atol(rho),\n                         rtol=_state_rtol(rho),\n                         memory_budget=512*1024^2)\nstabilizer_renyi_entropy(rho, plan, workspace; base=\\mathit{e}, ...)\n\nCompute the second stabilizer Rényi entropy M_2 (nonstabilizerness or \"magic\") of a pure permutation-symmetric qubit state using the method of Passarelli, Fazio, and Lucignano, Phys. Rev. A 110, 022436 (2024). The default natural logarithm matches the paper; base=2 reports bits.\n\nThe algorithm sums one representative for each Pauli multiplicity quadruple with its exact multinomial degeneracy.  A bounded hypergeometric–Krawtchouk transform improves the paper's stored-matrix contraction to O(N^4) time, with an O(N^3) reusable plan and O(N^2) task-owned workspace.  The final fourth moment is reduced by log-sum-exp, so neither 2^N nor a large multinomial coefficient is converted separately.\n\nThis function follows the paper's pure-state definition strictly.  It validates the density operator, verifies unit purity, and requires exact zero support outside the fully symmetric Schur sector.  Mixed states and general multi-sector PI states are rejected: applying the same fourth-moment formula to a mixed state is not the cited magic monotone (the maximally mixed state is a simple counterexample).  The input is never normalized, projected, symmetrized, or otherwise repaired.\n\nFor one call, omit plan and workspace.  For a scan, construct one StabilizerRenyiPlan(rho.basis; T=...) and one workspace per concurrent task. Both are tied to their exact owner.  A wider plan may analyze a narrower state, but a narrower plan is rejected.  memory_budget guards only storage allocated by the convenience route; a fully supplied plan and workspace have already paid that cost.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.stabilizer_renyi_entropy"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.sector_resolved_entropy","page":"Observables and quantum information","text":"Schur-sector probabilities and conditional entropy contributions.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.sector_resolved_entropy"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.entropy_decomposition","page":"Observables and quantum information","text":"Decompose entropy into sector-label, conditional-irrep, and multiplicity parts.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.entropy_decomposition"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.sector_resolved_coherence","page":"Observables and quantum information","text":"Relative-entropy coherence in the stored GT basis, resolved by Schur sector.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.sector_resolved_coherence"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.relative_entropy_of_coherence","page":"Observables and quantum information","text":"Total relative-entropy coherence in the stored Gelfand–Tsetlin basis.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.relative_entropy_of_coherence"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.symmetry_twirl","page":"Observables and quantum information","text":"Pinch a PI state in eigenspaces of a local, prepared collective, or PI symmetry generator.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.symmetry_twirl"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.relative_entropy_of_asymmetry","page":"Observables and quantum information","text":"Relative entropy of asymmetry, equal to S(twirl(rho))-S(rho).\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.relative_entropy_of_asymmetry"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.relative_entropy_of_symmetry","page":"Observables and quantum information","text":"Alias for relative_entropy_of_asymmetry.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.relative_entropy_of_symmetry"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.wigner_yanase_asymmetry","page":"Observables and quantum information","text":"Wigner–Yanase skew information for a local, prepared collective, or PI generator.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.wigner_yanase_asymmetry"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.sector_resolved_qfi","page":"Observables and quantum information","text":"QFI contributions of normalized conditional sectors for a local, prepared collective, or PI generator.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.sector_resolved_qfi"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.relative_entropy_decomposition","page":"Observables and quantum information","text":"relative_entropy_decomposition(rho, sigma; base=2, atol, rtol)\n\nClassical sector-label and conditional quantum parts of D(rho||sigma). Conditional support is tested with a sector-level projector onto sigma's numerical nullspace; positive rho weight above the documented roundoff floor returns Inf rather than being discarded overlap by overlap.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.relative_entropy_decomposition"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.qfim_sector_decomposition","page":"Observables and quantum information","text":"Split tangent-state QFIM into classical sector-population and intra-sector parts.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.qfim_sector_decomposition"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.hermiticity_error","page":"Observables and quantum information","text":"Return the largest Frobenius Hermiticity residual among coefficient blocks.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.hermiticity_error"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.minimum_sector_eigenvalue","page":"Observables and quantum information","text":"Return the smallest eigenvalue among all physical Schur blocks.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.minimum_sector_eigenvalue"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.check_generator","page":"Observables and quantum information","text":"check_generator(model)\n\nReturn structural diagnostics for a PIModel: sparse trace-preservation residual when available, static-Hamiltonian Hermiticity, the presence of negative constant rates, and sector connectivity. For a driven or otherwise non-materializable model the trace-preservation entry is missing.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.check_generator"},{"category":"type","location":"api/analysis/#PermutationalInvariantDynamics.LocalFactorTracePlan","page":"Observables and quantum information","text":"LocalFactorTracePlan(source_basis, local_dimensions;\n                     traced_factor=2, T=Float64,\n                     memory_budget=512*1024^2,\n                     atol=nothing, rtol=nothing)\nLocalFactorTracePlan(rho, local_dimensions; kwargs...)\n\nPrepare the exact PI map which traces the same internal tensor factor from every identical supersite. local_dimensions=(d1,d2) follows Julia's kron(factor1,factor2) ordering and must satisfy d1*d2 == source_basis.d. traced_factor is 1 or 2.\n\nThe output basis is always the complete PIBasis(source_basis.N,dkeep). Local tracing can populate several output Young sectors even when the source basis is restricted to one sector.\n\nThe plan stores two read-only exact-support sparse rectangular transforms. Reusing it reduces each state with two sparse matrix-vector products. Setup constructs one occupation column at a time and discards only values satisfying iszero; it never applies a numerical dropping tolerance. Setup and application never construct a source_basis.d^N state, a full-system density matrix, or a list of local operator strings.\n\nmemory_budget guards both retained storage and conservative setup temporaries; pass Inf only as an explicit opt-out. The rho constructor infers T from the state's real floating-point component type.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.LocalFactorTracePlan"},{"category":"type","location":"api/analysis/#PermutationalInvariantDynamics.LocalFactorTraceWorkspace","page":"Observables and quantum information","text":"LocalFactorTraceWorkspace(plan)\n\nAllocate the one output-occupation vector needed by repeated applications of a LocalFactorTracePlan. A workspace belongs to one exact plan and must be owned by one task at a time.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.LocalFactorTraceWorkspace"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.local_factor_trace","page":"Observables and quantum information","text":"local_factor_trace(source, plan; workspace=nothing, kwargs...)\nlocal_factor_trace(source, local_dimensions;\n                   traced_factor=2, memory_budget=512*1024^2,\n                   setup_atol=nothing, setup_rtol=nothing, kwargs...)\n\nReturn the PI state obtained by tracing one internal tensor factor from every identical supersite. Pass a prepared plan and workspace for parameter scans.\n\nThis operation is different from reduced_state: local-factor tracing keeps all N particles and reduces each particle's internal Hilbert space, whereas reduced_state keeps only a selected number of particles.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.local_factor_trace"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.local_factor_trace!","page":"Observables and quantum information","text":"local_factor_trace!(output, source, plan, workspace;\n                    check=true,\n                    atol=_analysis_atol(source),\n                    rtol=_state_rtol(source))\n\nTrace the selected local factor from every supersite of source, writing the result into output. The plan and task-owned workspace are reused without hidden state-sized allocation. output must use plan.output_basis.\n\nWith check=true, both input and output are validated at the requested tolerance. Set check=false only when validation is already provided by the surrounding prepared workflow and the allocation-free contraction itself is required. The routine never normalizes, symmetrizes, clips eigenvalues, or otherwise repairs either state.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.local_factor_trace!"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.pseudomode_trace_plan","page":"Observables and quantum information","text":"pseudomode_trace_plan(site;\n                      T=real(eltype(site)),\n                      memory_budget=512*1024^2, kwargs...)\n\nPrepare the exact PI map that traces every local pseudomode from an identical system+pseudomode supersite. All modes are combined into the trailing local factor, so the output is the complete PIBasis(N, system_dimension).\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.pseudomode_trace_plan"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.trace_pseudomodes","page":"Observables and quantum information","text":"trace_pseudomodes(rho, site;\n                  plan=nothing, workspace=nothing,\n                  memory_budget=512*1024^2, kwargs...)\n\nTrace all local pseudomodes from every identical supersite directly in PI coordinates. Reuse a pseudomode_trace_plan and one task-owned LocalFactorTraceWorkspace across a scan.\n\n\n\n\n\ntrace_pseudomodes(rho::CompositePIState,\n                  model::GlobalPseudomodeModel)\n\nTrace the single shared mode and return the system PIState.  This is a factor-coordinate contraction, not a reconstruction of the d^N system.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.trace_pseudomodes"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.trace_pseudomodes!","page":"Observables and quantum information","text":"trace_pseudomodes!(output, rho, site, plan, workspace; kwargs...)\n\nTrace every local pseudomode into a caller-owned spin/qudit PI state. plan must come from pseudomode_trace_plan, and workspace must be a task-owned LocalFactorTraceWorkspace. This is the allocation-free repeated-application route used in parameter scans.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.trace_pseudomodes!"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.negativity","page":"Observables and quantum information","text":"negativity(rho, k; atol=_analysis_atol(rho), rtol=_state_rtol(rho),\n           plan=nothing,\n           workspace=nothing)\n\nCompute the entanglement negativity across the bipartition k | N-k,\n\nmathcal N(rho) = (rho^T_A_1-1)2\n\nThis method is polynomial in N for fixed local dimension d. Fully symmetric states use occupation branching, general qubit states use SU(2) recoupling, and general qudit states use Littlewood–Richardson intertwiner spaces computed from U(d) generator equations.\n\nOn the fully symmetric branch, the calculation embeds Sym^N(C^d) into Sym^k(C^d) ⊗ Sym^(N-k)(C^d) using exact occupation-number branching coefficients. The partially transposed matrix has dimension binomial(k+d-1,k) * binomial(N-k+d-1,N-k).\n\nPass a ReductionPlan(rho.basis,k) to reuse the product-Schur recoupling or subduction data across repeated states. A caller-owned ReductionWorkspace(plan,rho) additionally reuses the numerical product-block and partial-transpose scratch.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.negativity"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.logarithmic_negativity","page":"Observables and quantum information","text":"logarithmic_negativity(rho, k; base=2, atol=_analysis_atol(rho),\n                       rtol=_state_rtol(rho),\n                       plan=nothing, workspace=nothing)\n\nReturn log_base(‖rho^T_A‖₁) = log_base(2*negativity(rho,k)+1) for the general PI bipartitions supported by negativity.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.logarithmic_negativity"},{"category":"type","location":"api/analysis/#PermutationalInvariantDynamics.ReductionPlan","page":"Observables and quantum information","text":"ReductionPlan(basis, k; atol=2e-11)\n\nPrepare product-Schur intertwiners for the fixed bipartition k | N-k. Reuse the plan with reduced_state, reduced_purity, negativity, and partial_transpose_spectrum for many states on the exact same PIBasis. Qubits store their multiplicity-free SU(2) recoupling matrices. Qudits retain the Littlewood–Richardson intertwiner spaces as exact-support sparse blocks, one per discarded-factor weight, while the public subduction_intertwiners function continues to return ordinary dense matrices. No full d^N object is built.\n\nThe plan retains every required product-Schur intertwiner but no mutable application scratch.  It is therefore most useful for state or parameter scans at fixed (basis,k). For qudits, exact lattice-tableau counts first remove forbidden weight spaces, after which sparse simple-root constraints are factorized with SPQR. Dense nullspace matrices are temporary setup data; the prepared plan drops every exact structural zero before retention.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.ReductionPlan"},{"category":"type","location":"api/analysis/#PermutationalInvariantDynamics.ReductionWorkspace","page":"Observables and quantum information","text":"ReductionWorkspace(plan; T=Float64, mode=:both)\nReductionWorkspace(plan, rho; mode=:both)\n\nAllocate mutable scratch for repeated applications of a ReductionPlan. The workspace reuses the largest product-Schur block, multiplication intermediate, parent block, partial transpose, and all reduced-sector blocks. Reduced-state contractions trace the discarded product factor before a full product block is formed. It also prepares the exact parent multiplicity factors and scalar-type-matched recoupling matrices once in the workspace precision, so repeated applications do not redo BigInt/rational setup or trigger mixed-eltype matrix-multiplication fallbacks. The immutable plan keeps its compact recoupling representation (real for qubits); an already type-matched plan matrix is shared rather than copied into the workspace. Use mode=:reduction when only reduced states or purities are required, or mode=:negativity when only negativity is required. These modes omit the same-size partial-transpose matrix or the partial-trace/reduced-block storage, respectively. The default mode=:both preserves the general-purpose workspace. A mode-specific workspace raises if passed to an incompatible operation instead of allocating missing scratch implicitly.\n\nIt belongs to the exact ReductionPlan used at construction and must be used by only one task at a time.\n\nReductionWorkspace(plan, rho) selects a scalar type that can represent both the cached intertwiners and rho.  The keyword constructor interprets T as the real floating-point component type, consistently with PIState(...; T=...). For BigFloat, the state constructor requires uniform source storage, captures that precision and the current rounding mode, and creates every recoupler, exact scale, and scratch value in the same context. Applications re-enter the captured context; mismatched source or output storage raises rather than being silently rounded.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.ReductionWorkspace"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.reduced_state","page":"Observables and quantum information","text":"reduced_state(rho, k; atol=_analysis_atol(rho),\n              rtol=_state_rtol(rho), plan=nothing,\n              workspace=nothing, check=true)\n\nReturn the reduced state of any k particles as a PIState on PIBasis(k,d). Permutation invariance makes the result independent of which particles are retained.\n\nThe partial trace is evaluated directly in product Schur blocks. Qubits use specialized SU(2) recoupling and qudits use cached Littlewood–Richardson intertwiners. No computational-basis matrix of dimension d^k is formed. The returned coefficient blocks obey the package's equation-(7) normalization. Pass plan=ReductionPlan(rho.basis,k) when the same reduction is evaluated for several states. Add workspace=ReductionWorkspace(plan,rho) to reuse application scratch, or call reduced_state! to reuse the output too. Set check=false only in a prepared workflow that already validated rho. For a uniform BigFloat state, both prepared-workspace and plan-only paths execute at the owned source/plan precision even when the ambient precision is different.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.reduced_state"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.reduced_state!","page":"Observables and quantum information","text":"reduced_state!(out, rho, k; plan, workspace,\n               check=true,\n               atol=_analysis_atol(rho), rtol=_state_rtol(rho))\n\nWrite the k-particle reduced density matrix into out while reusing a caller-owned ReductionWorkspace. out must use plan.output_basis, and the plan, workspace, and input state must belong to the exact same parent PIBasis object. Set check=false only when rho has already been validated by the surrounding workflow; resource compatibility, Hermiticity roundoff, and output normalization checks remain active.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.reduced_state!"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.reduced_purity","page":"Observables and quantum information","text":"reduced_purity(rho, k; atol=_analysis_atol(rho),\n               rtol=_state_rtol(rho), plan=nothing,\n               workspace=nothing, check=true)\n\nReturn tr(rho_k^2) for the k-particle state returned by reduced_state. The result for k=0 is one and that for k=N equals purity.\n\nSet check=false only when the caller has already validated rho. BigFloat plan-only and prepared-workspace calls preserve their source/plan precision independently of the ambient precision.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.reduced_purity"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.reduced_purities","page":"Observables and quantum information","text":"reduced_purities(rho; ks=0:N, atol=_analysis_atol(rho),\n                 rtol=_state_rtol(rho), plans=nothing, check=true)\n\nCompute reduced purities for the requested subsystem sizes and return them in the same order as ks. The input state is validated exactly once, rather than once per requested subsystem size. Set check=false only when validation was already performed by the caller.\n\n\n\n\n\nreduced_purities(rho, plan_set; workspace=nothing, check=true, kwargs...)\n\nPrepared multi-bipartition purity evaluation with one parent-state validation.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.reduced_purities"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.partial_transpose_spectrum","page":"Observables and quantum information","text":"partial_transpose_spectrum(rho, k; plan=nothing)\n\nReturn product-Schur partial-transpose spectra as named tuples containing alpha, beta, multiplicity, and eigenvalues. Multiplicities are not expanded, avoiding exponentially repeated output.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.partial_transpose_spectrum"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.minimum_partial_transpose_eigenvalue","page":"Observables and quantum information","text":"minimum_partial_transpose_eigenvalue(rho, k; kwargs...)\n\nReturn the smallest eigenvalue across the multiplicity-compressed product-Schur blocks produced by partial_transpose_spectrum(rho, k).\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.minimum_partial_transpose_eigenvalue"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.bipartition_negativities","page":"Observables and quantum information","text":"Negativity for every inequivalent particle bipartition 1:floor(N/2).\n\n\n\n\n\nbipartition_negativities(rho, plan_set; workspace=nothing, kwargs...)\n\nEvaluate negativity in the exact order of plan_set.ks. The state is validated once; a ReductionWorkspaceSet(mode=:negativity) or :both workspace avoids per-state scratch allocation.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.bipartition_negativities"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.charge_resolved_negativity","page":"Observables and quantum information","text":"Negativity contributions resolved by partial-transpose charge imbalance.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.charge_resolved_negativity"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.number_resolved_negativity","page":"Observables and quantum information","text":"Charge-resolved negativity for the local number operator diag(0:d-1).\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.number_resolved_negativity"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.subduction_intertwiners","page":"Observables and quantum information","text":"subduction_intertwiners(alpha, beta, lambda; atol=2e-11,\n                        algorithm=:auto)\n\nReturn an orthonormal set of intertwiners from U(lambda) into U(alpha) ⊗ U(beta). By Schur–Weyl duality these span the symmetric-group subduction multiplicity space for S(lambda) ↓ S(weight(alpha)) × S(weight(beta)). Each returned matrix has orthonormal columns; its row basis is the product GT basis.\n\nThe number of matrices is the Littlewood–Richardson coefficient c^lambda_{alpha,beta}.\n\nThe default backend removes forbidden weights first, assembles sparse simple-root constraints, and uses a rank-revealing SPQR factorization. algorithm=:dense retains the former stacked Kronecker construction as a small-problem reference.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.subduction_intertwiners"},{"category":"function","location":"api/analysis/#PermutationalInvariantDynamics.littlewood_richardson_coefficient","page":"Observables and quantum information","text":"Return the exact Littlewood–Richardson coefficient c^lambda_{alpha,beta}. The lattice-tableau recursion performs no numerical nullspace construction.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.littlewood_richardson_coefficient"},{"category":"section","location":"framework/#Framework-and-physical-conventions","page":"Framework and conventions","text":"PermutationalInvariantDynamics.jl simulates open dynamics of N identical d-level systems when both the state and its evolution are invariant under relabelling the particles. The reduction is exact: the package keeps every degree of freedom in the permutationally invariant (PI) operator space, rather than closing correlations or projecting onto a mean-field ansatz. At fixed local dimension d, this space grows polynomially with N, even though the full many-body Hilbert and Liouville spaces grow exponentially.\n\nThe representation follows Thierry Bastin and John Martin, J. Phys. A: Math. Theor. 58, 275301 (2025), doi:10.1088/1751-8121/addfc1. This chapter introduces the physical assumptions, the Schur–Weyl blocks stored by the package, and the recommended path from a model to research observables.\n\nIf you prefer to construct and solve a model before studying the representation, start with Getting started: from a model to a solution, then return here for the mathematical conventions behind each object.","title":"Framework and physical conventions"},{"category":"section","location":"framework/#When-the-PI-framework-applies","page":"Framework and conventions","text":"Let\n\nmathcal H=(mathbb C^d)^otimes N\n\nand let P_pi permute the tensor factors according to piin S_N. An operator A is permutationally invariant when\n\nP_pi A P_pi^dagger=A\nqquadtextfor every piin S_N\n\nIn particular, a PI density operator describes particles that are physically equivalent under relabelling. This condition does not mean that the state must be pure, that the particles must be bosons, or that the state must be supported only in the fully symmetric Hilbert-space sector.\n\nDefine the permutation superoperator\n\nmathcal P_pi(X)=P_pi X P_pi^dagger\n\nA sufficient and natural condition for a generator to preserve PI states is permutation covariance,\n\nmathcal P_pimathcal L(t)\n=mathcal L(t)mathcal P_pi\nqquadtextfor every pi\n\nThus, if rho(0) is PI, the solution remains PI. An individual local jump operator need not itself be invariant: the sum of identical channels over all particle labels is covariant as a whole. The package's built-in term constructors encode precisely these PI sums.\n\nThe deterministic dynamics may be a general time-local Lindblad-like equation,\n\nfracdrhodt\n=-fracihbarH(t)rho\n+sum_kgamma_k(t)mathcal DL_k(t)rho\n\nwith\n\nmathcal DLrho\n=Lrho L^dagger\n-frac12left(L^dagger Lrho+rho L^dagger Lright)\n\nTime-dependent real rates may be negative. This is useful for time-local non-Markovian models, but the resulting map is not guaranteed to be completely positive. Quantum-jump trajectories require nonnegative rates, because negative rates do not define jump probabilities.","title":"When the PI framework applies"},{"category":"section","location":"framework/#Schur–Weyl-decomposition","page":"Framework and conventions","text":"Schur–Weyl duality decomposes the many-body Hilbert space as\n\n(mathbb C^d)^otimes N\nsimeq\nbigoplus_substacknuvdash Nell(nu)le d\nS^nuotimes U^nu(d)\n\nHere nu=(nu_1ldotsnu_d) is a partition of N, padded with zeros, and may be drawn as a Young diagram. The symmetric-group irrep S^nu has dimension\n\nf^nu=dim S^nu\n\nthe number of standard Young tableaux of shape nu. The unitary-group irrep U^nu(d) has dimension\n\ng_nu=dim U^nu(d)\n\nThe package labels a basis of U^nu(d) by Gel'fand–Tsetlin (GT) patterns, denoted W below. A Schur basis vector can therefore be written\n\nnuTWrangle\n\nwhere T=1ldotsf^nu is the symmetric-group multiplicity label and W=1ldotsg_nu is retained explicitly.\n\nSchur's lemma implies that every PI operator acts identically on the f^nu equivalent copies within a sector:\n\nA_mathrmPI\n=bigoplus_nu I_S^nuotimes R_nu\n\nThe small matrices R_nu contain all physical information. The package constructs and evolves these blocks directly; production algorithms never construct a Schur transform or a d^N-dimensional operator.","title":"Schur–Weyl decomposition"},{"category":"section","location":"framework/#The-orthonormal-PI-basis","page":"Framework and conventions","text":"The numerical representation uses the orthonormal operator basis of equation (7) of Bastin and Martin,\n\nF_nu^WW\n=frac1sqrtf^nu\nsum_T=1^f^nu\nnuTWranglelanglenuTW\n\nA PI operator is stored as\n\nA=sum_nuWW(C_nu)_WWF_nu^WW\n\ncoefficient_block(A, nu) returns the stored matrix C_nu, while physical_block(A, nu) returns the matrix acting on one Schur copy,\n\nR_nu=fracC_nusqrtf^nu\n\nFor block-wise input and inspection, each_schur_block(A) iterates over partition => physical_block pairs in the basis sector order.  The matching constructors apply the normalization explicitly:\n\nblocks = collect(each_schur_block(A))\nB = operator_from_schur_blocks(A.basis, blocks)\n\n# Stored equation-(7) blocks can instead be round-tripped explicitly.\nC = operator_from_schur_blocks(\n    A.basis,\n    each_schur_block(A; representation=:coefficient);\n    representation=:coefficient,\n)\n\nstate_from_schur_blocks follows the same convention and optionally calls validate_state; it never normalizes or repairs the supplied blocks. sector_metadata(basis) reports each block dimension, flattened coordinate range, exact symmetric-group multiplicity, and retained Hilbert-space dimension without expanding the multiplicity copies.\n\nThis normalization makes the global PI coordinates orthonormal in the Hilbert–Schmidt inner product:\n\nmathrmTr(A^dagger B)\n=sum_nu\nmathrmtrleft(C_nu(A)^dagger C_nu(B)right)\n\nSeveral package conventions then follow immediately:\n\nbeginaligned\nmathrmTr(A)\n  =sum_nusqrtf^numathrmtr(C_nu)\nC_nu(AB)\n  =fracC_nu(A)C_nu(B)sqrtf^nu\nC_nu(I)\n  =sqrtf^nuI\nmathrmTr(rho^2)\n  =sum_nulVert C_nu(rho)rVert_F^2\nendaligned\n\nFor a density operator, the population of sector nu is\n\np_nu\n=f^numathrmtr(R_nu)\n=sqrtf^numathrmtr(C_nu)\n\nA valid state has Hermitian positive-semidefinite R_nu in every retained sector and sum_nu p_nu=1. validate_state checks these properties without normalizing, symmetrizing, or clipping the input.\n\nYoung-diagram visualizations display the shape nu, not an arbitrarily chosen filled tableau. The T label is summed over in F_nu^WW, and its exact multiplicity f^nu is retained analytically. See Schur-block visualization for the block and rendering conventions.","title":"The orthonormal PI basis"},{"category":"section","location":"framework/#PI-is-broader-than-the-symmetric-sector","page":"Framework and conventions","text":"The fully symmetric Hilbert-space sector is only nu=(N0ldots0), for which f^nu=1. A pure identical product state psirangle^otimes N lies in this sector, but an identical mixed product state sigma^otimes N generally has support in several Schur sectors. Independent local dissipation can also transfer population between sectors even though the resulting state remains PI.\n\nConsequently, PIBasis(N, d) retains all sectors by default. A restricted basis is appropriate only when the model preserves the requested sector set. For example, a purely collective model can often be studied in the symmetric sector, whereas a generic LocalJump cannot. Model construction rejects a restricted basis when a built-in local process requires omitted sectors; it never truncates those transitions silently. Completeness is certified from the exact Schur–Weyl PI-dimension identity, and local p-body closure is generated from the p-box ancestors of the retained sectors and their descendants. Model validation therefore does not enumerate every partition merely to validate a large restricted basis.","title":"PI is broader than the symmetric sector"},{"category":"section","location":"framework/#Certified-diagonal-population-dynamics","page":"Framework and conventions","text":"Some generators preserve states that are diagonal in the stored GT-pattern basis. In that case define the physical population of pattern W in sector nu by\n\np_nuW=sqrtf^nu(C_nu)_WW\n             =f^nu(R_nu)_WW\n\nThese populations include the exact symmetric-group multiplicity and satisfy sum(p) == trace(rho). PopulationPlan(model) certifies that no diagonal input can generate a retained off-diagonal Schur coordinate, then lowers the model to a sparse reduced generator. The resulting coordinate count is sum(g_nu), rather than the general PI count sum(g_nu^2). For qubits this reduces the evolving vector from cubic to quadratic growth in N.\n\nplan = PopulationPlan(model)\nreport = plan.invariance\np0 = diagonal_populations(rho0)\nsolution = solve_populations(plan, p0, (0.0, 10.0); saveat=0.1)\nrho_final = state(solution, lastindex(solution))\n\nConstructing the plan once and reading plan.invariance avoids compiling the restriction twice; call population_invariance(model) only when no plan is needed. The default certificate is structurally strict (atol=rtol=0), so a weak but nonzero mixing term is never projected away. Supplying a nonzero tolerance is an explicit approximate request recorded as reason=:within_tolerance when it accepts nonzero leakage.\n\nThe certificate is deliberately basis dependent. Scalar time-dependent rates on fixed operators are checked term by term and remain supported. A genuinely operator-valued schedule returns an inconclusive report until the model is freezed at an explicit time. An appreciably coherent initial state is also rejected by diagonal_populations; check=false is the explicit state- projection route. No population path silently removes coherences or normalizes a state.\n\nThe current PopulationPlan compiler forms the standalone sqrt(f^nu) coordinate scale and the parent Liouvillian trace functional in the working real type. Construction therefore raises when that factor is outside the type's finite range; use wider coefficients for the plan. Direct population extraction and reconstruction use prepared exact scaled products and may still succeed whenever the requested final entries are representable.","title":"Certified diagonal-population dynamics"},{"category":"section","location":"framework/#Spin-and-Dicke-conveniences","page":"Framework and conventions","text":"spin_matrices(d) returns the standard spin-j=(d-1)2 generators in the ascending order -jj. For qubits the package local order is (ge)=(-12+12). Thus jm is ge and jx, jy, and jz are Pauli matrices divided by two. collective_spin(basis, :x) and the other named components lower these matrices directly to Schur blocks.\n\nThe state helpers retain the same convention. computational_product_state uses a one-based local level and works for qubits or qudits. dicke_state(basis,j,m) and dicke_operator(basis,j,m,mp) map qubit spin labels to the corresponding partition and GT-pattern weights. In a sector of multiplicity f^nu, their selected physical block entry is 1f^nu: the PI object is uniform over the indistinguishable multiplicity copies. ghz_state and spin_coherent_state construct their symmetric-sector states without forming a 2^N vector.","title":"Spin and Dicke conveniences"},{"category":"section","location":"framework/#Dimension-and-computational-scaling","page":"Framework and conventions","text":"The different representations have the following dimensions:\n\nSpace Dimension\nMany-body Hilbert space d^N\nFull Liouville space d^2N\nComplete PI operator space n_mathrmPI=sum_nu g_nu^2=N+d^2-1choose N\n\nAt fixed d,\n\nn_mathrmPI=Oleft(N^d^2-1right)\n\nso the symmetry changes exponential growth into polynomial growth. For qubits,\n\nn_mathrmPI=N+3choose 3\n\nIf only the fully symmetric sector is retained, its Hilbert-space irrep has\n\ng_(N)=N+d-1choose N\n\nstates and g_(N)^2 PI operator coordinates.\n\nIn this one-sector case, collective one-body operators are lifted directly in the occupation basis through\n\na_a^dagger a_blvertboldsymbol nrangle\n=sqrtn_b(n_a+1)\n lvertboldsymbol n+boldsymbol e_a-boldsymbol e_brangle\nquad (ane b)\n\nThis avoids constructing the general one-box recoupling geometry. Fixed collective terms retain their exact sparse block support, while driven terms fill preallocated dense block scratch because their support may change with time. Collective-only models spanning several Schur sectors prepare only sector-diagonal one-box contractions. Models with local gains still use the complete sector-changing one-box geometry.\n\nPolynomial scaling does not make every operation inexpensive. A dense PI Liouvillian contains n_mathrmPI^2 entries, and a complete dense eigendecomposition has cubic cost in n_mathrmPI. The matrix-free backend avoids materializing that matrix, but it still stores PI state and Krylov vectors of length n_mathrmPI. At fixed body order p, the local contraction cost includes d^2p, and general-qudit reductions can retain large packed Littlewood–Richardson intertwiner spaces and use a dense temporary nullspace basis during setup. Consult Architecture and efficient workflows and Matrix-free Krylov solvers before a large scan.","title":"Dimension and computational scaling"},{"category":"section","location":"framework/#Exact-combinatorics-and-representable-numerical-factors","page":"Framework and conventions","text":"Combinatorial coefficients can exceed a machine integer, or even floatmax(T), well before the corresponding PI calculation becomes too large.  The public helpers return exact integers:\n\nchoose = exact_binomial(200, 100)            # BigInt\nways = exact_multinomial((80, 70, 50))       # BigInt\n\nsymmetric_group_dimension, unitary_group_dimension, and commutant_dimension likewise return exact BigInt results.  These exact functions do not return logarithms or floating approximations; storing and printing an exceptionally large answer still costs space proportional to the number of its digits.\n\nNumerical PI kernels delay floating conversion until exact cancellations have been performed.  One- and p-body Schur prefactors are formed from exact rational branching/path weights rather than separately converting a large binomial coefficient and large sector multiplicities.  Square roots are evaluated after an exact power-of-two rescaling, so an intermediate coefficient may exceed floatmax(T) when the final square root is still representable.  When a large exact factor multiplies a small rate, block, or contraction, their binary scales are combined before conversion; the factor is never required to be representable by itself.  Ordinary representable factors retain a direct native-arithmetic path. Cancellation-prone collective blocks and static local-gain coordinates first check their native path sum, then selectively recompute only the affected sector or sector-pair group with guarded wider isometries and contractions before converting the checked result back to the declared type. This includes severe cancellation when every exact path-pair factor is individually representable, and large sums whose native forward-error bound cannot certify even the final working-precision ulp. A widened value is certified against a forward roundoff bound derived from its absolute path sum and operation count. If it remains inside that uncertainty interval, the operation raises rather than treating the interval as proof of an exact cancellation; stable nonzero values still undergo the package's strict range checks. For an InPlaceTimeOperator, caller-owned scratch fixes the scalar type in advance; a cancellation-prone dynamic p-body block or local gain therefore raises with guidance to widen the prototype instead of returning a working-precision discrepancy. Preallocated one-body schedules follow the same rule once N exceeds the native geometry's cancellation threshold. Static collective blocks and local-map elements can instead widen only the requested result. Dynamic direct-PI schedules retain their coefficient storage and apply the inverse Schur-multiplicity scale in a fused form, so an unrepresentable standalone sqrt(f^nu) does not discard a representable physical block.\n\nPI operator multiplication likewise keeps its ordinary BLAS block product when the intermediate is representable. If C_A*C_B overflows before the required 1/sqrt(f^nu) scaling, the exceptional path fuses that exact scale with each product term and widens only an uncertified dot product or complex component cancellation. Thus large-multiplicity identities remain idempotent without imposing wider arithmetic on ordinary sectors. Physical Schur visualization metrics also aggregate directly first; only a failed physical-range conversion causes power-of-two entry scaling before the norm or SVD and joint restoration of the binary and multiplicity scales.\n\nPure product-state amplitudes use normalized conditional binomial recurrences, seeded near each binomial mode and advanced by adjacent ratios, instead of evaluating a large multinomial coefficient separately from the amplitude powers. One-site normalization is checked in at least the recurrence precision, and its predicted tensor-power trace must remain within the requested tolerance. Thus a low-precision norm that rounds to one cannot silently become a badly normalized large-N state.\n\nThis handling preserves the requested storage type; it does not silently change a Float32 or Float64 result to wider storage. If the final stored value lies outside that type's exact nonzero finite range, the package raises an ArgumentError rather than returning zero, Inf, or NaN. Individual state amplitudes still follow ordinary rounding in their requested output type. Supply wider numerical data, such as BigFloat, when the final stored values themselves require the wider range.\n\nState analyses avoid forming a per-copy block when it is unnecessary. They use sqrt(f^nu) * C_nu = f^nu * rho_nu, whose trace is the sector probability, for entropy, distances, collective information, reductions, and spin phase space. Thus these bounded quantities can remain available after a per-copy physical_block, purity, or Float64 coefficient-space trace vector has become genuinely unrepresentable.","title":"Exact combinatorics and representable numerical factors"},{"category":"section","location":"framework/#Physical-model-terms","page":"Framework and conventions","text":"Let h, ell, and L be matrices on one particle, and let h_p, ell_p, and L_p act on p particles. For a subset Ssubset1ldotsN, a subscript S denotes the corresponding embedded operator. The built-in terms map to the following physical objects.\n\nConstructor Contribution before the common scalar rate\nLocalHamiltonian(h) H=sum_i h^(i)\nCollectiveHamiltonian(h) H=sum_i h^(i)\nLocalJump(ell) sum_imathcal Dell^(i)\nCollectiveJump(L) mathcal DJ, J=sum_iL^(i)\nCorrelatedLocalJumps((La,...), Gamma) sum_isum_abGamma_abmathcal D_abL_a^(i)L_b^(i)\nCorrelatedCollectiveJumps((La,...), Gamma) sum_abGamma_abmathcal D_abJ_aJ_b\nPBodyHamiltonian(hp, p) H_p=sum_lvert Srvert=p(h_p)_S\nLocalPBodyJump(ellp, p) sum_lvert Srvert=pmathcal D(ell_p)_S\nCollectivePBodyJump(Lp, p) mathcal DJ_p, J_p=sum_lvert Srvert=p(L_p)_S\nDirectPIHamiltonian(H) Commutator with an existing PIOperator H\nDirectPIJump(L) Dissipator of an existing PI operator L\n\nLocalHamiltonian and CollectiveHamiltonian lower to the same identical one-body Hamiltonian sum; the two names allow a model to express its physical intent. Local and collective jump constructors are not equivalent. The local channel is an incoherent sum of dissipators and can couple Schur sectors. The collective channel first sums the amplitudes and then forms one dissipator; it is sector diagonal.\n\nThe correlated constructors accept a Hermitian positive-semidefinite Kossakowski matrix Gamma, with mathcal D_abAB(rho)=Arho B^dagger-B^dagger Arho2. A fixed matrix is validated and factorized once in the small channel space; its effective independent jumps then use the same PI kernels as LocalJump or CollectiveJump. InPlaceTimeOperator also accepts a fixed-shape Kossakowski-matrix schedule. Its evaluated matrix and factorization scratch belong to LiouvillianWorkspace, and every value is revalidated before use.\n\nThe rate keyword multiplies the displayed contribution. Hamiltonian terms also accept hbar. Operators supplied to a p-body constructor have size d^ptimes d^p and must be invariant under permutations of their p tensor factors. All subset sums are over unordered subsets. The package uses rates exactly as supplied and never inserts a Kac or thermodynamic scaling factor. When both a Hamiltonian rate and hbar are exact Integer/Rational values, their quotient is cancelled exactly before checked floating conversion. Ordinary floating inputs retain the direct division path, so small fixed-precision models pay no multiprecision cost.\n\nFor time dependence, a scalar coefficient may be written as rate=(t, parameters) -> .... InPlaceTimeOperator provides a prepared, fixed-shape schedule when the operator itself changes with time. Stationary states and ordinary spectra require an autonomous generator. Use freeze only when the stationary properties of one instantaneous generator are the intended question; use direct time evolution or Floquet analysis for the actual driven dynamics.\n\nFor the common six-channel qubit bath, qubit_ensemble_model is a convention-explicit shorthand. Its local keywords emission, dephasing, and pumping multiply dissipators of j_-, j_z, and j_+ respectively; the three collective_... keywords use the corresponding coherent sums. Because j_z=sigma_z2, a single-qubit off-diagonal element decays at half the dephasing keyword rate. Fixed zero rates are omitted, so a collective-only model can use a symmetry-restricted basis, while callable or negative rates are retained exactly as supplied.","title":"Physical model terms"},{"category":"section","location":"framework/#Package-objects-and-ownership","page":"Framework and conventions","text":"The main data flow is\n\nPIBasis -> PIModel -> compile -> CompiledPIModel -> solver or analysis\n\n(PI and finite factors) -> CompositePIBasis -> CompositeSuperoperator\n\n(PI system, one shared mode) -> GlobalPseudomodeModel -> generator or workspace\n\n(system, identical local modes) -> PISupersite -> pseudomode_model -> compile\n\n(composite background, monitored jumps) -> CompositeTrajectoryPlan\n                                         -> quantum_trajectories\n\n(parameter grid, builder) -> ParameterScanPlan -> parameter_scan\n\n(periodic compiled generator) -> FloquetMap -> multiplier or fixed-point solver\n\n(autonomous compiled generator) -> ResponseWorkspace -> response or adjoint action\n\n(PI system, exponential baths) -> scaled/unscaled HEOMPlan -> HEOM state or solver\n\n(PI Hamiltonian, shared HOPSBaths) -> HOPSPlan -> HOPSWorkspace per path\n                                               -> root-state ensemble\n\nPI–HOPS has a stronger symmetry requirement than the averaged open-system equation: every individual noise realization must preserve the PI representation. The exact PI route therefore supports shared baths whose coupling operators are collective/PI. Independent local colored noises are PI only after ensemble averaging and generally leave the PI pseudo-ket space on each path; use PI–HEOM or identical local pseudomode supersites for that case. Linear HOPS propagates an unnormalized root pseudo-ket psi_boldsymbol 0. Its density estimate is mathbb Epsi_boldsymbol 0ranglelanglepsi_boldsymbol 0: average the outer products without normalizing individual paths.\n\nObject Role Ownership rule\nPIBasis Partitions, GT patterns, block offsets, and representation geometry labels Share read-only\nPISupersite, BosonicPseudomode Exact factorization and finite-cutoff metadata for one identical system plus its local auxiliaries Share read-only; one supersite basis is reused by every model at the same cutoffs\nPIState, PIOperator Dense vectors of orthonormal PI coefficients Mutable value owned by the caller\nCompositePIBasis, CompositePIState, CompositePIOperator Tensor products of several PI spaces and finite auxiliary matrix spaces Basis is shared read-only; state/operator data belong to the caller\nGlobalPseudomodeModel One PI system factor, one shared finite mode, collective couplings, and separated mode damping channels Model and prepared maps are shared read-only; one global-pseudomode workspace per concurrent application\nPIModel Declarative immutable tuple of physical terms Share read-only\nCompiledPIModel Prepared term lowering and sparse or matrix-free backend Compile once and share read-only\nLiouvillianWorkspace and solver workspaces Mutable multiplication and Krylov scratch One per concurrent task or thread\nAdvanced Krylov workspaces Dominant bases/residual arrays for block, multi-shift, recycled, or exponential actions One per concurrent solve; recycled state belongs to one ordered chain\nFloquetMap, FloquetWorkspace Immutable period/integration recipe and forward/adjoint RK scratch Map shared with synchronized convenience calls; one explicit workspace per concurrent period action\nResponseWorkspace Restarted-GMRES and/or exponential-action storage for one prepared source Reuse sequentially; one workspace per concurrent response task\nParameterScanPlan, ParameterScanWorkspace Immutable scan recipe and mutable continuation/solver scratch Plan shared read-only; one workspace per serial caller or threaded worker\nHEOMPlan, HEOM workspaces Immutable scaled/unscaled ADO topology/couplings and application/RK4 scratch Plan shared read-only; one workspace per concurrent application/evolution\nHOPSPlan, HOPSWorkspace Immutable shared-bath hierarchy geometry and mutable auxiliary pseudo-kets/noise/RK scratch Plan shared read-only; one workspace and RNG per concurrent path\nWeakPITrajectoryPlan, weak-PI workspaces Immutable Schur-Kraus unraveling and task-owned path/batch scratch Plan shared read-only; one workspace and RNG per concurrent worker\nQuditHusimiPlan Dense coherent vectors for one exact basis, point set, and sector selection Share read-only across states; setup can dominate\nDiffusiveBatchPlan, batch workspaces Prepared trajectory request and worker-local path/RNG buffers Plan shared read-only; workspace reused sequentially only\nConvergenceStudyResult All raw refinement results, estimates, diagnostics, and decisions Immutable record; memory includes every retained evaluator result\nCollectiveObservablePlan, LocalFactorTracePlan, ReductionPlan, CompositeReductionPlan Prepared observable, internal local-factor trace, particle-bipartition geometry, or composite-factor trace Share read-only; tied to the exact basis object\nOneBodyRDMWorkspace, LocalFactorTraceWorkspace, ReductionWorkspace Mutable one-body, occupation, or product-Schur reduction scratch One per concurrent task\nCorrelationPlan, CorrelationWorkspace Prepared quantum-regression insertions and their evolution/GMRES scratch Plan shared read-only; one workspace per task\nCompositeSuperoperator, CompositeSuperoperatorWorkspace Sum of factorized maps and its tensor-fibre scratch Generator shared read-only; one workspace per task\nCompositeTrajectoryPlan, composite trajectory workspaces Explicit monitored tensor-product channels and density-valued conditional evolution Plan shared read-only; one workspace and RNG per concurrent path worker\n\ncompile(model; backend=:auto) performs the expensive representation setup once and chooses a conservative backend. For standard fixed kernels, the sparse-memory bound uses their prepared exact block support; driven or custom kernels with unknown support retain a safe dense-coordinate fallback. This lets structured collective generators select sparse storage without charging them for a fictitious dense PI matrix. Matrix-free application is preferable when the resulting sparse matrix would still dominate memory, for driven models, or for Krylov methods. Explicit apply! and apply_adjoint! calls should reuse a caller-owned workspace in hot or parallel loops. The convenient compatibility calls are synchronized and safe, but concurrent calls serialize.\n\nWhen a small model needs several one-body/Appendix-D geometry families, compilation automatically shares one bounded, transient OneBoxCGCache during lowering. For repeated independent compilations, prepare a basis-owned cache at the greatest required body order and pass it as coefficient_cache to LiouvillianPlan, compile, liouvillian, steady_state, or compile_family. It is read-only in use and never becomes process-global state; incompatible basis, depth, or precision requests raise.\n\nPrepared observables and reductions follow the same pattern: construct the read-only plan once, then reuse it for many states. LocalFactorTracePlan traces one internal tensor factor from every supersite and returns a complete PI basis at the kept local dimension. Its prepared occupation transforms are rectangular, exact-support sparse, and memory-guarded. ReductionPlan instead traces or partially transposes groups of particles at fixed local dimension. For a reduced state it contracts the discarded product factor one slice at a time; only negativity needs the complete product block. Qudit ReductionPlan objects can be much larger than collective-observable plans because they may retain many subduction intertwiners. They store those maps as exact-support sparse discarded-weight blocks and report the packed versus dense-equivalent size in plan.estimates, but the weight-restricted SPQR setup can still be costly. Benchmark setup and retained memory before caching many reductions. The detailed stable, advanced, and experimental interfaces are listed in API tiers and prepared analysis.","title":"Package objects and ownership"},{"category":"section","location":"framework/#A-complete-qubit-example","page":"Framework and conventions","text":"The following script defines driven qubits with independent and collective decay, evolves an initially excited product state, samples a collective observable, and computes the autonomous steady state.\n\nusing PermutationalInvariantDynamics\n\nN = 12\nbasis = PIBasis(N, 2)  # all Schur sectors\n\nspin = spin_matrices()  # local order: (|g>, |e>)\n\nmodel = PIModel(basis, [\n    LocalHamiltonian(spin.jx; rate=0.8),\n    LocalJump(spin.jm; rate=0.10),\n    CollectiveJump(spin.jm; rate=0.02 / N),\n])\n\n# Local level 2 is |e>; the constructor uses Julia's one-based index.\nrho0 = computational_product_state(basis, 2)\n\n# Representation geometry and Liouvillian kernels are prepared once.\nprepared = compile(model; backend=:auto)\n\ntimes = range(0.0, 10.0; length=101)\nsolution = solve_dynamics(\n    prepared, rho0, (first(times), last(times));\n    saveat=times,\n    steps_per_interval=16,\n    observables=(magnetization=spin.jz,),\n    save_states=false,\n)\n\n# A local matrix in `observables` denotes its collective sum. Only the\n# resulting scalar time series is retained here, not 101 PI state vectors.\nmagnetization = real.(solution.observables[:magnetization]) ./ N\n\nrho_ss = stationary_state(prepared)\nreport = diagnostics(rho_ss)\n\nprintln(\"PI coordinate dimension = \", pi_dimension(basis))\nprintln(\"final magnetization per particle = \", last(magnetization))\nprintln(\"steady-state trace error = \", report.trace_error)\n\nThe fixed-step solver is deliberately explicit about its temporal resolution; increase steps_per_interval until the reported observables are converged. For adaptive or stiff integration, construct dynamics_problem(prepared, rho0, tspan) and solve it with a compatible SciML algorithm from an added solver package. The paired scripts in Research examples show literature models and their numerical checks.","title":"A complete qubit example"},{"category":"section","location":"framework/#Choosing-a-workflow","page":"Framework and conventions","text":"Research task Recommended entry point Essential qualification\nExact finite-N PI dynamics compile, then solve_dynamics Converge the fixed time step\nAdaptive or stiff dynamics dynamics_problem Add and configure a SciML solver\nAutonomous steady state stationary_state Inspect residual, trace, and possible degeneracy\nSlow modes or Liouvillian gap liouvillian_spectrum, pi_liouvillian_gap A selected Krylov window is not a complete spectrum\nPeriodic dynamics and selected multipliers floquet_map, selected_floquet_multipliers, floquet_steady_state Converge the one-period integration separately from Krylov residuals\nQuantum-jump ensembles quantum_trajectories Rates nonnegative; converge tolerances and ensemble size\nTrajectory estimate of an autonomous steady state trajectory_steady_state Converge settling time, within-path spacing, path integration, and independent-path count; residual is not a convergence proof\nConfidence-controlled ensembles adaptive_quantum_trajectories, adaptive_weak_pi_quantum_trajectories, adaptive_diffusive_trajectories Sampling certificate only; separately converge the path integrator\nPure Schur pseudo-ket ensembles WeakPITrajectoryPlan, weak_pi_quantum_trajectories Auxiliary weak-PI states, not labeled-particle wavefunctions; fixed operators; converge fixed-step or event-driven controls\nWeak-PI trajectory estimate of an autonomous steady state weak_pi_trajectory_steady_state Density outer products precede path averaging; converge burn-in, sampling window, selected path integrator, and independent-path count\nObservable-only output solve_dynamics(...; observables=..., save_states=false) The state history is deliberately unavailable\nState-free trajectory statistics quantum_trajectories(...; observables=..., save_states=false) Waiting-time output still scales with recorded jumps unless disabled\nTwo-time correlations and spectra CorrelationPlan, two_time_correlation, stationary_correlation_spectrum Autonomous QRT; converge RK4 or GMRES controls\nResolvents, adjoint evolution, and susceptibility ResponseWorkspace, resolvent_norm, adjoint_evolve, steady_state_susceptibility Matrix-free routes require an adjoint where applicable; iterative estimates retain residual diagnostics\nSeveral PI ensembles or a finite ancilla CompositePIBasis, CompositeSuperoperator, CompositeTrajectoryPlan Cross maps and monitored gains are factorized; composite paths are density-valued\nOne shared cavity or explicit global pseudomode global_pseudomode_model The mode is one finite global factor coupled through a collective system operator; converge its cutoff\nIdentical systems with identical local pseudomodes pseudomode_supersite, pseudomode_model The complete system+local-modes tuple is one PI particle; converge every finite mode cutoff\nRelated steady states or spectra ParameterScanPlan, parameter_scan Serial continuation is path dependent; independent points may be threaded/distributed\nStructured matrix-free linear families block_gmres, multishift_gmres, recycled_gmres, krylov_expv Inspect raw residuals/error estimates and reuse task-owned workspaces\nFinite-memory bosonic bath HEOMBath, HEOMPlan, heom_evolve, heom_steady_state Prefer exact scaled ADOs when conditioning benefits; converge bath poles, hierarchy depth, and time/Krylov discretization separately\nShared-bath finite-memory pure-state ensemble HOPSBath, HOPSPlan, hops_average Coupling and noise must preserve PI on every path; average unnormalized root outer products and converge bath poles, hierarchy depth, time step, and sample count separately\nGeneralized qudit coherent-state Q QuditHusimiPlan, qudit_husimi_q Normalized Haar data on supplied points; no qudit Wigner convention inferred\nNumerical refinement evidence convergence_study and specialized wrappers Final refinement agreement is distinct from inner solver convergence\nLarge-N product prediction MeanFieldPlan, solve_meanfield Approximate after correlations develop\nRepeated collective observable CollectiveObservablePlan Reuse one plan for many states\nTrace a mode/ancilla inside every PI supersite LocalFactorTracePlan, LocalFactorTraceWorkspace Complete kept-factor PI output; exact-support sparse transforms have a setup memory guard\nRepeated marginal or negativity ReductionPlan, ReductionWorkspace Setup can be large for qudits\nRepeated one-factor composite trace CompositeReductionPlan Packed exact diagonal contraction; no full composite trace vector\n\nFor product-state predictions at N beyond a practical PI basis, the mean-field layer evaluates\n\nrho^(p)approxsigma^otimes p\n\ndirectly on one dtimes d density matrix. Its finite rule reproduces the exact initial one-body derivative of a supported factorized state, but later propagation discards generated correlations. It must not be described as exact PI dynamics. See Mean-field dynamics and predictions for the finite and thermodynamic conventions.","title":"Choosing a workflow"},{"category":"section","location":"framework/#Conventions-and-reliability","page":"Framework and conventions","text":"The following conventions matter when comparing with analytical formulas or manipulating coefficient data directly:\n\nPartitions are descending length-d tuples padded with zeros and ordered in descending lexicographic order.\nGT patterns are ordered by their stored entry tuples; sector matrices are flattened in Julia column-major order.\nThe paper labels local computational states 0ldotsd-1, whereas Julia matrix indices are 1ldotsd. The ordering of the supplied local matrices and state vectors must be consistent.\nRates, Hamiltonian prefactors, and normalization factors are never changed implicitly. In particular, no Kac factor is inferred.\nInvalid states are rejected rather than normalized, symmetrized, or made positive by clipping eigenvalues.\nA complete dense spectrum and a selected Krylov spectrum answer different questions. Residuals, convergence flags, and spectral scope must accompany claims about slow modes or gaps.\nA finite product closure, a finite trajectory ensemble, and an under-resolved integrator are approximations even though the underlying PI representation is exact.\n\nRemaining research-scale limitations are also structured rather than hidden: large qudit irreps can make sparse Littlewood–Richardson factorization expensive; a single very large Schur block still needs dense linear algebra for some diagnostics; the Evans test can be inconclusive for unsupported direct microscopic recouplings; and complete Floquet spectra or the dense spectral-visualization source convenience still materialize the PI-dimensional one-period map. Selected multiplier, periodic-state, and stroboscopic calculations can instead use FloquetMap without that matrix.\n\nContinue with Architecture and efficient workflows for backend and concurrency details, Mathematics and API for analysis methods, Published-model validation for finite-size reference checks, and the API reference for individual signatures.","title":"Conventions and reliability"},{"category":"section","location":"meanfield/#Mean-field-dynamics-and-predictions","page":"Mean-field predictions","text":"The mean-field layer derives a closed equation for the identical one-particle density matrix sigma directly from the physical terms used by a PIModel. It is a product-BBGKY closure,\n\nrho^(p)approxsigma^otimes p\n\nand works for a general local dimension d. It is not restricted to qubit Bloch vectors and never constructs a d^N state, a PI basis vector, or a Liouvillian matrix. The evolved state contains only d^2 complex entries; for a supported p-body term, the largest scratch matrices are d^ptimes d^p, requiring O(d^2p) storage but remaining independent of N and the full many-body Hilbert space.","title":"Mean-field dynamics and predictions"},{"category":"section","location":"meanfield/#Quick-start","page":"Mean-field predictions","text":"using LinearAlgebra\nusing PermutationalInvariantDynamics\n\nN = 100\nsx = ComplexF64[0 1; 1 0]\nsm = ComplexF64[0 1; 0 0]\nbasis = PIBasis(N, 2; sectors=[(N, 0)])\nmodel = PIModel(basis, [\n    CollectiveHamiltonian(sx / 2; rate=1.3),\n    CollectiveJump(sm; rate=0.8 / N),\n])\n\nplan = MeanFieldPlan(model; limit=:finite)\nsigma0 = ComplexF64[1 0; 0 0]\ntimes = range(0.0, 10.0; length=101)\nsolution = solve_meanfield(\n    plan, sigma0, (first(times), last(times));\n    saveat=times, steps_per_interval=16)\n\npopulation = [real(meanfield_expectation(sigma, sm' * sm))\n              for sigma in solution]\n\nWhen no PI calculation is needed, construct the plan directly from the particle number, local dimension, and physical terms:\n\nplan = MeanFieldPlan(N, 2, (\n    CollectiveHamiltonian(sx / 2; rate=1.3),\n    CollectiveJump(sm; rate=0.8 / N),\n); limit=:thermodynamic)\n\nThis form is the efficient route for a very large formal N: it does not construct Schur geometry merely to obtain a mean-field equation.","title":"Quick start"},{"category":"section","location":"meanfield/#Product-closure-equations","page":"Mean-field predictions","text":"The library uses\n\nmathcal DLsigma\n=Lsigma L^dagger-tfrac12L^dagger Lsigma\n\nFor a one-particle Hamiltonian term with rate r,\n\ndotsigma=-fracirhbarhsigma\n\nFor a permutation-symmetric p-particle Hamiltonian summed over unordered subsets, define\n\nh_rm eff(sigma)=mathrmtr_2ldots p\nlefthleft(Iotimessigma^otimes(p-1)right)right\n\nThe finite product closure is\n\ndotsigma=-fracirhbar\nN-1choose p-1h_rm eff(sigma)sigma\n\nA local one-particle jump contributes rmathcal DLsigma. For LocalPBodyJump(L,p), let\n\nG(sigma)=mathrmtr_2ldots p\nleftLsigma^otimes pL^daggerrightqquad\nQ_rm eff(sigma)=mathrmtr_2ldots p\nleftL^dagger L\nleft(Iotimessigma^otimes(p-1)right)right\n\nIts contribution is\n\nrN-1choose p-1\nleft(G(sigma)-tfrac12Q_rm eff(sigma)sigmaright)\n\nFinally, for a collective one-particle jump J=sum_iL_i, write ell=mathrmtr(Lsigma). The exact finite-N product closure is\n\ndotsigmabig_J=rleft\nmathcal DLsigma+\nfracN-12\nleft(ell^*Lsigma-ellL^daggersigmaright)\nright\n\nThese equations use the same unordered-subset convention as PBodyHamiltonian and LocalPBodyJump.\n\nFor the collective p-particle jump\n\nJ=sum_S=pL_S\n\nthe dissipator contains ordered pairs of subsets. Define the contracted operators\n\nE_m(sigma)=mathrmtr_m+1ldots p\nleftLleft(I^otimes motimes\nsigma^otimes(p-m)right)right\nqquad 0leq mleq p\n\nso that E_0=ell=mathrmtr(Lsigma^otimes p) and E_p=L, and introduce the cross dissipator\n\nPhi_AB(R)=ARB^dagger-\nfrac12left(B^dagger A R+R B^dagger Aright)\n\nOnly ordered subset pairs whose union contains the retained particle contribute. If both subsets contain it and have total overlap r, their number is\n\na_r=N-1choose p-1p-1choose r-1N-pchoose p-r\nqquad 1leq rleq p\n\nIf exactly the first subset contains it and the remaining overlap is r, the number is\n\nb_r=N-1choose p-1p-1choose rN-pchoose p-r\nqquad 0leq rleq p-1\n\nA binomial coefficient is understood to vanish when its lower argument lies outside its allowed range; this automatically removes overlap classes that do not fit at small N.\n\nWriting widehat E_r=I_1otimes E_r, the exact finite product closure is\n\nbeginaligned\ndotsigmabig_J=r_JBigg\nsum_r=1^pa_r\nmathrmtr_2ldots r\nPhi_E_rE_r(sigma^otimes r)\n+sum_r=0^p-1b_r\nmathrmtr_2ldots r+1\nleft(Phi_E_r+1widehat E_r\n+Phi_widehat E_rE_r+1right)\n(sigma^otimes(r+1))Bigg\nendaligned\n\nThe implementation evaluates every E_m directly and never constructs an operator on the union of two subsets. Its largest matrices therefore remain d^ptimes d^p, independent of N. For p=1, the two sums reduce to the one-particle dissipator and the collective field shown above.","title":"Product-closure equations"},{"category":"section","location":"meanfield/#Supported-terms-and-explicit-errors","page":"Mean-field predictions","text":"PI term Product mean-field lowering\nLocalHamiltonian, CollectiveHamiltonian Supported\nPBodyHamiltonian Supported for permutation-symmetric input operators\nLocalJump Supported\nCollectiveJump Supported, including the finite self term\nCorrelatedLocalJumps, CorrelatedCollectiveJumps Rejected by the current product-closure compiler; factor the fixed Kossakowski matrix into ordinary jumps first\nLocalPBodyJump Supported for permutation-symmetric input operators\nCollectivePBodyJump(..., 1) Equivalent to CollectiveJump\nCollectivePBodyJump(..., p) with p > 1 Supported through exact finite overlap classes and their leading thermodynamic class\nDirectPIHamiltonian, DirectPIJump Rejected; Schur blocks do not retain a unique microscopic body-order decomposition\nOperator-valued time functions Rejected by the prepared path; use fixed operators and time-dependent scalar rates\n\nAn unsupported term raises an error. It is never silently omitted or replaced by a lower-body process. In particular, a PIOperator stored in a direct term may represent many inequivalent microscopic operators, and a mixed product state may occupy sectors absent from a restricted basis. Supply an equivalent local or PBody decomposition when a mean-field prediction is required.","title":"Supported terms and explicit errors"},{"category":"section","location":"meanfield/#Finite-product-closure-versus-thermodynamic-closure","page":"Mean-field predictions","text":"limit=:finite keeps exact finite-size subset counts. At the initial time it reproduces the exact one-body derivative of an initially factorized PI state for every supported term. Subsequent propagation closes newly generated correlations as products, so it is generally not exact finite-system dynamics.\n\nlimit=:thermodynamic retains the leading subset count\n\nN-1choose p-1longrightarrow\nfracN^p-1(p-1)\n\nfor Hamiltonian and local-jump p-body terms. For collective one-particle jumps it drops the subleading local dissipator and replaces N-1 by N. Thus a rate written explicitly as r=kappaN has the finite closure\n\nfrackappaNmathcal DLsigma\n+kappafracN-1NC_L(sigma)\n\nand thermodynamic closure kappa C_L(sigma), where C_L=(ell^*Lsigma-ellL^daggersigma)2.\n\nThe plan cannot infer whether an arbitrary numerical rate was intended to scale as 1N, 1N^p-1, or remain constant. The user must encode the physical Kac scaling in the term rate. In both modes rates are used exactly as supplied; limit=:thermodynamic changes combinatorial factors but never inserts a compensating power of N. It is therefore not a symbolic limit of arbitrary numeric terms.\n\nFor a collective p-body jump, the unique leading class consists of two disjoint subsets with exactly one containing the retained particle. Its count is replaced by\n\nb_0=N-1choose p-1N-pchoose p\nlongrightarrow\nfracN^2p-1(p-1)p\n\nAll shared-particle and self terms are subleading and are dropped in this mode. With E_1 and ell=E_0, the retained contribution is\n\nr_JfracN^2p-12(p-1)p\nleft(ell^*E_1sigma-ellE_1^daggersigmaright)\n\nConsequently, an extensive collective-p-body model commonly supplies an explicit rate proportional to N^-(2p-1). The package does not insert that factor automatically.","title":"Finite product closure versus thermodynamic closure"},{"category":"section","location":"meanfield/#Plans,-workspaces,-and-time-dependence","page":"Mean-field predictions","text":"A MeanFieldPlan owns copied, read-only operator data and prepared tensor contractions. Share it across parameter scans or tasks. A MeanFieldWorkspace owns mutable stage and contraction scratch and must be used by only one task at a time:\n\nworkspace = MeanFieldWorkspace(plan, sigma0)\ndu = zeros(eltype(workspace), size(sigma0))\n\nmeanfield_rhs!(du, plan, sigma0, 0.0, parameters, workspace)\n\nAfter warm-up, the explicit-workspace RHS is the allocation-conscious hot path. For threaded scans, create one workspace per thread. The convenience form allocates its result:\n\ndu = meanfield_rhs(plan, sigma0; time=0.0, parameters=parameters)\n\nThe workspace type is inferred from the state, fixed operators, constant rates, and Hamiltonian prefactors. Reusing it with a state or destination that would lose precision raises an error. A scalar rate function must likewise return a number representable by that workspace type; for example, return a Float32 rate in an otherwise ComplexF32 calculation.\n\nFixed operators with scalar rates of the form (t, p) -> rate remain on the prepared path. Negative time-dependent rates are accepted, consistently with the PI dynamics API; the resulting time-local equation need not generate a completely positive map.\n\nThe RHS preserves trace and Hermiticity algebraically. Numerical propagation does not normalize, symmetrize, or positivity-clip a density matrix. Converge the time step and inspect trace, Hermiticity, and eigenvalues when these properties matter.","title":"Plans, workspaces, and time dependence"},{"category":"section","location":"meanfield/#Fixed-step-and-adaptive-evolution","page":"Mean-field predictions","text":"solve_meanfield is the compact fixed-step interface. It uses a reusable RK4 workspace and returns a MeanFieldResult with times, states, limit, and algorithm fields; the result also supports indexing and iteration:\n\nsolution = solve_meanfield(\n    plan, sigma0, (0.0, 20.0);\n    saveat=0.1, steps_per_interval=32)\n\nsigma_final = last(solution)\n\nFor repeated propagation into caller-owned storage, use meanfield_evolve!. Its classical RK4 path uses three full one-site integration matrices rather than retaining four derivatives simultaneously:\n\nworkspace = MeanFieldWorkspace(plan, sigma0)\ndestination = zeros(eltype(workspace), size(sigma0))\nmeanfield_evolve!(destination, plan, sigma0, (0.0, 1.0);\n                  steps=256, workspace=workspace)\n\nFor adaptive or stiff integration, use the SciML adapter and add a solver in the active environment:\n\nusing OrdinaryDiffEq\n\nproblem = meanfield_problem(plan, sigma0, (0.0, 20.0);\n                            parameters=parameters)\nadaptive = solve(problem, Rodas5P(); saveat=0.1)\n\nThe ODEProblem captures one workspace. Construct a separate problem for each concurrently running solve.","title":"Fixed-step and adaptive evolution"},{"category":"section","location":"meanfield/#Product-state-observable-predictions","page":"Mean-field predictions","text":"For a local observable X,\n\nlocal_mean = meanfield_expectation(sigma, X)\nmoments = meanfield_collective_moments(plan, sigma, X)\n\nThe returned collective moments use\n\nlangle X_crangle=Nlangle Xrangle_sigma\nqquad\nlangle X_c^2rangle=\nNlangle X^2rangle_sigma+N(N-1)langle Xrangle_sigma^2\n\nfor X_c=sum_iX_i. A symmetric p-body sum is predicted with\n\nvalue = meanfield_pbody_expectation(plan, sigma, Xp, p)\n\nwhich evaluates\n\nNchoose pmathrmtr\nleft(X_psigma^otimes pright)\n\nThese are product-closure predictions. They omit connected correlations and must not be presented as exact moments of an evolved finite PI state.","title":"Product-state observable predictions"},{"category":"section","location":"meanfield/#Fixed-points-and-linear-stability","page":"Mean-field predictions","text":"An autonomous nonlinear closure can have several stable fixed points, unstable solutions, limit cycles, or no attracting stationary point. The stationary helper relaxes from a supplied seed and therefore selects a basin:\n\nfixed = meanfield_stationary_state(\n    plan, sigma_seed; return_info=true)\nsigma_fixed = fixed.state\n\nInspect its convergence and RHS residual before using the state. Failure to converge is not evidence that no fixed point exists; change the seed and integration controls, or use a dedicated nonlinear solver.\n\nLinearization is performed in the real traceless-Hermitian tangent space of dimension d^2-1:\n\nJ = meanfield_jacobian(plan, sigma_fixed)\nstability = meanfield_stability(plan, sigma_fixed)\n\nstability.stable\nstability.eigenvalues\n\nNegative real parts identify a linearly attracting fixed point. Eigenvalues near the imaginary axis require tolerance and time-step convergence checks; the linear test does not determine the nonlinear basin of attraction. Fixed points and autonomous stability analysis are not substitutes for Floquet analysis of a periodically driven mean-field equation.\n\nSee examples/meanfield_time_crystal.jl for finite and thermodynamic closures compared with an exact matrix-free PI calculation.","title":"Fixed points and linear stability"},{"category":"section","location":"research_utilities/#Research-utilities,-channels,-and-control","page":"Research utilities and control","text":"This experimental surface collects analysis and workflow tools which operate directly on retained PI coordinates. None reconstructs a d^N density matrix. The runnable overview is examples/research_utilities.jl.","title":"Research utilities, channels, and control"},{"category":"section","location":"research_utilities/#Compressed-spectra-and-populations","page":"Research utilities and control","text":"spectral_trace(rho,f) evaluates mathrmtrf(rho) from physical Schur eigenvalues with exact multiplicities. Specialized entropy routines remain preferable when logarithmic rescaling is needed. Population-coordinate iterators expose the exact sector, GT pattern, multiplicity, population index, and diagonal PI index; population_transitions adds the directed off-diagonal rates of a certified PopulationPlan.","title":"Compressed spectra and populations"},{"category":"section","location":"research_utilities/#Channels,-measurements,-and-tomography","page":"Research utilities and control","text":"PIChannel and MatrixFreePIChannel act in equation-(7) coefficient coordinates. check_pi_channel certifies trace preservation and, for a materialized map, complete positivity on the retained direct-sum Schur algebra. A certificate says nothing about omitted sectors.\n\nPI POVMs use the same scope. Sampling builds one cumulative categorical table; maximum-likelihood tomography uses a diluted, positivity-preserving Rrho R iteration and always reports convergence.","title":"Channels, measurements, and tomography"},{"category":"section","location":"research_utilities/#Checkpoints","page":"Research utilities and control","text":"The built-in :pid format records schema version, scalar type, restricted partitions, coefficients, optional time, and string metadata. Loading never normalizes or repairs the state. JLD2 and HDF5 are weak dependencies activated only when explicitly imported.","title":"Checkpoints"},{"category":"section","location":"research_utilities/#Joint-symmetries-and-gradients","page":"Research utilities and control","text":"joint_symmetry_projector intersects commuting unitary-conjugation charge sectors without constructing a reduced Liouvillian. Construction verifies commutation and certifies the intersection rank. Use one JointSymmetryProjectorWorkspace per concurrent task.\n\nimplicit_steady_state_gradient solves the trace-fixed tangent equation with matrix-free GMRES. checkpointed_adjoint_gradient propagates piecewise controls with RK4, retains only periodic forward checkpoints, recomputes each segment once, and uses endpoint-trapezoidal gradient quadrature. Converge the Krylov tolerance or control grid for the intended observable.","title":"Joint symmetries and gradients"},{"category":"section","location":"research_utilities/#API","page":"Research utilities and control","text":"","title":"API"},{"category":"function","location":"research_utilities/#PermutationalInvariantDynamics.spectral_trace","page":"Research utilities and control","text":"spectral_trace(rho, f; atol=_analysis_atol(rho), rtol=_state_rtol(rho))\n\nEvaluate tr(f(rho)) directly from multiplicity-compressed physical Schur spectra.  Each eigenvalue of sector nu is counted with its exact symmetric-group multiplicity f^nu; no d^N spectrum is expanded.\n\nThe callback f is evaluated on physical density eigenvalues.  Generic callbacks cannot be algebraically rescaled, so an eigenvalue or final weighted term outside the state's floating type raises with wider-precision guidance. Use specialized routines such as von_neumann_entropy and renyi_entropy for their overflow-safe logarithmic formulas.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.spectral_trace"},{"category":"type","location":"research_utilities/#PermutationalInvariantDynamics.PopulationCoordinate","page":"Research utilities and control","text":"Metadata for one public Schur/GT population coordinate.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.PopulationCoordinate"},{"category":"type","location":"research_utilities/#PermutationalInvariantDynamics.PopulationCoordinates","page":"Research utilities and control","text":"PopulationCoordinates(basis)\neach_population_coordinate(basis)\n\nAllocation-free iterable over the population coordinates used by PopulationPlan.  Entries follow sector order and then stored GT-pattern order.  index addresses a population vector, pi_index is the corresponding diagonal PI coefficient, and multiplicity is exact BigInt.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.PopulationCoordinates"},{"category":"function","location":"research_utilities/#PermutationalInvariantDynamics.each_population_coordinate","page":"Research utilities and control","text":"Return PopulationCoordinates for basis.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.each_population_coordinate"},{"category":"type","location":"research_utilities/#PermutationalInvariantDynamics.PopulationTransition","page":"Research utilities and control","text":"One directed off-diagonal transition of a reduced population generator.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.PopulationTransition"},{"category":"function","location":"research_utilities/#PermutationalInvariantDynamics.population_transitions","page":"Research utilities and control","text":"population_transitions(plan; time=nothing, parameters=nothing,\n                       atol=0)\n\nReturn directed off-diagonal transitions of a certified population generator. Rates are read from the sparse reduced generator at the requested time; diagonal escape terms are omitted.  source and destination contain full PopulationCoordinate metadata.  Values with magnitude at or below the nonnegative absolute atol are omitted without modifying the generator.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.population_transitions"},{"category":"type","location":"research_utilities/#PermutationalInvariantDynamics.AbstractPIChannel","page":"Research utilities and control","text":"Supertype for composable linear channels on one retained PI operator algebra.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.AbstractPIChannel"},{"category":"type","location":"research_utilities/#PermutationalInvariantDynamics.PIChannel","page":"Research utilities and control","text":"PIChannel(basis, matrix; check=false, atol=1e-12, rtol=1e-10)\n\nWrap an explicit dense or sparse PI-coefficient channel.  matrix acts on the orthonormal equation-(7) coordinates.  With check=true, construction requires complete positivity and trace preservation on the retained direct-sum Schur algebra; this scoped certificate is not a claim about sectors omitted from a restricted basis.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.PIChannel"},{"category":"type","location":"research_utilities/#PermutationalInvariantDynamics.MatrixFreePIChannel","page":"Research utilities and control","text":"MatrixFreePIChannel(basis, T, action!, adjoint_action!)\n\nConstruct a matrix-free PI channel from preallocated forward and adjoint callbacks callback(destination, source).  Compatibility calls are synchronized; callbacks themselves may be used with task-owned external scratch when their implementation provides it.  Complete positivity cannot be certified without materialization, while trace preservation is checked through the supplied adjoint.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.MatrixFreePIChannel"},{"category":"function","location":"research_utilities/#PermutationalInvariantDynamics.apply_channel!","page":"Research utilities and control","text":"apply_channel!(destination, channel, source)\napply_channel(channel, source)\n\nApply a PI channel to compatible PIState or PIOperator data.  The result is not normalized, clipped, symmetrized, or positivity-repaired.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.apply_channel!"},{"category":"function","location":"research_utilities/#PermutationalInvariantDynamics.apply_channel","page":"Research utilities and control","text":"Allocate and return the result of applying channel to a PI state or operator.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.apply_channel"},{"category":"function","location":"research_utilities/#PermutationalInvariantDynamics.compose_channels","page":"Research utilities and control","text":"compose_channels(outer, inner)\n\nReturn the lazy composition outer o inner.  Only one PI-sized temporary is created per compatibility application; explicit matrices are multiplied once at construction.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.compose_channels"},{"category":"function","location":"research_utilities/#PermutationalInvariantDynamics.channel_adjoint","page":"Research utilities and control","text":"Return the Hilbert–Schmidt adjoint of a PI channel.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.channel_adjoint"},{"category":"function","location":"research_utilities/#PermutationalInvariantDynamics.identity_channel","page":"Research utilities and control","text":"Construct the identity channel on a retained PI operator algebra.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.identity_channel"},{"category":"function","location":"research_utilities/#PermutationalInvariantDynamics.kraus_channel","page":"Research utilities and control","text":"kraus_channel(operators; check=false)\n\nConstruct the sector-preserving channel rho -> sum_a K_a rho K_a' from PI Kraus operators on one exact basis.  Physical Schur blocks are used directly, and no full-system Kraus matrix is formed.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.kraus_channel"},{"category":"type","location":"research_utilities/#PermutationalInvariantDynamics.PIChannelCheck","page":"Research utilities and control","text":"Scoped CP/TP diagnostics for a retained PI channel algebra.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.PIChannelCheck"},{"category":"function","location":"research_utilities/#PermutationalInvariantDynamics.check_pi_channel","page":"Research utilities and control","text":"check_pi_channel(channel; atol=1e-12, rtol=1e-10,\n                 materialize=false)\n\nCheck trace preservation and unitality from PI coefficient trace/identity vectors.  For an explicit channel, also test complete positivity of every map between source and destination Schur matrix algebras by Choi eigenvalues. For a matrix-free/lazy channel CP is reported as missing unless materialize=true is explicitly requested.  The scope is always :retained_pi_algebra; omitted Schur sectors are outside the certificate.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.check_pi_channel"},{"category":"function","location":"research_utilities/#PermutationalInvariantDynamics.pi_povm_probabilities","page":"Research utilities and control","text":"pi_povm_probabilities(rho, effects; complete=true, atol=..., rtol=...)\n\nReturn Born probabilities for PI POVM effects without reconstructing a d^N density matrix.  Effects are required to be Hermitian positive semidefinite; with complete=true they must sum to the retained PI identity. No negative value is clipped: values outside the stated tolerance raise, while roundoff-sized negative values are returned as exact zero only inside this probability-only output buffer.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.pi_povm_probabilities"},{"category":"type","location":"research_utilities/#PermutationalInvariantDynamics.PIPOVMSample","page":"Research utilities and control","text":"Outcome counts and exact model probabilities from sample_pi_povm.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.PIPOVMSample"},{"category":"function","location":"research_utilities/#PermutationalInvariantDynamics.sample_pi_povm","page":"Research utilities and control","text":"sample_pi_povm(rho, effects, shots; rng=Random.default_rng(), kwargs...)\n\nDraw categorical PI POVM outcomes.  The cumulative table is built once and sampling uses binary search, so cost is O(number_of_effects + shots*log M).\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.sample_pi_povm"},{"category":"type","location":"research_utilities/#PermutationalInvariantDynamics.PITomographyResult","page":"Research utilities and control","text":"Result of constrained PI maximum-likelihood tomography.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.PITomographyResult"},{"category":"function","location":"research_utilities/#PermutationalInvariantDynamics.maximum_likelihood_tomography","page":"Research utilities and control","text":"maximum_likelihood_tomography(basis, effects, counts;\n    initial_state=nothing, maxiter=10_000, atol=1e-10, rtol=1e-8,\n    dilution=1, require_convergence=false)\n\nEstimate a PI state with the positivity- and trace-preserving diluted R*rho*R maximum-likelihood iteration.  Every iterate is represented in Schur blocks; the full Hilbert space is never formed.  dilution in (0,1] mixes the likelihood operator with the identity and can stabilize incomplete or ill-conditioned POVMs.\n\nThe returned PITomographyResult always exposes convergence.  Set require_convergence=true to raise instead of returning the best final iterate.  This is a constrained MLE within the retained PI algebra, not a tomography model for permutation-breaking states.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.maximum_likelihood_tomography"},{"category":"constant","location":"research_utilities/#PermutationalInvariantDynamics.PI_CHECKPOINT_VERSION","page":"Research utilities and control","text":"Current on-disk schema version written by save_checkpoint.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.PI_CHECKPOINT_VERSION"},{"category":"type","location":"research_utilities/#PermutationalInvariantDynamics.PIStateCheckpoint","page":"Research utilities and control","text":"PIStateCheckpoint(state; time=nothing, metadata=Dict())\n\nVersioned, detached checkpoint of one PI state.  Metadata keys and values are stored as strings for portability across the built-in binary, JLD2, and HDF5 backends.  The exact retained partition list and coefficient precision are part of the schema; loading never substitutes a complete basis for a restricted one.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.PIStateCheckpoint"},{"category":"function","location":"research_utilities/#PermutationalInvariantDynamics.checkpoint_state","page":"Research utilities and control","text":"Return a detached PIState from a PIStateCheckpoint.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.checkpoint_state"},{"category":"function","location":"research_utilities/#PermutationalInvariantDynamics.save_checkpoint","page":"Research utilities and control","text":"save_checkpoint(path, checkpoint; format=:auto)\nsave_checkpoint(path, state; time=nothing, metadata=Dict(), format=:auto)\n\nWrite a versioned PI state checkpoint.  format=:auto selects JLD2 for .jld2, HDF5 for .h5/.hdf5, and the dependency-free :pid format otherwise.  Optional formats become available when their package extension is loaded.  Existing files are replaced only because the caller explicitly requested this write.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.save_checkpoint"},{"category":"function","location":"research_utilities/#PermutationalInvariantDynamics.load_checkpoint","page":"Research utilities and control","text":"load_checkpoint(path; format=:auto)\n\nLoad and validate a versioned PIStateCheckpoint.  The coefficient vector, restricted sector list, time, and string metadata are detached from the file backend.  State physicality is not silently repaired; call validate_state when loading untrusted numerical data.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.load_checkpoint"},{"category":"type","location":"research_utilities/#PermutationalInvariantDynamics.JointSymmetryProjector","page":"Research utilities and control","text":"JointSymmetryProjector\n\nMatrix-free orthogonal projector onto the intersection of several commuting unitary-conjugation charge sectors.  charges records the requested charge of each component and range_dimension is the exact numerical rank certified at construction.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.JointSymmetryProjector"},{"category":"type","location":"research_utilities/#PermutationalInvariantDynamics.JointSymmetryProjectorWorkspace","page":"Research utilities and control","text":"Caller-owned scratch for a simultaneous weak-symmetry projector.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.JointSymmetryProjectorWorkspace"},{"category":"function","location":"research_utilities/#PermutationalInvariantDynamics.joint_symmetry_projector","page":"Research utilities and control","text":"joint_symmetry_projector(basis, specifications; atol=1e-12, rtol=1e-10)\n\nConstruct the intersection projector for several commuting weak unitary symmetries.  Each specification is U => charge or (U, charge), where U has the same meaning as in matrixfree_symmetry_projector.  Pairwise commutation of the induced conjugations is certified sector by sector; merely approximately compatible noncommuting projectors are rejected.\n\nThe resulting projector plugs directly into harmonic Arnoldi and the PI gap API by passing it as symmetry=projector.  One explicit JointSymmetryProjectorWorkspace is required per concurrent task.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.joint_symmetry_projector"},{"category":"type","location":"research_utilities/#PermutationalInvariantDynamics.SteadyStateGradientPlan","page":"Research utilities and control","text":"SteadyStateGradientPlan(source, stationary_state; operator_scale=nothing,\n                        atol=1e-10, rtol=1e-8)\n\nPrepare the trace-fixed matrix-free linear system used for implicit stationary state derivatives.  stationary_state is validated for trace and residual but is never normalized or repaired.  The immutable plan may be shared; every concurrent solve requires a separate SteadyStateGradientWorkspace.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.SteadyStateGradientPlan"},{"category":"type","location":"research_utilities/#PermutationalInvariantDynamics.SteadyStateGradientWorkspace","page":"Research utilities and control","text":"Reusable GMRES and application scratch for implicit steady-state gradients.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.SteadyStateGradientWorkspace"},{"category":"type","location":"research_utilities/#PermutationalInvariantDynamics.SteadyStateGradientResult","page":"Research utilities and control","text":"Implicit tangent states and optional observable gradients.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.SteadyStateGradientResult"},{"category":"function","location":"research_utilities/#PermutationalInvariantDynamics.implicit_steady_state_gradient","page":"Research utilities and control","text":"implicit_steady_state_gradient(plan, derivatives; observables=nothing, ...)\nimplicit_steady_state_gradient(source, state, derivatives; kwargs...)\n\nSolve\n\nmathcal Lpartial_murho_ss=-(partial_mumathcal L)rho_ss\nqquad mathrmtr(partial_murho_ss)=0\n\nwith restarted matrix-free GMRES on a rank-one trace-fixed operator.  Neither the Liouvillian nor its inverse is materialized.  derivatives may contain matrices, compiled PI generators, or matrix-free PI generators.  Optional observables returns real(tr(A' * partial_mu rho)) in rows.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.implicit_steady_state_gradient"},{"category":"type","location":"research_utilities/#PermutationalInvariantDynamics.AdjointControlResult","page":"Research utilities and control","text":"AdjointControlResult\n\nTerminal objective, piecewise-control gradient, final state, initial costate, and checkpoint/recomputation metadata from checkpointed_adjoint_gradient.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.AdjointControlResult"},{"category":"function","location":"research_utilities/#PermutationalInvariantDynamics.checkpointed_adjoint_gradient","page":"Research utilities and control","text":"checkpointed_adjoint_gradient(base, derivatives, rho0, objective,\n                              times, controls; checkpoint_stride=16)\n\nCompute the terminal-objective gradient for piecewise-constant controls in L(t)=base+sum_mu controls[mu,i]*derivatives[mu].  controls has one column per interval of times; objective is a Hermitian PI operator and the scalar target is real(tr(objective*rho(T))).\n\nForward states are retained only every checkpoint_stride intervals.  Each segment is recomputed once during the backward continuous-adjoint RK4 sweep, reducing retained state memory from O(n_PI*n_times) to O(n_PI*(n_times/checkpoint_stride + checkpoint_stride)).  The reported gradient uses endpoint trapezoidal quadrature; refine the control grid to converge both propagation and gradient.  No dense Liouvillian is required.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.checkpointed_adjoint_gradient"},{"category":"section","location":"api_tiers/#API-tiers-and-prepared-analysis","page":"API tiers and prepared analysis","text":"The package exposes three API tiers. The tiers describe the recommended level of abstraction and the expected rate of interface evolution; they do not replace numerical convergence checks or the mathematical caveats of a method.\n\nFor a first model, read Getting started: from a model to a solution before using this page to decide how far below the high-level API your calculation needs to go.","title":"API tiers and prepared analysis"},{"category":"section","location":"api_tiers/#Stable-high-level-API","page":"API tiers and prepared analysis","text":"These entry points are intended for ordinary research scripts and examples. They validate basis ownership and return package-level state or result objects. New scripts may import the deliberately compact PermutationalInvariantDynamics.Workflow namespace instead of the complete expert surface. Its bindings are identical to the parent module's bindings; it does not wrap calls or alter dispatch. Convention-tested starting models live under Models.\n\nArea Recommended entry points\nRepresentation PIBasis, PIState, PIOperator, PISupersite, GlobalPseudomodeModel, pseudomode_model, global_pseudomode_model, exact_binomial, exact_multinomial, Schur-block constructors, spin/state conveniences, PIModel, and the physical term constructors\nPreparation compile, compile_family, compile_affine_family, specialize, prepare_geometry, isautonomous, freeze\nDynamics solve_dynamics (including observable-only streaming), solve_populations, dynamics_problem, floquet_steady_state, quantum_trajectories (including online ensemble statistics), trajectory_steady_state, HOPSPlan, hops_trajectory, and hops_average\nMean-field predictions MeanFieldPlan, solve_meanfield, meanfield_problem, meanfield_stationary_state, meanfield_stability\nStationary and spectral analysis stationary_state, liouvillian_spectrum, pi_liouvillian_gap, diagnostics, recommend_solver\nObservables and information collective_expectation, collective_variance, qfi, qfim, stabilizer_renyi_entropy, two_time_correlation, delayed_second_order_correlation, stationary_correlation_spectrum, entropy and distance functions\nVisualization schur_block_structure, visualize_schur_blocks, spin_husimi_q, spin_wigner, qudit_husimi_q, visualize_spin_phase_space, and the density/Liouvillian/Floquet spectrum data and renderers\nReductions and entanglement one_body_rdm, one_body_rdm!, local_factor_trace, local_factor_trace!, trace_pseudomodes, trace_pseudomodes!, global_pseudomode_state, composite_reduced_state, composite_reduced_state!, reduced_state, reduced_state!, reduced_purity, negativity, partial_transpose_spectrum, ppt_mixture_test\nValidation state_diagnostics, positivity_diagnostics, validate_state\nReproducibility PIExperiment, plan_experiment, explain_experiment, verified_solve, save_experiment, load_experiment\nInference and counting fit_parameters, parameter_identifiability, TiltedLiouvillianPlan, counting_scgf, counting_cumulants\n\nPrefer these commands in new code. In particular, stationary_state returns a package state container (PIState, or CompositePIState for a shared global mode), while the older low-level steady_state interface may return raw coordinates.","title":"Stable high-level API"},{"category":"section","location":"api_tiers/#Advanced-reusable-API","page":"API tiers and prepared analysis","text":"Advanced objects expose memory ownership and numerical algorithms explicitly. They are supported and tested, but callers must obey their compatibility and threading contracts.\n\nOneBodyGeometry, PBodyGeometry, CollectiveObservablePlan, LocalFactorTracePlan, ReductionPlan, and CompositeReductionPlan own prepared representation data. They can only be used with the exact PIBasis object from which they were constructed. LocalFactorTracePlan changes the local dimension by tracing the same internal factor from every supersite; it is distinct from the particle bipartition represented by ReductionPlan. pseudomode_trace_plan prepares that same map for all trailing modes of one exact PISupersite.\nPreparedGeometryBundle groups immutable one-body, selected p-body, and multi-bipartition data for one exact basis and arithmetic context. PreparationCache is an explicit, synchronized, user-owned store with a retained-memory budget; there is no process-global cache and no unstable binary persistence contract.\nPPTMixturePlan owns the sparse real conic map for the PI qubit PPT-mixture test. It is tied to one exact basis and is safe to share across state scans; Clarabel solver state is call-local. Even for a sector-restricted input basis, the plan enforces equality in every Schur sector of the complete basis.\nStabilizerRenyiPlan owns normalized Krawtchouk transforms and stable binomial metadata for one exact qubit basis and precision. StabilizerRenyiWorkspace owns the O(N^2) mutable transform scratch. Reuse both across pure symmetric states, share only the plan between tasks, and read the nonstabilizerness guide before interpreting the result. Mixed or multi-sector states are rejected rather than assigned the paper's pure-state measure.\nLiouvillianPlan, CompiledPIModel, CompiledPIModelFamily, and AffineCompiledPIModelFamily hold lowered model data; LiouvillianWorkspace, ThreadedLiouvillianWorkspace, EvolutionWorkspace, KrylovWorkspace, ArnoldiWorkspace, SymmetryProjectorWorkspace, OneBodyRDMWorkspace, and ReductionWorkspace hold mutable scratch.\nBlockGMRESWorkspace, MultiShiftGMRESWorkspace, RecycledGMRESWorkspace, and KrylovExpvWorkspace reuse dominant full-coordinate storage for structured linear solves and exponential actions. Projected dense factorizations may still allocate, and every workspace is task-owned.\nParameterScanPlan holds a copied parameter grid and callable model recipe; ParameterScanWorkspace holds continuation, family-Liouvillian, and solver scratch. Serial continuation, independent threaded scans, and optional process-distributed scans have deliberately different ownership contracts described in the scan guide.\nHEOMPlan stores the finite hierarchy topology and physical coupling data. HEOMWorkspace and HEOMEvolutionWorkspace separate application scratch from the three hierarchy-sized arrays used by low-storage forward RK4. Hard hierarchy truncation and bath-pole convergence remain explicit research choices. Matrix right-hand sides flatten ADO and RHS columns and reuse one bounded system batch; HEOMWorkspace(plan; batch_columns=q) prepares the bounded capacity for a repeated width q.\nHOPSPlan stores a finite pure-state hierarchy in direct-sum Schur-irrep coordinates for shared Gaussian baths. HOPSWorkspace owns one path's hierarchy, RK4, coupling, and colored-noise scratch and is task-local. Linear-HOPS roots are unnormalized; physical densities are averages of their unnormalized outer products. Independent local colored noises are outside this exact PI path representation. PIUnitaryPulse and HierarchyPulseSequence are prepared, read-only event data shared by the HEOM and HOPS drivers; tetrahedral_pulse_sequence, octahedral_pulse_sequence, and icosahedral_pulse_sequence prepare the published Eulerian Platonic schedules from two shared generators. Task-owned hierarchy workspaces perform the pulse applications.\nQuditHusimiPlan retains dense coherent vectors for a fixed point/sector set and exact basis. Reuse it across states; setup can dominate for many large irreps. ConvergenceStudyResult retains every requested refinement result, so extract compact estimates when histories are large.\nTrajectoryPlan holds one immutable fixed-operator jump lowering. TrajectoryWorkspace owns one path's integration buffers, while TrajectoryBatchWorkspace owns task-local workspaces and RNGs for repeated ensembles. One batch workspace must not serve concurrent ensemble calls.\nBatchedConditionalPlan and fixed-capacity BatchedConditionalWorkspace expose matrix-RHS drift, RK4, intensity, and grouped-gain kernels for cohorts already assigned the same time step. They do not replace the independent adaptive scheduling semantics of quantum_trajectories.\nCorrelationPlan owns copied quantum-regression insertion blocks and may be shared read-only. CorrelationWorkspace owns the conditional state, block products, RK4 storage, and shifted-GMRES storage; use one per concurrent task. Time-domain correlations and resolvent spectra require an autonomous generator.\nCompositeSuperoperator is a read-only sum of tensor-factor maps. CompositeSuperoperatorWorkspace owns its full-coordinate buffers, small factor fibres, and nested PI application workspaces. CompositeSuperoperatorBatchWorkspace is the fixed-capacity matrix-RHS counterpart and raises rather than growing. Use explicit task-owned workspaces in hot or parallel loops; the convenience matrix-free wrapper serializes access to one compatibility workspace.\nGlobalPseudomodeModel owns the read-only system-plus-one-mode factorization, with background separated from explicit mode damping_channels. global_pseudomode_workspace returns one task-owned composite workspace; the synchronized global_pseudomode_matrixfree wrapper owns or reuses exactly one such workspace. Read the global-pseudomode guide before choosing it instead of replicated local modes.\nCompositeTrajectoryPlan owns explicit fixed monitored channels, their assembled unconditional generator, and multiplicity-aware trace metadata. CompositeTrajectoryWorkspace shares two full channel buffers across all gains and losses; batch workspaces add one complete path workspace and RNG per active task. The supplied background excludes monitored dissipators.\nPopulationPlan stores the certified sparse action on multiplicity-weighted Schur-diagonal probabilities. PopulationWorkspace owns its application and three-array forward RK4 scratch. Certification is exact by default, compile-only coordinate lookup scales with the population dimension and is not retained, and scalar time-dependent rates reuse the same reduced matrices. Operator-valued schedules must be frozen before certification. Plan construction currently requires standalone sqrt(f^nu) coordinate scales to fit the working real type; model-derived plans also inherit the PI trace-vector bound. Direct population extraction and reconstruction instead use bounded prepared products.\nInPlaceTimeOperator describes a fixed-shape built-in operator schedule, including Appendix-D p-body operators. Its callback writes into the evaluated operator stored by each LiouvillianWorkspace; batched forward and adjoint calls evaluate it once per application stage rather than once per input column.\nThreadedLiouvillianWorkspace partitions complete destination Schur sectors among task-private block scratch. threaded_apply! and its adjoint evaluate every operator/rate schedule once before spawning and never perform atomic output updates or scheduler-ordered reductions. It is useful only after benchmarking against ordinary application; small blocks can be dominated by task or nested-BLAS overhead. One task takes the ordinary inline fast path.\nthreaded_matrixfree wraps a prepared autonomous PI source for a Krylov consumer while reusing one guarded ThreadedLiouvillianWorkspace. Share the underlying compiled plan, not this synchronized compatibility wrapper, between concurrent solves.\naccelerator_preflight estimates the simultaneous host/device cost and compatibility of an exact sparse upload without materializing it or initializing hardware. Core currently reports CUDA as unavailable; a backend becomes usable only through a separately tested optional extension, never through an implicit host fallback.\nMeanFieldPlan lowers supported physical terms to one-site contractions with at most d^p by d^p scratch matrices. MeanFieldWorkspace owns its three-matrix forward RK and contraction scratch; use one workspace per concurrent task.\nliouvillian, apply!, apply_adjoint!, evolve!, steady_state, and the lower-level Krylov routines provide explicit control over storage and solver choices.\n\nPrepared plans are read-only during application and may be shared between tasks. Mutable workspaces must not be shared concurrently; allocate one per task or thread.\n\nFor repeated supersite reductions, prepare the local trace and the subsequent spin bipartition separately:\n\ntrace_plan = LocalFactorTracePlan(supersite_basis, (2, mode_levels);\n    traced_factor=2)\ntrace_work = LocalFactorTraceWorkspace(trace_plan)\nrho_spin = local_factor_trace(rho_supersite, trace_plan;\n    workspace=trace_work)\n\ncut = ReductionPlan(trace_plan.output_basis, fld(N, 2))\ncut_work = ReductionWorkspace(cut, rho_spin; mode=:negativity)\nspin_negativity = negativity(\n    rho_spin, fld(N, 2); plan=cut, workspace=cut_work)\n\nThe trace plan retains only exact nonzero sparse support and has a 512 MiB setup guard. Reuse is therefore important, and large local dimensions can still make setup memory the limiting resource.\n\nMean-field propagation is a controlled product-state closure, not exact PI dynamics after correlations form. See Mean-field predictions for the finite-N and thermodynamic conventions and the supported term set.\n\nschur_block_structure separates quantitative sector diagnostics from SVG rendering. Reuse the returned structure when changing titles or colour scales; matrix-free superoperator analysis needs one exact PI-coordinate application per input coordinate. Young diagrams are a presentation option and do not change or recompute that numerical structure. See Schur-block visualization.\n\npi_density_spectrum, liouvillian_spectrum_data, and floquet_spectrum_data similarly separate spectral computation from presentation. Pass an existing compressed density result, eigenvalue result, Floquet propagator, or multiplier vector whenever possible; repeated visualize_density_spectrum or visualize_spectrum calls then perform no eigensolve or time integration. Density spectra retain exact Schur multiplicities rather than expanding to d^N entries. Partial Krylov data retain their residual/convergence metadata and must not be presented as a certified complete spectrum. See Spectral visualization.\n\nspin_husimi_q and spin_wigner compute one normalized sphere per retained qubit Schur sector. The aggregate grid is explicitly the angular marginal over total spin; use resolved=true only when individual sector grids are needed. Rendering a SpinPhaseSpaceData object never recomputes its transform. See Sector-resolved spin phase space.","title":"Advanced reusable API"},{"category":"section","location":"api_tiers/#Extending-physical-terms","page":"API tiers and prepared analysis","text":"New AbstractPITerm subtypes use qualified dispatch hooks; no change to PIModel or to its Liouvillian compiler is required. The hooks are intentionally not exported, so an extension makes its dependency explicit. For example, this custom collective channel delegates to the supported CollectiveJump lowering:\n\nconst PID = PermutationalInvariantDynamics\n\nstruct MyCollectiveJump{O,R} <: AbstractPITerm\n    operator::O\n    rate::R\nend\n\nPID.term_operator(t::MyCollectiveJump) = t.operator\nPID.term_rate(t::MyCollectiveJump) = t.rate\nPID.body_order(::MyCollectiveJump) = 1\nPID.term_scope(::MyCollectiveJump) = Val(:collective)\nPID.term_process(::MyCollectiveJump) = Val(:jump)\nPID.validate_term(t::MyCollectiveJump, basis::PIBasis) =\n    PID.validate_term(CollectiveJump(t.operator; rate=t.rate), basis)\nPID.compile_term(t::MyCollectiveJump, context) =\n    PID.compile_term(CollectiveJump(t.operator; rate=t.rate), context)\n\ncompile_term must delegate to an equivalent built-in term. Compiled kernel types and their application hooks are private and are not an extension surface. A local sector-changing term must also define PID.required_sectors; a term with a time-dependent operator or rate must define PID.rebuild_term(term, evaluated_operator, evaluated_rate) so that freeze can produce an autonomous term. Incomplete extensions fail during model validation with an explicit error.","title":"Extending physical terms"},{"category":"section","location":"api_tiers/#Experimental-research-API","page":"API tiers and prepared analysis","text":"These routines are useful for specialized studies, but their signatures or algorithms may evolve as the remaining numerical bottlenecks are addressed.\n\nharmonic_arnoldi_spectrum and matrix-free weak-symmetry projection target difficult interior modes. Residuals and spectral scope must be inspected; near-zero ordering is not a certified global-gap search.\nstrong_symmetry_report, strong_symmetry_reduction, and the sector-resolved steady/spectral wrappers provide automatic diagonal strong-symmetry workflows. Binary support discovery has deliberately narrow completeness metadata, and every retained charge is returned explicitly.\nimplicitly_restarted_arnoldi_spectrum (method=:iram) applies exact unwanted Ritz shifts through implicit QR for bounded-memory spectral-edge extraction.\njacobi_davidson_spectrum (method=:jd) and JacobiDavidsonWorkspace provide hard invariant-subspace locking and optional preconditioned inexact correction solves for nonnormal or degenerate near-target clusters.\nsubduction_intertwiners exposes the qudit subduction engine. Its default backend removes forbidden weights, assembles sparse simple-root equations, and obtains the exact prescribed nullity with SPQR; algorithm=:dense remains a small-problem reference. littlewood_richardson_coefficient uses an exact lattice-tableau recursion and constructs no intertwiners.\nPseudospectral and response helpers are intended for moderate PI dimensions and require problem-dependent grid and tolerance convergence.\nPI channels/tomography, portable checkpoints, joint symmetry projectors, implicit steady-state gradients, and checkpointed adjoint control are experimental research utilities. Channel and POVM certificates cover only the retained Schur algebra; control results require grid/Krylov convergence.\nDiffusive plans are immutable and shareable, but each homodyne/heterodyne realization requires a task-owned DiffusiveWorkspace. The normalized Euler–Maruyama step must be converged in dt and is not a finite-step positivity certificate.\nConfidence-controlled quantum-jump and diffusive ensembles use bounded Hermitian observables and simultaneous empirical-Bernstein checks at fixed batch boundaries. Their converged flag covers Monte Carlo sampling only, not integration-step, hierarchy, model, or finite-size convergence.\nordered_local_moments, the versioned neutral cumulant payloads, and the optional QuantumCumulants adapter provide exact finite-N reference data for higher-order closures. The extension can also lower supported microscopic PIModel terms to QuantumCumulants 0.5 indexed equations; direct PI terms and ambiguous schedules are rejected. The selected order remains an approximation and still carries d^(2k) local-tensor storage.\nFinite-exponential bath fitting and bounded least-squares parameter inference report rank, residual, convergence, and model limitations explicitly. Fit convergence is not a hierarchy-depth, pseudomode-cutoff, or global-identifiability certificate.\nWeakPIPseudoKet, WeakPITrajectoryPlan, and the weak_pi_* trajectory functions provide an opt-in direct-sum Schur-irrep unraveling. Fixed local gains are decomposed into checked one-box Kraus branches for qubits and qudits. The pseudo-state and its path statistics depend on this unraveling convention and are not labeled-particle pure states. weak_pi_trajectory_steady_state streams density-valued path means without storing pseudo-ket histories; it returns a generally mixed PIState and its optional path-level uncertainty diagnostics. Fixed-step RK4 and continuous- hazard adaptive/event-driven timing are supported. Operator-valued and local-p-body jumps and composite pseudo-ket paths are not yet part of this surface.\nCompositePIBasis, FiniteOperatorBasis, factorized composite states and operators, CompositeSuperoperator, and CompositeTrajectoryPlan provide deterministic and density-valued quantum-jump dynamics of several PI ensembles and small finite auxiliaries without a global Kronecker superoperator. Monitored tensor-product channels are explicit and use task-owned fixed-step workspaces. Composite pseudo-kets, diffusive/event- driven paths, arbitrary gain maps, and implicit extensions of single-ensemble reduction routines remain outside this surface.\n\nExperimental does not mean “unchecked”: these paths have regression tests. It means that algorithm selection, diagnostic fields, or preallocation interfaces may change as these restarted spectral algorithms and iterative extreme-scale LR construction are exercised on larger research problems.","title":"Experimental research API"},{"category":"section","location":"api_tiers/#Preparing-repeated-observables","page":"API tiers and prepared analysis","text":"OneBodyGeometry contains the CG contractions shared by every local matrix. Its sparse logical cells are stored as packed offsets into contiguous tuple arrays, so qudit caches do not retain one heap vector per empty cell. Use it while preparing several collective observables, then retain only the smaller observable plans:\n\ngeometry = OneBodyGeometry(basis)\nJx = CollectiveObservablePlan(basis, sx/2; cache=geometry)\nJz = CollectiveObservablePlan(basis, sz/2; cache=geometry)\n\nmx = collective_expectation(rho, Jx)\nvx = collective_variance(rho, Jx)\nF = qfim(rho, [sx/2, sz/2]; plans=(Jx, Jz))\n\nA CollectiveObservablePlan retains one physical matrix per Schur sector, roughly sum(dim_nu^2) complex numbers. It does not retain the larger OneBodyGeometry. For a single contraction, pass cache=geometry directly; for many states and one observable, the prepared plan avoids rebuilding the collective blocks as well.\n\none_body_rdm(rho; cache=geometry) contracts every local matrix unit in one geometry traversal. For repeated states, also reuse the largest-sector scratch and output:\n\none_body_work = OneBodyRDMWorkspace(geometry, rho)\nrho_one = zeros(ComplexF64, basis.d, basis.d)\none_body_rdm!(rho_one, rho, one_body_work)\n\nThis is especially useful for repeated qudit marginals. If an enclosing workflow has already called validate_state, it may select check=false on the in-place call.","title":"Preparing repeated observables"},{"category":"section","location":"api_tiers/#Preparing-repeated-reductions","page":"API tiers and prepared analysis","text":"For a fixed particle bipartition, prepare its product-Schur data once:\n\nreduction = ReductionPlan(basis, k)\nwork = ReductionWorkspace(reduction, rho)\n\nrhoA = reduced_state(rho, k; plan=reduction, workspace=work)\npA = reduced_purity(rho, k; plan=reduction, workspace=work)\nneg = negativity(rho, k; plan=reduction, workspace=work)\npt = partial_transpose_spectrum(rho, k; plan=reduction)\n\n# Reuse the returned state as well as all contraction scratch.\nrhoA_buffer = PIState(reduction.output_basis)\nreduced_state!(rhoA_buffer, rho, reduction, work)\n\nFor qubits, the plan stores multiplicity-free SU(2) recoupling matrices. For qudits, it stores every required Littlewood–Richardson intertwiner as exact-support CSC blocks split by discarded-factor weight. Reuse removes nearly all repeated subduction setup, while plan.estimates reports packed and dense-equivalent sizes. Qudit construction still performs sparse rank-revealing factorizations and may have a large temporary dense nullspace basis. Construct one plan per exact (basis,k), benchmark its setup and retained size, and avoid retaining plans for bipartitions that are used only once.\n\nFor a sequence of subsystem sizes, ReductionPlanSet shares the qubit factorial table or qudit Littlewood–Richardson construction caches. ReductionWorkspaceSet is task-owned and lets reduced_states, reduced_purities, and bipartition_negativities validate the parent state once. Its setup report distinguishes shared_lr_intertwiner_count (the number of cached intertwiner matrices) from shared_lr_intertwiner_entries (their total scalar-entry count).\n\nReductionPlan remains immutable and may be shared between tasks. ReductionWorkspace owns mutable multiplication, partial-transpose, and reduced-block buffers plus recoupling data matched once to its working scalar type. Matching qudit workspaces share immutable packed blocks; wider workspaces copy only their nonzeros. The reduced-state path evaluates tr_B(U*C*U') = sum_q U_q*C*U_q' with one discarded-factor slice, so mode=:reduction omits the complete product block. Negativity retains that block because it must partially transpose it. Compact real qubit matrices are copied into a complex workspace. Use one workspace per task. reduced_state still allocates its returned PIState, whereas reduced_state! also reuses caller-owned output. Spectral routines necessarily allocate their returned eigenvalue arrays.","title":"Preparing repeated reductions"},{"category":"section","location":"api_tiers/#Validation-before-analysis","page":"API tiers and prepared analysis","text":"report = state_diagnostics(rho; atol=1e-12, rtol=1e-10)\nvalidate_state(rho; trace_one=true, hermitian=true, positive=true)\n\nValidation never normalizes, symmetrizes, or clips the input. Analysis functions may average only skew-Hermitian components already shown to be within the requested tolerance.","title":"Validation before analysis"},{"category":"section","location":"api_tiers/#Curated-namespace","page":"API tiers and prepared analysis","text":"Workflow includes the complete prepared-geometry lifecycle—validation, accessors, explicit cache management—and the multi-bipartition reduction consumers. A script can therefore stay within the curated namespace while preparing and reusing these immutable plans.","title":"Curated namespace"},{"category":"section","location":"api_tiers/#Curated-model-recipes","page":"API tiers and prepared analysis","text":"","title":"Curated model recipes"},{"category":"module","location":"api_tiers/#PermutationalInvariantDynamics.Workflow","page":"API tiers and prepared analysis","text":"Workflow\n\nCurated, stable entry points for constructing, solving, validating, and analyzing PI models. The parent module retains the complete expert API; Workflow is intentionally smaller and is the recommended namespace for new research scripts whose imports should remain easy to audit.\n\nThe namespace contains no wrappers or mutable state. Its bindings are the same functions and types exported by PermutationalInvariantDynamics, so objects move between the curated and expert interfaces without conversion.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.Workflow"},{"category":"module","location":"api_tiers/#PermutationalInvariantDynamics.Models","page":"API tiers and prepared analysis","text":"Models\n\nCurated, convention-explicit model recipes built from the stable PI term API. These constructors are the single source used by flagship documentation and are intended to prevent literature examples from carrying subtly different normalizations of the same model.\n\nUse Models.catalog() for machine-readable discovery.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.Models"},{"category":"type","location":"api_tiers/#PermutationalInvariantDynamics.AffineCompiledPIModelFamily","page":"API tiers and prepared analysis","text":"AffineCompiledPIModelFamily\n\nPrepared family for a Liouvillian which is affine in named real parameters,\n\nmathcal L(theta)=mathcal L_0+sum_atheta_amathcal L_a\n\nEach component is expressed with ordinary physical PI terms.  The complete term geometry is lowered once through compile_family; parameter specialization only forms the scalar rates required by the already prepared kernels.  The object is immutable and may be shared between tasks.\n\nThe affine coefficients multiply complete generator contributions.  For a jump operator written as a * L, supply abs2(a) as the coefficient of the D[L] component.  A parameter-dependent Kossakowski matrix can be represented as a sum of fixed positive-semidefinite correlated-jump components when that decomposition is physically appropriate.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.AffineCompiledPIModelFamily"},{"category":"function","location":"api_tiers/#PermutationalInvariantDynamics.compile_affine_family","page":"API tiers and prepared analysis","text":"compile_affine_family(base_model, components;\n                      defaults=nothing,\n                      bigfloat_precision=precision(BigFloat),\n                      coefficient_cache=nothing)\n\nPrepare a fixed affine generator family. base_model supplies mathcal L_0. components is a nonempty NamedTuple; each value is a PIModel on the exact same basis, one AbstractPITerm, or a tuple/vector of terms.  A component's scalar parameter multiplies every prototype term rate in that component.\n\nAll operators and prototype rates must be autonomous.  Negative specialized coefficients are accepted because deterministic time-local generators may have negative rates; stochastic use must perform its own nonnegative-rate validation.  Geometry, selection rules, and output precision are identical to compiling the explicitly specialized model.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.compile_affine_family"},{"category":"type","location":"api_tiers/#PermutationalInvariantDynamics.ThreadedMatrixFreeLiouvillian","page":"API tiers and prepared analysis","text":"ThreadedMatrixFreeLiouvillian\n\nPrepared matrix-free wrapper which applies one PI Liouvillian action with deterministic Schur-sector task parallelism. Construct it with threaded_matrixfree. The immutable source and sector assignment may be shared for read access, but the wrapper owns one mutable ThreadedLiouvillianWorkspace and therefore serializes public applications. Krylov solvers call it sequentially and each application uses the configured task team internally.\n\nThis wrapper is intended for large mixed-sector actions. It does not change BLAS thread settings and is not automatically preferable to the ordinary matrix-free route for small blocks.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.ThreadedMatrixFreeLiouvillian"},{"category":"function","location":"api_tiers/#PermutationalInvariantDynamics.threaded_matrixfree","page":"API tiers and prepared analysis","text":"threaded_matrixfree(source; tasks=Threads.nthreads())\n\nWrap an autonomous or driven prepared LiouvillianPlan, CompiledPIModel, or SpecializedPIModel in a matrix-free operator whose vector actions use Schur-sector task parallelism. A driven wrapper supports explicit apply! calls; autonomous-only Krylov operations such as mul!, stationary_state, and liouvillian_spectrum retain their ordinary autonomy checks.\n\nThe returned operator owns fixed-capacity worker scratch and is safe to share between callers only through its compatibility lock. Concurrent hot loops should construct one wrapper per task.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.threaded_matrixfree"},{"category":"type","location":"api_tiers/#PermutationalInvariantDynamics.ReductionPlanSet","page":"API tiers and prepared analysis","text":"ReductionPlanSet\n\nImmutable collection of ReductionPlan objects for several particle bipartitions of one exact PIBasis. Setup shares the complete SU(2) factorial table for qubits and the Littlewood–Richardson generator/intertwiner caches for qudits. This avoids repeating the dominant representation setup in entanglement-scaling studies.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.ReductionPlanSet"},{"category":"type","location":"api_tiers/#PermutationalInvariantDynamics.ReductionWorkspaceSet","page":"API tiers and prepared analysis","text":"ReductionWorkspaceSet(plan_set, rho; mode=:both)\n\nTask-owned numerical scratch matching every plan in a ReductionPlanSet. The workspace set must not be shared concurrently; the immutable plan set may be shared read-only.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.ReductionWorkspaceSet"},{"category":"function","location":"api_tiers/#PermutationalInvariantDynamics.reduction_plan","page":"API tiers and prepared analysis","text":"Return the prepared plan for subsystem size k.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.reduction_plan"},{"category":"function","location":"api_tiers/#PermutationalInvariantDynamics.reduced_states","page":"API tiers and prepared analysis","text":"reduced_states(rho, plan_set; workspace=nothing, check=true, kwargs...)\n\nCompute all requested reduced density matrices while validating the parent state once. A supplied ReductionWorkspaceSet reuses every numerical buffer.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.reduced_states"},{"category":"function","location":"api_tiers/#PermutationalInvariantDynamics.Models.catalog","page":"API tiers and prepared analysis","text":"Return detached metadata for the curated built-in model recipes.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.Models.catalog"},{"category":"function","location":"api_tiers/#PermutationalInvariantDynamics.Models.driven_qubits","page":"API tiers and prepared analysis","text":"driven_qubits(N; drive=0.7, detuning=0, emission=0.12,\n              pumping=0.02, dephasing=0, sectors=nothing, T=Float64)\n\nStandard complete-PI qubit ensemble in local order (|g>,|e>), with H = drive*jx + detuning*jz summed over particles and independent Lindblad channels. Rates use the package convention D[L]ρ=LρL†-{L†L,ρ}/2.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.Models.driven_qubits"},{"category":"function","location":"api_tiers/#PermutationalInvariantDynamics.Models.independent_dephasing","page":"API tiers and prepared analysis","text":"independent_dephasing(N; gamma=1, sectors=nothing, T=Float64)\n\nIndependent dephasing normalized so one-qubit coherence decays as exp(-gamma*t).\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.Models.independent_dephasing"},{"category":"function","location":"api_tiers/#PermutationalInvariantDynamics.Models.local_pump_decay","page":"API tiers and prepared analysis","text":"local_pump_decay(N; down=1, up=0.25, sectors=nothing, T=Float64)\n\nIndependent emission and pumping with the exact product stationary excited population up/(down+up) when down+up>0.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.Models.local_pump_decay"},{"category":"function","location":"api_tiers/#PermutationalInvariantDynamics.Models.one_axis_twisting","page":"API tiers and prepared analysis","text":"one_axis_twisting(N; chi=1, sectors=nothing, T=Float64)\n\nKitagawa–Ueda Hamiltonian H=chi*Jz^2. The complete PI basis is retained by default so this recipe can be combined safely with later local channels.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.Models.one_axis_twisting"},{"category":"function","location":"api_tiers/#PermutationalInvariantDynamics.Models.steady_superradiance","page":"API tiers and prepared analysis","text":"steady_superradiance(N; collective_decay=1, pump=1, T=Float64)\n\nMeiser–Holland collective emission with independent incoherent pumping. Every Schur sector is retained because the local pump changes total spin.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.Models.steady_superradiance"},{"category":"function","location":"api_tiers/#PermutationalInvariantDynamics.Models.boundary_time_crystal","page":"API tiers and prepared analysis","text":"boundary_time_crystal(N; omega=1.5, kappa=1, T=Float64)\n\nIemini et al. symmetric-sector boundary-time-crystal generator with H=omega*Jx and package-convention collective rate 2kappa/N.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.Models.boundary_time_crystal"},{"category":"section","location":"correlations/#Quantum-regression-and-optical-correlations","page":"Quantum regression and spectra","text":"The quantum regression backend evolves operator-inserted PI states with the same compiled Liouvillian used for density-matrix dynamics. It remains in the PI coefficient space and does not construct a d^N state, a d^2N superoperator, or a dense Liouvillian.","title":"Quantum regression and optical correlations"},{"category":"section","location":"correlations/#Convention","page":"Quantum regression and spectra","text":"For a stationary or specified initial PI state rho, the two-operator function is\n\nC_AB(tau)=mathrmtrleft\nA e^mathcal Ltau(Brho R)right\n\nwhere the optional right insertion R is omitted by default. The first operator is not implicitly adjointed. For example, the optical first-order correlation langle c^dagger(tau)c(0)rangle uses A=adjoint(c) and B=c.\n\nThis explicit convention matters because the general package operation expectation(rho, A) is mathrmtr(A^daggerrho). Internally the correlation readout is arranged so that it instead evaluates exactly mathrmtr(Arho_rm conditional).","title":"Convention"},{"category":"section","location":"correlations/#Prepared-workflow","page":"Quantum regression and spectra","text":"prepared = compile(model; backend=:matrixfree)\nplan = CorrelationPlan(prepared, A, B)\nworkspace = CorrelationWorkspace(plan; krylovdim=40)\n\nvalues = two_time_correlation(\n    plan, rho, delays;\n    steps_per_interval=64,\n    workspace=workspace,\n)\n\nCorrelationPlan is immutable and owns copied, read-only physical Schur blocks for the insertions. CorrelationWorkspace owns the conditional state, block-product scratch, one EvolutionWorkspace, and shifted-GMRES storage. A plan may be shared between tasks; a workspace must be owned by one task and reused only sequentially.\n\nFor time-domain work without resolvent spectra, omit the shifted-GMRES basis:\n\ntime_workspace = CorrelationWorkspace(plan; mode=:time)\nvalues = two_time_correlation(\n    plan, rho, delays; workspace=time_workspace)\n\nThe default mode=:both remains appropriate when the same workspace will also be passed to stationary_correlation_spectrum. A time-only workspace is also accepted by the sampled correlation_spectrum_fft route; it is rejected explicitly by the shifted-GMRES spectrum rather than growing behind the caller's back.\n\nThe representation and insertion are exact within the retained PI basis. The default time-domain integrator is numerical fixed-step RK4. Increase steps_per_interval and verify convergence when quantitative integration error matters.\n\nTime and frequency arithmetic follows the prepared plan's real precision. A matching delay vector is reused directly; a narrower vector is converted once to the plan precision. Wider floating delays, frequencies, and explicit tolerances are rejected instead of being silently narrowed, while integer inputs must be exactly representable.","title":"Prepared workflow"},{"category":"section","location":"correlations/#Delayed-intensity-correlations","page":"Quantum regression and spectra","text":"For a PI lowering or output operator c, delayed_second_order_correlation evaluates\n\nG^(2)(tau)=mathrmtrleft\nc^dagger ce^mathcal Ltau(crho c^dagger)right\n\nWith normalized=true, it returns g^(2)(tau)=G^(2)(tau)I^2, where I=mathrmtr(c^dagger crho). This normalization is stationary: the routine validates unit trace and stationarity, and raises when I=0. Choose normalized=false for a nonstationary or unnormalized result.","title":"Delayed intensity correlations"},{"category":"section","location":"correlations/#Infinite-time-and-sampled-spectra","page":"Quantum regression and spectra","text":"stationary_correlation_spectrum evaluates\n\nS_AB(omega)=int_0^infty e^-iomegatau\nC^rm conn_AB(tau)dtau\n\nthrough matrix-free shifted GMRES. The exact stationary product is subtracted from the conditional seed. A rank-one trace constraint regularizes the zero mode without changing the trace-zero connected solution. The function returns the complex one-sided transform: it does not apply a factor two or discard the imaginary part. The disconnected stationary spectrum contains a Dirac delta, so requesting it as an ordinary resolvent function is rejected.\n\nFor a frequency grid, the default solver=:auto groups shifts into bounded batches and solves them from one shared Arnoldi factorization. Columns that do not meet the requested full-space residual are retried with restarted single-shift GMRES. Set solver=:sequential for the historical path or solver=:multishift to require shared-Arnoldi convergence without fallback. shared_memory_budget and multishift_batchsize bound live solution storage; the result reports shared_batches and fallback_frequencies. Repeated calls may pass a compatible MultiShiftGMRESWorkspace. The supplied workspace's fixed nshifts is the batch size. Automatic mode handles a final short remainder sequentially; forced multi-shift mode requires an exact number of full batches.\n\noptical_spectrum(L, rho, c, frequencies) is the convenience form with A=c^dagger and B=c.\n\nFor a finite uniformly sampled record, correlation_spectrum_fft provides a dependency-free radix-two FFT. It uses the same e^-iomegatau sign, trapezoidal endpoint weights, and optional zero padding. Its result is a finite-window integral. When called with a plan and a stationary state, it subtracts the exact stationary offset instead of guessing it from a tail sample.\n\nSee the runnable quantum-regression example for a pumped emitter with analytical time-domain, antibunching, and spectral checks.","title":"Infinite-time and sampled spectra"},{"category":"section","location":"correlations/#API","page":"Quantum regression and spectra","text":"","title":"API"},{"category":"type","location":"correlations/#PermutationalInvariantDynamics.CorrelationPlan","page":"Quantum regression and spectra","text":"CorrelationPlan(L, A, B; right=nothing)\n\nPrepare the quantum-regression correlation\n\nC_AB(tau)=mathrmtrleftAe^mathcal Ltau\n    (Brho R)right\n\nwhere R is the optional right operator (the identity when omitted). L may be a PIModel, compiled model, matrix-free Liouvillian, plan, or matrix.  A model is compiled with the matrix-free backend.  The generator must be autonomous; explicitly driven generators should first be frozen at the desired time.\n\nThe Schur blocks of the insertion operators and the readout vector are copied once into the immutable plan.  Numerical scratch is deliberately excluded; construct one CorrelationWorkspace per task.\n\nThe first operator is not implicitly adjointed: the returned contraction is exactly tr(A * ...).  Thus the standard optical first-order correlation is obtained with A=adjoint(c) and B=c.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.CorrelationPlan"},{"category":"type","location":"correlations/#PermutationalInvariantDynamics.CorrelationWorkspace","page":"Quantum regression and spectra","text":"CorrelationWorkspace(plan; krylovdim=30, mode=:both)\n\nCaller-owned scratch for time-domain quantum regression and matrix-free shifted-GMRES spectra.  A workspace is tied to one plan and may be reused sequentially, but must not be shared concurrently between tasks.\n\nThe default mode=:both preserves the complete time- and frequency-domain workspace.  Use mode=:time when only two_time_correlation, delayed_second_order_correlation, or the sampled correlation_spectrum_fft path is required.  The time-only mode omits the two frequency-domain vectors and the restarted-GMRES basis; passing it to stationary_correlation_spectrum raises instead of allocating that storage lazily.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.CorrelationWorkspace"},{"category":"function","location":"correlations/#PermutationalInvariantDynamics.two_time_correlation","page":"Quantum regression and spectra","text":"two_time_correlation(plan, rho, delays; kwargs...)\ntwo_time_correlation(L, rho, A, B, delays; right=nothing, kwargs...)\n\nReturn the quantum-regression correlation in the exact PI representation, tr(A * exp(L*tau) * (B*rho*R)).  The convenience form prepares a plan; reuse a CorrelationPlan and CorrelationWorkspace for repeated calls.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.two_time_correlation"},{"category":"function","location":"correlations/#PermutationalInvariantDynamics.two_time_correlation!","page":"Quantum regression and spectra","text":"two_time_correlation!(destination, plan, rho, delays;\n                      steps_per_interval=64, workspace=nothing)\n\nEvaluate tr(A * exp(L*tau) * (B*rho*R)) in the exact PI representation at ordered nonnegative delays, using the quantum-regression theorem and one preallocated numerical RK4 evolution workspace.  No Liouvillian matrix is materialized.  destination may be a complex vector of any scalar type that can represent the plan output. A delay vector in the plan's real precision is reused without allocation; narrower inputs are converted once so integration still uses the plan precision. Wider floating inputs and nonrepresentable integers raise instead of narrowing silently. Plans are autonomous by construction, so every RK4 stage deliberately uses parameters=nothing.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.two_time_correlation!"},{"category":"function","location":"correlations/#PermutationalInvariantDynamics.delayed_second_order_correlation","page":"Quantum regression and spectra","text":"delayed_second_order_correlation(L, rho, c, delays;\n                                 normalized=true, kwargs...)\n\nCompute\n\nG^(2)(tau)=mathrmtrleftc^dagger c\ne^mathcal Ltau(crho c^dagger)right\n\nand, by default, return g2 = G2 / tr(c'c*rho)^2.  This normalization is the stationary one, so normalized=true verifies that rho is trace one and stationary.  A zero stationary intensity makes the normalized quantity undefined and raises DomainError; the unnormalized, possibly nonstationary function remains available with normalized=false.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.delayed_second_order_correlation"},{"category":"function","location":"correlations/#PermutationalInvariantDynamics.second_order_correlation","page":"Quantum regression and spectra","text":"Alias for delayed_second_order_correlation.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.second_order_correlation"},{"category":"function","location":"correlations/#PermutationalInvariantDynamics.stationary_correlation_spectrum","page":"Quantum regression and spectra","text":"stationary_correlation_spectrum(plan, rho, frequencies;\n    connected=true, krylovdim=nothing, maxiter=500, atol=nothing,\n    rtol=nothing, workspace=nothing, solver=:auto,\n    shared_memory_budget=64*1024^2, multishift_batchsize=nothing,\n    multishift_workspace=nothing)\n\nEvaluate the one-sided stationary spectrum\n\nS_AB(omega)=int_0^infty e^-iomegatau\n C^mathrmconn_AB(tau)dtau\n\nwith a matrix-free shifted-GMRES solve of (im*omega*I - L)x = B*rho*R - rho*tr(B*rho*R).  The optional connected subtraction is exact and removes the stationary delta contribution; no late-time estimate is used.  A rank-one trace constraint makes the zero frequency solve nonsingular without changing the trace-zero solution.\n\nOnly the connected spectrum is an ordinary function: without subtraction the stationary component is a Dirac delta distribution.  Consequently connected=false is rejected instead of returning a misleading regularized number.\n\nThe return value is a named tuple containing frequencies, complex values, GMRES diagnostics, connected, and convention=:one_sided_exp_minus_iomega_t. No factor 2 or real part is applied; users needing a conventional two-sided Hermitian spectrum may form 2real.(values). solver=:auto shares one Arnoldi factorization across compatible frequency shifts in bounded batches, then retries only unconverged columns with the restarted single-shift solver. solver=:sequential selects the historical path, while solver=:multishift requires every shared-Arnoldi solve to converge. shared_memory_budget bounds the additional shared workspace and solution block; an insufficient automatic budget falls back to sequential solves. A compatible MultiShiftGMRESWorkspace may be supplied for repeated calls. Its fixed nshifts sets the batch size: :auto sends a final short remainder through the sequential solver, while :multishift requires the frequency count to be an exact multiple.\n\nFrequencies and explicit tolerances must be representable without narrowing in the plan's real precision; nonrepresentable integer frequencies also raise.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.stationary_correlation_spectrum"},{"category":"function","location":"correlations/#PermutationalInvariantDynamics.optical_spectrum","page":"Quantum regression and spectra","text":"optical_spectrum(L, rho, c, frequencies; kwargs...)\noptical_spectrum(plan, rho, frequencies; kwargs...)\n\nConvenience stationary emission spectrum using A=adjoint(c), B=c.  The result follows the one-sided exp(-im*omega*tau) convention of stationary_correlation_spectrum. The plan overload permits reuse of an optical CorrelationPlan and its workspace without constructing another plan.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.optical_spectrum"},{"category":"function","location":"correlations/#PermutationalInvariantDynamics.correlation_spectrum_fft","page":"Quantum regression and spectra","text":"correlation_spectrum_fft(delays, correlations;\n                         offset=0, nfft=nothing)\n\nCompute the finite-time one-sided transform\n\nS_T(omega)=int_0^T e^-iomegatau\n    C(tau)-C_inftydtau\n\nfrom a uniform delay grid using trapezoidal endpoint weights and an in-place radix-two FFT implemented without an additional dependency.  offset is the known C_infinity; it is never estimated from the final sample.  nfft defaults to the next power of two and may request additional zero padding.\n\nThe returned frequencies are angular frequencies in ascending order.  The complex values use exp(-im*omega*tau), contain no factor two or real-part projection, and approximate a finite-window integral rather than the infinite-time resolvent.\n\n\n\n\n\ncorrelation_spectrum_fft(plan, rho, delays; connected=true,\n                         workspace=nothing, kwargs...)\n\nPropagate a stationary QRT correlation on delays and transform it with correlation_spectrum_fft.  With connected=true, the exact stationary product tr(A*rho)*tr(B*rho*R) is subtracted before the FFT. rho is checked for unit trace and stationarity; no tail sample is used as a surrogate for the infinite-time value.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.correlation_spectrum_fft"},{"category":"section","location":"bath_fitting/#Bath-fitting-and-hierarchy-preparation","page":"Bath-correlation fitting","text":"PI–HEOM and PI–HOPS consume a finite exponential correlation,\n\nC(t)=sum_k=1^K c_k e^-nu_k t\nqquad mathrmRenu_k0\n\nThe bath-preparation workflow turns either correlation samples or a sampled spectral density into this representation. It deliberately keeps three convergence questions separate:\n\nconvergence of the frequency quadrature and spectral cutoff;\nresidual and identifiability of the finite exponential fit;\nconvergence of the HEOM/HOPS hierarchy, time step, and trajectory count.\n\nA small fitting residual proves only item 2.","title":"Bath fitting and hierarchy preparation"},{"category":"section","location":"bath_fitting/#Fit-correlation-samples","page":"Bath-correlation fitting","text":"If physically motivated poles are known, fitting only their coefficients is the most reliable route:\n\ntimes = range(0.0, 12.0; length=401)\ncorrelation = measured_or_analytic_correlation.(times)\npoles = ComplexF64[\n    0.35,\n    0.8 + 1.2im,\n    0.8 - 1.2im,\n]\n\nfit = fit_bath_correlation(\n    times,\n    correlation;\n    poles,\n    weights=ones(length(times)),\n    rtol=1e-7,\n)\n\nfit.report.converged\nfit.report.relative_residual\nfit.report.condition_number\nfit.report.identifiable\n\nThe fit is linear in the coefficients. No nonlinear optimizer, random initialization, or unbounded search is hidden in this path.\n\nIf the poles are not known, provide a finite candidate set and a maximum term count:\n\ncandidates = ComplexF64[\n    decay + im * frequency\n    for decay in exp10.(range(-2, 1; length=20))\n    for frequency in range(-3, 3; length=25)\n]\n\nfit = fit_bath_correlation(\n    times,\n    correlation;\n    candidate_poles=candidates,\n    nterms=8,\n    rtol=1e-5,\n    memory_budget=512 * 1024^2,\n)\n\nThis uses deterministic residual-minimizing forward selection followed by a final weighted least-squares solve. Candidate count and retained dense storage are checked against memory_budget. Supplying neither pole keyword constructs a bounded positive-real grid; that convenience is intended for nonoscillatory correlations.\n\nThe following fields deserve inspection before hierarchy construction:\n\nconverged: the requested weighted residual was reached;\nidentifiable: the selected exponential columns have full numerical rank;\ncondition_number: local coefficient sensitivity;\nstable: every pole has positive real decay;\nhops_stationary_ou_compatible: coefficients work with the built-in stationary Ornstein–Uhlenbeck HOPS path.\n\nAn explicit ridge adds Tikhonov regularization. Its value is never selected automatically, and the report records it.\n\nThe fitting precision is promoted from the samples, poles, weights, and explicit floating-point ridge, atol, rtol, and rank_rtol values. Thus a Float64 fitting parameter is never silently narrowed into a Float32 fit. Leaving rtol=nothing selects 1e-6 directly in the inferred fit precision, so the default does not widen otherwise-Float32 data.","title":"Fit correlation samples"},{"category":"section","location":"bath_fitting/#Start-from-a-spectral-density","page":"Bath-correlation fitting","text":"For the convention\n\nC(t)=frac1piint_0^infty J(omega)\nleft\ncothleft(fracbetaomega2right)cos(omega t)\n-isin(omega t)\nrightdomega \n\nprepare finite trapezoidal samples first:\n\nomega = range(1e-4, 20.0; length=20_001)\nJ = @. 2lambda * gamma * omega / (gamma^2 + omega^2)\ntimes = range(0.0, 12.0; length=401)\n\nsamples = correlation_from_spectral_density(\n    omega,\n    J,\n    times;\n    inverse_temperature=beta,\n)\n\nfit = fit_bath_correlation(\n    samples;\n    candidate_poles=candidates,\n    nterms=8,\n    rtol=1e-5,\n)\n\nThe frequency grid must be strictly positive. At zero frequency the limit of J(omega)coth(betaomega2) is model dependent, so the routine refuses to invent it. Refine the spacing, upper cutoff, and time window independently. Every thermal factor, integrand, weighted contribution, accumulator, and final quadrature value is checked for finite arithmetic. Overflow or nonzero underflow raises with rescaling/wider-precision guidance.\n\nfit_bath_from_spectral_density combines these two calls while keeping their options in separate named tuples:\n\nfit = fit_bath_from_spectral_density(\n    omega,\n    J,\n    times;\n    correlation_options=(inverse_temperature=beta,),\n    fit_options=(\n        candidate_poles=candidates,\n        nterms=8,\n        rtol=1e-5,\n    ),\n)","title":"Start from a spectral density"},{"category":"section","location":"bath_fitting/#Construct-HEOM-or-HOPS-baths","page":"Bath-correlation fitting","text":"For a Hermitian PI coupling:\n\nbasis = PIBasis(N, 2)\ncoupling = collective_operator(basis, spin_matrices().jz)\n\nheom_bath = prepare_heom_bath(coupling, fit)\nheom_plan = HEOMPlan(model, heom_bath; max_depth=4)\n\nprepare_heom_bath rejects an unconverged or rank-deficient fit by default. The explicit accept_unconverged=true and accept_rank_deficient=true switches are intended for controlled convergence studies, not as automatic repairs.\n\nHOPS preparation follows the same contract:\n\nhops_bath = prepare_hops_bath(\n    coupling,\n    fit;\n    require_stationary_ou=true,\n)\nhops_plan = HOPSPlan(hamiltonian, hops_bath; max_depth=4)\n\nrequire_stationary_ou=true is appropriate when using the built-in stationary noise generator. General signed or complex decompositions need an explicit covariance-correct noise provider.\n\nThe bath fit stores no hierarchy workspace, does not construct a full d^N state, and does not cache mutable process-global data. Known-length input iterators are memory-preflighted before they are consumed. Unknown-length iterators are collected under an incremental bound. Raw input copies, converted arrays, dense fitting work, and returned fit arrays are included conservatively in estimated_peak_bytes.","title":"Construct HEOM or HOPS baths"},{"category":"section","location":"bath_fitting/#API","page":"Bath-correlation fitting","text":"","title":"API"},{"category":"type","location":"bath_fitting/#PermutationalInvariantDynamics.BathCorrelationSamples","page":"Bath-correlation fitting","text":"BathCorrelationSamples\n\nFinite samples of a stationary bath correlation function. times are nonnegative and strictly increasing, values[j] is the correlation at times[j], and metadata records how the samples were prepared. The object does not claim that a time/frequency quadrature or finite time window has converged.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.BathCorrelationSamples"},{"category":"type","location":"bath_fitting/#PermutationalInvariantDynamics.BathFitResult","page":"Bath-correlation fitting","text":"BathFitResult\n\nResult of fitting a finite exponential correlation\n\nC(t)=sum_k c_kexp(-nu_k t)\n\nThe vectors are detached from the inputs. report.converged refers only to the requested weighted sample residual; it is not a hierarchy-depth, time-step, or spectral-quadrature certificate.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.BathFitResult"},{"category":"function","location":"bath_fitting/#PermutationalInvariantDynamics.correlation_from_spectral_density","page":"Bath-correlation fitting","text":"correlation_from_spectral_density(frequencies, spectral_density, times;\n                                  inverse_temperature=Inf,\n                                  normalization=inv(pi),\n                                  memory_budget=512MiB)\n\nCompute trapezoidal samples of the bosonic correlation convention\n\nC(t)=aint_0^infty J(omega)\n leftcothleft(fracbetaomega2right)cos(omega t)\n       -isin(omega t)rightdomega \n\nwhere a=normalization. The supplied frequency grid must be finite, strictly positive, and increasing, and the spectral density samples must be finite, real, and nonnegative. Strict positivity avoids silently inventing the model-dependent omega -> 0 limit of J(omega)coth(beta*omega/2).\n\nThe returned metadata records the finite integration interval and quadrature. It is not a quadrature-convergence certificate; refine the frequency grid and cutoff separately from the subsequent exponential fit.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.correlation_from_spectral_density"},{"category":"function","location":"bath_fitting/#PermutationalInvariantDynamics.fit_bath_correlation","page":"Bath-correlation fitting","text":"fit_bath_correlation(times, values;\n                     poles=nothing, candidate_poles=nothing,\n                     nterms=4, weights=nothing, ridge=0,\n                     atol=0, rtol=nothing, rank_rtol=nothing,\n                     memory_budget=512MiB)\n\nFit sampled complex correlation data to a stable finite exponential sum.\n\nWith poles, only the coefficients are fitted by weighted least squares. With candidate_poles, deterministic residual-minimizing forward selection retains at most nterms poles before a final least-squares solve. If neither is supplied, a bounded logarithmic grid of positive real poles is generated; oscillatory data should normally provide a physically motivated complex candidate grid.\n\nThe operation is finite and memory guarded. ridge is an explicit Tikhonov coefficient and is never chosen automatically. The returned report exposes the residual, numerical rank, condition number, selected candidates, and whether the coefficients are compatible with the built-in stationary Ornstein–Uhlenbeck HOPS noise (real(c_k) >= 0 and no imaginary parts).\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.fit_bath_correlation"},{"category":"function","location":"bath_fitting/#PermutationalInvariantDynamics.fit_bath_from_spectral_density","page":"Bath-correlation fitting","text":"fit_bath_from_spectral_density(frequencies, spectral_density, times;\n                               correlation_options=(;),\n                               fit_options=(;))\n\nPrepare finite-temperature correlation samples with correlation_from_spectral_density, then fit them with fit_bath_correlation. Spectral quadrature and exponential-fit options are deliberately separate so their convergence claims cannot be confused.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.fit_bath_from_spectral_density"},{"category":"function","location":"bath_fitting/#PermutationalInvariantDynamics.prepare_heom_bath","page":"Bath-correlation fitting","text":"prepare_heom_bath(coupling, fit; right_coefficients=nothing, residue=0,\n                  accept_unconverged=false,\n                  accept_rank_deficient=false, metadata=(;), kwargs...)\n\nConstruct an HEOMBath from a finite exponential fit. By default an unconverged or rank-deficient fit is rejected rather than silently promoted to a dynamical model. Fit provenance and diagnostics are attached to the bath metadata. Remaining kwargs are forwarded to HEOMBath.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.prepare_heom_bath"},{"category":"function","location":"bath_fitting/#PermutationalInvariantDynamics.prepare_hops_bath","page":"Bath-correlation fitting","text":"prepare_hops_bath(coupling, fit; label=:fitted_bath,\n                  require_stationary_ou=false,\n                  accept_unconverged=false,\n                  accept_rank_deficient=false)\n\nConstruct a HOPSBath from a finite exponential fit. Set require_stationary_ou=true when the built-in stationary Ornstein–Uhlenbeck path will be used; complex or negative fitted residues are then rejected. A general HOPS decomposition may still be used with an explicit covariance-correct noise provider.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.prepare_hops_bath"},{"category":"section","location":"weak_pi_trajectories/#Weak-PI-Schur-pseudo-ket-trajectories","page":"Weak-PI pseudo-ket trajectories","text":"The weak-PI trajectory backend is an opt-in pure-state unraveling inside the direct sum of retained Schur irreps,\n\nmathcal H_rm weak=bigoplus_nu U_nu\nqquad\npsirangle=bigoplus_nupsi_nurangle\n\nIts dimension is sum_nudim U_nu. This is generally much smaller than both the PI density-coordinate dimension sum_nu(dim U_nu)^2 and the full labeled-particle dimension d^N. Production code constructs neither a d^N ket nor a d^Ntimes d^N operator.\n\nRelated literature includes Elliot W. Lloyd, Aleksandra A. Ziolkowska, and Jonathan Keeling, Permutation-symmetric quantum trajectories, arXiv:2605.11103 (2026), which develops weak-permutation-symmetric stochastic unravelings for emitters coupled to a common system such as a cavity. The single-ensemble backend documented here implements Schur-sector pseudo-ket trajectories; the distinction from the shared-system cases in that work is stated under scope and convergence.","title":"Weak-PI Schur pseudo-ket trajectories"},{"category":"section","location":"weak_pi_trajectories/#What-the-pseudo-ket-means","page":"Weak-PI pseudo-ket trajectories","text":"A WeakPIPseudoKet maps to a physical PI density state through\n\nC_nu=fracpsi_nuranglelanglepsi_nusqrtf^nu\nqquad\nrho_nu=fracC_nusqrtf^nu\n\nConsequently sum_nulanglepsi_nupsi_nurangle=1 is the physical trace condition. Relative phases between different nu sectors disappear from every PI density operator and observable; they are not physical coherences. A weak-PI pseudo-ket is therefore not a pure state of N labeled particles. It is also distinct from the density-valued conditional state returned by quantum_trajectory.\n\nweak_pi_pseudoket(rho) accepts only states whose multiplicity-weighted block sqrtf^nuC_nu has rank at most one in every occupied sector. A general mixed state is rejected rather than purified or approximated silently. weak_pi_density(psi) performs the reverse conversion.","title":"What the pseudo-ket means"},{"category":"section","location":"weak_pi_trajectories/#Sector-changing-local-Kraus-branches","page":"Weak-PI pseudo-ket trajectories","text":"For a fixed one-particle local channel L, the PI gain map between input sector nu and output sector lambda factorizes over common one-box children muvdash N-1:\n\nG_lambdanu(C_nu)\n=sum_mu s_lambdamunu\n A_lambdamunu C_nu\n A_lambdamunu^dagger\n\nThe pseudo-density block is barrho_nu=sqrtf^nuC_nu. Its Kraus matrices are therefore\n\nK_lambdamunu\n=sqrts_lambdamunu\n       sqrtfracf^lambdaf^nu\n A_lambdamunu\n\nWeakPITrajectoryPlan constructs these matrices directly from the cached one-box Clebsch–Gordan contractions. It does not form and diagonalize a dense Choi matrix. At setup it verifies, separately for every physical channel and source sector,\n\nsum_lambdamuK_lambdamunu^dagger\nK_lambdamunu=Q_nu\n\nwhere Q_nu is the prepared physical Schur block entering the no-jump generator. This makes the ensemble gain exactly the same CP map as the density-valued PI trajectory backend, up to floating-point setup error.\n\nCollective, direct-PI, and collective p-body jump operators preserve the Schur sector and contribute one Kraus branch per nonzero sector block. Fixed one-body LocalJump channels may change sectors. The construction is the same for qubits and qudits.\n\nThe Kraus decomposition defines one legitimate unresolved measurement record; it does not reconstruct a particle label. Another Kraus rotation gives the same master equation and all ensemble-linear observables but can change individual paths and trajectory-level variances.","title":"Sector-changing local Kraus branches"},{"category":"section","location":"weak_pi_trajectories/#Prepared-workflow","page":"Weak-PI pseudo-ket trajectories","text":"using PermutationalInvariantDynamics\nusing Random\n\nN = 6\nbasis = PIBasis(N, 2)\nsm = ComplexF64[0 1; 0 0]\nmodel = PIModel(basis, (\n    CollectiveJump(sm; rate=0.2),\n    LocalJump(sm; rate=0.8),\n))\n\nrho0 = iid_pure_state(basis, ComplexF64[0, 1])\npsi0 = weak_pi_pseudoket(rho0)\nplan = WeakPITrajectoryPlan(model)\nbatch = WeakPITrajectoryBatchWorkspace(plan, psi0)\n\ntimes = collect(0.0:0.1:1.0)\npaths = weak_pi_quantum_trajectories(\n    plan, psi0, times, 500;\n    dt=0.005, max_jump_probability=0.03,\n    seed=2026, threaded=Threads.nthreads() > 1,\n    workspace=batch,\n)\n\nsummary = weak_pi_trajectory_statistics(\n    paths;\n    observables=(excitation=adjoint(sm) * sm,),\n    nchannels=2,\n)\nrho_mean = summary.average_states[end]\n\nUse continuous hazard roots by selecting the adaptive algorithm:\n\nevent_paths = weak_pi_quantum_trajectories(\n    plan, psi0, times, 500;\n    algorithm=:event, dt=0.02, dtmax=0.05,\n    abstol=1e-9, reltol=1e-7,\n    event_time_tolerance=1e-10,\n    seed=2026, workspace=batch,\n)\n\nThe aliases :adaptive and :event_driven select the same algorithm. An embedded Dormand–Prince 5(4) trial advances the normalized pseudo-ket and the integrated jump hazard with common stages. A crossing is located from the unchanged step-start state, then the ordinary sector-resolved Kraus branch is sampled at the physical event time. Event times are therefore not restricted to step endpoints.\n\nPlans are immutable and shareable. A WeakPITrajectoryWorkspace owns the Runge–Kutta, intensity, and selected-branch scratch for one path. A WeakPITrajectoryBatchWorkspace gives every worker independent scratch and an independent RNG while retaining trajectory-index-derived seeds. Serial and threaded batches are therefore reproducible for a fixed seed.\n\nWeakPITrajectoryWorkspace(mode=:full) is the default and supports both algorithms. Fixed propagation uses three full-vector RK4 registers. mode=:fixed omits k3, k4, and the six adaptive-only vectors and rejects an event-driven call rather than allocating scratch lazily. Batch workspaces accept the same mode keyword.\n\nEvery no-jump stage first combines the rate-weighted channel loss blocks into one effective Schur operator, requiring one matrix-vector action per sector. Individual channel and Kraus-branch intensities are evaluated only after an event must be selected. For event-driven paths, the accepted seven DOPRI stages supply a quartic continuous extension. Four scalar hazard coefficients are retained for bisection and the root pseudo-ket is reconstructed once from the retained stages; root localization performs no additional RHS trials.\n\nWeakPIJumpRecord stores the model channel, flattened Kraus branch, source partition, target partition, and the N-1 child partition for a local event. weak_pi_trajectory_statistics reports ordinary channel counts plus the sampled Schur-sector transition counts. Observable statistics accept the same Hermitian local matrices and PI operators as the density trajectory tools. weak_pi_expectation evaluates one pseudo-ket directly.","title":"Prepared workflow"},{"category":"section","location":"weak_pi_trajectories/#History-free-stationary-density-estimate","page":"Weak-PI pseudo-ket trajectories","text":"For an autonomous model, weak_pi_trajectory_steady_state estimates the initial-state-selected stationary density operator without retaining pseudo-ket histories or jump records:\n\nestimate = weak_pi_trajectory_steady_state(\n    plan, psi0;\n    trajectories=2_000,\n    settling_time=50.0,\n    samples_per_trajectory=11,\n    sampling_interval=2.0,\n    dt=0.002,\n    max_jump_probability=0.03,\n    seed=2026,\n    threaded=Threads.nthreads() > 1,\n    workspace=batch,\n    observables=(excitation=adjoint(sm) * sm,),\n    return_info=true,\n)\nrho_stationary = estimate.state\n\nThe estimator also accepts algorithm=:event. For a time-series error diagnostic, request complete batches:\n\nestimate = weak_pi_trajectory_steady_state(\n    plan, psi0;\n    trajectories=500,\n    settling_time=50.0,\n    samples_per_trajectory=40,\n    sampling_interval=0.5,\n    batch_size=10,\n    dt=0.01,\n    return_info=true,\n)\nbatch_report = estimate.metadata.batch_means\nbatch_report.standard_error\nbatch_report.effective_independent_samples\n\nbatch_size requires return_info=true, because the diagnostic is returned only through estimate.metadata.batch_means. The primary estimate.standard_error continues to use independently seeded path means. The optional report first averages batch_size consecutive time samples and treats the complete batch means as approximately independent. Increase the batch length and total window until the estimate stabilizes. This does not certify burn-in or finite-window bias; those qualifications are recorded explicitly in batch_report.assumptions.\n\nThe density conversion must precede every average. For independent path r and its K selected post-settling times, the path mean is\n\noverline C^(r)_nu\n=frac1Ksum_k=1^K\n  fracpsi^(r)_nu(t_k)rangle\n        langlepsi^(r)_nu(t_k)sqrtf^nu\nqquad\nwidehat C_nu=frac1Msum_r=1^Moverline C^(r)_nu\n\nIt would be incorrect to average pseudo-ket amplitudes before taking their outer products: the density map is nonlinear, sector-relative phases are unphysical, and the stationary estimate is generally mixed. The function therefore returns a PIState, never a WeakPIPseudoKet.\n\nTime samples from one realization can be autocorrelated. They are first combined into overline C^(r), and only the M independent path means enter the uncertainty estimate. Because the equation-(7) PI coefficient basis is Hilbert–Schmidt orthonormal,\n\ns_mathrmHS\n=sqrtfrac1M-1sum_r=1^M\n leftoverline C^(r)-widehat Cright_2^2\nqquad\nmathrmSE_mathrmHS=fracs_mathrmHSsqrt M\n\nAt least two independent paths are required. With return_info=true, the shared TrajectorySteadyStateResult reports this sample spread and standard error, optional Hermitian-observable statistics across the same path means, the density Liouvillian residual, relative residual, and trace error. These are diagnostics, not a converged claim. The averaged state is not silently normalized, symmetrized, or positivity-repaired.\n\nIts metadata identifies backend=:weak_pi and the selected algorithm, records the effective worker and sampling controls, and exposes pseudo_ket_dimension and density_dimension. The reduction and uncertainty labels are respectively :post_settling_density_mean and :independent_path_mean.\n\nThe state/density reduction is history-free: each worker retains one weak pseudo-ket, one PI-coordinate path mean, and online accumulators, independently of the number of paths and sampling times. Reproducible trajectory-indexed streams retain one UInt64 seed per path. Producing a full density estimate still requires forming every retained Schur outer product at each requested sample. Optional observables are reduced from the same path density mean and therefore use the same independent-path sampling unit.","title":"History-free stationary density estimate"},{"category":"section","location":"weak_pi_trajectories/#Confidence-controlled-weak-PI-ensembles","page":"Weak-PI pseudo-ket trajectories","text":"Observable-only calculations can stop at deterministic batch boundaries once all simultaneous empirical-Bernstein widths meet their target:\n\nadaptive = adaptive_weak_pi_quantum_trajectories(\n    plan, psi0, times;\n    observables=(excitation=adjoint(sm) * sm,),\n    algorithm=:event, dt=0.02,\n    min_trajectories=128,\n    max_trajectories=10_000,\n    batch_size=64,\n    confidence=0.95,\n    atol=2e-3,\n    rtol=1e-2,\n    seed=2026,\n)\n\nObservables are contracted directly from pseudo-kets; no state history or sampled PI density operator is retained. Each separately seeded trajectory is one independent sample for the stopping certificate, reported by adaptive.metadata.effective_independent_samples. The certificate controls Monte Carlo dispersion only. It does not cover integrator, finite-time initialization, finite-window, or model-truncation bias.","title":"Confidence-controlled weak-PI ensembles"},{"category":"section","location":"weak_pi_trajectories/#Scope-and-convergence","page":"Weak-PI pseudo-ket trajectories","text":"The fixed weak-PI integrator is RK4 with a maximum jump probability; the event integrator is adaptive Dormand–Prince with continuous hazard roots. Converge the controls of the selected algorithm before interpreting Monte Carlo error bars. Rates must evaluate to finite, nonnegative real values representable in the prepared precision; time grids and controls may not silently narrow. Hamiltonian rates must likewise be finite and real.\n\nFixed operator-valued collective/direct jumps and fixed one-body local jumps are supported. Operator-valued schedules and LocalPBodyJump are rejected. The composite backend can represent a finite cavity and density-valued tensor-product quantum jumps, but a composite pseudo-ket trajectory compiler is still separate future work.\n\nThe stationary estimator additionally requires an autonomous source and a WeakPIPseudoKet initial state. A PIState can be converted with weak_pi_pseudoket only when every occupied multiplicity-weighted block has rank at most one; a general mixed state is not silently purified. Separately refine the settling time, within-path sampling interval and window length, the selected integration controls, and independent trajectory count. The primary Monte Carlo uncertainty does not include burn-in, finite-window, integration, or model-truncation bias. Optional batch means diagnose temporal autocorrelation only under their explicit approximately-independent-batches assumption. A nonunique stationary manifold can retain dependence on the initial state or strong-symmetry component. Kraus rotations preserve the ensemble master equation but can alter finite-sample variance.\n\nThe runnable weak_pi_trajectories.jl example uses the decay model of Zhang, Zhang, and Mølmer, New J. Phys. 20, 112001 (2018). For the paper's gamma_lGamma_c=1 case it compares four routes: certified population dynamics, general matrix-free PI master evolution, density-valued PI trajectories, and weak-PI pseudo-kets. The stochastic backends use equal ensemble sizes and identical fixed-step controls. Lloyd, Ziolkowska, and Keeling's related 2026 paper cited above provides the broader common-system weak-symmetry context, but this example does not reproduce its shared-cavity calculations.\n\nTwo optional CairoMakie figures show the fluxes and 95% confidence bands, PI-state ensemble errors, sampled Jrightarrow J sector changes, representation-size scaling, and a warmed per-path timing from the current run. An equal-ensemble retained-history panel complements the asymptotic coordinate counts; it excludes plans, worker scratch, and transient peak RAM. For observable-only work, the density-valued backend can instead use save_states=false; the ordinary weak-PI batch/statistics interface retains paths before forming statistics. weak_pi_trajectory_steady_state is the history-free weak-PI route when the full stationary density is required. The timing is descriptive rather than a regression threshold. The scaling panel uses the exact complete-qubit counts\n\nsum_nudim U_nu\n=left(leftlfloor N2rightrfloor+1right)\n left(leftlceil N2rightrceil+1right)\nqquad\nsum_nu(dim U_nu)^2=binomN+33\n\nalongside 2^N and 4^N. These curves are evaluated as formulas; no full-Hilbert state is constructed. The paired examples/weak_pi_trajectories.md guide gives the numerical checks, timing scope, figure panels, and interpretation boundaries in detail. Set PI_EXAMPLE_PLOTS=0 for a numerical-only compatibility or timing run that skips the optional Makie import.","title":"Scope and convergence"},{"category":"section","location":"composite_systems/#Composite-PI-systems","page":"Composite systems","text":"CompositePIBasis combines independently permutationally invariant ensembles with small ordinary Hilbert-space factors. Typical uses include two atomic species, several spatial ensembles, or PI emitters coupled to a truncated cavity or distinguished ancilla. Each ensemble remains in its compressed PI operator space: constructing a composite basis never reconstructs an ensemble's d^N Hilbert space.","title":"Composite PI systems"},{"category":"section","location":"composite_systems/#Factors-and-coordinate-order","page":"Composite systems","text":"A PI factor is an ordinary PIBasis. A finite factor is described by FiniteOperatorBasis(m) and retains all m^2 column-major operator coordinates of that auxiliary system.\n\nusing PermutationalInvariantDynamics\n\natoms_a = PIBasis(2, 2)\natoms_b = PIBasis(1, 2)\nancilla = FiniteOperatorBasis(2; label=:ancilla)\nbasis = CompositePIBasis(atoms_a, atoms_b, ancilla)\n\nThe first declared factor is the fastest-varying flattened index. If x1, x2, and x3 are factor operator-coordinate vectors, the composite vector is\n\nkron(x3, kron(x2, x1))\n\nThis convention is also used by every factorized superoperator. It makes a term with actions (A1, A2, A3) mathematically equal to kron(A3, kron(A2, A1)), although that Kronecker matrix is never formed by the matrix-free kernel.","title":"Factors and coordinate order"},{"category":"section","location":"composite_systems/#States,-operators,-and-normalization","page":"Composite systems","text":"Construct factorized objects with composite_tensor_state and composite_tensor_operator. PI state components must be PIStates; PI operator components must be PIOperators. A finite component is an ordinary square matrix of the declared auxiliary dimension.\n\nrho_a = iid_state(atoms_a, ComplexF64[1 0; 0 0])\nrho_b = iid_state(atoms_b, ComplexF64[0 0; 0 1])\nrho_c = ComplexF64[1 0; 0 0]\nrho = composite_tensor_state(basis, rho_a, rho_b, rho_c)\n\nI_a = identity_operator(atoms_a)\nI_b = identity_operator(atoms_b)\nZ_c = ComplexF64[1 0; 0 -1]\nZ = composite_tensor_operator(basis, I_a, I_b, Z_c)\n\ntrace(rho)                 # product of the physical factor traces\nexpectation(rho, Z)        # tr(Z' * rho)\n\nPI factors retain the package's equation-(7) coefficient convention and finite factors use orthonormal matrix units. Their tensor product is therefore again orthonormal. trace contracts only joint diagonal coordinates and fuses exact Schur multiplicities; it does not allocate a full composite trace vector. composite_trace_vector is available when a solver explicitly needs that vector.\n\nTo trace all factors except one repeatedly, prepare the packed diagonal contraction once:\n\nreduction = CompositeReductionPlan(rho, 1)\nrho_a = composite_reduced_state(rho, reduction)\n\n# Reuse the result storage too.\nrho_a_buffer = PIState(reduction.kept_basis)\ncomposite_reduced_state!(rho_a_buffer, rho, reduction)\n\nThe plan stores only exact-support diagonal offsets and exact Schur multiplicity scales. It is immutable, tied to the exact composite basis, and safe to share between tasks; each call writes only to its caller-owned result. For a retained finite factor, use a square matrix as the destination.","title":"States, operators, and normalization"},{"category":"section","location":"composite_systems/#Matrix-free-generators","page":"Composite systems","text":"A CompositeSuperoperatorTerm is a product of factor maps, with nothing denoting the identity on a factor. The convenience constructor names only the active factors:\n\nterm = factorized_superoperator_term(\n    basis,\n    1 => action_a,\n    3 => action_c;\n    coefficient=0.25,\n)\ngenerator = CompositeSuperoperator(basis, term)\n\nFor a local PI master equation, compile it normally and lift the compiled action. Basis provenance is checked by object identity.\n\nsm = ComplexF64[0 1; 0 0]\nlocal_model = PIModel(atoms_a, (LocalJump(sm; rate=0.1),))\nlocal_action = compile(local_model; backend=:matrixfree)\nlocal_term = local_superoperator_term(basis, 1, local_action)\n\nTensor-product interactions can be built from factor-coordinate left, right, and sandwich maps. The higher-level constructors implement\n\n-i rbigotimes_f H_frho\n\nand\n\nrmathcal Dbigotimes_f L_f(rho)qquad\nmathcal DL(rho)=Lrho L^dagger-frac12L^dagger Lrho\n\nJx_a = collective_operator(atoms_a, ComplexF64[0 1; 1 0])\nX_c = ComplexF64[0 1; 1 0]\ninteraction = composite_hamiltonian_superoperator(\n    basis, 1 => Jx_a, 3 => X_c; rate=0.05)\n\nJm_b = collective_operator(atoms_b, sm)\nloss = composite_dissipator_superoperator(\n    basis, 2 => Jm_b, 3 => sm; rate=0.02)\n\ngenerator = CompositeSuperoperator(basis, local_term) + interaction + loss\n\nHamiltonian factors are checked for Hermiticity by default. Hamiltonian and dissipator rates must be real, but may be negative; as elsewhere in the package, a negative dissipative rate need not generate a completely positive map.\n\nUse one explicit workspace per task in repeated applications or evolution:\n\ny = similar(rho.data)\nworkspace = CompositeSuperoperatorWorkspace(generator, rho.data)\napply!(y, generator, rho.data, 0.0, nothing, workspace)\n\nevolution_workspace = EvolutionWorkspace(generator, rho.data)\nevolve!(y, generator, rho.data, (0.0, 1.0);\n        steps=128, workspace=evolution_workspace)\n\nThe warmed tensor-mode application reuses full-vector buffers, factor fibers, and any nested LiouvillianWorkspace. composite_matrixfree(generator) gives a synchronized compatibility MatrixFreeLiouvillian for APIs that require that wrapper; parallel hot loops should retain explicit task-owned composite workspaces instead. Prepared package consumers such as sensitivity_problem discover the retained immutable composite plan and construct a fresh task-owned batch workspace. A bare compatibility matrix product through the wrapper remains a synchronized column fallback.\n\nSeveral right-hand sides use a separate fixed-capacity workspace:\n\nX = hcat(rho.data, perturbation_1, perturbation_2)\nY = similar(X)\nbatch_work = CompositeSuperoperatorBatchWorkspace(\n    generator; capacity=size(X, 2))\n\napply!(Y, generator, X, 0.0, nothing, batch_work)\n\nThe factor maps consume equal tensor fibers from all columns together. apply_adjoint! uses the same workspace. Capacity is immutable: a wider matrix raises instead of growing hidden buffers. This path is useful for block Krylov methods and parameter sensitivities; it is not a global Kronecker materialization.","title":"Matrix-free generators"},{"category":"section","location":"composite_systems/#Density-valued-composite-quantum-jumps","page":"Composite systems","text":"Monitored tensor-product channels are declared explicitly. The background contains trace-preserving coherent and unmonitored physics and must exclude the monitored dissipators:\n\nbackground = composite_hamiltonian_superoperator(\n    basis, 1 => Jx_a, 3 => X_c; rate=0.05)\n\nchannel = CompositeJumpChannel(\n    basis, 2 => Jm_b, 3 => sm;\n    rate=0.02,\n    label=:joint_emission,\n)\nplan = CompositeTrajectoryPlan(background, channel)\n\nThe plan adds one complete dissipator per channel and exposes the resulting unconditional generator through composite_master_superoperator(plan). This split is intentional: an arbitrary sum of superoperator terms does not retain enough information to infer a unique physical unraveling, and accepting an already complete generator plus the same channels would double-count their gains.\n\nFor channel k with G_krho=J_krho J_k^dagger and Q_k=J_k^dagger J_k, the normalized conditional equation is\n\ndotrho_c=mathcal L_0(rho_c)\n-frac12sum_kgamma_kQ_krho_c\n+left(sum_klambda_kright)rho_c\nqquad\nlambda_k=gamma_kmathrmtr(Q_krho_c)\n\nA selected jump maps rho_c to G_krho_cmathrmtr(G_krho_c). The fixed operator maps remain factorized. Scalar rates may be driven, but every evaluated trajectory rate must be finite, real, nonnegative, and representable in the prepared precision. Rate schedules used by threaded batches must be pure and thread safe; mutable external callback state is not synchronized by the plan.\n\nThe fixed-step backend integrates each channel hazard with the same RK4 stages as the conditional state. A trial is shortened and recomputed whenever the jump probability corresponding to its integrated total hazard would exceed max_jump_probability; a rapidly changing rate therefore cannot bypass the configured probability cap merely because it was small at the beginning of the step.\n\nworkspace = CompositeTrajectoryWorkspace(plan, rho)\npath = quantum_trajectory(\n    plan, rho, 0.0:0.05:2.0;\n    dt=0.005,\n    workspace=workspace,\n)\n\nbatch = CompositeTrajectoryBatchWorkspace(plan, rho)\npaths = quantum_trajectories(\n    plan, rho, 0.0:0.05:2.0, 1_000;\n    dt=0.005,\n    seed=11,\n    threaded=true,\n    workspace=batch,\n)\nmean_states = trajectory_average(paths)\n\nPlans are immutable and shareable. A single-path workspace owns the RK4 stages, one background workspace, one shared pair of full tensor buffers, and small channel-specific factor-fibre scratch. The number of full composite buffers is therefore independent of the number of monitored channels. Batch workers own distinct workspaces and RNGs; global trajectory-index seeds make serial and threaded scheduling sample the same ordered paths.\n\nWith named Hermitian CompositePIOperator observables and save_states=false, quantum_trajectories returns online means, variances, confidence intervals, and optional jump statistics without storing sampled states. Individual paths are density-valued and can be mixed when the background contains unresolved local PI channels.","title":"Density-valued composite quantum jumps"},{"category":"section","location":"composite_systems/#Current-scope","page":"Composite systems","text":"Finite auxiliary factors retain all m^2 operator coordinates, so a bosonic mode must first be truncated to a finite m.\nCross terms are sums of tensor products of factor maps. General interactions should be decomposed into such terms rather than materialized as a composite Kronecker matrix.\nThe current high-level PIModel compiler acts on one PI ensemble. Composite generators are assembled from compiled local actions and explicit cross maps as above.\nComposite trajectories currently support fixed tensor-product jump operators and fixed-step conditional integration. Callable jump operators, arbitrary CP gain maps, event-driven integration, diffusive monitoring, adaptive-confidence stopping, Distributed batches, and composite weak-PI pseudo-kets are not implemented.\nA finite bosonic mode needs an explicit truncation. Converge that truncation, the fixed time step, maximum jump probability, and trajectory count independently.\nSingle-ensemble analysis routines such as Schur-sector negativity or the population backend are not implicitly generalized to composite states. Reduce or contract the intended factors explicitly when an analysis needs a subsystem state.","title":"Current scope"},{"category":"section","location":"composite_systems/#API","page":"Composite systems","text":"See examples/composite_ensembles.jl for a complete small calculation with two PI ensembles and one finite ancilla. examples/composite_quantum_trajectories.jl compares a cross-factor jump ensemble with the independently propagated master equation.","title":"API"},{"category":"type","location":"composite_systems/#PermutationalInvariantDynamics.FiniteOperatorBasis","page":"Composite systems","text":"FiniteOperatorBasis(d; label=:auxiliary)\n\nOperator-space factor for an ordinary finite d-dimensional auxiliary Hilbert space.  Its d^2 coordinates are vec(A) in Julia column-major order.  This factor is intended for small cavities, ancillas, or other finite systems coupled to one or more compressed PIBasis factors.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.FiniteOperatorBasis"},{"category":"type","location":"composite_systems/#PermutationalInvariantDynamics.CompositePIBasis","page":"Composite systems","text":"CompositePIBasis(factors...)\n\nTensor product of compressed PI operator spaces and ordinary finite operator spaces.  At least one factor is required.  The first declared factor is the fastest-varying index in the flattened coordinate vector.  Consequently a factorized coordinate vector is ordered as kron(x_last, ..., x_second, x_first).\n\nOnly retained PI coordinates and finite auxiliary d^2 coordinates are formed; a PI factor's full d^N Hilbert space is never reconstructed.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.CompositePIBasis"},{"category":"type","location":"composite_systems/#PermutationalInvariantDynamics.CompositePIOperator","page":"Composite systems","text":"CompositePIOperator(basis, data)\nCompositePIOperator(basis; T=Float64)\n\nAn operator in the tensor product of the factor operator-coordinate spaces. PI factors retain their equation-(7) coefficient convention; finite factors use column-major matrix units.  The first basis factor varies fastest.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.CompositePIOperator"},{"category":"type","location":"composite_systems/#PermutationalInvariantDynamics.CompositePIState","page":"Composite systems","text":"CompositePIState(basis, data)\nCompositePIState(basis; T=Float64)\n\nDensity-state container in the same tensor-product coordinates as CompositePIOperator.  Construction checks dimensions only and does not normalize, symmetrize, or repair the input.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.CompositePIState"},{"category":"type","location":"composite_systems/#PermutationalInvariantDynamics.CompositeReductionPlan","page":"Composite systems","text":"CompositeReductionPlan(basis, factor; T=Float64,\n                       memory_budget=512*1024^2)\nCompositeReductionPlan(rho, factor; kwargs...)\n\nPrepare the exact contraction which traces every composite factor except factor. The plan packs only joint physical-diagonal source offsets, group boundaries, and exact products of Schur multiplicities. It never constructs the full composite trace vector or a Hilbert-space density matrix.\n\nFor ordinary representable multiplicities, their square roots are converted once and the repeated contraction is allocation-free at machine precision. If a square root is not independently representable, the plan retains its exact BigInt multiplicity and a binary-scaled factor so application can fuse that scale with the traced value without premature overflow or underflow.\n\nThe plan is immutable and may be shared. It is tied to the exact CompositePIBasis, selected factor, scalar type, and, for BigFloat, the captured precision and rounding mode.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.CompositeReductionPlan"},{"category":"function","location":"composite_systems/#PermutationalInvariantDynamics.composite_tensor_operator","page":"Composite systems","text":"composite_tensor_operator(basis, components...)\n\nConstruct a factorized composite operator.  A PI component is a PIOperator; a finite component is a square matrix.  The returned coordinates use kron(x_last, ..., x_first) ordering.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.composite_tensor_operator"},{"category":"function","location":"composite_systems/#PermutationalInvariantDynamics.composite_tensor_state","page":"Composite systems","text":"composite_tensor_state(basis, components...)\n\nConstruct a factorized composite state from one PIState per PI factor and one density matrix per finite factor.  Inputs are copied and are not normalized implicitly.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.composite_tensor_state"},{"category":"function","location":"composite_systems/#PermutationalInvariantDynamics.composite_identity_operator","page":"Composite systems","text":"Construct the identity operator on all retained composite factors.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.composite_identity_operator"},{"category":"function","location":"composite_systems/#PermutationalInvariantDynamics.composite_trace_vector","page":"Composite systems","text":"composite_trace_vector(basis; T=Float64)\n\nReturn the tensor-product physical trace functional in composite coordinate order.  For a PI factor this includes its exact Schur multiplicity weights; for a finite factor it is ordinary matrix trace.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.composite_trace_vector"},{"category":"function","location":"composite_systems/#PermutationalInvariantDynamics.composite_reduced_state","page":"Composite systems","text":"composite_reduced_state(rho, factor; memory_budget=512*1024^2)\ncomposite_reduced_state(rho, plan)\n\nReturn the state of one retained composite factor after tracing all other factors. A PI factor returns a PIState, while an ordinary finite factor returns its dense density matrix. No normalization or state repair is performed.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.composite_reduced_state"},{"category":"function","location":"composite_systems/#PermutationalInvariantDynamics.composite_reduced_state!","page":"Composite systems","text":"composite_reduced_state!(destination, rho, factor;\n                         memory_budget=512*1024^2)\ncomposite_reduced_state!(destination, rho, plan)\n\nTrace every composite factor except factor directly in tensor-product operator coordinates. For a retained PIBasis, destination must be a PIState on the exact retained basis. For a retained FiniteOperatorBasis, it must be a square matrix of that factor's Hilbert-space dimension.\n\nThe contraction visits only physical diagonal coordinates of traced factors, uses exact products of Schur multiplicities, and never reconstructs a many-particle Hilbert space or a full tensor-product trace vector. Source and destination must not alias.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.composite_reduced_state!"},{"category":"type","location":"composite_systems/#PermutationalInvariantDynamics.CompositeSuperoperatorTerm","page":"Composite systems","text":"CompositeSuperoperatorTerm(basis, actions; coefficient=1)\n\nOne factorized superoperator term. actions is a tuple with one entry per factor; use nothing for an identity map.  Each other entry must be a square operator-space map of the corresponding factor dimension.  The coefficient may be a number or a value_at-compatible time-dependent schedule. Matrix actions are copied at construction so the resulting term is read-only.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.CompositeSuperoperatorTerm"},{"category":"function","location":"composite_systems/#PermutationalInvariantDynamics.factorized_superoperator_term","page":"Composite systems","text":"factorized_superoperator_term(basis, factor=>action...; coefficient=1)\n\nConstruct a term by naming only its nonidentity factor actions.  Duplicate or out-of-range factor indices are rejected.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.factorized_superoperator_term"},{"category":"function","location":"composite_systems/#PermutationalInvariantDynamics.local_superoperator_term","page":"Composite systems","text":"Lift one factor action while leaving every other factor unchanged.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.local_superoperator_term"},{"category":"type","location":"composite_systems/#PermutationalInvariantDynamics.CompositeSuperoperator","page":"Composite systems","text":"CompositeSuperoperator(basis, terms...; T=nothing)\n\nRead-only matrix-free sum of factorized superoperators.  No Kronecker matrix is formed.  T is required only when the scalar type cannot be inferred, for example for an empty sum or a driven identity term.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.CompositeSuperoperator"},{"category":"type","location":"composite_systems/#PermutationalInvariantDynamics.CompositeSuperoperatorWorkspace","page":"Composite systems","text":"CompositeSuperoperatorWorkspace(S[, source])\nCompositeSuperoperatorWorkspace(S; T=eltype(S))\n\nCaller-owned full-vector, tensor-fiber, and nested Liouvillian scratch for apply!.  Reuse it sequentially; use one workspace per concurrent task.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.CompositeSuperoperatorWorkspace"},{"category":"type","location":"composite_systems/#PermutationalInvariantDynamics.CompositeSuperoperatorBatchWorkspace","page":"Composite systems","text":"CompositeSuperoperatorBatchWorkspace(S; capacity, T=eltype(S))\n\nTask-owned, fixed-capacity scratch for matrix right-hand sides of a CompositeSuperoperator. The workspace batches equal tensor fibers from all supplied right-hand sides through each factor map. It never forms a global Kronecker matrix and never grows after construction: applying more than capacity columns raises.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.CompositeSuperoperatorBatchWorkspace"},{"category":"function","location":"composite_systems/#PermutationalInvariantDynamics.factor_left_superoperator","page":"Composite systems","text":"Return the factor-coordinate map X -> A*X without a full PI Hilbert space.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.factor_left_superoperator"},{"category":"function","location":"composite_systems/#PermutationalInvariantDynamics.factor_right_superoperator","page":"Composite systems","text":"Return the factor-coordinate map X -> X*A without a full PI Hilbert space.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.factor_right_superoperator"},{"category":"function","location":"composite_systems/#PermutationalInvariantDynamics.factor_sandwich_superoperator","page":"Composite systems","text":"Return the factor-coordinate map X -> A*X*B'.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.factor_sandwich_superoperator"},{"category":"function","location":"composite_systems/#PermutationalInvariantDynamics.composite_hamiltonian_superoperator","page":"Composite systems","text":"composite_hamiltonian_superoperator(\n    basis, factor=>operator...; rate=1, check=true)\n\nMatrix-free commutator generated by a tensor-product Hamiltonian on the named factors.  It is represented as two products of factor left/right maps.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.composite_hamiltonian_superoperator"},{"category":"function","location":"composite_systems/#PermutationalInvariantDynamics.composite_dissipator_superoperator","page":"Composite systems","text":"composite_dissipator_superoperator(basis, factor=>jump...; rate=1)\n\nMatrix-free Lindblad dissipator for a tensor-product jump on the named factors.  The gain and two anticommutator pieces are three products of factor sandwich/left/right maps.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.composite_dissipator_superoperator"},{"category":"function","location":"composite_systems/#PermutationalInvariantDynamics.composite_matrixfree","page":"Composite systems","text":"composite_matrixfree(S; T=eltype(S))\n\nWrap a composite sum as the package's synchronized MatrixFreeLiouvillian.  The wrapper owns one compatibility workspace; explicit parallel hot loops should call apply! with one CompositeSuperoperatorWorkspace per task instead. Prepared package consumers, including sensitivity_problem, recover fresh task-owned vector or fixed-capacity batch scratch from the wrapper. Bare compatibility matrix products retain the synchronized column fallback.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.composite_matrixfree"},{"category":"type","location":"composite_systems/#PermutationalInvariantDynamics.CompositeJumpChannel","page":"Composite systems","text":"CompositeJumpChannel(basis, factor=>operator...; rate=1, label=:jump)\n\nA monitored tensor-product jump channel for a CompositePIBasis. Each named PI factor requires a compatible fixed PIOperator, and each finite factor requires a fixed square matrix. Unnamed factors carry the identity. The channel owns copied, factorized gain and anticommutator maps; it never constructs the global Kronecker matrix.\n\nrate may be a fixed real number or a scalar value_at-compatible schedule. Quantum trajectories require every evaluated rate to be finite and nonnegative. The channel order and label identify the chosen unraveling.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.CompositeJumpChannel"},{"category":"type","location":"composite_systems/#PermutationalInvariantDynamics.CompositeTrajectoryPlan","page":"Composite systems","text":"CompositeTrajectoryPlan(basis, channels...; background=nothing, T=nothing)\nCompositeTrajectoryPlan(background, channels...; T=nothing)\n\nImmutable prepared model for density-valued composite quantum-jump trajectories. background is a trace-preserving CompositeSuperoperator containing coherent and unmonitored physics; it must exclude the monitored channels. The plan assembles the complete unconditional generator as the background plus one Lindblad dissipator per channel. Use composite_master_superoperator to access it.\n\nThe plan retains factorized gain/loss maps and multiplicity-aware trace metadata. It may be shared by tasks. Each task needs its own CompositeTrajectoryWorkspace. Fixed jump operators and scalar time-dependent rates are supported; arbitrary gain superoperators are not silently interpreted as an unraveling.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.CompositeTrajectoryPlan"},{"category":"type","location":"composite_systems/#PermutationalInvariantDynamics.CompositeTrajectoryWorkspace","page":"Composite systems","text":"CompositeTrajectoryWorkspace(plan, rho)\n\nPreallocated three-register classical-RK4 scratch, shared tensor-mode buffers, exact-trace scratch, and factor-fibre workspaces for one density-valued composite trajectory. Full composite buffers are shared sequentially across all jump channels, so their count does not grow with the number of channels. Reuse one workspace sequentially; concurrent paths require distinct workspaces.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.CompositeTrajectoryWorkspace"},{"category":"type","location":"composite_systems/#PermutationalInvariantDynamics.CompositeTrajectoryBatchWorkspace","page":"Composite systems","text":"CompositeTrajectoryBatchWorkspace(plan, rho; workers=Threads.nthreads())\n\nReusable task-local workspace and RNG pool for batched composite quantum trajectories. The immutable plan is shared, while every active worker owns all mutable numerical scratch. Reuse a batch workspace sequentially only.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.CompositeTrajectoryBatchWorkspace"},{"category":"type","location":"composite_systems/#PermutationalInvariantDynamics.CompositeQuantumTrajectory","page":"Composite systems","text":"One density-valued composite quantum-jump realization.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.CompositeQuantumTrajectory"},{"category":"function","location":"composite_systems/#PermutationalInvariantDynamics.composite_master_superoperator","page":"Composite systems","text":"composite_master_superoperator(plan)\n\nReturn the complete unconditional matrix-free generator prepared by a CompositeTrajectoryPlan. It is the supplied background plus the Lindblad dissipators of every monitored channel and is useful for direct ensemble comparisons.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.composite_master_superoperator"},{"category":"section","location":"global_pseudomodes/#Global-pseudomodes-and-shared-cavities","page":"Global pseudomodes and shared cavities","text":"A global pseudomode is one finite-cutoff bosonic mode coupled to a whole permutationally invariant ensemble. Permutations act on the identical systems but not on that distinguished mode. Typical examples are a shared cavity, a reaction coordinate coupled collectively to the ensemble, or one damped pseudomode realization of a common structured reservoir.\n\nThis topology is different from one independent pseudomode per particle. Choose the topology from the physics before comparing coordinate counts.","title":"Global pseudomodes and shared cavities"},{"category":"section","location":"global_pseudomodes/#Representation-and-master-equation","page":"Global pseudomodes and shared cavities","text":"For a PI system basis with n_mathrmPI operator coordinates and a mode with levels 0ldotsn_max, the package constructs\n\nCompositePIBasis(system_basis, FiniteOperatorBasis(nmax + 1))\n\nand stores\n\nn_mathrmPI(n_max+1)^2\n\ncoordinates. The system coordinate is the first and fastest factor. Factorized maps are applied one tensor mode at a time; neither a d^N system state nor the global Kronecker superoperator is formed.\n\nFor a one-particle matrix L, PseudomodeCoupling(L; strength=g) uses the collective operator\n\nJ_L=sum_i=1^N L_i\n\nand the rotating-wave interaction\n\nH_mathrmint=gJ_La^dagger+g^*J_L^dagger a\n\nAn optional counterrotating_strength=h adds hJ_La+h^*J_L^dagger a^dagger. No Kac or other N-dependent scaling is inserted. Supply the scaling required by the model explicitly.\n\nThe mode part is\n\n-iomega a^dagger arho\n+kappa(bar n+1)mathcal Darho\n+kappabar nmathcal Da^daggerrho\n\nThe package convention is mathcal Dcrho=crho c^dagger-c^dagger crho2. Consequently, damping=κ makes a free mode amplitude decay at rate kappa2.","title":"Representation and master equation"},{"category":"section","location":"global_pseudomodes/#Build-a-shared-mode-model","page":"Global pseudomodes and shared cavities","text":"The most explicit workflow starts from the bare PI system:\n\nusing LinearAlgebra\nusing PermutationalInvariantDynamics\n\nN = 4\nspin = spin_matrices()\nsystem_basis = PIBasis(N, 2; sectors=[(N, 0)])\nsystem_model = PIModel(system_basis, ())\n\nmode = BosonicPseudomode(\n    N + 1; label=:cavity, frequency=0.0, damping=0.2)\ncoupling = PseudomodeCoupling(\n    spin.jm; mode=:cavity, strength=0.4)\n\nmodel = global_pseudomode_model(\n    system_model, mode; couplings=coupling)\nrho0 = pseudomode_product_state(\n    model, computational_product_state(system_basis, 2))\n\nThe PIModel overload preserves the exact system-basis object and is the recommended route when the system already has local, collective, or many-body terms. Two conveniences are equivalent:\n\nsame = pseudomode_model(system_model, mode; couplings=coupling)\nsame_alias = shared_pseudomode_model(\n    system_model, mode; couplings=coupling)\n\nThe constructor from a one-particle Hamiltonian is\n\nmodel = global_pseudomode_model(\n    N, zeros(ComplexF64, 2, 2), mode;\n    couplings=coupling, system_terms=())\n\nFor code that chooses the embedding from an option, use pseudomode_model(N, H, mode; topology=:global, ...). The same signature defaults to topology=:local, so set the keyword explicitly when the mode is shared.\n\nGlobalPseudomodeModel exposes prepared read-only pieces:\n\nField Meaning\nbackground PI system generator, mode frequency, and coherent couplings; excludes damping jumps\ndamping_channels Explicit loss and thermal-gain CompositeJumpChannels\ngenerator Complete unconditional deterministic generator\nmode_operators Identity, annihilation, creation, number, parity, and top-level projector matrices\ncoupling_operators Prepared collective system operators and quadratures\nsystem_reduction_plan Packed exact diagonal contraction retaining the PI system\nmode_reduction_plan Packed exact diagonal contraction retaining the finite mode\nresource_estimates Coordinate, coupling-map/operator, retained, setup, nested-workspace, and action-transient memory estimates\n\nThe separation between background and damping_channels avoids counting the same dissipator twice when constructing a CompositeTrajectoryPlan:\n\ntrajectory_plan = CompositeTrajectoryPlan(\n    model.background, model.damping_channels...)\n\nThe PIModel overload accepts T to select a wider working type for an empty system model or for mode and coupling data accompanying a system model already lowered at that type. It does not retroactively widen a nonempty PI system generator: rebuild those system terms at the target precision first. It rejects a T that would narrow any input; omitting T promotes inputs without narrowing. Product-state inputs may be narrower and are converted to the prepared type, but wider inputs require rebuilding the model. Workspaces, reductions, and stationary solvers preserve the prepared BigFloat precision and rounding mode even when called from a narrower ambient precision context.\n\nPreparation, matrix-free wrapping, product-state construction, and stationary solving enforce memory_budget before their guarded allocations. The estimates include retained model data, coupling maps and operators, task-owned workspace, per-application transients, relevant state intermediates, and the matrix-free wrapper's copied trace vector. memory_budget=Inf is the explicit opt-out; the package does not silently narrow precision or switch to a dense method.","title":"Build a shared-mode model"},{"category":"section","location":"global_pseudomodes/#Evolution,-observables,-and-reductions","page":"Global pseudomodes and shared cavities","text":"For a compact state history, evolve the factorized generator directly:\n\ntimes = collect(range(0.0, 4.0; length=41))\nstates = time_evolution(\n    model.generator, rho0, times; steps_per_interval=8)\n\nBuild observables as products of system and mode factors:\n\nsystem_identity = identity_operator(system_basis)\nmode_identity = model.mode_operators.identity\nJnumber = collective_operator(system_basis, spin.jp * spin.jm)\n\natoms = composite_tensor_operator(\n    model.basis, Jnumber, mode_identity)\nphotons = composite_tensor_operator(\n    model.basis, system_identity,\n    model.mode_operators.number_operator)\ntop = composite_tensor_operator(\n    model.basis, system_identity,\n    model.mode_operators.top_projector)\n\natom_values = [real(expectation(rho, atoms)) for rho in states]\nphoton_values = [real(expectation(rho, photons)) for rho in states]\ntop_values = [real(expectation(rho, top)) for rho in states]\n\nThe mode-traced system remains a compressed PIState; the system-traced mode is an ordinary dense matrix of size (nmax+1) x (nmax+1):\n\nrho_system = trace_pseudomodes(last(states), model)\nrho_mode = global_pseudomode_state(last(states), model)\n\n@assert rho_mode ≈ composite_reduced_state(last(states), 2)\n\nThese functions reuse the two model-owned CompositeReductionPlans and contract tensor-factor coordinates directly. They do not rebuild Schur-sector groups, reconstruct the ensemble Hilbert space, normalize the result, or repair an invalid state.","title":"Evolution, observables, and reductions"},{"category":"section","location":"global_pseudomodes/#Matrix-free-application-and-solvers","page":"Global pseudomodes and shared cavities","text":"model.generator is already a read-only CompositeSuperoperator. Explicit task-owned scratch is useful in repeated or parallel applications:\n\nwork = global_pseudomode_workspace(model)\ny = similar(rho0.data)\napply!(y, model, rho0.data, work)\n\nUse one workspace per concurrent task. The synchronized convenience wrapper\n\nL = global_pseudomode_matrixfree(model; workspace=work)\n\nprovides forward and adjoint actions for Krylov and response routines. liouvillian(model; representation=:matrixfree) returns the same kind of wrapper. Sparse materialization of the complete composite Kronecker superoperator is intentionally unsupported.\n\nFor an autonomous model, prefer the typed high-level result:\n\nsteady = stationary_state(model; return_info=true)\nrho_ss = steady.state\n\nThe advanced steady_state(model; method=:krylov, ...) route returns raw coordinate-level solver output. Both use the matrix-free operator. AutoAlgorithm() resolves to restarted GMRES; supported explicit choices are GMRESAlgorithm and RecycledGMRESAlgorithm. Direct, dense, shift-invert, and the PI-only Schur preconditioner are rejected for this composite coordinate. A numerical residual establishes solver convergence, not uniqueness or mode-cutoff convergence.\n\nSelected decay modes use the same factorized route:\n\nslow_modes = liouvillian_spectrum(\n    model; algorithm=:arnoldi, nev=4)\n\nAutomatic spectral selection chooses a matrix-free Arnoldi family. Complete dense diagonalization of the global composite map is intentionally unsupported.","title":"Matrix-free application and solvers"},{"category":"section","location":"global_pseudomodes/#Shared-mode,-replicated-local-modes,-or-HEOM?","page":"Global pseudomodes and shared cavities","text":"Physical model Package representation Main coordinate count What must converge\nOne shared explicit mode GlobalPseudomodeModel on CompositePIBasis n_mathrmPI(n_max+1)^2 Mode cutoff and numerical solver\nOne independent mode per identical system PISupersite and local pseudomode_model binomN+d(n_max+1)^2-1N for one mode Every local-mode cutoff and numerical solver\nCommon Gaussian bath represented by exponential correlations HEOMBath and HEOMPlan n_mathrmPIn_mathrmADO Bath decomposition, hierarchy depth, and numerical solver\n\nThe first two descriptions retain physical mode density matrices and occupations. HEOM eliminates the bath into auxiliary density operators and does not provide a physical single-mode state. Conversely, one damped pseudomode represents a particular finite Markovian embedding, whereas HEOM can retain several correlation poles without interpreting each ADO as a physical oscillator.\n\nA smaller shared-mode coordinate count is not an optimization of the local model: it describes different bath correlations. The two coincide only in special cases such as N=1.","title":"Shared mode, replicated local modes, or HEOM?"},{"category":"section","location":"global_pseudomodes/#Cutoff-and-reliability-checks","page":"Global pseudomodes and shared cavities","text":"Monitor mode_operators.top_projector. It measures the boundary population of the one shared mode; do not divide it by N.\nCompare common observables and reduced states after increasing nmax. A small top-level population is useful evidence, not a general error bound.\nRecheck time-step, Krylov, or stationary residual convergence independently of the cutoff.\nRestricted Schur sectors are valid only when every system term and collective coupling preserves them. Otherwise use a complete PIBasis.\nDeterministic time-local system rates may be negative, but stochastic damping channels require finite nonnegative rates.\n\nThe complete dependency-free example is examples/global_pseudomode_cavity.jl.","title":"Cutoff and reliability checks"},{"category":"section","location":"global_pseudomodes/#API","page":"Global pseudomodes and shared cavities","text":"","title":"API"},{"category":"type","location":"global_pseudomodes/#PermutationalInvariantDynamics.GlobalPseudomodeModel","page":"Global pseudomodes and shared cavities","text":"GlobalPseudomodeModel\n\nPrepared time-local model for one truncated bosonic pseudomode shared by all members of a permutationally invariant ensemble.  The system remains in its exact PIBasis, the oscillator is a FiniteOperatorBasis, and generator is a matrix-free sum of factorized superoperators.\n\nbackground contains the supplied PI system generator, the mode frequency, and coherent system–mode couplings.  It excludes damping_channels, so the two can be passed directly to CompositeTrajectoryPlan.  generator contains both pieces and is the unconditional deterministic generator. Prepared numerical data are read-only; obtain a task-owned workspace with global_pseudomode_workspace.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.GlobalPseudomodeModel"},{"category":"function","location":"global_pseudomodes/#PermutationalInvariantDynamics.global_pseudomode_model","page":"Global pseudomodes and shared cavities","text":"global_pseudomode_model(system_model, mode;\n                        couplings=(), frequency=nothing,\n                        damping=nothing, thermal_occupation=nothing,\n                        retain_zero_terms=false,\n                        coefficient_cache=nothing,\n                        T=nothing,\n                        memory_budget=512*1024^2)\n\nPrepare one truncated pseudomode shared by every member of an existing PI system model.  A PseudomodeCoupling contains a one-particle matrix L; this builder lifts it explicitly to J_L = sum(i=1:N) L_i and uses\n\ng*J_L⊗a' + conj(g)*J_L'⊗a\n\nplus the optional counter-rotating interaction.  No Kac or other N-dependent scaling is inserted.\n\nT may explicitly select a wider working type for the mode and coupling data, but it does not widen a nonempty PI system generator after lowering. Such a system model must already use the requested real type. T may not narrow any input; with T=nothing, input scalar types are promoted without narrowing.\n\nThe result uses CompositePIBasis(system_model.basis, FiniteOperatorBasis(mode.levels)), so its coordinate dimension is length(system_model.basis) * mode.levels^2.  It never forms the full system Hilbert space or the full composite Kronecker superoperator.\n\nThe mode dissipator is damping*(nbar+1)*D[a] + damping*nbar*D[a'].  Stochastic trajectories may use result.background with result.damping_channels; deterministic dynamics use result.generator or global_pseudomode_matrixfree.\n\n\n\n\n\nglobal_pseudomode_model(N, system_hamiltonian, mode;\n                        system_terms=(), system_rate=1,\n                        sectors=nothing, kwargs...)\n\nConvenience constructor for a shared pseudomode. system_hamiltonian is the one-particle Hamiltonian summed over all N systems, and system_terms may contain additional PI terms in the bare system dimension.  For a prebuilt system model, prefer the PIModel method so its exact basis and prepared geometry can be reused.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.global_pseudomode_model"},{"category":"function","location":"global_pseudomodes/#PermutationalInvariantDynamics.shared_pseudomode_model","page":"Global pseudomodes and shared cavities","text":"Alias emphasizing that a global pseudomode is one mode shared by the ensemble.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.shared_pseudomode_model"},{"category":"function","location":"global_pseudomodes/#PermutationalInvariantDynamics.global_pseudomode_workspace","page":"Global pseudomodes and shared cavities","text":"global_pseudomode_workspace(model; T=eltype(model),\n                            memory_budget=512*1024^2)\n\nAllocate one task-owned composite application workspace while preserving the prepared BigFloat precision and rounding mode. T must use the prepared model's real floating-point type; rebuild the model to change that precision.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.global_pseudomode_workspace"},{"category":"function","location":"global_pseudomodes/#PermutationalInvariantDynamics.global_pseudomode_matrixfree","page":"Global pseudomodes and shared cavities","text":"global_pseudomode_matrixfree(model;\n                             workspace=nothing,\n                             memory_budget=512*1024^2)\n\nReturn a synchronized MatrixFreeLiouvillian for Krylov and response solvers.  The wrapper owns or reuses exactly one composite workspace and provides explicit forward and adjoint callbacks.  Parallel hot loops should instead call apply! with one global_pseudomode_workspace per task. The memory preflight includes the workspace and the wrapper's copied sparse physical trace functional.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.global_pseudomode_matrixfree"},{"category":"function","location":"global_pseudomodes/#PermutationalInvariantDynamics.global_pseudomode_state","page":"Global pseudomodes and shared cavities","text":"global_pseudomode_state(rho, model)\n\nTrace the PI system factor and return the dense reduced density matrix of the single shared pseudomode.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.global_pseudomode_state"},{"category":"function","location":"global_pseudomodes/#PermutationalInvariantDynamics.global_pseudomode_state!","page":"Global pseudomodes and shared cavities","text":"global_pseudomode_state!(output, rho, model)\n\nTrace the PI system factor into the caller-owned mode density matrix output. This is the preallocated counterpart of global_pseudomode_state; output must have size model.mode.levels × model.mode.levels.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.global_pseudomode_state!"},{"category":"section","location":"mathematics/#Mathematics-and-API","page":"Mathematical reference","text":"For each partition nu, the stored matrix is C_nu; the physical block is C_nu/sqrt(f^nu). Flattening is by partition, then GT patterns, then column major block order. Trace and multiplication implement equations (12) and (15).\n\nThe one-body geometry implements the CG products (B.10–B.11), the three-nu dyadics (22), the local kernel (26–27), and collective blocks (31). Local and collective dissipators implement equations (38) and (39).","title":"Mathematics and API"},{"category":"section","location":"mathematics/#PI-spectra","page":"Mathematical reference","text":"pi_liouvillian_spectrum(L) returns the complete spectrum of a static Liouvillian in PI-coordinate space, sorted by decreasing real part by default. It accepts a PIModel, sparse/dense matrix, or MatrixFreeLiouvillian; vectors=true additionally returns sorted right eigenvectors. A complete spectrum necessarily uses a dense PI-dimensional eigendecomposition, but it never constructs the exponentially larger full Liouville representation.\n\npi_liouvillian_gap(L) computes the slowest nonstationary decay rate after excluding every eigenvalue in a tolerance-controlled stationary cluster. It therefore remains correct for degenerate steady-state manifolds, unlike simply taking the second sorted eigenvalue. With return_info=true it reports the controlling complex mode and oscillation frequency, stationary multiplicity, spectral abscissa, stability flag, tolerance, and PI dimension. Unstable generators raise by default; use check_stability=false for diagnostics.\n\nPass symmetry=U to exploit a verified unitary weak symmetry. The induced Liouville action is diagonalized, equal charges are grouped, and the Liouvillian spectrum is computed independently in each invariant block; the global gap is then selected from their union. return_info=true adds symmetry_sectors, containing each charge, block dimension, stationary multiplicity, spectral abscissa, and sector gap. symmetry=:auto scans the usual unitary candidates and uses the detected candidate with the largest number of charge blocks. For an already assembled PI Liouvillian, also pass basis=basis. Antiunitary covariance alone does not define complex-linear charge eigenspaces and is therefore deliberately not used for this reduction.\n\npi_density_spectrum(rho) diagonalizes the physical Schur blocks and returns values, exact degeneracies, sectors, and within-sector indices. Each eigenvalue is stored once and its symmetric-group multiplicity is kept as an exact integer, so the default output remains polynomial in N at fixed d. Aliases are pi_density_operator_spectrum and density_operator_spectrum. Set expanded=true only for small systems; max_expanded_dimension prevents accidental exponential allocation. Pass the default compressed result to visualize_density_spectrum; sector colours and exact degeneracy annotations are presentation metadata and do not alter the eigenvalues.","title":"PI spectra"},{"category":"section","location":"mathematics/#Evans-uniqueness-certificate","page":"Mathematical reference","text":"evans_uniqueness(H,jumps) implements the finite-dimensional Evans commutant test by finding the joint nullspace of the vectorized commutators with H, every L_k, and every L_k^dagger. A scalar commutant certifies a unique stationary state for the trace-preserving GKSL semigroup. The result reports the numerical commutant dimension and tolerance; use return_basis=true to inspect its vectorized basis. This is an algebraic certificate, distinct from estimating the zero-eigenvalue multiplicity of a numerically assembled Liouvillian. See D. E. Evans, Commun. Math. Phys. 54, 293 (1977), DOI 10.1007/BF01614091.\n\nevans_uniqueness(model) avoids full d^N construction. It first applies cheap exact certificates for one-particle, one-sector, locally irreducible, and collective-only models. General built-in combinations of local, collective, and Appendix-D p-body terms use the ket/bra-regrouped auxiliary space with one local dimension q=d^2. On this space the positive operator\n\nK=mathrmad_H^daggermathrmad_H+\nsum_kleft(mathrmad_L_k^daggermathrmad_L_k+\nmathrmad_L_k^dagger^daggermathrmad_L_k^daggerright)\n\nis permutation invariant. Its Schur blocks are constructed with the same one- and p-body representation geometry as the dynamics, and their kernel dimensions are weighted by the exact symmetric-group multiplicities. Thus the reported dimension is the commutant dimension on the complete physical Hilbert space, not merely in the PI operator subspace.\n\nThe auxiliary PI coordinate count is binomial(N+d^4-1,N), so this stronger test is polynomial at fixed d but can still be expensive. memory_budget guards setup and returns unique=missing before construction when its conservative estimate is too large. Direct PIOperator terms and custom terms also return missing when their microscopic ket/bra recoupling is unavailable. return_basis=true returns kernel vectors compressed by auxiliary Schur sector and records each sector multiplicity; it never expands those copies. Because K is a normal-equation operator, the report distinguishes the requested singular-value tolerance from the effective roundoff floor used for its eigenvalue nullity.\n\nhas_unique_steady_state_evans(model) returns the resulting Bool-or-missing field. The theorem requires a static GKSL generator, so operator-valued time dependence, non-Hermitian Hamiltonian coefficients, and negative or complex dissipative rates are rejected.","title":"Evans uniqueness certificate"},{"category":"section","location":"mathematics/#Weak-unitary-and-antiunitary-symmetries","page":"Mathematical reference","text":"check_liouvillian_symmetry(L,U;kind=:unitary,basis=basis) tests the weak covariance condition mathcal Lmathcal U=mathcal Umathcal L with mathcal U(rho)=Urho U^dagger. For kind=:antiunitary, it treats the Hilbert-space antiunitary as UK in the chosen computational basis and tests mathcal Lmathcal U=mathcal Uoverlinemathcal L, corresponding to mathcal U(rho)=Urho^*U^dagger. Reports include absolute and relative residuals and the tolerance used. is_liouvillian_symmetric returns only the Boolean result.\n\nFor PI inputs, a local d×d unitary is lifted as U^⊗N by exponentiating its Hermitian generator inside each Schur block; neither a full d^N unitary nor a full Liouville transformation is constructed. A PIOperator can specify different unitary actions in the retained Schur sectors. For a full-space Liouvillian, omit basis and pass its Hilbert-space unitary directly.\n\nusual_liouvillian_symmetries checks named clock/phase and cyclic-shift unitaries and complex conjugation. For qubits it additionally checks Pauli-X and Pauli-Z parity and spin time reversal isigma_yK. Custom named tuples, dictionaries, or vectors of pairs can replace either candidate set. These are weak superoperator symmetries; they do not assert that every Hamiltonian and jump operator commutes individually with U (the stronger notion).","title":"Weak unitary and antiunitary symmetries"},{"category":"section","location":"mathematics/#Automatic-strong-symmetry-reduction","page":"Mathematical reference","text":"strong_symmetry_report(model) performs the stronger, term-resolved test: every Hamiltonian and every effective Lindblad operator must commute with the same diagonal local unitary. A jump that changes by a phase can pass the weak covariance test above while correctly failing this strong test. Reports use true, false, or missing; an unfrozen operator schedule is inconclusive instead of being evaluated at an implicit time.\n\nThe default search tests the diagonal clock (Pauli-Z parity for qubits) and solves the exact support constraints over mathbb Z_2 to find binary diagonal sign parities. Its completeness claim is limited to binary sign candidates represented by the supported microscopic matrices. Arbitrary continuous diagonal symmetries, non-diagonal symmetries, and unsupported custom microscopic representations are not claimed to have been exhausted. Every returned true candidate is nevertheless checked term by term.\n\nreport = strong_symmetry_report(model)\nreduction = strong_symmetry_reduction(model; report)\n\n# One validated solution per trace-bearing charge, retained in reduced form.\nstationary = strong_symmetry_steady_states(\n    reduction; embed_states=false)\n\n# Each spectrum remains explicitly charge resolved.\nspectra = strong_symmetry_spectra(\n    reduction; method=:krylov, nev=4)\n\nstrong_symmetry_reduction(model) compiles the source once and constructs a certified RestrictedLiouvillian for every equal ket/bra charge. These are all trace-bearing charge blocks: the routine deliberately does not choose one stationary sector, since strong symmetry generally makes the global stationary state nonunique. strong_symmetry_steady_states returns one trace-fixed solution and its ambient residual for each block. Set embed_states=false to retain only reduced state coordinates after validation. strong_symmetry_spectra similarly returns separate charge-resolved spectra and, by default, checks every returned eigenvector against the unreduced Liouvillian. A selected spectrum remains partial and charge resolved; it is not silently promoted to a certified global gap. Even complete spectra of all trace-bearing blocks omit off-diagonal ket/bra charge sectors and therefore are not the global Liouvillian spectrum. Reduction, multi-sector stationary output, and multi-sector spectral output each enforce one aggregate memory budget rather than applying the same budget independently to every charge.","title":"Automatic strong-symmetry reduction"},{"category":"section","location":"mathematics/#Vectorization-and-permutation-covariance","page":"Mathematical reference","text":"The package uses column-major vectorization. The exported constructors left_superoperator(A), right_superoperator(A), and sandwich_superoperator(A,B) implement, respectively, Xmapsto AX, Xmapsto XA, and Xmapsto AXB^dagger through the identities mathrmvec(AXB^dagger)=(bar Botimes A) mathrmvec(X). commutator_superoperator and dissipator_superoperator build the corresponding Hamiltonian and Lindblad pieces. Sparse PI Liouvillian assembly uses these same centralized identities.\n\nFor small full-space reference calculations, is_pi_operator(A,N,d) tests P_pi A P_pi^dagger=A and is_pi_superoperator(S,N,d) tests covariance of a Liouville matrix under Xmapsto P_pi X P_pi^dagger. It is sufficient to check the N-1 adjacent transpositions, so the implementation permutes matrix indices directly and constructs neither permutation matrices nor all N! group elements. These validation functions necessarily accept objects of dimensions d^N and d^(2N); they are not the production PI simulation path. A PIModel returns true by construction.","title":"Vectorization and permutation covariance"},{"category":"section","location":"mathematics/#Prepared-one-box-Clebsch–Gordan-coefficients","page":"Mathematical reference","text":"One-body contractions and Appendix-D paths repeatedly use the same real one-box U(d) Clebsch–Gordan coefficients.  For several geometry setups on one exact basis, prepare them once and pass the read-only cache explicitly:\n\ncoefficients = OneBoxCGCache(\n    basis; max_depth=3, T=Float64, memory_budget=64 * 1024^2)\none_body = OneBodyGeometry(\n    basis; T=Float64, coefficient_cache=coefficients)\nthree_body = PBodyGeometry(\n    basis, 3; T=Float64, coefficient_cache=coefficients)\n\n# The same cache can serve all compatible geometry families in one model.\nprepared = compile(model; coefficient_cache=coefficients)\n\nmax_depth=1 covers OneBodyGeometry when N > 0 (the zero-particle case uses depth zero); PBodyGeometry(basis,p) requires a cache prepared through at least depth p. The cache is tied to the exact PIBasis object and floating type, stores only the structurally admissible one-box data within that depth, and checks its predicted storage against memory_budget before allocating the coefficient tables. It may be shared by sequential or concurrent read-only construction. Omitting coefficient_cache preserves the call-local path, which computes each transition once for that geometry and retains no process-global mutable state.  That default is often preferable for one small, one-off geometry because a standalone cache also has a setup cost; the explicit cache pays off when its coefficients are reused.  For individual coefficient queries, the same object can be passed as cache=coefficients to cgc or three_nu_symbol.\n\nOrdinary model compilation already uses this optimization automatically when a model needs more than one one-box geometry family and its dimensions are small: currently d=2, N<=32, d=3, N<=8, or d=4, N<=5 (and the trivial d=1, N<=64 case).  These bounds also apply to a restricted basis with the same (N,d).  That automatic cache is bounded by 64 MiB, exists only during lowering, and is released after the finished packed geometries have been built.  A single geometry family keeps the lower-overhead call-local route. Prepare and pass an explicit cache when several independent model compilations should share the same coefficients or when a larger calculation has a deliberately chosen memory budget. The explicit coefficient_cache keyword is accepted by LiouvillianPlan, compile, liouvillian, steady_state, and compile_family; each route validates exact basis ownership, required removal depth, and scalar precision before lowering any term.\n\nThis is deliberately not a table of generic Wigner 3j or 6j symbols. The package's three_nu_symbol is a dyadic of two one-box U(d) coefficients, and Appendix-D isometries are products of the same one-box maps; a Wigner-symbol table would therefore add SU(2)-specific storage without accelerating the qudit geometry.  Qubit reduction and negativity instead prepare the complete recoupling matrices they actually use inside ReductionPlan, where their lifetime, precision, and memory cost remain explicit.","title":"Prepared one-box Clebsch–Gordan coefficients"},{"category":"section","location":"mathematics/#Appendix-D-p-body-processes","page":"Mathematical reference","text":"PBodyGeometry(basis,p) enumerates the allowed sequences of p removable corners and caches the successive one-box CG isometries. These realize the generalized partition triangle and generalized three-nu tensors of equations (D.5)–(D.15), without constructing computational-space operators of size d^N. Setup indexes each GT-pattern list once, caches every one-box edge matrix shared by several removal paths, and propagates all centre patterns through two preallocated buffers. It does not construct a pattern-amplitude dictionary for every local word. Each completed path is retained as one exact- support CSC matrix whose columns combine the centre pattern and local word; Appendix-D contractions iterate those nonzeros rather than scanning the zero-heavy three-index tensor. geometry.estimates reports retained and dense entry counts and payload sizes. Before raw-model compilation, a coefficient-free memory bound follows removal-path multiplicities and GT-content selection rules for every distinct body order. Driven cancellation-risk blocks accumulate their error bound in preallocated real scratch while traversing the same packed support as the value kernel.\n\npbody_collective_operator(basis,X,p) represents sum_n_1cdotsn_pX^(n_1ldotsn_p), while pbody_kernel_operator(basis,X,Y,p) represents the local superoperator K_XY of equation (D.2). The term constructors are:\n\nPBodyHamiltonian(H,p) for the symmetric sum of p-particle Hamiltonians;\nLocalPBodyJump(L,p) for a sum of independent subset dissipators;\nCollectivePBodyJump(L,p) for the dissipator of the coherent subset sum.\n\nThe supplied d^p × d^p operator must be invariant under permutations of its p tensor factors. Static sparse and preallocated matrix-free Liouvillians support all three terms. At fixed d and p, path enumeration remains polynomial in N; the unavoidable local contraction cost grows with d^(2p).","title":"Appendix-D p-body processes"},{"category":"section","location":"mathematics/#Tracing-an-internal-factor-of-every-supersite","page":"Mathematical reference","text":"local_factor_trace changes the local PI dimension while keeping all N particles. For a local factorization mathbb C^d_1otimesmathbb C^d_2, it applies the same partial trace inside every supersite. This is not the particle reduction performed by reduced_state.\n\nLet E_ab be the matrix-unit basis on the kept factor and let mathbf n contain the occupation counts of its d_rm keep^2 letters. The normalized symmetric operator\n\nB_mathbf n\n=frac1sqrtM_mathbf n\n sum_textdistinct strings\n E_r_1otimescdotsotimes E_r_N\nqquad\nM_mathbf n=fracNprod_r n_r\n\nis one member of an orthonormal basis of the kept-factor PI operator space. There are exactly binomN+d_rm keep^2-1N such occupations. If q_mathbf n is its kept-factor Schur-coordinate vector and ell_mathbf n is the source-coordinate vector after the adjoint insertion E_abmapsto E_abotimes I (or Iotimes E_ab), then\n\nx_rm kept\n=Q L^dagger x_rm source\nqquad\nQ=q_mathbf nquad L=ell_mathbf n\n\nThe columns are constructed by polarizing the same one-box Schur recurrence used for tensor-power states. If P_lambdamathbf n is the coefficient of the corresponding matrix-unit monomial in the physical irrep block, its stored equation-(7) block is\n\nC_lambdamathbf n\n=sqrtfracf^lambdaM_mathbf n\n P_lambdamathbf n\n\nThus setup never enumerates d^N local words. A LocalFactorTracePlan retains the exact nonzero support of L and Q as sparse CSC matrices; structural zeros are selected with iszero, never a numerical dropping tolerance. A LocalFactorTraceWorkspace owns the one short occupation vector used by the two matrix-vector contractions. The complete kept-factor output basis is required even for a sector-restricted source because local tracing can populate several output Young sectors. Setup validates lVert Q^dagger Q-IrVert_infty one sparse Gram column at a time. It retains row adjacency plus one stamped output-sized accumulator, rather than materializing the potentially dense Q^dagger Q matrix.","title":"Tracing an internal factor of every supersite"},{"category":"section","location":"mathematics/#Entanglement-negativity","page":"Mathematical reference","text":"negativity(rho, k) and logarithmic_negativity(rho, k) compute the exact partial-transpose trace norm for general PI states. Fully symmetric states use occupation-number branching into Sym^k(C^d) ⊗ Sym^(N-k)(C^d). General qubit states use multiplicity-free SU(2) Clebsch–Gordan recoupling. General qudit states construct the matching Littlewood–Richardson/subduction multiplicity spaces as nullspaces of U(d) generator intertwining equations. All representations have polynomial size in N at fixed d. LR multiplicities are counted exactly by lattice tableaux; forbidden weights are removed before sparse simple-root assembly and SPQR nullspace recovery. Prepared plans retain only exact-support discarded-weight blocks, but the rank-revealing factorization and temporary dense nullspace basis can nevertheless become the practical setup bottleneck for larger d and partitions. Large-spin qubit recoupling switches from an allocation-light factorial formula to an exact-rational Racah recurrence before machine factorials can overflow. Reduction and negativity contractions fuse exact Schur multiplicities into the stored coefficient blocks, so a representable physical answer is not lost merely because an intermediate per-copy block underflows.\n\nreduced_state(rho, k) returns the k-particle marginal as another normalized PIState. reduced_purity(rho, k) computes mathrmtr(rho_k^2), while reduced_purities(rho) returns the values for every subsystem size. None of these routines constructs the exponentially sized computational-basis marginal.\n\nquantum_fisher_information(rho, G) (or qfi) evaluates the exact mixed-state spectral formula for a collective observable. Passing a local matrix constructs G=sum_n G^(n); a PIOperator can be passed directly. Sector results are weighted by their exact symmetric-group multiplicities, without constructing a full-Hilbert-space matrix. The implementation diagonalizes the bounded multiplicity-weighted blocks sqrtf^nuC_nu rather than first forming the possibly unrepresentable physical block C_nusqrtf^nu.\n\nquantum_fisher_information_matrix(rho, generators) (alias qfim) extends this to multiparameter unitary estimation. It diagonalizes every density block once, shares collective geometry between local generators, and returns a real symmetric matrix. Local matrices and existing PIOperator generators may be mixed.\n\ncollective_expectation(rho, X), collective_variance(rho, X), and collective_moments(rho, X) contract equation-(31) blocks directly. They avoid assembling the flattened collective_operator; solution-wrapper methods return the corresponding quantity along a trajectory. For repeated observable, covariance-matrix, or scalar-QFI calls on the same basis, construct cache=OneBodyGeometry(rho.basis) once and pass cache=cache; this avoids rebuilding representation geometry without changing the contraction.\n\nRun julia --startup-file=no --project=benchmark benchmark/performance_audit.jl for the global warmed time/allocation report and its sparse-versus-matrix-free precision guards. The BenchmarkTools workloads in benchmark/benchmarks.jl cover longer statistical measurements and larger scaling studies.","title":"Entanglement negativity"},{"category":"section","location":"mathematics/#Information,-metrology,-and-response","page":"Mathematical reference","text":"Sectorwise spectral functions provide von_neumann_entropy, renyi_entropy, reduced entropies, mutual/conditional information, fidelity, Bures and trace distances, and quantum relative entropy. Exact multiplicities are included. Large multiplicities enter through exact binary scaling and logarithms, while the ordinary representable case keeps the direct machine-arithmetic path. Numerical-rank decisions are relative to each sector's own weighted spectrum, so a small but physically relevant sector is not discarded by a tolerance set by another sector.\n\nEvery density spectrum used by these functions is first checked for positivity with the requested atol and rtol. Accepted nonpositive roundoff values may then be treated as numerical zeros only inside a square root, logarithm, or support test; neither the state nor any returned spectrum is clipped. Fidelity uses this rule for its two square roots and corrects a final result to an endpoint of [0,1] only inside a scale-aware arithmetic bound. A result farther outside the interval raises, and the Bures distance does not mask it with an unconditional clamp.\n\nFor quantum_relative_entropy, sigma's numerical support in a sector is defined relative to that sector's weighted spectral scale (the floor is 8n eps(T) times that scale for an n-dimensional block). Rho's total weight on the complementary projector is evaluated in one contraction. Positive weight above the analogous numerical roundoff floor gives Inf; it is not dropped through separate rho-eigenvalue or eigenvector-overlap cutoffs. The same support rule is used by relative_entropy_decomposition.","title":"Information, metrology, and response"},{"category":"section","location":"mathematics/#Symmetry-resolved-information","page":"Mathematical reference","text":"sector_resolved_entropy returns the probability of every Schur sector and the entropy of its normalized conditional irrep state. entropy_decomposition splits the total entropy exactly into the Shannon entropy of the sector label, conditional irrep entropy, and the entropy of the symmetric-group multiplicity space. relative_entropy_decomposition provides the analogous classical/intra-sector split for quantum relative entropy, using the same sector-projector support test as the undecomposed quantity.\n\nsector_resolved_coherence uses the stored GT basis and reports normalized conditional relative-entropy coherence and its weighted contribution. symmetry_twirl instead pinches within degenerate eigenspaces of a supplied collective charge. The entropy increase is returned by relative_entropy_of_asymmetry (relative_entropy_of_symmetry), while wigner_yanase_asymmetry provides the skew-information measure. Local charges are lowered to a CollectiveObservablePlan, so these routines can use finite physical generator blocks even when storing the corresponding equation-(7) PIOperator coefficients would overflow.\n\nsector_resolved_qfi decomposes collective-generator QFI into conditional sector QFIs. For general parameter tangents, qfim_sector_decomposition separates classical Fisher information from changing sector probabilities and the remaining intra-sector QFIM.\n\ncharge_resolved_negativity resolves negativity into eigenspaces of the partial-transpose imbalance Q_A^T-Q_B. It verifies the corresponding charge symmetry rather than silently discarding off-block terms. number_resolved_negativity uses the local charge diag(0:d-1).\n\nCollective covariance matrices support Kitagawa–Ueda and Wineland squeezing, QFI entanglement-depth bounds, two-particle correlators, and normalized second-order correlations. Multiparameter tools include SLD operators, their commutator/compatibility matrix, mean Uhlmann curvature, QFIM from state tangents, and classical Fisher information for PI POVMs.\n\nOpen-system response helpers expose decay modes and observable overlaps, resolvent norms and grid pseudospectral abscissae, adjoint observable evolution, integrated autocorrelation times, steady-state susceptibilities, and SciML tangent problems. Dense spectral routines are intended for moderate PI dimensions; they do not construct the full d^N Hilbert space.\n\nsteady_state(model) uses the exact equation-(7) trace functional in a bordered, trace-constrained solve and checks stationarity and normalization. For an explicitly supplied matrix use steady_state(L; basis=basis), since an unannotated matrix does not determine the physical trace convention. The SVD method handles degenerate stationary manifolds and return_info=true reports the residual, trace error, numerical nullity, and selected algorithm.\n\nThe method keyword selects the numerical algorithm:\n\n:auto tries the bordered direct solve and falls back to SVD;\n:direct uses only the trace-bordered sparse solve;\n:svd returns the minimum-norm trace-one member of a degenerate nullspace;\n:eigen uses the dense eigenvector nearest zero;\n:shiftinvert performs sparse inverse iteration near shift (:shift_invert and :inverse_iteration are aliases);\n:krylov (or :gmres) solves the trace-fixed stationary equation with restarted matrix-free GMRES.\n\nShift-invert accepts shift, maxiter, and initial_state. A small nonzero shift is selected automatically if omitted. atol and rtol control the stationarity test. return_info=true additionally reports the iteration count, approximate eigenvalue, convergence flag, residual, and trace error. Basic diagnostics do not perform an extra factorization. Request diagnostics=:nullity explicitly when a dense SVD-based nullity estimate is required.","title":"Symmetry-resolved information"},{"category":"section","location":"mathematics/#Prepared-models-and-workspaces","page":"Mathematical reference","text":"compile(model; backend=:auto) builds fixed one- and p-body Schur data once and returns a CompiledPIModel. The backend choice and conservative memory estimate are available in prepared.backend and prepared.estimates. Repeated and concurrent matrix-free calls should use caller-owned scratch:\n\nprepared = compile(model; backend=:matrixfree)\nwork = LiouvillianWorkspace(prepared)\napply!(dest, prepared, source, t, parameters, work)\n\nUse one workspace per task or thread. The compatibility mul! and action! entry points are synchronized and safe, but serialize concurrent calls. apply_adjoint! and batched matrix application use the same prepared kernels.\n\nCollectiveObservablePlan(basis, X) stores collective Schur blocks for a frequently sampled observable. Construct several plans from one OneBodyGeometry and then pass the plan directly to collective expectation, variance, QFI, or QFIM routines. ReductionPlan(basis, k) similarly reuses qubit recouplers or qudit LR/subduction geometry through the plan= keyword of reduced-state, purity, negativity, and partial-transpose routines.\n\nReductionWorkspace(plan, rho) adds caller-owned product-block, partial-trace, partial-transpose, and reduced-sector scratch for repeated state scans. Pass it through workspace= to reduced_state, reduced_purity, or negativity. Use mode=:reduction to omit partial-transpose storage or mode=:negativity to omit partial-trace and reduced-sector storage when a balanced bipartition makes the general-purpose mode=:both workspace too large. A mode-specific workspace rejects incompatible operations. reduced_state!(out, rho, plan, workspace) also avoids allocating a new result. Plans may be shared; use one mutable workspace per concurrent task.\n\nObservable plans retain one Schur-sized matrix per sector. Qudit reduction plans can be substantially larger because they retain all required LR intertwiners. Setup uses exact tableau counts and weight-restricted sparse simple-root constraints; the prepared result keeps only exact-support CSC blocks at fixed discarded-factor weight. plan.estimates exposes the packed and dense-equivalent entry counts. Product-irrep dimensions are checked before allocation and retained in each packed map. SPQR can still dominate setup for large qudit irreps. See API tiers and prepared analysis for usage and memory tradeoffs.","title":"Prepared models and workspaces"},{"category":"section","location":"mathematics/#Periodic-and-Floquet-dynamics","page":"Mathematical reference","text":"time_evolve(L,rho,tspan) propagates a density matrix after its Liouvillian has been defined. Sparse/dense matrices and matrix-free Liouvillians share a fixed-step fourth-order Runge–Kutta implementation. evolve! writes into a supplied state or coordinate vector, and an EvolutionWorkspace reuses all three full scratch arrays (stage state, derivative, and weighted accumulator) across repeated calls. The classical RK4 formula and order are unchanged; only its forward storage schedule differs. time_evolution(L,rho,times) advances sequentially with one workspace. Step counts should be convergence-tested for stiff generators; dynamics_problem remains available for adaptive solvers.","title":"Periodic and Floquet dynamics"},{"category":"section","location":"mathematics/#Quantum-trajectories","page":"Mathematical reference","text":"quantum_trajectory(model,rho0,times;dt=...) implements a stochastic quantum-jump unraveling entirely in PI coordinates. Collective and direct-PI jumps use their usual gain maps. A local channel is unresolved over particle labels and uses sum_i L_irho L_i^dagger; consequently an individual PI trajectory can be mixed even though its ensemble converges to the same master equation. Appendix-D local and collective channels follow the same rule.\n\nWith algorithm=:fixed, the normalized no-jump equation is integrated with preallocated RK4 stages and steps are shortened automatically to enforce max_jump_probability. With algorithm=:event (also :adaptive or :event_driven), an embedded Dormand–Prince 5(4) solve advances the normalized state and its integrated hazard together. Bisection locates each continuous hazard root, so jump times are not tied to sampling-grid endpoints. abstol, reltol, dtmin, dtmax, and event_time_tolerance control this adaptive path. TrajectoryPlan compiles the immutable representation-theoretic geometry once. TrajectoryWorkspace owns the mutable integrator scratch for one path, and TrajectoryBatchWorkspace retains one such workspace and RNG per worker for reuse across ensembles. Fixed-step RK4 uses three full-vector registers; mode=:fixed omits the otherwise unused k3 and k4 registers plus six adaptive-only vectors per worker. The default mode=:full supports both algorithms and is required for continuous event-time integration. An empty model accepts T=Float32 (or another concrete real floating type) because it has no term from which to infer that precision. During no-jump propagation, the rate-weighted K^dagger K Schur blocks are first combined into one effective loss operator per sector. Individual channel intensities and a full gain state are formed only when an actual jump must be selected. Accepted Dormand–Prince stages supply a four-scalar quartic hazard interpolant, so bisection locates the event without repeating full integration trials. quantum_trajectories creates reproducible independent realizations and trajectory_average returns the sampled ensemble density matrices. For an autonomous fixed-operator unraveling, trajectory_steady_state(source, rho0; trajectories, settling_time, dt, ...) streams an approximate stationary density operator without retaining path histories or jump records. If several post-settling samples are requested, it first forms the time average on each path and then averages those independent path means. Consequently correlated samples separated by sampling_interval do not artificially increase the reported sample count. At least two paths are required. The Hilbert–Schmidt sample spread and standard error reported by TrajectorySteadyStateResult are computed across these independent path means in the orthonormal PI coefficient coordinates. Optional Hermitian observables use the same path-level sampling unit for their variances, standard errors, and normal confidence intervals.\n\nThe returned Liouvillian residual, relative residual, and trace error diagnose the averaged density operator but do not certify convergence or uniqueness. Research use must independently increase the settling time, vary the within-path spacing and sample count, converge fixed-step or event-driven integration controls, and increase the number of trajectories. In a non-unique stationary manifold the ensemble mean may depend on the initial state. For a fixed Lindbladian its ensemble-mean evolution is independent of the valid unraveling; the conditional-path distribution, Monte Carlo variance, and finite-sample realization can depend on that unraveling. Driven sources are rejected: use Floquet analysis for periodic dynamics or freeze only when the instantaneous autonomous generator is the intended question.\n\nThe weak-PI pseudo-ket estimator follows the same path-level reduction, but the nonlinear density conversion is performed first. For path r and its post-settling samples,\n\noverline C^(r)_nu\n=frac1Ksum_k\nfracpsi^(r)_nu(t_k)rangle\n      langlepsi^(r)_nu(t_k)sqrtf^nu\nqquad\nwidehat C=frac1Msum_roverline C^(r)\n\nNo cross-sector outer product is physical, and amplitudes must not be averaged before this map. weak_pi_trajectory_steady_state consequently returns a mixed PIState. Its Hilbert–Schmidt uncertainty uses C_2 in the orthonormal PI coefficient basis and treats only the M path means as independent samples. Stochastic trajectories require finite, nonnegative real rates representable in the prepared precision; negative time-local rates remain supported by deterministic evolution but do not define this jump process. Time grids, fixed-step dt, and explicitly supplied adaptive controls must likewise be representable without narrowing. Defaults and every RK stage are formed in the prepared real precision. Fixed-step dt, adaptive tolerances, and the number of trajectories must be convergence-tested for research results. Set threaded=true to use dynamically scheduled, task-owned workspaces. The scheduler claims small chunks of paths to amortize atomic operations without turning a long realization into a static load imbalance. Random streams are seeded by trajectory index, so a fixed seed produces the same ordered paths in serial and threaded execution. For repeated ensembles, construct one TrajectoryPlan and TrajectoryBatchWorkspace and pass the latter as workspace=; plans may be shared, whereas batch workspaces may only be reused sequentially. Outer trajectory threading generally performs best when BLAS is not itself using several threads, but the package never changes the process-wide BLAS configuration.\n\nThe returned trajectories intentionally own independent time grids, jump records, and saved PI states. Their unavoidable output payload therefore scales as O(n_rm traj n_rm save n_rm PI) even though plan and integrator storage are shared or reused. For large PI dimensions, save only the times required for the analysis; retaining a dense time history can dominate both the batch workspace and the simulation kernel's temporary memory.\n\njump_statistics reports total and channel-resolved counts, unbiased count variances, empirical rates, Fano factors, no-jump probability, and pooled waiting times. trajectory_observable_statistics accepts named Hermitian local matrices or PI operators and returns time-resolved Monte Carlo means, unbiased variances, standard errors, and configurable normal confidence intervals. trajectory_statistics combines both reports with the averaged PI density matrices. Observable operators are assembled once and reused across all realizations and sampling times.\n\nTime-dependent scalar coefficients may be supplied as rate=(t,p)->... on any fixed one-body, collective, direct-PI, or Appendix-D term. The matrix-free Liouvillian caches its operator geometry and sector workspaces once; evaluating the coefficient does not assemble an instantaneous sparse superoperator. InPlaceTimeOperator(prototype, update!) likewise prepares every built-in operator-valued term, including local, collective, direct-PI, and Appendix-D p-body processes. Evaluated operators, Schur blocks, quadratic jump data, and local p-body path contractions live in LiouvillianWorkspace; a raw function remains the allocating compatibility path.\n\nfloquet_propagator(model,T) integrates the one-period PI map using a preallocated fourth-order Runge–Kutta workspace. floquet_multipliers, floquet_exponents, and floquet_gap analyze this map. floquet_steady_state solves for its trace-normalized fixed point, while stroboscopic_evolution and floquet_evolve apply the map for integer numbers of periods. Propagator buffers derive their precision from the Liouvillian, period, and time origin: a completely Float32 problem stays ComplexF32. Materialized and compatible custom operators can promote for wider time inputs. A compiled matrix-free PI plan owns fixed-precision application scratch, so a wider period or origin is rejected before allocating the propagator; compile the model at Float64 when Float64 integration is required. An integer time must be exactly representable in the selected working precision. steps controls the fixed-step convergence and should be checked for demanding or stiff protocols.\n\nfloquet_gap(F, T; atol=...) removes a fixed multiplier only when an eigenvalue lies within atol of one. It raises when the supplied map has no such fixed point instead of silently discarding an unrelated closest mode, and also raises when a remaining multiplier is outside the unit disk by more than atol. The returned decay rate follows the multiplier and period precision; a one-dimensional map returns a precision-matched NaN because it has no subleading mode.","title":"Quantum trajectories"},{"category":"section","location":"schur_visualization/#Visualizing-Schur-blocks","page":"Schur-block visualization","text":"The Schur-block visualizer turns a PI state, operator, or superoperator into a compact sector diagram. It has no plotting dependency: visualize_schur_blocks returns a small object with an SVG display, which notebooks can render inline, and save_schur_block_visualization writes the same SVG to a file.\n\nFor a PI state, one tile is shown for every partition nu retained by the PIBasis. For a superoperator the diagram is a block matrix: rows are output sectors and columns are input sectors. Thus a nonzero tile at row lambda and column nu means that the map can send an operator in sector nu to sector lambda. Collective Hamiltonians and collective jumps are sector diagonal. Permutation-invariant sums of unresolved local channels can couple different Schur sectors, and appear as off-diagonal tiles.","title":"Visualizing Schur blocks"},{"category":"section","location":"schur_visualization/#Quick-start","page":"Schur-block visualization","text":"using PermutationalInvariantDynamics\n\nbasis = PIBasis(4, 2)\nsigma = ComplexF64[0.7 0.1; 0.1 0.3]\nrho = iid_state(basis, sigma)\n\nstate_structure = schur_block_structure(rho)\nstate_figure = visualize_schur_blocks(state_structure;\n    title=\"Product state\")\ndisplay(state_figure)\n\nsm = ComplexF64[0 1; 0 0]\nmodel = PIModel(basis, [LocalJump(sm; rate=0.2)])\nprepared = compile(model; backend=:matrixfree)\n\ngenerator_structure = schur_block_structure(prepared.operator, basis)\ngenerator_figure = visualize_schur_blocks(generator_structure;\n    title=\"Local decay Liouvillian\", show_young_diagrams=true)\ndisplay(generator_figure)\n\nThe analysis and rendering steps are separate deliberately. Reuse a structure to change the title or colour scale without probing a matrix-free operator again. The sector labels and activity threshold belong to the extracted structure and remain fixed.","title":"Quick start"},{"category":"section","location":"schur_visualization/#Young-diagrams-and-tableau-multiplicities","page":"Schur-block visualization","text":"Young diagrams are shown by default beside the sector labels. For a state or operator, each top-axis diagram is the shape of its diagonal Schur sector. For a superoperator, the left-axis diagrams label output sectors and the top-axis diagrams label input sectors, matching the row/column convention of the block matrix.\n\nA partition shape does not select one standard Young tableau. In the PI basis the standard-tableau label is summed over, and the number of such labels is the exact symmetric-group multiplicity f^nu. Hovering a diagram reports the padded partition, its U(d) irrep dimension, f^nu, and this convention. Use show_young_diagrams=false when a compact text-only layout is preferred.\n\nFor diagrams with at most 64 boxes, the SVG draws every box. Larger shapes use normalized row bands with at most 64 graphical nodes, so rendering cost and file size remain bounded independently of a large particle number. This changes only the thumbnail; the partition and exact multiplicity in the tooltip remain unchanged.","title":"Young diagrams and tableau multiplicities"},{"category":"section","location":"schur_visualization/#Coefficient-blocks-and-physical-blocks","page":"Schur-block visualization","text":"In the package convention, a state is stored as coefficient blocks C_nu in the orthonormal PI basis of equation (7). Its physical block in the Schur–Weyl decomposition is\n\nrho_nu=fracC_nusqrtf^nu\n\nwhere f^nu is the symmetric-group multiplicity. The complete operator is bigoplus_nu rho_nuotimes I_f^nu. Consequently,\n\nlVert C_nurVert_F^2\n=f^nulVertrho_nurVert_F^2\n\nChoose which matrix is measured with representation=:coefficient or representation=:physical. With metric=:frobenius, the coefficient choice makes the squared value the Hilbert–Schmidt contribution of that whole Schur sector, including its multiplicity. The physical choice instead measures one irrep copy and omits f^nu. metric=:trace_norm applies the same choice to the Schatten one-norm. For a density operator, metric=:population reports the trace weight f^numathrmtr(rho_nu). No metric changes or renormalizes the state. Population mode requires each reported sector trace to be real (up to roundoff) and nonnegative; it throws for invalid data instead of taking an absolute value or clipping it. physical_block(rho, nu) remains the appropriate API when the matrix acting on one irrep copy is needed explicitly.\n\nmetric=:trace_norm uses LinearAlgebra.svdvals and therefore requires a scalar type supported by Julia's SVD backend. Unsupported types such as BigFloat produce a clear ArgumentError; :frobenius and :population retain the state's scalar precision.\n\nFor a superoperator with metric=:frobenius, each value is the Frobenius norm of the corresponding linear map between vectorized coefficient blocks. The coefficient basis is orthonormal and uses Julia column-major vectorization. The value therefore measures total coupling strength across all source coordinates in that block; it is not a transition probability, rate, eigenvalue, or spectral norm. metric=:maxabs instead reports the largest absolute matrix element in the block. Comparisons are most informative between blocks of the same model and with the same metric.","title":"Coefficient blocks and physical blocks"},{"category":"section","location":"schur_visualization/#Thresholds-and-colour-scaling","page":"Schur-block visualization","text":"The numerical structure retains the measured block norms. A visualization threshold only controls which tiles are declared active and drawn as couplings; it does not modify the state or superoperator. Use an absolute threshold in the same units as the block norm, and increase it only to hide roundoff-level leakage:\n\nstructure = schur_block_structure(\n    prepared.operator, basis; threshold=1e-12)\nfigure = visualize_schur_blocks(structure; title=\"Resolved couplings\")\n\nDo not interpret a block hidden by an arbitrarily large threshold as a proven selection rule. For a statement about exact structure, compare against a scale-aware tolerance and the construction of the physical terms.\n\nColour normalization affects only the SVG. normalize=:global uses a common scale, while :row, :column, and :none support more specialized structural comparisons. scale=:linear preserves magnitude ratios; scale=:log reveals weak couplings across a wider dynamic range. The original norms remain in structure.weights and the thresholded Boolean mask in structure.active; use those fields for assertions or quantitative reporting. With normalize=:none, raw values are interpreted directly on the unit colour interval, so values above one saturate at the darkest colour.","title":"Thresholds and colour scaling"},{"category":"section","location":"schur_visualization/#Sparse-and-matrix-free-superoperators","page":"Schur-block visualization","text":"A sparse PI Liouvillian can be split directly into sector rectangles. For a matrix-free Liouvillian, the exact Frobenius norm of every block is obtained by applying the map to coordinate vectors, one input coordinate at a time, and accumulating the squared output norm by sector. If the PI coordinate dimension is\n\nn_mathrmPI=sum_nu dim(U_nu)^2\n\nthis requires n_mathrmPI applications of the Liouvillian. It avoids storing the n_mathrmPItimes n_mathrmPI matrix, but it is a diagnostic setup cost rather than a cheap operation inside a time-stepping or parameter-scan loop. Compute the structure once and reuse it.\n\nThe matrix-free probe uses PI vectors and the compiled Schur kernels. It never constructs a d^N Hilbert-space matrix or a d^2N Liouville-space matrix. A superoperator's linear dimensions alone do not determine its Schur sector boundaries, so supply the matching PIBasis:\n\nstructure = schur_block_structure(prepared.operator, basis)\n\nFor a time-dependent matrix-free map, time= and parameters= define the instantaneous generator at the supplied values. Fixed-operator scalar rates are evaluated by the prepared kernels during each probe; general operator-valued terms are evaluated and lowered once before probing:\n\nstructure = schur_block_structure(\n    driven.operator, basis; time=t, parameters=parameters)\n\nThe same matrix-free path accepts MatrixFreeSymmetryProjector objects and uses one reusable SymmetryProjectorWorkspace; their diagrams expose which Schur sectors are retained by the selected weak-symmetry charge.","title":"Sparse and matrix-free superoperators"},{"category":"section","location":"schur_visualization/#Saving-SVG-without-repository-artifacts","page":"Schur-block visualization","text":"mktempdir() do directory\n    path = joinpath(directory, \"schur-blocks.svg\")\n    save_schur_block_visualization(path, generator_figure)\n    @assert isfile(path)\nend\n\nSVG is text and remains sharp in papers and presentations. The runnable examples/irrep_block_visualization.jl example solves a pump–decay steady state, renders its sector populations and multiplicity-compressed density spectrum, compares collective and local dissipators, and writes all generated figures only inside a temporary directory.","title":"Saving SVG without repository artifacts"},{"category":"section","location":"accelerators/#Optional-accelerators","page":"Optional accelerators","text":"The package treats accelerator support as an explicit prepared backend, not as an automatic array conversion. This is important for PI calculations: a matrix-free Schur-block action is often more memory-efficient on the CPU than materializing its PI-coordinate matrix merely to move it to a GPU.","title":"Optional accelerators"},{"category":"section","location":"accelerators/#Current-support-boundary","page":"Optional accelerators","text":"The stable core exposes capability and resource preflight:\n\ncompiled = compile(model; backend=:matrixfree)\n\ncapability = accelerator_capability(:cuda)\nreport = accelerator_preflight(\n    compiled;\n    rhs_columns=16,\n    rhs_kind=:matrix,\n    memory_budget=2 * 1024^3,\n    device_memory_budget=1024^3,\n)\n\ncapability.functional\nreport.ready\nreport.issues\nreport.combined_peak_bytes\nreport.device_peak_bytes\n\nThe core does not currently claim a functional CUDA backend. A CUDA package being installed does not prove that a compatible device, driver, sparse library, scalar precision, and tested multiplication path are available. Consequently,\n\naccelerate(compiled; backend=:cuda)\n\nraises before materialization unless a tested optional extension reports a functional backend.\n\nThis conservative boundary prevents two failure modes:\n\nsilently constructing a large sparse PI Liouvillian on the host before discovering that the device cannot accept it;\nsilently copying a vector between host and device for every matrix-vector product, which can make an apparently accelerated Krylov solve slower.","title":"Current support boundary"},{"category":"section","location":"accelerators/#Contract-for-a-future-CUDA-extension","page":"Optional accelerators","text":"A CUDA implementation must remain deliberately narrow:\n\naccept an autonomous, already prepared LiouvillianPlan, CompiledPIModel, or SpecializedPIModel;\nmaterialize the same exact-support sparse PI matrix used by the CPU sparse backend, under the normal memory guard;\nupload that matrix once, with checked 32-bit sparse indices;\nsupport only Float32, Float64, ComplexF32, and ComplexF64;\nmultiply device-resident vector and matrix right-hand sides without an implicit transfer;\nreject the general matrix-free Schur kernels rather than presenting a hidden CPU fallback as GPU execution; and\npass forward, adjoint, vector-RHS, and matrix-RHS comparisons on a GPU CI runner before functional=true is exposed.","title":"Contract for a future CUDA extension"},{"category":"section","location":"accelerators/#Understanding-the-preflight","page":"Optional accelerators","text":"accelerator_preflight is allocation-light and never initializes a device. It retains the exact source basis and prepared scalar type in the report.\n\nWhen the source already owns a sparse operator, the report uses its exact nonzero count. For a matrix-free source it uses the same structural nonzero upper bound as guarded sparse materialization. The combined peak includes:\n\nthe already retained prepared source;\nsimultaneous host sparse assembly and result storage, when materialization is required;\ndevice sparse values, row indices, and column pointers; and\nboth input and output device arrays for the requested RHS width.\n\nmemory_budget guards that combined peak. device_memory_budget separately guards device-resident storage. Inf is the only explicit opt-out.\n\nrhs_kind=:auto selects :vector when rhs_columns==1 and :matrix otherwise. Use rhs_kind=:matrix, rhs_columns=1 when the backend must receive a one-column matrix rather than a vector. rhs_kind=:vector requires exactly one column. The selected kind is recorded in report.rhs_kind.\n\nAn unavailable backend is returned as a report issue, rather than being confused with a model incompatibility. Typical issue symbols include :backend_unavailable, :nonautonomous_source, :sparse_materialization_unsupported, :unsupported_scalar_type, :sparse_index_overflow, :rhs_kind_unsupported, :unsupported_transfer_policy, :memory_budget_exceeded, and :device_memory_budget_exceeded.\n\nA functional extension is accepted only when it declares transfer_policy=:explicit_once. Any capability that would perform implicit host/device transfers during multiplication is rejected at preflight.","title":"Understanding the preflight"},{"category":"section","location":"accelerators/#When-acceleration-is-likely-to-help","page":"Optional accelerators","text":"Sparse GPU multiplication is most plausible when the same Liouvillian is applied repeatedly to a wide matrix of right-hand sides. It is usually a poor fit for a single small PI vector, a driven model that must be rebuilt frequently, or a model whose exact sparse materialization is much larger than its matrix-free Schur workspace. Always benchmark full time to solution, including sparse construction and upload.","title":"When acceleration is likely to help"},{"category":"section","location":"accelerators/#API","page":"Optional accelerators","text":"","title":"API"},{"category":"type","location":"accelerators/#PermutationalInvariantDynamics.AcceleratorCapability","page":"Optional accelerators","text":"AcceleratorCapability\n\nImmutable description of one optional accelerator backend. Query it with accelerator_capability before requesting an upload.\n\ntransfer_policy=:explicit_once means that a backend may upload a prepared operator and caller-owned right-hand sides, but must not perform an implicit host/device transfer during each multiplication. A backend reporting supports_matrixfree_pi=false accepts only an explicitly materialized sparse PI Liouvillian, not the general Schur-block matrix-free kernels.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.AcceleratorCapability"},{"category":"type","location":"accelerators/#PermutationalInvariantDynamics.AcceleratorPreflight","page":"Optional accelerators","text":"AcceleratorPreflight\n\nRead-only resource and compatibility report returned by accelerator_preflight. basis is the exact prepared PIBasis owned by the source, and scalar_type is the prepared Liouvillian precision.\n\ncombined_peak_bytes accounts conservatively for the already retained source, the simultaneous host sparse-materialization peak when needed, the uploaded sparse operator, and both input and output device right-hand sides. The device subset is reported separately in device_peak_bytes.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.AcceleratorPreflight"},{"category":"function","location":"accelerators/#PermutationalInvariantDynamics.accelerator_capability","page":"Optional accelerators","text":"accelerator_capability(backend=:cuda)\n\nReport whether an optional accelerator is loaded and functional. This query never initializes a device and never treats package availability as proof that hardware works. In particular, functional=true is reserved for an extension which has completed its own runtime/device checks.\n\nThe core package currently provides a conservative CUDA contract for preflighting only. It reports functional=false unless a separately tested extension implements the backend.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.accelerator_capability"},{"category":"function","location":"accelerators/#PermutationalInvariantDynamics.accelerator_preflight","page":"Optional accelerators","text":"accelerator_preflight(source; backend=:cuda, rhs_columns=1,\n                      rhs_kind=:auto,\n                      memory_budget=512*1024^2,\n                      device_memory_budget=512*1024^2,\n                      bigfloat_precision=precision(BigFloat))\n\nEstimate the simultaneous host and accelerator memory required to upload and apply an exact-support sparse PI Liouvillian. source must be an already prepared LiouvillianPlan, CompiledPIModel, or SpecializedPIModel. Requiring prepared input keeps this query from silently constructing geometry.\n\nThe report uses the actual sparse nonzero count when source already owns a sparse operator. Otherwise it uses the same exact-support upper bound as the guarded sparse PI materializer. No matrix is materialized, no device is initialized, and no memory is allocated in proportion to the PI dimension.\n\nThe intended CUDA contract is narrow: autonomous sources, Float32, Float64, ComplexF32, or ComplexF64, 32-bit sparse indices, and vector or matrix right-hand sides already resident on the device. General matrix-free Schur kernels and implicit per-action transfers are not supported.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.accelerator_preflight"},{"category":"function","location":"accelerators/#PermutationalInvariantDynamics.accelerate","page":"Optional accelerators","text":"accelerate(source; backend=:cuda, rhs_columns=1, rhs_kind=:auto, ...)\n\nPrepare an optional accelerated sparse PI Liouvillian. The core method always runs accelerator_preflight first and rejects unavailable, nonfunctional, incompatible, or over-budget backends before materialization. An optional backend extension must then perform one explicit sparse upload and return an operator whose multiplication accepts device-resident vector and matrix right-hand sides without hidden transfers.\n\nNo CUDA implementation is claimed by the core package. This is intentional: loading CUDA.jl without a supported functional device and GPU test coverage is not enough to establish a reliable numerical backend.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.accelerate"},{"category":"section","location":"inference/#Parameter-inference","page":"Parameter inference","text":"The inference layer fits model predictions to real observations while keeping the derivative route and local identifiability explicit. Its objective is\n\nchi^2(theta)2\n=frac12sum_j\nleft\nfracf_j(theta)-y_jsigma_j\nright^2 \n\nwhere sigma_j are supplied observation standard deviations.\n\nThe core implementation is dependency free. It uses a finite, memory-preflighted Levenberg–Marquardt search and never changes the model, normalizes a failed state, or accepts an unconverged stationary solve.","title":"Parameter inference"},{"category":"section","location":"inference/#Fit-a-generic-predictor","page":"Parameter inference","text":"design = [\n    1.0  2.0\n    3.0 -1.0\n    1.0  0.0\n]\nobservations = [0.2, 1.1, 0.4]\n\nproblem = LeastSquaresInferenceProblem(\n    parameters -> design * parameters,\n    observations,\n    [0.1, 0.1];\n    jacobian=parameters -> design,\n    standard_deviations=[0.03, 0.05, 0.02],\n    parameter_names=(:drive, :detuning),\n    lower_bounds=[0.0, -2.0],\n    upper_bounds=[2.0, 2.0],\n)\n\nresult = fit_parameters(\n    problem;\n    derivative_method=:auto,\n    maxiter=50,\n    memory_budget=512 * 1024^2,\n)\n\nInspect convergence instead of using the fitted vector alone:\n\nresult.converged\nresult.termination\nresult.objective\nresult.derivative_method\nresult.identifiability.identifiable\nresult.identifiability.condition_number\nresult.identifiability.covariance\n\nWhen jacobian is absent, :auto uses finite differences and records every central or one-sided scheme and step. Bounds determine whether central, forward, or backward differences are possible. No complex-step or automatic differentiation method is claimed implicitly.","title":"Fit a generic predictor"},{"category":"section","location":"inference/#Steady-state-observable-fitting","page":"Parameter inference","text":"For PI models, an analytic generator derivative can be propagated through the stationary equation,\n\nmathcal Lpartial_murho_mathrmss\n=-(partial_mumathcal L)rho_mathrmss\nqquad\nmathrmtr(partial_murho_mathrmss)=0\n\nPrepare a model and one derivative generator per parameter:\n\nbasis = PIBasis(N, 2)\nspin = spin_matrices()\nlowering = ComplexF64[0 1; 0 0]\nexcited = collective_operator(\n    basis,\n    ComplexF64[0 0; 0 1],\n)\n\nmodel_builder = parameters -> PIModel(basis, (\n    CollectiveJump(lowering; rate=gamma),\n    LocalHamiltonian(spin.jx; rate=parameters[1]),\n))\n\nderivative_builder = (parameters, model) -> (\n    PIModel(basis, (\n        LocalHamiltonian(spin.jx; rate=1.0),\n    )),\n)\n\nproblem = steady_state_inference_problem(\n    model_builder,\n    (excited,),\n    measured_populations,\n    [0.2];\n    derivative_builder,\n    parameter_names=(:drive,),\n    lower_bounds=[0.0],\n    upper_bounds=[2.0],\n    solver_options=(\n        algorithm=GMRESAlgorithm(\n            krylovdim=30,\n            maxiter=500,\n        ),\n    ),\n)\n\nresult = fit_parameters(problem)\n\nWith derivative_builder, :auto uses implicit_steady_state_gradient. The already computed stationary state is reused for its tangent solve, and its unscaled residual and trace diagnostics remain in the inference metadata. Without it, the same workflow uses and reports finite differences of complete stationary solves.\n\nmodel_builder may return a PIModel, CompiledPIModel, or SpecializedPIModel. Returning specializations of a shared CompiledPIModelFamily avoids rebuilding fixed Schur geometry during a fit.","title":"Steady-state observable fitting"},{"category":"section","location":"inference/#Fisher-and-identifiability-diagnostics","page":"Parameter inference","text":"At a chosen parameter point:\n\nreport = parameter_identifiability(problem, result.parameters)\n\nFor the weighted Jacobian widetilde J_jmu=J_jmusigma_j, the reported local Fisher matrix is\n\nF=widetilde J^mathsfTwidetilde J \n\nThe report includes:\n\nsingular values and the explicit numerical-rank threshold;\nlocal numerical rank and a full-column-rank identifiable flag;\ncondition number;\ncovariance and correlation matrices when the Jacobian is full rank;\na truncated-SVD pseudocovariance for diagnostics when it is not.\n\nThe implementation requests a thin SVD, so a tall data set does not retain an unused observations-by-observations left-singular matrix. Weighting, standardized residuals, objectives, Fisher products, covariance normalizations, and accepted parameter updates are checked for nonfinite overflow and nonzero-to-zero underflow. A failure asks for wider inference data instead of continuing with a silently corrupted diagnostic.\n\nThe pseudocovariance of a rank-deficient fit is not an uncertainty certificate. Likewise, this local Fisher analysis does not establish global identifiability, resolve multimodality, or include model discrepancy.","title":"Fisher and identifiability diagnostics"},{"category":"section","location":"inference/#Resource-and-convergence-boundaries","page":"Parameter inference","text":"The inference memory budget covers its retained Jacobian, Fisher, factorization, and optimization arrays. User builders, compiled models, stationary solvers, and external data have their own resource contracts and are listed as exclusions in the result.\n\nfit_parameters has finite maxiter and max_trials bounds. It returns converged=false with an explicit termination reason when those conditions are not met. Use require_convergence=true when a nonconverged result should raise.","title":"Resource and convergence boundaries"},{"category":"section","location":"inference/#API","page":"Parameter inference","text":"","title":"API"},{"category":"type","location":"inference/#PermutationalInvariantDynamics.LeastSquaresInferenceProblem","page":"Parameter inference","text":"LeastSquaresInferenceProblem(predictor, observations, initial_parameters;\n                             jacobian=nothing,\n                             standard_deviations=nothing,\n                             parameter_names=nothing,\n                             lower_bounds=nothing, upper_bounds=nothing,\n                             metadata=(;))\n\nImmutable description of a real weighted least-squares problem. predictor maps a parameter vector to predicted observations. An optional jacobian returns a matrix whose rows are observations and columns are parameters. Without it, fit_parameters can use explicitly reported finite differences. standard_deviations are known positive observation errors; the objective is\n\nfrac12sum_j\nleft(fracf_j(theta)-y_jsigma_jright)^2\n\nBounds are enforced exactly as supplied. The constructor copies all numerical inputs but cannot make user callbacks immutable or task safe.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.LeastSquaresInferenceProblem"},{"category":"type","location":"inference/#PermutationalInvariantDynamics.ParameterIdentifiabilityReport","page":"Parameter inference","text":"ParameterIdentifiabilityReport\n\nLocal weighted-Jacobian diagnostics at one parameter point. identifiable means the numerical Jacobian has full column rank at the explicit tolerance. covariance is available only in that case. pseudocovariance is always the truncated-SVD local generalized inverse and must not be presented as an uncertainty certificate for a rank-deficient model.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.ParameterIdentifiabilityReport"},{"category":"type","location":"inference/#PermutationalInvariantDynamics.ParameterInferenceResult","page":"Parameter inference","text":"ParameterInferenceResult\n\nBounded Levenberg–Marquardt result. converged=false is retained rather than repaired or relabelled. derivative_method is the method actually used: :analytic, :implicit_steady_state, or :finite_difference.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.ParameterInferenceResult"},{"category":"function","location":"inference/#PermutationalInvariantDynamics.steady_state_inference_problem","page":"Parameter inference","text":"steady_state_inference_problem(model_builder, observables, observations,\n                               initial_parameters;\n                               derivative_builder=nothing,\n                               compile_options=(backend=:matrixfree,),\n                               solver_options=(;), gradient_options=(;),\n                               kwargs...)\n\nConstruct a LeastSquaresInferenceProblem whose predictions are steady-state PI expectation values. model_builder returns a PIModel or prepared PI model for the current parameter vector. If derivative_builder returns the corresponding generators dL/dtheta_mu, derivative_method=:auto uses implicit_steady_state_gradient and reuses the already solved stationary state. Otherwise the inference report explicitly records finite differences.\n\nThe observables must be Hermitian PIOperators on the exact model basis. Compilation and solver choices remain explicit; no failed solve is converted to an approximate prediction.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.steady_state_inference_problem"},{"category":"function","location":"inference/#PermutationalInvariantDynamics.parameter_identifiability","page":"Parameter inference","text":"parameter_identifiability(problem, parameters=problem.initial_parameters;\n                          derivative_method=:auto,\n                          finite_difference_step=nothing,\n                          rank_rtol=nothing,\n                          memory_budget=512MiB)\n\nEvaluate the local weighted Jacobian, Fisher matrix, numerical rank, condition number, and covariance diagnostics without optimizing parameters. Finite differences and implicit-gradient solver metadata are retained in the report.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.parameter_identifiability"},{"category":"function","location":"inference/#PermutationalInvariantDynamics.fit_parameters","page":"Parameter inference","text":"fit_parameters(problem; derivative_method=:auto, maxiter=50,\n               damping=1e-3, max_trials=8,\n               gradient_tolerance=1e-8,\n               step_tolerance=1e-8,\n               objective_tolerance=1e-10,\n               finite_difference_step=nothing,\n               rank_rtol=nothing,\n               memory_budget=512MiB,\n               require_convergence=false)\n\nRun a bounded Levenberg–Marquardt least-squares fit. Every iteration has at most max_trials damping attempts, and the retained dense storage is preflighted against memory_budget. Predictor/model construction and solver storage are callback dependent and are listed as resource exclusions.\n\n:auto uses an explicit Jacobian callback, implicit PI stationary-state gradients, or finite differences in that order of applicability. The actual method, finite-difference schemes, gradient solver reports, rejected steps, and termination reason are returned. Nonconvergence is never relabelled; require_convergence=true raises after producing the same bounded search.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.fit_parameters"},{"category":"section","location":"architecture/#Architecture-and-efficient-workflows","page":"Architecture and efficient workflows","text":"This chapter explains performance and ownership after the physical model has been defined. New users should first follow Getting started: from a model to a solution.","title":"Architecture and efficient workflows"},{"category":"section","location":"architecture/#Data-flow","page":"Architecture and efficient workflows","text":"The package separates declarative physics, immutable prepared data, and mutable numerical scratch:\n\nPIBasis → PIModel → compile → CompiledPIModel\n                              ├─ LiouvillianPlan\n                              ├─ sparse or matrix-free backend\n                              └─ LiouvillianWorkspace per task\n\nfixed operators, varying scalar rates → compile_family\n                                      → CompiledPIModelFamily\n                                      → specialize per parameter point\n\n(N, d, terms) → MeanFieldPlan → one-site nonlinear RHS\n                                └─ MeanFieldWorkspace per task\n\ninvariant PIModel → PopulationPlan → reduced sparse action\n                                   └─ PopulationWorkspace per task\n\n(PIBasis..., finite factors...) → CompositePIBasis\n                               → CompositeSuperoperator\n                                 └─ CompositeSuperoperatorWorkspace per task\n\n(composite background, monitored channels) → CompositeTrajectoryPlan\n                                            ├─ unconditional generator\n                                            └─ CompositeTrajectoryWorkspace per path\n\n(parameter grid, builder) → ParameterScanPlan\n                           → ParameterScanWorkspace per serial caller/worker\n                             (continuation, Liouvillian, and Krylov scratch)\n\n(PI system, HEOMBath...) → HEOMPlan\n                         ├─ HEOMWorkspace per application task\n                         └─ HEOMEvolutionWorkspace per RK4 task\n\n(PI Hamiltonian, shared HOPSBath...) → HOPSPlan\n                                     ├─ immutable hierarchy and noise data\n                                     └─ HOPSWorkspace per stochastic path\n\nPIBasis stores only polynomial-size Schur data. PIModel is an immutable tuple of physical terms. LiouvillianPlan lowers fixed operators to prepared sector blocks and local gain maps. Fixed local one-body and safely scaled Appendix-D gains remain rectangular Schur contractions rather than expanded four-index coordinate tables. When several fixed numerical terms are present, the plan also combines their Hamiltonian blocks and anticommutator loss blocks; gain channels remain separate, so this introduces no dissipative cross terms. The rectangular maps are expanded only for an explicitly requested sparse Liouvillian. The plan never contains mutable application scratch. A LiouvillianWorkspace contains the multiplication matrices needed for each Schur sector and, for an in-place operator schedule, the evaluated operator and dynamic Schur/gain buffers. It can be reused by dynamics and Krylov algorithms.\n\nComposite coordinates keep each PI factor in the same equation-(7) normalization and each finite factor in column-major matrix-unit coordinates. The first factor varies fastest. Tensor-mode kernels apply a sum of factorized maps with two reusable composite buffers and one small fibre buffer per active factor; they do not construct the corresponding global Kronecker matrix. Density-valued composite quantum jumps reuse the same tensor-mode kernel. Their plan assembles the unconditional master generator from a trace-preserving background plus explicit CompositeJumpChannels. A path workspace owns one shared pair of full channel buffers, so full-coordinate storage does not scale with the channel count.\n\nPI–HOPS stores one weak-PI pseudo-ket per retained hierarchy multi-index and never constructs a labeled-particle Hilbert space. This exact reduction is available for shared baths with PI coupling operators, because the stochastic generator then preserves the PI pseudo-ket space on every realization. Independent local colored noises restore permutation symmetry only after averaging and are outside this pathwise PI representation. Linear HOPS root pseudo-kets are unnormalized, so the physical estimator is the arithmetic mean of root outer products, not a mean of pathwise normalized states.","title":"Data flow"},{"category":"section","location":"architecture/#Recommended-commands","page":"Architecture and efficient workflows","text":"prepared = compile(model)  # backend=:auto\n\nsol = solve_dynamics(prepared, rho0, (0.0, 10.0); saveat=0.1)\nrho_ss = stationary_state(prepared)\nvalues = liouvillian_spectrum(prepared; target=:largest_real, nev=6)\n\ndiagnostics(rho_ss)\nrecommend_solver(model; task=:steady_state)\n\nUse state(solution, i) when i is a saved-state index and state_at(solution, t) when t is a physical time. The explicit form avoids the integer ambiguity and uses allocation-free lookup on saved ordered grids.\n\nThe preparation route depends on the task rather than on one universal plan:\n\nTask Input Prepared object Task-owned scratch\nPI dynamics and stationary/spectral solves PIModel CompiledPIModel / LiouvillianPlan LiouvillianWorkspace plus the selected integrator or Krylov workspace\nFixed geometry with changing scalar rates PIModel prototype CompiledPIModelFamily then SpecializedPIModel one Liouvillian/solver workspace per worker\nCertified diagonal dynamics invariant PIModel PopulationPlan PopulationWorkspace\nPeriodic dynamics periodic Liouvillian source FloquetMap FloquetWorkspace or FloquetBatchWorkspace\nFinite-memory bath PI source and HEOMBaths HEOMPlan HEOMWorkspace / HEOMEvolutionWorkspace\nFinite-memory shared-bath trajectories PI Hamiltonian and HOPSBaths HOPSPlan one HOPSWorkspace and RNG per path\nQuantum regression autonomous source and insertions CorrelationPlan CorrelationWorkspace\nParameter continuation parameter recipe or compiled family ParameterScanPlan ParameterScanWorkspace\n\nAll prepared source wrappers participate in one private linear-operator trait protocol. This is an implementation boundary for maintainers, not another public abstraction users must learn.\n\nWhen only observables are needed, replace the first call by\n\nseries = solve_dynamics(\n    prepared, rho0, (0.0, 10.0);\n    saveat=0.1,\n    observables=(Jz=sz/2,),\n    save_states=false,\n)\n\nThis retains one evolving PI vector and the requested scalar series. The same keywords on quantum_trajectories produce online Welford statistics without sampled PIState histories. See Streaming output.\n\nAutonomous dynamical correlations have their own prepared flow:\n\ncompiled Liouvillian + A,B[,R] → CorrelationPlan\n                               → CorrelationWorkspace\n                               ├─ sequential RK4 QRT samples\n                               └─ shifted-GMRES connected spectra\n\nThe readout convention is explicitly tr(A*exp(L*tau)*(B*rho*R)); it differs from the adjoint implicit in a Hilbert–Schmidt expectation call. See Quantum regression and optical correlations.\n\nThese are high-level commands. Lower-level research control remains available through liouvillian, apply!, steady_state, krylov_liouvillian_spectrum, harmonic_arnoldi_spectrum, implicitly_restarted_arnoldi_spectrum, and jacobi_davidson_spectrum. The latter combines hard invariant-subspace locking with optionally preconditioned matrix-free correction solves. Structured repeated systems additionally use block_gmres, multishift_gmres, recycled_gmres, and krylov_expv; their workspaces reuse dominant full-coordinate arrays but still form small dense projected problems. Prepared scans, HEOM, HOPS, and explicit refinement reports have dedicated guides: Parameter scans, PI–HEOM, PI–HOPS, and Numerical convergence. See API tiers and prepared analysis for the supported high-level, advanced, and experimental surfaces.","title":"Recommended commands"},{"category":"section","location":"architecture/#Certified-population-only-workflow","page":"Architecture and efficient workflows","text":"When a model preserves the GT-basis diagonal subspace, construct one PopulationPlan and inspect its stored report:\n\npopulation_plan = PopulationPlan(model)\n@assert population_plan.invariance.invariant === true\np0 = diagonal_populations(rho0)\nsolution = solve_populations(\n    population_plan, p0, (0.0, 10.0); saveat=0.1)\n\nThe default certificate is exact at the working scalar precision. Explicit nonzero tolerances opt into approximate projection; they are never selected automatically from a generator scale. Compile-time reverse lookup has one entry per population and is discarded after construction. The retained plan contains only the basis and reduced sparse kernels, so it does not retain a full-PI-coordinate lookup. Reuse PopulationWorkspace for in-place scans and one workspace per concurrent task.\n\nAt present, plan construction forms the standalone sqrt(f^nu) population coordinate conversion and the parent PI trace functional in the chosen real type. It therefore raises if that factor is outside the finite scalar range; use wider coefficients for such a plan. This bound is stricter than diagonal_populations and state_from_populations, which use prepared scaled products and can still handle finite population values without materializing the standalone factor.","title":"Certified population-only workflow"},{"category":"section","location":"architecture/#Concurrency","page":"Architecture and efficient workflows","text":"For parallel applications, construct one workspace per task or thread:\n\nprepared = compile(model; backend=:matrixfree)\nwork = [LiouvillianWorkspace(prepared) for _ in 1:Threads.nthreads()]\n\nThreads.@threads for column in axes(X,2)\n    apply!(view(Y,:,column), prepared, view(X,:,column), 0.0, nothing,\n           work[Threads.threadid()])\nend\n\nThe compatibility mul! and MatrixFreeLiouvillian.action! routes are synchronized and safe, but concurrent calls serialize. Explicit workspaces avoid that lock.\n\nThe same ownership rule applies to SymmetryProjectorWorkspace, all ordinary and advanced Krylov workspaces, MeanFieldWorkspace, scan workspaces, HEOM and HOPS workspaces, and stochastic batch workspaces: workspaces are mutable and must not be shared by concurrent calls. RecycledGMRESWorkspace additionally owns the ordered continuation chain itself.","title":"Concurrency"},{"category":"section","location":"architecture/#Mean-field-prediction-beside-exact-PI-dynamics","page":"Architecture and efficient workflows","text":"MeanFieldPlan(model) reuses the physical term conventions but does not compile Schur geometry. It evaluates the product-state closure Tr_{2:N} L[sigma^otimes N] with storage set by the local dimension and the largest supported body order. The direct MeanFieldPlan(N, d, terms) constructor therefore remains practical even when constructing PIBasis(N,d) would be unnecessary.\n\nUse limit=:finite when comparing the derivative with exact finite-N PI dynamics. Use limit=:thermodynamic only for explicitly size-scaled rates; it keeps leading combinatorial contributions and drops subleading collective self terms. The package does not infer or insert Kac normalization. See Mean-field predictions for equations and validation examples.","title":"Mean-field prediction beside exact PI dynamics"},{"category":"section","location":"architecture/#Static-and-driven-generators","page":"Architecture and efficient workflows","text":"isautonomous(model) reports whether every operator is fixed and every rate is constant. Stationary-state, spectrum, gap, adjoint, and ordinary mul! operations reject driven generators. To analyse an instantaneous generator, make the choice explicit:\n\nLt = freeze(model; time=1.25, parameters=params)\nrho_t_stationary = stationary_state(Lt; basis=model.basis)\n\nUse solve_dynamics, dynamics_problem, or Floquet routines for actual time-dependent evolution.","title":"Static and driven generators"},{"category":"section","location":"architecture/#In-place-operator-schedules","page":"Architecture and efficient workflows","text":"When the operator itself changes with time, wrap a fixed-shape prototype and an in-place callback in InPlaceTimeOperator:\n\nsm = ComplexF64[0 1; 0 0]\nsx = ComplexF64[0 1; 1 0]\n\ndrive = InPlaceTimeOperator(sm, (destination, t, parameters) -> begin\n    @. destination = cos(t) * sm + parameters.mix * sin(t) * sx\n    nothing\nend)\n\nmodel = PIModel(basis, [LocalJump(drive; rate=(t, p) -> p.gamma)])\nprepared = compile(model; backend=:matrixfree)\nwork = LiouvillianWorkspace(prepared)\napply!(du, prepared, u, t, parameters, work)\n\nThe destination is reset to the prototype before every callback. The callback must mutate it without changing its shape, basis, or scalar type, and return the destination or nothing. Callback allocations remain the callback author's responsibility. Prepared schedules are implemented for every built-in term family, including direct PI and Appendix-D Hamiltonians and jumps. Dynamic collective blocks, quadratic jump products, and all local-gain cross terms are rebuilt in the task-local workspace without assembling a PI Liouvillian. A dynamic local p-body jump retains one contraction buffer per compatible pair of Appendix-D removal paths and applies each gain block as C * X * C' with one reusable matrix-multiplication buffer. It does not store the quadratic-size PI-coordinate I/J/value representation. Inspect the factorized workspace memory for large qudit irreps just as for the static p-body geometry. Fixed-precision scheduled one-body blocks are rejected at compile time once large-N branch cancellation requires wider geometry; widen the InPlaceTimeOperator prototype. Scheduled direct-PI blocks use prepared fused inverse-multiplicity scales and keep the ordinary direct-divisor loop for small representable multiplicities.\n\nA batched apply! or apply_adjoint! evaluates every scheduled operator once and reuses the result for all columns. Separate workspaces are required for concurrent calls. A plain operator-valued Function remains available as an allocating compatibility path and constructs an instantaneous sparse generator. Dissipative rates must evaluate to real numbers; negative real rates remain accepted for deterministic time-local evolution.\n\nKrylov and Floquet storage is precision-derived rather than unconditionally ComplexF64. Fully Float32 inputs retain half-width complex buffers; wider seeds, targets, periods, or time origins promote compatible materialized and custom operators. A compiled matrix-free plan owns fixed-precision block scratch, so wider inputs are rejected and must be handled by compiling the model at the wider precision. Incompatible explicit workspaces also raise. This saves RAM without silently narrowing mixed-precision research inputs.","title":"In-place operator schedules"},{"category":"section","location":"architecture/#Prepared-observables-and-reductions","page":"Architecture and efficient workflows","text":"Repeated observable evaluation should prepare collective blocks once:\n\ngeometry = OneBodyGeometry(basis)\nJx = CollectiveObservablePlan(basis, sx/2; cache=geometry)\nJz = CollectiveObservablePlan(basis, sz/2; cache=geometry)\nmeans = collective_expectation.(states, Ref(Jz))\nF = qfim(last(states), [sx/2, sz/2]; plans=(Jx, Jz))\n\nThe geometry is shared only during construction; each observable plan retains one matrix per Schur sector. For a fixed bipartition, ReductionPlan(basis,k) caches qubit recouplers or qudit LR/subduction intertwiners:\n\nreduction = ReductionPlan(basis, k)\nwork = ReductionWorkspace(reduction, rho)\nrhoA = reduced_state(rho, k; plan=reduction, workspace=work)\npurityA = reduced_purity(rho, k; plan=reduction, workspace=work)\nneg = negativity(rho, k; plan=reduction, workspace=work)\n\nBoth plan types require the exact basis object used at construction. Observable plans are compact and usually worthwhile for state scans. Qudit reduction plans construct sparse weight-restricted nullspaces and retain each resulting intertwiner as exact-support CSC blocks at fixed discarded-factor weight. Their estimates field reports retained versus dense entry counts and payload bytes. Nullspace setup can nevertheless be substantial, so these plans are intended for repeated fixed-k analysis rather than every one-off bipartition. Plans contain no mutable scratch and may be shared read-only. A ReductionWorkspace belongs to one exact plan and reuses the recoupling and application buffers; it must not be shared concurrently. Reduced-state and purity paths contract tr_B(U*C*U') = sum_q U_q*C*U_q' directly, so a mode=:reduction workspace retains only a discarded-factor slice rather than the complete product block. Negativity still retains that complete block because it must partially transpose it. The workspace converts compact real qubit recouplers once to its complex working type so repeated multiplication stays allocation-light on every supported Julia release. Already type-matched packed qudit intertwiners are shared read-only; wider workspaces copy only their stored nonzeros. Use reduced_state! when the output PIState should also be reused.\n\nWhen each PI particle is a composite supersite, LocalFactorTracePlan instead traces the same internal factor from every particle and returns a complete PI state at the kept local dimension. Its prepared rectangular occupation transforms retain only exact nonzero sparse support and avoid both full-Hilbert reconstruction and an exponential list of lifted local observables. Reuse one LocalFactorTraceWorkspace per task, then apply ReductionPlan to the resulting state if a particle bipartition is also required.\n\nFor repeated one-body marginals, prepare both geometry and scratch:\n\none_body_geometry = OneBodyGeometry(basis)\none_body_work = OneBodyRDMWorkspace(one_body_geometry, rho)\nrho_one = zeros(ComplexF64, basis.d, basis.d)\none_body_rdm!(rho_one, rho, one_body_work)\n\nThis contracts all matrix units in one pass, reuses the largest multiplicity-weighted Schur block, and avoids preparing d^2 independent collective operators. A validated enclosing scan may use check=false on the in-place call. For BigFloat, build the geometry at the state's stored precision; the workspace rejects a mixed or mismatched precision instead of silently narrowing the contraction.","title":"Prepared observables and reductions"},{"category":"section","location":"architecture/#Schur-block-diagnostics","page":"Architecture and efficient workflows","text":"schur_block_structure(rho) measures the retained state/operator block in each partition. For a PI superoperator it returns a matrix whose row is the output sector and whose column is the input sector. Sparse matrices are scanned directly. Matrix-free generators are probed one PI coordinate at a time with reusable scratch, so the calculation avoids materializing the global PI Liouvillian but still costs one application per input coordinate.\n\nRendering is separate: visualize_schur_blocks(structure) creates a compact object with text and SVG displays, and save_schur_block_visualization writes that SVG without adding a plotting dependency. Reuse the numerical structure when experimenting with titles, normalization, or colour scales. See Schur-block visualization for representation and norm conventions.","title":"Schur-block diagnostics"},{"category":"section","location":"architecture/#Spectral-diagnostics","page":"Architecture and efficient workflows","text":"Density and complex spectral data use the same analysis/presentation split. pi_density_spectrum diagonalizes physical Schur blocks and retains one value per irrep eigenvector with its exact symmetric-group degeneracy; visualize_density_spectrum renders that compressed result without repeating the diagonalization or constructing a d^N list. liouvillian_spectrum_data normalizes complete or selected results into a ComplexSpectrum without changing their order or repeated roots. floquet_spectrum_data accepts an already converged one-period map or a precomputed multiplier/exponent vector. visualize_spectrum then renders the stored values repeatedly without invoking a solver.\n\nDensity plots show raw eigenvalue versus compressed mode rank and colour points by Schur sector. Liouvillian plots show real(lambda)=0; multiplier plots show the unit circle; principal-branch exponent plots show real(xi)=0 and imag(xi)=±π/T. Classifications are tolerance-based presentation metadata and never clip or move a point. Complete dense spectra and selected ordinary/harmonic/IRAM/JD windows remain explicitly distinguished. See Spectral visualization for conventions and efficient reuse.","title":"Spectral diagnostics"},{"category":"section","location":"architecture/#Backend-selection-and-memory","page":"Architecture and efficient workflows","text":"compile(...; backend=:auto, memory_budget=...) keeps driven models matrix-free. For standard fixed kernels, it bounds sparse storage from the prepared exact block support; dynamic, custom, or unknown kernels retain a conservative dense-coordinate fallback. Explicit sparse compilation and liouvillian(...; representation=:sparse) use the same 512 MiB default guard and reject a conservative live-assembly peak before allocating the matrix. Scalar-rate families additionally support specialize(...; backend=:auto, memory_budget=...); their precomputed metadata lets repeated specializations make this decision without rebuilding or re-estimating Schur geometry. Pass memory_budget=Inf only to opt out explicitly. Inspect prepared.estimates; the choice and budget status are never hidden.\n\nAdditional estimates are available through:\n\npi_dimension\nestimate_state_bytes\nestimate_basis_bytes\nestimate_liouvillian_bytes\nestimate_geometry_bytes\nestimate_solver_bytes\nrecommend_solver\n\nbasis_summary and estimate_state_bytes use the retained coordinate dimension, including for a sector-restricted basis; the legacy (N,d) estimate_memory function intentionally describes a complete basis. estimate_geometry_bytes separately reports conservative retained and peak setup bounds for the sparse one-body transition geometry. Qudit estimates count every content-compatible parent in multiplicity-bearing GT weight spaces before bounding contraction tuples; they do not use a multiplicity-free approximation. Krylov estimates are accumulated as exact BigInt byte counts so the estimator itself cannot overflow on a large problem. Fixed-size isbits scalar arrays retain exact inline sizeof(T) accounting. Heap-backed BigFloat and Complex{BigFloat} arrays instead use a conservative retained-storage bound at the explicit bigfloat_precision keyword (the active precision by default); this is an upper estimate, not an ABI promise. GMRES also accounts for its real residual/history array using the real component type of T. Other heap-backed scalar types use a padded zero-value sample and report scalar_storage_estimate=:sample_based_retained_estimate; their arbitrary value payload is not claimed to have a type-level worst-case bound.\n\nFor a PIModel or CompiledPIModel, recommend_solver includes one-body geometry only when its terms require that lowering. Bare bases, states, operators, and lowered plans lack term provenance, so they retain the conservative allowance and report geometry_requirement=:conservative_unknown. The report separates resources.setup, resources.retained, resources.solve, and resources.output, then takes the maximum live setup/solve phase instead of adding temporaries that do not coexist. Each component states whether its bytes are measured (:actual), structurally bounded (:upper_bound), modeled (:estimate), or unavailable (:unknown). Inspect budget_status and safe_to_run: the latter remains missing when an estimate is informative but cannot certify a fit.\n\nOperation-specific accounting matters. A direct stationary solve includes a bordered sparse factorization bound, a dense spectrum includes matrix copies and eigensolver work arrays, iterative routes include their Krylov basis, and dynamics includes saved states and streamed scalar series. For example:\n\nadvice = recommend_solver(\n    model;\n    task=:dynamics,\n    samples=1001,\n    saved_states=0,\n    observable_series=2,\n    memory_budget=2^30,\n)\n\nadvice.resources.output\nadvice.budget_status\n\nThe coordinate type is inferred from the supplied model or prepared source. Dense compatibility guards conservatively reserve storage at no less than ComplexF64 precision. For a standalone dynamics estimate with mixed precisions, pass observable_type and time_type; the high-level solve_dynamics command infers both from the actual observables and saved-time vector.\n\nHigh-level stationary_state, liouvillian_spectrum, and solve_dynamics use the same 512 MiB default preflight. Automatic stationary/spectral routes switch to matrix-free Krylov when the direct/dense candidate exceeds the budget, and then check the selected Krylov route itself. An explicit factorizing route that exceeds the budget raises before materialization. Pass memory_budget=Inf only as an intentional opt-out after checking available RAM.\n\nThe same contract covers the other user-facing routes that can otherwise hide quadratic or history-sized output:\n\ncomplete Liouvillian and Floquet eigendecompositions, dense Floquet propagators, and dense Floquet fixed-point solves check operation-specific multi-array peak estimates;\nselected Liouvillian/Floquet and response calculations check their actual Arnoldi, GMRES, recycled, or exponential-action workspace capacity, including a supplied reusable workspace;\nresponse method=:auto uses a dense matrix only when its factorization fits, then verifies the selected Krylov alternative as well;\nstroboscopic_evolution and density-valued, weak-PI, diffusive, and composite trajectory histories check their requested saved-coordinate lower bound. Predictable retained observable-statistics arrays and diffusive measurement/innovation records are included. Use final-state or observable-only streaming APIs when saved states are the bottleneck;\npseudospectral_abscissa additionally limits the Cartesian grid point count through max_grid_points, because bounded workspace alone does not bound the number of repeated linear solves.\n\nThese checks never reduce nev, discard output times, lower precision, or declare a nonconverged iterative result acceptable. An insufficient budget raises with a matrix-free/streaming alternative instead of silently changing the requested numerical problem.\n\nThe estimates distinguish retained storage from temporary setup allocations. Always benchmark representative values of N, d, body order, and sector content before a large scan.","title":"Backend selection and memory"},{"category":"section","location":"architecture/#Validation-contract","page":"Architecture and efficient workflows","text":"validate_state rejects trace, Hermiticity, or positivity violations without normalizing, clipping, or symmetrizing the input. state_diagnostics returns the corresponding errors and tolerances without throwing. Analysis routines only remove roundoff-level skew-Hermitian components after validation. A validated spectral routine may also project accepted roundoff eigenvalues to numerical rank zero where a square root, logarithm, or support projector requires it. This is temporary internal arithmetic: it does not repair the input, change returned eigenvalues, or authorize clipping a genuinely invalid state. Endpoint correction in fidelity is similarly bounded and throws when the discrepancy is larger than documented roundoff.","title":"Validation contract"},{"category":"section","location":"convergence/#Numerical-convergence-studies","page":"Numerical convergence reports","text":"A solver's internal converged=true means that one discretized problem met its own stopping rule. It does not establish convergence in a time step, Krylov dimension, hierarchy depth, sector cutoff, closure order, or system size. The refinement-study API records this separate layer of numerical evidence without discarding the underlying solver reports.","title":"Numerical convergence studies"},{"category":"section","location":"convergence/#Generic-refinement-sequences","page":"Numerical convergence reports","text":"convergence_study evaluates all requested levels and compares neighbouring estimates. The default requires agreement in the final two comparisons, so at least three refinement levels are needed for a positive conclusion:\n\nreport = convergence_study([8, 16, 32, 64];\n    parameter=:resolution,\n    refinement_scale=n -> 1 / n,\n    rtol=1e-6,\n) do resolution\n    result = solve_at_resolution(resolution)\n    (\n        value = observable(result),\n        converged = result.converged,\n        iterations = result.iterations,\n    )\nend\n\nif report.converged\n    estimate = convergence_estimate(report)\nelse\n    @warn \"resolution is not converged\" reason=report.reason errors=report.pairwise_errors\nend\n\nThe do-block is the evaluator(level). It may return a number, array, PIState, or a result carrying an estimate, value, solution, or state property. For another schema, supply estimate=result -> ....\n\nThe report retains:\n\nevery refinement and unmodified evaluator result;\nthe extracted estimates and diagnostics;\ninner-solver convergence flags when provided;\npairwise errors and scale-aware tolerances;\nempirical rates when three successive refinement_scale values are geometrically spaced (otherwise the rate is missing);\nthe first passing window and the status of the final window.\n\nBecause every raw result and estimate is retained, a generic study can be as large as all of its saved histories combined. For a memory-bounded production study, make the evaluator return compact diagnostics and use estimate to extract only the common observable, reduced state, or final vector needed for the comparison. The specialized HEOM-depth helper already retains complete hierarchy data only at the finest depth.\n\nAn earlier passing window does not override a later disagreement. The result then has converged=false and reason=:refinement_not_stable. Likewise, an inner converged=false blocks the outer claim even when estimates happen to agree.\n\nThis is a consistency report, not an automatic extrapolator or proof of an asymptotic regime. Empirical orders are emitted only for compatible geometric refinement scales and should be interpreted beside the raw errors.\n\nFor custom objects, provide their metric explicitly:\n\nreport = convergence_study(level -> solve(level), levels;\n    estimate = result -> result.observables,\n    distance = (a, b) -> maximum(abs, a .- b),\n    estimate_norm = a -> maximum(abs, a),\n)\n\nAll errors and norms must be finite nonnegative real values. Invalid numerical data raise instead of being treated as unconverged finite data.","title":"Generic refinement sequences"},{"category":"section","location":"convergence/#Time-step-convergence","page":"Numerical convergence reports","text":"timestep_convergence requires positive, strictly decreasing steps and uses the step itself when estimating empirical order:\n\nsteps = [0.02, 0.01, 0.005, 0.0025]\nreport = timestep_convergence(steps; rtol=1e-5) do dt\n    final_observable(dt)\nend\n\nFor stochastic paths, use common random numbers when studying time-step bias; otherwise sampling noise may dominate the pairwise difference. Sampling confidence intervals and discretization convergence are separate claims. The adaptive-ensemble converged flag controls Monte Carlo half-width only and must not be reused as evidence that the trajectory integrator's step is converged.","title":"Time-step convergence"},{"category":"section","location":"convergence/#Krylov-dimension-convergence","page":"Numerical convergence reports","text":"Partial Ritz data and finite Krylov linear solves should be checked as their subspace grows:\n\ndimensions = [20, 30, 45, 60]\nreport = krylov_dimension_convergence(dimensions;\n    estimate = result -> result.values,\n    rtol=1e-7,\n) do dimension\n    krylov_liouvillian_spectrum(L;\n        nev=4,\n        krylovdim=dimension,\n        require_convergence=false,\n    )\nend\n\nThe raw Arnoldi results and their residual flags remain in report.results. The nominal rate axis is 1/dimension; it is descriptive and is not an assumption that Krylov error follows a power law. Unless three consecutive dimensions form a geometric sequence, observed_rates therefore remains missing while the pairwise convergence decision is still available.","title":"Krylov-dimension convergence"},{"category":"section","location":"convergence/#HEOM-depth-and-sector-cutoffs","page":"Numerical convergence reports","text":"Hierarchy convergence rebuilds the plan at every depth:\n\ndepth_report = hierarchy_depth_convergence([1, 2, 3, 4];\n    estimate = result -> result.observable,\n    rtol=1e-5,\n) do depth\n    solve_heom_at_depth(depth)\nend\n\nsector_cutoff_convergence assumes that a larger positive integer retains a larger approximation space:\n\nsector_report = sector_cutoff_convergence([2, 4, 6, 8]) do cutoff\n    observable(solve_with_first_sectors(cutoff))\nend\n\nStates represented in different truncated bases do not share a coordinate space. Compare a common observable, reduced state, or an explicitly defined embedding rather than subtracting their coefficient vectors directly.\n\nIf a model defines its cutoff in the opposite direction, use the generic API and pass a strictly decreasing refinement_scale matching that convention.","title":"HEOM depth and sector cutoffs"},{"category":"section","location":"convergence/#Consuming-a-result-safely","page":"Numerical convergence reports","text":"convergence_estimate(report) raises unless the final window passed. This makes it harder for downstream code to accidentally strip the convergence status. An exploratory script may request the finest value with require_convergence=false, but should retain and display the complete report beside it.\n\nSet require_convergence=true on convergence_study or a convenience wrapper when an unconverged study should abort a production workflow.","title":"Consuming a result safely"},{"category":"section","location":"convergence/#API","page":"Numerical convergence reports","text":"","title":"API"},{"category":"type","location":"convergence/#PermutationalInvariantDynamics.ConvergenceStudyResult","page":"Numerical convergence reports","text":"ConvergenceStudyResult\n\nComplete record of a numerical refinement study. refinements, results, estimates, and diagnostics retain every evaluated level. The first entry of pairwise_errors, tolerances, and pairwise_converged is missing because no coarser comparison exists. Rates are also missing until three estimates and a refinement scale are available. solver_converged is missing when an evaluator did not report an inner solver status.\n\nconverged refers to the final requested refinement window, never merely to an earlier pair or to an inner solver flag. Use convergence_estimate to obtain the final estimate with an explicit convergence guard.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.ConvergenceStudyResult"},{"category":"function","location":"convergence/#PermutationalInvariantDynamics.convergence_study","page":"Numerical convergence reports","text":"convergence_study(evaluator, refinements; parameter=:refinement,\n                  estimate=_convergence_default_estimate,\n                  diagnostics=_convergence_default_diagnostics,\n                  distance=_convergence_default_distance,\n                  estimate_norm=_convergence_default_norm,\n                  refinement_scale=nothing, atol=0, rtol=nothing,\n                  consecutive=2, require_convergence=false)\n\nEvaluate a deterministic refinement sequence and compare every estimate with its immediate predecessor. A pair passes when\n\nd(x_ix_i-1) leq a + rmax(lVert x_irVertlVert x_i-1rVert)\n\nThe final consecutive pairs must all pass, and no inner solver in that window may report converged=false, before the study reports convergence. The default requires two consecutive pairwise agreements, hence at least three levels. Set consecutive=1 only when one comparison is scientifically adequate for the problem.\n\nevaluator(level) may return the estimate directly or a result carrying an estimate, value, solution, state, values, or solutions property. All raw results are retained. Supply extraction functions for other result schemas. If refinement_scale(level) is given, it must be positive and strictly decrease; the report includes empirical rates from three successive estimates when the three scales are geometrically spaced. Rates are missing on a non-geometric grid rather than applying a formula that is not valid there.\n\nThe precision-aware default is rtol=sqrt(eps(R)), where R is the real error type. Nonfinite estimates, errors, norms, scales, or tolerances raise. Set require_convergence=true to raise when the final window fails.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.convergence_study"},{"category":"function","location":"convergence/#PermutationalInvariantDynamics.convergence_estimate","page":"Numerical convergence reports","text":"convergence_estimate(report; require_convergence=true)\n\nReturn the finest estimate stored in a convergence report. By default an unconverged report raises; set require_convergence=false only when the caller will propagate the report's status and error data explicitly.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.convergence_estimate"},{"category":"function","location":"convergence/#PermutationalInvariantDynamics.timestep_convergence","page":"Numerical convergence reports","text":"timestep_convergence(evaluator, timesteps; kwargs...)\n\nRun convergence_study for a strictly decreasing sequence of positive time steps. Empirical rates use the time step itself as the decreasing refinement scale.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.timestep_convergence"},{"category":"function","location":"convergence/#PermutationalInvariantDynamics.krylov_dimension_convergence","page":"Numerical convergence reports","text":"krylov_dimension_convergence(evaluator, dimensions; kwargs...)\n\nRun a convergence study for strictly increasing positive Krylov dimensions. The nominal rate scale is 1/dimension; residual diagnostics returned by each inner solve are retained and an explicit converged=false blocks the report.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.krylov_dimension_convergence"},{"category":"function","location":"convergence/#PermutationalInvariantDynamics.hierarchy_depth_convergence","page":"Numerical convergence reports","text":"hierarchy_depth_convergence(evaluator, depths; kwargs...)\n\nRun a convergence study for strictly increasing nonnegative hierarchy depths. The nominal refinement scale is 1/(depth+1). This wrapper is suitable for HEOMPlan depth studies but remains independent of a particular HEOM solver or bath decomposition.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.hierarchy_depth_convergence"},{"category":"function","location":"convergence/#PermutationalInvariantDynamics.sector_cutoff_convergence","page":"Numerical convergence reports","text":"sector_cutoff_convergence(evaluator, cutoffs; kwargs...)\n\nRun a convergence study for a strictly increasing positive retained-sector cutoff. The nominal refinement scale is 1/cutoff. If a model uses the opposite cutoff convention, call convergence_study directly with an appropriate refinement_scale.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.sector_cutoff_convergence"},{"category":"section","location":"pseudomodes/#Local-pseudomodes-and-PI-supersites","page":"Local pseudomodes and PI supersites","text":"A finite-cutoff pseudomode turns a selected non-Markovian environment into a larger time-local Lindblad problem. When every physical system has the same local pseudomodes, the object permuted by the symmetric group is the complete local tuple\n\ntextsystem_iotimestextmode_i1otimescdots\notimestextmode_iM\n\nThe package treats this tuple as one identical supersite. If the physical system has dimension d_S and mode mu retains occupations 0n_maxmu, one supersite has dimension\n\nD=d_Sprod_mu=1^M(n_maxmu+1)\n\nThe exact complete PI operator space then has\n\nbinomN+D^2-1N\n\ncoordinates. This is polynomial in N at fixed D and never constructs a D^N state, but it can still grow rapidly with the number and cutoff of the local modes. Always preflight and converge the cutoffs.\n\nThis construction describes identical independent local pseudomodes. It is not the same as a shared cavity or a common HEOMBath: either common environment can contain cross-correlations between different particles. See Global pseudomodes and shared cavities and PI–HEOM non-Markovian dynamics for those distinct workflows.\n\nThe complete generator must still be invariant under every permutation of the supersites. Identical onsite terms and local damping satisfy this automatically; a many-system interaction must be a uniform symmetric p-body sum, such as an all-to-all Ising term. Translation invariance or periodic boundary conditions alone do not make a nearest-neighbour chain PI.","title":"Local pseudomodes and PI supersites"},{"category":"section","location":"pseudomodes/#Internal-ordering","page":"Local pseudomodes and PI supersites","text":"PISupersite follows Julia's Kronecker-product ordering. Factors are declared as\n\n(system, mode1, mode2, ...)\n\nand a local tensor operator is\n\nkron(system_operator, mode1_operator, mode2_operator, ...)\n\nso the last internal factor is fastest. This local convention is different from CompositePIBasis, which combines complete global operator spaces. CompositePIBasis cannot preserve the pairing between system i and its own mode i; local pseudomodes therefore belong inside one PISupersite.\n\nThe low-level supersite layer is also useful for finite ancillas that are not bosonic:\n\nusing LinearAlgebra\nusing PermutationalInvariantDynamics\n\nsite = PISupersite(\n    3, (3, 2);\n    labels=(:system, :ancilla),\n)\n\nA = Matrix{ComplexF64}(I, 3, 3)\nB = ComplexF64[0 1; 1 0]\n\nA_on_site = lift_system_operator(site, A)\nB_on_site = lift_supersite_operator(site, B; factor=:ancilla)\nAB = supersite_tensor_operator(site, A, B)\n\n@assert AB == A_on_site * B_on_site\n\nThese helpers allocate only a site.basis.d-dimensional local matrix. They do not lift it to the labeled-particle Hilbert space.\n\nWhen a basis has already been prepared, use PISupersite(basis, factor_dimensions; ...) or pseudomode_supersite(basis, system_dimension, modes...). The wrapper keeps that exact basis object, including an explicitly retained sector set, and validates that its local dimension matches the factorization.","title":"Internal ordering"},{"category":"section","location":"pseudomodes/#One-pseudomode-per-system","page":"Local pseudomodes and PI supersites","text":"The following model contains N qubits, one damped local mode per qubit, and a uniform all-to-all Ising interaction. The local basis is qubit ⊗ mode.\n\nusing LinearAlgebra\nusing PermutationalInvariantDynamics\n\nN = 3\nspin = spin_matrices(2)\nsigma_x = 2spin.jx\nsigma_z = 2spin.jz\nsigma_minus = spin.jm\n\nOmega = 0.35\nJ = 0.8\nomega_c = 1.0\nkappa = 0.4\ng = 0.18\n\nmode = BosonicPseudomode(\n    2;\n    label=:cavity,\n    frequency=omega_c,\n    damping=kappa,\n    thermal_occupation=0,\n)\n\ncoupling = PseudomodeCoupling(\n    sigma_minus;\n    mode=:cavity,\n    strength=g,\n)\n\n# This rate is the literal coefficient of every unordered pair. The package\n# never inserts a Kac factor, so it is written explicitly here.\npair_term = PBodyHamiltonian(\n    kron(sigma_x, sigma_x), 2;\n    rate=-J / (N - 1),\n)\n\n# Separate geometry from physical rates when the same cutoff is reused.\nsite = pseudomode_supersite(N, 2, mode)\n\nembedding = pseudomode_model(\n    site, (Omega / 2) * sigma_z;\n    couplings=(coupling,),\n    system_terms=(pair_term,),\n)\n\nbasis = embedding.basis\nmodel = embedding.model\n\nThe builder retains separate fixed operators and scalar rates for the system Hamiltonian, mode frequency, couplings, damping channels, and lifted system terms. Compilation can therefore share their Schur geometry and compile_family can specialize compatible scalar-rate scans. embedding.site_hamiltonian (equivalently embedding.base_site_hamiltonian) contains the one-supersite system, frequency, and coupling terms. Collective and p-body entries cannot be represented by that local matrix; inspect embedding.lifted_system_terms and embedding.supersite_terms for those contributions. The aggregate construction estimate is available as embedding.resource_estimates. Calling pseudomode_model(site, ...) again preserves exact basis identity; the frequencies, dampings, and thermal_occupations keywords can override the immutable mode defaults for a fixed-cutoff scan.\n\nFor the fastest scan, construct one prototype and pass the indices of the rates that actually vary to compile_family; specialize then changes only those scalars. If a varied component is zero at the prototype point, construct the prototype with retain_zero_terms=true so its fixed operator geometry is kept explicitly. The default omits fixed zero components to keep an ordinary single-point model small.\n\nFor example, this changes physical rates without reconstructing the basis:\n\nscan_point = pseudomode_model(\n    site, (Omega / 2) * sigma_z;\n    couplings=(\n        PseudomodeCoupling(\n            sigma_minus;\n            mode=:cavity,\n            strength=0.21,\n        ),\n    ),\n    system_terms=(pair_term,),\n    frequencies=1.05,\n    dampings=0.45,\n    thermal_occupations=0.02,\n)\n\n@assert scan_point.basis === basis\n\nThe interaction specified by strength=g is\n\ngL a^dagger + g^* L^dagger a\n\nL need not be Hermitian and g may be complex. The optional counterrotating_strength=h adds\n\nhL a + h^* L^dagger a^dagger\n\nInternally, a complex strength is split into real rates multiplying Hermitian quadratures; Hamiltonian rates are never made complex. For Hermitian L, the rotating expression with a real strength is already proportional to L(a+a^dagger). Adding an equal real counter-rotating strength would add the same operator again.\n\nThe damping convention is the package convention\n\nmathcal Da(rho)\n=arho a^dagger-frac12a^dagger arho\n\nThus damping=kappa produces kappa(bar n+1)mathcal Da and kappabar nmathcal Da^dagger. The mode amplitude decays at kappa2. Convert explicitly when a paper defines its dissipator with an extra factor of two.","title":"One pseudomode per system"},{"category":"section","location":"pseudomodes/#Product-initial-states","page":"Local pseudomodes and PI supersites","text":"pseudomode_product_state tensors only the local factor states and then calls the PI tensor-power constructor. Modes start in their vacuum kets unless mode_states is supplied:\n\nexcited = ComplexF64[0, 1]\nrho0 = pseudomode_product_state(site, excited)\nvalidate_state(rho0)\n\nA mixed system state or a finite-temperature initial mode is also accepted. For a single truncated mode, a normalized truncated geometric state can be constructed explicitly:\n\nnbar_initial = 0.2\nq = nbar_initial / (nbar_initial + 1)\nweights = q .^ (0:mode.nmax)\nweights ./= sum(weights)\nrho_mode = Diagonal(ComplexF64.(weights))\n\nrho_system = ComplexF64[0.7 0; 0 0.3]\nrho0_thermal = pseudomode_product_state(\n    site, rho_system;\n    mode_states=rho_mode,\n)\n\nNo normalization is performed silently: each supplied local state is validated by the ordinary PI product-state constructors.\n\nIf the state of one supersite already contains system–mode correlations, construct that local vector or density matrix explicitly and use supersite_iid_state:\n\nground = ComplexF64[1, 0]\none_photon = zeros(ComplexF64, mode.levels)\none_photon[2] = 1\n\nlocal_correlated = (\n    kron(ground, mode.vacuum) +\n    kron(excited, one_photon)\n) / sqrt(2)\n\nrho0_locally_correlated = supersite_iid_state(\n    site, local_correlated,\n)\n\nThis makes N identical copies of the correlated local supersite state; it does not claim that the different supersites are initially entangled.","title":"Product initial states"},{"category":"section","location":"pseudomodes/#Matrix-free-dynamics-and-stationary-states","page":"Local pseudomodes and PI supersites","text":"The supersite model is an ordinary PIModel, so the standard prepared workflow applies unchanged:\n\nprepared = compile(model; backend=:matrixfree)\n\ntimes = range(0.0, 12.0; length=121)\nsolution = solve_dynamics(\n    prepared, rho0, (first(times), last(times));\n    saveat=times,\n    steps_per_interval=8,\n)\n\nFor an autonomous stationary problem, explicitly selecting GMRES keeps the solve matrix-free:\n\nsteady = stationary_state(\n    prepared;\n    algorithm=GMRESAlgorithm(krylovdim=40, maxiter=1000),\n    atol=1e-11,\n    rtol=1e-9,\n    return_info=true,\n)\n\nrho_ss = steady.state\n\nInspect steady.info.residual and the state diagnostics rather than treating solver return alone as a uniqueness or cutoff certificate. Use recommend_solver before a large solve or materialization and reuse explicit Krylov/Liouvillian workspaces in ordered scans.\n\nIndependent mode damping is represented by LocalJump, not CollectiveJump. It generally populates multiple Schur sectors even when the initial supersite state lies in the fully symmetric sector. Consequently a complete PIBasis(N,D) is normally required. pseudomode_model(...; sectors=...) accepts a restriction only when PIModel can certify that every requested local process stays inside it; it never silently projects the dynamics.","title":"Matrix-free dynamics and stationary states"},{"category":"section","location":"pseudomodes/#System-observables-and-all-to-all-interactions","page":"Local pseudomodes and PI supersites","text":"A system-only one-body observable must first be lifted to the local supersite:\n\nsigma_x_site = lift_system_operator(site, sigma_x)\nmean_x = real(collective_expectation(rho_ss, sigma_x_site)) / N\n\nThe model builder applies the same lift automatically to each fixed built-in entry of system_terms. For a general system-only p-body term, it uses lift_system_pbody_operator to interleave the auxiliary identities particle by particle:\n\n(system1 ⊗ modes1) ⊗ ... ⊗ (systemp ⊗ modesp).\n\nThis is not the grouped ordering obtained from kron(system_pbody_operator, I_modes). The lifted matrix is sparse, uses the exact support of the input, and is guarded by memory_budget. The lower-level PISupersite constructor permits arbitrary factor labels, but lift_system_pbody_operator requires the :system factor to be first. pseudomode_supersite always establishes that ordering.\n\nThe lower-level equivalent of the system_terms call above is:\n\nlifted_pair_term = lift_system_term(site, pair_term)\n@assert lifted_pair_term isa PBodyHamiltonian\n\nlift_system_term supports fixed microscopic local, collective, and symmetric p-body built-in terms. A direct Schur-space term is already tied to another basis and cannot be lifted this way.","title":"System observables and all-to-all interactions"},{"category":"section","location":"pseudomodes/#Multiple-local-pseudomodes","page":"Local pseudomodes and PI supersites","text":"Give every mode a unique label and select it from each coupling:\n\nmode_fast = BosonicPseudomode(\n    1;\n    label=:fast,\n    frequency=1.4,\n    damping=0.7,\n)\n\nmode_slow = BosonicPseudomode(\n    2;\n    label=:slow,\n    frequency=0.45,\n    damping=0.12,\n    thermal_occupation=0.05,\n)\n\ncouplings = (\n    PseudomodeCoupling(\n        sigma_minus;\n        mode=:fast,\n        strength=0.16,\n    ),\n    PseudomodeCoupling(\n        sigma_minus;\n        mode=:slow,\n        strength=0.10,\n        counterrotating_strength=0.025im,\n    ),\n)\n\nN_multi = 2\npair_term_multi = PBodyHamiltonian(\n    kron(sigma_x, sigma_x), 2;\n    rate=-J / (N_multi - 1),\n)\n\ntwo_mode = pseudomode_model(\n    N_multi, (Omega / 2) * sigma_z, (mode_fast, mode_slow);\n    couplings=couplings,\n    system_terms=(pair_term_multi,),\n)\n\n@assert two_mode.supersite.factor_dimensions == (2, 2, 3)\n@assert two_mode.basis.d == 12\n\nrho0_two_mode = pseudomode_product_state(\n    two_mode.supersite, excited,\n) # both modes use their vacuum kets\n\ntwo_mode.mode_operators[1] and [2] contain the lifted annihilation, creation, number, parity, and top-level-projector matrices. The equivalent label-based lookup is\n\nslow_operators = pseudomode_operators(two_mode.supersite, :slow)\n\nEach additional mode multiplies the local dimension before the PI coordinate count is evaluated. Increasing several cutoffs simultaneously can therefore be much more expensive than increasing one cutoff.","title":"Multiple local pseudomodes"},{"category":"section","location":"pseudomodes/#Arbitrary-qudits-and-system-channels","page":"Local pseudomodes and PI supersites","text":"Nothing in the supersite construction is qubit-specific. Here is a qutrit with local and collective system relaxation:\n\nd = 3\nDelta = 0.6\nHq = Diagonal(ComplexF64[0, Delta, 2 * Delta])\nlower = ComplexF64[\n    0 1 0\n    0 0 sqrt(2)\n    0 0 0\n]\n\nqutrit_mode = BosonicPseudomode(\n    1;\n    label=:qutrit_bath,\n    frequency=1.1,\n    damping=0.3,\n)\n\nqutrit_embedding = pseudomode_model(\n    N, Hq, qutrit_mode;\n    couplings=(\n        PseudomodeCoupling(\n            lower;\n            mode=:qutrit_bath,\n            strength=0.08 + 0.02im,\n        ),\n    ),\n    system_terms=(\n        LocalJump(lower; rate=0.01),\n        CollectiveJump(lower; rate=0.02 / N),\n    ),\n)\n\nqutrit_excited = ComplexF64[0, 0, 1]\nrho0_qutrit = pseudomode_product_state(\n    qutrit_embedding.supersite, qutrit_excited,\n)\n\nThe distinction between the two relaxation terms is preserved after lifting: LocalJump remains a sum of independent channels, whereas CollectiveJump remains one coherent summed jump.","title":"Arbitrary qudits and system channels"},{"category":"section","location":"pseudomodes/#Tracing-all-modes-back-to-the-physical-systems","page":"Local pseudomodes and PI supersites","text":"The system-only state is obtained directly in PI coordinates. Prepare the rectangular trace map once, and give every concurrent task its own workspace:\n\ntrace_plan = pseudomode_trace_plan(site)\ntrace_work = LocalFactorTraceWorkspace(trace_plan)\n\nrho_system_ss = trace_pseudomodes(\n    rho_ss, site;\n    plan=trace_plan,\n    workspace=trace_work,\n)\n\n@assert rho_system_ss.basis === trace_plan.output_basis\n@assert rho_system_ss.basis.d == 2\n@assert isapprox(trace(rho_system_ss), 1; atol=1e-10)\n\nmean_z = real(\n    collective_expectation(rho_system_ss, sigma_z),\n) / N\n\nFor a repeated scan, preallocate the output as well:\n\nrho_system_buffer = PIState(trace_plan.output_basis)\ntrace_pseudomodes!(\n    rho_system_buffer, rho_ss, site, trace_plan, trace_work;\n    check=false, # only when the surrounding workflow already validates rho_ss\n)\n\nAll trailing pseudomodes are grouped into one local auxiliary factor before applying LocalFactorTracePlan. The trace keeps all N physical systems; it is different from reduced_state, which discards particles at fixed local dimension. The prepared plan is tied to the exact supersite basis, so a different cutoff needs a different plan.","title":"Tracing all modes back to the physical systems"},{"category":"section","location":"pseudomodes/#Cutoff-convergence","page":"Local pseudomodes and PI supersites","text":"The occupation of the highest retained mode level is a useful boundary diagnostic:\n\nmode_ops = pseudomode_operators(site, :cavity)\ntop_population = real(\n    collective_expectation(rho_ss, mode_ops.top_projector),\n) / N\n\nA small top_population is necessary but not sufficient. The physical quantity of interest must also stabilize when nmax is increased. Since supersite states at different cutoffs belong to different bases, do not compare their raw PI coordinate vectors. Trace the modes or compare a common observable instead:\n\nfunction cutoff_point(nmax)\n    local_mode = BosonicPseudomode(\n        nmax;\n        label=:cavity,\n        frequency=omega_c,\n        damping=kappa,\n    )\n    local_coupling = PseudomodeCoupling(\n        sigma_minus;\n        mode=:cavity,\n        strength=g,\n    )\n    local_embedding = pseudomode_model(\n        N, (Omega / 2) * sigma_z, local_mode;\n        couplings=(local_coupling,),\n        system_terms=(pair_term,),\n    )\n    local_prepared = compile(\n        local_embedding.model;\n        backend=:matrixfree,\n    )\n    local_steady = stationary_state(\n        local_prepared;\n        algorithm=GMRESAlgorithm(krylovdim=40, maxiter=1000),\n    )\n\n    operators = only(local_embedding.mode_operators)\n    boundary = real(\n        collective_expectation(\n            local_steady, operators.top_projector,\n        ),\n    ) / N\n\n    physical = trace_pseudomodes(\n        local_steady, local_embedding.supersite,\n    )\n    magnetization = real(\n        collective_expectation(physical, sigma_z),\n    ) / N\n\n    (;nmax, boundary, magnetization)\nend\n\ncutoff_data = cutoff_point.((1, 2, 3))\n\nRepeat this refinement for dynamics at common physical times when the desired result is time dependent. Solver tolerance, time-step convergence, finite N, and pseudomode cutoff are separate numerical claims.","title":"Cutoff convergence"},{"category":"section","location":"pseudomodes/#Performance-checklist","page":"Local pseudomodes and PI supersites","text":"Compute D and commutant_dimension(N,D) before building a large model.\nKeep independent local damping on a complete Schur basis unless a requested restricted basis is explicitly validated.\nUse compile(...; backend=:matrixfree) when sparse materialization would dominate memory.\nReuse the compiled model, Krylov workspaces, observable plans, and one pseudomode_trace_plan plus task-owned trace workspace per exact cutoff/basis; use trace_pseudomodes! when the output can also be reused.\nKeep the coupling strengths and frequencies as scalar term rates so compile_family can reuse fixed geometry in parameter scans.\nMonitor the top-level population and converge every reported system observable in the cutoff.\nUse a wider floating type rather than allowing a required scale to underflow or overflow.\n\nThe historical convenience call\n\nindependent_local_pseudomode_model(\n    N, Hsystem, L;\n    nmax, frequency, coupling_strength, damping,\n)\n\nremains available for one mode. The BosonicPseudomode / PseudomodeCoupling workflow is the general route for multiple modes, counter-rotating terms, system-term lifting, state preparation, and prepared mode tracing.","title":"Performance checklist"},{"category":"section","location":"hops/#Permutationally-invariant-HOPS","page":"PI–HOPS stochastic non-Markovian dynamics","text":"The hierarchy of pure states (HOPS) backend provides a stochastic, wave-function counterpart to PI–HEOM. It is useful when the system couples to one or more shared structured bosonic environments and propagating pure Schur-sector amplitudes is cheaper than propagating a hierarchy of PI density operators.\n\nHOPS introduces two independent numerical limits:\n\nthe finite hierarchy depth;\nthe number of statistically independent noise realizations.\n\nNeither limit replaces the time-step convergence check. A result should be reported as converged only after all three have been varied independently.","title":"Permutationally invariant HOPS"},{"category":"section","location":"hops/#Physical-convention","page":"PI–HOPS stochastic non-Markovian dynamics","text":"For bath b, HOPSBath represents\n\nC_b(t)=sum_kin bc_k e^-nu_k tqquad tgeq0\n\nwhere mathrmRenu_k0. Its PI coupling operator is L_b. Different baths have independent proper complex Gaussian processes,\n\nmathbb Ez_b(t)=mathbb Ez_b(t)z_b(s)=0qquad\nmathbb Ez_b(t)z_b^*(s)\n =delta_bbC_b(t-s)\n\nAfter flattening all exponential terms into one pole index k, the linear hierarchy is\n\nbeginaligned\ndotpsi_boldsymbol n\n=left-iH-sum_k n_knu_k+\n          sum_b L_bz_b^*(t)rightpsi_boldsymbol n\n +sum_k n_kc_kL_b(k)\n       psi_boldsymbol n-boldsymbol e_k\n  -sum_k L_b(k)^dagger\n       psi_boldsymbol n+boldsymbol e_k\nendaligned\n\nInitially only the root is occupied: psi_boldsymbol 0(0)=psi_0 and psi_boldsymbol n(0)=0 for boldsymbol nne0. The root of a linear HOPS trajectory is deliberately not normalized. The unbiased density estimator is\n\nrho(t)=mathbb Eleft\n psi_boldsymbol0(t)rangle\n langlepsi_boldsymbol0(t)right\n\nnot the average of individually normalized projectors. Calling hops_density applies this rule.\n\nThis convention is the one introduced by Süß, Eisfeld, and Strunz, Phys. Rev. Lett. 113, 150403 (2014). The underlying non-Markovian quantum-state-diffusion construction is described by Diósi, Gisin, and Strunz, Phys. Rev. A 58, 1699 (1998).","title":"Physical convention"},{"category":"section","location":"hops/#Prepared-PI-workflow","page":"PI–HOPS stochastic non-Markovian dynamics","text":"For one collective bath, the basic workflow is\n\nbasis = PIBasis(N, 2)\nspin = spin_matrices()\nH = collective_operator(basis, spin.jx)\nQ = collective_operator(basis, spin.jz)\n\nbath = HOPSBath(Q, coefficient, decay)\nplan = HOPSPlan(H, bath; max_depth=4, scaling=:scaled)\nworkspace = HOPSWorkspace(plan)\n\nrho0 = iid_pure_state(basis, ComplexF64[1, 1] / sqrt(2))\npsi0 = weak_pi_pseudoket(rho0)\ntimes = range(0.0, 2.0; length=101)\n\npath = hops_trajectory(\n    plan, psi0, times;\n    dt=0.005, rng=Random.Xoshiro(11), workspace)\nrho_path = hops_density(path, lastindex(times))\n\nmean_states = hops_average(\n    plan, psi0, times, 1000;\n    dt=0.005, seed=11, threaded=true)\n\nHOPSPlan owns the immutable hierarchy topology, physical Schur blocks, decay factors, and scaling data. HOPSWorkspace owns trajectory-local hierarchy, integration, and noise scratch. A workspace must not be shared by simultaneous tasks.\n\nNo state or operator of dimension d^N is constructed. Every PI Hamiltonian and coupling acts blockwise on the direct sum of Schur irreps used by WeakPIPseudoKet. The multiplicity spaces are spectators.\n\nA general mixed PI state can be passed directly to hops_average. The library prepares a HOPSInitialEnsemble by diagonalizing each multiplicity-weighted Schur block and samples its positive eigencomponents:\n\ninitial_ensemble = hops_initial_ensemble(plan, rho0_mixed)\nresult = hops_average(\n    plan, initial_ensemble, times, 1000;\n    dt=0.005, return_info=true)\n\nThe spectral preparation retains only PI-sized block data, never clips a negative eigenvalue, and does not sample multiplicity tableaux. The optional result reports Hilbert–Schmidt Monte Carlo spread and standard error at every saved time. Those statistics do not include time-step or hierarchy error.\n\nA physical HEOMBath with zero white-noise residue can be passed directly to HOPSPlan or converted with HOPSBath(heom_bath). Conversion checks by default that its right correlation is the conjugate of its left correlation. A nonzero HEOM residue requires a separate Markovian unraveling and is therefore rejected by this linear colored-noise backend.","title":"Prepared PI workflow"},{"category":"section","location":"hops/#Instantaneous-PI-pulses","page":"PI–HOPS stochastic non-Markovian dynamics","text":"An ideal control pulse must act on every hierarchy auxiliary; restarting hops_trajectory after a pulse would reset both the auxiliaries and their colored-noise memory and therefore solve a different problem. Prepare the Schur action once:\n\nUx = exp(-im * pi * spin.jx)\npulse = PIUnitaryPulse(basis, Ux)\nsequence = HierarchyPulseSequence(pulse_times, pulse)\n\nresult = hops_average(\n    plan, psi0, times, trajectories;\n    dt=0.005, pulses=sequence, return_info=true)\n\nA local d-by-d matrix is lifted as U^otimes N without constructing that full-Hilbert matrix. A compatible PIOperator may be supplied instead. The pulse maps every auxiliary pseudo-ket as psi_boldsymbol nmapsto Upsi_boldsymbol n, while the OU state and the other hierarchy data remain continuous.\n\nPulse times are finite, ordered events. The driver splits a nominal step exactly at every event. It applies events in (start, stop], so a pulse at a saved time is applied before saving and is not applied again in the adjacent interval. Equal-time events are applied in input order. These are ideal instantaneous unitaries; a finite pulse shape requires a model and evolution route that explicitly represent its time-dependent Hamiltonian rather than interpreting its duration as zero.\n\nThe published Platonic Eulerian sequences can be prepared without transcribing their Cayley-graph words:\n\ntedd = tetrahedral_pulse_sequence(basis, tau0)\noedd = octahedral_pulse_sequence(basis, tau0)\niedd = icosahedral_pulse_sequence(basis, tau0)\n\nThey contain 24, 48, and 120 equally spaced ideal pulses per cycle. The constructors reuse their two immutable global rotation generators and return ordinary HierarchyPulseSequence objects, so the same schedules work with both PI–HOPS and PI–HEOM. See Read, Serrano-Ensástiga, and Martin, Quantum 9, 1661 (2025) for the decoupling and anisotropy conditions. The instantaneous hierarchy backend does not by itself simulate the finite-width control Hamiltonians considered in the robustness analysis.","title":"Instantaneous PI pulses"},{"category":"section","location":"hops/#What-“PI-HOPS”-means","page":"PI–HOPS stochastic non-Markovian dynamics","text":"The exact reduced backend applies to a shared bath whose system coupling L_b is itself PI. Such an operator is block diagonal in the Schur decomposition and acts as the identity on each multiplicity space. The HOPS path therefore remains representable by Schur-irrep amplitudes.\n\nThis condition excludes independent local colored noises,\n\nsum_i=1^N L_i z_i(t)\n\neven when all z_i have identical statistics. A single realization has different values of z_i, breaks permutation symmetry, and generally acts nontrivially on the Schur multiplicity spaces. The ensemble-averaged density may be PI, but an individual path is not a weak-PI pseudo-ket. Use PI–HEOM or the finite-cutoff local-pseudomode supersite backend for that problem. Replacing the independent z_i by one common process would describe a different, collectively correlated environment.\n\nLikewise, a WeakPIPseudoKet is an auxiliary state in the direct sum of Schur irreps. It is not a labeled-particle vector in the full Hilbert space, and relative phases between different Schur sectors are not physical PI data.","title":"What “PI HOPS” means"},{"category":"section","location":"hops/#Finite-temperature-correlations","page":"PI–HOPS stochastic non-Markovian dynamics","text":"For a self-adjoint coupling Q=Q^dagger, the linear equation above can use the complete physical thermal correlation as one sum process. This is the finite-temperature construction described in the original HOPS work. The two-sided kernel must nevertheless be a valid Gaussian covariance: C(-t)=C(t)^*, C(0) must be real and nonnegative, and every finite covariance matrix sampled from it must be positive semidefinite.\n\nAn arbitrary HEOMBath is therefore not automatically interchangeable with a HOPSBath. HEOM can evolve a user-supplied left/right exponential decomposition algebraically, whereas HOPS must also realize its left correlation as a stochastic covariance. In particular, an inconsistent explicit right correlation, a truncated fit that is not positive semidefinite, or an unapplied white-noise residue must not be hidden by the noise generator.\n\nFor strong coupling, Hartmann and Strunz recommend an alternative exact thermal construction for Hermitian coupling: use the zero-temperature correlation in the quantum hierarchy and add the thermal part as an independent Hermitian stochastic potential. That formulation can converge at substantially smaller hierarchy depth. It is not silently inferred from a finite-temperature pole list and is not a separate evolution mode in this initial linear backend. Use the full physical covariance or PI–HEOM instead. A non-Hermitian finite-temperature coupling requires the thermofield-doubled construction rather than the one-process Hermitian shortcut.","title":"Finite-temperature correlations"},{"category":"section","location":"hops/#Noise-generation","page":"PI–HOPS stochastic non-Markovian dynamics","text":"The built-in fast generator is an exact discrete-time stationary Ornstein–Uhlenbeck construction for exponential terms whose coefficients are real and nonnegative. For one term,\n\nx(t+Delta t)=e^-nuDelta tx(t)\n +sqrtcleft(1-e^-2mathrmRe(nu)Delta tright)eta\n\nwhere eta is a proper unit-variance complex Gaussian. The initial value is sampled from the stationary distribution, x(0)=sqrt ceta_0; setting it to zero would generate the wrong nonstationary covariance. Components belonging to the same bath are summed before applying L_b.\n\nA finite exponential representation may contain complex or negative individual coefficients even though its total correlation is physical. Taking a complex square root of each coefficient does not generate the required Gaussian process. For such a decomposition, pass an explicitly prepared noise realization to hops_trajectory. A callable has the form noise(time, bath_number) and must return the same value whenever the integrator revisits a time. In stochastic use it should interpolate one realization prepared before propagation, and its ensemble must obey the total two-time covariance. Spectral synthesis is a standard construction; Hartmann and Strunz give an FFT implementation in J. Chem. Theory Comput. 13, 5834 (2017).\n\nNoise must be fixed before a path is integrated. Drawing a new value whenever an adaptive right-hand side is evaluated changes the stochastic process and does not implement HOPS. The built-in path integrator therefore uses one explicit grid and a fixed dt. Repeat with a smaller dt.","title":"Noise generation"},{"category":"section","location":"hops/#Scaling-and-hierarchy-truncation","page":"PI–HOPS stochastic non-Markovian dynamics","text":"With scaling=:scaled, the stored auxiliary is\n\nwidehatpsi_boldsymbol n\n=fracpsi_boldsymbol n\n prod_ksqrtn_ka_k^n_k\n\nwith positive prepared pole scales a_k. The corresponding downward and upward coefficients are\n\nsqrtn_kfracc_ksqrta_k\nqquad\nsqrt(n_k+1)a_k\n\nrespectively. This is an exact diagonal similarity transformation of the retained hierarchy. It changes neither the root nor the modeled bath. scaling=:unscaled is useful when comparing equations term by term. hops_coordinate_scale exposes s_boldsymbol n for a retained node. Exact duplicate poles within one bath are combined before these scales and the hierarchy are built; exact cancellations are removed.\n\nmax_depth=D retains multi-indices satisfying sum_k n_kle D. For P exponential poles, the number of retained auxiliary pure states is\n\nbinomP+DD\n\nMissing upward neighbors are zero in the finite hierarchy. This hard truncation is an approximation, and convergence must be checked by increasing max_depth. A deeper hierarchy is commonly needed for stronger coupling, slower bath decay, or longer propagation. Alternative HOPS truncations and their convergence behavior are analyzed by Zhang, Bentley, and Eisfeld, J. Chem. Phys. 148, 134103 (2018).\n\nA positive importance_cutoff additionally retains a deterministic downward-closed order ideal. Its score is a heuristic, not an error bound. The setup is budget-bounded by retained and frontier nodes, so it can prepare a small retained hierarchy even when the exact unpruned binomial count does not fit Int. hops_auxiliary_importances returns the retained scores. Report and converge this cutoff separately.\n\nFor one Schur irrep of dimension m_nu, a trajectory stores quantities scaling as m_nubinomP+DD, rather than the m_nu^2binomP+DD density coordinates of an analogous HEOM hierarchy. HOPS exchanges that per-path reduction for Monte Carlo sampling. Which method is faster depends on the Schur support, hierarchy depth, sampling accuracy, and requested observables.\n\nWithin one path, every prepared Schur block acts on all hierarchy nodes in a matrix–matrix kernel. Edges are grouped by bath, and one hierarchy-sized action buffer is reused for L and L^dagger. A self-adjoint coupling uses one combined edge pass; a non-Hermitian coupling overwrites the same buffer and uses separate downward/upward edge passes. Across paths, hops_average(...; threaded=true) uses deterministic balanced partitions with one workspace and random stream per active task. Float32, Float64, and BigFloat inputs retain their selected scalar type. BigFloat plans capture the required precision and rounding context and restore it for workspace construction, conditioned application, integration, and ensemble reduction.","title":"Scaling and hierarchy truncation"},{"category":"section","location":"hops/#Reliability-checklist","page":"PI–HOPS stochastic non-Markovian dynamics","text":"For a research calculation:\n\nverify that every bath coupling is PI and that each noise represents a shared environment;\ncheck that the exponential fit is accurate throughout the simulated memory interval;\ndecrease dt;\nincrease max_depth;\nincrease the number of independent trajectories and report a statistical uncertainty;\ncompare against PI–HEOM at a smaller size or shallower hierarchy when possible.\n\nAgreement between two trajectory counts is not a hierarchy convergence test. Agreement between two hierarchy depths is not a sampling-error estimate. Initial system–bath correlations are outside the factorized Gaussian-bath derivation and are not inferred by the backend.","title":"Reliability checklist"},{"category":"section","location":"hops/#Runnable-examples","page":"PI–HOPS stochastic non-Markovian dynamics","text":"The HOPS examples separate four complementary workflows:\n\nexamples/pi_hops.jl introduces a pure-state, one-bath collective-dephasing calculation and compares the HOPS mean with deterministic PI–HEOM and the analytical coherence.\nexamples/pi_hops_collective_emission.jl uses the non-Hermitian coupling L=J_- in a one-excitation manifold. It validates the exact depth-one closure, prescribed conditioned noise, hops_density, hierarchy inspection, and deterministic hops_rhs!.\nexamples/pi_hops_mixed_multibath.jl starts from a genuinely mixed, multi-sector PI state and two independent shared baths. It demonstrates hops_initial_ensemble, HOPSBatchWorkspace, return_info=true, Monte Carlo error contraction, and setup-only importance pruning.\nexamples/nonmarkovian_dynamical_decoupling.jl applies ideal CPMG and UDD4 pulses to every hierarchy auxiliary without restarting the colored noise. It compares the HOPS ensemble with PI–HEOM and the exact filter function for the same full-line one-pole Lorentzian correlation, alongside a separately integrated positive-frequency analytical curve.\n\nAll four scripts retain numerical assertions when CairoMakie is unavailable. With the examples environment they also render the already validated arrays; plotting never triggers another HOPS solve.","title":"Runnable examples"},{"category":"section","location":"hops/#API","page":"PI–HOPS stochastic non-Markovian dynamics","text":"","title":"API"},{"category":"type","location":"hops/#PermutationalInvariantDynamics.PIUnitaryPulse","page":"PI–HOPS stochastic non-Markovian dynamics","text":"PIUnitaryPulse(basis, unitary;\n               atol=0, rtol=sqrt(eps(...)), cache=nothing,\n               memory_budget=512MiB)\n\nPrepare one instantaneous PI system unitary for hierarchy dynamics. unitary may be either a local d-by-d matrix, which is lifted as unitary^⊗N, or a PIOperator on the exact basis. Only physical Schur blocks are retained; no PI-coordinate superoperator or full-Hilbert matrix is constructed.\n\nThe prepared pulse is immutable and shareable. Applying it to HEOM maps every ADO as rho_n -> U*rho_n*U'; applying it to HOPS maps every auxiliary ket as psi_n -> U*psi_n. Mutable application scratch belongs to the corresponding hierarchy workspace.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.PIUnitaryPulse"},{"category":"type","location":"hops/#PermutationalInvariantDynamics.HierarchyPulseSequence","page":"PI–HOPS stochastic non-Markovian dynamics","text":"HierarchyPulseSequence(times, pulse)\nHierarchyPulseSequence(times, pulses)\n\nPrepare ordered instantaneous events for HEOM or HOPS evolution. The first form reuses one PIUnitaryPulse at every time. The second accepts one compatible prepared pulse per time. Times must be finite and nondecreasing; equal times are allowed and their pulses are applied deterministically in input order.\n\nEvolution applies events in the half-open convention (start, stop]. Thus a pulse exactly at a saved time is applied before that state is saved, while a pulse at the initial time is not applied implicitly. This convention prevents double application when adjacent propagation intervals share an endpoint. Events outside a requested propagation span are retained but ignored.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.HierarchyPulseSequence"},{"category":"function","location":"hops/#PermutationalInvariantDynamics.platonic_pulse_sequence","page":"PI–HOPS stochastic non-Markovian dynamics","text":"platonic_pulse_sequence(basis, group, pulse_interval;\n                        cycles=1, start_time=0, T=Float64,\n                        atol=0, rtol=nothing, cache=nothing,\n                        memory_budget=512MiB)\n\nConstruct one of the ideal Eulerian Platonic dynamical-decoupling schedules of Read, Serrano-Ensástiga, and Martin, Quantum 9, 1661 (2025). group must be :tetrahedral, :octahedral, or :icosahedral, producing the published TEDD, OEDD, or IEDD word of 24, 48, or 120 pulses per cycle.\n\nEvery pulse is a global spin rotation, represented locally as exp(-im*angle*(axis_x*jx + axis_y*jy + axis_z*jz)) for the spin j=(basis.d-1)/2. The first event occurs after one positive pulse_interval; a free interval precedes every event, including the final cyclic pulse. Repeated cycles concatenate the published word without repreparing its two generators. start_time shifts the complete schedule. The built-in axis–angle matrix preparation supports T=Float32 and T=Float64; wider custom rotations can still be supplied to PIUnitaryPulse as prepared PI operators and scheduled with HierarchyPulseSequence.\n\nThe returned HierarchyPulseSequence acts directly on every HEOM ADO or HOPS auxiliary. These are instantaneous pulses. Their Eulerian finite-duration robustness does not model a nonzero pulse width; finite pulses require an explicit time-dependent control Hamiltonian.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.platonic_pulse_sequence"},{"category":"function","location":"hops/#PermutationalInvariantDynamics.tetrahedral_pulse_sequence","page":"PI–HOPS stochastic non-Markovian dynamics","text":"tetrahedral_pulse_sequence(basis, pulse_interval; kwargs...)\n\nConstruct the published 24-pulse tetrahedral Eulerian DD sequence (TEDD). See platonic_pulse_sequence for timing, precision, and memory keywords.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.tetrahedral_pulse_sequence"},{"category":"function","location":"hops/#PermutationalInvariantDynamics.octahedral_pulse_sequence","page":"PI–HOPS stochastic non-Markovian dynamics","text":"octahedral_pulse_sequence(basis, pulse_interval; kwargs...)\n\nConstruct the published 48-pulse octahedral Eulerian DD sequence (OEDD). See platonic_pulse_sequence for timing, precision, and memory keywords.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.octahedral_pulse_sequence"},{"category":"function","location":"hops/#PermutationalInvariantDynamics.icosahedral_pulse_sequence","page":"PI–HOPS stochastic non-Markovian dynamics","text":"icosahedral_pulse_sequence(basis, pulse_interval; kwargs...)\n\nConstruct the published 120-pulse icosahedral Eulerian DD sequence (IEDD). See platonic_pulse_sequence for timing, precision, and memory keywords.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.icosahedral_pulse_sequence"},{"category":"function","location":"hops/#PermutationalInvariantDynamics.apply_hierarchy_pulse!","page":"PI–HOPS stochastic non-Markovian dynamics","text":"apply_hierarchy_pulse!(state, pulse, workspace)\n\nApply a prepared instantaneous system unitary to every ADO of state, in place, as rho_n -> U*rho_n*U'. The hierarchy scaling and every system–bath memory auxiliary are retained. workspace must be a task-owned HEOMEvolutionWorkspace for the same plan.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.apply_hierarchy_pulse!"},{"category":"type","location":"hops/#PermutationalInvariantDynamics.HOPSBath","page":"PI–HOPS stochastic non-Markovian dynamics","text":"HOPSBath(coupling, coefficients, frequencies; label=:bath)\n\nFinite exponential correlation for one shared Gaussian bath in the hierarchy of pure states (HOPS),\n\nC(t)=sum_k c_kexp(-nu_k t)qquad tgeq0\n\ncoupling is a fixed PI operator L. Unlike HEOMBath, it need not be Hermitian: the linear HOPS hierarchy uses L in its noise and downward terms and L^dagger in its upward term. Every coefficient must be finite and every pole must be finite with strictly positive real part. The constructor copies all input data.\n\nThe bath describes one shared noise realization multiplying a PI operator. Independent local colored noises are not PI path by path and cannot be represented by this backend.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.HOPSBath"},{"category":"type","location":"hops/#PermutationalInvariantDynamics.HOPSPlan","page":"PI–HOPS stochastic non-Markovian dynamics","text":"HOPSPlan(hamiltonian, baths; max_depth, scaling=:scaled,\n         scaling_factors=nothing, importance_cutoff=0,\n         importance_metric=:normalized_coupling_decay,\n         memory_budget=512MiB)\n\nPrepare the linear hierarchy of pure states for a fixed Hermitian PI Hamiltonian and one or more HOPSBaths. Each hierarchy node is a pseudo-ket in directsum_nu U_nu and therefore has weak_pi_dimension amplitudes rather than length(basis) density coordinates.\n\nFor flattened pole index k and multi-index n, the unscaled equation is\n\ndotpsi_n=(-iH-ncdotnu+sum_bL_bz_b^*)psi_n\n +sum_k n_kc_kL_b(k)psi_n-e_k\n -sum_kL_b(k)^daggerpsi_n+e_k\n\nmax_depth is a hard zero boundary. A positive importance_cutoff applies the same deterministic downward-closed heuristic as PI–HEOM and is an additional explicit approximation. With scaling=:scaled, the stored coordinate is divided by prod_k sqrt(n_k a_k^n_k). This is an exact diagonal similarity of the retained hierarchy; the default positive scale is abs(c_k). Duplicate equal poles within one bath are combined exactly, and an exactly cancelled pole is removed before hierarchy construction.\n\nThe plan is read-only and shareable. Mutable hierarchy, RK4, coupling, and colored-noise storage belongs to HOPSWorkspace. The memory budget is enforced before hierarchy enumeration; Inf is the explicit opt-out.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.HOPSPlan"},{"category":"type","location":"hops/#PermutationalInvariantDynamics.HOPSWorkspace","page":"PI–HOPS stochastic non-Markovian dynamics","text":"HOPSWorkspace(plan; memory_budget=512MiB)\n\nTask-owned mutable storage for one HOPSPlan. The workspace retains four low-storage RK4 hierarchy vectors, one reusable coupling-action hierarchy, three OU component stages, and three bath-noise stages. Reuse it across sequential paths. It must not be used concurrently.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.HOPSWorkspace"},{"category":"type","location":"hops/#PermutationalInvariantDynamics.HOPSBatchWorkspace","page":"PI–HOPS stochastic non-Markovian dynamics","text":"HOPSBatchWorkspace(plan; workers=Threads.nthreads(),\n                   memory_budget=512MiB)\n\nReusable pool for hops_average. The immutable plan is shared; every worker owns an independent HOPSWorkspace and RNG. A batch workspace is mutable and must not serve concurrent ensemble calls.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.HOPSBatchWorkspace"},{"category":"type","location":"hops/#PermutationalInvariantDynamics.HOPSInitialEnsemble","page":"PI–HOPS stochastic non-Markovian dynamics","text":"HOPSInitialEnsemble(rho; atol=..., rtol=...)\nhops_initial_ensemble(plan, rho; kwargs...)\n\nPrepare a categorical pure-root ensemble for a general normalized PI density operator. Each multiplicity-weighted Schur block sqrt(f^nu) C_nu is diagonalized. Its strictly positive eigenvalues are the component weights and its eigenvectors are roots supported in that Schur sector. No multiplicity-tableau sampling and no d^N state are required.\n\nThe input must be Hermitian within the explicit tolerances, have unit trace within those tolerances, and have no negative computed eigenvalue. Negative eigenvalues are never clipped. Zero eigenvalues are omitted exactly.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.HOPSInitialEnsemble"},{"category":"type","location":"hops/#PermutationalInvariantDynamics.HOPSRootKet","page":"PI–HOPS stochastic non-Markovian dynamics","text":"HOPSRootKet(basis, data)\n\nUnnormalized direct-sum Schur-irrep root of a linear HOPS hierarchy. Its sector slice psi_nu contributes psi_nu*psi_nusqrt(f^nu) to the PI coefficient block. Unlike WeakPIPseudoKet, construction does not require unit norm.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.HOPSRootKet"},{"category":"type","location":"hops/#PermutationalInvariantDynamics.HOPSTrajectory","page":"PI–HOPS stochastic non-Markovian dynamics","text":"HOPSTrajectory\n\nSaved root pseudo-kets and bath-noise values from one linear PI-HOPS path. Only the root is retained at requested output times; auxiliary hierarchy states remain in the reusable workspace. noise[b,j] is the shared process z_b at times[j].\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.HOPSTrajectory"},{"category":"type","location":"hops/#PermutationalInvariantDynamics.HOPSEnsembleResult","page":"PI–HOPS stochastic non-Markovian dynamics","text":"HOPSEnsembleResult\n\nOptional diagnostic result from hops_average(...; return_info=true). states are the unmodified averages of unnormalized root outer products. sample_spread[j] is the unbiased Hilbert–Schmidt sample spread at times[j], and standard_error[j] is its norm divided by the square root of the independent path count. These are Monte Carlo diagnostics, not hierarchy- or time-step-error estimates.\n\nIndexing and iteration traverse states.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.HOPSEnsembleResult"},{"category":"function","location":"hops/#PermutationalInvariantDynamics.hops_number_auxiliaries","page":"PI–HOPS stochastic non-Markovian dynamics","text":"Return the number of retained pure-state hierarchy nodes.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.hops_number_auxiliaries"},{"category":"function","location":"hops/#PermutationalInvariantDynamics.hops_multiindices","page":"PI–HOPS stochastic non-Markovian dynamics","text":"Return detached copies of the retained HOPS hierarchy multi-indices.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.hops_multiindices"},{"category":"function","location":"hops/#PermutationalInvariantDynamics.hops_hierarchy_metadata","page":"PI–HOPS stochastic non-Markovian dynamics","text":"hops_hierarchy_metadata(plan)\n\nReturn hierarchy size, truncation, scaling, noise, and PI-coordinate metadata without materializing a hierarchy state.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.hops_hierarchy_metadata"},{"category":"function","location":"hops/#PermutationalInvariantDynamics.hops_auxiliary_importances","page":"PI–HOPS stochastic non-Markovian dynamics","text":"hops_auxiliary_importances(plan)\n\nReturn the dimensionless heuristic score associated with every retained HOPS node. The root score is one. Scores determine an explicit positive importance_cutoff; they are diagnostics, not error bounds.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.hops_auxiliary_importances"},{"category":"function","location":"hops/#PermutationalInvariantDynamics.hops_coordinate_scale","page":"PI–HOPS stochastic non-Markovian dynamics","text":"hops_coordinate_scale(plan, label)\n\nReturn the positive scalar s_n relating one stored scaled auxiliary root to the conventional unscaled root, psi_n=s_n*psihat_n. label may be a one-based retained-node index or its pole-occupation vector. The root and every node of an unscaled plan return one.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.hops_coordinate_scale"},{"category":"function","location":"hops/#PermutationalInvariantDynamics.hops_initial_ensemble","page":"PI–HOPS stochastic non-Markovian dynamics","text":"hops_initial_ensemble(plan, rho; kwargs...)\n\nPrepare a HOPSInitialEnsemble and require exact PI-basis and scalar compatibility with plan.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.hops_initial_ensemble"},{"category":"function","location":"hops/#PermutationalInvariantDynamics.hops_trajectory","page":"PI–HOPS stochastic non-Markovian dynamics","text":"hops_trajectory(plan, initial, times; dt, rng=Random.default_rng(),\n                noise=nothing, workspace=nothing, pulses=nothing,\n                memory_budget=512MiB)\n\nIntegrate one fixed-step linear PI-HOPS path. initial is a normalized WeakPIPseudoKet; all auxiliary hierarchy nodes start at zero. The returned HOPSTrajectory retains only root pseudo-kets and bath-noise values at times.\n\nWhen every exponential coefficient is real and nonnegative, noise=nothing uses the exact stationary complex Ornstein–Uhlenbeck transition at every half step. Otherwise pass a deterministic provider supporting either noise(time, bath_index) or in-place noise!(destination, time). The provider must represent the covariance of the total bath correlation and must return the same value whenever queried at the same time. Adaptive integration is intentionally absent because rejected stochastic steps need a consistent OU/Brownian bridge. A HierarchyPulseSequence passed as pulses splits steps exactly at every event; roots saved at a pulse time are post-pulse.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.hops_trajectory"},{"category":"function","location":"hops/#PermutationalInvariantDynamics.hops_density","page":"PI–HOPS stochastic non-Markovian dynamics","text":"hops_density(root)\nhops_density(trajectory, index)\n\nConstruct the unnormalized PI density contribution psi_0psi_0 of one linear-HOPS root. The physical Monte Carlo estimator averages these returned coefficient vectors without normalizing individual paths. Consequently trace(hops_density(root)) == norm(root)^2 up to roundoff.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.hops_density"},{"category":"function","location":"hops/#PermutationalInvariantDynamics.hops_average","page":"PI–HOPS stochastic non-Markovian dynamics","text":"hops_average(plan, initial, times, trajectories;\n             dt, seed=0, threaded=false, workspace=nothing,\n             return_info=false, pulses=nothing,\n             memory_budget=512MiB)\nhops_average(plan, rho::PIState, times, trajectories; ...)\n\nAverage independent linear PI-HOPS paths without retaining their hierarchy or root histories. Every sampled root is converted to its unnormalized PI outer product before online Welford reduction. Individual roots and the final average are never normalized or positivity-repaired.\n\nThe fixed seed assigns one UInt64 stream to every trajectory index. threaded=true shares the read-only plan and uses task-owned workspaces with deterministic balanced partitions. Floating reductions are merged in worker order and can differ by roundoff when the active worker count changes. Pass a reusable HOPSBatchWorkspace to amortize hierarchy storage across ensembles.\n\nBy default the function returns Vector{PIState}. Set return_info=true for a HOPSEnsembleResult carrying Hilbert–Schmidt Monte Carlo uncertainties. Time step, hierarchy depth/cutoff, correlation decomposition, and path count require separate convergence checks.\n\nA general normalized PIState is accepted through an automatically prepared HOPSInitialEnsemble. Its Schur eigencomponent is sampled before the stationary bath noise on each independently seeded path. Use initial_atol/initial_rtol only to acknowledge known roundoff-level input errors; negative computed eigenvalues are never clipped.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.hops_average"},{"category":"function","location":"hops/#PermutationalInvariantDynamics.hops_rhs!","page":"PI–HOPS stochastic non-Markovian dynamics","text":"hops_rhs!(destination, plan, source, noise, workspace)\n\nApply the deterministic conditioned linear-HOPS hierarchy action to flat node-major vectors. noise contains one current z_b value per shared bath. Random sampling is never performed by this function; see hops_trajectory for prepared colored-noise paths.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.hops_rhs!"},{"category":"section","location":"interoperability/#Optional-ecosystem-integrations","page":"Optional ecosystem integrations","text":"The core package remains lightweight. Optional Julia packages activate extensions only after they are loaded; none changes the PI representation or causes a full-Hilbert reconstruction.","title":"Optional ecosystem integrations"},{"category":"section","location":"interoperability/#QuantumOptics.jl-and-QuantumToolbox.jl","page":"Optional ecosystem integrations","text":"One-particle operators from either general-purpose package can be passed directly to LocalHamiltonian, CollectiveHamiltonian, LocalJump, CollectiveJump, and collective_operator after loading the corresponding package:\n\nusing PermutationalInvariantDynamics\nusing QuantumOptics\n\nlocal_basis = NLevelBasis(2)\nlowering = transition(local_basis, 1, 2)\nbasis = PIBasis(20, 2)\nmodel = PIModel(basis, (LocalJump(lowering; rate=0.1),))\n\nThe same pattern works with a QuantumToolbox QuantumObject of operator type. The bridge copies only the supplied one-site matrix and preserves its dense or sparse storage and scalar type. It rejects kets, superoperators, rectangular maps, QuantumOptics maps whose left and right Hilbert-space bases differ even when their dimensions agree, nonfinite data, and an operator whose dimension does not match the PI basis. It never imports a d^N state or superoperator. The dependency-neutral local_operator_matrix helper can be used explicitly when a script wants to inspect the detached matrix before constructing terms.","title":"QuantumOptics.jl and QuantumToolbox.jl"},{"category":"section","location":"interoperability/#Clarabel","page":"Optional ecosystem integrations","text":"Clarabel.jl supplies the semidefinite optimizer for the PI qubit PPT-mixture test. Load it explicitly before the first solve:\n\nusing PermutationalInvariantDynamics\nimport Clarabel\n\nbasis = PIBasis(3, 2)\nresult = ppt_mixture_test(ghz_state(basis))\n\nThe core PPTMixturePlan prepares the sparse Schur-block conic map without loading Clarabel. The extension creates solver state for each call, maps the complex Hermitian constraints to real PSD cones, and returns its primal and dual vectors to core code for independent certificate validation. A solver termination status alone is never promoted to an entanglement claim. See Genuine multipartite entanglement for the three-valued result semantics, restricted-sector behavior, scaling, and memory controls.","title":"Clarabel"},{"category":"section","location":"interoperability/#Tables.jl","page":"Optional ecosystem integrations","text":"After using Tables, ParameterScanResult is a lazy row table. Large states, eigenvectors, and nested diagnostics are excluded from its implicit schema. ComplexSpectrum, QuditHusimiData, and ConvergenceStudyResult expose dependency-free numerical columns (including HEOM depth reports):\n\nusing Tables\ncolumns = Tables.columntable(scan_result)\nspectral_rows = Tables.rowtable(complex_spectrum)\n\nUse the explicit parameter_scan_rows(...; include_output=true) route when a sink genuinely needs large saved outputs.\n\nColumn adapters borrow result-owned vectors when possible and should be treated as read-only views of the source object. Missing spectrum diagnostics use an O(1) read-only logical column rather than allocating one missing per mode. Qudit tabular data includes the aggregate point-index/Q series, not the optional sector matrix. Convergence tables include estimate; those entries may themselves be states or arrays, so collecting the table is not automatically a compact export. Choose a scalar estimate in the original refinement study when bounded output is required.","title":"Tables.jl"},{"category":"section","location":"interoperability/#Makie","page":"Optional ecosystem integrations","text":"Loading Makie or CairoMakie activates plot conversions for existing numerical objects:\n\nusing CairoMakie\nscatter(complex_spectrum)       # Re(lambda), Im(lambda)\nheatmap(spin_phase_space_data)  # theta, phi, density\nheatmap(schur_structure)        # input/output sector weights\nlines(convergence_report)       # refinement versus pairwise error\nlines(qudit_husimi_data)        # supplied point index versus Q\n\nConversions never run a solver or phase-space transform. Axis labels, palettes, logarithmic scales, and publication styling remain ordinary Makie attributes. The dependency-free SVG renderers continue to be available when Makie is absent.\n\nConversions prefer ranges, views, and lazy permutations. They expose the aggregate spin/qudit data and raw Schur weights; resolved sectors, manifold coordinates, residual annotations, and physical labels remain explicit user plotting choices. Generic scalar layouts may require a safe allocating fallback, so conversion should not be treated as an allocation-free solver kernel.","title":"Makie"},{"category":"section","location":"interoperability/#Distributed","page":"Optional ecosystem integrations","text":"using Distributed activates distributed_parameter_scan. Independent points are assigned to deterministic, balanced worker chunks and merged in global index order. Continuation scans remain serial because partitioning a path-dependent warm start would change the computation. Builders, parameters, algorithms, and diagnostics must serialize, and every selected worker must use a compatible active project. Master callbacks require save_outputs=true; all chunks finish before master-side stopping or failure policy is applied.\n\nIt also activates process-parallel quantum-jump and diffusive ensembles. Each worker prepares geometry once for a deterministic contiguous chunk, while the master assigns random streams by global trajectory index. Full saved results must be serialized back to the master, so threaded or adaptive state-free ensembles remain preferable when only statistics are required. Distributed adaptive stopping is not implemented.","title":"Distributed"},{"category":"section","location":"interoperability/#QuantumCumulants,-JLD2,-and-HDF5","page":"Optional ecosystem integrations","text":"The QuantumCumulants 0.5 extension supplies exact PI initial moments and automatic microscopic symbolic lowering; see the cumulant bridge. Symbolic spaces and indices are created only inside the extension. Direct PI/nonmicroscopic terms and unevaluated schedules are rejected, and the resulting selected-order closure remains an approximation to check against exact PI moments. JLD2 and HDF5 activate portable checkpoint writers described in the research utilities. All optional methods validate their schema and reject ambiguous or narrowing conversions.\n\nThe repository's isolated optional-extension CI environment smoke-tests the Clarabel PPT-mixture solve, Makie conversions, QuantumOptics and QuantumToolbox one-site imports, QuantumCumulants lowering, and JLD2/HDF5 checkpoint round trips without adding any of these packages to the core dependency set.","title":"QuantumCumulants, JLD2, and HDF5"},{"category":"section","location":"interoperability/#API","page":"Optional ecosystem integrations","text":"","title":"API"},{"category":"function","location":"interoperability/#PermutationalInvariantDynamics.distributed_quantum_trajectories","page":"Optional ecosystem integrations","text":"distributed_quantum_trajectories(model, rho0, times, n;\n    workers=Distributed.workers(), seed=0, dt, trajectory_keywords...)\n\nGenerate an ordered ensemble of independent PI quantum-jump trajectories on multiple Julia worker processes. Loading the Distributed standard library activates the implementation. The master creates the same trajectory-indexed random streams as quantum_trajectories, partitions them into deterministic contiguous chunks, and restores global trajectory order after the workers finish.\n\nEach worker lowers the supplied PIModel once and reuses one TrajectoryWorkspace for its chunk. The model, initial state, time grid, parameters, and callable rates must therefore be serializable and every worker must be started with an environment containing a compatible package version. Full state histories cross the process boundary; use the threaded or adaptive ensemble APIs when only online statistics are required.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.distributed_quantum_trajectories"},{"category":"function","location":"interoperability/#PermutationalInvariantDynamics.distributed_diffusive_trajectories","page":"Optional ecosystem integrations","text":"distributed_diffusive_trajectories(model, rho0, times, monitors, n;\n    workers=Distributed.workers(), seed=0, dt, parameters=nothing,\n    observables=nothing, save_states=true)\n\nGenerate an ordered ensemble of diffusive PI trajectories on multiple Julia worker processes. The optional implementation is activated by loading the Distributed standard library. Random streams are derived from the global trajectory index, so results agree with diffusive_trajectories for the same model, controls, seed, and numerical environment.\n\nEvery worker constructs one DiffusiveBatchPlan and reuses one DiffusiveWorkspace. The model, monitors, observables, initial state, parameters, and time grid must be serializable. Saved states, measurement records, and requested observable histories are transferred back to the master; set save_states=false when only records or online observable values are needed.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.distributed_diffusive_trajectories"},{"category":"function","location":"interoperability/#PermutationalInvariantDynamics.local_operator_matrix","page":"Optional ecosystem integrations","text":"local_operator_matrix(operator; dimension=nothing)\n\nReturn a detached matrix representation of a one-particle operator for use in PI model terms. Plain dense and sparse matrices are copied without changing their scalar type or sparse support. dimension, when supplied, validates the expected local Hilbert-space dimension.\n\nOptional package extensions add this method for QuantumOptics.jl and QuantumToolbox.jl operator objects. Only a genuine one-site operator is accepted: kets, superoperators, rectangular maps, and full-system operators whose dimension does not match the requested local dimension are rejected. No full-Hilbert reconstruction or basis-order conversion is performed.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.local_operator_matrix"},{"category":"section","location":"api_reference/#Complete-public-API-index","page":"Complete index","text":"This index covers every exported function, constructor, type, result object, and compatibility alias in PermutationalInvariantDynamics. Entries link to the source docstrings rendered on the categorized reference pages, so the website and Julia's interactive ?name help use the same descriptions.\n\nFunctions whose names end in ! mutate caller-owned output or workspace storage. Read API tiers and prepared analysis before using the advanced workspace and solver interfaces directly.","title":"Complete public API index"},{"category":"section","location":"api_reference/#Reference-sections","page":"Complete index","text":"Section Contents\nRepresentations, states, and models Partitions, GT patterns, Schur blocks, state/operator construction, physical terms, Appendix-D processes, and vectorized superoperators\nDynamics and evolution Liouvillian preparation and application, SciML integration, fixed-step evolution, quantum trajectories, mean field, and Floquet dynamics\nObservables and quantum information Moments, correlations, squeezing, QFI/QFIM, spin phase space, reduced states, negativity, entropies, distances, and symmetry-resolved information\nNonstabilizerness of symmetric qubit states Second stabilizer Rényi entropy, prepared Krawtchouk transforms, scope, and validation\nGenuine multipartite entanglement PI qubit PPT-mixture plans, validated solver results, and the optional Clarabel backend\nStationary states, spectra, and research solvers Direct and Krylov solvers, Liouvillian spectra and gaps, Evans tests, weak symmetries, response, memory estimates, and typed high-level commands\nVisualization Schur-block, density-spectrum, Liouvillian-spectrum, Floquet-spectrum, and spin phase-space SVG renderers\nStreaming output Observable-only deterministic evolution and state-free online trajectory statistics\nMatrix-RHS trajectory cohorts Fixed-capacity conditional propagation, intensities, grouped gains, and index-stable stochastic streams\nWeak-PI pseudo-ket trajectories Direct-sum Schur-irrep pseudo-kets and sector-changing local Kraus branches\nQuantum regression and spectra Prepared two-time correlations, delayed intensity correlations, shifted-GMRES spectra, and finite-window FFTs\nHigher-order cumulant bridge Exact distinct-site PI moments, neutral microscopic metadata, closure comparisons, and the optional QuantumCumulants adapter\nResearch utilities and control Compressed spectral/population inspection, PI channels, tomography, checkpoints, joint symmetries, and gradients\nDiffusive monitoring Preallocated homodyne/heterodyne conditional PI dynamics, reproducible ensembles, and confidence-controlled stochastic stopping\nQudit Husimi phase space Sector-wise generalized coherent-state Q data for arbitrary local dimension\nComposite systems Multiple PI ensembles, finite auxiliary factors, preallocated tensor-product superoperators, and density-valued quantum jumps\nGlobal pseudomodes and shared cavities One shared finite-cutoff mode, collective coupling, factorized dynamics, cutoff observables, and system/mode reductions\nLocal pseudomodes and PI supersites Identical finite-cutoff local modes, system-term lifting, matrix-free dynamics, cutoff checks, and prepared mode tracing\nReusable prepared geometry Immutable representation bundles and an explicit user-owned preparation cache\nPrepared parameter scans Continuation, resumable point records, deterministic threaded scans, and tabular exports\nBlock, multi-shift, and recycled Krylov Thick-restarted block spectra, multiple-right-hand-side solves, shifted families, recycled subspaces, and exponential actions\nOptional accelerators Device capability reports, sparse-upload resource preflight, and guarded extension contracts\nPI–HEOM non-Markovian dynamics PI auxiliary-density hierarchies, matrix-free propagation, and stationary states\nPI–HOPS stochastic non-Markovian dynamics Direct-sum Schur-irrep pure-state hierarchies, colored-noise trajectories, and Monte Carlo density reconstruction\nNumerical convergence reports Time-step, Krylov, HEOM-depth, and sector-cutoff refinement evidence\nReproducible verified experiments Typed calculations, resource explanations, physical/solver/refinement evidence, and portable result archives\nCounting statistics and rare events Matrix-free tilted generators, finite-time MGFs, SCGFs, cumulants, and discrete rate functions\nBath-correlation fitting Spectral quadrature, finite exponential fits, rank diagnostics, and guarded HEOM/HOPS bath construction\nParameter inference Bounded weighted fitting, implicit stationary sensitivities, Fisher matrices, and identifiability diagnostics\nOptional ecosystem integrations QuantumOptics/QuantumToolbox operator bridges, Tables and Makie adapters, process-parallel scans/ensembles, symbolic cumulants, and checkpoint backends","title":"Reference sections"},{"category":"section","location":"api_reference/#Alphabetical-index","page":"Complete index","text":"Modules = [\n    PermutationalInvariantDynamics,\n    PermutationalInvariantDynamics.Models,\n]\nPages = [\"api/representation.md\", \"api/dynamics.md\", \"api/analysis.md\", \"api/solvers.md\", \"api/visualization.md\", \"api_tiers.md\", \"streaming_output.md\", \"batched_trajectories.md\", \"diffusive_monitoring.md\", \"weak_pi_trajectories.md\", \"correlations.md\", \"cumulant_bridge.md\", \"research_utilities.md\", \"composite_systems.md\", \"global_pseudomodes.md\", \"prepared_artifacts.md\", \"parameter_scans.md\", \"krylov_extensions.md\", \"accelerators.md\", \"heom.md\", \"hops.md\", \"bath_fitting.md\", \"counting_statistics.md\", \"experiments.md\", \"inference.md\", \"convergence.md\", \"qudit_phase_space.md\", \"nonstabilizerness.md\", \"genuine_entanglement.md\", \"interoperability.md\"]\nOrder = [:type, :function, :constant, :macro]","title":"Alphabetical index"},{"category":"module","location":"api_reference/#PermutationalInvariantDynamics","page":"Complete index","text":"PermutationalInvariantDynamics\n\nExact and matrix-free tools for time-local open-system dynamics in the permutationally invariant operator subspace of N identical d-level systems. The package represents PI states and operators in Schur–Weyl blocks, supports local, collective, and symmetric p-body processes, and provides dynamics, stationary-state, spectral, information-theoretic, and visualization routines without constructing the full d^N Hilbert space in production algorithms.\n\nStart with PIBasis, PIModel, compile, and the high-level commands solve_dynamics, stationary_state, and liouvillian_spectrum.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics"},{"category":"section","location":"experiments/#Reproducible-experiments","page":"Reproducible verified experiments","text":"The experiment layer packages one calculation together with the evidence needed to interpret it:\n\nPIExperiment\n    │\n    ├── plan_experiment      resource estimate and selected exact route\n    │\n    └── verified_solve\n            ├── high-level solver result\n            ├── requested observables\n            ├── physical and numerical checks\n            └── environment and model provenance\n\nIt does not introduce another numerical backend. It coordinates stationary_state, solve_dynamics, recommend_solver, state_diagnostics, and the convergence study functions while preserving their precision, memory, and ownership contracts.","title":"Reproducible experiments"},{"category":"section","location":"experiments/#A-stationary-state-experiment","page":"Reproducible verified experiments","text":"using PermutationalInvariantDynamics\n\nN = 6\nbasis = PIBasis(N, 2)\nspin = spin_matrices(2)\n\nmodel = PIModel(basis, (\n    LocalHamiltonian(0.7 * spin.jx),\n    LocalJump(spin.jm; rate=0.12),\n    LocalJump(spin.jp; rate=0.02),\n))\n\nexperiment = PIExperiment(\n    model;\n    task=:steady_state,\n    algorithm=AutoAlgorithm(),\n    observables=(\n        polarization=collective_operator(basis, spin.jz) / N,\n    ),\n    memory_budget=512 * 1024^2,\n    metadata=(\n        project=\"driven ensemble scan\",\n        parameter_set=\"figure-2-point-17\",\n    ),\n)\n\nInspect the planned route before allocating solver storage:\n\nplan = explain_experiment(experiment)\n\nplan.backend\nplan.selected_algorithm\nplan.recommendation.reason\nplan.recommendation.known_peak_bytes\nplan.recommendation.budget_status\n\nThe planning call validates the specification and invokes the ordinary resource preflight. It does not compile or solve the model.\n\nFor a raw PIModel, construction snapshots every fixed built-in term operator, including an InPlaceTimeOperator prototype, before retaining the model. Later mutation of the caller's matrix therefore cannot change the experiment. Prepared immutable sources are shared. Custom extension terms and callable schedules retain their documented caller-owned behavior; because their captured state cannot be inspected generically, the provenance digest is marked incomplete.\n\nRun the calculation with its requested checks:\n\nresult = verified_solve(experiment)\n\nrho_ss = result.solution.state\nresult.observables[:polarization]\nresult.report\nresult.provenance\n\nFor a steady state, the report retains:\n\nthe solver residual, physical trace error, iteration status, and resource preflight;\ntrace, Hermiticity, and positivity diagnostics for the returned state;\nwhether a complete PI basis or an explicitly declared sector restriction was used;\npackage, Julia, platform, thread-count, and user metadata;\nthe ambient BigFloat precision and rounding mode, together with the per-value precision of any retained BigFloat data;\na deterministic structural checksum of supported model and experiment fields.\n\nNo failed physical state is normalized, symmetrized, or clipped.","title":"A stationary-state experiment"},{"category":"section","location":"experiments/#Exactness-is-explicit","page":"Reproducible verified experiments","text":"The default\n\nrepresentation = :complete_pi\n\nrequires every Schur sector in the PI basis. This is an exact representation of the declared PI model, up to the reported numerical solver tolerance.\n\nA deliberately restricted model must say so:\n\nrestricted = PIExperiment(\n    restricted_model;\n    representation=:declared_sectors,\n)\n\nIts execution report contains\n\nrestricted_plan.exactness.physical_approximation ==\n    :user_declared_sector_restriction\n\nThe experiment layer never infers that omitted sectors are harmless. Use the package's symmetry and population certification tools when a physical invariance statement is required.\n\nLikewise, algorithm=:auto may select an exact sparse or matrix-free route within the ordinary high-level policy. An explicit direct, GMRES, or exponential-action request is never replaced silently.","title":"Exactness is explicit"},{"category":"section","location":"experiments/#Dynamics-and-time-step-evidence","page":"Reproducible verified experiments","text":"rho0 = iid_pure_state(basis, ComplexF64[1, 0])\n\ndynamics_experiment = PIExperiment(\n    model;\n    task=:dynamics,\n    initial_state=rho0,\n    tspan=(0.0, 5.0),\n    saveat=0.0:0.1:5.0,\n    steps_per_interval=32,\n    observables=(\n        polarization=collective_operator(basis, spin.jz) / N,\n    ),\n)\n\ndynamics = verified_solve(dynamics_experiment)\n\nAll retained states are checked for trace, Hermiticity, and positivity. Without a refinement request, the report deliberately says\n\ndynamics.report.refinement_converged === missing\n\nbecause checking physicality at one resolution does not establish time-discretization convergence.\n\nRequest a study explicitly when final-state accuracy needs refinement evidence:\n\nrefinement = RefinementSpec(\n    :steps_per_interval,\n    (16, 32, 64, 128);\n    atol=1e-9,\n    rtol=1e-7,\n    consecutive=2,\n    require_convergence=true,\n)\n\nverified_dynamics = PIExperiment(\n    model;\n    task=:dynamics,\n    initial_state=rho0,\n    tspan=(0.0, 5.0),\n    saveat=0.0:0.1:5.0,\n    observables=(\n        polarization=collective_operator(basis, spin.jz) / N,\n    ),\n    verification=VerificationSpec(refinement=refinement),\n)\n\nresult = verified_solve(verified_dynamics)\nresult.report.refinement_converged\nresult.report.evidence.refinement.pairwise_errors\n\nEach listed number is the number of fixed RK4 steps between adjacent saved times. The comparison uses the retained final PI states. Consequently, time-step refinement currently requires save_states=true.\n\nBefore the first refinement solve, the experiment preflight includes the cumulative retained outputs from earlier levels plus the peak storage of the next level. It never starts a refinement sequence that is already known to exceed memory_budget.\n\nState-free observable dynamics remain available when physical-state validation is intentionally disabled:\n\nstreaming = PIExperiment(\n    model;\n    task=:dynamics,\n    initial_state=rho0,\n    tspan=(0.0, 5.0),\n    saveat=0.0:0.01:5.0,\n    observables=(polarization=collective_operator(basis, spin.jz) / N,),\n    save_states=false,\n    verification=VerificationSpec(require_physical=false),\n)\n\nThe report then marks physical_valid=missing; it does not infer physicality from the observable series.","title":"Dynamics and time-step evidence"},{"category":"section","location":"experiments/#GMRES-refinement","page":"Reproducible verified experiments","text":"Krylov-dimension refinement is available only when GMRES was explicitly requested:\n\ngmres_refinement = RefinementSpec(\n    :krylov_dimension,\n    (20, 30, 40);\n    atol=1e-9,\n    rtol=1e-7,\n    consecutive=1,\n)\n\nexperiment = PIExperiment(\n    model;\n    task=:steady_state,\n    algorithm=GMRESAlgorithm(maxiter=500),\n    verification=VerificationSpec(refinement=gmres_refinement),\n)\n\nThis restriction is intentional. A refinement request must not turn a direct or automatic request into a different solver behind the user's back.","title":"GMRES refinement"},{"category":"section","location":"experiments/#Saving-a-result","page":"Reproducible verified experiments","text":"save_experiment(\"run-017.pidrun\", result)\narchive = load_experiment(\n    \"run-017.pidrun\";\n    memory_budget=512 * 1024^2,\n)\n\nThe .pidrun path is a versioned directory containing:\n\none portable .pid checkpoint for each retained PI state;\nprecision-preserving binary time and observable arrays;\nverification, route, environment, checksum, and user metadata.\n\nExisting paths are never overwritten. The archive contains numerical results and provenance, not executable Julia code. In particular, loading it does not deserialize or execute model closures, workspaces, factorizations, or callback objects. Archive schema 2 also flattens solver, physical-state, and refinement evidence into inert string metadata; large state/resource payloads embedded in diagnostics are deliberately omitted. Schema-1 archives remain readable. Before allocating state or observable arrays, load_experiment validates declared lengths against each file payload and checks the combined retained state, basis, time, and observable estimate against memory_budget. It also requires consistent state bases, task/state/time cardinalities, ordered finite times, unique observable names, and scalar-versus-series metadata. The loader does not validate physical positivity automatically; apply validate_state when importing an untrusted numerical result.\n\narchive.states\narchive.times\narchive.observables\narchive.metadata[\"structural_digest\"]\narchive.metadata[\"verified\"]\n\nFor a callable rate or another opaque user object, provenance records its type but cannot inspect captured closure state. In that case result.provenance.digest_complete is false. Record a source revision, configuration-file checksum, or parameter-set identifier in metadata.","title":"Saving a result"},{"category":"section","location":"experiments/#Present-limitations","page":"Reproducible verified experiments","text":"The initial experiment layer intentionally covers deterministic PI steady-state and dynamics calculations that return PIState objects.\n\nIt does not yet coordinate:\n\ncomposite or shared-pseudomode state physicality;\nHEOM/HOPS hierarchy and pseudomode-cutoff refinement;\nstochastic trajectory confidence intervals;\nspectral target refinement;\nexecutable model serialization.\n\nThose workflows already have specialized plans and diagnostics. They should be integrated only with task-specific evidence rather than treated as though a deterministic PI-state residual certified them.","title":"Present limitations"},{"category":"section","location":"experiments/#API","page":"Reproducible verified experiments","text":"","title":"API"},{"category":"type","location":"experiments/#PermutationalInvariantDynamics.RefinementSpec","page":"Reproducible verified experiments","text":"RefinementSpec(parameter, levels; atol=0, rtol=nothing,\n               consecutive=2, require_convergence=true)\n\nImmutable numerical-refinement request for PIExperiment. parameter=:steps_per_interval is supported for deterministic dynamics and parameter=:krylov_dimension for matrix-free GMRES stationary states.\n\nA refinement request never changes the physical representation. In particular, it does not add or remove Schur sectors, change a pseudomode cutoff, or replace deterministic dynamics by trajectories.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.RefinementSpec"},{"category":"type","location":"experiments/#PermutationalInvariantDynamics.VerificationSpec","page":"Reproducible verified experiments","text":"VerificationSpec(; atol=1e-10, rtol=1e-8,\n                 positivity_method=:auto, dense_threshold=256,\n                 require_physical=true,\n                 require_solver_convergence=true,\n                 refinement=nothing)\n\nValidation policy applied by verified_solve. The checks report physicality and solver/refinement evidence without normalizing, symmetrizing, clipping, or otherwise repairing a state.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.VerificationSpec"},{"category":"type","location":"experiments/#PermutationalInvariantDynamics.PIExperiment","page":"Reproducible verified experiments","text":"PIExperiment(source; task=:steady_state, initial_state=nothing,\n             algorithm=AutoAlgorithm(), observables=nothing,\n             tspan=nothing, saveat=nothing, steps_per_interval=64,\n             parameters=nothing, save_states=true,\n             memory_budget=512*1024^2,\n             representation=:complete_pi,\n             verification=VerificationSpec(),\n             solver_options=NamedTuple(), metadata=NamedTuple())\n\nTyped immutable description of one reproducible PI calculation. Supported tasks are :steady_state and :dynamics.\n\nrepresentation=:complete_pi (the default) requires the complete PI Schur basis. A deliberately restricted basis must be declared explicitly with representation=:declared_sectors; the resulting report marks this as a user-declared representation restriction rather than silently calling it an exact full-PI calculation.\n\nsolver_options is reserved for stationary-state keyword arguments. The high-level memory guard remains active, and no explicit algorithm is replaced by another one. algorithm=:auto or AutoAlgorithm() delegates only to the package's exact high-level route selection.\n\nFixed operator matrices in built-in terms of a raw PIModel are copied into the experiment. Prepared immutable sources are retained directly. Callbacks and custom extension terms remain caller-owned and make the provenance digest incomplete unless their state is represented by supported immutable fields.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.PIExperiment"},{"category":"type","location":"experiments/#PermutationalInvariantDynamics.ExperimentExecutionPlan","page":"Reproducible verified experiments","text":"ExperimentExecutionPlan\n\nRead-only explanation of the route selected for a PIExperiment. recommendation is the ordinary recommend_solver resource report. Constructing a plan performs validation and resource estimation, but no model compilation or solve.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.ExperimentExecutionPlan"},{"category":"function","location":"experiments/#PermutationalInvariantDynamics.plan_experiment","page":"Reproducible verified experiments","text":"plan_experiment(experiment)\nexplain_experiment(experiment)\n\nReturn an immutable, explainable resource and route plan without solving the experiment. explain_experiment is an alias intended for interactive use.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.plan_experiment"},{"category":"function","location":"experiments/#PermutationalInvariantDynamics.explain_experiment","page":"Reproducible verified experiments","text":"Interactive alias for plan_experiment.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.explain_experiment"},{"category":"type","location":"experiments/#PermutationalInvariantDynamics.ExperimentProvenance","page":"Reproducible verified experiments","text":"ExperimentProvenance\n\nImmutable environment and model-identity record attached to every ExperimentResult. structural_digest is a deterministic non-cryptographic checksum over supported model/specification fields. If digest_complete=false, a callable or opaque user object was represented by its type only; use experiment metadata to record a source revision or parameterization identifier. The ambient BigFloat precision and rounding mode are recorded explicitly, and BigFloat values contribute their own stored precision to the digest.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.ExperimentProvenance"},{"category":"type","location":"experiments/#PermutationalInvariantDynamics.ExperimentReport","page":"Reproducible verified experiments","text":"ExperimentReport\n\nImmutable verification summary. verified=true means every check requested by the associated VerificationSpec passed. It does not turn a single-resolution dynamics calculation into a discretization-converged one: refinement_converged remains missing unless a refinement was requested.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.ExperimentReport"},{"category":"type","location":"experiments/#PermutationalInvariantDynamics.ExperimentResult","page":"Reproducible verified experiments","text":"Typed result of verified_solve.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.ExperimentResult"},{"category":"function","location":"experiments/#PermutationalInvariantDynamics.verified_solve","page":"Reproducible verified experiments","text":"verified_solve(experiment)\n\nPlan, run, and validate a deterministic :steady_state or :dynamics experiment. The result retains the high-level solver output, observables, the resource-selection plan, task-aware verification evidence, and provenance.\n\nSteady states are checked using solver residual/trace evidence and physical state diagnostics. Dynamics checks every retained state. A requested RefinementSpec(:steps_per_interval, ...) additionally compares successive final states. Without that explicit refinement, the report states refinement_converged=missing; it does not claim time-discretization convergence.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.verified_solve"},{"category":"constant","location":"experiments/#PermutationalInvariantDynamics.PI_EXPERIMENT_ARCHIVE_VERSION","page":"Reproducible verified experiments","text":"Current directory-archive schema written by save_experiment.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.PI_EXPERIMENT_ARCHIVE_VERSION"},{"category":"type","location":"experiments/#PermutationalInvariantDynamics.ExperimentArchive","page":"Reproducible verified experiments","text":"ExperimentArchive\n\nDetached, portable payload returned by load_experiment. It retains saved PI states, times, numeric observable series, and string provenance/report metadata. It intentionally does not deserialize executable models, closures, workspaces, or solver factorizations.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.ExperimentArchive"},{"category":"function","location":"experiments/#PermutationalInvariantDynamics.save_experiment","page":"Reproducible verified experiments","text":"save_experiment(path, result)\n\nWrite a dependency-free, versioned experiment archive directory. Existing paths are never overwritten. States use the portable .pid checkpoint format; time and observable arrays preserve their supported floating precision. The archive contains result/provenance evidence, not executable Julia closures or a serialized solver workspace.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.save_experiment"},{"category":"function","location":"experiments/#PermutationalInvariantDynamics.load_experiment","page":"Reproducible verified experiments","text":"load_experiment(path; memory_budget=512MiB)\n\nLoad and validate a save_experiment archive into an ExperimentArchive. No model or code is executed while loading. Loaded states remain unmodified; call validate_state or inspect the recorded verification metadata when consuming an untrusted archive.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.load_experiment"},{"category":"section","location":"#PermutationalInvariantDynamics.jl","page":"Home","text":"PermutationalInvariantDynamics.jl simulates finite ensembles of N identical d-level systems directly in their permutationally invariant (PI) operator space. It supports time-local open dynamics, local and collective dissipation, symmetric many-body processes, stationary states, spectra, entanglement and information measures, trajectories, Floquet dynamics, and mean-field predictions. Observable-only output, PI quantum regression, and factorized deterministic and density-valued stochastic dynamics of several PI ensembles are also available. Prepared continuation scans, confidence-controlled ensembles, generalized qudit Husimi data, advanced matrix-free Krylov families, explicit convergence reports, finite-exponential PI–HEOM, stochastic PI–HOPS for shared structured baths, and exact PI supersites for identical finite-cutoff local pseudomodes extend that workflow for larger research calculations. A separate factorized embedding covers one finite-cutoff pseudomode shared by the complete ensemble. Matrix-free full counting statistics, finite-exponential bath fitting, parameter inference, and verified experiment archives support reproducible model-to-data studies.\n\nThe exact PI representation scales polynomially with N at fixed d and does not construct the full d^N Hilbert space in production algorithms. General PI density operators may occupy all Schur sectors; they are not restricted to the fully symmetric subspace.","title":"PermutationalInvariantDynamics.jl"},{"category":"section","location":"#Installation","page":"Home","text":"Until registration, install the package directly from its GitHub repository:\n\nusing Pkg\nPkg.add(url=\"https://github.com/aenictusGitHub/PermutationalInvariantDynamics.jl\")\n\nFor a local checkout under development:\n\nusing Pkg\nPkg.develop(path=\"/path/to/PermutationalInvariantDynamics.jl\")\n\nTo run examples from a fresh repository checkout, instantiate its root environment once:\n\njulia --project=. -e 'using Pkg; Pkg.instantiate()'\n\nAfter the package is registered in Julia's General registry, installation is:\n\nusing Pkg\nPkg.add(\"PermutationalInvariantDynamics\")\n\nThe package supports Julia 1.10 and later.","title":"Installation"},{"category":"section","location":"#Start-here","page":"Home","text":"For a first calculation, follow Getting started: from a model to a solution. It explains, line by line, how to:\n\ndecide whether PI symmetry applies;\nchoose a basis and local-state convention;\ntranslate a master equation into physical terms;\nbuild and compile a reusable model;\ncompute dynamics or an autonomous stationary state;\nread result objects, validate states, and check numerical convergence.\n\nThe runnable companion is examples/getting_started.jl.\n\nTo start from formulas instead, the browser-only PI model code generator translates a documented LaTeX subset into commented, memory-guarded stationary, dynamics, spectrum, or gap programs. It keeps formulas in the browser and refuses ambiguous local-versus-collective jump semantics. Its architecture selector also covers identical local pseudomodes and one shared global pseudomode without exposing free-form tensor code. Compatible state and dynamics calculations can use deterministic prepared solvers or streaming quantum-trajectory statistics. For supported deterministic PI calculations it can instead emit a typed PIExperiment, show the resource/representation plan, verify the result, and download the Julia program with a normalized JSON manifest and README.","title":"Start here"},{"category":"section","location":"#Five-minute-preview","page":"Home","text":"This compact version evolves eight driven qubits with independent emission and pumping, records their excitation fraction, and computes the autonomous stationary state. The getting-started chapter explains every choice.\n\nusing PermutationalInvariantDynamics\n\nmodel = Models.driven_qubits(8)\nbasis = model.basis\nspin = spin_matrices()  # local order: (|g>, |e>)\nnumber = spin.jp * spin.jm\n\nrho0 = computational_product_state(basis, 2)\nprepared = compile(model; backend=:auto)\n\nsolution = solve_dynamics(\n    prepared, rho0, (0.0, 4.0);\n    saveat=0.1, steps_per_interval=16,\n    observables=(excited=number,),\n    save_states=false,\n)\n\nexcited_fraction = real.(solution.observables[:excited]) / basis.N\n\nsteady = stationary_state(prepared; return_info=true)\nreport = diagnostics(steady.state)\n\nCompile a model once and reuse prepared for dynamics, stationary states, spectra, and repeated analysis. steady.info carries the stationary residual and convergence metadata. Adaptive or stiff integration is available through dynamics_problem and an installed SciML solver package.","title":"Five-minute preview"},{"category":"section","location":"#Where-to-continue","page":"Home","text":"Getting started is the task-oriented model-to-solution tutorial and troubleshooting guide.\nPI model code generator creates a minimal stationary, observable-dynamics, selected-spectrum, or gap script from supported LaTeX ingredients. It includes the two finite-cutoff pseudomode embeddings, compatible trajectory routes, and optional physical-system purity, entropy, one-body-RDM, and collective-QFI analysis.\nFramework introduction derives the PI Schur-block representation, scaling, model terms, and validity conditions.\nArchitecture and efficient workflows explains sparse and matrix-free backends, preallocated workspaces, concurrency, and solver use.\nReusable prepared geometry covers immutable representation bundles and an explicit user-owned preparation cache.\nAPI tiers and prepared analysis separates recommended high-level commands from advanced research interfaces and documents the compact Workflow namespace.\nStreaming output, weak-PI pseudo-ket trajectories, quantum regression and spectra, higher-order cumulant closures, diffusive monitoring, research utilities and control, composite systems, global pseudomodes, and local pseudomode supersites describe memory-conscious research extension workflows.\nPrepared parameter scans, advanced Krylov families, optional accelerator preflight, and numerical convergence reports cover continuation and explicit numerical evidence; reproducible experiments combine planning, verification, refinement, and portable numerical archives.\nCounting statistics, bath-correlation fitting, and parameter inference cover rare-event, non-Markovian preparation, and model-to-data workflows.\nMatrix-RHS trajectory cohorts expose fixed-capacity batched kernels when an external scheduler has already grouped paths at the same time and step.\nPI–HEOM documents the deterministic common-bath finite-memory convention, while PI–HOPS propagates direct-sum Schur pseudo-ket hierarchies and reconstructs the density by Monte Carlo; global pseudomodes cover one explicitly retained shared mode; local pseudomodes cover identical independent finite-mode embeddings; and qudit Husimi phase space describes generalized coherent-state Q data.\nOptional ecosystem integrations records the precise QuantumOptics, QuantumToolbox, Tables, Makie, Distributed, QuantumCumulants, JLD2, and HDF5 boundaries.\nComplete public API index categorizes every exported function and type and links its full description.\nPublished validation and Research examples connect runnable scripts to the literature.","title":"Where to continue"},{"category":"section","location":"#Mathematical-source","page":"Home","text":"The framework follows Thierry Bastin and John Martin, J. Phys. A: Mathematical and Theoretical 58, 275301 (2025), doi:10.1088/1751-8121/addfc1.\n\nNegative time-dependent rates are accepted for deterministic time-local models. Such a generator need not define a completely positive evolution; quantum-jump trajectories require nonnegative stochastic rates.","title":"Mathematical source"},{"category":"section","location":"#License-and-development-disclosure","page":"Home","text":"The package is distributed under the GNU General Public License version 3 only (GPL-3.0-only). OpenAI Codex has been used extensively to assist with implementation, tests, documentation, performance work, and code audits. The maintainers remain responsible for reviewing, understanding, and validating every release candidate.","title":"License and development disclosure"},{"category":"section","location":"research_examples/#Research-examples","page":"Research examples","text":"","title":"Research examples"},{"category":"section","location":"research_examples/#Choose-an-example-by-goal","page":"Research examples","text":"If you are new to the package, begin with the fully explained model-to-solution tutorial and its getting_started.jl script. Then choose the closest workflow below.\n\nGoal Suggested runnable example What it adds\nFirst deterministic model getting_started.jl Basis, terms, compilation, dynamics, stationary state, diagnostics, and time-step refinement\nPrepared observables and a reduced state driven_qubits.jl Shared one-body geometry, a prepared observable, and a specialized one-body marginal workspace\nCompare stationary solvers steady_state_methods.jl Direct, shift-invert, SVD, and prepared-preconditioned matrix-free GMRES paths\nKeep observables but not states streaming_output.jl Memory-light deterministic and stochastic output\nSimulate jump records quantum_trajectories.jl Event-driven paths and analytical ensemble checks\nStudy periodic dynamics periodic_decay_channel.jl Reusable matrix-free period maps, selected multipliers, and a periodic state\nCompare density and Schur pseudo-ket paths weak_pi_trajectories.jl Fixed/event-driven weak-PI paths, confidence stopping, and stationary batch diagnostics\nCombine ensembles or an ancilla composite_ensembles.jl Factorized composite coordinates, cross-system maps, and fixed-capacity matrix-RHS actions\nCouple the ensemble to one shared cavity global_pseudomode_cavity.jl Collective Tavis–Cummings dynamics, factor reductions, and a mode-cutoff diagnostic\nAdd a finite-memory bath pi_heom.jl Exactly scaled ADOs, depth convergence, fixed-capacity matrix-RHS actions, SciML construction, and a block-preconditioned steady solve\nUnravel one shared structured bath pi_hops.jl Direct-sum Schur pure-state hierarchy, stationary colored noise, and an HEOM/analytic comparison\nApply ideal pulses without losing bath memory nonmarkovian_dynamical_decoupling.jl CPMG/UDD4 filter validation and finite-bath stroboscopic TEDD refinement through every HEOM ADO and HOPS auxiliary\nUse a non-Hermitian HOPS coupling pi_hops_collective_emission.jl Exact one-excitation collective emission, hard-depth comparison, prescribed noise, and conditioned hierarchy application\nStart HOPS from a mixed state pi_hops_mixed_multibath.jl Multiple shared baths, Schur spectral sampling, reusable batch workspaces, Monte Carlo errors, and pruning metadata\nEmbed one truncated pseudomode per spin all_to_all_xx_spin_local_pseudomodes.jl Uniform-all-pair PI supersites, trajectory estimates, spin-only negativity, a strong-parity-reduced x-GHZ steady solve, and cutoff checks\n\nEach script has a same-basename guide in the repository's examples/ directory. The sections below explain the specialized workflows and link to their numerical conventions.","title":"Choose an example by goal"},{"category":"section","location":"research_examples/#PI-research-utilities","page":"Research examples","text":"examples/research_utilities.jl demonstrates compressed spectral and population inspection, a Kraus channel with retained-algebra CP/TP checks, POVM sampling and tomography, versioned checkpoint round trips, and a joint weak-symmetry projector. The paired research-utilities guide records the scope and workspace conventions; none of these operations expands the state to a d^N Hilbert-space matrix.","title":"PI research utilities"},{"category":"section","location":"research_examples/#Diffusive-monitoring","page":"Research examples","text":"examples/homodyne_pi_trajectories.jl compares collective homodyne trajectories with unconditional PI evolution and checks ensemble convergence. The model already contains the monitored-channel dissipator; monitoring adds only the conditioned innovation. See Diffusive monitoring for homodyne/heterodyne conventions, task-owned workspaces, reproducible batch streams, and Euler–Maruyama convergence requirements.","title":"Diffusive monitoring"},{"category":"section","location":"research_examples/#Higher-order-cumulant-closures","page":"Research examples","text":"examples/cumulant_bridge.jl exports exact distinct-site PI moments and neutral microscopic term metadata, then compares a factorized closure with product and correlated states.  The core bridge has no symbolic-algebra dependency; an optional QuantumCumulants.jl 0.5 extension maps the exact values onto user-supplied symbolic averages.","title":"Higher-order cumulant closures"},{"category":"section","location":"research_examples/#Correlated-Kossakowski-reservoirs","page":"Research examples","text":"examples/correlated_reservoirs.jl compares a correlated local bath with its exact factorization into independent effective jumps, evolves the model matrix-free, and checks a preallocated collective Kossakowski schedule:\n\ngamma = mixing * mixing'\nterm = CorrelatedLocalJumps((sigma_minus, sigma_z), gamma; rate=0.08)\nprepared = compile(PIModel(basis, (term,)); backend=:matrixfree)\n\nThe fixed factorization is only in the small reservoir-channel space. Off-diagonal entries retain noise interference; no full d^N operator is built. The complete guide is examples/correlated_reservoirs.md.","title":"Correlated Kossakowski reservoirs"},{"category":"section","location":"research_examples/#Memory-light-observable-output","page":"Research examples","text":"examples/streaming_output.jl compares history-carrying evolution with observable-only deterministic output and state-free trajectory aggregation:\n\nseries = solve_dynamics(\n    prepared, rho0, (0.0, 2.0);\n    saveat=0.05,\n    observables=(excitation=number,),\n    save_states=false,\n)\n\nThe deterministic result retains one evolving PI vector. The trajectory path uses task-local Welford accumulators for means, unbiased variances, standard errors, and confidence intervals, while preserving trajectory-indexed random streams. Optional pooled waiting times still scale with the number of jumps; disable jump statistics when they are not needed. See Streaming and observable-only output.","title":"Memory-light observable output"},{"category":"section","location":"research_examples/#Quantum-regression-correlations-and-spectra","page":"Research examples","text":"examples/quantum_regression.jl validates the PI quantum-regression path for an incoherently pumped emitter. It compares a non-Hermitian first-order correlation, delayed antibunching, and the connected optical spectrum with closed forms:\n\nplan = CorrelationPlan(prepared, adjoint(c), c)\nwork = CorrelationWorkspace(plan; krylovdim=20)\nC = two_time_correlation(plan, rho_ss, delays; workspace=work)\nS = stationary_correlation_spectrum(\n    plan, rho_ss, frequencies; workspace=work)\n\nThe same matrix-free Liouvillian drives sequential RK4 samples and shifted GMRES resolvents. The first readout operator is not implicitly adjointed, and the stationary spectrum is the connected one-sided complex transform. See Quantum regression and optical correlations.\n\nFor general response calculations, ResponseWorkspace reuses the dominant shifted-GMRES and exponential-action arrays across resolvent_norm, pseudospectral_abscissa, adjoint_evolve, integrated_correlation_time, and steady_state_susceptibility. The matrix-free resolvent is a converged power/GMRES estimate rather than a rigorous upper bound; every shifted solve must converge. Trace-fixed Poisson and tangent solves report their physical residual and trace error. See Matrix-free Krylov solvers.","title":"Quantum-regression correlations and spectra"},{"category":"section","location":"research_examples/#Several-PI-ensembles-and-finite-auxiliaries","page":"Research examples","text":"examples/composite_ensembles.jl combines two independent compressed PI ensembles with a finite two-level auxiliary factor. Local compiled Liouvillians and tensor-product Hamiltonian/dissipator terms are applied as a sum of factor maps:\n\nbasis = CompositePIBasis(ensemble_a, ensemble_b,\n                         FiniteOperatorBasis(2))\ngenerator = CompositeSuperoperator(basis, local_a, local_b) + interaction\nwork = CompositeSuperoperatorWorkspace(generator, rho.data)\napply!(destination, generator, rho.data, 0.0, nothing, work)\n\nNo global Kronecker superoperator or full Hilbert representation of either PI ensemble is formed. Finite auxiliaries are explicitly truncated. examples/composite_quantum_trajectories.jl additionally compares density-valued cross-factor jump paths with the independently propagated unconditional generator and demonstrates state-free statistics plus trajectory-index reproducibility. See Composite PI systems.","title":"Several PI ensembles and finite auxiliaries"},{"category":"section","location":"research_examples/#One-shared-cavity","page":"Research examples","text":"examples/global_pseudomode_cavity.jl specializes the composite backend to one finite cavity shared by a PI emitter ensemble. It evolves a damped Tavis–Cummings model, extracts atomic and photon observables, traces either factor directly, and monitors the highest cavity level without constructing a global Kronecker superoperator. The mode is one distinguished global factor, not one replicated auxiliary per emitter. See Global pseudomodes and shared cavities.","title":"One shared cavity"},{"category":"section","location":"research_examples/#Sector-resolved-spin-phase-space","page":"Research examples","text":"examples/spin_phase_space.jl constructs a state occupying two total-spin sectors and computes both its positive Husimi-Q density and signed spin-Wigner quasidistribution. Each sector sphere integrates to its physical Schur population, while the displayed aggregate is their angular marginal:\n\nq = spin_husimi_q(rho; ntheta=81, nphi=160, resolved=true)\nw = spin_wigner(rho; ntheta=81, nphi=160, resolved=true)\n\ndisplay(visualize_spin_phase_space(q))\ndisplay(visualize_spin_phase_space(\n    w; sector=first(w.sectors)))\n\nThe script verifies multiplicity weighting, normalization, the coherent-state peak, and Wigner negativity without constructing a full Hilbert-space state. See Sector-resolved spin phase space for the precise normalization and multipole convention.\n\nexamples/qudit_coherent_state_q_distribution.jl extends the coherent-state Q check to qutrit Schur sectors. It reuses one QuditHusimiPlan, verifies normalized-Haar population weighting and the maximally mixed density, and checks the d=2 factor against the spin-sphere convention. See Qudit Husimi phase space; this is not a generalized qudit Wigner transform.","title":"Sector-resolved spin phase space"},{"category":"section","location":"research_examples/#Prepared-continuation-and-non-Markovian-hierarchy-examples","page":"Research examples","text":"examples/parameter_scan.jl follows an exactly soluble emission/pumping steady-state curve while retaining no state history. It compiles the fixed geometry as one scalar-rate family, reuses a bounded GCRO recycle space, stops, stores one ownership-safe continuation seed, resumes in a fresh workspace, and exports compact columns. A setup-only sensitivity_problem check exercises the same prepared matrix-RHS protocol. Threaded and optional Distributed execution apply only after continuation is disabled. See Prepared parameter scans.\n\nexamples/pi_heom.jl stores every ADO of a collectively dephased qubit ensemble in exactly similarity-scaled PI coordinates and compares three hierarchy depths with an exact exponential-bath coherence. It separately demonstrates that convergence of one observable is weaker than convergence of the complete reduced state. A one-step heom_problem check exposes the same matrix-free right-hand side to SciML, while a small unique stationary model demonstrates a reusable ADO-diagonal HEOMBlockPreconditioner and validates fixed-capacity forward/adjoint matrix-RHS applications. Scaling changes conditioning and stored auxiliary coordinates, not the root reduced state or the hierarchy truncation. See PI–HEOM and Numerical convergence reports.\n\nexamples/pi_hops.jl propagates the corresponding hierarchy of pure states in direct-sum Schur-irrep coordinates, averages unnormalized root outer products, and compares the result with both PI–HEOM and the analytic collective-dephasing coherence. Its shared bath is PI on every noise realization. Independent local colored noises are not replaced by a common noise and remain an HEOM or local-pseudomode problem. See PI–HOPS.\n\nexamples/nonmarkovian_dynamical_decoupling.jl applies ideal CPMG and UDD4 pi_x pulses to the same finite-memory pure-dephasing problem. A PIUnitaryPulse is lifted from one local matrix, and HierarchyPulseSequence makes the event convention explicit. Each event acts on every HEOM ADO or HOPS auxiliary, so neither the hierarchy nor the colored noise is restarted at a pulse. The script follows Colin Read's September 2023 report Studying pure dephasing and dynamical decoupling: comparison of the HOPS method with exact analytical solutions and targets its Figs. 8–9 qualitatively.\n\nThe same executable also constructs the 24-edge tetrahedral Eulerian dynamical-decoupling word of Read, Serrano-Ensástiga, and Martin, Platonic dynamical decoupling sequences for interacting spin systems, Quantum 9, 1661 (2025). It verifies projective closure and the zeroth-order group average of Q=sigma_z, then evolves repeated TEDD cycles against the nonzero bath with both hierarchy backends. Only closed-cycle fidelities are interpreted. An eight-cycle HEOM calculation halves the edge interval at fixed final time and checks the rapid-control refinement without reusing the scalar CPMG/UDD filter formula.\n\nThe comparison deliberately uses the exact one-pole correlation C(t)=cexp-(kappa-iomega_c)t. For the report's Lorentzian, that exponential follows from extending the frequency integral from the positive half-line to the whole real line. PI–HOPS and PI–HEOM therefore converge to the full-line filter reference, not to the distinct physical positive-frequency Lorentzian model. The latter is nevertheless plotted as an analytical comparison using two converged half-line quadrature orders; it is not attributed to either hierarchy. The executable uses ideal instantaneous pulses because the supplied report does not specify the Gaussian pulse width or bare qubit frequency needed to reproduce its finite-pulse HOPS calculation. Its same construction remains PI for N1 under one collective bath coupling Q=2J_z and the global pulse U_x^otimes N; this common-bath extension is not a model of independent local colored noises.\n\nTwo additional scripts expose the specialized HOPS interfaces. examples/pi_hops_collective_emission.jl uses the non-Hermitian coupling L=J_- and a symmetric one-excitation state, for which depth one closes exactly and the Lorentzian survival amplitude is analytical. It also demonstrates a prescribed conditioned noise and deterministic hops_rhs! application. examples/pi_hops_mixed_multibath.jl prepares a general mixed PI state as a Schur spectral ensemble, combines two independent shared dephasing baths, reuses HOPSBatchWorkspace, reports state-level Monte Carlo errors, and inspects a setup-only importance-pruned hierarchy. Its pruning score is explicitly not treated as an accuracy certificate.\n\nThese hierarchy examples still use a finite exponential decomposition and a hard hierarchy boundary. Bath-decomposition error, depth error, integration error, and (for HOPS) sampling error are separate refinements. The HOPS backend supports fixed shared PI bath couplings, including non-Hermitian couplings; it does not infer residue corrections, terminators, or the specialized hierarchy for independent local non-Markovian baths.","title":"Prepared continuation and non-Markovian hierarchy examples"},{"category":"section","location":"research_examples/#Uniform-all-pair-Ising-spins-with-local-pseudomodes","page":"Research examples","text":"examples/all_to_all_xx_spin_local_pseudomodes.jl demonstrates a different finite-memory strategy: each spin and its truncated local pseudomode are combined into one supersite of dimension d=2(nmax+1). The all-to-all interaction has the same coefficient for every unordered spin pair, and every supersite has the same spin–mode coupling and mode-loss channel. The enlarged model is therefore exactly PI and can use the ordinary prepared Liouvillian workflow. At fixed cutoff the enlarged spin-plus-pseudomode state follows a Markovian Lindblad equation, while tracing the modes produces the intended finite-memory, generally non-Markovian spin dynamics:\n\ninclude(\"examples/paper_models.jl\")\nusing .PaperModels\n\noperators = local_pseudomode_operators(nmax)\nbasis = PIBasis(N, operators.dsite)\nmodel = all_to_all_xx_spin_local_pseudomode_model(\n    basis, operators;\n    Jpair=J / (N - 1), omega_c, gamma, kappa,\n    coupling=:minus,\n)\nprepared = compile(model; backend=:matrixfree)\n\nThat snippet is the basic prepared workflow, not the integrator used for the long manuscript-style panel. The executable propagates each omega_c*t=0:0.5:100 curve interval by interval with a matrix-free generator, one reusable dimension-30 KrylovExpvWorkspace, and preallocated krylov_expv!. This costs 6000 Liouvillian applications per curve and avoids retaining a state history. A separate dimension-40 run with tighter tolerances checks the broad kappa/omega_c=20 curve; the current pointwise Cxx difference is about 8e-16. This replaces a fixed-step RK4 refinement, which was not stable enough in that broad-decay regime.\n\nAt one stationary reference point the script calls both trajectory_steady_state and weak_pi_trajectory_steady_state. The first uses density-valued conditional paths; the second resolves the local gain into one-box Schur Kraus branches and evolves direct-sum pseudo-kets. Each route streams a post-settling density average within every independent path before combining path means, and neither retains histories or jump logs. Comparisons with the direct stationary solution include the full PI-coordinate error, Hilbert–Schmidt Monte Carlo standard error, Liouvillian residual, Cxx, and spin-only negativity. The colored stars on the stationary maps show the weak-PI estimate. Burn-in time, within-path spacing, the selected fixed-step or event-driven integration controls, path count, and pseudomode cutoff remain separate convergence requirements.\n\nThe helper treats Jpair as the literal coefficient of every unordered pair; the displayed J uses the explicit Kac choice Jpair=J/(N-1). It also maps the manuscript convention D_paper=2D_package to a local mode-jump rate 2kappa and sets the exchange strength to sqrt(gamma*kappa). Both coupling=:minus and coupling=:z are available. The latter retains a strong spin-parity symmetry, so an unrestricted steady state need not be unique. For that branch the script restricts both Schur-block indices to the initial even-parity Hilbert support. This is a 204-coordinate stationary solve instead of the full 816-coordinate default problem; an ordinary weak-conjugation projector would retain both diagonal parity blocks and would not resolve the stationary degeneracy. Every point checks strong-block leakage, parity, state diagnostics, and the full norm(L*rho) residual. One adaptive matrix-free krylov_expv propagation to omega_c*t=1600 plus a half-time value remain as independent reference checks rather than being repeated across the grid.\n\nThe longitudinal model also has the PI-compatible weak symmetry generated by sigma_xotimes(-1)^a^dagger a on every supersite. The example certifies it explicitly, but does not restrict the selected state to one of its charges: for odd N that transformation exchanges the two strong spin-parity sectors and a trivial-charge projection would alter the GHZ coherence.\n\nThe supplied manuscript studies a nearest-neighbour periodic chain. That geometry is not invariant under arbitrary permutations, so this runnable model is deliberately the uniform-all-pair specialization, not a reproduction of the spatial chain. All distinct pairs share one Cxx; there is no distance coordinate or spatial correlation length. The finite-size stationary heat maps must likewise not be advertised as a thermodynamic phase diagram.\n\nFor the repeated stationary maps, the example prepares one LocalFactorTracePlan(basis,(2,levels); traced_factor=2). It traces every pseudomode directly in PI coordinates, then uses ReductionPlan first to keep two spins and again to evaluate their 1|1 negativity. A 16-Pauli-moment reconstruction remains as an independent reference-point oracle. The plotted quantity therefore measures two physical spins after tracing both pseudomodes, rather than two spin–mode supersites. For longitudinal coupling the example still uses full N-particle moments with the mode identity to evaluate the relative-phase-optimized spin x-GHZ fidelity\n\nF_mathrmGHZ_x^max\n=fracP_+x+P_-x2\n +leftlangle +x^otimes Nrho_mathrmspin\n                    -xrangle^otimes Nright\n\nThe plotted 0.5 contour is the usual genuine-multipartite-entanglement witness threshold. No full spin or spin–mode Hilbert matrix is formed.\n\nThe script also compares nmax=1 and 2 and checks the wider calculation's highest-level population for the coupling=:minus correlation curve. That short cutoff comparison still uses fixed-step solve_dynamics; it does not by itself certify either the long-sweep integration or the cutoff of the separate longitudinal GHZ map. The retained PI dimension is\n\nn_mathrmPI=binomN+2(n_max+1)^2-1N\n\nso cutoff growth must be estimated and convergence repeated for each claimed observable. The optional Makie output contains a manuscript-style dynamics panel, stationary correlation and spin-negativity maps, and a separate cutoff figure. A third, manuscript-Fig.-4-style figure shows the parity-selected x-GHZ witness, its 0.5 contour, and the maximum long-time residual. The extracted Cxx=0 and GHZ-witness boundaries are also written, independently of Makie, as run-qualified tab-delimited text files beside the figures. Each file retains the raw interpolated contour, the origin-constrained quadratic candidate, and any available fallback candidate together with fit metadata. Set PI_EXAMPLE_FIGURE_DIR to redirect both numerical and graphical output. The complete example guide records the model boundary, rate conversion, solver checks, and figure stems.","title":"Uniform all-pair Ising spins with local pseudomodes"},{"category":"section","location":"research_examples/#Visualizing-Liouvillian-and-Floquet-spectra","page":"Research examples","text":"examples/spectral_visualization.jl computes a complete N=3 PI Liouvillian spectrum and a converged one-period map for a periodically driven, decaying qubit ensemble. The numerical results are retained independently of their SVG presentation:\n\ndata = liouvillian_spectrum_data(\n    prepared; nev=pi_dimension(prepared), algorithm=:dense)\ndisplay(visualize_spectrum(data))\n\nF = floquet_propagator(periodic, period; steps=256)\nmultipliers = floquet_spectrum_data(F; period=period)\ndisplay(visualize_spectrum(multipliers))\n\nThe script checks the continuous-time stability half-plane, the Floquet fixed multiplier and unit disk, RK4 step doubling, and the principal-log exponent strip. It diagonalizes the converged map once, reuses its multiplier vector for multiplier and exponent diagrams, and writes generated SVGs only inside a temporary directory. See Spectral visualization for the complete/partial-spectrum and branch conventions.","title":"Visualizing Liouvillian and Floquet spectra"},{"category":"section","location":"research_examples/#Inspecting-Schur-block-structure","page":"Research examples","text":"examples/irrep_block_visualization.jl solves the full-rank N=4 local pump–decay steady state and compares it with its exact iid thermal form. It then contrasts collective- and local-decay Liouvillians. Reusable state-sector data are obtained with schur_block_structure and rendered separately:\n\nstructure = schur_block_structure(prepared;\n    metric=:frobenius,\n    threshold=1e-12)\nfigure = visualize_schur_blocks(structure;\n    scale=:log,\n    title=\"Local decay: Schur-sector coupling\",\n    show_young_diagrams=true)\nsave_schur_block_visualization(\"local_decay.svg\", figure)\n\nThe Young-diagram thumbnails identify the input and output partitions. Their tooltips retain the exact standard-tableau multiplicity f^nu; they do not choose a particular tableau filling.\n\nFor the solved state, metric=:population displays physical sector trace weights. The same state is diagonalized once, block by block, and its exact Schur multiplicities remain compressed in the density-spectrum view:\n\ndensity = pi_density_spectrum(rho_ss)\ndensity_figure = visualize_density_spectrum(\n    density; show_degeneracies=true)\nsave_density_spectrum_visualization(\"density.svg\", density_figure)\n\nThe density plot contains one point per irrep eigenvector, coloured by sector, with exact degeneracies in labels and tooltips. It does not create the exponentially large expanded=true eigenvalue list.\n\nState and operator structures are diagonal in the Schur labels. Their default physical representation measures C_nu/sqrt(f^nu); pass representation=:coefficient to inspect stored equation-(7) blocks instead. For a superoperator, rows are output sectors and columns are input sectors in orthonormal PI coefficient coordinates. Consequently, an off-diagonal tile shows a map between Schur sectors, not a state coherence or a transition probability.\n\nSparse and dense superoperators are scanned directly. A matrix-free source is probed exactly once for each of its n_PI=length(basis) input coordinates, with reusable scratch and without retaining a global Liouvillian. This setup cost should normally be paid once and the resulting SchurBlockStructure reused. A driven source requires time=...; general operator-valued fallback terms are frozen and lowered once at that time before all coordinate probes.\n\nThe returned figures display directly in SVG-capable notebooks and can be written with save_schur_block_visualization and save_density_spectrum_visualization. Both renderers are dependency-free; no plotting package or full d^N state is constructed. See Schur-block visualization for the available metrics, normalizations, extraction thresholds, and rendering options, and Spectral visualization for the compressed density spectrum convention.","title":"Inspecting Schur-block structure"},{"category":"section","location":"research_examples/#Certified-Schur-diagonal-population-dynamics","page":"Research examples","text":"examples/qubit_population_dynamics.jl constructs all six local and collective qubit bath channels with qubit_ensemble_model. A diagonal spin Hamiltonian and central Dicke initial state make the Schur-basis diagonal subspace invariant. The script constructs the reduced plan once and inspects its strict invariance certificate:\n\nplan = PopulationPlan(model)\nreport = plan.invariance\np0 = diagonal_populations(rho0)\nsolution = solve_populations(plan, p0, (0.0, 2.0); saveat=0.25)\n\nFor N=6, only 16 physical GT-pattern populations are evolved instead of 84 general PI coordinates. The example propagates the complete PI density state on the same grid and asserts agreement of every saved population vector. It then calls stationary_populations, checks the reduced-generator residual and normalization, reconstructs a validated PIState, and compares it with a direct full-PI stationary solve. The paired guide examples/qubit_population_dynamics.md gives the six-rate and multiplicity conventions.","title":"Certified Schur-diagonal population dynamics"},{"category":"section","location":"research_examples/#Quantum-trajectories-against-analytical-and-master-equation-references","page":"Research examples","text":"examples/quantum_trajectories.jl tests event-driven PI trajectories for independent spontaneous emission. In addition to deterministic density-matrix evolution, it uses the exact tensor-power state, binomial excitation/photon counts, and no-jump probability as analytical Mølmer-method references. Every stochastic assertion is scaled by the corresponding Monte Carlo standard error.\n\nexamples/superradiant_quantum_trajectories.jl combines collective cavity decay and individual free-space decay at the two rate ratios used in Fig. 2 of Zhang, Zhang, and Mølmer, New J. Phys. 20, 112001 (2018):\n\ninclude(\"examples/paper_models.jl\")\nusing .PaperModels\n\nmodel = collective_local_decay_model(\n    N; GammaC=1.0, gammaL=10.0)\nbasis = model.basis\nrho0 = iid_pure_state(basis, ComplexF64[0, 1])\nradiation = collective_local_radiation_operators(\n    basis; GammaC=1.0, gammaL=10.0)\ntimes = collect(range(0.0, 0.6; length=31))\ntrajectories = quantum_trajectories(\n    model, rho0, times, 256;\n    algorithm=:event, dt=0.02, dtmax=0.05, seed=2019)\nstatistics = trajectory_statistics(\n    trajectories;\n    observables=(cavity=radiation.cavity,\n                 free_space=radiation.free_space),\n    nchannels=2)\n\nThe two radiated intensities are checked pointwise against a certified population-space master equation. The example guide explains why the particle-unresolved local gain map has the same ensemble dynamics but a different conditional record from the article's sector-shift-resolved pure pseudo-state unraveling.\n\nexamples/weak_pi_trajectories.jl then exercises the separate opt-in WeakPITrajectoryPlan. It factorizes that same local PI gain into complete one-box subduction Kraus branches, propagates direct-sum Schur-irrep pseudo-kets, and compares their ensemble with density-valued paths, certified population dynamics, and general matrix-free PI evolution for gamma_lGamma_c=1. Equal trajectory counts and fixed-step controls make the warmed per-path timing descriptive of that run, but not a portable speed certificate. The same prepared Schur-Kraus plan is also exercised with adaptive Dormand–Prince propagation and continuous hazard roots. An observable-only adaptive ensemble stops only at deterministic batch boundaries when its simultaneous empirical-Bernstein targets pass or its trajectory cap is reached.\n\nThe example also forms a history-free stationary density estimate from event-driven pseudo-kets. It takes sector-block outer products before every average, averages correlated samples within a path, and uses independently seeded path means for its primary Monte Carlo standard error. Optional batch means diagnose within-path autocorrelation under an explicitly approximate independence assumption; they do not certify burn-in or finite-window bias. Integrator controls, settling time, sampling window, and independent path count therefore remain separate convergence checks.\n\nThe pseudo-kets are not labeled-particle wavefunctions, and their particular Kraus record must not be identified path-by-path with another unraveling. Composite cavity pseudo-ket trajectories remain outside this single-ensemble backend.\n\nWith the examples-only CairoMakie environment active, both trajectory scripts also render their numerical comparisons. The weak-PI example uses 95% confidence bands and adds logarithmic state error, sampled sector-transition, exact coordinate-scaling, and current-run timing panels. The separate Zhang–Mølmer density-path figure places the two decay-rate ratios side by side. PDF and PNG outputs are written without adding Makie to the package dependency graph.","title":"Quantum trajectories against analytical and master-equation references"},{"category":"section","location":"research_examples/#Publication-figures-for-the-literature-examples","page":"Research examples","text":"All standalone paper-specific scripts use the optional loader in examples/utils/makie_support.jl. Their numerical assertions, convergence checks, and printed diagnostics run unchanged in the core environment; with --project=examples, each script additionally displays a CairoMakie figure and writes PDF and PNG copies under examples/figures/ (or the directory selected by PI_EXAMPLE_FIGURE_DIR).\n\nThe panels are organized by the quantity actually validated in each script:\n\nHuelga and Kitagawa–Ueda overlay exact metrology curves with PI dynamics; the latter also shows the one-spin purity generated by twisting.\nShammah, Morrison–Parkins, and Meiser–Holland visualize steady-state sectors, exact-state observables, radiated intensity, and cooperative enhancement.\nIemini, Piccitto, Nakanishi–Sasamoto, and Gambetta show finite-size slow modes, gaps or Floquet multipliers together with oscillatory dynamics. Their annotations explicitly distinguish finite-size precursors from a thermodynamic time-crystal claim.\nThe finite/thermodynamic mean-field comparison overlays both closures with exact PI evolution and its analytical finite-N curve.\n\nThe Damanet, Pausch, Mølmer, and Zhang–Mølmer figures use the same convention for the previously implemented pulse, mean-field, and trajectory comparisons. Each same-basename guide describes the precise panels and saved-file stem.","title":"Publication figures for the literature examples"},{"category":"section","location":"research_examples/#Appendix-D-pair-processes","page":"Research examples","text":"examples/pbody_pair_processes.jl combines a two-particle Ising interaction, independent pair loss, and a collective pair-loss channel:\n\npair_loss = kron(sm, sm)\npair_interaction = kron(sz, sz)\nmodel = PIModel(basis, [\n    PBodyHamiltonian(pair_interaction, 2; rate=0.05),\n    LocalPBodyJump(pair_loss, 2; rate=0.02),\n    CollectivePBodyJump(pair_loss, 2; rate=0.001),\n])\n\nThe script verifies the pair-sum identity, trace preservation, and equality of sparse and matrix-free application. It deliberately exposes the advanced workspace API for that backend check:\n\nprepared = compile(model; backend=:matrixfree)\nworkspace = LiouvillianWorkspace(prepared)\nLs = liouvillian(prepared; representation=:sparse)\napply!(y, prepared, rho0.data, 0.0, nothing, workspace)\n\nThe explicit workspace makes repeated Appendix-D applications preallocated; ordinary evolution can pass prepared directly to solve_dynamics.","title":"Appendix-D pair processes"},{"category":"section","location":"research_examples/#Comparing-steady-state-solvers","page":"Research examples","text":"examples/steady_state_methods.jl compares a trace-bordered solve, SVD, dense diagonalization, sparse shift-invert, and restarted matrix-free GMRES against an exact product thermal state.\n\nprepared = compile(model; backend=:sparse)\nresult = stationary_state(prepared;\n    algorithm=ShiftInvertAlgorithm(shift=-1e-3, maxiter=100),\n    atol=1e-12,\n    rtol=1e-10,\n    return_info=true,\n)\nrho_ss = result.state\n\nFor parameter scans, the previous stationary state can be passed through initial_state. The example reports residuals, trace errors, iteration counts, reference-state errors, and indicative wall times. Timings include compilation on a first Julia run and should not be interpreted as benchmarks.\n\nFor a solve that never assembles the Liouvillian, reuse both Krylov storage and the Schur preconditioner across a scan:\n\nprepared_mf = compile(model; backend=:matrixfree)\nworkspace = KrylovWorkspace(prepared_mf, 30)\nP = schur_sector_preconditioner(prepared_mf, model.basis;\n                                expected_reuses=10)\nresult = stationary_state(prepared_mf;\n    algorithm=GMRESAlgorithm(krylovdim=30, maxiter=500,\n                             preconditioner=P),\n    workspace=workspace,\n    return_info=true)\npreconditioner_cost(P)\n\nSelected slow modes and the gap use the same matrix-free action:\n\nmodes = pi_liouvillian_spectrum(model;\n    method=:arnoldi, nev=6, krylovdim=40, vectors=true)\ngap = pi_liouvillian_gap(model;\n    method=:arnoldi, nev=6, krylovdim=40, return_info=true)\n\nIncrease krylovdim until every requested Ritz residual is converged. A partial spectrum cannot certify a stationary multiplicity larger than the requested nev; inspect stationary_multiplicity_certified in gap reports.","title":"Comparing steady-state solvers"},{"category":"section","location":"research_examples/#Periodic-decay-and-Floquet-analysis","page":"Research examples","text":"examples/periodic_decay_channel.jl uses a sinusoidally modulated local decay rate. It performs an RK4 convergence study against the exact commuting one-period map and computes multipliers, the Floquet gap, the periodic steady state, and stroboscopic populations.\n\nrate = (t,p) -> gamma*(1 + a*cos(2pi*t/period))\nmodel = PIModel(basis, [LocalJump(sm; rate=rate)])\nprepared = compile(model; backend=:matrixfree)\nF = floquet_map(prepared, period; steps=160)\nselected = selected_floquet_multipliers(\n    F; method=:arnoldi, which=:LM, nev=4,\n    krylovdim=length(basis))\ngap = floquet_gap(\n    F; method=:arnoldi, nev=4,\n    krylovdim=length(basis), return_info=true)\nperiodic = floquet_steady_state(\n    F; method=:krylov, krylovdim=20, return_info=true)\nstates = stroboscopic_evolution(rho0, F, 4)\n\nEvery period action integrates only the supplied PI vector. Arnoldi/IRAM and the trace-fixed GMRES solve therefore reuse the period map without constructing a dense PI-dimensional channel. The script checks selected Ritz residuals, the periodic-state residual, and a certified matrix-free parity restriction. A small dense floquet_propagator remains only as an independent reference for the complete multiplier set.\n\nfloquet_gap(...; return_info=true) distinguishes a residual-resolved selected decay estimate from a certified global gap. The latter requires a complete multiplier set; a partial Arnoldi/IRAM window must retain global_gap_certified=false. Jacobi–Davidson can target a particular multiplier but is not a global spectral-radius method.\n\nIncrease steps until the relevant map action, multiplier, or observable has converged, independently of Krylov tolerances. Stiff protocols may require a dedicated integration strategy rather than the fixed-step Floquet helper.\n\nexamples/dissipative_discrete_time_crystal.jl applies the same piecewise workflow to the fully connected dissipative Rydberg protocol of Gambetta et al., Phys. Rev. Lett. 122, 015701 (2019). The short pulse and long relaxation segment are integrated separately and composed in their physical order. At N=4, the script checks the negative period-doubling multiplier against an independent full-Hilbert-space calculation and performs a step-doubling convergence check. This is explicitly a finite-size precursor; the paper's time-crystalline conclusion comes from lifetime scaling at larger N.","title":"Periodic decay and Floquet analysis"},{"category":"section","location":"research_examples/#Further-literature-models","page":"Research examples","text":"examples/cooperative_fluorescence.jl implements Eq. (1) of Morrison and Parkins, Phys. Rev. A 77, 043810 (2008), and compares every finite-size stationary state from stationary_state with their exact Eq. (2). The script explicitly converts the package's |g>,|e> ordering to the spin axes used in the paper and shares one OneBodyGeometry between two prepared collective observables.\n\nexamples/steady_superradiance.jl implements Eq. (1) of Meiser and Holland, Phys. Rev. A 81, 033847 (2010). It compiles each parameter point, uses a typed direct stationary-state solve, reports the collective intensity of Eq. (2), and compares its maximum with the large-N prediction N^2Gamma_c8 from Eq. (10).\n\nexamples/boundary_time_crystal.jl implements Eq. (2) of Iemini et al., Phys. Rev. Lett. 121, 035301 (2018). It uses the high-level liouvillian_spectrum ordering and contrasts the gapped omega_0kappa1 regime with the time-crystalline omega_0kappa1 regime, where finite-size oscillatory modes approach the imaginary axis as N increases.\n\nexamples/interacting_boundary_time_crystal.jl implements the nonlinear p=2,q=1 collective-spin model of Piccitto et al., Phys. Rev. B 104, 014307 (2021). Its Appendix-D pair Hamiltonian is algebraically identical to the paper's normalized Jz^2 term up to a scalar identity. Complete small symmetric-sector spectra resolve the complex branch whose decay decreases with size; the script does not infer the paper's approximate N^-0.4 law from only three small systems.\n\nexamples/pt_symmetric_time_crystal.jl implements the balanced-gain/loss model of Nakanishi and Sasamoto, Phys. Rev. A 107, L010201 (2023). It matches every finite-size eigenvalue to their exact Eq. (14), verifies the gap 4kappa/N and uniform irrep steady state, then checks the exact damped magnetization with matrix-free evolution at a larger N.\n\nexamples/meanfield_time_crystal.jl lowers the same physical terms through MeanFieldPlan and compares the finite product closure, its thermodynamic limit, and exact matrix-free PI dynamics. The finite closure retains the one-site dissipator and reproduces the damped magnetization in this balanced model; the thermodynamic rule removes that subleading term and yields the undamped oscillation. See Mean-field predictions for the general closure equations and supported term set.","title":"Further literature models"},{"category":"section","location":"counting_statistics/#Full-counting-statistics-and-dynamical-large-deviations","page":"Counting statistics and rare events","text":"The counting-statistics backend resolves selected physical jump channels of an autonomous PI model without constructing a full-Hilbert-space state or a dense PI Liouvillian.","title":"Full counting statistics and dynamical large deviations"},{"category":"section","location":"counting_statistics/#Convention","page":"Counting statistics and rare events","text":"For channel counts K_a(t) and signed increments q_a, the moment-generating function is\n\nZ(st)=mathbb Eleft\nexpleft(ssum_a q_a K_a(t)right)right\n\nOnly the jump gain is tilted:\n\nmathcal L_s=mathcal L+\nsum_aleft(e^s q_a-1right)mathcal J_a\n\nThe anticommutator loss remains unchanged. Consequently, Z(st)=mathrmtrexp(tmathcal L_s)rho(0). The scaled cumulant-generating function (SCGF) is the eigenvalue with largest real part,\n\ntheta(s)=lim_ttoinftyfraclog Z(st)t\n\nIts first two derivatives at zero are the stationary current and zero-frequency noise. The implementation uses the real moment-generating field s. A complex field can be used with tilted_liouvillian and finite_time_mgf for characteristic functions, but counting_scgf deliberately requires a real field so that largest-real-part selection follows the large-deviation branch.","title":"Convention"},{"category":"section","location":"counting_statistics/#Prepared-workflow","page":"Counting statistics and rare events","text":"using PermutationalInvariantDynamics\n\nbasis = PIBasis(8, 2)\nspin = spin_matrices()\nmodel = PIModel(basis, (\n    LocalJump(spin.jm; rate=0.7),\n    LocalJump(spin.jp; rate=0.3),\n))\n\ncompiled = compile(model; backend=:matrixfree)\n\n# Channel numbers match TrajectoryPlan and recorded jump histories.\ncounting = TiltedLiouvillianPlan(\n    compiled;\n    channels=[1],\n    increments=[1],\n)\nworkspace = TiltedLiouvillianWorkspace(counting)\n\nrho0 = computational_product_state(basis, 2)\nZ = finite_time_mgf(\n    counting, rho0, 4.0, 0.1;\n    workspace,\n    krylovdim=40,\n)\n\ntheta = counting_scgf(counting, 0.1; krylovdim=50)\ntransport = counting_cumulants(counting; krylovdim=50)\n@show transport.current transport.noise transport.fano\n\nA LocalJump is one unresolved PI channel: it counts jumps summed over all identical particles. A collective channel is likewise one channel. Fixed correlated reservoirs are counted after their prepared positive factorization, matching the channel indices recorded by quantum trajectories. Selecting the same channel twice is rejected; use a non-unit increment to assign its charge.\n\nPlans accept only autonomous, fixed-operator trajectory lowerings. Freeze a driven model at an explicit time and parameter value before constructing the plan. Counting also enforces finite, nonnegative rates, even though the deterministic time-local solver permits negative rates.","title":"Prepared workflow"},{"category":"section","location":"counting_statistics/#Matrix-free-application","page":"Counting statistics and rare events","text":"For a custom eigensolver, prepare the operator once:\n\nLtilted = tilted_liouvillian(counting, 0.1; workspace)\ny = similar(rho0.data)\nmul!(y, Ltilted, rho0.data)\n\nThe operator precomputes the factors expm1(s*q) and reuses one synchronized workspace. Concurrent tasks should share counting, not workspace; give each task a separate TiltedLiouvillianWorkspace and call apply_tilted!. The adjoint action is also matrix-free.","title":"Matrix-free application"},{"category":"section","location":"counting_statistics/#Large-deviation-curve","page":"Counting statistics and rare events","text":"curve = counting_scgf_curve(\n    counting, range(-0.5, 0.5; length=41);\n    krylovdim=60,\n)\n\ncurrents = range(0.0, 0.5; length=51)\nrate = large_deviation_rate_function(curve, currents)\n\nThe rate function is the discrete Legendre–Fenchel estimate\n\nI(j)=max_ss j-theta(s)\n\nInspect rate.boundary_maxima. A maximum on the edge of the supplied field grid means that the continuous supremum has not been resolved; the routine reports this rather than extrapolating or silently extending the grid.","title":"Large-deviation curve"},{"category":"section","location":"counting_statistics/#Reliability-and-convergence","page":"Counting statistics and rare events","text":"finite_time_mgf reports adaptive Krylov exponential-action diagnostics with return_info=true.\ncounting_scgf(...; return_info=true) reports the dominant Ritz residual.\ncounting_cumulants compares steps h and h2 and returns Richardson discrepancy estimates. Refine step and krylovdim independently.\nA nonzero imaginary part of the real-field SCGF is retained. Cumulant and Legendre routines reject it when it exceeds the Ritz-residual reliability scale.\nmemory_budget guards model-to-counting preparation, Krylov and tilted-application workspaces, and the retained output of an SCGF field curve. The guard runs before a known-length field iterator is consumed.\nfinite_time_mgf(...; return_info=false) returns only the scalar and does not copy the propagated PI vector into a state. With return_info=true, the additional detached tilted state is deliberately retained and included in the memory estimate.\nProducts s*q and weights expm1(s*q) are checked for nonfinite values and underflow in the prepared precision before they enter the generator.\n\nFinite-time trajectory counts offer an independent stochastic cross-check: for a stored trajectory, evaluate exp(s * sum(q[channel] for channel in trajectory.jump_channels)) and average this weight over independent paths. The deterministic tilted result should lie within the reported Monte Carlo confidence interval after time-step and ensemble-size convergence.","title":"Reliability and convergence"},{"category":"section","location":"counting_statistics/#API","page":"Counting statistics and rare events","text":"","title":"API"},{"category":"type","location":"counting_statistics/#PermutationalInvariantDynamics.TiltedLiouvillianPlan","page":"Counting statistics and rare events","text":"TiltedLiouvillianPlan(source; channels=:all, increments=1)\n\nPrepare the jump-gain maps needed for full counting statistics. source may be an autonomous PIModel, CompiledPIModel, or TrajectoryPlan. Channel numbers are the jump-channel numbers of the term-resolved trajectory plan; a local channel counts the sum of identical local jumps and does not resolve the particle label.\n\nFor the moment-generating convention used here, a trajectory with channel counts K_a is weighted by\n\nexpleft(ssum_a q_a K_aright)\n\nwhere q_a is the corresponding entry of increments. The tilted generator is therefore\n\nmathcal L_s=mathcal L+\nsum_aleft(e^s q_a-1right)mathcal J_a\n\nOnly the gain map J_a[rho] = rate_a * L_a*rho*L_a' is tilted; its anticommutator loss is unchanged. Signed real increments are supported. Time-dependent models must first be frozen at an explicit time. All jump rates are checked to be finite and nonnegative, as required for a counting interpretation.\n\nThe plan is immutable and shareable. Each concurrent application needs its own TiltedLiouvillianWorkspace.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.TiltedLiouvillianPlan"},{"category":"type","location":"counting_statistics/#PermutationalInvariantDynamics.TiltedLiouvillianWorkspace","page":"Counting statistics and rare events","text":"TiltedLiouvillianWorkspace(plan)\n\nTask-owned mutable scratch for a TiltedLiouvillianPlan. It retains one ordinary Liouvillian workspace and one PI-coordinate gain vector. Reuse it sequentially; never share one workspace between concurrent tasks.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.TiltedLiouvillianWorkspace"},{"category":"type","location":"counting_statistics/#PermutationalInvariantDynamics.TiltedLiouvillian","page":"Counting statistics and rare events","text":"TiltedLiouvillian\n\nAutonomous matrix-free tilted-generator adapter. Construct it with tilted_liouvillian. The adapter owns and synchronizes one workspace; parallel hot loops should share its immutable plan and use separate TiltedLiouvillianWorkspace objects instead.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.TiltedLiouvillian"},{"category":"function","location":"counting_statistics/#PermutationalInvariantDynamics.apply_tilted!","page":"Counting statistics and rare events","text":"apply_tilted!(destination, plan, source, field, workspace)\n\nApply the tilted generator in PI coordinates without materializing it. Source and destination must not alias. For repeated applications at one field, use tilted_liouvillian, which prepares the exponential channel factors once.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.apply_tilted!"},{"category":"function","location":"counting_statistics/#PermutationalInvariantDynamics.apply_tilted_adjoint!","page":"Counting statistics and rare events","text":"apply_tilted_adjoint!(destination, plan, source, field, workspace)\n\nApply the Frobenius adjoint of a tilted PI generator with caller-owned scratch.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.apply_tilted_adjoint!"},{"category":"function","location":"counting_statistics/#PermutationalInvariantDynamics.tilted_liouvillian","page":"Counting statistics and rare events","text":"tilted_liouvillian(plan, field; workspace=nothing)\n\nReturn an autonomous matrix-free operator for the counting-field generator L_field. The compatibility operator owns and synchronizes one workspace. Parallel Krylov or custom hot loops should instead call apply_tilted! with one explicit workspace per task.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.tilted_liouvillian"},{"category":"function","location":"counting_statistics/#PermutationalInvariantDynamics.counting_scgf","page":"Counting statistics and rare events","text":"counting_scgf(plan, field; krylovdim=40, return_info=false, ...)\n\nReturn the scaled cumulant-generating function, the eigenvalue of the tilted generator with largest real part. The counting field must be real. return_info=true returns the raw complex Ritz value, residual, convergence flags, and channel convention. No PI-coordinate matrix is materialized.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.counting_scgf"},{"category":"function","location":"counting_statistics/#PermutationalInvariantDynamics.counting_cumulants","page":"Counting statistics and rare events","text":"counting_cumulants(plan; step=nothing, ...)\n\nEstimate the stationary current and zero-frequency noise (the first two cumulants per unit time) from Richardson-extrapolated centered derivatives of the SCGF at zero. Results include the coarse/fine discrepancy as a finite-difference error estimate and all four nonzero-field spectral reports.\n\nThe convention is theta(s) = lim(t->Inf) log(E[exp(s*K_t)])/t, so current = theta'(0) and noise = theta''(0).\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.counting_cumulants"},{"category":"function","location":"counting_statistics/#PermutationalInvariantDynamics.counting_scgf_curve","page":"Counting statistics and rare events","text":"counting_scgf_curve(plan, fields; ...)\n\nEvaluate the dominant tilted eigenvalue on a strictly increasing real field grid. The returned values remain complex so numerical branch leakage is never silently discarded.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.counting_scgf_curve"},{"category":"function","location":"counting_statistics/#PermutationalInvariantDynamics.large_deviation_rate_function","page":"Counting statistics and rare events","text":"large_deviation_rate_function(curve, currents; atol=..., rtol=...)\n\nCompute the discrete Legendre–Fenchel estimate I(j) = maximum_s (s*j - theta(s)) from a result of counting_scgf_curve. A maximum at either field-grid boundary is reported in boundary_maxima; such a point is not certified as a resolved continuous-field supremum.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.large_deviation_rate_function"},{"category":"function","location":"counting_statistics/#PermutationalInvariantDynamics.finite_time_mgf","page":"Counting statistics and rare events","text":"finite_time_mgf(plan, rho0, time, field; ...)\n\nCompute tr(exp(time*L_field)*rho0) by an adaptive matrix-free Krylov exponential action. The returned scalar is the finite-time moment-generating function. Set return_info=true to also retain the unnormalized tilted state, its logarithm, and Krylov convergence diagnostics.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.finite_time_mgf"},{"category":"section","location":"benchmarks/#Performance-benchmarks-and-Julia-ecosystem-comparisons","page":"Performance benchmarks","text":"The benchmark suite is intended to make performance investigations reproducible, not to provide a universal package ranking. Results depend on the physical model, retained representation, scalar type, Julia and package versions, solver choices, thread settings, and hardware. A result is useful only together with that context and the numerical checks performed on the same case.\n\nThere are seven complementary internal suites and one cross-package track:\n\nperformance_regression.jl supplies deterministic CI correctness, equivalence, retained-storage, and hot-allocation gates without machine-dependent timing thresholds;\nperformance_audit.jl is a broad warmed, human-readable survey;\nbenchmarks.jl is the longer BenchmarkTools collection for targeted optimization;\ninternal_scaling.jl retains a stable four-family schema for core PI preparation and application scaling;\ncold_start.jl measures independent-process Julia startup and package-load latency;\ntime_to_solution.jl separates setup, solve, validation, and total time for representative stationary-state, dynamics, streaming-trajectory, and reduction workflows;\nbatched_trajectories.jl compares fixed-capacity matrix-RHS conditional trajectory kernels with equivalent repeated scalar workspaces, including warmed allocation measurements; and\nthe cross-package runner compares a deliberately small common operation with general-purpose Julia quantum-dynamics packages where the underlying representations can be matched or clearly identified.\n\nThe command-line details and current case grids are canonical in benchmark/README.md and benchmark/comparison/README.md. Generated timing tables and local manifests are intentionally ignored rather than committed. This avoids presenting one developer machine's measurements as package-wide results.","title":"Performance benchmarks and Julia ecosystem comparisons"},{"category":"section","location":"benchmarks/#Startup-latency-and-complete-workflows","page":"Performance benchmarks","text":"Package loading and warmed numerical work answer different performance questions and are therefore measured by separate entry points:\n\njulia --startup-file=no --project=benchmark \\\n  benchmark/cold_start.jl --mode quick\njulia --startup-file=no --project=benchmark \\\n  benchmark/time_to_solution.jl --mode quick\n\ncold_start.jl launches one new Julia process for each raw sample. It measures a startup-only control and a package-load probe; the latter also reports the child's interval around using PermutationalInvariantDynamics and constructs a tiny basis as a deterministic smoke test.\n\ntime_to_solution.jl first discards complete workflow warmups, then reports raw setup, solve, validation, and summed total rows. Its four cases exercise:\n\nan independently pumped and decaying qubit ensemble with an analytic thermal product stationary state;\nindependent emission dynamics with an analytic product state at the final time;\nfixed-seed, state-free trajectory statistics checked against binomial excitation and jump-count laws; and\na reusable Schur reduction plan checked against the half-system GHZ marginal purity.\n\nBoth scripts write schema-versioned TSV plus a metadata sidecar. Run controls, environment and manifest hashes, Git/worktree identity, hardware, and validation outcomes belong with every timing table. Defaults use one discarded warmup, a 512 MiB explicit operation budget, and ignored files under benchmark/results/. --dry-run exercises parsing and metadata output without timing anything. The canonical options and case sizes are documented in benchmark/README.md.","title":"Startup latency and complete workflows"},{"category":"section","location":"benchmarks/#Internal-workflow-coverage","page":"Performance benchmarks","text":"The regression, audit, and detailed suites exercise current prepared APIs over a broader surface than the fixed internal-scaling runner:\n\nWorkflow Regression gate Human-readable audit Detailed BenchmarkTools suite Internal scaling\nPrepared PI core sparse/matrix-free equivalence, forward/adjoint batch action, threading, allocation setup, retained storage, scalar and batch action separate setup, sparse, matrix-free, driven, threaded, and observable workloads four fixed qubit/qutrit families\nTime evolution and Floquet low-storage RK4 and adaptive-expv storage checks RK4, matrix-free Floquet-map setup, period/batch action, and stroboscopic evolution prepared RK4 and Floquet setup/period/batch workloads driven one-period action is not included\nAppendix-D p-body and reductions packed-support and allocation checks p-body geometry/application, qudit reduction, local-factor trace cached/uncached p-body setup and prepared reduction hot paths not included\nIdentical local pseudomodes supersite compile/application and spin-factor trace checks prepared supersite construction, action, and reduction reusable local-pseudomode setup and hot action not included\nShared/global pseudomodes matrix-free composite action and subsystem reduction checks global-mode setup, action, and reduction prepared global-pseudomode setup and hot action not included\nPI–HEOM packed hierarchy forward/adjoint/batch checks hierarchy setup and forward/adjoint/batch action setup, prepared forward/adjoint/batch action, and preconditioner workloads not included\nPI–HOPS conditioned hierarchy action and reused-ensemble checks plan/workspace, conditioned action, and ensemble survey plan, conditioned action, and reused-ensemble workloads not included\nComposite and stochastic systems composite scalar/batch/adjoint action plus density, weak-PI, and composite trajectory gates deterministic composite, batched, diffusive, density, weak-PI, and composite trajectory survey reusable composite/batch and trajectory workloads not included\nStationary and spectral solvers adaptive-expv storage and symmetry-restricted workspace checks GMRES, Arnoldi, harmonic Arnoldi, symmetry restriction, and preconditioner survey reusable Krylov, preconditioned, recycled, and multi-shift workloads not included\n\nThis is a code-path inventory, not a claim that every suite uses the same problem size or accuracy target. Regression uses deliberately small deterministic stochastic and non-Markovian cases; the audit and detailed suite are where their cutoffs, ensemble sizes, or Krylov dimensions should be increased.","title":"Internal workflow coverage"},{"category":"section","location":"benchmarks/#Internal-PI-scaling-suite","page":"Performance benchmarks","text":"From the repository root, prepare the isolated benchmark environment once:\n\njulia --startup-file=no --project=benchmark -e \\\n  'using Pkg; Pkg.develop(path=pwd()); Pkg.instantiate()'\n\nRun either the short development grid or the larger research grid:\n\njulia --startup-file=no --project=benchmark \\\n  benchmark/internal_scaling.jl --mode quick\njulia --startup-file=no --project=benchmark \\\n  benchmark/internal_scaling.jl --mode full\n\nThe runner also accepts explicit sample, timing, warm-up, validation-size, and output controls. Consult benchmark/README.md rather than copying defaults into long-lived automation.\n\nThe runner remains the internal-scaling schema-version-2 suite with exactly four families:\n\nall-Schur-sector qubits with local and collective terms;\nall-Schur-sector qutrits with local and collective terms;\nfully symmetric qubits with collective terms only; and\nfully symmetric qutrits with collective terms only.\n\nThe current grids are explicit:\n\nMode All-sector qubits All-sector qutrits Symmetric qubits Symmetric qutrits\nquick N = 2, 4, 8 N = 2, 3, 4 N = 4, 8, 16 N = 2, 4\nfull N = 2, 4, 8, 12, 16, 20, 24 N = 2, 3, 4, 5, 6 N = 4, 8, 16, 32, 48, 64, 96 N = 2, 4, 8, 12\n\nAll current cases use ComplexF64. The source script is authoritative for the term coefficients and any future grid changes. Pseudomodes, HEOM, HOPS, composite dynamics, trajectories, p-body reductions, and solver time-to-solution are covered by the broader suites above rather than added to this scaling schema.\n\nEvery case separates the following phases:\n\nPhase Included work Interpretation\nBasis setup construction of the retained PI basis representation setup cost for that N and d\nMatrix-free compilation lowering the already specified model into a prepared plan reusable model-preparation cost\nPrepared application one explicit-workspace generator action after warm-up hot operator-action cost; it excludes basis and model preparation\nSymmetric sparse compilation/application explicit sparse setup and warmed mul! sparse-first construction and retained-support action for the occupation route\nSymmetric driven compilation/application InPlaceTimeOperator collective Hamiltonian and jump with reusable workspace preallocated time-dependent occupation-block cost\nAutomatic backend probe backend=:auto with a fixed 512 MiB budget whether structured support estimates select sparse or matrix-free\nValidation sparse-oracle comparison when bounded, trace preservation, and adjoint duality numerical evidence, timed separately from the performance phases\n\nSetup and hot application report minimum and median elapsed nanoseconds. Their allocated bytes and allocation counts belong to the fastest measured sample. Retained-memory columns report the standalone summarysize of the basis, model, compiled object, plan, workspace, and vectors, together with a non- double-counted retained total. summarysize is a Julia object-graph estimate: it is not peak resident memory and does not include every native-library or operating-system allocation.\n\nThe schema also records the geometry route, conservative geometry storage, exact-support contribution and retained-nonzero bounds, predicted sparse operator/assembly/peak bytes, actual sparse nonzeros and retained bytes, and the automatic backend. Sparse, driven, and automatic-probe fields are NA for all-sector rows because these additional phases target the symmetric occupation-space collective path; the common matrix-free phases still cover every row.\n\nThis is internal-scaling result schema version 2. The sibling metadata table records the same version and the sparse, driven, and automatic-probe policies.\n\nThe runner uses BenchmarkTools, performs warm-up passes, sets one evaluation per sample, and forces BLAS to one thread. Its metadata sidecar records the mode and controls, Julia/package and Git information, thread settings, machine details, active project and selected manifest SHA-256 hashes, and whether the startup file was disabled. The default output is benchmark/results/internal_scaling_<mode>.tsv with a sibling .metadata.tsv; neither is a source artifact.\n\nThe audit, regression, and detailed entry points serve different purposes:\n\njulia --startup-file=no --project=benchmark \\\n  benchmark/performance_audit.jl\nJULIA_NUM_THREADS=4 julia --startup-file=no --project=benchmark \\\n  benchmark/performance_regression.jl\njulia --startup-file=no --project=benchmark \\\n  benchmark/batched_trajectories.jl\njulia --startup-file=no --project=benchmark -e \\\n  'include(\"benchmark/benchmarks.jl\"); display(run(SUITE; verbose=true))'\n\nperformance_audit.jl is a human-readable warmed timing/allocation survey. performance_regression.jl primarily enforces allocation and numerical- equivalence gates and deliberately avoids fragile wall-clock thresholds. benchmark/benchmarks.jl remains the detailed BenchmarkTools workload collection.","title":"Internal PI scaling suite"},{"category":"section","location":"benchmarks/#Cross-package-comparison","page":"Performance benchmarks","text":"The comparison environments are isolated from both the package root and one another. This is necessary because the compared packages can resolve different SciML dependency ranges; it also prevents one backend from changing another backend's dependency graph. Package loading and precompilation are not part of the timed operation.\n\nPrepare all comparison environments and then run every backend with:\n\njulia --startup-file=no benchmark/comparison/setup.jl\njulia --startup-file=no benchmark/comparison/run_all.jl\n\nFor example, a controlled run can specify --samples 50 --seconds 2 --threads 1 --output /tmp/pid-comparison on run_all.jl. The default ignored output is under benchmark/comparison/results/. The runner writes one TSV per backend and a joined comparison table. It also records package versions, representation and dimensions, sparse nonzero counts, retained summarysize, minimum and median application times, allocations, execution backend and action kind, validation results, requested and actual sample counts, timestamp, OS/architecture, BenchmarkTools version, benchmark Git revision/dirty state and worktree hash, full BLAS configuration, and a SHA-256 of the active manifest. The current cross-package row schema is version 3 and is identical for every adapter.\n\nEach backend is loaded and warmed before measurement. The setup phase constructs the basis, initial state, and sparse generator. The apply phase measures a warmed in-place sparse matrix-vector product. These phases must not be mixed: a package with more reusable preparation can have a different setup tradeoff from its repeated-action tradeoff.","title":"Cross-package comparison"},{"category":"section","location":"benchmarks/#Matched-collective-dynamics","page":"Performance benchmarks","text":"The first track uses collective emission gamma * D[J_-], with gamma = 0.37, from the fully excited state at N = 4, 8, 16, 32, 64. It is an exact comparison in the fully symmetric spin-N/2 sector:\n\nPermutationalInvariantDynamics explicitly retains only partition (N,0) for this track;\nQuantumOptics uses its spin-N/2 basis; and\nQuantumToolbox uses its spin-N/2 representation.\n\nThe state, generator convention, and retained Hilbert dimension are therefore matched. Every row validates the vanishing trace derivative and the analytical Frobenius norm sqrt(2) * gamma * N, Hermiticity, and the complete analytical derivative eigenspectrum before its timing is interpreted. The equation-(7) PI coordinates are orthonormal, so their Euclidean norm is the same physical Hilbert-space Frobenius norm used by the full-space adapters.\n\nAdapters reuse an existing SparseMatrixCSC without copying it and convert another matrix representation exactly once. PID setup therefore measures the symmetric occupation-number lowering and sparse-first materialization; the backend and action_kind columns distinguish that package-specific setup from the common warmed SparseArrays.mul! action.\n\nThis track measures a collective symmetric-sector operation. It does not measure the defining advantage of an all-Schur-sector PI representation under local noise. Moreover, the materialized sparse generators have the same CSC dimensions and nonzero count, so every warmed action invokes the same Julia SparseArrays.mul! implementation. Treat tiny hot-action differences as measurement noise; package-specific differences in this track are setup costs.","title":"Matched collective dynamics"},{"category":"section","location":"benchmarks/#Independent-local-emission","page":"Performance benchmarks","text":"The second track uses gamma * sum_i D[sigma_-^(i)], again with gamma = 0.37, from the fully excited product state. Independent local jumps populate several total-spin sectors, so a single spin-N/2 density matrix is not an exact representation of this dynamics.\n\nFor this reason:\n\nPermutationalInvariantDynamics uses its complete PI operator basis at N = 2, 4, 6, 8, 16; while\nQuantumOptics and QuantumToolbox use the full tensor-product Hilbert space only at N = 2, 4, 6, where that construction is deliberately bounded.\n\nEvery row validates the analytical derivative norm gamma * sqrt(N * (N + 1)), Hermiticity, trace preservation, and the full analytical eigenvalue/multiplicity signature. The result should be read as two representation-scaling curves with a small common correctness overlap, not as like-for-like backend speed. A fixed-spin result is not reported as a speed ratio for local dissipation, because it would solve a different problem.\n\nThe table separates physical_hilbert_dimension=2^N from the vector length actually used by the sparse action, retained_operator_dimension. The latter is (N+1)^2 for a fixed spin irrep, the complete PI coefficient count for the all-sector backend, or 4^N in the full-Hilbert baselines.","title":"Independent local emission"},{"category":"section","location":"benchmarks/#What-counts-as-a-direct-competitor?","page":"Performance benchmarks","text":"Our review did not identify another maintained Julia package whose public numerical representation covers exact mixed PI states spanning all retained Schur sectors with both independent local and collective open-system terms. That is a statement about the packages and interfaces reviewed for this benchmark, not a proof that no such code exists. Contributions extending the comparison are welcome when they provide an equivalent physical problem and a reproducible environment.\n\nThe packages discussed here occupy useful but different scopes:\n\nPackage Relevant scope Role in this comparison\nQuantumOptics.jl documentation / repository General quantum dynamics with spin and composite bases Direct for the matched symmetric collective track; full-Hilbert small-N reference for local emission\nQuantumToolbox.jl documentation / repository General quantum dynamics with dense/sparse operators and SciML solvers Same bounded comparison roles as QuantumOptics\nQuantumCumulants.jl documentation / repository Symbolic equations closed by a selected-order cumulant approximation Contextual alternative for large systems, not a direct benchmark of exact PI mixed-state dynamics\nCollectiveSpins.jl documentation / repository Spatially distributed, interacting collective-spin models and reduced descriptions Contextual ecosystem package, not an exact all-Schur-sector PI backend in this benchmark\n\nThe registry search also considered PermutationSymmetricTensors.jl, which is a general permutation-symmetric multidimensional-array container, and DickeModel.jl, which targets the specific quantum and classical Dicke model. Neither exposes the equivalent general mixed-state all-Schur PI dynamics workload, so neither is assigned a timing ratio here.\n\nQuantumCumulants or a mean-field/cluster method may be the scientifically appropriate choice when controlled approximate observables are sufficient at sizes beyond exact PI calculations. Its runtime should be accompanied by a closure-order and accuracy study, not divided directly by an exact density- operator runtime. Similarly, general-purpose full-Hilbert packages provide features and model classes outside this package's PI scope; one microbenchmark does not rank those broader capabilities.","title":"What counts as a direct competitor?"},{"category":"section","location":"benchmarks/#Reading-results-responsibly","page":"Performance benchmarks","text":"When reporting a benchmark:\n\nretain the generated metadata and resolved package versions;\nstate the physical equation, initial state, representation, and retained dimensions;\nreport validation errors beside timing and memory metrics;\ndistinguish cold setup, reusable preparation, and warmed application;\nuse the same thread and BLAS settings, scalar precision, and hardware; and\nrepeat the run after changing Julia, a dependency, or the machine.\n\nMinimum time is useful only when it is comfortably above measurement overhead, while the median is a more robust typical sample. A near-zero corrected minimum, especially 0.001 ns, is unresolved and must not be used in a speed ratio; time a larger batch instead. Julia allocation bytes do not equal peak RAM, and sparse nnz does not include every retained object. Neither metric establishes time-to-solution for steady states, trajectories, adaptive dynamics, or spectra. Those tasks require their own matched accuracy criterion and convergence study.\n\nAccordingly, the repository publishes the harness and validation rules, not a timeless claim that one package is universally faster.","title":"Reading results responsibly"},{"category":"section","location":"streaming_output/#Streaming-and-observable-only-output","page":"Streaming output","text":"Fixed-step deterministic evolution and PI quantum trajectories can record small analysis products while discarding sampled density-state histories. The numerical evolution is unchanged: streaming controls only what is retained at requested output times.","title":"Streaming and observable-only output"},{"category":"section","location":"streaming_output/#Deterministic-dynamics","page":"Streaming output","text":"Pass one or more named observables to solve_dynamics:\n\nresult = solve_dynamics(\n    prepared, rho0, (0.0, 1.0);\n    saveat=0.05,\n    observables=(energy=H, polarization=sz),\n    save_states=false,\n)\nenergy = result.observables[:energy]\n\nThe observable collection may be a named tuple, dictionary, pair collection, or a single local matrix/PIOperator. Local matrices represent collective sums. With save_states=false, one mutable PI state is propagated and no sampled state vector is copied into the result. Set save_states=true to retain states and expectations in the same run.","title":"Deterministic dynamics"},{"category":"section","location":"streaming_output/#Trajectory-ensembles","page":"Streaming output","text":"For trajectories, the corresponding call returns online Monte Carlo means, unbiased variances, standard errors, and normal confidence intervals:\n\nensemble = quantum_trajectories(\n    plan, rho0, times, 10_000;\n    dt=0.002, seed=1, threaded=true,\n    observables=(magnetization=Jz,),\n    save_states=false,\n)\nmagnetization = ensemble.observables.observables[:magnetization]\n\nEach task owns its trajectory workspace, observable buffer, and Welford accumulator. The accumulators are merged in worker order; no mutable numerical scratch is shared between tasks. Random streams remain indexed by trajectory, as in the history-returning API.\n\nJump summaries are enabled by default and retain pooled waiting times, giving storage proportional to the number of recorded waiting intervals rather than PI-state history size. Use jump_statistics=false when those data are not needed. At least one observable is required when save_states=false; the no-observable call deliberately keeps the legacy vector-of-trajectories return type. In threaded runs pooled waiting samples have no trajectory-order guarantee; their values and aggregate statistics remain tied to the trajectory-indexed random streams. confidence selects the reported normal confidence interval and must lie strictly between zero and one.","title":"Trajectory ensembles"},{"category":"section","location":"streaming_output/#Trajectory-steady-state-estimates","page":"Streaming output","text":"For an autonomous model, trajectory_steady_state estimates a stationary density operator without retaining complete paths, saved state histories, or jump records:\n\nrho_mc = trajectory_steady_state(\n    plan, rho0;\n    trajectories=2_000,\n    settling_time=50.0,\n    dt=0.002,\n    samples_per_trajectory=11,\n    sampling_interval=2.0,\n    seed=1,\n    threaded=true,\n)\n\nEach independent realization first evolves through settling_time. The selected post-settling states are averaged within that path, and those path means are then averaged across trajectories. This ordering is important: states separated by sampling_interval on one path are correlated and are not counted as independent Monte Carlo realizations. The default samples_per_trajectory=1 uses only the state at the end of the settling interval and does not require sampling_interval. At least two independent paths are required.\n\nBy default the function returns the approximate PIState. Set return_info=true to obtain a TrajectorySteadyStateResult containing the state, the Hilbert–Schmidt sample spread and standard error across independent path means, the Liouvillian residual and relative residual, and the trace error. Named Hermitian observables additionally obtain path-level means, unbiased variances, standard errors, and normal confidence intervals:\n\nestimate = trajectory_steady_state(\n    plan, rho0;\n    trajectories=2_000,\n    settling_time=50.0,\n    dt=0.002,\n    samples_per_trajectory=11,\n    sampling_interval=2.0,\n    observables=(magnetization=Jz,),\n    confidence=0.95,\n    return_info=true,\n)\nmagnetization = estimate.observables.observables[:magnetization]\n\nObservable statistics belong to the detailed result, so an observables request requires return_info=true rather than computing a report that would then be discarded.\n\nThis routine requires an autonomous fixed-operator TrajectoryPlan; a model or compiled source is prepared into that form automatically. It deliberately does not claim convergence. Increase settling_time, vary sampling_interval, converge the fixed step or adaptive controls, and increase trajectories separately. A small residual is a diagnostic of the averaged state, not a proof of uniqueness or of equilibration. When the Liouvillian has several stationary states, its ensemble mean can retain dependence on rho0. Different valid unravelings of one fixed Lindbladian have the same ensemble mean, although their path distributions, variances, and finite Monte Carlo realizations can differ.\n\nThe direct-sum Schur pseudo-ket backend has the analogous history-free weak_pi_trajectory_steady_state. It converts every sampled pseudo-ket to PI density coordinates before forming within-path means; averaging pseudo-ket amplitudes would give the wrong mixed state. See Weak-PI Schur pseudo-ket trajectories for its formula, supported channels, and fixed-step restrictions.\n\nThe runnable streaming_output.jl example compares both routes with an exactly soluble emission model.","title":"Trajectory steady-state estimates"},{"category":"section","location":"streaming_output/#Result-types","page":"Streaming output","text":"","title":"Result types"},{"category":"type","location":"streaming_output/#PermutationalInvariantDynamics.DynamicsStreamResult","page":"Streaming output","text":"DynamicsStreamResult\n\nMemory-conscious high-level dynamics output returned by solve_dynamics when observables are requested or save_states=false. observables maps each user-supplied name to its sampled expectation-value vector. states is either the ordinary saved PIState vector or nothing.\n\nWhen states === nothing, propagation owns one mutable state and observable values are evaluated before that state is reused. Thus retained output storage does not scale with the PI-coordinate dimension. Observable callbacks are not used in this path, so user code cannot accidentally retain the integrator state.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.DynamicsStreamResult"},{"category":"type","location":"streaming_output/#PermutationalInvariantDynamics.TrajectoryEnsembleResult","page":"Streaming output","text":"TrajectoryEnsembleResult\n\nMemory-conscious result returned by quantum_trajectories when observables are requested or save_states=false. trajectories is either the ordinary vector of QuantumTrajectory objects or nothing when state histories were not saved. observables contains online Monte Carlo statistics and jumps contains online channel-resolved jump statistics; either field is nothing when the corresponding output was not requested. Indexing and iteration return stored trajectories and raise when histories were disabled.\n\nWith save_states=false, no sampled PIState is constructed. Observable buffers and Welford accumulators are task-owned and their retained storage is O(workers * length(times) * number_of_observables). Jump statistics retain only their summary and the pooled inter-jump waiting times. The pooled waiting samples have no trajectory-order guarantee under threaded scheduling.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.TrajectoryEnsembleResult"},{"category":"section","location":"qudit_phase_space/#Generalized-qudit-Husimi-phase-space","page":"Qudit Husimi phase space","text":"qudit_husimi_q extends the sector-resolved Husimi transform beyond qubits. A phase-space point is a local unitary Uin U(d) (or a Hermitian generator H with U=exp(-iH)). The same local transformation is lifted into each retained Schur irrep; no d^N state or operator is constructed.\n\nFor partition nu, the transform uses the extremal GT vector and the multiplicity-weighted state block barrho_nu=sqrtf^nuC_nu:\n\nQ_nu(U)=dim(U_nu)\nlanglenuUbarrho_nunuUrangle\n\nWith normalized Haar measure, the integral of one sector density is exactly its physical sector population. The aggregate integrates to the sum of the selected populations. Numerical values are never clipped; an invalid input state is rejected.","title":"Generalized qudit Husimi phase space"},{"category":"section","location":"qudit_phase_space/#Prepared-use","page":"Qudit Husimi phase space","text":"using PermutationalInvariantDynamics, LinearAlgebra\n\nbasis = PIBasis(8, 3)\nrho = maximally_mixed_state(basis)\ngenerators = [Diagonal([x, -x, 0.0])\n              for x in range(0, 2pi; length=64)]\n\nplan = QuditHusimiPlan(basis, generators;\n    representation=:generator)\nq = qudit_husimi_q(rho, plan; resolved=true)\n\nReuse QuditHusimiPlan for multiple states on the same point set. Unitary input uses a Schur factorization and supports Float32/Float64; the generator route remains available for other supported scalar types. The result stores one aggregate value per supplied point and, with resolved=true, a sector-by-point matrix.\n\nFor selected sector nu, the plan retains dim(U_nu) * number_of_points coherent-vector entries. Setup constructs the lifted dense generator and its exponential for every sector/point pair, so plan construction can dominate a large orbit sample even though it never scales as d^N. The plan is tied to the exact PIBasis; share it read-only and rebuild it rather than narrowing a state or point precision.\n\nThis API provides generalized Husimi-Q data, not a qudit Wigner transform. The U(d) orbit has a redundant stabilizer and no single two-dimensional coordinate chart is inferred. QuditHusimiData therefore preserves supplied point order but not a manifold coordinate system; the optional Makie adapter plots point index against aggregate Q. Use sector_values explicitly for resolved custom plots.\n\nFor d=2, the normalized-Haar result equals 4pi times the package's sphere-density spin_husimi_q convention at the corresponding spin-coherent direction. See examples/qudit_coherent_state_q_distribution.jl for that sanity check and a qutrit scan.","title":"Prepared use"},{"category":"section","location":"qudit_phase_space/#API","page":"Qudit Husimi phase space","text":"","title":"API"},{"category":"type","location":"qudit_phase_space/#PermutationalInvariantDynamics.QuditHusimiPlan","page":"Qudit Husimi phase space","text":"QuditHusimiPlan(basis, points; representation=:unitary,\n                sectors=:all, T=nothing, atol=nothing, rtol=nothing)\n\nPrepare generalized coherent vectors for a qudit Husimi-Q transform.  Each point is either a d-by-d unitary (representation=:unitary) or a Hermitian generator H (representation=:generator) representing the local unitary exp(-im*H).  A single matrix or an iterable of matrices is accepted.\n\nFor a sector nu, the fiducial vector is the first vector in the package's ascending GT order, whose content is reverse(nu.parts).  The lifted local unitary acts through the physical collective one-body generator.  The plan is read-only, tied to the exact PIBasis, and may be reused for many states.\n\nUnitary input currently uses the LAPACK Schur factorization and therefore supports Float32 and Float64 real precision.  Use Hermitian generators for other scalar types.  An explicit T may widen floating-point coordinates but may not silently narrow them; integer and exact coordinates remain accepted. No d^N object is constructed.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.QuditHusimiPlan"},{"category":"type","location":"qudit_phase_space/#PermutationalInvariantDynamics.QuditHusimiData","page":"Qudit Husimi phase space","text":"QuditHusimiData\n\nGeneralized qudit Husimi-Q values with respect to normalized Haar measure on the local unitary group.  values[point] is the sum over selected Schur sectors.  When resolved=true, sector_values[sector, point] retains each sector density separately.  Every sector density integrates to its entry in populations; consequently the aggregate integrates to the selected physical population.\n\nThe unitary parametrization is generally redundant because a highest-weight state has a nontrivial stabilizer.  This does not change the normalized Haar identity used by the transform.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.QuditHusimiData"},{"category":"function","location":"qudit_phase_space/#PermutationalInvariantDynamics.qudit_husimi_q","page":"Qudit Husimi phase space","text":"qudit_husimi_q(rho, points; representation=:unitary, sectors=:all,\n               resolved=false, plan=nothing, atol=nothing, rtol=nothing)\nqudit_husimi_q(rho, plan; resolved=false, atol=nothing, rtol=nothing)\n\nEvaluate the generalized U(d) coherent-state Husimi-Q density of a PI state. For normalized Haar measure dU, sector nu uses\n\nQ_nu(U)=dim(U_nu)langlenuUbarrho_nunuUrangle\nqquad barrho_nu=sqrtf^nuC_nu\n\nand therefore integral Q_nu(U) dU equals the physical population of that sector.  The aggregate is the sum over selected sectors.  Values are returned without clipping; invalid input states are rejected by validate_state.\n\nConstruct and reuse a QuditHusimiPlan when evaluating several states on the same phase-space points.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.qudit_husimi_q"},{"category":"section","location":"matrix_free_krylov/#Matrix-free-Krylov-solvers","page":"Matrix-free Krylov solvers","text":"The Krylov routines compute stationary states and selected Liouvillian modes using only products of the form mul!(y, L, x). They do not assemble the Liouvillian matrix, compute a dense spectrum, or perform a sparse LU factorization. This is the preferred path when the PI Liouville dimension is large enough that storing the superoperator is the dominant cost.\n\nThis page covers stationary states, selected eigenvalues, matrix-free Floquet maps, and response calculations. For several right-hand sides, a common shift family, continuation with a recycled subspace, or direct exponential action, see Block, shifted, recycled, and exponential Krylov methods.\n\nPublic high-level commands report canonical solver names even when a retained compatibility alias is supplied:\n\nTask Preferred spelling Compatibility aliases\nTrace-fixed GMRES :gmres :krylov\nShift-invert stationary solve :shiftinvert :shift_invert, :inverse_iteration\nOrdinary Arnoldi spectrum :arnoldi :krylov, :ordinary_arnoldi\nBlock Arnoldi spectrum :block_arnoldi :block\nImplicitly restarted Arnoldi :iram :implicit_qr\nJacobi–Davidson :jd :jacobi_davidson\n\nThe low-level steady_state(...; method=:krylov) spelling remains supported because it predates the high-level GMRESAlgorithm interface.","title":"Matrix-free Krylov solvers"},{"category":"section","location":"matrix_free_krylov/#Constructing-the-matrix-free-Liouvillian","page":"Matrix-free Krylov solvers","text":"From a static PIModel, request the matrix-free representation explicitly:\n\nL = liouvillian(model; representation=:matrixfree)\n\nPassing model directly to a public routine with method=:krylov for a steady state, or method=:arnoldi, :harmonic, :iram, or :jd for a spectrum (with :block_arnoldi for a block start), also selects this representation automatically:\n\nrho_ss = steady_state(model; method=:krylov)\n\nFixed operators and their Schur geometry are cached in L. Scalar time-dependent rates are supported by matrix-free application, but stationary and spectral calculations require a time-independent Liouvillian.","title":"Constructing the matrix-free Liouvillian"},{"category":"section","location":"matrix_free_krylov/#Stationary-states:-restarted-GMRES","page":"Matrix-free Krylov solvers","text":"steady_state(...; method=:krylov) and method=:gmres select the same solver. The stationary equation is singular, so the implementation solves the trace-fixed rank-one system\n\n(mathcal L + v t^dagger)rho=v\nqquad t^dagger v=1\n\nwhere t is the physical PI trace functional. For a trace-preserving Liouvillian with a unique stationary state, this is equivalent to mathcal Lrho=0 and mathrmtrrho=1.\n\ninfo = steady_state(model;\n    method=:krylov,\n    krylovdim=40,\n    maxiter=500,\n    atol=1e-11,\n    rtol=1e-9,\n    return_info=true,\n)\n\nrho_ss = PIState(model.basis, info.state)\n\nThe principal options are:\n\nOption Meaning\nkrylovdim Maximum GMRES basis size before a restart; default 30\nmaxiter Maximum total Krylov iterations across all restarts\natol, rtol Absolute and relative residual tolerances\ninitial_state Initial vector or PIState; useful in parameter scans\nworkspace Reusable KrylovWorkspace\npreconditioner nothing, :schur, or a reusable preconditioner object\nreturn_info Return residuals and iteration diagnostics\n\nThe returned information contains state, residual, normalized_residual, operator_scale, trace_error, linear_residual, iterations, restarts, krylov_dimension, and converged. The trace-fixed system uses L/operator_scale, making convergence invariant under a global rate rescaling. residual remains the physical mathcal Lrho value; normalized_residual is the scale-independent accuracy check.","title":"Stationary states: restarted GMRES"},{"category":"section","location":"matrix_free_krylov/#Reusing-storage","page":"Matrix-free Krylov solvers","text":"GMRES stores O(nm) numbers for Liouville dimension n and Krylov dimension m. Reuse that storage for repeated solves:\n\nL = liouvillian(model; representation=:matrixfree)\nworkspace = KrylovWorkspace(L, 40)\n\nfirst = steady_state(L;\n    method=:krylov,\n    workspace=workspace,\n    return_info=true,\n)\n\nsecond = steady_state(L;\n    method=:krylov,\n    workspace=workspace,\n    initial_state=first.state,\n    return_info=true,\n)\n\nOne workspace must not be used concurrently. For threaded parameter sweeps, allocate one workspace per thread.\n\nWorkspace scalar types follow the actual problem precision. A fully ComplexF32 Liouvillian with Float32 seeds, shifts, and explicit scales keeps its dominant Krylov matrices in ComplexF32, roughly halving their storage relative to ComplexF64. Materialized and compatible custom operators promote for a wider initial vector, non-integer target, preconditioner, projector, regularization, or operator scale. A compiled matrix-free PI plan instead owns scratch in its compiled scalar type, so a wider solver input is rejected before allocating the Krylov basis; compile the model at that wider precision when it is required. A caller-supplied workspace that cannot represent all solver inputs is likewise rejected instead of silently narrowing data. Integer zero remains a precision-neutral default target.","title":"Reusing storage"},{"category":"section","location":"matrix_free_krylov/#Schur-sector-preconditioning","page":"Matrix-free Krylov solvers","text":"The Schur preconditioner keeps the diagonal sector blocks of the trace-fixed operator and LU-factorizes them independently. Local processes may couple different Schur sectors; those off-diagonal couplings remain in the exact matrix-free GMRES operator but are omitted from the preconditioner.\n\nFor a one-off solve, construction can be requested automatically:\n\ninfo = steady_state(model;\n    method=:krylov,\n    preconditioner=:schur,\n    krylovdim=40,\n    return_info=true,\n)\n\nFor repeated solves, construct and retain the factors:\n\nL = liouvillian(model; representation=:matrixfree)\nP = schur_sector_preconditioner(L, model.basis)\nworkspace = KrylovWorkspace(L, 40)\n\ninfo = steady_state(L;\n    basis=model.basis,\n    method=:krylov,\n    preconditioner=P,\n    workspace=workspace,\n    return_info=true,\n)\n\nFor a prepared LiouvillianPlan, CompiledPIModel, or compiled MatrixFreeLiouvillian, setup lowers each diagonal sector block directly from the immutable physical term kernels. The only complete Liouvillian applications are the three reproducible operator-scale probes (or none when operator_scale is supplied). An arbitrary matrix-free operator retains the generic compatibility route, which probes one basis vector per PI coordinate. SpecializedPIModel uses the same direct route while evaluating every prepared family-rate schedule with that specialization's bound rates. Pass the specialization itself with schur_sector_preconditioner(specialized); its exact basis is inferred. A detached, plan-less matrix-free callback deliberately retains the generic fallback because its bound parameters cannot be inferred. Both routes store sum_s n_s^2 block coefficients rather than the full n^2 matrix and produce the same trace-bordered block operator. Setup is worthwhile when GMRES is difficult or the factors are reused. For a small, rapidly convergent problem, unpreconditioned GMRES may be faster because the block construction and triangular solves have overhead.\n\nThe reusable object's metadata reports setup Liouvillian applications, their split between scale and block probes, the block-construction route, factorizations, stored coefficients/bytes, per-apply triangular solves, and a conservative suggested reuse count. Construction warns when expected_reuses is too small to amortize setup.\n\nIf a diagonal sector block is singular, an error is raised. A small explicit regularization can be supplied:\n\nP = schur_sector_preconditioner(\n    L, model.basis; regularization=1e-12)\n\nThe same shortcut is available as preconditioner_regularization=1e-12 with preconditioner=:schur. Increase it only enough to make the approximate block solves stable, and always validate the final unpreconditioned residual. The report fields preconditioned and preconditioner record whether and which preconditioner was used.","title":"Schur-sector preconditioning"},{"category":"section","location":"matrix_free_krylov/#Supplying-a-trace-functional","page":"Matrix-free Krylov solvers","text":"A MatrixFreeLiouvillian already stores the correct PI trace vector. For a custom linear operator, supply either its PI basis or its trace vector:\n\nrho = krylov_steady_state(custom_L; basis=basis)\n# or\nrho = krylov_steady_state(custom_L; trace_vector=t)\n\nThe routine rejects a custom operator when the physical trace is ambiguous.","title":"Supplying a trace functional"},{"category":"section","location":"matrix_free_krylov/#Partial-spectra:-Arnoldi","page":"Matrix-free Krylov solvers","text":"Use Arnoldi when only a few modes are needed:\n\nmodes = pi_liouvillian_spectrum(model;\n    method=:arnoldi,      # :krylov remains a compatibility alias\n    nev=6,\n    krylovdim=40,\n    sortby=:real,\n    vectors=true,\n    atol=1e-11,\n    rtol=1e-9,\n)\n\nWith vectors=true, the result contains:\n\nvalues: selected Ritz eigenvalues;\nvectors: corresponding right Ritz vectors;\nresiduals: estimates of mathcal Lv-lambda v;\nconverged: convergence flag for every requested pair;\niterations, krylov_dimension, and full PI dimension.\n\nWith vectors=false, pi_liouvillian_spectrum returns only the selected eigenvalues. The lower-level krylov_liouvillian_spectrum always returns the diagnostic named tuple and additionally accepts which:\n\nwhich Selected Ritz values\n:LR Largest real part; appropriate for slow Liouvillian modes\n:LM Largest magnitude\n:SM Smallest magnitude\n\nThe public wrapper maps sortby=:real, rev=true to :LR, and magnitude sorting to :LM or :SM. Ascending real-part selection is not supported.\n\nThe starting vector can be controlled with initial_vector. High-level wrappers use a deterministic local RNG by default; pass an explicit rng=MersenneTwister(seed) when a scan should own and record its random stream.\n\nFor repeated spectra, reuse the large basis and pencil arrays:\n\nworkspace = ArnoldiWorkspace(L, 40; mode=:ordinary)\nmodes = krylov_liouvillian_spectrum(L;\n    nev=6, krylovdim=40, workspace=workspace)\n\nmode=:ordinary retains only the Arnoldi basis, Hessenberg matrix, and two full-coordinate vectors. The ordinary solver selects this lean mode automatically when it constructs its own workspace. Use the default mode=:full when one workspace must also be reused sequentially by harmonic Arnoldi, implicit-QR Arnoldi, or Jacobi–Davidson. Those advanced solvers reject an ordinary-only workspace rather than allocating omitted arrays. No workspace may be shared concurrently.\n\nBy default, failure of any requested Ritz pair raises an error. Set require_convergence=false only when inspecting the returned residuals manually.","title":"Partial spectra: Arnoldi"},{"category":"section","location":"matrix_free_krylov/#Thick-restarted-harmonic-Arnoldi-near-zero","page":"Matrix-free Krylov solvers","text":"For stationary and metastable modes clustered around zero, use:\n\nmodes = pi_liouvillian_spectrum(model;\n    method=:harmonic, nev=6, krylovdim=48,\n    thickdim=12, maxrestarts=30, vectors=true)\n\nHarmonic Ritz extraction targets interior eigenvalues without factorizing a shifted Liouvillian. At each restart, converged and best unconverged Ritz vectors are retained as a thick subspace. The result additionally reports restarts, retained_dimension, target, harmonic_shift, ritz_extraction, and search_space_exhausted. While the search space is partial, an exact zero target receives a tolerance-scale negative internal shift because the stationary vector otherwise makes the harmonic pencil singular. If the full ambient space or the exact range of a MatrixFreeSymmetryProjector has been spanned, the solver instead uses ordinary Rayleigh–Ritz extraction in that complete invariant space. This removes the singular-pencil sensitivity without changing the requested residual tolerance or materializing the projected Liouvillian.\n\nIncrease thickdim for clustered modes while keeping it below krylovdim. Increase maxrestarts when Ritz residuals decrease steadily but have not reached tolerance. The lower-level interface is harmonic_arnoldi_spectrum.","title":"Thick-restarted harmonic Arnoldi near zero"},{"category":"section","location":"matrix_free_krylov/#Implicit-QR-restarted-Arnoldi","page":"Matrix-free Krylov solvers","text":"For spectral-edge modes when an unrestarted Arnoldi basis is too large, the exact-shift route applies unwanted Ritz values as implicit QR shifts of the Hessenberg factorization:\n\nmodes = pi_liouvillian_spectrum(model;\n    method=:iram,\n    nev=6,\n    sortby=:real,\n    krylovdim=40,\n    retained_dimension=12,\n    maxrestarts=30,\n    vectors=true,\n)\n\nThe direct interface is implicitly_restarted_arnoldi_spectrum, where which=:LR, :LM, or :SM controls selection.\n\nThe transformed Arnoldi relation is truncated and expanded using only mul!(y,L,x). retained_dimension must be smaller than krylovdim; retaining more directions helps clusters but leaves fewer new directions per cycle. A non-nothing target changes selection to distance from that point, although polynomial implicit restarting remains primarily an edge solver. Use harmonic Arnoldi or Jacobi–Davidson for a difficult interior cluster.\n\nA scalar Arnoldi start contains only one direction in an exactly degenerate eigenspace. Requesting every vector in a repeated eigenspace therefore requires a block method or the hard-locking Jacobi–Davidson route below.","title":"Implicit-QR restarted Arnoldi"},{"category":"section","location":"matrix_free_krylov/#Preconditioned-Jacobi–Davidson-and-hard-locking","page":"Matrix-free Krylov solvers","text":"jacobi_davidson_spectrum targets the eigenvalues nearest a chosen value and solves an inexact projected correction equation by restarted GMRES:\n\nP = schur_sector_preconditioner(\n    L, model.basis; expected_reuses=20)\nworkspace = JacobiDavidsonWorkspace(L, 40, 20)\n\nmodes = pi_liouvillian_spectrum(model;\n    method=:jd,\n    nev=6,\n    target=0,\n    krylovdim=40,\n    correction_krylovdim=20,\n    correction_maxiter=60,\n    preconditioner=P,\n    workspace=workspace,\n    vectors=true,\n)\n\nThe direct interface is jacobi_davidson_spectrum. The :jd wrapper always orders by distance from the numeric target; sortby does not replace that near-target selection.\n\nThe correction projector removes both the current Ritz direction and the already converged invariant subspace. Converged Schur directions are hard locked, so subsequent searches can resolve repeated or tightly clustered modes. The supplied preconditioner is also projected and need only implement size and ldiv!(destination,P,source); it is an approximation to the shifted correction operator, so an imperfect Schur preconditioner affects speed but not the final residual test.\n\nImportant result fields are hard_locked, correction_iterations, correction_failures, operator_applications, restarts, and restart_history. Returned right eigenvectors are reconstructed from the locked Ritz block and checked explicitly with L v-lambda v. A nonzero correction_failures is not by itself a failure: inexact corrections are intentional, and a projected Davidson direction is used when GMRES stagnates. Convergence is decided only by the outer eigenpair residuals.","title":"Preconditioned Jacobi–Davidson and hard locking"},{"category":"section","location":"matrix_free_krylov/#Matrix-free-weak-symmetry-projection","page":"Matrix-free Krylov solvers","text":"For a unitary weak symmetry, a Liouville charge satisfies\n\nUrho U^dagger=qrhoqquad q=1\n\nThe implementation diagonalizes only sector-sized Schur representations of U. Projection is block-local; neither the full symmetry superoperator nor a reduced Liouvillian matrix is constructed.\n\nsz = ComplexF64[1 0; 0 -1]\nmodes = pi_liouvillian_spectrum(model;\n    method=:harmonic, symmetry=sz, charge=1,\n    nev=4, krylovdim=32, vectors=true)\n\nThe trivial charge also accepts charge=:trivial. symmetry=:auto searches common local unitaries and accepts a projector only when deterministic matrix-free commutator probes pass. Assembled matrices use the full commutator instead of probes. For reuse, construct the projector explicitly:\n\nPq = matrixfree_symmetry_projector(model.basis, sz; charge=-1)\nPwork = SymmetryProjectorWorkspace(Pq)\nmodes = harmonic_arnoldi_spectrum(L;\n    projector=Pq, projector_workspace=Pwork, nev=4)\n\nThe ordinary mul! path uses a locked compatibility workspace and is allocation-free, so sharing one projector is safe but serialized. Parallel scans should use one explicit SymmetryProjectorWorkspace per task.\n\nAntiunitary symmetries do not define complex-linear charge sectors and cannot be used for this projection.","title":"Matrix-free weak-symmetry projection"},{"category":"section","location":"matrix_free_krylov/#Compressed-strong-symmetry-coordinates","page":"Matrix-free Krylov solvers","text":"A strong diagonal local symmetry can reduce the stored Krylov vectors to one ket/bra charge block rather than repeatedly projecting a full PI vector. For example, a parity-preserving model may be restricted with\n\nZ = Diagonal(ComplexF64[1, -1])\nrestriction = diagonal_symmetry_restriction(\n    model.basis, Z; ket_charge=1, bra_charge=1)\n\nL = compile(model; backend=:matrixfree)\nrestricted = RestrictedLiouvillian(L, restriction)\nwork = RestrictedLiouvillianWorkspace(restricted)\nrho_ss = restricted_steady_state(restricted)\n\nConstruction exhaustively checks leakage before accepting the restriction. Explicit matrices are scanned columnwise (stored nonzeros only for sparse matrices); a matrix-free source is probed once per retained coordinate. restriction_full_residual subsequently embeds a computed mode and evaluates the original generator, independently checking both the retained and omitted coordinates. Nothing is silently projected when leakage is nonzero.\n\nWith a sparse or dense source, RestrictedLiouvillian(...; backend=:auto) stores the actual submatrix. With a prepared matrix-free source containing fixed Hamiltonian, collective-dissipator, or local-gain kernels, the default :lowered backend restricts the ket and bra Schur blocks and filters gain coordinates once. Its applications, adjoints, and Krylov vectors never touch ambient PI vectors. RestrictedLiouvillianWorkspace(restricted) returns the corresponding task-owned reduced scratch. ResponseWorkspace(restricted) keeps the same reduced application path for resolvents, adjoint evolution, and sensitivities.\n\nAn explicit mask that is not a Cartesian ket-pattern by bra-pattern block, or an unsupported operator-valued prepared kernel, retains the certified :embedded fallback. That fallback still compresses Krylov storage but uses two ambient application vectors. The selected route is recorded in restricted.backend; backend=:compressed requires an explicit matrix and backend=:lowered can be requested to reject rather than fall back.\n\nSeparate ket_charge and bra_charge values expose off-diagonal operator blocks. Such a block normally has a zero physical trace and is useful for decay modes, but restricted_steady_state rejects it because it cannot contain a normalized density operator. Plans and workspaces are safe to share only according to the same immutable-plan/task-owned-workspace rules as the rest of the matrix-free API.\n\nrestriction_full_residual is deliberately different: it re-evaluates the original ambient generator to certify omitted-coordinate leakage. Repeated calls therefore need an ambient RestrictedLiouvillianWorkspace constructed from restricted.source and restricted.restriction, not the reduced workspace returned for ordinary lowered applications.","title":"Compressed strong-symmetry coordinates"},{"category":"section","location":"matrix_free_krylov/#Matrix-free-Liouvillian-gap","page":"Matrix-free Krylov solvers","text":"The gap is obtained from Ritz values with largest real part:\n\ngapinfo = pi_liouvillian_gap(model;\n    method=:iram, # or :krylov for one unrestarted Arnoldi factorization\n    nev=6,\n    krylovdim=40,\n    return_info=true,\n)\n\nImplicit QR remains a largest-real Ritz search in the gap command. The gap routine rejects method=:jd: distance from zero can miss a slowly decaying mode with large imaginary part, so near-target Jacobi–Davidson cannot certify the global gap. Use it to study a known near-zero cluster, not to replace a largest-real search.\n\nImportant fields are:\n\ngap and decay_eigenvalue;\noscillation_frequency;\nritz_residuals;\nstationary_multiplicity;\nstationary_multiplicity_certified;\nstable and spectral_abscissa.\n\nChoose nev large enough to include the stationary cluster and at least one decaying mode. A partial spectrum cannot certify the stationary multiplicity when stationary modes fill the entire requested window. The stationary_multiplicity_certified field records this distinction.\n\nHarmonic Ritz extraction must not be used as a certified global-gap method: it orders modes by distance to zero rather than by real part, and can therefore miss a slowly decaying mode with a large oscillation frequency. A request for method=:harmonic without a symmetry consequently raises an error.\n\nWith method=:harmonic, symmetry=U, charge=q, and return_info=true, the routine reports a near-zero charge-sector decay estimate. Nontrivial charge sectors are not required to contain a stationary mode. The result records scope=:charge_sector, selection=:near_zero, sector_dimension, and gap_certified. The last field is true only when the complete selected sector was extracted; otherwise inspect the Ritz residuals directly. Use the largest-real Krylov route or a complete dense symmetry-block spectrum when a global gap is required.","title":"Matrix-free Liouvillian gap"},{"category":"section","location":"matrix_free_krylov/#Matrix-free-Floquet-maps","page":"Matrix-free Krylov solvers","text":"Periodic problems have the same plan/workspace split. floquet_map prepares the RK grid and the underlying Liouvillian action, but does not construct the n_mathrmPItimes n_mathrmPI one-period matrix:\n\nperiod_map = floquet_map(prepared, period; steps=320)\nmap_work = FloquetWorkspace(period_map)\n\nimage = similar(rho0.data)\napply!(image, period_map, rho0.data, map_work)\n\nmultipliers = selected_floquet_multipliers(\n    period_map;\n    method=:block_arnoldi,\n    block_size=4,\n    which=:LM,\n    nev=min(6, size(period_map, 1)),\n    krylovdim=40,\n    vectors=true,\n)\n\nperiodic = floquet_steady_state(\n    period_map;\n    method=:krylov,\n    krylovdim=40,\n    return_info=true,\n)\n\nOrdinary Arnoldi (method=:arnoldi), thick-restarted block Arnoldi (method=:block_arnoldi), and IRAM select multipliers by a spectral criterion; which=:LM is the usual slow-decay choice. Block Arnoldi uses a fixed-capacity matrix workspace and sends each search block through one batched period-map application. Jacobi–Davidson (method=:jd) targets a specified multiplier, one by default, and is not a global spectral-radius calculation. Every selected result retains Ritz residuals, convergence flags, the full operator dimension, and whether its scope is partial. floquet_steady_state solves the trace-fixed equation (F-I)rho=0 by restarted GMRES and reports the physical period residual.\n\nfloquet_gap(period_map; return_info=true) reports residual certification for the selected multipliers. A partial largest-modulus window can identify a well-resolved candidate decay rate, but global_gap_certified becomes true only when the complete map dimension has been resolved and every required residual passes. Never promote a partial multiplier window to a global gap or stability certificate.\n\nThe single-vector forward and adjoint actions reuse the same FloquetWorkspace. For blocks, construct FloquetBatchWorkspace(map, p; mode=:forward) to retain only three stage matrices beyond the destination, or use mode=:full when exact batched adjoint actions are required. Capacity is explicit and never grows during application. The destination is the low-storage RK4 accumulator and must therefore have exactly the map's scalar type; a representable narrower input is copied into map-precision scratch. The adjoint reverses the actual finite-step RK4 computation, so apply_adjoint! is the numerical adjoint of the discretized period map. This is important for response calculations and differs from independently discretizing a continuous adjoint equation.\n\nFor a certified strong diagonal symmetry, first build a SymmetryCoordinateRestriction, then call restricted_floquet_map(period_map, restriction). Selected multiplier and fixed-point Krylov vectors are stored in reduced coordinates. Period applications still use caller-owned ambient scratch, and the returned fixed-point report includes the full-space residual and leakage. The restriction is accepted only after exhaustive invariance checks.\n\nConverge two numerical layers separately: increase the period-map steps until the physical results are stable, then increase Krylov dimensions and iteration limits until the reported linear or Ritz residuals pass. The dense floquet_propagator remains useful as a small-problem reference or when the complete explicit channel is genuinely required.","title":"Matrix-free Floquet maps"},{"category":"section","location":"matrix_free_krylov/#Matrix-free-response-and-adjoint-analysis","page":"Matrix-free Krylov solvers","text":"The response tools accept the same prepared matrix-free source. A ResponseWorkspace can retain restarted-GMRES storage, adaptive exponential- action storage, or both:\n\nwork = ResponseWorkspace(\n    prepared; krylovdim=40, expv_krylovdim=40, mode=:both)\n\nresolvent = resolvent_norm(\n    prepared, 0.2im;\n    method=:krylov,\n    workspace=work,\n    return_info=true,\n)\n\nA_t = adjoint_evolve(\n    prepared, A, 2.0;\n    method=:krylov,\n    workspace=work,\n)\n\ntau = integrated_correlation_time(\n    prepared, rho_ss, A;\n    method=:krylov,\n    workspace=work,\n    return_info=true,\n)\n\nchi = steady_state_susceptibility(\n    prepared, rho_ss, dL;\n    observable=A,\n    method=:krylov,\n    workspace=work,\n    return_info=true,\n)\n\nHere A is a PIOperator, rho_ss is a stationary PIState, and dL is the autonomous derivative of the Liouvillian with respect to the parameter of interest. integrated_correlation_time and steady_state_susceptibility solve trace-fixed Poisson/tangent equations and return the raw physical residual and trace error. The tangent state is not normalized or positivity-projected.\n\nFor a matrix-free source, resolvent_norm uses power iteration on the forward and adjoint resolvents, with two shifted GMRES solves per outer iteration. It therefore requires an explicit adjoint action. Nonconverged shifted solves or power iterations raise instead of returning a finite value, but a converged result is still a numerical estimate rather than a rigorous upper bound. pseudospectral_abscissa repeats this calculation on a supplied complex grid; reuse one mode=:linear workspace to avoid rebuilding the dominant Krylov arrays at every point.\n\nadjoint_evolve applies adaptive krylov_expv! to the exact prepared adjoint. Use mode=:evolution when this is the only requested operation. Use mode=:linear for resolvents and trace-fixed response solves, or mode=:both when one task will call both kinds sequentially. As with every mutable solver workspace, do not share a ResponseWorkspace between concurrent tasks.","title":"Matrix-free response and adjoint analysis"},{"category":"section","location":"matrix_free_krylov/#Choosing-tolerances-and-Krylov-dimensions","page":"Matrix-free Krylov solvers","text":"Start with krylovdim between 30 and 60. Increase it when:\n\nGMRES reaches maxiter without satisfying the residual tolerance;\nrequested Ritz residuals remain above tolerance;\nthe Liouvillian is strongly nonnormal;\nseveral eigenvalues cluster near zero.\n\nA larger Krylov basis usually improves convergence but uses more memory and orthogonalization time. For a fixed memory budget, increase maxiter to allow more GMRES restarts. Basic method=:arnoldi uses one factorization; :krylov remains a compatibility alias on the spectral API. method=:harmonic supports thick restarting through maxrestarts. The lower-level implicit-QR and Jacobi–Davidson routines add bounded-memory restarts and, for Jacobi–Davidson, hard locking plus preconditioned correction solves.\n\nWhen unpreconditioned GMRES stagnates, try the Schur-sector preconditioner before making the Krylov basis very large. Compare both the number of iterations and total setup-plus-solve time: preconditioning is not guaranteed to reduce iterations for every model.\n\nAlways check the physical residual, trace error, and (when required) positivity of the resulting state. Krylov convergence alone does not certify that an approximate stationary vector is a physical density operator.","title":"Choosing tolerances and Krylov dimensions"},{"category":"section","location":"matrix_free_krylov/#Choosing-between-methods","page":"Matrix-free Krylov solvers","text":"Task Recommended method\nSmall or moderate sparse steady state method=:direct\nLarge matrix-free steady state method=:krylov\nSparse eigenvalue nearest a chosen shift method=:shiftinvert\nComplete PI spectrum method=:dense\nModes with largest real part method=:arnoldi\nInterior modes nearest zero method=:harmonic\nRestarted spectral-edge extraction method=:iram / implicitly_restarted_arnoldi_spectrum\nHard nonnormal/degenerate interior cluster method=:jd / jacobi_davidson_spectrum\nNear-zero charge-sector decay estimate method=:harmonic, symmetry=..., charge=..., return_info=true\nSelected Floquet multipliers floquet_map, then selected_floquet_multipliers\nMatrix-free periodic state floquet_steady_state(floquet_map(...); method=:krylov)\nResolvent or pseudospectral estimate ResponseWorkspace(...; mode=:linear), then shifted-GMRES response tools\nAdjoint observable evolution ResponseWorkspace(...; mode=:evolution), then adjoint_evolve\nDiagnose a degenerate nullspace method=:svd for manageable dimensions\n\nThe Krylov steady-state formulation assumes the trace constraint selects a unique solution. If the Liouvillian has multiple stationary states, use a nullspace-aware method or impose additional conserved-charge constraints.","title":"Choosing between methods"},{"category":"section","location":"model_code_generator/#PI-model-code-generator","page":"Model code generator","text":"This browser-only assistant turns a small, explicit subset of LaTeX model notation into a commented Julia program for PI stationary states, streamed observable dynamics, selected Liouvillian spectra, or certification-aware gap estimation. It supports an ordinary PI ensemble and two finite-cutoff Markovian embeddings: one identical pseudomode per constituent or one pseudomode shared by the whole ensemble. It uses physical package terms, complete PI bases, prepared observable geometry, factorized composite operators, explicit memory budgets, and deterministic or trajectory routes where the corresponding package API is available.\n\nThe assistant deliberately does not send formulas to a server or a language model. Translation is deterministic and restricted: unsupported or ambiguous notation produces an error instead of guessed physics. Besides a single Julia file, it can download a dependency-free experiment bundle containing the program, a normalized JSON manifest, and a plain-text run guide.\n\n<div id=\"pid-code-generator\">\n  <div class=\"pid-privacy-banner\">\n    <span class=\"pid-privacy-mark\">Local</span>\n    <div>\n      <strong>Your formulas stay in this browser.</strong>\n      This page generates code but cannot execute Julia. Review the PI\n      assumptions, channel semantics, numerical convergence, and any\n      stationarity, uniqueness, or spectral-completeness claim before using a\n      result.\n    </div>\n  </div>\n\n  <div class=\"pid-layout\">\n    <form id=\"pid-generator-form\" class=\"pid-panel\">\n      <div class=\"pid-panel-heading\">\n        <h2>1. Describe the model</h2>\n        <label class=\"pid-field\">\n          <span class=\"pid-mini-label\">Start from</span>\n          <select id=\"pid-preset\">\n            <option value=\"driven\">Driven qubits with local bath</option>\n            <option value=\"collective\">Local and collective decay</option>\n            <option value=\"lmg\">Collective LMG polynomial</option>\n            <option value=\"qutrit\">Spin-1 qutrit ensemble</option>\n            <option value=\"dynamics\">Streaming relaxation dynamics</option>\n            <option value=\"trajectoryDynamics\">Trajectory relaxation dynamics</option>\n            <option value=\"spectrum\">Near-zero Liouvillian spectrum</option>\n            <option value=\"gap\">Liouvillian gap diagnostics</option>\n            <option value=\"localPseudomode\">Identical local pseudomodes</option>\n            <option value=\"globalPseudomode\">One shared pseudomode</option>\n          </select>\n        </label>\n      </div>\n\n      <label class=\"pid-field pid-architecture-field\">\n        <span class=\"pid-label\">System architecture</span>\n        <select id=\"pid-architecture\">\n          <option value=\"pi\">Ordinary PI ensemble</option>\n          <option value=\"local-pseudomode\">\n            Identical local pseudomode per constituent\n          </option>\n          <option value=\"global-pseudomode\">\n            One pseudomode shared by the ensemble\n          </option>\n        </select>\n        <span class=\"pid-hint\">\n          This choice fixes the tensor topology. Local modes become part of\n          each permuted supersite; a shared mode remains a separate composite\n          factor.\n        </span>\n      </label>\n\n      <div class=\"pid-grid-two\">\n        <label class=\"pid-field\">\n          <span class=\"pid-label\">Particles, N</span>\n          <input id=\"pid-particle-count\" type=\"number\" min=\"1\" step=\"1\"\n                 value=\"8\" required>\n        </label>\n        <label class=\"pid-field\">\n          <span class=\"pid-label\">Local dimension, d</span>\n          <input id=\"pid-local-dimension\" type=\"number\" min=\"2\" step=\"1\"\n                 value=\"2\" required>\n        </label>\n      </div>\n\n      <label class=\"pid-field\">\n        <span class=\"pid-label\">Requested calculation</span>\n        <select id=\"pid-calculation\">\n          <option value=\"steady-observable\">\n            Stationary state and expectation value\n          </option>\n          <option value=\"steady-state\">Stationary density operator only</option>\n          <option value=\"dynamics-observable\">\n            Time-dependent expectation value\n          </option>\n          <option value=\"liouvillian-spectrum\">\n            Selected Liouvillian spectrum\n          </option>\n          <option value=\"liouvillian-gap\">Liouvillian gap</option>\n        </select>\n      </label>\n\n      <label class=\"pid-field\">\n        <span class=\"pid-label\">Generated workflow</span>\n        <select id=\"pid-workflow\">\n          <option value=\"direct-api\">\n            Direct high-level package API\n          </option>\n          <option value=\"verified-experiment\">\n            Typed PIExperiment with planning and verification\n          </option>\n        </select>\n        <span class=\"pid-hint\">\n          The typed experiment route records an explainable resource plan,\n          verification report, and reproducibility digest. It currently covers\n          deterministic stationary states and dynamics for ordinary PI and\n          identical-local-pseudomode models.\n        </span>\n      </label>\n\n      <label id=\"pid-method-section\" class=\"pid-field\">\n        <span class=\"pid-label\">Calculation method</span>\n        <select id=\"pid-steady-method\">\n          <option value=\"deterministic\">\n            Deterministic density-operator route\n          </option>\n          <option value=\"trajectory\">\n            Quantum trajectories\n          </option>\n        </select>\n        <span class=\"pid-hint\">\n          Stationary trajectories are available for ordinary PI ensembles and\n          identical local pseudomodes. Observable trajectories are also\n          available for a shared global mode. Deterministic shared-mode\n          observable dynamics is intentionally rejected until it has a\n          memory-guarded state-free streamer.\n        </span>\n      </label>\n\n      <fieldset id=\"pid-initial-state-section\"\n                class=\"pid-subpanel\" hidden>\n        <legend>Initial product state</legend>\n        <label class=\"pid-field\">\n          <span class=\"pid-label\">Initial local level, 1…d</span>\n          <input id=\"pid-initial-level\" type=\"number\"\n                 min=\"1\" step=\"1\" value=\"1\">\n        </label>\n        <span class=\"pid-hint\">\n          Every physical system starts in this one-based computational level.\n          Local and shared pseudomodes start in vacuum. Test other PI initial\n          states manually when strong symmetries make the result\n          initial-state dependent.\n        </span>\n      </fieldset>\n\n      <fieldset id=\"pid-trajectory-section\"\n                class=\"pid-subpanel\" hidden>\n        <legend>Quantum-trajectory controls</legend>\n        <div class=\"pid-grid-two\">\n          <label class=\"pid-field\">\n            <span class=\"pid-label\">Independent trajectories</span>\n            <input id=\"pid-trajectory-count\" type=\"number\" min=\"2\" step=\"1\"\n                   value=\"512\">\n          </label>\n          <label class=\"pid-field\">\n            <span class=\"pid-label\">Integrator step, dt</span>\n            <input id=\"pid-trajectory-dt\" type=\"number\"\n                   min=\"0\" step=\"any\" value=\"0.002\">\n          </label>\n          <label class=\"pid-field\">\n            <span class=\"pid-label\">Maximum jump probability</span>\n            <input id=\"pid-trajectory-max-jump-probability\" type=\"number\"\n                   min=\"0\" max=\"1\" step=\"any\" value=\"0.02\">\n          </label>\n          <label class=\"pid-field\">\n            <span class=\"pid-label\">Random seed</span>\n            <input id=\"pid-trajectory-seed\" type=\"number\"\n                   min=\"0\" step=\"1\" value=\"2026\">\n          </label>\n        </div>\n        <div id=\"pid-trajectory-stationary-controls\" class=\"pid-grid-two\">\n          <label class=\"pid-field\">\n            <span class=\"pid-label\">Settling time</span>\n            <input id=\"pid-trajectory-settling-time\" type=\"number\"\n                   min=\"0\" step=\"any\" value=\"50\">\n          </label>\n          <label class=\"pid-field\">\n            <span class=\"pid-label\">Samples per trajectory</span>\n            <input id=\"pid-trajectory-samples\" type=\"number\"\n                   min=\"1\" step=\"1\" value=\"5\">\n          </label>\n          <label class=\"pid-field\">\n            <span class=\"pid-label\">Sampling interval</span>\n            <input id=\"pid-trajectory-sampling-interval\" type=\"number\"\n                   min=\"0\" step=\"any\" value=\"2\">\n          </label>\n        </div>\n        <span class=\"pid-hint\">\n          These are explicit starting values, not convergence guarantees.\n          Increase the path count and reduce <code>dt</code>. For stationary\n          estimates, also converge the settling time, sampling interval, and\n          sampling window.\n        </span>\n      </fieldset>\n\n      <fieldset id=\"pid-dynamics-section\"\n                class=\"pid-subpanel\" hidden>\n        <legend>Observable dynamics</legend>\n        <div class=\"pid-grid-two\">\n          <label class=\"pid-field\">\n            <span class=\"pid-label\">Start time</span>\n            <input id=\"pid-dynamics-start-time\" type=\"number\"\n                   step=\"any\" value=\"0\">\n          </label>\n          <label class=\"pid-field\">\n            <span class=\"pid-label\">Final time</span>\n            <input id=\"pid-dynamics-final-time\" type=\"number\"\n                   step=\"any\" value=\"10\">\n          </label>\n          <label class=\"pid-field\">\n            <span class=\"pid-label\">Output samples</span>\n            <input id=\"pid-dynamics-samples\" type=\"number\"\n                   min=\"2\" step=\"1\" value=\"101\">\n          </label>\n          <label id=\"pid-dynamics-steps-field\" class=\"pid-field\">\n            <span class=\"pid-label\">RK4 steps per output interval</span>\n            <input id=\"pid-dynamics-steps\" type=\"number\"\n                   min=\"1\" step=\"1\" value=\"16\">\n          </label>\n        </div>\n        <span class=\"pid-hint\">\n          Deterministic dynamics streams one observable with matrix-free RK4.\n          Trajectory dynamics instead reports the ensemble mean, standard\n          error, and 95% confidence interval. Neither route retains state\n          histories.\n        </span>\n      </fieldset>\n\n      <fieldset id=\"pid-spectrum-section\"\n                class=\"pid-subpanel\" hidden>\n        <legend>Selected Liouvillian spectrum</legend>\n        <div class=\"pid-grid-two\">\n          <label class=\"pid-field\">\n            <span class=\"pid-label\">Spectral target</span>\n            <select id=\"pid-spectrum-target\">\n              <option value=\"largest-real\">Largest real part</option>\n              <option value=\"near-zero\">Near zero</option>\n              <option value=\"largest-magnitude\">Largest magnitude</option>\n            </select>\n          </label>\n          <label class=\"pid-field\">\n            <span class=\"pid-label\">Requested eigenvalues</span>\n            <input id=\"pid-spectrum-nev\" type=\"number\"\n                   min=\"1\" step=\"1\" value=\"6\">\n          </label>\n          <label class=\"pid-field\">\n            <span class=\"pid-label\">Random seed</span>\n            <input id=\"pid-spectrum-seed\" type=\"number\"\n                   min=\"0\" step=\"1\" value=\"2026\">\n          </label>\n        </div>\n        <span class=\"pid-hint\">\n          Automatic selection uses a complete dense spectrum only when it fits\n          the budget; otherwise it uses a matrix-free selected eigensolver. A\n          partial spectrum is not a certified global gap.\n        </span>\n      </fieldset>\n\n      <fieldset id=\"pid-gap-section\"\n                class=\"pid-subpanel\" hidden>\n        <legend>Liouvillian gap</legend>\n        <div class=\"pid-grid-two\">\n          <label class=\"pid-field\">\n            <span class=\"pid-label\">Requested Ritz values</span>\n            <input id=\"pid-gap-nev\" type=\"number\"\n                   min=\"2\" step=\"1\" value=\"8\">\n          </label>\n          <label class=\"pid-field\">\n            <span class=\"pid-label\">Krylov dimension</span>\n            <input id=\"pid-gap-krylovdim\" type=\"number\"\n                   min=\"3\" step=\"1\" value=\"32\">\n          </label>\n        </div>\n        <span class=\"pid-hint\">\n          The Krylov dimension must exceed the Ritz count. The generated\n          program prints stability, stationary multiplicity, and certification\n          flags; never report the numerical gap as certified unless\n          <code>gap_certified</code> is true.\n        </span>\n      </fieldset>\n\n      <label class=\"pid-field\">\n        <span class=\"pid-label\">Bare-system Hamiltonian H in LaTeX</span>\n        <textarea id=\"pid-hamiltonian\"\n          class=\"pid-latex-input\"\n          spellcheck=\"false\"\n          placeholder=\"\\Omega J_x + \\chi J_z^2\"></textarea>\n        <span class=\"pid-hint\">\n          Use collective spin symbols J<sub>x,y,z,+,-</sub>, or an explicit\n          sum such as \\sum_i \\sigma_x^{(i)}. Leave empty for a purely\n          dissipative model. For a pseudomode architecture, do not add the\n          oscillator frequency or system--mode coupling here; the structured\n          controls below add them exactly once.\n        </span>\n      </label>\n\n      <fieldset id=\"pid-pseudomode-section\"\n                class=\"pid-subpanel pid-pseudomode-panel\" hidden>\n        <legend>Finite-cutoff pseudomode</legend>\n        <p id=\"pid-local-pseudomode-description\"\n           class=\"pid-architecture-note\" hidden>\n          Every constituent has its own identical mode. Permutations act on\n          complete system+mode supersites, so the embedding remains one PI\n          problem.\n        </p>\n        <p id=\"pid-global-pseudomode-description\"\n           class=\"pid-architecture-note\" hidden>\n          One mode is shared by all constituents. The generated solver keeps\n          the PI system and oscillator as factorized composite coordinates.\n        </p>\n\n        <div class=\"pid-pseudomode-grid\">\n          <label class=\"pid-field\">\n            <span class=\"pid-label\">Fock cutoff, nmax</span>\n            <input id=\"pid-pseudomode-cutoff\" type=\"number\" min=\"0\" step=\"1\"\n                   value=\"2\">\n            <span class=\"pid-hint\">\n              Retained levels are 0,…,nmax. Converge this approximation.\n            </span>\n          </label>\n          <label class=\"pid-field\">\n            <span class=\"pid-label\">Mode frequency</span>\n            <input id=\"pid-pseudomode-frequency\"\n                   class=\"pid-latex-input\" type=\"text\" spellcheck=\"false\"\n                   value=\"\\omega_c\" placeholder=\"\\omega_c\">\n          </label>\n          <label class=\"pid-field\">\n            <span class=\"pid-label\">Mode damping</span>\n            <input id=\"pid-pseudomode-damping\"\n                   class=\"pid-latex-input\" type=\"text\" spellcheck=\"false\"\n                   value=\"\\kappa\" placeholder=\"\\kappa\">\n          </label>\n          <label class=\"pid-field\">\n            <span class=\"pid-label\">Thermal occupation</span>\n            <input id=\"pid-pseudomode-thermal-occupation\"\n                   class=\"pid-latex-input\" type=\"text\" spellcheck=\"false\"\n                   value=\"0\" placeholder=\"nbar\">\n          </label>\n          <label class=\"pid-field pid-span-two\">\n            <span class=\"pid-label\">System coupling seed</span>\n            <input id=\"pid-pseudomode-coupling-operator\"\n                   class=\"pid-latex-input\" type=\"text\" spellcheck=\"false\"\n                   value=\"\\sigma_-\" placeholder=\"\\sigma_- or j_z\">\n            <span class=\"pid-hint\">\n              Enter one linear local spin operator. The selected architecture\n              performs the corresponding local-supersite or collective lift.\n            </span>\n          </label>\n          <label class=\"pid-field\">\n            <span class=\"pid-label\">Rotating coupling strength</span>\n            <input id=\"pid-pseudomode-coupling-strength\"\n                   class=\"pid-latex-input\" type=\"text\" spellcheck=\"false\"\n                   value=\"g\" placeholder=\"g\">\n          </label>\n          <label class=\"pid-field\">\n            <span class=\"pid-label\">Counter-rotating strength</span>\n            <input id=\"pid-pseudomode-counterrotating-strength\"\n                   class=\"pid-latex-input\" type=\"text\" spellcheck=\"false\"\n                   value=\"0\" placeholder=\"0\">\n          </label>\n        </div>\n        <span class=\"pid-hint\">\n          With this package's dissipator convention, damping\n          <code>kappa</code> makes the free-mode amplitude decay at\n          <code>kappa/2</code>. The generated program reports the highest\n          retained Fock-level population as a cutoff diagnostic.\n        </span>\n      </fieldset>\n\n      <div class=\"pid-field\">\n        <span class=\"pid-label\">Dissipative channels</span>\n        <div id=\"pid-jump-list\" class=\"pid-jump-list\"></div>\n        <div class=\"pid-button-row\">\n          <button id=\"pid-add-local-jump\" type=\"button\"\n                  class=\"pid-button pid-button-quiet\">+ Local channel</button>\n          <button id=\"pid-add-collective-jump\" type=\"button\"\n                  class=\"pid-button pid-button-quiet\">+ Collective channel</button>\n        </div>\n        <span class=\"pid-hint\">\n          Enter only the bare-system jump seed. The selector distinguishes\n          \\sum_i D[l_i] from D[\\sum_i l_i]; the assistant never guesses this\n          physically important choice. In a pseudomode embedding, mode damping\n          is set separately above.\n        </span>\n      </div>\n\n      <label id=\"pid-observable-section\" class=\"pid-field\">\n        <span class=\"pid-label\">Observable in LaTeX</span>\n        <input id=\"pid-observable\"\n               class=\"pid-latex-input\"\n               type=\"text\"\n               spellcheck=\"false\"\n               value=\"J_z/N\"\n               placeholder=\"J_z/N or J_x^2/N^2\">\n        <span class=\"pid-hint\">\n          A linear collective observable uses a prepared one-body contraction.\n          A polynomial such as J_x^2 remains entirely in PI coordinates.\n        </span>\n      </label>\n\n      <fieldset id=\"pid-analysis-section\"\n                class=\"pid-subpanel\" hidden>\n        <legend>Optional stationary-state analyses</legend>\n        <div class=\"pid-analysis-grid\">\n          <label class=\"pid-check-field\">\n            <input id=\"pid-analysis-purity\" type=\"checkbox\">\n            <span>System purity</span>\n          </label>\n          <label class=\"pid-check-field\">\n            <input id=\"pid-analysis-entropy\" type=\"checkbox\">\n            <span>System von Neumann entropy</span>\n          </label>\n          <label class=\"pid-check-field\">\n            <input id=\"pid-analysis-one-body-rdm\" type=\"checkbox\">\n            <span>One-body density matrix</span>\n          </label>\n          <label class=\"pid-field\">\n            <span class=\"pid-label\">Collective QFI generator</span>\n            <select id=\"pid-analysis-qfi-axis\">\n              <option value=\"none\">None</option>\n              <option value=\"x\">Collective x axis</option>\n              <option value=\"y\">Collective y axis</option>\n              <option value=\"z\">Collective z axis</option>\n            </select>\n          </label>\n        </div>\n        <span class=\"pid-hint\">\n          For a pseudomode model these quantities refer to the physical systems\n          after tracing out all retained pseudomodes. Purity, entropy, and QFI\n          of a trajectory-averaged state are nonlinear plug-in estimates, not\n          observables with the reported path standard error.\n        </span>\n      </fieldset>\n\n      <label class=\"pid-field\">\n        <span class=\"pid-label\">Numerical parameter values</span>\n        <textarea id=\"pid-parameters\"\n          class=\"pid-latex-input\"\n          spellcheck=\"false\"\n          placeholder=\"\\Omega = 0.7&#10;\\gamma = 0.1\"></textarea>\n        <span class=\"pid-hint\">\n          One finite numerical assignment per line. Missing parameters receive\n          an explicit <code>1.0 # TODO</code> placeholder in the output.\n        </span>\n      </label>\n\n      <label class=\"pid-field\">\n        <span class=\"pid-label\">Memory budget, MiB</span>\n        <input id=\"pid-memory-budget\" type=\"number\"\n               min=\"1\" step=\"1\" value=\"512\" required>\n        <span class=\"pid-hint\">\n          This explicit finite budget guards model preparation, task-owned\n          workspaces, and requested output. Raise it deliberately only after\n          inspecting the generated resource route.\n        </span>\n      </label>\n\n      <button type=\"submit\" class=\"pid-button pid-button-primary\">\n        Generate Julia code\n      </button>\n    </form>\n\n    <section class=\"pid-panel\" aria-labelledby=\"pid-output-heading\">\n      <div class=\"pid-panel-heading\">\n        <h2 id=\"pid-output-heading\">2. Review and run</h2>\n        <div class=\"pid-output-actions\">\n          <button id=\"pid-copy-code\" type=\"button\"\n                  class=\"pid-button pid-button-quiet\">Copy</button>\n          <button id=\"pid-download-code\" type=\"button\"\n                  class=\"pid-button pid-button-quiet\">Download .jl</button>\n          <button id=\"pid-download-bundle\" type=\"button\"\n                  class=\"pid-button pid-button-quiet\">\n            Download experiment bundle\n          </button>\n        </div>\n      </div>\n      <div id=\"pid-generator-summary\" class=\"pid-summary\"></div>\n      <div id=\"pid-generator-status\" class=\"pid-status\"\n           role=\"status\" aria-live=\"polite\">\n        Loading the generator…\n      </div>\n      <div class=\"pid-code-shell\" role=\"region\" tabindex=\"0\"\n           aria-labelledby=\"pid-output-heading\">\n        <pre id=\"pid-generated-code\" class=\"pid-code\"\n             aria-label=\"Generated Julia code\"></pre>\n      </div>\n      <ul id=\"pid-generator-warnings\" class=\"pid-warning-list\"></ul>\n    </section>\n  </div>\n</div>","title":"PI model code generator"},{"category":"section","location":"model_code_generator/#Supported-notation","page":"Model code generator","text":"The grammar is intentionally small enough that the generated physics is auditable:\n\nLaTeX ingredient Meaning in generated code\nJ_xJ_yJ_zJ_pm Collective spin J_a=sum_i j_a^(i)\nj_xj_yj_zj_pm One-site spin-(d-1)2 matrix, accepted for local jump seeds\nsigma_xsigma_ysigma_zsigma_pm Qubit Pauli/lowering/raising matrices; requires d = 2\nsum_i sigma_a^(i) Explicit Pauli sum; for a=xyz, this is 2J_a\n+, -, products, /, ^2 Scalar combinations and collective polynomials\n\\frac{a}{b}, \\sqrt{a} Scalar fraction and square root\nnamed Greek or ASCII scalars Parameters defined in the numerical-value box\n\nThe pseudomode frequency, damping, thermal occupation, and two coupling strengths use the same real-scalar grammar. The system–mode coupling seed must be one linear local operator j_a or sigma_a. The generated interaction is the package PseudomodeCoupling: the rotating part is g L a^dagger + g^* L^dagger a, with an optional counter-rotating strength. The assistant does not infer a coupling normalization or Kac scaling.\n\nFor Hamiltonians, linear collective expressions lower to LocalHamiltonian, while collective polynomials such as J_z^2 use a compressed PIOperator and DirectPIHamiltonian. A local channel lowers to LocalJump; a collective channel lowers to CollectiveJump. No d^N-dimensional state or operator is generated.\n\nThe rate box multiplies the standard package dissipator mathcal DLrho=Lrho L^dagger-L^dagger Lrho2. A coefficient placed inside L is therefore squared by the dissipator: write either rate \\gamma with operator j_-, or rate 1 with operator \\sqrt{\\gamma}j_-, but do not count the same rate twice.\n\nAn unsummed local observable such as j_z is interpreted as the identical one-site expectation langle j_z^(1)rangle=N^-1langle J_zrangle. For clarity in papers, prefer writing the normalization explicitly as J_zN.","title":"Supported notation"},{"category":"section","location":"model_code_generator/#Composite-and-pseudomode-routes","page":"Model code generator","text":"The architecture selector fixes the physical topology:\n\nArchitecture Generated representation\nOrdinary PI ensemble Complete PIBasis(N, d)\nIdentical local pseudomodes Complete PI basis of system+mode PISupersite objects\nOne shared pseudomode PI system factor times one finite-mode operator factor\n\nFor a local mode, the supersite dimension is D=d(n_max+1), and the complete PI coordinate count grows as binomN+D^2-1N. Increasing the oscillator cutoff can therefore be expensive even at modest N. For one shared mode, the composite coordinate count is pi_dimension(system_basis) * (nmax + 1)^2; the generated code never forms its global Kronecker superoperator.\n\nStationary-state programs for both pseudomode routes print the population of the highest retained oscillator level. This is a useful warning signal, not a convergence proof: repeat the calculation at larger nmax and compare the observables or reduced states of interest. The global stationary route evaluates a requested spin observable on trace_pseudomodes(rho_ss, embedding). The local route lifts the spin observable into each system+mode supersite without reconstructing a D^N-dimensional state.","title":"Composite and pseudomode routes"},{"category":"section","location":"model_code_generator/#Calculation-and-method-compatibility","page":"Model code generator","text":"The calculation and method selectors are independent because they answer different questions: the first chooses the requested output, while the second chooses deterministic density-operator propagation or stochastic unravelling. The implemented combinations are:\n\nCalculation Ordinary PI Identical local modes One shared mode\nDeterministic stationary state or observable Supported Supported Factorized matrix-free GMRES\nTrajectory stationary state or observable Supported Supported Not currently supported\nDeterministic observable dynamics State-free matrix-free stream State-free PI-supersite stream Not currently supported\nTrajectory observable dynamics Online path statistics Online PI-supersite statistics Factorized composite trajectories\nSelected Liouvillian spectrum Supported Supported Factorized matrix-free spectrum\nLiouvillian gap Krylov route with certification metadata Same Factorized matrix-free route\n\nThe browser never silently changes an incompatible choice. It leaves the selection visible and reports the core validation error, so the numerical method is always an explicit user decision.","title":"Calculation and method compatibility"},{"category":"section","location":"model_code_generator/#Direct-and-verified-workflows","page":"Model code generator","text":"The direct workflow emits the ordinary high-level package calls documented in the table above. It is the route to use for trajectories, selected spectra, gaps, a shared global pseudomode, or state-free deterministic dynamics.\n\nThe typed workflow emits a PIExperiment, calls explain_experiment before the solve, and executes it with verified_solve. The resulting program prints the selected route, verification report, and deterministic provenance digest. It also includes an optional, commented save_experiment call for a portable result directory. This workflow currently accepts deterministic stationary states and deterministic dynamics on an ordinary PI model or an identical local-pseudomode supersite model. Unsupported combinations are rejected; the assistant does not fall back to the direct workflow silently.\n\nVerified dynamics retains the state at every requested output time so that physicality can be checked. The package preflight counts this history against the declared memory budget. Select the direct workflow when only a streamed observable is needed and retaining those states would be wasteful.\n\nEvery generated program reports the retained representation, exact coordinate count, and memory budget before solving. The browser also reports the exact bytes in one ComplexF64 coordinate vector. That number is deliberately labelled a lower bound: prepared geometry, task-owned workspaces, Krylov history, and output can dominate it. The Julia-side resource preflight is authoritative for components it marks as measured or bounded and reports :unknown for exclusions rather than claiming a certified fit. Generated stationary-trajectory programs preflight known raw-model preparation costs before constructing the channel-resolved plan and pass the same budget to trajectory_steady_state, which counts the retained plan/workspace and its predictable seed, sampling, accumulator, and result arrays. Allocator metadata and RNG implementation storage remain documented exclusions.","title":"Direct and verified workflows"},{"category":"section","location":"model_code_generator/#Stationary-states-and-optional-analyses","page":"Model code generator","text":"For a deterministic stationary calculation, ordinary and local-pseudomode programs prepare the Liouvillian and solve its trace-constrained null-vector problem. The shared-global-mode program instead keeps the composite generator factorized and passes the embedding directly to matrix-free GMRES. The trajectory alternative prepares a channel-resolved TrajectoryPlan, reuses a fixed-capacity TrajectoryBatchWorkspace, and calls trajectory_steady_state. Post-settling states are reduced online, so no path history is retained.\n\nFor the shared-global-mode deterministic route, the generated checks certify the full composite trace and Hermiticity and validate the reduced physical system state. They do not certify positivity of the complete composite density operator. A calculation that needs that stronger check must use a separately budgeted full-composite positivity analysis; reduced-state positivity alone is not equivalent.\n\nThe trajectory route emits a reproducible seed and an explicit pure product initial state. For a pseudomode model, each physical system starts in the selected one-based local level and every finite mode starts in vacuum. The stationary result reports the path-to-path Hilbert–Schmidt standard error together with the Liouvillian residual, relative residual, and trace error.\n\nThose numbers are diagnostics, not certificates. Independently converge the settling time, fixed integration step, sampling interval and window, and number of independent trajectories. Samples from the same trajectory are correlated, so the estimator averages them within each path before computing path-to-path uncertainty. Strong symmetries or a nonunique stationary subspace can retain dependence on the initial product state; repeat the run from other PI initial states when that distinction matters.\n\nThe history-free trajectory steady-state helper currently accepts ordinary PIModel/TrajectoryPlan sources and the PI model produced by pseudomode_model. It does not yet accept GlobalPseudomodeModel, so the assistant rejects a trajectory request for one shared global mode instead of silently emitting a different approximation.\n\nPurity, base-two von Neumann entropy, the one-body density matrix, and collective-axis QFI can be appended to either stationary route. For a pseudomode architecture, these analyses refer to the physical-system state after all retained modes have been traced out. The local-mode route prepares and reuses an exact sparse LocalFactorTracePlan; it does not reconstruct the supersite Hilbert space.\n\nPurity, entropy, and QFI are nonlinear functions of a trajectory-averaged density operator. Their values are therefore plug-in estimates: the standard error reported for the state is not an uncertainty bar for those nonlinear quantities. Converge them directly against independent ensembles or path count.","title":"Stationary states and optional analyses"},{"category":"section","location":"model_code_generator/#Streamed-observable-dynamics","page":"Model code generator","text":"Every dynamics program constructs an explicit computational product state and a finite output grid. Deterministic ordinary-PI and local-pseudomode routes use matrix-free RK4 through solve_dynamics with save_states=false; only the requested observable series is retained. Refine the RK4 steps per output interval until the curve is stable.\n\nThe trajectory route calls quantum_trajectories with fixed-capacity, task-owned worker workspaces and online observable statistics. It prints time, ensemble mean, standard error, and the selected confidence interval without retaining trajectories or state histories. Refine both the fixed integration step and the number of statistically independent paths.\n\nShared-mode trajectory dynamics monitors the explicit pseudomode damping channels through a CompositeTrajectoryPlan. Bare-system jump terms remain in the unconditional background and are not individually unravelled. This distinction matters for jump-resolved physical interpretations even though the unconditional evolution is retained.","title":"Streamed observable dynamics"},{"category":"section","location":"model_code_generator/#Spectra-and-gaps","page":"Model code generator","text":"The selected-spectrum calculation calls liouvillian_spectrum with an explicit target, eigenvalue count, seed, and memory budget. Automatic selection uses a complete dense spectrum only when its resource preflight fits; otherwise it selects a matrix-free Krylov route. A selected set of modes is partial unless the returned metadata establishes completeness, so it must not be presented as a certified global gap.\n\nThe dedicated gap calculation instead calls pi_liouvillian_gap through a largest-real Krylov route and prints the numerical gap together with stability, stationary multiplicity, and certification flags. The Krylov dimension must exceed the requested Ritz count. Treat the result as certified only when gap_certified is true; a plausible positive number alone is insufficient.","title":"Spectra and gaps"},{"category":"section","location":"model_code_generator/#Boundaries-of-the-assistant","page":"Model code generator","text":"The generator rejects site-dependent couplings, nearest-neighbour chains, arbitrary coupling graphs, time-dependent coefficients, multiple pseudomodes per constituent, and general LaTeX. The architecture selector covers the two specific one-mode Markovian embeddings above; it does not parse arbitrary generic composite tensor expressions such as A \\otimes B. Those cases require a deliberate translation using the framework guide, symmetric p-body terms, local-pseudomode workflow, shared-pseudomode workflow, or the composite-system workflow.\n\nThe generator emits Float64 model data. Wider or lower precision needs explicitly typed matrices, geometry, tolerances, and a compatible matrix-free solver; follow the matrix-free Krylov guide rather than changing only one literal type.\n\nThe generated program uses PIBasis(N, d) rather than guessing a fully-symmetric-sector restriction. It also does not guess a population-only backend: those optimizations require structural certification by the library. Stationary solves check convergence and apply the route-specific state checks described above (the shared-mode route does not certify full-composite positivity); trajectory calculations report statistical diagnostics; selected spectra report solver metadata; and the gap route reports explicit certification flags. None of these checks substitutes for the others. Use the Evans-unicity tools when uniqueness is part of the physical claim.\n\nAfter downloading, run the program from an environment containing the package:\n\njulia --project=. generated_pi_deterministic_steady_observable.jl\n\nStart a downloaded trajectory program with multiple threads to use its preallocated path workers:\n\njulia --threads=auto --project=. generated_pi_trajectory_dynamics_observable.jl\n\nThe generated filename records the architecture, method where applicable, and calculation. For example, a selected spectrum uses generated_pi_liouvillian_spectrum.jl.\n\n“Download experiment bundle” creates three local files with a common stem:\n\nthe executable .jl program;\na machine-readable .json manifest containing the normalized typed model, calculation, parameters, representation, resource lower bound, and warnings;\na _README.txt with the exact run command and interpretation caveats.","title":"Boundaries of the assistant"},{"category":"section","location":"model_code_generator/#License-of-generated-artifacts","page":"Model code generator","text":"The emitted Julia program contains generator-supplied template code and is licensed under GPL-3.0-only, without an exception for generated output. Its header carries SPDX copyright and license tags, the applicable version-only notice, a no-warranty statement, and a link to the complete GNU GPL version 3 text. Redistribution or modification of that program must therefore comply with the root package license.\n\nThe JSON manifest is descriptive metadata: it records normalized user model inputs and generated resource information, but embeds no Julia or JavaScript program template. It does not receive a source-code header. The downloaded _README.txt repeats both points so the licensing status remains explicit when browsers save the three bundle files separately. Users remain responsible for rights in formulas, comments, data, or other material they supply to the assistant.\n\nNo archive library or remote service is used. Some browsers ask once for permission to download multiple files from the page; allow it to receive all three artifacts. The JSON manifest is descriptive and contains no executable JavaScript or Julia expression beyond the separately reviewed program.","title":"License of generated artifacts"},{"category":"section","location":"spin_phase_space/#Sector-resolved-spin-phase-space","page":"Spin phase space","text":"The spin phase-space functions turn a qubit PI density operator into Husimi-Q or spin-Wigner data on the sphere without reconstructing its 2^N Hilbert space. They work one Schur block at a time and distinguish the total-spin label from the angular coordinates.","title":"Sector-resolved spin phase space"},{"category":"section","location":"spin_phase_space/#Why-a-PI-state-has-several-spheres","page":"Spin phase space","text":"For qubits, a retained partition\n\nnu=(nu_1nu_2)\n\ncarries total spin j=(nu_1-nu_2)2, spin dimension n_j=2j+1, and symmetric-group multiplicity f^nu. A general PI state can occupy several such sectors. It therefore does not belong to one spin-j Hilbert space and does not have one irreducible spin sphere.\n\nThe canonical result returned by this package is the discrete collection of sector spheres. On each sphere the multiplicity-weighted Schur block\n\nbarrho_j=f^nurho_j=sqrtf^nuC_nu\n\nhas trace equal to the physical sector population p_j. Here C_nu is the stored equation-(7) block and rho_j=C_nusqrtf^nu is the physical block.\n\nSpinPhaseSpaceData.values is the sum of the selected sector densities. It is useful as a two-dimensional angular marginal, but it is explicitly obtained by forgetting the discrete j label. It must not be interpreted as though all sectors were one larger spin.","title":"Why a PI state has several spheres"},{"category":"section","location":"spin_phase_space/#Husimi-Q-normalization","page":"Spin phase space","text":"For a spin coherent state thetaphijrangle, the package returns\n\nP_Q(jthetaphi)=frac2j+14pi\nlanglethetaphijbarrho_jthetaphijrangle\n\nThis is a density with respect to dOmega=sinthetadthetadphi:\n\nint P_Q(jOmega)dOmega=p_j\n\nIt is nonnegative for a physical input state. The implementation evaluates the coherent amplitudes with a normalized recurrence about the binomial mode, so it does not convert exponentially large binomial coefficients to floating point. Azimuthal dependence is evaluated as a finite Fourier series.","title":"Husimi-Q normalization"},{"category":"section","location":"spin_phase_space/#Spin-Wigner-convention","page":"Spin phase space","text":"spin_wigner uses the Agarwal multipole construction with orthonormal Condon–Shortley polarization tensors T_kq and standard spherical harmonics Y_kq. With\n\nt_kq=mathrmtr(T_kq^daggerbarrho_j)\n\nthe normalized sector density is\n\nP_W(jthetaphi)=\nsqrtfrac2j+14pisum_k=0^2jsum_q=-k^k\nt_kqY_kq(thetaphi)\n\nIt also integrates to p_j. A maximally mixed conditional block is the uniform density p_j(4pi). For one qubit with Bloch vector r, the convention reduces to\n\nP_W(mathbf n)=frac1+sqrt3mathbf rcdotmathbf n4pi\n\nUnlike the Q distribution, the Wigner distribution may be negative. Negative values are retained numerically and displayed with a diverging palette; they are never clipped or repaired.","title":"Spin-Wigner convention"},{"category":"section","location":"spin_phase_space/#Computing-and-retaining-sector-data","page":"Spin phase space","text":"using PermutationalInvariantDynamics\n\nbasis = PIBasis(4, 2)\nrho = spin_coherent_state(basis, 0.8, 0.3)\n\nq = spin_husimi_q(rho; ntheta=91, nphi=180, resolved=true)\nw = spin_wigner(rho; ntheta=91, nphi=180, resolved=true)\n\nq.values                    # aggregate, indexed [phi, theta]\nq.sectors                   # retained partitions\nq.populations               # exact phase-space integral of each sector\nq.sector_values[1]          # first sector sphere\n\nThe default resolved=false streams one sector grid into the aggregate using reusable scratch. This keeps retained phase-space RAM proportional to one angular grid. Pass resolved=true only when the individual matrices will be inspected or rendered. A subset can be selected explicitly:\n\nsymmetric = spin_husimi_q(\n    rho; sectors=Partition((4, 0)), resolved=true)\n\nThe aggregate then integrates to the selected population, not necessarily one. Sector selection never renormalizes the result.\n\nThe coherent-state Q evaluation costs O(n_thetasum_j(n_j^2+n_phi n_j)). Wigner multipole setup costs O(sum_j n_j^3) and uses only sector-sized, call-local scratch; its grid evaluation has the same Fourier scaling as Q. No global mutable geometry cache or full Hilbert-space state is introduced.","title":"Computing and retaining sector data"},{"category":"section","location":"spin_phase_space/#Dependency-free-SVG","page":"Spin phase space","text":"Numerical calculation and presentation are separate:\n\nfigure = visualize_spin_phase_space(\n    w; title=\"Spin Wigner marginal\")\ndisplay(figure)\n\nsector_figure = visualize_spin_phase_space(\n    w; sector=first(w.sectors), title=\"Symmetric-sector Wigner function\")\n\nThe SVG is an equirectangular heatmap: azimuth phi is horizontal and polar angle theta is vertical, with the north pole at the top. Rendering a sector requires data computed with resolved=true. Husimi data use a sequential color scale, while Wigner data use a zero-centered diverging scale. Explicit colorlimits only change the mapping to colors; the numerical data are retained unchanged.\n\nThe renderer groups equal-color cells into a bounded number of SVG paths and has no plotting dependency. It requires regular, increasing coordinate grids and rejects more than 100,000 cells rather than silently resampling. Save the same notebook representation with save_spin_phase_space_visualization.\n\nSee the paired runnable example examples/spin_phase_space.jl for a mixed-sector state, normalization checks, sector rendering, and temporary SVG output.","title":"Dependency-free SVG"},{"category":"section","location":"nonstabilizerness/#Nonstabilizerness-of-symmetric-qubit-states","page":"Nonstabilizerness","text":"The second stabilizer Rényi entropy quantifies how far a pure state is from the stabilizer set. For an N-qubit pure state rho=psiranglelangle psi, Passarelli, Fazio, and Lucignano define\n\nM_2(rho) =\n-lnleft\n2^-Nsum_Pinmathcal P_N\nbigl(mathrmtr(rho P)bigr)^4\nright\n\nwhere mathcal P_N=IXYZ^otimes N is the phase-free Pauli basis. The default logarithm is natural, as in Phys. Rev. A 110, 022436 (2024). Set base=2 to report the same quantity in bits.\n\nstabilizer_renyi_entropy implements this definition for pure, permutation-symmetric qubit states. It returns zero for a stabilizer state and is positive for a nonstabilizer state, up to the documented numerical tolerances.","title":"Nonstabilizerness of symmetric qubit states"},{"category":"section","location":"nonstabilizerness/#PI-efficient-evaluation","page":"Nonstabilizerness","text":"A direct calculation enumerates 4^N Pauli strings. Permutation symmetry reduces the distinct expectations to the Pauli occupation counts (n_In_Xn_Yn_Z). The implementation goes further: it evaluates all representatives with normalized Krawtchouk transforms of the symmetric Schur block and stable hypergeometric weights.\n\nThe resulting costs are:\n\nO(N^4)\narithmetic for one state;\nO(N^3)\nretained data in a reusable StabilizerRenyiPlan;\nO(N^2)\nmutable scratch in a StabilizerRenyiWorkspace.\n\nNo state of size 2^N and no Pauli table of size 4^N is constructed. Multinomial degeneracies are accumulated in the logarithmic domain, and the same pass checks the pure-state Pauli second-moment identity as an internal reliability diagnostic.","title":"PI-efficient evaluation"},{"category":"section","location":"nonstabilizerness/#One-calculation","page":"Nonstabilizerness","text":"For one state, the convenience call prepares and releases its own plan and workspace:\n\nusing PermutationalInvariantDynamics\n\nN = 12\nbasis = PIBasis(N, 2; sectors=[(N, 0)])\n\n# A tensor product of single-qubit T states.\nket = ComplexF64[1, cis(pi / 4)] / sqrt(2)\nrho = iid_pure_state(basis, ket)\n\nM2 = stabilizer_renyi_entropy(rho)\n@assert isapprox(M2, N * log(4 / 3); rtol=1e-10)\n\nM2_bits = stabilizer_renyi_entropy(rho; base=2)\n\nThe restricted basis is not required: a complete PIBasis(N, 2) is also accepted when the state has support only in its fully symmetric sector.","title":"One calculation"},{"category":"section","location":"nonstabilizerness/#Repeated-states-on-one-basis","page":"Nonstabilizerness","text":"Prepare the immutable transform data once when scanning many pure symmetric states:\n\nplan = StabilizerRenyiPlan(basis; T=Float64)\nworkspace = StabilizerRenyiWorkspace(plan)\n\nfor phi in range(0, pi / 2; length=101)\n    local_ket = ComplexF64[1, cis(phi)] / sqrt(2)\n    state = iid_pure_state(basis, local_ket)\n    value = stabilizer_renyi_entropy(\n        state; plan=plan, workspace=workspace)\n    # store or process value\nend\n\nThe plan is read-only and may be shared. A workspace owns mutable matrices and must be used by only one task at a time. Construct one workspace per concurrent task. Both objects are tied to the exact PIBasis and numerical precision for which they were built. Setup and workspace allocations obey the common memory_budget safeguard; the default is 512 MiB and Inf is the explicit opt-out.","title":"Repeated states on one basis"},{"category":"section","location":"nonstabilizerness/#Scope-and-validation","page":"Nonstabilizerness","text":"This API deliberately follows the pure-state measure in the paper. It therefore rejects:\n\nlocal dimensions other than two;\nmixed states, including a maximally mixed state;\nstates with support outside the fully symmetric partition (N0);\nnonunit-trace, non-Hermitian, nonpositive, or nonfinite inputs;\na plan or workspace built from a different basis or incompatible precision;\nan invalid logarithm base or a request exceeding the memory budget.\n\nThe function does not normalize, project, symmetrize, or clip its input. In particular, applying the fourth Pauli-moment expression to a mixed state would not define the nonstabilizerness measure used here and is not silently accepted.\n\nFor a numerically prepared state, pass explicit atol and rtol only when they represent a justified state-preparation error. These tolerances validate purity, real Pauli expectations, and the Pauli second-moment identity; support outside the symmetric sector remains an exact, non-tolerant requirement. They do not alter the state.","title":"Scope and validation"},{"category":"section","location":"nonstabilizerness/#See-also","page":"Nonstabilizerness","text":"Observables and quantum information\nAPI tiers and prepared analysis\nFramework and conventions","title":"See also"},{"category":"section","location":"api/solvers/#Stationary-states,-spectra,-and-research-solvers","page":"Stationary states, spectra, and solvers","text":"","title":"Stationary states, spectra, and research solvers"},{"category":"section","location":"api/solvers/#Stationary-state-and-Krylov-algorithms","page":"Stationary states, spectra, and solvers","text":"","title":"Stationary-state and Krylov algorithms"},{"category":"section","location":"api/solvers/#PI-spectra","page":"Stationary states, spectra, and solvers","text":"","title":"PI spectra"},{"category":"section","location":"api/solvers/#Evans-uniqueness-and-weak-symmetries","page":"Stationary states, spectra, and solvers","text":"Simultaneous commuting-charge projectors are documented in Research utilities and control.","title":"Evans uniqueness and weak symmetries"},{"category":"section","location":"api/solvers/#Response-and-sensitivities","page":"Stationary states, spectra, and solvers","text":"","title":"Response and sensitivities"},{"category":"section","location":"api/solvers/#Sizing-and-summaries","page":"Stationary states, spectra, and solvers","text":"","title":"Sizing and summaries"},{"category":"section","location":"api/solvers/#High-level-algorithms-and-results","page":"Stationary states, spectra, and solvers","text":"","title":"High-level algorithms and results"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.steady_state","page":"Stationary states, spectra, and solvers","text":"steady_state(L; basis=nothing, trace_vector=nothing, method=:auto,\n             shift=nothing, maxiter=200, initial_state=nothing,\n             atol=1e-10, rtol=1e-8, return_info=false,\n             diagnostics=:basic, krylovdim=30, recycle_dim=0, workspace=nothing,\n             preconditioner=nothing, memory_budget=512*1024^2)\n\nSolve L*rho = 0 subject to the physical trace constraint. Passing a PIModel, a basis, or a matrix-free Liouvillian supplies the exact equation-(7) trace functional. method=:direct uses a bordered sparse solve; :svd returns the minimum-norm member of a possibly degenerate stationary manifold; :eigen selects the dense eigenvector closest to zero; and :shiftinvert performs sparse inverse iteration near shift. :krylov (:gmres) applies a restarted matrix-free GMRES solve with a rank-one trace constraint and never assembles the Liouvillian. Set recycle_dim>0 to retain an augmentation space in a compatible RecycledGMRESWorkspace across a sequence of related solves. :auto validates the direct solve before falling back to SVD. When the prepared scalar precision is unsupported by Julia's sparse/dense factorizations, :auto with basic diagnostics selects matrix-free Krylov before materializing the operator. An explicitly requested factorizing method instead raises with guidance; no wider input is silently narrowed to ComplexF64. Exact integer/rational shift and initial-state components use checked conversion and raise on overflow or nonzero underflow. diagnostics=:basic reports residual and trace checks without an extra dense factorization. Request diagnostics=:nullity only when the numerical stationary-space dimension is needed; this performs an SVD for methods that do not already require one.\n\nDense/factorizing routes are preflighted against memory_budget using a conservative PI-coordinate work-array estimate. When method=:auto and basic diagnostics do not fit, the routine selects matrix-free Krylov instead of starting a dense fallback. An explicitly requested factorizing method raises before materialization; pass memory_budget=Inf only to opt in after checking available RAM. For preconditioner=:schur, the preflight also includes the block extraction and LU-factor setup peak. Storage owned by an arbitrary caller-supplied preconditioner is not introspectable and remains the caller's responsibility.\n\nFor the steady_state(model::PIModel; ...) route, coefficient_cache may be a compatible OneBoxCGCache. It accelerates preparation only and does not change the stationary solve or its numerical tolerances.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.steady_state"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.krylov_steady_state","page":"Stationary states, spectra, and solvers","text":"krylov_steady_state(L; trace_vector=nothing, basis=nothing, ...)\n\nSolve the trace-fixed stationary equation with restarted GMRES, using only mul! applications of L. The equivalent nonsingular system is (L/s + v*t')rho = v, where s is a reproducible operator-scale estimate, t is the physical trace functional, and dot(t,v)=1. No Liouvillian matrix or factorization is constructed.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.krylov_steady_state"},{"category":"type","location":"api/solvers/#PermutationalInvariantDynamics.KrylovWorkspace","page":"Stationary states, spectra, and solvers","text":"Reusable storage for restarted GMRES on a PI Liouville-space vector.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.KrylovWorkspace"},{"category":"type","location":"api/solvers/#PermutationalInvariantDynamics.ArnoldiWorkspace","page":"Stationary states, spectra, and solvers","text":"ArnoldiWorkspace(L, krylovdim; mode=:full)\n\nReusable basis, image, pencil, and vector storage shared by ordinary, harmonic, and implicit-QR Arnoldi. A workspace is mutable and must be owned by one task at a time.\n\nThe default mode=:full preserves storage for every Arnoldi-family solver. Use mode=:ordinary when the workspace will only be passed to krylov_liouvillian_spectrum; this omits the four additional PI-vector blocks and two projected pencils used by restarted/interior algorithms. Harmonic Arnoldi, implicit-QR Arnoldi, and Jacobi–Davidson reject an ordinary-only workspace rather than allocating the missing arrays.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.ArnoldiWorkspace"},{"category":"type","location":"api/solvers/#PermutationalInvariantDynamics.JacobiDavidsonWorkspace","page":"Stationary states, spectra, and solvers","text":"Reusable large-vector storage for jacobi_davidson_spectrum.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.JacobiDavidsonWorkspace"},{"category":"type","location":"api/solvers/#PermutationalInvariantDynamics.SchurSectorPreconditioner","page":"Stationary states, spectra, and solvers","text":"SchurSectorPreconditioner\n\nBlock-diagonal approximation to the trace-fixed Liouvillian in Schur-sector Liouville coordinates. Each block is LU-factorized once and can be reused by matrix-free GMRES solves.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.SchurSectorPreconditioner"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.schur_sector_preconditioner","page":"Stationary states, spectra, and solvers","text":"schur_sector_preconditioner(L, basis; trace_vector=nothing,\n                            regularization=0)\n\nConstruct a left preconditioner from the diagonal Schur-sector blocks of the scale-normalized trace-fixed operator L/s + v*t'. Only sum_s n_s^2 coefficients are retained, where n_s is a sector's Liouville dimension. Construction uses matrix-free applications of L for an arbitrary source. A compatible prepared PI Liouvillian instead lowers the blocks directly from its immutable physical term plan, so only the operator-scale probes apply the complete Liouvillian. The resulting LU factors should be reused across solves. The implementation normalizes L by a reproducible operator-scale estimate, matching krylov_steady_state. Set a small positive regularization only when a sector block is singular. Cost and amortization estimates are available as preconditioner_cost(P); expected_reuses controls the setup warning.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.schur_sector_preconditioner"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.preconditioner_cost","page":"Stationary states, spectra, and solvers","text":"Return setup, storage, apply-cost, and amortization metadata for P.\n\n\n\n\n\nReturn setup, storage, and application-cost metadata for P.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.preconditioner_cost"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.krylov_liouvillian_spectrum","page":"Stationary states, spectra, and solvers","text":"krylov_liouvillian_spectrum(L; nev=6, krylovdim=max(20, 2nev+4), which=:LR)\n\nApproximate selected Liouvillian eigenpairs by matrix-free Arnoldi iteration. The returned residual estimates are norm(L*v-lambda*v) Ritz estimates. Pass a reusable ArnoldiWorkspace to avoid reallocating the large basis and Hessenberg arrays. Residual tolerances scale with the sampled operator action, without an absolute unit-scale floor.\n\nThis is a single-vector Arnoldi method. An exact degenerate eigenspace may therefore contribute only one Ritz direction even when nev is larger; use block_arnoldi_spectrum when spectral multiplicities must be resolved.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.krylov_liouvillian_spectrum"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.harmonic_arnoldi_spectrum","page":"Stationary states, spectra, and solvers","text":"harmonic_arnoldi_spectrum(L; nev=6, krylovdim=40, thickdim=12,\n                          maxrestarts=20, projector=nothing,\n                          initial_subspace=nothing)\n\nCompute eigenvalues nearest target with thick-restarted harmonic Arnoldi. Converged and best unconverged harmonic Ritz vectors are retained between cycles. An optional matrix-free orthogonal projector restricts every basis and residual vector to a weak-symmetry charge sector. initial_subspace accepts several continuation vectors and is mutually exclusive with initial_vector; dependent or projector-null columns are dropped by two-pass orthogonalization. When the complete projector range is known and has been spanned, extraction switches to ordinary Rayleigh–Ritz in that invariant subspace.  This avoids the singular harmonic pencil at an exactly represented target without loosening the requested residual tolerance.  The result fields ritz_extraction and search_space_exhausted record the selected path. workspace reuses Arnoldi and pencil storage; projector_workspace supplies caller-owned symmetry scratch. Restart reports include locked-mode counts and per-cycle residual history.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.harmonic_arnoldi_spectrum"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.implicitly_restarted_arnoldi_spectrum","page":"Stationary states, spectra, and solvers","text":"implicitly_restarted_arnoldi_spectrum(L; nev=6, krylovdim=40,\n                                       retained_dimension=nev,\n                                       maxrestarts=20, which=:LR,\n                                       target=nothing)\n\nCompute selected eigenpairs using matrix-free implicitly restarted Arnoldi. Every restart applies exact unwanted Ritz shifts by implicit QR transformations of the Hessenberg matrix; the transformed Arnoldi factorization is truncated and expanded without materializing L. Set target to order Ritz values by distance from an interior point, or leave it as nothing and select with which=:LR, :LM, or :SM.\n\nThis routine is most effective for spectral-edge modes. For difficult interior or strongly nonnormal clusters, use jacobi_davidson_spectrum, whose correction equations can be preconditioned.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.implicitly_restarted_arnoldi_spectrum"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.jacobi_davidson_spectrum","page":"Stationary states, spectra, and solvers","text":"jacobi_davidson_spectrum(L; nev=6, target=0, subspace_dim=40,\n                         preconditioner=nothing)\n\nCompute matrix-free eigenpairs nearest target with restarted Jacobi–Davidson. The inexact correction equation P*(L-theta*I)*P*t=-r is solved by restarted GMRES, where P removes the current Ritz vector and the converged invariant subspace. Converged Schur directions are hard locked. Pass any reusable object supporting ldiv!(y,P,x) as preconditioner; it is projected consistently inside the correction solve.\n\nReturned right vectors are reconstructed from the final locked Ritz block and validated with explicit norm(L*v-lambda*v) residuals. Only mul! and, when requested, ldiv! actions are used.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.jacobi_davidson_spectrum"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.liouvillian_eigenvalues","page":"Stationary states, spectra, and solvers","text":"Return selected Liouvillian eigenvalues according to :LR, :LM, or :SM.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.liouvillian_eigenvalues"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.liouvillian_gap","page":"Stationary states, spectra, and solvers","text":"Alias for pi_liouvillian_gap.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.liouvillian_gap"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.pi_liouvillian_spectrum","page":"Stationary states, spectra, and solvers","text":"pi_liouvillian_spectrum(L; sortby=:real, rev=true, vectors=false,\n                        return_info=false,\n                        memory_budget=512*1024^2)\n\nWith method=:dense, compute the complete spectrum in the polynomial-size PI operator space. With method=:krylov, compute selected ordinary Ritz modes; method=:block_arnoldi advances several directions through one batched operator application and uses an explicitly named thick restart; method=:harmonic computes thick-restarted harmonic Ritz modes near zero, method=:iram applies implicit-QR restarting, and method=:jd uses hard-locking Jacobi–Davidson near target. L may be a static PIModel, an assembled matrix, or a matrix-free PI Liouvillian. Set vectors=true to also return consistently sorted right eigenvectors. Matrix-free inputs are materialized in PI coordinates, never in the full d^(2N) Liouville space, only when method=:dense; Krylov, harmonic, IRAM, and Jacobi–Davidson methods apply them without materializing the PI Liouvillian.\n\nComplete dense diagonalization is guarded by a conservative work-array estimate. Increase memory_budget only when the required RAM is available, or pass memory_budget=Inf as an explicit opt-in. return_info=true keeps solver diagnostics but, independently of that choice, right eigenvectors are retained only when vectors=true.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.pi_liouvillian_spectrum"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.pi_density_spectrum","page":"Stationary states, spectra, and solvers","text":"pi_density_spectrum(rho; expanded=false, max_expanded_dimension=10^7)\n\nDiagonalize every physical Schur block of a PI density operator. The default compressed result stores each irrep eigenvalue once together with its exact symmetric-group degeneracy. expanded=true returns the full eigenvalue list only when its size does not exceed max_expanded_dimension.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.pi_density_spectrum"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.pi_liouvillian_gap","page":"Stationary states, spectra, and solvers","text":"pi_liouvillian_gap(L; atol=1e-12, rtol=1e-10,\n                   check_stability=true, return_info=false,\n                   initial_vector=nothing)\n\nReturn the asymptotic Liouvillian decay gap -max(real(lambda)) after excluding the numerical stationary cluster around zero. Unlike selecting the second sorted eigenvalue, this handles degenerate stationary spaces and oscillatory decay modes. return_info=true also reports the controlling eigenvalue, oscillation frequency, stationary multiplicity, stability, tolerance, and PI dimension.\n\nmethod=:harmonic is deliberately not accepted for a global gap: harmonic Ritz values are selected by distance to zero, so they can miss a slow mode with a large oscillation frequency. With a unitary symmetry and return_info=true, it instead reports a near-zero decay estimate for the selected charge sector. A nontrivial charge sector is not required to contain a stationary eigenvalue. The estimate is certified only when the complete selected sector was extracted; inspect gap_certified, sector_dimension, and scope in the returned information.\n\nFor iterative methods, initial_vector supplies an explicit reproducible Krylov seed. The solver projects it into the requested symmetry charge when applicable and rejects a seed with no component in that sector.\n\nmethod=:iram is a bounded-memory global-gap route because it selects largest-real Ritz values. method=:jd is rejected here: near-target Jacobi–Davidson has the same large-imaginary slow-mode caveat as harmonic selection and currently has no charge-projector specialization.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.pi_liouvillian_gap"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.pi_density_operator_spectrum","page":"Stationary states, spectra, and solvers","text":"Alias for pi_density_spectrum.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.pi_density_operator_spectrum"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.density_operator_spectrum","page":"Stationary states, spectra, and solvers","text":"Alias for pi_density_spectrum.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.density_operator_spectrum"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.evans_uniqueness","page":"Stationary states, spectra, and solvers","text":"evans_uniqueness(H, jumps; atol=1e-12, rtol=1e-10, return_basis=false)\n\nApply the finite-dimensional Evans commutant criterion to explicit Hilbert-space operators. The commutant of {H,L_k,L_k'} is obtained as the nullspace of stacked vectorized commutators. A one-dimensional commutant certifies a unique steady state (assuming the finite-dimensional trace-preserving GKSL semigroup).\n\n\n\n\n\nevans_uniqueness(model::PIModel; atol=1e-12, rtol=1e-10,\n                 memory_budget=512*1024^2)\n\nReturn a theorem-based uniqueness report without constructing the full d^N Hilbert space. unique is true, false, or missing when the efficient Evans tests cannot run within memory_budget or a direct/custom PI term lacks a microscopic recoupling. After inexpensive special cases, the general path constructs the positive joint-commutator operator on an auxiliary PI problem with local dimension d^2, diagonalizes its Schur blocks, and counts kernel vectors with their exact symmetric-group multiplicities. It never constructs a d^N operator.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.evans_uniqueness"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.has_unique_steady_state_evans","page":"Stationary states, spectra, and solvers","text":"Boolean-or-missing convenience result from evans_uniqueness(model).\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.has_unique_steady_state_evans"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.check_liouvillian_symmetry","page":"Stationary states, spectra, and solvers","text":"check_liouvillian_symmetry(L, U; kind=:unitary, basis=nothing,\n                           atol=1e-12, rtol=1e-10)\n\nCheck a weak Liouvillian symmetry. For kind=:unitary, test covariance under rho -> U*rho*U'. For kind=:antiunitary, test covariance under rho -> U*conj(rho)*U', i.e. the antiunitary U*K in the chosen basis. With a PIBasis, a local d×d unitary is lifted as U^⊗N using Schur blocks; a PIOperator may instead specify sector-dependent unitary blocks.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.check_liouvillian_symmetry"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.is_liouvillian_symmetric","page":"Stationary states, spectra, and solvers","text":"Return only the Boolean symmetric field from check_liouvillian_symmetry.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.is_liouvillian_symmetric"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.usual_liouvillian_symmetries","page":"Stationary states, spectra, and solvers","text":"usual_liouvillian_symmetries(model_or_L; basis=nothing,\n                             unitary_candidates=nothing,\n                             antiunitary_candidates=nothing)\n\nCheck named common weak symmetries. Defaults are the local clock/phase and cyclic-shift unitaries plus complex conjugation; qubits additionally include Pauli parity axes and spin time reversal i*sigma_y*K. Candidate collections may be dictionaries, named tuples, or vectors of pairs.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.usual_liouvillian_symmetries"},{"category":"type","location":"api/solvers/#PermutationalInvariantDynamics.MatrixFreeSymmetryProjector","page":"Stationary states, spectra, and solvers","text":"Matrix-free projector with a locked compatibility workspace.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.MatrixFreeSymmetryProjector"},{"category":"type","location":"api/solvers/#PermutationalInvariantDynamics.SymmetryProjectorWorkspace","page":"Stationary states, spectra, and solvers","text":"Caller-owned scratch for allocation-free symmetry projection.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.SymmetryProjectorWorkspace"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.matrixfree_symmetry_projector","page":"Stationary states, spectra, and solvers","text":"matrixfree_symmetry_projector(basis, U; charge=1)\n\nConstruct an orthogonal matrix-free projector onto the eigenspace with U*rho*U' = charge*rho. U may be a local unitary or a block PI unitary. Only sector-sized unitary matrices and Boolean masks are stored. The standard mul! path serializes a reusable compatibility workspace. For parallel or repeated explicit application, allocate one SymmetryProjectorWorkspace per task and call apply!(y, P, x, workspace). LAPACK-backed construction preserves ComplexF32 or ComplexF64; other scalar types are rejected with explicit conversion guidance. Exact in-place application apply!(x, P, x, workspace) is supported, but distinct partially overlapping views are rejected.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.matrixfree_symmetry_projector"},{"category":"type","location":"api/solvers/#PermutationalInvariantDynamics.SymmetryCoordinateRestriction","page":"Stationary states, spectra, and solvers","text":"SymmetryCoordinateRestriction(basis, indices; label=:explicit)\n\nDescribe an orthonormal coordinate restriction of a PI operator space.  The retained indices are copied, checked to be distinct PI coefficient coordinates, and stored in ascending order.  The restriction is therefore the isometry whose columns are the selected standard basis vectors; it never constructs a dense projection matrix.\n\nThis low-level constructor is also the escape hatch for a support rule derived outside the package.  Constructing a restriction does not claim that a Liouvillian preserves it.  Use restriction_invariance, or construct a RestrictedLiouvillian, which performs an exhaustive leakage check.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.SymmetryCoordinateRestriction"},{"category":"type","location":"api/solvers/#PermutationalInvariantDynamics.RestrictionInvarianceReport","page":"Stationary states, spectra, and solvers","text":"Numerical certificate for exhaustive coordinate-subspace leakage testing.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.RestrictionInvarianceReport"},{"category":"type","location":"api/solvers/#PermutationalInvariantDynamics.RestrictedLiouvillian","page":"Stationary states, spectra, and solvers","text":"RestrictedLiouvillian(source, restriction;\n                      atol=0, rtol=nothing, backend=:auto)\n\nPrepare the compressed operator P' * source * P after exhaustively certifying that the autonomous source leaves the coordinate range invariant. For a sparse or dense matrix source, backend=:auto stores and applies the actual compressed matrix source[indices,indices]; neither the reduced Krylov iteration, one operator application, nor the compatibility wrapper then retains or touches ambient vectors. For a prepared matrix-free source whose fixed kernels have Cartesian Schur support, backend=:auto lowers those kernels directly to the selected ket and bra GT-pattern rectangles.  This :lowered backend retains no ambient application vectors and is the preferred strong-symmetry path.  Unsupported prepared or explicit coordinate masks retain the :embedded fallback, whose large working vectors remain caller-owned through RestrictedLiouvillianWorkspace. backend=:embedded may be selected explicitly; backend=:compressed requires an AbstractMatrix source, and backend=:lowered requires a compatible prepared LiouvillianPlan.\n\nCompatibility embedded mul! calls use a locked workspace; parallel code should call apply! with one explicit workspace per task.\n\nThe stored trace functional is the exact equation-(7) trace vector restricted in the source working precision.  A restriction with a zero trace functional is valid for decay-mode calculations but is rejected by restricted_steady_state.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.RestrictedLiouvillian"},{"category":"type","location":"api/solvers/#PermutationalInvariantDynamics.RestrictedLiouvillianWorkspace","page":"Stationary states, spectra, and solvers","text":"RestrictedLiouvillianWorkspace(source, restriction)\nRestrictedLiouvillianWorkspace(operator)\n\nCaller-owned scratch for restricted Liouvillian certification and application. The source/restriction constructor owns two ambient vectors needed by exhaustive matrix-free leakage checks and by the embedded fallback. The prepared-operator constructor instead returns reduced Schur-block scratch for a :lowered operator, so hot applications retain no ambient vectors.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.RestrictedLiouvillianWorkspace"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.diagonal_symmetry_restriction","page":"Stationary states, spectra, and solvers","text":"diagonal_symmetry_restriction(basis, local_unitary;\n    charge=nothing, ket_charge=nothing, bra_charge=nothing,\n    atol=1e-12, rtol=1e-10, label=:diagonal_strong_symmetry)\n\nConstruct the exact PI-coordinate support associated with separate Hilbert- space ket and bra charges of local_unitary^tensor N.  local_unitary must be diagonal in the package's local computational basis.  With charge=q, both charges are set to q; otherwise bra_charge defaults to ket_charge, and the latter defaults to one.\n\nFor a GT pattern with local occupation tuple n, its charge is evaluated as prod(diag(local_unitary).^n).  A coefficient coordinate is retained exactly when its row pattern has ket_charge and its column pattern has bra_charge. Thus a density operator supported on one strong-symmetry Hilbert block uses equal ket and bra charges.  Different charges describe an off-diagonal operator block and generally have an identically zero physical trace.\n\nOnly the support is constructed here.  This does not infer strong symmetry from a model; RestrictedLiouvillian exhaustively certifies that the supplied autonomous generator leaves the support invariant.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.diagonal_symmetry_restriction"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.retained_indices","page":"Stationary states, spectra, and solvers","text":"Return a detached, ascending copy of the retained PI coordinate indices.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.retained_indices"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.restricted_trace_vector","page":"Stationary states, spectra, and solvers","text":"restricted_trace_vector(restriction, [T=ComplexF64])\n\nReturn the equation-(7) physical trace functional on the retained coordinates. The scalar type is explicit because Schur multiplicity square roots must remain representable in the working precision.  An off-diagonal ket/bra charge block legitimately returns the zero functional.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.restricted_trace_vector"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.restrict!","page":"Stationary states, spectra, and solvers","text":"Restrict a full PI coefficient vector into preallocated reduced storage.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.restrict!"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.embed!","page":"Stationary states, spectra, and solvers","text":"Embed reduced coordinates into a preallocated, zero-filled full PI vector.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.embed!"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.restriction_invariance","page":"Stationary states, spectra, and solvers","text":"restriction_invariance(source, restriction;\n    atol=0, rtol=nothing, workspace=nothing)\n\nExhaustively certify numerical invariance of the selected coordinate range. Explicit matrices are scanned directly, while a matrix-free autonomous source is applied to every retained coordinate vector.  The reported leakage_norm is the Frobenius norm of (I-P)*source*P, while action_norm is the Frobenius norm of source*P.  No global Liouvillian or projection matrix is materialized.  rtol=nothing selects 100eps(R) in the source's real working precision.\n\nThis is an exhaustive floating-point certificate, not a symbolic theorem.  A driven source must first be made autonomous with freeze; testing one instant would not certify invariance for all later applications.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.restriction_invariance"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.restriction_full_residual","page":"Stationary states, spectra, and solvers","text":"restriction_full_residual(operator, reduced_vector;\n                          eigenvalue=0, workspace=nothing)\n\nEmbed a reduced vector, evaluate the original full-coordinate source, and return its full, retained, and leakage residual norms for source*x = eigenvalue*x.  The returned physical trace uses the restricted equation-(7) trace functional.  This helper is intentionally evaluated in the ambient PI space, so it independently checks the compressed result without a d^N reconstruction.\n\nThis diagnostic necessarily needs ambient PI scratch. The workspace returned by RestrictedLiouvillianWorkspace(operator) for a :lowered operator is reduced-only and is therefore rejected; construct RestrictedLiouvillianWorkspace(operator.source, operator.restriction) when reusing this diagnostic.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.restriction_full_residual"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.restricted_steady_state","page":"Stationary states, spectra, and solvers","text":"restricted_steady_state(operator; return_info=false, ...)\n\nSolve the trace-fixed stationary equation directly in the certified compressed coordinates using the existing restarted matrix-free GMRES implementation. The returned value is an ambient PIState, with exact zeros outside the selected support.  initial_state may be either an ambient PIState or a reduced coordinate vector.\n\nAfter GMRES converges, the solution is embedded and checked with restriction_full_residual.  A full residual exceeding the same operator-scaled tolerance raises.  The method certifies the linear stationary equation and physical trace, not uniqueness or positivity; use the usual state diagnostics separately when those claims are required.  A preconditioner, when supplied, must act in the reduced coordinate space.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.restricted_steady_state"},{"category":"type","location":"api/solvers/#PermutationalInvariantDynamics.StrongSymmetryReport","page":"Stationary states, spectra, and solvers","text":"StrongSymmetryReport\n\nTerm-by-term report for diagonal strong unitary symmetries of a PIModel.  Each entry of candidates contains name, unitary, status, terms, and the Hilbert-space charges present in the retained Schur basis.  status is true, false, or missing; missing is inconclusive and is never treated as a symmetry certificate.\n\nUnlike usual_liouvillian_symmetries, this report tests commutation with every Hamiltonian and jump operator separately.  A jump which merely changes by a phase is therefore a weak covariance symmetry, but not a strong symmetry.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.StrongSymmetryReport"},{"category":"type","location":"api/solvers/#PermutationalInvariantDynamics.StrongSymmetryReduction","page":"Stationary states, spectra, and solvers","text":"StrongSymmetryReduction\n\nPrepared collection of every trace-bearing Hilbert-charge block of one certified diagonal strong symmetry.  sectors contains one named tuple per charge with its SymmetryCoordinateRestriction and exhaustively certified RestrictedLiouvillian.  No stationary sector is selected implicitly.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.StrongSymmetryReduction"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.strong_symmetry_report","page":"Stationary states, spectra, and solvers","text":"strong_symmetry_report(model; candidates=:usual,\n                       atol=1e-12, rtol=1e-10)\n\nDiscover and certify diagonal strong unitary symmetries without constructing a d^N operator.  candidates=:usual tests the local clock (Pauli-Z parity for qubits) and derives additional binary ±1 parity candidates from exact nonzero supports of the model's microscopic one- and p-body operators.  An explicit matrix or a named collection of matrices may be supplied instead.\n\nEvery candidate is checked term by term.  Hamiltonians and every jump operator must commute separately with U^tensor N.  The candidate status is true, false, or missing; scheduled operators that have not been freezed are reported as missing.  Scalar time-dependent rates do not obstruct certification because they cannot change a commutator.\n\nThe nullspace search is complete only within binary diagonal sign symmetries, and only when report.discovery.microscopic_support_complete=true.  The usual clock is an additional tested candidate, not part of that completeness claim.  An incomplete search means that additional symmetries may exist; it does not weaken any reported true certificate.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.strong_symmetry_report"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.strong_symmetry_reduction","page":"Stationary states, spectra, and solvers","text":"strong_symmetry_reduction(model; candidate=:auto, report=nothing,\n                          memory_budget=512*1024^2, ...)\n\nCompile model matrix-free once and construct an exhaustively certified RestrictedLiouvillian for every trace-bearing charge of one diagonal strong symmetry.  candidate=:auto chooses the certified candidate with the largest number of charge blocks; it never chooses one stationary charge block.  Pass a candidate name from strong_symmetry_report to make the symmetry choice explicit.\n\nmodel must be autonomous.  Driven operator schedules must first be frozen at an explicit time.  Each returned reduced operator independently certifies ambient-coordinate leakage, and supported fixed kernels use the lowered matrix-free backend.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.strong_symmetry_reduction"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.strong_symmetry_steady_states","page":"Stationary states, spectra, and solvers","text":"strong_symmetry_steady_states(reduction; embed_states=true,\n                              memory_budget=512*1024^2, kwargs...)\n\nSolve the trace-fixed stationary equation in every trace-bearing charge block of a StrongSymmetryReduction.  The result always contains one entry per charge; it never silently chooses a sector.  By default every entry includes the ambient PIState, reduced coordinates, invariance certificate, and the independent full-coordinate residual produced by restricted_steady_state. Set embed_states=false to retain only reduced coordinates and diagnostics; one ambient state is still a bounded transient while its full residual is validated.\n\nThis computes one stationary solution per charge block.  It does not certify uniqueness or positivity inside a block.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.strong_symmetry_steady_states"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.strong_symmetry_spectra","page":"Stationary states, spectra, and solvers","text":"strong_symmetry_spectra(reduction; method=:krylov, nev=nothing,\n                        krylovdim=nothing, vectors=false,\n                        validate_full=true, ...)\n\nCompute a Liouvillian spectrum separately in every trace-bearing strong-charge block.  By default right Ritz vectors are retained internally long enough to evaluate restriction_full_residual against the original ambient Liouvillian, then discarded unless vectors=true.  A failed full residual raises instead of accepting a reduced-space Ritz pair.\n\nSelected iterative spectra remain charge-resolved partial spectra; the return value does not relabel their union as a certified global gap. Off-diagonal ket/bra charge blocks are not trace bearing and are deliberately omitted, so even complete per-sector diagonal spectra are not the global Liouvillian spectrum. When nev is omitted, each block requests at most six modes; an explicit nev is never reduced silently.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.strong_symmetry_spectra"},{"category":"type","location":"api/solvers/#PermutationalInvariantDynamics.ResponseWorkspace","page":"Stationary states, spectra, and solvers","text":"ResponseWorkspace(source; krylovdim=30, expv_krylovdim=30,\n                  mode=:both, memory_budget=512*1024^2)\n\nReusable matrix-free response scratch. mode=:linear retains two restarted- GMRES workspaces and response vectors, mode=:evolution retains only adaptive Krylov exponential-action storage, and mode=:both supports every response route. A workspace is tied to its prepared source and may be reused sequentially, never concurrently. A RestrictedLiouvillian with the :lowered backend retains only its reduced Schur-block application scratch; the response layer does not recreate ambient PI vectors. Construction preflights Krylov, response-vector, and matrix-free action scratch; memory_budget=Inf is the explicit opt-out.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.ResponseWorkspace"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.liouvillian_modes","page":"Stationary states, spectra, and solvers","text":"liouvillian_modes(L; k=6, which=:largest_real, method=:auto, ...)\n\nReturn selected Liouvillian eigenvalues and right decay modes. Explicit matrices retain the dense compatibility path under method=:auto only when its work-array estimate fits memory_budget; otherwise ordinary Arnoldi is selected and its workspace is checked against the same budget. method=:iram and :jd expose the corresponding bounded-memory solvers. Iterative results retain residuals, convergence flags, and partial spectral scope.\n\nOrdinary Arnoldi advances one initial vector, so an exact degenerate eigenspace may contribute only one Ritz direction. Use pi_liouvillian_spectrum(...; method=:block_arnoldi) when resolving spectral multiplicities is required.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.liouvillian_modes"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.resolvent_norm","page":"Stationary states, spectra, and solvers","text":"resolvent_norm(L, z; method=:auto, krylovdim=30, ...)\n\nReturn the spectral norm of (z*I-L)^(-1). Explicit matrices retain the dense SVD path under method=:auto only when it fits memory_budget. Otherwise matrix-free power iteration is selected and its forward/adjoint GMRES workspace is checked against the same bound. Matrix-free sources use power iteration on R'R, with both shifted systems solved by restarted GMRES. This route requires a certified adjoint action; a custom matrix-free source without one is rejected rather than materialized implicitly.\n\nreturn_info=true reports power/GMRES convergence. The iterative value is a converged numerical estimate, not a certified upper bound on the resolvent. At a singular or insufficiently resolved shift, a failed GMRES solve raises; the routine never reports a finite norm from a nonconverged shifted solve.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.resolvent_norm"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.adjoint_evolve","page":"Stationary states, spectra, and solvers","text":"adjoint_evolve(L, A, t; method=:auto, workspace=nothing, ...)\n\nEvolve a PI observable under the adjoint of an autonomous Liouvillian. Explicit matrices retain the dense exponential under method=:auto only when its work arrays fit memory_budget; otherwise the checked adaptive krylov_expv! workspace is used with an explicit adjoint action. return_info=true includes exponential-action convergence and error diagnostics.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.adjoint_evolve"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.sensitivity_problem","page":"Stationary states, spectra, and solvers","text":"sensitivity_problem(L, rho0, tspan, dLs; parameters=nothing)\n\nConstruct an in-place augmented ODE for the state and tangent states d rho / d theta_mu. dLs[mu] is either a static matrix/operator or a callable (t,p) -> dL/dtheta_mu. The augmented state has columns [rho, drho/dtheta_1, ...]. Built-in prepared generators apply L to all columns in one matrix-RHS call, so driven schedules are evaluated once per ODE right-hand side. Task-owned workspaces for static derivative generators are also prepared once when the problem is constructed.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.sensitivity_problem"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.sensitivity_state","page":"Stationary states, spectra, and solvers","text":"Extract parameter tangent mu from an augmented sensitivity solution.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.sensitivity_state"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.classical_fisher_information","page":"Stationary states, spectra, and solvers","text":"Classical Fisher matrix for PI POVM effects and tangent states.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.classical_fisher_information"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.observable_decay_modes","page":"Stationary states, spectra, and solvers","text":"Liouvillian decay modes together with Hilbert–Schmidt observable overlaps.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.observable_decay_modes"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.integrated_correlation_time","page":"Stationary states, spectra, and solvers","text":"integrated_correlation_time(L, rho, A; method=:auto, ...)\n\nIntegrated connected autocorrelation time. Matrix-free sources solve the trace-zero Poisson equation with restarted GMRES and a rank-one physical-trace constraint; explicit matrices retain the dense pseudoinverse compatibility path only when it fits memory_budget. The selected dense or Krylov workspace is checked before allocation. return_info=true includes raw residual and trace diagnostics.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.integrated_correlation_time"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.steady_state_susceptibility","page":"Stationary states, spectra, and solvers","text":"steady_state_susceptibility(L, rho, dL; method=:auto, observable=nothing)\n\nSolve the trace-fixed tangent equation L*dρ = -dL*ρ. Matrix-free sources use restarted GMRES with a rank-one physical-trace constraint; explicit matrices retain the dense compatibility route only when it fits memory_budget. The selected dense or Krylov workspace is checked before allocation. The tangent is never normalized or projected onto positive states.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.steady_state_susceptibility"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.pseudospectral_abscissa","page":"Stationary states, spectra, and solvers","text":"Largest real grid coordinate where the resolvent norm exceeds 1/epsilon.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.pseudospectral_abscissa"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.qfim_from_derivatives","page":"Stationary states, spectra, and solvers","text":"QFIM from tangent density operators d rho / d theta_mu.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.qfim_from_derivatives"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.estimate_basis_size","page":"Stationary states, spectra, and solvers","text":"Legacy alias for commutant_dimension.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.estimate_basis_size"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.estimate_memory","page":"Stationary states, spectra, and solvers","text":"estimate_memory(N, d; T=ComplexF64,\n                bigfloat_precision=precision(BigFloat))\n\nLegacy estimate for one complete PI coordinate vector; prefer estimate_state_bytes for an already constructed, possibly sector-restricted basis. Fixed-size isbits values retain the historical exact inline formula; BigFloat values use a conservative bound at the stated precision.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.estimate_memory"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.basis_summary","page":"Stationary states, spectra, and solvers","text":"basis_summary(basis; T=ComplexF64,\n              bigfloat_precision=precision(BigFloat))\n\nReturn sector, retained-coordinate dimension, and memory information for a PIBasis.  state_bytes describes one vector on the retained basis, so it also remains accurate for sector-restricted bases.  The legacy estimate_memory function instead estimates a complete PI basis from only (N,d) and therefore cannot account for sector restrictions. Fixed-size isbits scalars use exact inline byte accounting. Heap-backed BigFloat values use the conservative retained-storage bound at bigfloat_precision shared by estimate_state_bytes.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.basis_summary"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.model_summary","page":"Stationary states, spectra, and solvers","text":"Return a compact named-tuple summary of a PIModel.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.model_summary"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.value_at","page":"Stationary states, spectra, and solvers","text":"value_at(x, time, parameters)\n\nReturn a constant specification unchanged, or evaluate a callable specification as x(time, parameters). This is the scalar/operator schedule convention used by PI model terms.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.value_at"},{"category":"type","location":"api/solvers/#PermutationalInvariantDynamics.AbstractPIAlgorithm","page":"Stationary states, spectra, and solvers","text":"Common supertype for dispatchable high-level solver choices.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.AbstractPIAlgorithm"},{"category":"type","location":"api/solvers/#PermutationalInvariantDynamics.AutoAlgorithm","page":"Stationary states, spectra, and solvers","text":"Select a conservative algorithm from the problem representation and size.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.AutoAlgorithm"},{"category":"type","location":"api/solvers/#PermutationalInvariantDynamics.DirectAlgorithm","page":"Stationary states, spectra, and solvers","text":"Trace-bordered direct stationary-state solve.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.DirectAlgorithm"},{"category":"type","location":"api/solvers/#PermutationalInvariantDynamics.SVDAlgorithm","page":"Stationary states, spectra, and solvers","text":"Dense SVD stationary-manifold solve for diagnostic-sized problems.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.SVDAlgorithm"},{"category":"type","location":"api/solvers/#PermutationalInvariantDynamics.EigenAlgorithm","page":"Stationary states, spectra, and solvers","text":"Dense eigenvector stationary-state solve.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.EigenAlgorithm"},{"category":"type","location":"api/solvers/#PermutationalInvariantDynamics.ShiftInvertAlgorithm","page":"Stationary states, spectra, and solvers","text":"Sparse shift-invert stationary-state iteration near an explicit or automatic shift.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.ShiftInvertAlgorithm"},{"category":"type","location":"api/solvers/#PermutationalInvariantDynamics.GMRESAlgorithm","page":"Stationary states, spectra, and solvers","text":"Restarted matrix-free GMRES stationary-state algorithm and optional preconditioner.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.GMRESAlgorithm"},{"category":"type","location":"api/solvers/#PermutationalInvariantDynamics.RecycledGMRESAlgorithm","page":"Stationary states, spectra, and solvers","text":"RecycledGMRESAlgorithm(; krylovdim=30, maxiter=500,\n                       recycle_dim=8, preconditioner=nothing)\n\nMatrix-free GCRO-style stationary-state algorithm for continuation through a sequence of slowly varying Liouvillians. A task-owned RecycledGMRESWorkspace retains up to recycle_dim near-zero Ritz directions between solves; every new operator is reapplied to the retained space and the full unpreconditioned residual is checked.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.RecycledGMRESAlgorithm"},{"category":"type","location":"api/solvers/#PermutationalInvariantDynamics.ExpvAlgorithm","page":"Stationary states, spectra, and solvers","text":"ExpvAlgorithm(; krylovdim=30, atol=1e-10, rtol=1e-8,\n              initial_step=nothing, minimum_step=nothing,\n              maximum_step=nothing, max_steps=10_000, safety=0.9)\n\nAdaptive restarted-Arnoldi exponential action for autonomous high-level dynamics. Each interval between requested output times is propagated as exp(Δt * L)ρ without materializing either L or its exponential. The workspace and task-owned source-action scratch are prepared once and reused for every interval.\n\nThis algorithm deliberately rejects driven generators and non-nothing parameters; use the RK4 or SciML paths for explicit time dependence.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.ExpvAlgorithm"},{"category":"type","location":"api/solvers/#PermutationalInvariantDynamics.HarmonicArnoldiAlgorithm","page":"Stationary states, spectra, and solvers","text":"Thick-restarted harmonic Arnoldi parameters for modes near zero.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.HarmonicArnoldiAlgorithm"},{"category":"type","location":"api/solvers/#PermutationalInvariantDynamics.SteadyStateResult","page":"Stationary states, spectra, and solvers","text":"Typed high-level stationary-state result.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.SteadyStateResult"},{"category":"type","location":"api/solvers/#PermutationalInvariantDynamics.DynamicsResult","page":"Stationary states, spectra, and solvers","text":"High-level dynamics result with collection semantics over saved states.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.DynamicsResult"},{"category":"type","location":"api/solvers/#PermutationalInvariantDynamics.SpectrumResult","page":"Stationary states, spectra, and solvers","text":"Typed selected-spectrum result.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.SpectrumResult"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.stationary_state","page":"Stationary states, spectra, and solvers","text":"stationary_state(x; algorithm=AutoAlgorithm(), basis=nothing,\n                 memory_budget=512*1024^2,\n                 return_info=false, kwargs...)\n\nHigh-level stationary-state command. Model and compiled-model inputs return a PIState; return_info=true returns a SteadyStateResult. The existing steady_state function remains the low-level coordinate-vector interface. Automatic selection preflights direct and matrix-free peaks. Explicit dense, direct, SVD, eigen, and shift-invert routes throw before materialization when their conservative structural peak exceeds memory_budget. Pass memory_budget=Inf to opt out explicitly. The returned information includes the complete resource_preflight report.\n\n\n\n\n\nstationary_state(model::GlobalPseudomodeModel;\n                 algorithm=AutoAlgorithm(),\n                 memory_budget=512*1024^2,\n                 return_info=false, kwargs...)\n\nHigh-level stationary-state solve for a PI ensemble coupled to one shared truncated pseudomode. The result is a CompositePIState. Automatic and iterative routes use the factorized matrix-free generator; no global Kronecker superoperator is assembled. AutoAlgorithm() selects matrix-free GMRES. Explicit choices are limited to GMRESAlgorithm and RecycledGMRESAlgorithm; dense, direct, and shift-invert algorithms are rejected.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.stationary_state"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.solve_dynamics","page":"Stationary states, spectra, and solvers","text":"solve_dynamics(x, rho0, tspan; algorithm=:auto, saveat=nothing,\n               steps_per_interval=64, parameters=nothing,\n               observables=nothing, save_states=true,\n               memory_budget=512*1024^2)\n\nCompile a model once when needed and propagate with the allocation-conscious fixed-step RK4 path by default. algorithm=:expv (or :krylov_expv) selects adaptive restarted-Arnoldi exponential action for an autonomous generator. Use ExpvAlgorithm to set its Krylov dimension, tolerances, and step controls. The result carries saved times and PI states and supports indexing and iteration. Use dynamics_problem directly for general adaptive SciML algorithms.\n\nPass a named tuple, dictionary, pair collection, or one local matrix/ PIOperator as observables. This returns a DynamicsStreamResult. With save_states=false, expectation values are accumulated while one mutable state is propagated, and no sampled state history is constructed. A local d-by-d matrix denotes its collective sum. Non-Hermitian observables are accepted and retain complex expectation values. A state-free call without an observable is rejected because it would return no dynamics output.\n\nBefore compiling a raw model, this command accounts for the matrix-free plan, the selected RK4 or Krylov exponential workspace, task-owned source-action scratch, saved states, sampled times, prepared observables, and scalar observable series. It throws when the known peak exceeds memory_budget; use save_states=false to stream output or memory_budget=Inf to opt out.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.solve_dynamics"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.liouvillian_spectrum","page":"Stationary states, spectra, and solvers","text":"liouvillian_spectrum(x; target=:largest_real, nev=6,\n                     algorithm=:auto, vectors=false,\n                     memory_budget=512*1024^2, return_info=false)\n\nConsistent high-level spectral command. target is one of :largest_real, :near_zero, or :largest_magnitude; method-specific sortby/which dialects remain available through the lower-level spectral functions. Automatic selection compares the complete dense-spectrum peak with the budget before choosing dense or matrix-free Arnoldi. Explicit dense requests exceeding the structural bound throw before materialization. memory_budget=Inf opts out. An explicit algorithm=:block_arnoldi uses the thick-restarted batched solver; its block_size is part of the resource preflight. With return_info=true, solver metadata and resource_preflight are returned without computing right eigenvectors unless vectors=true.\n\n\n\n\n\nliouvillian_spectrum(model::GlobalPseudomodeModel; kwargs...)\n\nCompute selected modes of a shared-pseudomode Liouvillian through its factorized matrix-free wrapper. Automatic selection uses Arnoldi (or harmonic Arnoldi for target=:near_zero); materializing dense-spectrum algorithms are rejected by the ordinary resource preflight.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.liouvillian_spectrum"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.diagnostics","page":"Stationary states, spectra, and solvers","text":"Unified diagnostics for PI states, models, and compiled/linear operators.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.diagnostics"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.pi_dimension","page":"Stationary states, spectra, and solvers","text":"Return the PI-coordinate dimension of a basis, state, model, or operator.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.pi_dimension"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.estimate_state_bytes","page":"Stationary states, spectra, and solvers","text":"estimate_state_bytes(x; T=ComplexF64,\n                     bigfloat_precision=precision(BigFloat))\n\nEstimated retained bytes occupied by one dense PI coordinate vector. Fixed-size isbits scalar types use the exact inline sizeof(T) value. Heap-backed BigFloat scalars use an explicitly conservative per-element bound at bigfloat_precision; pass the largest intended precision when it differs from the active process precision. Other heap-backed scalar types use a padded zero-value sample because their payload may not be bounded by their type; this route is an estimate, not a worst-case guarantee.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.estimate_state_bytes"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.estimate_basis_bytes","page":"Stationary states, spectra, and solvers","text":"Retained Julia heap size of an already constructed basis or plan.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.estimate_basis_bytes"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.estimate_liouvillian_bytes","page":"Stationary states, spectra, and solvers","text":"Return the retained Julia heap size in bytes of an assembled or matrix-free Liouvillian object.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.estimate_liouvillian_bytes"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.estimate_geometry_bytes","page":"Stationary states, spectra, and solvers","text":"estimate_geometry_bytes(basis; T=Float64,\n                        bigfloat_precision=precision(BigFloat))\n\nReturn a conservative structural memory estimate for construction and retention of the shared one-body Schur geometry.  The estimate performs no CG evaluation and reports exact BigInt byte counts.  setup_bytes is a peak live-storage upper bound for the sparse transition constructor, whereas retained_bytes describes the resulting read-only cache.  Allocator metadata and garbage-collector timing remain platform dependent, so benchmark a representative basis before a long scan. Fixed-size isbits geometry preserves the historical byte formula; heap-backed BigFloat tuple entries are inflated with the shared conservative bound at bigfloat_precision.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.estimate_geometry_bytes"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.estimate_solver_bytes","page":"Stationary states, spectra, and solvers","text":"estimate_solver_bytes(x; algorithm=:gmres, krylovdim=30, recycle_dim=0,\n                      block_size=4, T=ComplexF64,\n                      bigfloat_precision=precision(BigFloat))\n\nEstimate solver work-array storage, excluding the retained input operator. Fixed-size isbits values retain exact inline byte accounting; heap-backed BigFloat values use the same conservative precision-aware bound as estimate_state_bytes. GMRES real residual/history storage follows the real component type of T rather than assuming Float64. Matrix-free Krylov and RK4 formulas count their explicit arrays. Direct, shift-invert, dense eigenvalue, and SVD routes instead return conservative structural upper bounds for solver-owned matrix/factor arrays; allocator metadata and vendor-library internal buffers remain platform dependent.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.estimate_solver_bytes"},{"category":"function","location":"api/solvers/#PermutationalInvariantDynamics.recommend_solver","page":"Stationary states, spectra, and solvers","text":"recommend_solver(x; task=:steady_state, algorithm=:auto,\n                 memory_budget=512*1024^2, krylovdim=30, recycle_dim=0,\n                 nev=6, block_size=min(nev, 4), vectors=false,\n                 maxrestarts=20, samples=1, saved_states=1,\n                 observable_series=0, workers=1,\n                 bigfloat_precision=precision(BigFloat))\n\nReturn an assembly-free solver and resource preflight. The result preserves the historical flat byte fields and additionally provides resources=(setup, retained, solve, output, peak). Every component records its bytes, provenance (:actual, :upper_bound, :estimate, or :unknown), and explicit inclusions/exclusions. known_peak_bytes is the largest known setup/solve phase total; budget_status is :fits, :exceeds, :unknown, or :disabled, and safe_to_run is deliberately missing unless a measured or structural upper bound supports a safety claim.\n\nFor dynamics, samples is the number of retained time points, saved_states is the number of those points whose PI state is retained, and observable_series counts streamed scalar series. workers models concurrent task-owned workspaces with one shared prepared source. memory_budget=Inf explicitly disables budget enforcement. Model and CompiledPIModel inputs include one-body geometry only when their terms require it. Inputs carrying only basis metadata retain a conservative geometry allowance, identified by geometry_requirement=:conservative_unknown.\n\nCoordinate precision is inferred from x unless T is supplied. For a dynamics-only estimate, observable_type and time_type can describe wider prepared observables or saved-time vectors without incorrectly widening the state and selected RK4/Krylov-exponential workspace. Dense compatibility solvers account for their actual or conservatively bounded storage at no less than ComplexF64 precision. For task=:spectrum, algorithm=:block_arnoldi, block_size is included in the complete reusable block-workspace estimate rather than treated as a free matrix–matrix optimization. Prepared sources also contribute their bounded per-action materialization transient and any first-use batched Schur buffer; operator_action_per_worker_upper_bytes reports that part separately. Prepared dynamics additionally includes the mutable propagation state and the fresh task-owned application workspace constructed by the propagator.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.recommend_solver"},{"category":"section","location":"prepared_artifacts/#Reusing-prepared-representation-data","page":"Reusable prepared geometry","text":"Representation setup can dominate a parameter study even when every solve is matrix free. One-body branching maps, Appendix-D removal paths, and bipartition recouplers depend only on a PI basis and numerical coefficient context. They should therefore be prepared once and reused.\n\nusing PermutationalInvariantDynamics\n\nbasis = PIBasis(12, 2)\ngeometry = prepare_geometry(\n    basis;\n    pbody_orders=(2, 3),\n    reduction_ks=(1, 6),\n    memory_budget=512 * 1024^2,\n)\n\none_body = onebody_geometry(geometry)\npair_geometry = pbody_geometry(geometry, 2)\nhalf_partition = reduction_plan(prepared_reductions(geometry), 6)\n\nPreparedGeometryBundle is immutable and contains no mutable numerical workspace. It can be shared read-only, while each task still owns the workspaces used to apply operators or reductions. Every accessor checks exact PIBasis identity. The bundle also snapshots the sector and GT-pattern layout, and BigFloat bundles check the active precision and rounding mode.\n\nThe default coefficient_cache=:auto constructs one depth-bounded OneBoxCGCache and shares it across the requested one- and many-body geometry. Use coefficient_cache=nothing when a one-off call-local setup is preferable, or supply an existing basis-owned coefficient cache.","title":"Reusing prepared representation data"},{"category":"section","location":"prepared_artifacts/#Explicit-cache-for-recurring-requests","page":"Reusable prepared geometry","text":"When several parts of an application may independently request the same preparation, use a user-owned cache:\n\nstore = PreparationCache(memory_budget=512 * 1024^2)\n\ngeometry_a = prepare_geometry!(\n    store, basis;\n    pbody_orders=(2, 3),\n    reduction_ks=(1, 6),\n)\ngeometry_b = prepare_geometry!(\n    store, basis;\n    pbody_orders=(2, 3),\n    reduction_ks=(1, 6),\n)\n\n@assert geometry_a === geometry_b\npreparation_cache_summary(store)\n\nThe key includes exact basis object identity, scalar type, BigFloat precision and rounding mode, requested body orders, reduction sizes and tolerance, and coefficient-cache selection. Cache construction and lookup are synchronized. There is no process-global mutable cache.\n\nThe cache budget is checked before inserting a bundle. Its accounting conservatively sums each bundle's standalone summarysize, so a basis shared by two entries may be counted twice. evict_prepared_geometry! removes one exact bundle, and clear_preparation_cache! drops all cache-owned references.\n\nQudit ReductionPlanSet setup can require sparse rank-revealing QR and dense nullspace scratch for which no allocation-free upper bound is currently available. A bundle containing qudit reductions therefore requires the explicit memory_budget=Inf opt-out and rejects a finite budget before constructing any geometry. Prepare one- and p-body data separately when that opt-out is not acceptable.","title":"Explicit cache for recurring requests"},{"category":"section","location":"prepared_artifacts/#Why-bundles-are-not-persisted-yet","page":"Reusable prepared geometry","text":"PreparedGeometryBundle intentionally has no file save/load API. Its current contents include private sparse branching layouts and recoupling structures whose binary schema is not a public cross-version contract. Serializing those objects through Julia serialization or JLD2 would risk accepting stale coefficient conventions, package internals, or arithmetic contexts.\n\nReliable persistence requires a separate, versioned interchange schema with explicit reconstruction and validation of every partition, GT-pattern order, sparse support, scalar type, Julia/package version, and BigFloat context. Until that schema exists, reconstructing a bundle is safer than presenting a fragile disk cache as reusable research data. Mutable workspaces, callbacks, locks, and solver state must never be added to such a schema.","title":"Why bundles are not persisted yet"},{"category":"section","location":"prepared_artifacts/#API","page":"Reusable prepared geometry","text":"","title":"API"},{"category":"type","location":"prepared_artifacts/#PermutationalInvariantDynamics.PreparedGeometryBundle","page":"Reusable prepared geometry","text":"PreparedGeometryBundle\n\nImmutable, shareable collection of expensive representation data prepared for one exact PIBasis. A bundle may contain a OneBoxCGCache, a complete OneBodyGeometry, selected PBodyGeometry objects, and one ReductionPlanSet. It never contains mutable numerical workspaces, model callbacks, solver state, or locks.\n\nUse prepare_geometry for one bundle or prepare_geometry! with a user-owned PreparationCache when the same request may recur.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.PreparedGeometryBundle"},{"category":"function","location":"prepared_artifacts/#PermutationalInvariantDynamics.prepare_geometry","page":"Reusable prepared geometry","text":"prepare_geometry(basis; T=Float64, one_body=true, pbody_orders=(),\n                 reduction_ks=nothing, reduction_atol=2e-11,\n                 coefficient_cache=:auto,\n                 memory_budget=512 * 1024^2)\n\nPrepare one immutable bundle without a store. coefficient_cache=:auto retains a depth-bounded, basis-owned OneBoxCGCache shared by the requested geometries. Pass nothing to use call-local coefficient construction, or pass an existing compatible cache.\n\nKnown one-body and p-body setup peaks are checked before construction. Retained storage, including an optional ReductionPlanSet, is checked before the bundle is returned. Inf is the only explicit budget opt-out.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.prepare_geometry"},{"category":"function","location":"prepared_artifacts/#PermutationalInvariantDynamics.validate_prepared_geometry","page":"Reusable prepared geometry","text":"validate_prepared_geometry(bundle[, basis])\n\nValidate exact basis ownership, the retained basis layout, scalar precision, and every contained geometry/plan link. The check does not allocate numerical scratch or modify the bundle.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.validate_prepared_geometry"},{"category":"function","location":"prepared_artifacts/#PermutationalInvariantDynamics.onebody_geometry","page":"Reusable prepared geometry","text":"onebody_geometry(bundle[, basis])\n\nReturn the complete prepared one-body geometry after validating its exact basis and arithmetic context. Raise when the bundle was prepared without it.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.onebody_geometry"},{"category":"function","location":"prepared_artifacts/#PermutationalInvariantDynamics.pbody_geometry","page":"Reusable prepared geometry","text":"pbody_geometry(bundle, p[, basis])\n\nReturn the prepared geometry for body order p, preserving the order supplied to prepare_geometry.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.pbody_geometry"},{"category":"function","location":"prepared_artifacts/#PermutationalInvariantDynamics.prepared_reductions","page":"Reusable prepared geometry","text":"prepared_reductions(bundle[, basis])\n\nReturn the bundle's ReductionPlanSet after exact validation. Raise when no particle reductions were requested.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.prepared_reductions"},{"category":"type","location":"prepared_artifacts/#PermutationalInvariantDynamics.PreparationCache","page":"Reusable prepared geometry","text":"PreparationCache(; memory_budget=512 * 1024^2)\n\nExplicit, user-owned store for PreparedGeometryBundle objects. Entries are partitioned by exact PIBasis object identity and by scalar, precision, rounding, geometry-selection, and reduction-selection request. The cache is synchronized for concurrent lookup/construction, but prepared workspaces remain task-owned as usual.\n\nThe byte budget applies to the conservative sum of each retained bundle's standalone summarysize; shared basis/cache objects may therefore be counted more than once. This favors a predictable upper bound over hidden retention. No process-global cache is used.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.PreparationCache"},{"category":"function","location":"prepared_artifacts/#PermutationalInvariantDynamics.prepare_geometry!","page":"Reusable prepared geometry","text":"prepare_geometry!(cache, basis; kwargs...)\n\nReturn the exactly matching cached bundle, or prepare and retain one new bundle. Construction is serialized per cache so two tasks cannot duplicate an expensive miss. Cache capacity is checked before insertion; failure leaves the cache unchanged.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.prepare_geometry!"},{"category":"function","location":"prepared_artifacts/#PermutationalInvariantDynamics.evict_prepared_geometry!","page":"Reusable prepared geometry","text":"evict_prepared_geometry!(cache, bundle)\n\nRemove the exact bundle object from cache. Return true when it was present and false otherwise.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.evict_prepared_geometry!"},{"category":"function","location":"prepared_artifacts/#PermutationalInvariantDynamics.clear_preparation_cache!","page":"Reusable prepared geometry","text":"clear_preparation_cache!(cache)\n\nDrop all retained bundle references and reset accounting. Existing bundle objects held by callers remain valid.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.clear_preparation_cache!"},{"category":"function","location":"prepared_artifacts/#PermutationalInvariantDynamics.preparation_cache_summary","page":"Reusable prepared geometry","text":"preparation_cache_summary(cache)\n\nReturn stable cache accounting without exposing its mutable dictionaries.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.preparation_cache_summary"},{"category":"section","location":"spectral_visualization/#Visualizing-density,-Liouvillian,-and-Floquet-spectra","page":"Spectral visualization","text":"The spectral visualizers render dependency-free SVG while keeping spectral computation separate from presentation. Liouvillian and Floquet data use ComplexSpectrum and visualize_spectrum. Density operators instead use the multiplicity-compressed result of pi_density_spectrum and the physically distinct visualize_density_spectrum; density eigenvalues are not decay modes or Floquet multipliers.","title":"Visualizing density, Liouvillian, and Floquet spectra"},{"category":"section","location":"spectral_visualization/#Steady-state-density-spectrum","page":"Spectral visualization","text":"After solving a stationary state, diagonalize each physical Schur block once and reuse the compressed result:\n\nresult = stationary_state(prepared; return_info=true)\nrho_ss = result.state\n\ndensity = pi_density_spectrum(rho_ss)\nfigure = visualize_density_spectrum(\n    density;\n    title=\"Steady-state density spectrum\",\n    show_degeneracies=true)\ndisplay(figure)\n\nThe horizontal coordinate is the compressed eigenmode rank and the vertical coordinate is the unmodified real eigenvalue. Colours identify Schur sectors. Each tooltip gives the partition, its within-sector eigenvalue index, and the exact symmetric-group degeneracy. With show_degeneracies=true, an ×g annotation displays the same exact multiplicity next to a point.\n\nThe numerical result contains one eigenvalue per eigenvector of each physical block, not one entry per full-Hilbert-space eigenvector. Its fields include values, exact BigInt degeneracies, sectors, sector_indices, and total_dimension. For a complete basis, total_dimension == d^N; for a restricted basis it is the retained Hilbert dimension. The weighted identity\n\nsum(g * value for (g, value) in zip(\n    density.degeneracies, density.values)) ≈ 1\n\nchecks normalization without expanding the spectrum. Do not pass expanded=true for visualization: expansion can require exponentially many entries and discards the Schur-sector bookkeeping that makes the plot useful.\n\nThe convenience form\n\nfigure = visualize_density_spectrum(\n    rho_ss; spectrum_kwargs=(atol=1e-12,))\n\ncomputes the compressed spectrum once. Prefer passing a previously computed result when its values are also used quantitatively. Visualization never clips negative eigenvalues. Values below the presentation tolerance receive a red outline; presentation_atol and presentation_rtol control only that styling and do not repair the state or change any stored eigenvalue. Exact zero values remain visible on the zero reference line.","title":"Steady-state density spectrum"},{"category":"section","location":"spectral_visualization/#Liouvillian-spectrum","page":"Spectral visualization","text":"For a modest PI dimension, obtain a complete spectrum through the existing high-level solver and render it in the complex plane:\n\ndata = liouvillian_spectrum_data(\n    prepared;\n    target=:largest_real,\n    nev=pi_dimension(prepared),\n    algorithm=:dense,\n)\nfigure = visualize_spectrum(data; title=\"Liouvillian spectrum\")\ndisplay(figure)\n\nThe horizontal coordinate is the decay or growth rate real(lambda) and the vertical coordinate is the oscillation frequency imag(lambda). The line real(lambda)=0 is the stability boundary. Points are classified, without modifying their values, as:\n\n:stationary when lambda is numerically zero;\n:peripheral when its real part is zero but its frequency is nonzero;\n:decaying in the open left half-plane; or\n:unstable in the open right half-plane.\n\nThe tolerance used for this visual classification is stored in data.tolerance; it is not an eigensolver residual and is not used to clip or move a mode.\n\nFor a computed Liouvillian source, solver tolerances use the usual atol and rtol keywords, while classification_atol and classification_rtol control only the marker classes. For an already computed value vector or SpectrumResult, atol and rtol classify the stored values because no solver is run. Residual arrays keep their own numerical precision.\n\nFor a large PI dimension, pass selected matrix-free modes that have already been computed:\n\nmodes = krylov_liouvillian_spectrum(\n    prepared.operator;\n    nev=12,\n    krylovdim=48,\n    which=:LR,\n    require_convergence=true,\n)\ndata = liouvillian_spectrum_data(modes)\ndisplay(visualize_spectrum(data))\n\nResidual and convergence arrays remain aligned with the original mode order. A selected window is marked partial. Its rightmost displayed point is only the displayed spectral abscissa: a partial ordinary, harmonic, IRAM, or Jacobi–Davidson plot cannot by itself certify the global gap, the full stationary multiplicity, or the absence of an omitted unstable mode.","title":"Liouvillian spectrum"},{"category":"section","location":"spectral_visualization/#Floquet-multipliers","page":"Spectral visualization","text":"If the one-period propagator has already been converged, reuse it directly:\n\nF = floquet_propagator(periodic_model, period; steps=256)\ndata = floquet_spectrum_data(\n    F; period=period, representation=:multipliers)\ndisplay(visualize_spectrum(data; title=\"Floquet multipliers\"))\n\nThe map F is diagonalized once when the data object is constructed. The SVG shows the unit circle as the discrete-time stability boundary and marks the fixed multiplier mu=1. Multipliers are classified as :fixed, :peripheral, :contracting, or :unstable; values outside the unit disk remain visible and are never projected back into it.\n\nThe source convenience form constructs the one-period map once and then diagonalizes it:\n\ndata = floquet_spectrum_data(\n    periodic_model, period;\n    steps=256,\n    representation=:multipliers,\n)\n\nAlthough the instantaneous Liouvillian can be matrix free, the current Floquet propagator and its complete eigenspectrum are dense at the PI coordinate dimension. Reuse a previously computed F or multiplier vector when changing the figure or comparing logarithm branches.","title":"Floquet multipliers"},{"category":"section","location":"spectral_visualization/#Principal-branch-Floquet-exponents","page":"Spectral visualization","text":"With a positive period, multipliers can be converted to exponents\n\nxi_j=mathrmLog(mu_j)T\nqquad -piT leq mathrmImxi_j leq piT\n\nmultipliers = floquet_multipliers(F)\nexponents = floquet_spectrum_data(\n    multipliers;\n    input=:multipliers,\n    period=period,\n    representation=:exponents,\n    complete=true,\n)\ndisplay(visualize_spectrum(exponents))\n\nThe exponent view shows real(xi)=0 and the principal quasifrequency boundaries imag(xi)=±π/T. Both endpoints are written because Julia's principal complex logarithm distinguishes the signed-zero lips of the negative-real branch cut; away from that cut the usual half-open convention applies. A zero multiplier has no finite logarithm and is rejected with an error; use the multiplier representation rather than silently omitting it or replacing it by a finite value. Passing input=:exponents, representation=:multipliers performs the inverse conversion explicitly. Supplied exponent data with input=:exponents, representation=:exponents are preserved verbatim and may lie outside the principal zone; their default plot title therefore says only “Floquet exponents.” The metadata.branch field is :principal only when the library actually applied Log to multipliers.","title":"Principal-branch Floquet exponents"},{"category":"section","location":"spectral_visualization/#Reusing-results-and-controlling-the-viewport","page":"Spectral visualization","text":"liouvillian_spectrum_data accepts a raw eigenvalue vector, a SpectrumResult, or a spectral named tuple with values and optional residuals, converged, and dimension fields. floquet_spectrum_data similarly accepts a multiplier or exponent vector. Input order and repeated eigenvalues are retained. If residuals accompany a converted Floquet vector, they remain diagnostics of the input eigenproblem and are not rescaled; inspect data.metadata.residual_representation for their units.\n\nRendering options include title, width, height, marker_size, show_legend, show_indices, xlimits, and ylimits. Explicit limits do not delete numerical data; the footer reports how many points lie outside the viewport. Multiplier plots preserve equal aspect ratio so the unit circle is geometrically meaningful.\n\nfocused = visualize_spectrum(\n    data;\n    xlimits=(-0.2, 0.02),\n    ylimits=(-1.0, 1.0),\n    marker_size=5,\n    show_indices=true,\n)","title":"Reusing results and controlling the viewport"},{"category":"section","location":"spectral_visualization/#Saving-SVG","page":"Spectral visualization","text":"mktempdir() do directory\n    path = joinpath(directory, \"spectrum.svg\")\n    save_spectrum_visualization(path, figure)\nend\n\nFor a density spectrum, use the corresponding writer:\n\nsave_density_spectrum_visualization(\n    joinpath(directory, \"density-spectrum.svg\"), density_figure)\n\nSVG text, titles, tooltips, axes, and reference boundaries are generated without a plotting package. See the runnable examples/irrep_block_visualization.jl example for a solved steady-state Schur-population and compressed-density-spectrum workflow, and examples/spectral_visualization.jl for complete Liouvillian, multiplier, and exponent workflows with numerical checks.","title":"Saving SVG"},{"category":"section","location":"releasing/#Release-and-General-registration","page":"Release and General registration","text":"The repository contains the metadata and automation needed for registration, but registration itself must be triggered by a maintainer after reviewing a clean release commit. Do not register an unreviewed working tree.\n\nThe first-release sequence is deliberately one-way:\n\nclean candidate\n    -> assign one matching CHANGELOG/CITATION release date\n    -> run the finalized release gate and green CI\n    -> ask Registrator to open the General pull request\n    -> merge the General pull request\n    -> let TagBot create the immutable version tag\n    -> let tag-triggered CI verify the tag and build versioned docs\n\nDo not create, move, or reuse the version tag by hand.","title":"Release and General registration"},{"category":"section","location":"releasing/#Release-gate","page":"Release and General registration","text":"The dependency-free metadata gate checks the package/citation/changelog version, release-date state, GPL identifier and canonical license, public URLs, required copyright/provenance/third-party records, the tracked-manifest policy, generated-output paths, and optionally the complete nonignored worktree.\n\nDuring ordinary development and candidate preparation, run:\n\njulia --startup-file=no scripts/release_gate.jl \\\n  --expect-version X.Y.Z --require-clean\n\nIt deliberately accepts a synchronized release candidate whose changelog is still marked Unreleased. Before asking Registrator to act, replace Unreleased with the intended release date, add the same date-released to CITATION.cff, and run the stricter gate:\n\njulia --startup-file=no scripts/release_gate.jl \\\n  --expect-version X.Y.Z --require-clean --require-released\n\nIf publication must move to another date, update both files in a new candidate commit and repeat every final check before registration. Do not repair release metadata after a version tag has been published. The script only validates local metadata: it never creates or moves a tag, publishes a GitHub release, or contacts General.\n\nThe CI workflow has a manual release_readiness input. Dispatch it with that input enabled on the exact dated release commit. This makes the hosted metadata job use --require-released; an ordinary push or pull request keeps using the candidate gate.\n\nBefore changing the public version:\n\nRun the full test suite on Julia 1.10 and current stable Julia from a clean checkout, plus the threaded performance gates, Aqua/JET, documentation, and representative examples. The same dependency-free numerical examples used by CI can be run locally as two isolated shards:\njulia --startup-file=no --project=. test/run_quick_examples.jl --shard 1/2\njulia --startup-file=no --project=. test/run_quick_examples.jl --shard 2/2\nThese shards execute the examples' default numerical assertions with rendering explicitly disabled; they do not substitute smaller tolerances or weaker physics checks.\nConfirm that docs/Manifest.toml is the only tracked Manifest.toml. Root, quality, example, benchmark, comparison, optional-test, and notebook manifests are generated locally and must remain untracked. Also confirm that no generated figure, token, private key, or deploy credential is tracked.\nReview every public API change, numerical convention, convergence claim, and literature comparison. Update CHANGELOG.md, Project.toml, and the version in CITATION.cff together.\nAudit ownership and provenance before relying on the GPL declaration: confirm contributor and employer/institution rights; review COPYRIGHT.md, PROVENANCE.md, and THIRD_PARTY_NOTICES.md; retain the original notice, license, exact upstream revision, and modification record for adapted code; and verify the provenance ledger for curated figures. Review the browser assistant's generated-code license separately because its output contains reusable template text.\nConfirm LICENSE is the canonical GPL-3.0-only text and that repository, documentation, and citation URLs agree. Re-audit licenses for dependencies and binary artifacts whenever distributing a sysimage, application, container, or offline bundle rather than only package source. Confirm every external GitHub Action is pinned to a reviewed full commit SHA; Dependabot's monthly GitHub-Actions pull requests are the update path for those pins.\nRun reuse lint and require the pinned REUSE licensing CI job to pass. This verifies file-level declarations and every license text independently of the package's metadata gate.\nAssign the intended release date to both the current CHANGELOG.md heading and CITATION.cff, then run the strict --require-released gate.\nRequire green GitHub CI and documentation checks on the exact dated commit that will be registered. In particular, confirm both executable examples shards and the manually dispatched release metadata job with release_readiness=true.","title":"Release gate"},{"category":"section","location":"releasing/#Documentation-deployment","page":"Release and General registration","text":"Documenter publishes generated pages to the gh-pages branch. In repository Settings → Pages, select Deploy from a branch, then choose gh-pages and /(root). The branch is generated deployment output: do not merge it into main or edit it as package source.\n\nThe documentation workflow currently deploys with the job-scoped GITHUB_TOKEN. The repository also uses an SSH deploy key for TagBot and CompatHelper. Generate that key pair with DocumenterTools for this repository, then install it as follows:\n\nAdd the public key under Settings → Deploy keys, enable write access, and give it a repository-specific name.\nAdd the corresponding private value under Settings → Secrets and variables → Actions as DOCUMENTER_KEY.\nNever paste either private key material or the secret value into a tracked file, issue, workflow log, or pull request.\n\nAfter the first deployment, verify that the gh-pages branch exists and that the Pages source still points to its root. To recover a missing or stale site, run the Documentation workflow manually from main using workflow_dispatch. If the deployment cannot push, verify the workflow's contents: write permission and the repository Actions policy. If TagBot or CompatHelper reports SSH authentication failures, replace the deploy-key pair and update DOCUMENTER_KEY, then rerun the failed workflow. A successful manual deployment should recreate or update gh-pages; it must not require a generated documentation commit on main.","title":"Documentation deployment"},{"category":"section","location":"releasing/#Registering","page":"Release and General registration","text":"Install the JuliaRegistrator GitHub App for the repository. Configure a GitHub tag ruleset for v* that prevents published tags from being updated or deleted except by the narrowly authorized release automation.\n\nOnly after the exact dated commit passes the strict local gate and the manual release-readiness CI run, comment\n\n@JuliaRegistrator register\n\non that commit or its issue. Review the generated General-registry pull request. Once it merges, TagBot uses the repository's configured credentials to create the version tag; do not create the tag in advance. The tag push runs the release-metadata gate with --require-released --tag-ref refs/tags/vX.Y.Z. That check verifies the tag name, dated package metadata, and that the workflow checkout is the commit targeted by the tag. The same tag then builds versioned documentation.\n\nIf General does not merge on the assigned date, stop the release, close or supersede the pending registration, update both date fields, and rerun the strict gate and hosted checks on the new commit. Never move a published tag.\n\nFor later releases, increase the semantic version, update the changelog, and repeat the same clean-checkout gate. Never reuse or move an existing version tag.","title":"Registering"},{"category":"section","location":"assets/example_figures/README/#Curated-example-output","page":"Curated example output","text":"Copyright (C) 2026 PermutationalInvariantDynamics.jl contributors. These curated assets are distributed under GPL-3.0-only with the rest of the repository. They were generated locally from the paired package examples; no publisher-provided or digitized journal artwork is included.\n\nThese PNG and SVG files are lightweight, reviewable snapshots of the output produced by the paired scripts in examples/. They let GitHub render the expected numerical result directly in each example guide without running a solver during documentation builds.\n\nThe numerical assertions in the Julia scripts are the regression tests. The images are explanatory snapshots, not pixel-level test or convergence certificates. Stochastic snapshots use the seed recorded in their script; timings and uncertainty bands remain specific to the documented default run.\n\nTo update a CairoMakie snapshot:\n\ninstantiate the optional examples environment;\nrun the source script with its default checked controls;\ninspect both the console assertions and the generated examples/figures/<stem>.png;\ncopy only the reviewed PNG here; and\ncheck that its paired guide embeds it.\n\nRecord a new external data source, unpublished manuscript, or third-party asset in PROVENANCE.md before adding the snapshot. Confirm redistribution permission explicitly; a paper citation or open-access status alone is not permission to copy its figures. Keep the generating script, parameter values, seed, and numerical assertions sufficient for a maintainer to regenerate and review the output.\n\nDependency-free SVG snapshots are saved from the already computed visualization objects. Do not regenerate any of these assets in ordinary CI or in a Documenter build: several literature, HEOM, and trajectory examples are intentionally too costly or stochastic for that workflow.","title":"Curated example output"},{"category":"section","location":"diffusive_monitoring/#Diffusive-conditional-dynamics","page":"Diffusive monitoring","text":"diffusive_trajectory evolves a PI density operator conditioned on collective homodyne or heterodyne records.  The unconditional PIModel and the monitored operators are deliberately separate:\n\ndrho_c = mathcal Lrho_cdt\n + sqrtetamathcal He^-iphicrho_cdW\n\nwhere\n\nmathcal Harho=arho+rho a^dagger\n-mathrmtr(a+a^dagger)rhorho\n\nThus model must already contain the dissipator associated with c.  A monitor adds the innovation and the measurement record only; it never inserts a second dissipator implicitly.","title":"Diffusive conditional dynamics"},{"category":"section","location":"diffusive_monitoring/#Prepared-workflow","page":"Diffusive monitoring","text":"basis = PIBasis(20, 2)\nsm = ComplexF64[0 1; 0 0]\ngamma = 0.2\nmodel = PIModel(basis, (CollectiveJump(sm; rate=gamma),))\nrho0 = computational_product_state(basis, 2)\n\n# The collapse operator is sqrt(gamma) * J_-.\nmonitor = homodyne_monitor(sqrt(gamma) * sm;\n    efficiency=0.75, phase=0, label=:emission)\nplan = DiffusivePlan(model, monitor)\nworkspace = DiffusiveWorkspace(plan, rho0)\n\nresult = diffusive_trajectory(\n    plan, rho0, range(0, 1; length=101);\n    dt=0.001, workspace, rng=MersenneTwister(4),\n    observables=(magnetization=collective_spin(basis, :z),),\n)\n\nFor repeated or threaded ensembles, prepare the complete sampling request once. DiffusiveBatchPlan validates and stores the output grid, time step, and observable plans; DiffusiveBatchWorkspace owns one numerical workspace and random stream per worker:\n\nbatch = DiffusiveBatchPlan(plan, rho0, range(0, 1; length=101);\n    dt=0.001,\n    observables=(magnetization=collective_spin(basis, :z),))\nbatch_workspace = DiffusiveBatchWorkspace(batch, rho0; workers=Threads.nthreads())\npaths = diffusive_trajectories(batch, rho0, 1000;\n    workspace=batch_workspace, seed=7, threaded=true)\n\nThe immutable batch plan is shareable. Its workspace is mutable, reusable sequentially, and must not be shared by concurrent callers.\n\nA one-particle monitor matrix is always lifted to the collective operator sum_i c^(i).  Passing a prepared PIOperator monitors that PI operator directly.  Particle-resolved local records reveal particle labels and do not preserve the PI conditional state, so this API rejects them rather than silently averaging a non-PI measurement.\n\nheterodyne_monitor produces orthogonal I/Q records with two independent real Wiener processes and strength sqrteta2 per quadrature.  Efficiencies and phases may be scalar schedules (time, parameters).  Their values are validated at every step.","title":"Prepared workflow"},{"category":"section","location":"diffusive_monitoring/#Records,-ensembles,-and-numerical-scope","page":"Diffusive monitoring","text":"DiffusiveTrajectory.records and .innovations are cumulative at the output times.  Their rows are identified by record_labels; heterodyne labels carry :I and :Q suffixes.  save_states=false discards conditional state history while retaining these records and optional observable samples.\n\npaths = diffusive_trajectories(\n    plan, rho0, range(0, 1; length=51), 1000;\n    dt=0.001, seed=7, threaded=true,\n)\nrho_mean = diffusive_average(paths)\n\nStreams are derived from trajectory index, so serial and threaded calls are reproducible.  Each worker owns one DiffusiveWorkspace; the immutable plan is shared.\n\nThe current integrator is normalized Euler–Maruyama.  It is preallocated and converges to the Itô SME, but finite steps do not by themselves certify positivity.  Converge dt, and call validate_state when strict conditional state auditing is required.  The ensemble mean converges to the unconditional master equation already stored in the model.","title":"Records, ensembles, and numerical scope"},{"category":"section","location":"diffusive_monitoring/#Confidence-controlled-stopping","page":"Diffusive monitoring","text":"adaptive_quantum_trajectories, adaptive_weak_pi_quantum_trajectories, and adaptive_diffusive_trajectories process deterministic batches until every requested observable at every saved time has a confidence half-width below atol + rtol*abs(mean). They retain only online means and variances, not trajectory state histories. The result reports converged=false and stopping_reason=:maximum_trajectories when the sample cap is reached; it never silently accepts the cap as convergence.\n\nStopping uses a finite-horizon simultaneous empirical-Bernstein bound. For each observable and saved time, the implementation combines its sample variance with a certified spectral-range bound of the Hermitian PI operator. The failure probability 1-confidence is divided across all requested observable/time pairs and every possible batch check up to max_trajectories. Consequently, seeing no rare jump in an early batch does not produce a zero-width certificate for a nonconstant observable. This bound controls the complete adaptive stopping request; the pointwise normal intervals retained in result.observables remain useful descriptive output but are not the stopping certificate. The simultaneous half-width and worst tolerance ratio used at each check are stored in convergence_history. metadata.effective_independent_samples is the number of separately seeded trajectories used by the bound; this independence statement does not cover time-integration or finite-time initialization bias. Every sampled value is checked against the padded spectral bound. A value outside it raises instead of producing an invalid confidence claim. This observed-value check is not, by itself, a bounded-support proof for the finite-step diffusive sampler: Euler–Maruyama is not positivity preserving and its Gaussian increments have unbounded support. The diffusive empirical-Bernstein result is therefore a Monte Carlo stopping diagnostic conditional on a separate dt convergence and conditional-state physicality study, not an unconditional coverage certificate. Quantum-jump paths likewise require their deterministic no-jump integrator tolerances to be converged.\n\nadaptive = adaptive_diffusive_trajectories(batch, rho0;\n    min_trajectories=128, max_trajectories=20_000,\n    batch_size=64, confidence=0.95, rtol=0.02,\n    seed=7, threaded=true)\n\nThe quantum-jump variant takes a model or TrajectoryPlan, times, dt, and an observable collection. Both routes preserve trajectory-index-derived random streams, so serial and threaded runs of the same request use the same samples at each checked batch boundary. Parallel accumulation can change the last floating-point bits because worker partial sums are merged in a different order; scientific reproducibility should therefore compare at the requested statistical tolerance rather than assume bitwise identity.\n\nThe stopping certificate controls Monte Carlo sampling only. Converge the quantum-jump integrator or diffusive Euler–Maruyama step separately, and do not reuse sampling convergence as evidence for a hierarchy, finite-size, or model approximation. Optional quantum-jump summaries can still retain pooled waiting times; disable them when that jump-count-scaled storage is unnecessary. Adaptive stopping currently covers density-valued PI jump paths, weak-PI pseudo-kets, and collective diffusive paths, but not Distributed workers.\n\nThe runnable homodyne_pi_trajectories.jl compares the conditional paths, their ensemble average, and the PI master equation.","title":"Confidence-controlled stopping"},{"category":"section","location":"diffusive_monitoring/#API","page":"Diffusive monitoring","text":"","title":"API"},{"category":"type","location":"diffusive_monitoring/#PermutationalInvariantDynamics.DiffusiveMonitor","page":"Diffusive monitoring","text":"DiffusiveMonitor(operator; kind=:homodyne, efficiency=1, phase=0,\n                 label=:monitor)\nhomodyne_monitor(operator; efficiency=1, phase=0, label=:homodyne)\nheterodyne_monitor(operator; efficiency=1, phase=0, label=:heterodyne)\n\nDescribe a continuously monitored collective PI channel.  A one-particle matrix is interpreted as the collective operator sum_i operator^(i); a PIOperator is used directly.  efficiency must lie in [0,1], and phase is the local-oscillator phase.  Either scalar may follow the package (time, parameters) schedule convention.\n\nThe monitor contributes only the normalized Ito innovation.  Its unconditional dissipator must already be present in the PIModel; this separation avoids double counting and also permits partially monitored channels.  Homodyne monitoring produces one real Wiener record.  Heterodyne monitoring uses two orthogonal real records, each with innovation strength sqrt(efficiency/2). Particle-resolved local monitoring is not PI and is deliberately unsupported.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.DiffusiveMonitor"},{"category":"function","location":"diffusive_monitoring/#PermutationalInvariantDynamics.homodyne_monitor","page":"Diffusive monitoring","text":"Construct a homodyne DiffusiveMonitor with one real record.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.homodyne_monitor"},{"category":"function","location":"diffusive_monitoring/#PermutationalInvariantDynamics.heterodyne_monitor","page":"Diffusive monitoring","text":"Construct a heterodyne DiffusiveMonitor with I/Q records.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.heterodyne_monitor"},{"category":"type","location":"diffusive_monitoring/#PermutationalInvariantDynamics.DiffusivePlan","page":"Diffusive monitoring","text":"DiffusivePlan(model, monitors; T=nothing)\nDiffusivePlan(compiled, monitors; T=nothing)\n\nPrepare an immutable PI stochastic-master-equation plan.  monitors may be a single DiffusiveMonitor, a tuple/vector, or a named tuple whose keys replace the monitor labels.  Physical Schur blocks of all fixed monitored operators and the unconditional matrix-free Liouvillian are prepared once.\n\nT only selects the real floating precision of an otherwise empty model; a nonempty model infers precision from its compiled terms.  A plan is task safe, but each concurrent realization needs a separate DiffusiveWorkspace.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.DiffusivePlan"},{"category":"type","location":"diffusive_monitoring/#PermutationalInvariantDynamics.DiffusiveWorkspace","page":"Diffusive monitoring","text":"DiffusiveWorkspace(plan, rho)\n\nCaller-owned scratch for one diffusive conditional realization.  Reuse it sequentially; use one workspace per concurrent task.  The Euler–Maruyama hot step applies the unconditional Liouvillian and all Schur-block innovations without constructing a global superoperator or allocating integration arrays.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.DiffusiveWorkspace"},{"category":"type","location":"diffusive_monitoring/#PermutationalInvariantDynamics.DiffusiveBatchPlan","page":"Diffusive monitoring","text":"DiffusiveBatchPlan(source, rho0, times, monitors=nothing;\n                   dt, observables=nothing)\n\nPrepare the immutable setup shared by a batch of diffusive trajectories.  In addition to the DiffusivePlan, this stores the validated time grid, integration step, and concrete tuple of Hermitian observable operators.  A batch therefore performs no repeated time conversion, collective-operator construction, or observable validation for individual paths.\n\nThe plan is read-only and may be shared across tasks.  Mutable stochastic scratch belongs to DiffusiveBatchWorkspace.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.DiffusiveBatchPlan"},{"category":"type","location":"diffusive_monitoring/#PermutationalInvariantDynamics.DiffusiveBatchWorkspace","page":"Diffusive monitoring","text":"DiffusiveBatchWorkspace(batch, rho0; workers=1)\n\nTask-owned mutable workspaces and trajectory-indexed RNG storage for a prepared diffusive batch.  Use at least as many workers as concurrent tasks. One batch workspace may be reused sequentially, but not by overlapping batch calls.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.DiffusiveBatchWorkspace"},{"category":"type","location":"diffusive_monitoring/#PermutationalInvariantDynamics.DiffusiveTrajectory","page":"Diffusive monitoring","text":"DiffusiveTrajectory\n\nOne PI stochastic-master-equation realization.  records and innovations are cumulative real quadrature records sampled at times, with row labels in record_labels.  states is nothing when save_states=false.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.DiffusiveTrajectory"},{"category":"function","location":"diffusive_monitoring/#PermutationalInvariantDynamics.diffusive_trajectory","page":"Diffusive monitoring","text":"diffusive_trajectory(source, rho0, times, monitors; dt,\n                     rng=Random.default_rng(), parameters=nothing,\n                     workspace=nothing, save_states=true,\n                     observables=nothing)\n\nIntegrate one normalized PI homodyne/heterodyne stochastic master equation with preallocated Euler–Maruyama microsteps.  source is a PIModel, a CompiledPIModel, or a prepared DiffusivePlan.  The returned measurement and innovation arrays are cumulative at the requested times.\n\nThe unconditional source must already contain every monitored dissipator. Decrease dt to convergence.  Euler–Maruyama is trace-normalized but is not a finite-step positivity certificate; use validate_state on saved states when strict physicality auditing is required.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.diffusive_trajectory"},{"category":"function","location":"diffusive_monitoring/#PermutationalInvariantDynamics.diffusive_trajectories","page":"Diffusive monitoring","text":"diffusive_trajectories(source, rho0, times, monitors, n;\n                       seed=0, threaded=false, kwargs...)\n\nGenerate n reproducible diffusive PI trajectories.  Random streams are derived from trajectory index, so ordered results are independent of threaded scheduling.  Each worker owns a reusable DiffusiveWorkspace.\n\n\n\n\n\ndiffusive_trajectories(batch, rho0, n; seed=0, threaded=false,\n                       workspace=nothing, parameters=nothing,\n                       save_states=true)\n\nGenerate n trajectories from a DiffusiveBatchPlan.  Prepared times and observables are reused by every path.  Random streams are derived from the trajectory index, so serial and threaded results agree for a fixed seed. memory_budget covers saved states, times, fixed-size measurement and innovation records, and requested observable series; prepared plans and reusable worker scratch are additional.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.diffusive_trajectories"},{"category":"function","location":"diffusive_monitoring/#PermutationalInvariantDynamics.diffusive_average","page":"Diffusive monitoring","text":"diffusive_average(trajectories)\n\nAverage saved conditional PI states at every sampling time.  The result is an unconditional state estimate; all trajectories must share the same basis and time grid and must have been created with save_states=true.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.diffusive_average"},{"category":"type","location":"diffusive_monitoring/#PermutationalInvariantDynamics.AdaptiveTrajectoryResult","page":"Diffusive monitoring","text":"AdaptiveTrajectoryResult\n\nResult of a confidence-controlled trajectory ensemble.  observables uses the same time-resolved mean, variance, standard-error, and confidence-interval schema as TrajectoryEnsembleResult.  convergence_history records the worst simultaneous empirical-Bernstein half-width and worst tolerance ratio at every checked batch boundary. The pointwise normal intervals in observables are descriptive and are not substituted for this stopping certificate. converged=false is returned explicitly when max_trajectories is reached; the routine never labels that result converged. metadata.effective_independent_samples reports the trajectory count used by the stopping bound.  Its independence assumption concerns separately seeded trajectories; it does not remove time-discretization, settling, or model bias.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.AdaptiveTrajectoryResult"},{"category":"function","location":"diffusive_monitoring/#PermutationalInvariantDynamics.adaptive_quantum_trajectories","page":"Diffusive monitoring","text":"adaptive_quantum_trajectories(source, rho0, times;\n    observables, dt, min_trajectories=64, max_trajectories=10_000,\n    batch_size=32, confidence=0.95, atol=nothing, rtol=nothing,\n    seed=0, threaded=false, jump_statistics=false,\n    trajectory_keywords...)\n\nRun state-free PI quantum-jump trajectories until every requested observable at every saved time satisfies\n\nh_ot leq mathrmatol+mathrmrtolmathrmmean_ot\n\nThe convergence decision uses a finite-horizon simultaneous empirical- Bernstein bound derived from Hermitian-observable spectral ranges. It is evaluated only after complete batches and never before min_trajectories; zero observed variance therefore does not by itself imply zero uncertainty for a nonconstant observable. If the target is not reached, max_trajectories samples are returned with converged=false.  Model lowering, time conversion, observable construction, workspaces, and RNG storage are prepared once.  No sampled state history is retained.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.adaptive_quantum_trajectories"},{"category":"function","location":"diffusive_monitoring/#PermutationalInvariantDynamics.adaptive_weak_pi_quantum_trajectories","page":"Diffusive monitoring","text":"adaptive_weak_pi_quantum_trajectories(source, state0, times;\n    observables, dt, min_trajectories=64, max_trajectories=10_000,\n    batch_size=32, confidence=0.95, atol=nothing, rtol=nothing,\n    seed=0, threaded=false, jump_statistics=false,\n    algorithm=:fixed, trajectory_keywords...)\n\nRun state-free weak-PI pseudo-ket trajectories until every requested Hermitian-observable mean at every saved time satisfies the simultaneous empirical-Bernstein target.  The immutable Schur Kraus plan is shared and observable values are contracted directly from pseudo-kets; neither WeakPIQuantumTrajectory histories nor sampled PI density operators are constructed.\n\nalgorithm=:event and its :adaptive/:event_driven aliases use continuous hazard roots.  Fixed and event-driven integration bias must still be assessed by refining their numerical controls.  Each separately seeded trajectory is one independent sample for the stopping certificate, reported as result.metadata.effective_independent_samples.  The certificate controls Monte Carlo dispersion only; it does not cover finite-time initialization, time-grid, model-truncation, or observable-selection bias.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.adaptive_weak_pi_quantum_trajectories"},{"category":"function","location":"diffusive_monitoring/#PermutationalInvariantDynamics.adaptive_diffusive_trajectories","page":"Diffusive monitoring","text":"adaptive_diffusive_trajectories(batch, rho0;\n    min_trajectories=64, max_trajectories=10_000, batch_size=32,\n    confidence=0.95, atol=nothing, rtol=nothing, seed=0,\n    threaded=false, workspace=nothing, parameters=nothing)\n\nRun a prepared diffusive ensemble until all observables stored in batch.observables meet the requested sampling-width target.  The batch must contain at least one observable.  Times, operators, monitor blocks, and task workspaces are reused for every realization. Because finite-step Euler–Maruyama is not positivity preserving and has unbounded Gaussian increments, its empirical-Bernstein interpretation is conditional on a separate step-size/physicality study; this routine controls sampled Monte Carlo dispersion, not integrator bias.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.adaptive_diffusive_trajectories"},{"category":"section","location":"api/visualization/#Visualization","page":"Visualization","text":"","title":"Visualization"},{"category":"section","location":"api/visualization/#Schur-block-structure","page":"Visualization","text":"","title":"Schur-block structure"},{"category":"section","location":"api/visualization/#Density,-Liouvillian,-and-Floquet-spectra","page":"Visualization","text":"","title":"Density, Liouvillian, and Floquet spectra"},{"category":"section","location":"api/visualization/#Spin-phase-space","page":"Visualization","text":"","title":"Spin phase space"},{"category":"type","location":"api/visualization/#PermutationalInvariantDynamics.SchurBlockStructure","page":"Visualization","text":"SchurBlockStructure\n\nNumerical Schur-sector structure of a PI operator or superoperator.  For an operator only diagonal entries of weights are used.  For a superoperator, row i, column j is the norm of the map from input sector j to output sector i.  active applies the requested absolute threshold without modifying weights or the source object.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.SchurBlockStructure"},{"category":"type","location":"api/visualization/#PermutationalInvariantDynamics.SchurBlockVisualization","page":"Visualization","text":"SchurBlockVisualization\n\nDependency-free text/SVG rendering configuration for a Schur structure. show_young_diagrams=true displays the partition shape attached to every Schur-sector label.  These are unfilled Young diagrams: PI operators sum over the symmetric-group multiplicity labels and therefore do not select a unique standard Young tableau.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.SchurBlockVisualization"},{"category":"function","location":"api/visualization/#PermutationalInvariantDynamics.schur_block_structure","page":"Visualization","text":"schur_block_structure(A::Union{PIState,PIOperator};\n                      metric=:frobenius, representation=nothing,\n                      threshold=0)\n\nMeasure each retained Schur block without constructing a full d^N operator. Supported metrics are :frobenius and :trace_norm; PIState additionally supports :population.  The default representation is :physical.\n\n\n\n\n\nschur_block_structure(L, basis=nothing; metric=:frobenius,\n                      representation=nothing, threshold=0,\n                      time=nothing, parameters=nothing)\n\nMeasure sector-to-sector PI-coordinate blocks of a superoperator.  Rows are output sectors and columns are input sectors.  Sparse/dense matrices are scanned directly.  A matrix-free source is probed exactly once per input PI coordinate with reusable scratch and is never materialized.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.schur_block_structure"},{"category":"function","location":"api/visualization/#PermutationalInvariantDynamics.visualize_schur_blocks","page":"Visualization","text":"visualize_schur_blocks(structure; scale=:linear, normalize=:global,\n                       show_values=false, show_young_diagrams=true,\n                       title=nothing,\n                       width=720, height=560)\nvisualize_schur_blocks(source, [basis]; visualization_keywords...,\n                       extraction_keywords...)\n\nCreate a dependency-free text/SVG view of Schur-sector weights. scale is :linear or :log; normalize is :global, :row, :column, or :none. show_values=true prints the raw numerical weights inside SVG cells. When a state, operator, or superoperator is supplied directly, remaining keywords are forwarded to schur_block_structure.  By default, show_young_diagrams=true adds the unfilled partition shape beside each sector label.  Superoperators show shapes on both the output-row and input-column axes; states and operators show each diagonal sector shape once.  The diagrams do not choose a standard tableau filling because PI operators sum over that multiplicity label.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.visualize_schur_blocks"},{"category":"function","location":"api/visualization/#PermutationalInvariantDynamics.save_schur_block_visualization","page":"Visualization","text":"Write a Schur-block SVG and return the path as a String.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.save_schur_block_visualization"},{"category":"type","location":"api/visualization/#PermutationalInvariantDynamics.ComplexSpectrum","page":"Visualization","text":"ComplexSpectrum\n\nReusable complex spectral data for Liouvillian eigenvalues or Floquet multipliers/exponents. Values retain their input order and scalar type. classifications and tolerance are presentation metadata only; no value is clipped, shifted, or removed.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.ComplexSpectrum"},{"category":"type","location":"api/visualization/#PermutationalInvariantDynamics.SpectrumVisualization","page":"Visualization","text":"Dependency-free complex-spectrum text/SVG rendering configuration.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.SpectrumVisualization"},{"category":"type","location":"api/visualization/#PermutationalInvariantDynamics.DensitySpectrumVisualization","page":"Visualization","text":"DensitySpectrumVisualization\n\nDependency-free configuration for a multiplicity-compressed density-operator spectrum plot. spectrum is the exact named tuple returned by pi_density_spectrum; its values and exact BigInt multiplicities are never expanded or reordered by the visualization layer.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.DensitySpectrumVisualization"},{"category":"function","location":"api/visualization/#PermutationalInvariantDynamics.liouvillian_spectrum_data","page":"Visualization","text":"liouvillian_spectrum_data(values_or_result; ...)\nliouvillian_spectrum_data(source; target=:largest_real, nev=nothing,\n                          algorithm=:auto, classification_atol=0,\n                          classification_rtol=nothing, ...)\n\nConstruct reusable Liouvillian eigenvalue data. Raw vectors and existing spectral results are never recomputed. Source inputs delegate once to liouvillian_spectrum without requesting eigenvectors. For source inputs, atol and rtol are forwarded to the spectral solver; the distinct classification_atol and classification_rtol keywords control only marker classification.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.liouvillian_spectrum_data"},{"category":"function","location":"api/visualization/#PermutationalInvariantDynamics.floquet_spectrum_data","page":"Visualization","text":"floquet_spectrum_data(F; period=nothing, representation=:multipliers)\nfloquet_spectrum_data(source, period; steps=256, ...)\nfloquet_spectrum_data(values; input=:multipliers, ...)\n\nConstruct reusable Floquet multiplier or exponent data. Multiplier-to-exponent conversion uses Julia's principal complex logarithm. Supplied exponents remain unchanged and are not asserted to lie in the principal zone. A matrix is always interpreted as an already computed one-period propagator. A model or matrix-free source with positional period is integrated exactly once.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.floquet_spectrum_data"},{"category":"function","location":"api/visualization/#PermutationalInvariantDynamics.visualize_spectrum","page":"Visualization","text":"visualize_spectrum(spectrum::ComplexSpectrum; title=nothing,\n                   width=760, height=560, xlimits=nothing,\n                   ylimits=nothing, show_legend=true,\n                   show_indices=false, marker_size=5)\n\nCreate a dependency-free SVG visualization configuration. Rendering reuses the stored numerical data and never invokes an eigensolver or propagator.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.visualize_spectrum"},{"category":"function","location":"api/visualization/#PermutationalInvariantDynamics.visualize_liouvillian_spectrum","page":"Visualization","text":"visualize_liouvillian_spectrum(spectrum::ComplexSpectrum; options...)\nvisualize_liouvillian_spectrum(source; options..., solver_options...)\n\nRender existing Liouvillian spectral data, or compute selected modes once and render them. Presentation options are the same as for visualize_spectrum.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.visualize_liouvillian_spectrum"},{"category":"function","location":"api/visualization/#PermutationalInvariantDynamics.visualize_floquet_spectrum","page":"Visualization","text":"visualize_floquet_spectrum(spectrum::ComplexSpectrum; options...)\nvisualize_floquet_spectrum(F; period=nothing, options...)\nvisualize_floquet_spectrum(source, period; options...)\n\nRender existing Floquet data, an already computed one-period propagator, or a periodic PI source. A source is propagated and diagonalized exactly once.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.visualize_floquet_spectrum"},{"category":"function","location":"api/visualization/#PermutationalInvariantDynamics.save_spectrum_visualization","page":"Visualization","text":"Write a spectrum SVG and return the path as a String.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.save_spectrum_visualization"},{"category":"function","location":"api/visualization/#PermutationalInvariantDynamics.visualize_density_spectrum","page":"Visualization","text":"visualize_density_spectrum(rho::PIState; spectrum_kwargs=NamedTuple(), options...)\nvisualize_density_spectrum(spectrum::NamedTuple; options...)\n\nCreate a dependency-free, linear-scale SVG visualization of a density operator's multiplicity-compressed Schur spectrum. The horizontal coordinate is compressed eigenmode rank and the vertical coordinate is the raw physical eigenvalue. Points are colored by Schur sector. Exact degeneracy and the within-sector eigenvalue index are retained in each tooltip; the exponentially large repeated eigenvalue list is never constructed.\n\npresentation_atol and presentation_rtol only decide when a negative value gets a red outline. They never clip or replace an eigenvalue. Presentation options and spectrum_kwargs are validated before a PIState is diagonalized. Pass an existing compressed named tuple to render it without recomputation.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.visualize_density_spectrum"},{"category":"function","location":"api/visualization/#PermutationalInvariantDynamics.save_density_spectrum_visualization","page":"Visualization","text":"Write a multiplicity-compressed density-spectrum SVG and return its path.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.save_density_spectrum_visualization"},{"category":"type","location":"api/visualization/#PermutationalInvariantDynamics.SpinPhaseSpaceVisualization","page":"Visualization","text":"SpinPhaseSpaceVisualization\n\nDependency-free equirectangular SVG rendering configuration for a SpinPhaseSpaceData object. sector_index == 0 renders its aggregate Schur-sector marginal; a positive index renders one retained sector matrix. Rendering changes no numerical values and never recomputes a phase-space transform.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.SpinPhaseSpaceVisualization"},{"category":"function","location":"api/visualization/#PermutationalInvariantDynamics.visualize_spin_phase_space","page":"Visualization","text":"visualize_spin_phase_space(data; sector=:aggregate, title=nothing,\n                           width=760, height=460,\n                           colorlimits=nothing, palette=:auto,\n                           show_colorbar=true)\n\nCreate a dependency-free equirectangular heatmap of precomputed spin phase-space data. Azimuth phi runs horizontally and polar angle theta runs vertically with the north pole at the top. Both coordinate vectors must be strictly increasing regular grids; numerical evaluation itself permits arbitrary grids.\n\nThe default renders the aggregate marginal. Pass a retained partition to sector after computing the data with resolved=true to display one sector-resolved sphere. Husimi-Q data use a sequential palette and Wigner data use a zero-centered diverging palette by default. colorlimits affects only presentation: underlying values, including Wigner negativity and values outside the displayed color range, are unchanged.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.visualize_spin_phase_space"},{"category":"function","location":"api/visualization/#PermutationalInvariantDynamics.save_spin_phase_space_visualization","page":"Visualization","text":"Write a spin phase-space SVG and return the path as a String.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.save_spin_phase_space_visualization"},{"category":"section","location":"batched_trajectories/#Matrix-RHS-conditional-trajectories","page":"Matrix-RHS trajectory cohorts","text":"quantum_trajectories is the safe high-level ensemble interface. It assigns a random stream to each trajectory index, dynamically schedules independent paths, and lets each path shorten its own fixed step when its jump intensity would exceed max_jump_probability.\n\nThe conditional density matrices of many paths can nevertheless be propagated more efficiently when they currently share the same physical time and step. BatchedConditionalPlan and BatchedConditionalWorkspace expose that matrix-RHS kernel without changing the stochastic algorithm.\n\nusing PermutationalInvariantDynamics\n\nbasis = PIBasis(10, 2)\nspin = spin_matrices()\nmodel = PIModel(basis, (\n    LocalHamiltonian(spin.jx; rate=0.2),\n    LocalJump(spin.jm; rate=0.7),\n))\nrho0 = iid_pure_state(basis, ComplexF64[0, 1])\n\ntrajectory_plan = TrajectoryPlan(model)\nplan = BatchedConditionalPlan(trajectory_plan)\nwork = BatchedConditionalWorkspace(plan, rho0, 16)\n\n# Each column is one normalized PI density operator.\nstates = repeat(reshape(rho0.data, :, 1), 1, 16)\nbatched_conditional_rk4!(states, plan, 0.0, 0.002, nothing, work)\n\nThe workspace has immutable capacity. Passing more than 16 columns raises instead of growing its matrix buffers. Construction is protected by the same 512 MiB default memory budget as the high-level solvers.","title":"Matrix-RHS conditional trajectories"},{"category":"section","location":"batched_trajectories/#Intensities-and-jumps","page":"Matrix-RHS trajectory cohorts","text":"Channel intensities are evaluated for the whole cohort:\n\nrates = zeros(length(trajectory_plan.jumps), size(states, 2))\nbatched_channel_intensities!(\n    rates, plan, states, 0.002, nothing, work)\n\nKeep one RNG per trajectory index. Regrouping paths must move the RNG together with its state:\n\nrngs = batched_trajectory_rngs(1234, size(states, 2))\nchannels = zeros(Int, size(states, 2))\nbatched_sample_jumps!(channels, rates, 0.002, rngs)\nbatched_apply_jumps!(\n    states, channels, plan, 0.002, nothing, work)\n\nchannels[j] == 0 means that path did not jump. Columns selecting the same nonzero channel are gathered and passed to one matrix-RHS gain kernel.","title":"Intensities and jumps"},{"category":"section","location":"batched_trajectories/#Why-this-is-not-a-replacement-for-quantum_trajectories","page":"Matrix-RHS trajectory cohorts","text":"After jumps, path intensities differ. The scalar fixed-step algorithm caps each next step independently:\n\nh_j = minleft(Delta t\nfracp_maxlambda_jright)\n\nForcing all paths to use the smallest h_j changes their RK grid and random draw times. The package therefore does not silently do this. Batch only paths whose existing scheduler selected the same step, or use quantum_trajectories when exact high-level scheduling and output collection are wanted.\n\nThe matrix-RHS action and RK4 implementation are checked against repeated scalar trajectory workspaces in test/test_batched_trajectories.jl. Run the focused performance comparison with:\n\njulia --project=benchmark benchmark/batched_trajectories.jl","title":"Why this is not a replacement for quantum_trajectories"},{"category":"section","location":"batched_trajectories/#API","page":"Matrix-RHS trajectory cohorts","text":"","title":"API"},{"category":"type","location":"batched_trajectories/#PermutationalInvariantDynamics.BatchedConditionalPlan","page":"Matrix-RHS trajectory cohorts","text":"BatchedConditionalPlan(source[, rho])\n\nRead-only plan for propagating several density-matrix PI quantum trajectories through the same conditional (no-jump) time interval. source may be a TrajectoryPlan, PIModel, or CompiledPIModel. The optional rho argument is needed only to infer the scalar type of an empty model.\n\nThis is deliberately a low-level plan. Different stochastic paths generally acquire different intensity-limited step sizes after their first jump. A high-level routine that forced all paths onto the smallest such step would no longer reproduce quantum_trajectories. This plan therefore batches only cohorts for which the caller has already selected the same physical time and step; it never changes either value.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.BatchedConditionalPlan"},{"category":"type","location":"batched_trajectories/#PermutationalInvariantDynamics.BatchedConditionalWorkspace","page":"Matrix-RHS trajectory cohorts","text":"BatchedConditionalWorkspace(plan, rho, capacity; memory_budget=512MiB)\n\nFixed-capacity, task-owned scratch for BatchedConditionalPlan. The five length(rho) × capacity stage/gather matrices and the prepared Liouvillian batch buffers are allocated by the constructor. Subsequent calls reject a wider matrix instead of growing hidden storage.\n\nOne workspace may be reused sequentially. Concurrent calls need distinct workspaces referring to the same immutable plan.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.BatchedConditionalWorkspace"},{"category":"function","location":"batched_trajectories/#PermutationalInvariantDynamics.batched_conditional_action!","page":"Matrix-RHS trajectory cohorts","text":"batched_conditional_action!(Y, intensities, plan, X, time,\n                            parameters, workspace)\n\nApply the normalized conditional trajectory drift to all columns of X with one matrix-RHS Schur-kernel pass. intensities[j] receives the total jump intensity of column j. Y and X must not alias and the column count may not exceed the workspace capacity.\n\nDriven jump-rate caches are invalidated at every public call, so reusing a workspace with a new parameter object at the same time cannot expose stale loss blocks.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.batched_conditional_action!"},{"category":"function","location":"batched_trajectories/#PermutationalInvariantDynamics.batched_conditional_rk4!","page":"Matrix-RHS trajectory cohorts","text":"batched_conditional_rk4!(X, plan, time, step, parameters, workspace)\n\nAdvance every column of X by the same requested fixed RK4 step under the normalized conditional trajectory equation. Each column is trace-normalized after the step, matching the scalar fixed-step path. The function neither caps nor changes step; callers must use the same intensity-cap decision that they would use for each scalar path and batch only columns sharing that step.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.batched_conditional_rk4!"},{"category":"function","location":"batched_trajectories/#PermutationalInvariantDynamics.batched_channel_intensities!","page":"Matrix-RHS trajectory cohorts","text":"batched_channel_intensities!(rates, plan, X, time, parameters, workspace)\n\nEvaluate every channel intensity for every column. rates must have shape (number_of_channels, size(X,2)). Scalar schedules are evaluated once per channel and shared by the batch.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.batched_channel_intensities!"},{"category":"function","location":"batched_trajectories/#PermutationalInvariantDynamics.batched_apply_jumps!","page":"Matrix-RHS trajectory cohorts","text":"batched_apply_jumps!(X, channels, plan, time, parameters, workspace)\n\nApply selected jump gains in-place to columns of X. channels[j] == 0 means no jump for column j; positive values identify a prepared jump channel. Columns selecting the same channel are gathered, propagated through one matrix-RHS gain kernel, normalized, and scattered back.\n\nThis function does not draw random numbers. Pair it with batched_sample_jumps! so each trajectory index retains its own RNG stream.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.batched_apply_jumps!"},{"category":"function","location":"batched_trajectories/#PermutationalInvariantDynamics.batched_trajectory_rngs","page":"Matrix-RHS trajectory cohorts","text":"batched_trajectory_rngs(seed, count)\n\nConstruct one independent MersenneTwister stream per trajectory index using the same master-seed expansion as quantum_trajectories. Keep each stream attached to its trajectory when regrouping columns into common-step cohorts.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.batched_trajectory_rngs"},{"category":"function","location":"batched_trajectories/#PermutationalInvariantDynamics.batched_sample_jumps!","page":"Matrix-RHS trajectory cohorts","text":"batched_sample_jumps!(channels, rates, step, rngs)\n\nDraw one fixed-step jump decision per column from channel intensities rates. rngs[j] is used only for trajectory j; a no-jump decision consumes one draw and a jump consumes the same second channel-selection draw as the scalar algorithm. channels[j] is set to zero for no jump.\n\nThe supplied step is used exactly as given and is never capped. Callers must first enforce their requested max_jump_probability using the pre-step total intensity, exactly as in the scalar path.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.batched_sample_jumps!"},{"category":"section","location":"startup_performance/#Startup-latency-and-local-sysimages","page":"Startup latency and local sysimages","text":"Package loading and first-call compilation are separate from steady-state or trajectory runtime. The benchmark suite measures them separately; warmed kernel timings should never be presented as time-to-first-result.\n\nFor repeated interactive work on one machine, an optional PackageCompiler sysimage can precompile the common PI workflow without adding PackageCompiler to this package's dependencies:\n\nmkdir -p /tmp/pid-sysimage\njulia --project=/tmp/pid-sysimage -e \\\n  'using Pkg; Pkg.add(\"PackageCompiler\"); Pkg.develop(path=pwd())'\njulia --project=/tmp/pid-sysimage scripts/build_sysimage.jl \\\n  --output /tmp/PermutationalInvariantDynamics.dylib\n\nOn Linux, use a .so output; on Windows, use .dll. Start Julia with\n\njulia -J /tmp/PermutationalInvariantDynamics.dylib --project=.\n\nThe workload covers representation construction, matrix-free forward and adjoint application, deterministic dynamics, a stationary solve, collective observables, reductions, and one fixed-step trajectory. It uses small objects and writes no result. A sysimage is platform-, Julia-, CPU-target-, and environment-specific: rebuild it after changing Julia, package versions, or the local checkout. It does not change numerical algorithms, precision, memory guards, or convergence requirements.\n\nFor a portable benchmark rather than a local latency optimization, use the cold-start and end-to-end scripts described in Performance benchmarks.","title":"Startup latency and local sysimages"},{"category":"section","location":"genuine_entanglement/#Genuine-multipartite-entanglement","page":"Genuine multipartite entanglement","text":"ppt_mixture_test implements the permutationally invariant (PI) positive-partial-transpose-mixture criterion of Novo, Moroder, and Gühne, Phys. Rev. A 88, 012305 (2013). It tests general PI qubit states spanning several Schur sectors without constructing a 2^N density matrix.\n\nThis is a solver-backed entanglement test, so install and load the optional Clarabel.jl dependency before calling it:\n\nusing Pkg\nPkg.add(\"Clarabel\") # once, in the active environment\n\nusing PermutationalInvariantDynamics\nimport Clarabel\n\nbasis = PIBasis(3, 2)\nrho = ghz_state(basis)\n\nresult = ppt_mixture_test(rho)\nresult.classification\nresult.genuinely_multipartite_entangled\n\nimport Clarabel activates the package extension. It does not change the PI representation or load a conic solver during ordinary core-package use.","title":"Genuine multipartite entanglement"},{"category":"section","location":"genuine_entanglement/#What-the-criterion-proves","page":"Genuine multipartite entanglement","text":"A state is biseparable when it is a convex mixture of states separable across possibly different bipartitions. Every such state is a PPT mixture,\n\nrho = sum_Moverline M P_Moverline M\nqquad\nP_Moverline Msucceq 0\nqquad\nP_Moverline M^T_Msucceq 0\n\nConsequently, proving that a state is not a PPT mixture certifies genuine multipartite entanglement. The converse depends on particle number:\n\nResult Certified conclusion\nclassification == :gme_certified A validated numerical dual certificate establishes non-PPT-mixture membership within the reported tolerances and therefore detects genuine multipartite entanglement.\nclassification == :ppt_mixture, N == 3 For PI three-qubit states, PPT-mixture membership is equivalent to biseparability, so the state is not genuinely tripartite entangled.\nclassification == :ppt_mixture, N >= 4 The criterion did not detect genuine multipartite entanglement. It does not prove biseparability.\nclassification == :inconclusive Neither returned numerical certificate passed validation; draw no entanglement conclusion.\n\nFor the N == 2 consistency case, PPT is also equivalent to separability. The public result makes the asymmetry explicit: for N >= 4, a validated PPT mixture has genuinely_multipartite_entangled === missing and biseparable === missing, rather than misleading false and true values. Only :gme_certified is a general-N numerical GME certificate. It is not an exact-arithmetic proof: retain the reported residuals and tighten both solver and certificate tolerances when a conclusion lies close to the boundary.\n\nDo not classify a result from the sign of scaled_margin alone. The function reports a conclusive classification only after independently checking the returned primal Schur equality and PSD/PPT blocks, or the dual stationarity and PSD conditions. A time limit, iteration limit, inaccurate optimizer status, or failed post-solve check produces :inconclusive.","title":"What the criterion proves"},{"category":"section","location":"genuine_entanglement/#PI-Schur-block-formulation","page":"Genuine multipartite entanglement","text":"Permutation invariance reduces the exponentially many labeled bipartitions to the cut sizes\n\nk(N-k) qquad k=1ldotsleftlfloor N2rightrfloor\n\nFor each cut, its unnormalized PPT operator is invariant under permutations within both sides. It therefore decomposes into product-Schur blocks indexed by partitions alpha of k and beta of N-k. The implementation stores their multiplicity-weighted Hermitian matrices Y_kalphabeta and maximizes a common scaled slack t subject to\n\nY_kalphabetasucceq t I\nqquad\nY_kalphabeta^T_Asucceq t I\n\nA nonnegative feasible optimum is therefore a PPT-mixture decomposition; a validated numerical separating dual establishes nonmembership within the reported tolerances.\n\nClebsch–Gordan intertwiners assemble those product blocks into each total Schur sector. In the package's equation-(7) coefficient convention, the equality constraint is\n\nsqrtf^lambdaC_lambda\n=sum_kalphabetar\nU_alphabetarightarrowlambdar^dagger\nY_kalphabeta\nU_alphabetarightarrowlambdar\n\nThe left side is the multiplicity-weighted analysis block of the input PIState. This scaling keeps the assembled equality finite whenever the ordinary PI analysis block is representable. The optimized scaled_margin has the same sign interpretation as the slack s in Eq. (31) of the paper, but its magnitude is not that unscaled s because the product blocks use the package's Schur normalization.\n\nClarabel uses real semidefinite cones. Each complex Hermitian PSD constraint is therefore represented by its equivalent doubled real symmetric embedding. This changes storage constants, not the feasible set.","title":"PI Schur-block formulation"},{"category":"section","location":"genuine_entanglement/#Reusing-a-plan","page":"Genuine multipartite entanglement","text":"Preparing the Schur recouplings and sparse conic map can dominate a scan. Build one immutable plan and reuse it for states constructed from the exact same PIBasis object:\n\nplan = PPTMixturePlan(basis)\n\nresults = map(states) do state\n    ppt_mixture_test(state; plan=plan)\nend\n\nSolver state is call-local, so the plan itself is read-only and shareable. The function validates every input state and never normalizes, truncates, symmetrizes, or repairs it. The current implementation supports qubits and Float32/Float64 conic data; qudits and wider floating types are rejected instead of being silently converted.\n\natol and rtol control input-state validation. Optimizer stopping tolerances are set separately by solver_atol and solver_rtol, while certificate_atol and certificate_rtol govern the independent post-solve checks. Tighten both groups when auditing a point close to the PPT-mixture boundary. Additional Clarabel settings can be supplied through solver_options, except for settings controlled by explicit function keywords.","title":"Reusing a plan"},{"category":"section","location":"genuine_entanglement/#Restricted-input-sectors","page":"Genuine multipartite entanglement","text":"A sector-restricted input basis does not justify dropping other sectors from the decomposition variables. PPT operators for individual cuts can have support outside the input state's retained sectors even though their PI sum matches the state. PPTMixturePlan therefore constructs a complete internal PI basis at the same N, and inserts exact zero equality targets for every absent source sector.\n\nThis is a correctness safeguard. It also means that restricting the input state's Schur sectors does not reduce the SDP dimensions:\n\nrestricted = PIBasis(6, 2; sectors=[(6, 0)])\nplan = PPTMixturePlan(restricted) # internally enforces every N=6 sector","title":"Restricted input sectors"},{"category":"section","location":"genuine_entanglement/#Scaling-and-memory","page":"Genuine multipartite entanglement","text":"The paper reduces the formulation to a polynomial number of objects: the number of real parameters is O(N^7), there are O(N^3) product-Schur matrix blocks, and the largest block dimension is O(N^2). Polynomial does not mean inexpensive; sparse conic factorization fill and conditioning remain problem dependent.\n\nPPTMixturePlan stores a sparse conic matrix and reports estimated_setup_bytes and estimated_solve_bytes. Construction and solving use the package's default 512 MiB memory_budget; a request that exceeds it raises before the guarded allocation. Increase the budget only after checking the estimates and available memory. memory_budget=Inf is the explicit opt-out, not an automatic fallback, and the solver estimate cannot strictly bound problem-dependent factorization fill.\n\nFor repeated states, reuse the plan. For increasing N, record solver status, iterations, residual diagnostics, wall time, and peak memory rather than assuming that polynomial asymptotics alone imply practical tractability.","title":"Scaling and memory"},{"category":"section","location":"genuine_entanglement/#Result-diagnostics","page":"Genuine multipartite entanglement","text":"In addition to the three-valued scientific fields, PPTMixtureResult retains the quantities needed to audit the numerical decision:\n\nequality_residual, minimum_block_eigenvalue, and minimum_partial_transpose_eigenvalue validate a returned primal decomposition;\ndual_stationarity_residual and minimum_dual_cone_eigenvalue validate a returned dual witness;\nprimal_objective, dual_objective, solver_status, iterations, and solve_time describe the optimizer result;\ncertificate_atol, certificate_rtol, and message record how that output was classified.\n\nKeep the complete result with published scan data, especially for points near the inferred boundary.","title":"Result diagnostics"},{"category":"section","location":"genuine_entanglement/#API","page":"Genuine multipartite entanglement","text":"","title":"API"},{"category":"type","location":"genuine_entanglement/#PermutationalInvariantDynamics.PPTMixturePlan","page":"Genuine multipartite entanglement","text":"PPTMixturePlan(basis; T=Float64, memory_budget=512*1024^2)\n\nPrepare the polynomial-size semidefinite program of Novo, Moroder, and Gühne, Phys. Rev. A 88, 012305 (2013), for testing whether a PI qubit state is a positive-partial-transpose (PPT) mixture.\n\nFor every inequivalent cut k(N-k) the plan introduces one S_ktimes S_N-k-invariant operator.  Its product-Schur blocks are represented by multiplicity-weighted Hermitian matrices Y_kalphabeta and constrained by\n\nY_kalphabetasucceq0qquad\nY_kalphabeta^T_Asucceq0\n\nThe equality constraint is assembled directly in Schur blocks,\n\nsqrtf^lambdaC_lambda\n=sum_kalphabetar\nU_alphabetatolambdar^dagger\nY_kalphabeta\nU_alphabetatolambdar\n\nso no 2^N state or operator is constructed.  An internally complete PI basis is used even when basis is sector restricted; absent source sectors therefore enter the equality constraint as exact zeros.\n\nThe immutable plan stores a sparse real conic map and may be reused for many states on the exact same PIBasis.  T may be Float32 or Float64. memory_budget=Inf is the explicit opt-out from the conservative setup and solver-storage guard.\n\nThe paper derives this construction for qubits.  Qudit inputs are rejected rather than being passed through an unproved generalization.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.PPTMixturePlan"},{"category":"type","location":"genuine_entanglement/#PermutationalInvariantDynamics.PPTMixtureResult","page":"Genuine multipartite entanglement","text":"PPTMixtureResult\n\nResult of ppt_mixture_test.  classification is one of:\n\n:gme_certified: a converged, explicitly validated numerical dual certificate establishes, within the reported tolerances, that the state is not a PPT mixture and hence is genuinely multipartite entangled;\n:ppt_mixture: a converged primal decomposition passes the Schur equality, positivity, and partial-transpose checks;\n:inconclusive: neither numerical certificate passed.\n\nFor PI qubits with N == 3 (and for the bipartite N == 2 case), PPT-mixture membership is also sufficient for biseparability.  For N >= 4, a :ppt_mixture result means only that this criterion did not detect genuine multipartite entanglement; consequently genuinely_multipartite_entangled and biseparable are missing.\n\nscaled_margin is the optimized slack for multiplicity-weighted Schur variables.  Its sign has the paper's membership meaning, but its magnitude is not the unscaled s of Eq. (31).\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.PPTMixtureResult"},{"category":"function","location":"genuine_entanglement/#PermutationalInvariantDynamics.ppt_mixture_test","page":"Genuine multipartite entanglement","text":"ppt_mixture_test(rho; plan=nothing, solver=:clarabel,\n                 atol=_analysis_atol(rho), rtol=_state_rtol(rho),\n                 certificate_atol=nothing, certificate_rtol=nothing,\n                 solver_atol=nothing, solver_rtol=nothing,\n                 max_iterations=300, time_limit=Inf,\n                 verbose=false, solver_options=NamedTuple(),\n                 memory_budget=512*1024^2)\n\nTest a PI qubit state for genuine multipartite entanglement with the permutationally invariant PPT-mixture SDP of Novo, Moroder, and Gühne, Phys. Rev. A 88, 012305 (2013).\n\nLoad the optional solver first:\n\nimport Clarabel\nresult = ppt_mixture_test(rho)\n\nThe routine maximizes a common slack in multiplicity-weighted product-Schur blocks.  It returns a PPTMixtureResult, never an ambiguous Boolean. Only result.classification == :gme_certified reports genuine multipartite entanglement for arbitrary N, based on a validated numerical dual certificate and the PPT-mixture criterion.  For N >= 4, :ppt_mixture means that the criterion did not detect GME, not that the state is biseparable.  For PI three-qubit states the paper proves the criterion necessary and sufficient.\n\nplan=PPTMixturePlan(rho.basis) reuses the sparse SDP geometry across a state scan.  Plans are immutable and solver state is call-local.  The state is validated but never normalized, truncated, or repaired.  Solver output is classified only after explicit primal Schur-equality/PSD/PPT checks or an explicit dual stationarity/PSD check.  Early termination therefore returns :inconclusive.\n\nsolver_atol and solver_rtol configure the optimizer; the separate certificate_atol and certificate_rtol control post-solve validation. Explicit tolerances must be representable without narrowing in the plan's conic precision. solver_options is a named tuple of additional backend settings.  The default memory_budget checks both the prepared sparse data and a conservative solver-storage estimate; Inf explicitly opts out.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.ppt_mixture_test"},{"category":"section","location":"published_validation/#Published-model-validation","page":"Published validation","text":"","title":"Published-model validation"},{"category":"section","location":"published_validation/#Additional-literature-validations","page":"Published validation","text":"Three further examples reproduce finite-size reference results:\n\nKitagawa and Ueda, Phys. Rev. A 47, 5138 (1993): one-axis twisting, checked against langle J_x(t)rangle=(N2)cos^N-1(chi t).\nHuelga et al., Phys. Rev. Lett. 79, 3865 (1997): Ramsey coherence under independent dephasing, checked against langle J_x(t)rangle=(N2)e^-gamma t.\nShammah et al., Phys. Rev. A 98, 063815 (2018): local incoherent pumping and emission, checked against the exact tensor-power thermal state.\n\nThe Huelga and Kitagawa–Ueda scripts compile a matrix-free model once and use solve_dynamics for typed saved states. Both reuse a CollectiveObservablePlan for every sampled mean spin; Kitagawa–Ueda also reuses one ReductionPlan for the time-resolved one-particle purity. The Shammah example uses the typed stationary_state(...; return_info=true) result and checks diagnostics before comparison with the exact product state.\n\nWith the examples-only CairoMakie environment, these validations also produce PDF/PNG summaries. Huelga overlays the Ramsey signal with its exponential law and reports the pointwise error; Kitagawa–Ueda combines the exact mean-spin curve with the generated one-spin mixedness; Shammah resolves the thermal steady state by total-spin sector and physical Schur-block eigenvalue. Plotting therefore reuses the same checked numerical arrays rather than introducing a second calculation path.","title":"Additional literature validations"},{"category":"section","location":"published_validation/#Non-Markovian-dynamical-decoupling","page":"Published validation","text":"examples/nonmarkovian_dynamical_decoupling.jl follows Colin Read, Studying pure dephasing and dynamical decoupling: comparison of the HOPS method with exact analytical solutions (report, September 2023). The supplied report gives no journal venue or DOI, so this example is described as a literature-motivated validation rather than attributed to an unverified publication.\n\nThe script targets the CPMG and UDD4 comparisons of Figs. 8–9 qualitatively. It uses the report's Eq. (15) parameters\n\nomega_c=10qquad\nkappa=15qquad\ng=frac16omega_c^2kappaqquad\nT_c=frac27omega_c\n\nand the associated one-pole correlation\n\nC_mathrmfull(t)=\nfracgkappa2exp-(kappa-iomega_c)t\n\nThis correlation is the whole-real-line transform of the Lorentzian. It is not the exact positive-frequency transform of the physical zero-temperature spectral density. Both PI–HEOM and PI–HOPS are therefore checked against the exact ideal-pulse filter function for C_mathrmfull, not against the positive-frequency curve. The latter is plotted from a separate half-line Gauss–Legendre quadrature, with two quadrature orders compared as its deterministic convergence check. Hierarchy depth, time-step, and trajectory convergence cannot remove the model difference between these two curves.\n\nPIUnitaryPulse applies each ideal pi_x rotation to every HEOM ADO or HOPS auxiliary, and HierarchyPulseSequence defines the ordered event times. Consequently the comparison retains hierarchy memory and one continuous colored-noise realization across every pulse. The report's finite Gaussian pulse cannot be reproduced literally because its pulse width and bare qubit frequency are not specified; the example instead uses the instantaneous pulses of the analytical derivation in Eqs. (47)–(52).\n\nThe checked default is the report's single qubit. A fully symmetric N1 extension remains exact in PI coordinates for one common bath coupled through Q=2J_z and one collective pulse U_x^otimes N. It is not a validation of independent local non-Markovian environments.\n\nThe example additionally validates the 24-edge TEDD word from Read, Serrano-Ensástiga, and Martin, Platonic dynamical decoupling sequences for interacting spin systems, Quantum 9, 1661 (2025), DOI: 10.22331/q-2025-03-12-1661. It checks exact timing, the uniform tetrahedral group average, and projective closure before running repeated cycles against the nonzero one-pole bath. Four-cycle PI–HEOM and PI–HOPS results are compared at closed-cycle outputs, and an eight-cycle HEOM refinement checks that halving the ideal-kick edge interval reduces the final infidelity. This finite-bath calculation is not a scalar-filter or finite-width-pulse reproduction of the Platonic-sequence paper.","title":"Non-Markovian dynamical decoupling"},{"category":"section","location":"published_validation/#Quantum-trajectory-literature-benchmarks","page":"Published validation","text":"examples/quantum_trajectories.jl turns independent spontaneous emission into an analytical regression for the Monte Carlo wave-function method of Dalibard, Castin, and Mølmer, Phys. Rev. Lett. 68, 580 (1992), and Mølmer, Castin, and Dalibard, J. Opt. Soc. Am. B 10, 524 (1993). It compares the event-driven PI ensemble with the exact tensor-power state p_e(t)=e^-gamma t, the binomial excitation and photon-count laws, and the exact no-jump probability. Stochastic assertions are expressed in analytical standard-error units rather than as a brittle absolute tolerance.\n\nexamples/superradiant_quantum_trajectories.jl implements the decay-only specialization of Eq. (1) and the two rate ratios of Fig. 2(a,b) in Zhang, Zhang, and Mølmer, New J. Phys. 20, 112001 (2018). The default N=10, 256-path run compares cavity and free-space radiation against a deterministically exponentiated, certified 36-coordinate population generator. It is a finite-size regression of the published model and observables; the paper used N=50 and 512 paths.\n\nThe conditional records require a precise distinction. The default density-valued backend combines the particle-unresolved local gain and gives a generally mixed conditional PI state. The separate examples/weak_pi_trajectories.jl backend resolves the same CP gain into one-box Schur-sector Kraus branches and retains a pure direct-sum pseudo-ket. Both reproduce the master equation and linear ensemble observables, but a Kraus decomposition is not unique, so individual (JM) paths and trajectory variances are not asserted to equal the article's record. Lloyd, Ziolkowska, and Keeling's 2026 construction also includes a shared bosonic cavity; density-valued composite jumps are available, but composite pseudo-ket trajectory compilation remains outside the single-ensemble backend. The weak-PI example uses the article's gamma_lGamma_c=1 case and independently checks certified population and general matrix-free PI master evolution before comparing equal-size, equal-control stochastic batches.\n\nWhen run with --project=examples, every standalone literature validation produces a CairoMakie PDF/PNG figure. The trajectory panels show deterministic curves, ensemble means, and documented uncertainty bands. The weak-PI example additionally shows ensemble-state error, sampled Jrightarrow J events, exact representation scaling through N=50, and an illustrative warmed per-path timing. Damanet and Pausch add radiated-pulse/Schur-population and gap/mean-field summaries. Morrison–Parkins, Meiser–Holland, and the metrology examples visualize their validated steady observables or analytical comparisons. The boundary, interacting, PT, and Floquet time-crystal scripts pair finite-size spectra or gaps with the relevant dynamics and retain their finite-size caveats. Makie remains confined to the examples environment.","title":"Quantum-trajectory literature benchmarks"},{"category":"section","location":"published_validation/#Cooperative-spontaneous-emission-(2016)","page":"Published validation","text":"examples/correlated_superradiance.jl implements Eqs. (3)–(5) of Damanet, Braun, and Martin, Phys. Rev. A 94, 033838 (2016). The decay matrix is decomposed exactly as\n\ngammamathcal DJ_- +(gamma_0-gamma)sum_imathcal Dsigma_-^(i)\n\nThe example compiles the sparse PI generator once for each correlation strength and recreates the three curves in Fig. 6. Dense exponentiation is retained as the independent small-N reference route. Tests compare 31 points on each curve to Eqs. (41)–(43); the measured maximum absolute error is 1.4e-15 on Julia 1.12.6.\n\nIt then computes the altered-superradiance pulse for N=30 and Deltagammagamma_0=04. Since the fully excited state remains diagonal in the Schur/GT basis, a certified PopulationPlan evolves 256 physical populations instead of all 5,456 PI coordinates. Equation (39) is evaluated from prepared physical-block diagonals, and the density operator at the pulse maximum is rendered as multiplicity-weighted Schur-sector populations with Young-diagram labels. Neither calculation materializes a length-2^30 state vector or a 2^30times2^30 density matrix.","title":"Cooperative spontaneous emission (2016)"},{"category":"section","location":"published_validation/#Dissipative-LMG-model-(2024)","page":"Published validation","text":"examples/dissipative_collective_spin_pairing.jl implements Eqs. (1)–(6) of Pausch et al., Phys. Rev. A 110, 062208 (2024):\n\nH=fracVNj(J_x^2-J_y^2)qquad\ndotrho=-iHrho+fracgamma_Ijsum_imathcal DL^(i)rho\n+fracgamma_CNjmathcal DL_Crho\n\nBoth the spin-ladder and equal-matrix-element dissipators are covered for qubits and qutrits. The collective quadratic Hamiltonian is lowered exactly to its one-body self term and symmetric two-body cross term. This produces the same finite PI Liouvillian as the direct J_x^2-J_y^2 construction while retaining the microscopic body order needed by MeanFieldPlan.\n\nThe qubit example reports the finite-size gap across the transition (gammaI+gammaC)/abs(V)=2 and compares three distinct predictions: the exact correlated N=8 PI steady state, the finite-N product closure, and the thermodynamic mean-field fixed point following Eqs. (10). The last is also checked against the analytical branch and its numerical fixed-point residual. Equation (11) is not used: it concerns the singular collective-only case gamma_I=0.\n\nThe finite steady state restores the mathbb Z_2 symmetry and consequently has zero transverse one-body order. The comparison therefore uses the branch-independent polarization Z and the parity-even pair correlator\n\nC_perp=fraclangle J_x^2rangle+langle J_y^2rangle-2Nj^2\n              2N(N-1)j^2\n\nwhose product-state prediction is (X^2+Y^2)2. The exact moments use prepared collective-observable contractions. Finite PI, finite product closure, and thermodynamic mean field are deliberately not identified with one another after correlations develop.","title":"Dissipative LMG model (2024)"},{"category":"section","location":"api/dynamics/#Dynamics-and-evolution","page":"Dynamics and evolution","text":"","title":"Dynamics and evolution"},{"category":"section","location":"api/dynamics/#Liouvillian-preparation-and-application","page":"Dynamics and evolution","text":"Use InPlaceTimeOperator for an operator-valued drive that is evaluated many times. The compiled plan keeps representation geometry read-only and each LiouvillianWorkspace owns the evaluated matrices. In particular, a driven LocalJump is prepared as rectangular common-child Schur contractions and is applied as a sum of C * X * C' products. It does not retain a table over all pairs of PI coordinates. Driven CorrelatedLocalJumps use the same factorization for every active Kossakowski channel, so arbitrary interference between the supplied local operators is preserved. Vector, adjoint, and batched applications all reuse this workspace; a batch evaluates the schedule once and contracts all right-hand sides with matrix–matrix kernels.\n\nFixed floating rates and hbar participate in the prepared plan's scalar precision; they are never first narrowed to the operator prototype type. When this widens a dense fixed kernel, its blocks and contractions are converted once during preparation so preallocated application does not create mixed-precision packing buffers; sparse one-body support remains sparse. Callable scalar rates must evaluate to finite real values representable by the precision selected when the plan was compiled. A wider value, or a nonzero value that would underflow, raises with guidance to recompile at wider precision. Negative rates remain valid for deterministic time-local generators, although such a generator need not define a completely positive map. Fixed operators, in-place prototypes, and every evaluated in-place operator must contain only finite coefficients. apply! and apply_adjoint! require non-aliasing source and destination arrays, including overlapping views.\n\nFor repeated small-system representation setup, a basis-owned OneBoxCGCache may be supplied through coefficient_cache to LiouvillianPlan, compile, liouvillian, steady_state, or compile_family. Compatible models then reuse its one-box coefficients while building every required one-body and Appendix-D geometry. Exact basis, removal-depth, and scalar-precision mismatches raise. Compilation already creates a bounded transient cache for supported small bases when more than one geometry family is needed; an explicit cache is useful across independent compilations.\n\nFixed local jump terms use compact rectangular Schur contractions in a matrix-free plan. Fixed numeric Hamiltonian contributions and anticommutator losses are also combined at compilation, while every physical gain channel remains distinct. This lowers retained memory and repeated block work without changing rates or introducing jump-channel cross terms. Calling liouvillian(...; representation=:sparse) expands the same prepared factors only for that explicit representation. Channel-resolved trajectory and population commands automatically use their term-resolved lowering, so their physical channel metadata is unaffected by deterministic-kernel fusion.\n\nFor a sufficiently large collection of Schur sectors, single-vector application can opt into Julia-task parallelism with ThreadedLiouvillianWorkspace and threaded_apply!. Setup assigns each complete output sector to exactly one worker using a deterministic cubic-block-cost partition. Workers only read the input and prepared kernel data; there are no atomic output updates or scheduler-ordered reductions. Operator and rate schedules are evaluated once before spawning. The adjoint route follows the same ownership rule, while one task uses the ordinary serial fast path.\n\nplan = LiouvillianPlan(model)\nwork = ThreadedLiouvillianWorkspace(plan; tasks=Threads.nthreads())\ndestination = similar(source)\nthreaded_apply!(destination, plan, source, work)\n\nThe workspace is caller-owned, reusable sequentially, and guarded against concurrent use. This API does not alter global BLAS threading: benchmark single-level BLAS and Julia-task configurations separately, because nested threading can be slower for small Schur blocks. Raw allocating operator functions cannot provide a prepared target-sector kernel and are rejected; wrap such schedules in InPlaceTimeOperator first.\n\nFor an autonomous prepared generator, the high-level solver can replace many fixed RK4 applications by adaptive restarted-Arnoldi exponential actions:\n\nsolution = solve_dynamics(\n    prepared, rho0, (0.0, 20.0);\n    saveat=0.1,\n    algorithm=ExpvAlgorithm(\n        krylovdim=30,\n        atol=1e-11,\n        rtol=1e-9,\n    ),\n)\n\nThe output grid remains exact: each adjacent interval is propagated as exp(Delta tmathcal L)rho. One KrylovExpvWorkspace and one task-owned Liouvillian application workspace are reused across all intervals. Rejected time slices only reevaluate the small projected exponential and reuse their Arnoldi factorization. Driven generators and parameter-dependent applications are rejected because one fixed exponential does not represent their dynamics. Keep the default RK4 route for driven models, or use dynamics_problem with an adaptive SciML solver.","title":"Liouvillian preparation and application"},{"category":"section","location":"api/dynamics/#Certified-Schur-diagonal-populations","page":"Dynamics and evolution","text":"","title":"Certified Schur-diagonal populations"},{"category":"section","location":"api/dynamics/#Quantum-trajectories","page":"Dynamics and evolution","text":"","title":"Quantum trajectories"},{"category":"section","location":"api/dynamics/#Weak-PI-pseudo-ket-trajectories","page":"Dynamics and evolution","text":"","title":"Weak-PI pseudo-ket trajectories"},{"category":"section","location":"api/dynamics/#PI-hierarchy-of-pure-states","page":"Dynamics and evolution","text":"For finite-memory environments represented by exponential correlations, PI–HOPS evolves a hierarchy of weak-PI pseudo-kets instead of density-valued ADOs. The exact PI backend requires shared baths with collective/PI coupling operators: independent local colored noises generally break permutation symmetry on each realization even when their ensemble average is PI.\n\nPrepare one immutable HOPSPlan, then give each simultaneous stochastic path its own HOPSWorkspace and RNG. Linear HOPS returns an unnormalized root pseudo-ket. hops_density forms its outer-product contribution and hops_average averages those contributions without pathwise normalization. Converge the exponential decomposition, hierarchy depth, time step, and trajectory count independently. See PI hierarchy of pure states (HOPS) for conventions and a complete workflow.","title":"PI hierarchy of pure states"},{"category":"section","location":"api/dynamics/#Diffusive-conditional-dynamics","page":"Dynamics and evolution","text":"See the diffusive-monitoring guide for the canonical API documentation and the normalized Itô convention.","title":"Diffusive conditional dynamics"},{"category":"section","location":"api/dynamics/#Mean-field-predictions","page":"Dynamics and evolution","text":"","title":"Mean-field predictions"},{"category":"section","location":"api/dynamics/#Floquet-analysis","page":"Dynamics and evolution","text":"","title":"Floquet analysis"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.liouvillian","page":"Dynamics and evolution","text":"liouvillian(model; representation=:matrixfree,\n            memory_budget=512*1024^2, coefficient_cache=nothing)\nliouvillian(compiled; representation=compiled.backend)\n\nReturn a PI-coordinate Liouvillian as either a sparse matrix or a MatrixFreeLiouvillian. Model construction lowers from the same prepared term plan in both representations. Sparse materialization requires an autonomous model; use freeze at an explicit time or the matrix-free backend for driven dynamics. Its conservative live sparse-assembly bound is checked before the coordinate matrix is allocated. Pass memory_budget=Inf only as an explicit opt-out. Pass a compatible OneBoxCGCache to reuse one-box coefficients across model preparations; otherwise small mixed one-/p-body models prepare and share one automatically. Prefer compile when the generator will be reused.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.liouvillian"},{"category":"type","location":"api/dynamics/#PermutationalInvariantDynamics.MatrixFreeLiouvillian","page":"Dynamics and evolution","text":"MatrixFreeLiouvillian(n, action!, T, trace_vector;\n                      autonomous=true, plan=nothing, workspace=nothing,\n                      adjoint_action! = nothing,\n                      batched_action! = nothing,\n                      batched_adjoint_action! = nothing)\n\nMatrix-free n x n Liouvillian whose callback implements action!(destination, source, time, parameters). T is its scalar type and trace_vector encodes the physical trace functional. A custom, plan-less operator may additionally provide callbacks with the same four-argument signature for its adjoint and for matrix batches. Batched callbacks receive destination and source matrices whose columns are independent vectors. Missing batched callbacks fall back to the corresponding vector callback; missing adjoint callbacks retain the autonomous materialization fallback.\n\nCompatibility calls are synchronized through one lock shared by all supplied callbacks. Compiled PI hot loops should use compile, apply!, and one explicit LiouvillianWorkspace per task.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.MatrixFreeLiouvillian"},{"category":"type","location":"api/dynamics/#PermutationalInvariantDynamics.LiouvillianPlan","page":"Dynamics and evolution","text":"LiouvillianPlan(model; coefficient_cache=nothing)\n\nImmutable prepared term data and geometry for a PI Liouvillian. A supplied OneBoxCGCache is shared by every compatible one- and p-body geometry built for the model; it must belong to the exact basis and cover the required body-order depth and scalar type. When it is omitted, compilation automatically prepares a bounded temporary cache for small models that need more than one geometry family. Fixed local gains retain rectangular Schur factors, and compatible autonomous numeric kernels share one effective Hamiltonian and anticommutator loss. Gain channels remain separate. The internal fuse_static=false diagnostic route retains one kernel sequence per model term for reference and channel-resolved consumers.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.LiouvillianPlan"},{"category":"type","location":"api/dynamics/#PermutationalInvariantDynamics.LiouvillianWorkspace","page":"Dynamics and evolution","text":"Per-task mutable scratch for applying a LiouvillianPlan.\n\nVector application retains three matrices per Schur sector. Batched application grows an additional three largest-sector buffers on first use and then reuses them. This keeps ordinary vector workspaces lean while allowing sectorwise matrix–matrix kernels for multiple right-hand sides.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.LiouvillianWorkspace"},{"category":"type","location":"api/dynamics/#PermutationalInvariantDynamics.ThreadedLiouvillianWorkspace","page":"Dynamics and evolution","text":"ThreadedLiouvillianWorkspace(plan_or_compiled; tasks=Threads.nthreads())\n\nCaller-owned scratch and deterministic Schur-sector partition for threaded_apply! and threaded_apply_adjoint!.\n\nThe prepared plan, supplied directly or through a CompiledPIModel, is read-only. Dynamic schedules are evaluated exactly once in preparation; each worker then reads those values and owns disjoint output sectors together with one private matrix scratch. tasks is capped by the number of retained Schur sectors and must be positive. The workspace may be reused sequentially but must not be used concurrently.\n\nThis API does not change BLAS's global thread count. For reproducible benchmarking, choose the BLAS configuration before constructing the workspace and compare it separately with Julia task parallelism.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.ThreadedLiouvillianWorkspace"},{"category":"type","location":"api/dynamics/#PermutationalInvariantDynamics.CompiledPIModel","page":"Dynamics and evolution","text":"A model, its reusable Liouvillian plan, and an automatically selected backend.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.CompiledPIModel"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.compile","page":"Dynamics and evolution","text":"compile(model; backend=:auto, memory_budget=512*1024^2,\n        bigfloat_precision=precision(BigFloat), coefficient_cache=nothing)\n\nPrepare all fixed Schur geometry once and choose a sparse or matrix-free backend. For standard fixed kernels, backend=:auto bounds sparse storage from their prepared exact block support; dynamic, custom, or otherwise unknown kernels retain a conservative dense-coordinate fallback. Driven models always remain matrix-free. The returned CompiledPIModel can be passed directly to apply!, evolve!, and dynamics_problem.\n\nThe sparse bound includes the prepared plan and simultaneous sparse assembly temporaries. Matrix-free preparation separately bounds its retained plan and compatibility action scratch. Automatic selection uses matrix-free work when the sparse route does not fit, and raises if that bounded alternative also exceeds the budget. Pass memory_budget=Inf as an explicit opt-out.\n\nFor fixed-size isbits scalar types, storage bounds retain the exact inline sizeof(T) accounting. BigFloat and Complex{BigFloat} use an explicitly conservative retained-storage bound at bigfloat_precision; pass the maximum precision intended for generated matrix entries when it differs from the active process precision.\n\nPass a compatible OneBoxCGCache to share one-box Clebsch–Gordan coefficients across one- and p-body geometry construction and across calls. An explicit cache is caller-owned and is therefore excluded from the preparation memory estimate. If it is omitted, a small model with multiple geometry families may build one bounded temporary cache automatically; its storage is included in the estimate.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.compile"},{"category":"type","location":"api/dynamics/#PermutationalInvariantDynamics.CompiledPIModelFamily","page":"Dynamics and evolution","text":"CompiledPIModelFamily\n\nReusable compilation of a fixed-operator PIModel whose selected scalar term rates will be varied. The family owns one read-only LiouvillianPlan; specialize binds numerical rates without rebuilding Schur geometry. Construct families with compile_family.\n\nA family is not itself a Liouvillian. Share it freely between tasks, then give each task its own specialization or an explicit LiouvillianWorkspace.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.CompiledPIModelFamily"},{"category":"type","location":"api/dynamics/#PermutationalInvariantDynamics.SpecializedPIModel","page":"Dynamics and evolution","text":"SpecializedPIModel\n\nAn autonomous PI model obtained by binding numerical rates to a CompiledPIModelFamily. It behaves like a compiled model in apply!, steady_state, spectral routines, dynamics, and parameter scans, while retaining the family's prepared Schur geometry.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.SpecializedPIModel"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.compile_family","page":"Dynamics and evolution","text":"compile_family(model; rate_indices=nothing,\n               bigfloat_precision=precision(BigFloat),\n               coefficient_cache=nothing)\n\nCompile fixed operator geometry once for a family of autonomous models that differ only in selected scalar term rates. rate_indices=nothing varies every term rate; otherwise it is a nonempty collection of one-based term indices. Call specialize for each numerical parameter point.\n\nThe input model must be autonomous and every selected rate must be finite and real. Operators, body order, Kossakowski matrices, and the retained PI basis remain fixed. This strict contract is what makes specialization independent of Schur geometry construction. Negative time-local rates are preserved.\n\nPass a compatible OneBoxCGCache to reuse one-box Clebsch–Gordan coefficients during the single shared geometry preparation. When omitted, the same bounded small-model automatic sharing used by compile applies.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.compile_family"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.specialize","page":"Dynamics and evolution","text":"specialize(family[, rates]; backend=:matrixfree, workspace=nothing,\n           memory_budget=512*1024^2)\n\nBind numerical rates to a CompiledPIModelFamily without rebuilding Schur geometry. rates follows family.rate_indices order; a scalar is accepted when exactly one rate is selected. Omitting rates uses the prototype values.\n\nbackend=:matrixfree creates only a synchronized compatibility workspace; hot or parallel loops should pass an explicit LiouvillianWorkspace to apply!. Repeated sequential specializations may pass the same task-owned workspace; this is how family parameter scans avoid allocating Schur-block scratch at every point. Never share that mutable workspace concurrently. backend=:sparse materializes the bound linear combination from the already prepared kernels. backend=:auto selects sparse only when its conservative live-materialization bound fits memory_budget; otherwise it selects the matrix-free route. An explicit sparse request that exceeds the budget raises before allocating the sparse operator. The matrix-free bound includes its compatibility workspace and copied trace vector but excludes the already retained shared family plan. Pass memory_budget=Inf only as an explicit opt-out. Both backends are autonomous.\n\n\n\n\n\nspecialize(family::AffineCompiledPIModelFamily,\n           parameters=family.defaults; kwargs...)\n\nBind named or positional affine coefficients and return an ordinary SpecializedPIModel.  All sparse, matrix-free, solver, scan, and workspace APIs therefore work without an affine-specific execution path.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.specialize"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.apply!","page":"Dynamics and evolution","text":"Apply P using caller-owned scratch; one workspace may not be shared concurrently.\n\n\n\n\n\nApply a joint projector with caller-owned scratch.\n\n\n\n\n\napply!(destination, S, source, time, parameters, workspace)\napply!(destination, S, source, workspace)\n\nApply a sum-of-Kronecker composite superoperator without forming any Kronecker matrix.  The explicit workspace is preallocated and task-owned. Source and destination must not alias.\n\n\n\n\n\napply!(destination, S, source, time, parameters,\n       workspace::CompositeSuperoperatorBatchWorkspace)\n\nApply a composite generator to several right-hand sides through fixed-capacity task-owned scratch. Factor schedules and term coefficients are evaluated once per tensor fiber and batch rather than once per right-hand side.\n\n\n\n\n\nApply a restricted Liouvillian using caller-owned full-coordinate scratch.\n\n\n\n\n\napply!(destination, plan::HEOMPlan, source, time, parameters, workspace)\napply!(destination, plan::HEOMPlan, source, workspace)\n\nApply the truncated HEOM generator without constructing its matrix. The explicit-time method supports a driven system Liouvillian. The shorter method requires an autonomous system.\n\n\n\n\n\nApply a prepared one-period map with caller-owned scratch.\n\n\n\n\n\nApply a prepared one-period map to a fixed-capacity block in one batched RK4 traversal.\n\n\n\n\n\nApply a population plan with caller-owned scratch.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.apply!"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.apply_adjoint!","page":"Dynamics and evolution","text":"apply_adjoint!(destination, L, input, time, parameters, workspace)\napply_adjoint!(destination, L, input[, workspace])\n\nApply the adjoint PI Liouvillian in orthonormal coefficient coordinates. L may be a prepared plan, compiled model, or matrix-free Liouvillian; vector and batched matrix inputs are supported. The explicit-time form accepts driven generators. Reuse one compatible LiouvillianWorkspace per task to avoid scratch allocation; overloads without time require an autonomous generator.\n\n\n\n\n\napply_adjoint!(destination, S, source, time, parameters, workspace)\napply_adjoint!(destination, S, source, workspace)\n\nApply the adjoint of a factorized CompositeSuperoperator without forming a Kronecker matrix. Matrix factors use their adjoint maps and prepared PI factors delegate to their allocation-conscious apply_adjoint! methods. The scalar coefficient of every product term is conjugated. Reuse the same task-owned CompositeSuperoperatorWorkspace used by forward application.\n\n\n\n\n\napply_adjoint!(destination, S, source, time, parameters,\n               workspace::CompositeSuperoperatorBatchWorkspace)\n\nApply the exact adjoint to several right-hand sides using the same fixed-capacity batched tensor-fiber layout as apply!.\n\n\n\n\n\nApply the adjoint compressed action when the source provides one.\n\n\n\n\n\napply_adjoint!(destination, plan::HEOMPlan, source, time, parameters,\n               workspace)\napply_adjoint!(destination, plan::HEOMPlan, source, workspace)\n\nApply the exact Hilbert–Schmidt adjoint of the prepared HEOM generator in its stored (scaled or unscaled) coordinate basis. The explicit-time method supports a driven system Liouvillian. Source and destination storage must not overlap, and one HEOMWorkspace may be reused sequentially.\n\n\n\n\n\napply_adjoint!(destination, map::FloquetMap, input, workspace)\n\nApply the exact adjoint of the discrete RK4 period map.  Reverse accumulation uses the same stage times and coefficients as apply!, so the dot- product identity holds to roundoff for the chosen finite step count.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.apply_adjoint!"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.threaded_apply!","page":"Dynamics and evolution","text":"threaded_apply!(destination, plan_or_compiled, source, time, parameters, workspace)\nthreaded_apply!(destination, plan_or_compiled, source, workspace)\n\nApply a prepared PI Liouvillian with deterministic, output-sector-owned Julia tasks. The short form requires an autonomous plan. Unlike compatibility mul!, this opt-in route never changes global BLAS threading and never uses atomic output updates or task-order-dependent reductions.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.threaded_apply!"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.threaded_apply_adjoint!","page":"Dynamics and evolution","text":"threaded_apply_adjoint!(destination, plan_or_compiled, source, time, parameters, workspace)\nthreaded_apply_adjoint!(destination, plan_or_compiled, source, workspace)\n\nApply the Frobenius adjoint of a prepared PI Liouvillian using the same deterministic output-sector ownership as threaded_apply!.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.threaded_apply_adjoint!"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.isautonomous","page":"Dynamics and evolution","text":"Return whether a model or Liouvillian is independent of time and parameters.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.isautonomous"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.freeze","page":"Dynamics and evolution","text":"freeze(model; time, parameters=nothing, representation=:matrixfree)\n\nEvaluate every time- or parameter-dependent term at the explicitly supplied time and return an autonomous Liouvillian. This is the required bridge from a driven model to stationary-state or spectral linear algebra; it never silently assumes time == 0.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.freeze"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.dynamics_problem","page":"Dynamics and evolution","text":"dynamics_problem(source, rho0, tspan; parameters=nothing)\n\nConstruct an in-place SciMLBase.ODEProblem for PI coefficient dynamics. source may be a PIModel, compiled model, Liouvillian plan, or compatible matrix/matrix-free operator. The initial coefficients are copied, and prepared sources reuse one problem-owned Liouvillian workspace. Choose an adaptive or stiff solver from the SciML ecosystem separately.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.dynamics_problem"},{"category":"type","location":"api/dynamics/#PermutationalInvariantDynamics.PISolution","page":"Dynamics and evolution","text":"PISolution(raw, basis)\n\nAttach a PIBasis to a SciML solution whose state vectors are PI coefficients. Use state to reconstruct PIState objects at saved indices or interpolated times.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.PISolution"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.state","page":"Dynamics and evolution","text":"state(solution::PISolution, index)\nstate(solution::PISolution, time)\n\nReturn a PIState from a saved solution index. Use state_at for the raw solution's continuous interpolation at a physical time, including an integer-valued time; state(solution, integer) always selects a saved index.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.state"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.state_at","page":"Dynamics and evolution","text":"state_at(solution, time)\n\nReturn the state represented by solution at physical time. Saved-grid results require time to match a stored sample (up to isapprox); SciML solutions use their continuous interpolation. Unlike state(solution, i), an integer passed to state_at is always interpreted as a physical time rather than a saved-state index. Package solvers return nondecreasing saved grids; manually constructed result objects must preserve that invariant so the allocation-free binary lookup remains valid.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.state_at"},{"category":"type","location":"api/dynamics/#PermutationalInvariantDynamics.EvolutionWorkspace","page":"Dynamics and evolution","text":"Reusable storage for allocation-conscious fourth-order Liouvillian evolution.\n\nNew workspaces use three full integration arrays: tmp is the stage state, k1 is the current derivative, and k2 is the weighted stage accumulator. The legacy k3 and k4 fields remain as empty compatibility placeholders; legacy field-constructed workspaces with full arrays also remain accepted.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.EvolutionWorkspace"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.evolve!","page":"Dynamics and evolution","text":"evolve!(dest, L, src, tspan; steps=256, parameters=nothing, workspace=nothing)\n\nPropagate PI coordinates using a preallocated three-scratch fixed-step RK4 kernel. L may be a matrix, MatrixFreeLiouvillian, or PIModel. dest may alias src, but it must not alias active workspace scratch.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.evolve!"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.time_evolve","page":"Dynamics and evolution","text":"Return the composite state obtained by propagating rho over tspan.\n\n\n\n\n\nReturn the PI state obtained by propagating rho over tspan.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.time_evolve"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.time_evolution","page":"Dynamics and evolution","text":"Return composite states at ordered times using one reusable workspace.\n\n\n\n\n\nReturn states at ordered sampling times using one reusable workspace.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.time_evolution"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.population_dimension","page":"Dynamics and evolution","text":"population_dimension(basis)\n\nReturn the number sum_nu dim(U_nu) of diagonal GT-pattern populations in basis.  This differs from length(basis), which counts all sum_nu dim(U_nu)^2 PI operator coordinates.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.population_dimension"},{"category":"type","location":"api/dynamics/#PermutationalInvariantDynamics.PopulationInvarianceReport","page":"Dynamics and evolution","text":"PopulationInvarianceReport\n\nResult of testing whether a PI generator preserves Schur-basis diagonality. reason == :certified denotes exact zero leakage at the selected scalar precision. :within_tolerance is used only when the caller explicitly supplies a nonzero tolerance that accepts a nonzero residual; :offdiagonal_leakage rejects the restriction. Nonfinite generator data or tolerances throw and are never certified.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.PopulationInvarianceReport"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.population_invariance","page":"Dynamics and evolution","text":"population_invariance(source[, basis]; atol=nothing, rtol=nothing)\n\nReturn a PopulationInvarianceReport without constructing a usable plan. invariant == missing means that a global certificate is unavailable, most commonly because an operator-valued schedule has not been frozen. Defaults are structurally strict; nonzero atol or rtol explicitly permit an approximate projection and are reflected in the report's reason.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.population_invariance"},{"category":"type","location":"api/dynamics/#PermutationalInvariantDynamics.PopulationPlan","page":"Dynamics and evolution","text":"PopulationPlan(source[, basis]; atol=nothing, rtol=nothing)\n\nCompile the restriction of a PI Liouvillian to physical Schur-diagonal populations.  Construction succeeds only after certifying that the diagonal subspace is invariant.  Fixed operators with scalar time-dependent rates are compiled term by term and remain time dependent without rebuilding their sparse population matrices.  Operator-valued schedules must first be frozen at an explicit time. The default atol=nothing, rtol=nothing is structurally strict (both effective tolerances are zero), so a weak but nonzero coherence-generating term is never discarded. Supplying a nonzero tolerance is an explicit approximate-projection request and is recorded as invariance.reason == :within_tolerance whenever it accepts nonzero leakage.\n\nOnly the population generator and basis are retained. Full-PI coordinate lookup data are compile-local and scale with population_dimension(basis), not length(basis).\n\nThe current plan compiler forms the standalone coordinate conversion sqrt(f^nu) in its working real type; model/compiled-plan sources also inherit the parent Liouvillian trace functional. If a required standalone scale is outside that type's finite range, construction raises with wider-type guidance. This is a plan-construction limit, not a limitation of diagonal_populations or state_from_populations, whose prepared scaled products can remain finite without representing the factor by itself.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.PopulationPlan"},{"category":"type","location":"api/dynamics/#PermutationalInvariantDynamics.PopulationWorkspace","page":"Dynamics and evolution","text":"Reusable scratch for population-generator application and fixed-step RK4 evolution. New workspaces retain three full integration arrays (stage, k1, and k2) plus the independent kernel-application buffer. The legacy k3 and k4 fields are empty compatibility placeholders; full-sized legacy values are also accepted.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.PopulationWorkspace"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.diagonal_populations","page":"Dynamics and evolution","text":"diagonal_populations(A; check=true, atol=nothing, rtol=nothing)\n\nAllocate and return the multiplicity-weighted physical Schur-diagonal entries of A. See diagonal_populations! for validation and projection semantics.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.diagonal_populations"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.diagonal_populations!","page":"Dynamics and evolution","text":"diagonal_populations(A; check=true, atol=nothing, rtol=nothing)\ndiagonal_populations!(destination, A; ...)\n\nExtract multiplicity-weighted physical diagonal entries from every Schur sector in basis order.  Their sum equals trace(A).  By default appreciable off-diagonal entries raise rather than being discarded; check=false is an explicit projection request. atol and rtol measure off-diagonal entries in these same bounded multiplicity-weighted coordinates.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.diagonal_populations!"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.state_from_populations","page":"Dynamics and evolution","text":"state_from_populations(basis, populations; validate=false, validation_kwargs...)\n\nConstruct a Schur-diagonal PIState from multiplicity-weighted physical populations.  No normalization, positivity repair, or clipping is performed. Set validate=true to call validate_state on the result.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.state_from_populations"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.population_generator","page":"Dynamics and evolution","text":"population_generator(plan; representation=:matrixfree, time=nothing,\n                     parameters=nothing)\n\nReturn the reusable PopulationPlan itself for :matrixfree, or assemble its sparse reduced generator.  A driven plan requires an explicit time for sparse assembly.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.population_generator"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.evolve_populations!","page":"Dynamics and evolution","text":"evolve_populations!(destination, plan, source, tspan; steps=256,\n                    parameters=nothing, workspace=nothing)\n\nPropagate a population vector with preallocated three-scratch fourth-order Runge–Kutta evolution. destination may alias source, but it must not alias workspace scratch; one workspace may be reused sequentially but not concurrently.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.evolve_populations!"},{"category":"type","location":"api/dynamics/#PermutationalInvariantDynamics.PopulationSolution","page":"Dynamics and evolution","text":"Saved population vectors and their certified population plan.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.PopulationSolution"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.solve_populations","page":"Dynamics and evolution","text":"solve_populations(plan, initial, tspan; saveat=nothing,\n                  steps_per_interval=64, parameters=nothing)\n\nEvolve and save only certified Schur-diagonal physical populations.  Passing a PIState extracts its populations after checking that it is diagonal.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.solve_populations"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.stationary_populations","page":"Dynamics and evolution","text":"stationary_populations(plan; method=:auto, kwargs...)\n\nSolve the trace-one stationary problem in the reduced population space.  The returned vector uses physical populations and therefore has trace functional ones(size(plan,1)).\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.stationary_populations"},{"category":"type","location":"api/dynamics/#PermutationalInvariantDynamics.QuantumTrajectory","page":"Dynamics and evolution","text":"One PI quantum-jump realization and its recorded jump channel indices.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.QuantumTrajectory"},{"category":"type","location":"api/dynamics/#PermutationalInvariantDynamics.TrajectorySteadyStateResult","page":"Dynamics and evolution","text":"TrajectorySteadyStateResult\n\nDetailed result returned by trajectory_steady_state or weak_pi_trajectory_steady_state when return_info=true. state is the unmodified Monte Carlo average of the post-settling path means. sample_spread is the square root of their unbiased sample variance in Hilbert–Schmidt norm, and standard_error is the corresponding Hilbert–Schmidt standard-error norm. The PI coefficient basis is orthonormal, so these quantities are evaluated without reconstructing the full Hilbert space.\n\nresidual, relative_residual, and trace_error diagnose the returned average; they are reports, not convergence certificates. When named Hermitian observables were requested, that field contains statistics across the same independent path means. Samples taken along one path are averaged before any uncertainty is estimated and are therefore never counted as independent trajectories. A weak-PI result requested with batch_size retains an additional WeakPIBatchMeansDiagnostics in metadata.batch_means; the primary fields keep their independent-path meaning.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.TrajectorySteadyStateResult"},{"category":"type","location":"api/dynamics/#PermutationalInvariantDynamics.TrajectoryPlan","page":"Dynamics and evolution","text":"TrajectoryPlan(model; T=nothing)\nTrajectoryPlan(compiled; T=nothing)\n\nImmutable prepared geometry for PI quantum trajectories. A plan lowers the model once, separates Hamiltonian and jump kernels, and retains the sector-trace weights used to evaluate channel intensities without constructing gain states. It may be shared by tasks; each concurrent worker must use its own TrajectoryWorkspace.\n\nTrajectory plans require fixed jump operators. Scalar rates may still depend on time and parameters. Rates must evaluate to finite, nonnegative real values representable in the prepared precision. An empty model has no scalar-bearing term from which to infer a precision; pass its desired concrete real floating type as T when it is not Float64. The same keyword is accepted for an empty compiled model.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.TrajectoryPlan"},{"category":"type","location":"api/dynamics/#PermutationalInvariantDynamics.TrajectoryWorkspace","page":"Dynamics and evolution","text":"TrajectoryWorkspace(plan, rho; mode=:full)\nTrajectoryWorkspace(model, rho; mode=:full)\nTrajectoryWorkspace(compiled, rho; mode=:full)\n\nPreallocated mutable stage vectors and Schur-block scratch for one PI quantum trajectory at a time. Reuse it sequentially. Concurrent paths must have distinct workspaces, which may all refer to the same immutable TrajectoryPlan.\n\nThe workspace retains the rate-weighted jump-loss blocks at the current integration node. Repeated RK stages at that node reuse the prepared blocks; driven-rate caches are invalidated when a new trajectory starts.\n\nThe default mode=:full supports both fixed-step RK4 and adaptive event-driven Dormand–Prince paths. Fixed-step RK4 uses three full-vector registers; mode=:fixed additionally omits k3, k4, and the six Dormand–Prince stage, trial, embedded, and event-root vectors. A fixed-only workspace rejects algorithm=:event instead of allocating the omitted storage lazily.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.TrajectoryWorkspace"},{"category":"type","location":"api/dynamics/#PermutationalInvariantDynamics.TrajectoryBatchWorkspace","page":"Dynamics and evolution","text":"TrajectoryBatchWorkspace(plan, rho;\n                         workers=Threads.nthreads(), mode=:full)\nTrajectoryBatchWorkspace(model, rho;\n                         workers=Threads.nthreads(), mode=:full)\nTrajectoryBatchWorkspace(compiled, rho;\n                         workers=Threads.nthreads(), mode=:full)\n\nReusable worker pool for quantum_trajectories and trajectory_steady_state. The immutable trajectory plan is stored once, while every worker owns independent integrator scratch and a reusable random-number generator. A batch workspace may be reused sequentially but must not be used by concurrent ensemble or estimator calls. mode is forwarded to every worker; use mode=:fixed only when all calls will use the fixed-step algorithm.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.TrajectoryBatchWorkspace"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.quantum_trajectory","page":"Dynamics and evolution","text":"quantum_trajectory(source, rho0, times; dt, algorithm=:fixed, rng,\n                   parameters=nothing, max_jump_probability=0.05,\n                   abstol=1e-9, reltol=1e-7,\n                   dtmin=eps(R), dtmax=dt,\n                   event_time_tolerance=1e-10, workspace=nothing)\n\nSimulate one PI quantum-jump trajectory from a PIModel, CompiledPIModel, or reusable TrajectoryPlan. Local jump channels are unresolved over particle labels and therefore generally produce mixed conditional PI states. The fixed step is automatically shortened so the total jump probability remains below max_jump_probability.\n\nAll rate-weighted channel loss blocks are combined into one effective Schur operator per integration stage. The normalized no-jump drift therefore does not apply one matrix product per channel; individual channel intensities are evaluated only when an event must select its channel.\n\nSet algorithm=:event (aliases :adaptive and :event_driven) to integrate the normalized no-jump equation and its accumulated hazard with an embedded Dormand–Prince 5(4) method. Jump times are then continuous hazard roots, rather than endpoints of Bernoulli time steps. Root bisection evaluates the quartic continuous extension of the accepted stages, so it does not rerun a seven-stage trial at every candidate time. dt is the initial adaptive step and dtmax its upper bound; abstol, reltol, dtmin, and event_time_tolerance control the adaptive solve. A final step may be shorter than dtmin solely to land on a requested output time. Here R is the real floating precision of the prepared trajectory. Time grids and explicitly supplied controls must be representable in R without narrowing; default controls are constructed directly in R.\n\nmemory_budget preflights the predictable saved state/time history before trajectory-plan or workspace construction. It is an output lower-bound guard; data-dependent jump records and reusable worker scratch remain additional.\n\n\n\n\n\nquantum_trajectory(plan::CompositeTrajectoryPlan, rho0, times;\n                   dt, rng=Random.default_rng(), parameters=nothing,\n                   max_jump_probability=0.05, workspace=nothing)\n\nSimulate one density-valued composite quantum-jump trajectory with preallocated RK4 conditional evolution. Channel hazards are integrated with the same four stages, and a trial step is shortened and retried until its total jump probability is at most max_jump_probability. Rates, times, and dt must be representable in the prepared real precision.\n\nComposite event-driven trajectories are not currently implemented; refine dt explicitly to establish time-discretization convergence.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.quantum_trajectory"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.quantum_trajectories","page":"Dynamics and evolution","text":"quantum_trajectories(source, rho0, times, n; seed=0, threaded=false,\n                     workspace=nothing, observables=nothing,\n                     save_states=true, jump_statistics=true,\n                     confidence=0.95, trajectory_keywords...)\n\nGenerate independent PI trajectories. Model geometry is lowered once per batch and shared read-only. Serial execution reuses one worker; threaded execution uses dynamically scheduled task-owned workers and small work chunks, avoiding scratch races while amortizing atomic scheduling and retaining load balance for paths with different jump counts. Random streams are seeded by trajectory index, so a fixed seed gives the same ordered results independently of scheduling and thread count.\n\nPass a reusable TrajectoryBatchWorkspace to amortize setup across several ensembles. A single TrajectoryWorkspace is accepted only for serial execution. Returned trajectories retain independent time and state storage. With threaded=true, callable scalar rates and objects supplied via parameters must themselves be safe for concurrent read/evaluation. Because every requested state is returned, output storage scales as n * length(times) * length(rho0.data); request only the sampling times needed for analysis when state-history memory is limiting.\n\nPass named observables and set save_states=false for memory-light online ensemble statistics. This returns a TrajectoryEnsembleResult and never constructs sampled PIState objects. One observable buffer and one Welford accumulator are retained per active worker. Channel-resolved jump statistics are accumulated online by default; set jump_statistics=false to omit them. A state-free call requires at least one observable because the no-observable route preserves the legacy trajectory-vector return type. The default memory_budget rejects predictable saved state/time histories and retained observable-statistics arrays above 512 MiB before their allocation. It does not bound the data-dependent number of jump records; disable jump_statistics when those records are unnecessary.\n\n\n\n\n\nquantum_trajectories(plan::CompositeTrajectoryPlan, rho0, times, n;\n                     seed=0, threaded=false, workspace=nothing,\n                     observables=nothing, save_states=true,\n                     jump_statistics=true, confidence=0.95, dt, ...)\n\nGenerate independent density-valued composite trajectories. The immutable plan is shared, each worker owns its workspace and RNG, and random streams are derived from the global trajectory index. Serial and threaded calls therefore sample the same ordered paths for a fixed seed.\n\nWith named Hermitian CompositePIOperator observables and save_states=false, return a TrajectoryEnsembleResult containing online means, variances, confidence intervals, and optional jump statistics without constructing sampled state histories.\n\nmemory_budget preflights predictable saved state/time histories and retained observable-statistics arrays before they are allocated. Data-dependent jump records and reusable worker scratch are additional storage.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.quantum_trajectories"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.trajectory_steady_state","page":"Dynamics and evolution","text":"trajectory_steady_state(source, rho0;\n    trajectories, settling_time, dt,\n    samples_per_trajectory=1, sampling_interval=nothing,\n    seed=0, threaded=false, workspace=nothing,\n    observables=nothing, confidence=0.95, return_info=false,\n    memory_budget=512*1024^2,\n    parameters=nothing, max_jump_probability=0.05,\n    algorithm=:fixed, abstol=1e-9, reltol=1e-7,\n    dtmin=eps(R), dtmax=dt, event_time_tolerance=1e-10)\n\nEstimate an autonomous stationary PI density operator with quantum-jump trajectories. Each independent path is evolved from rho0 to settling_time. One or more states are then sampled at equal sampling_intervals and averaged within that path. The returned density operator is the average of those independent path means. No state history or jump record is constructed, so retained state storage is O(workers * length(rho0.data)) rather than proportional to the number of paths or post-settling samples.\n\nAt least two trajectories are required. Multiple samples can reduce the variance of an ergodic path average, but samples from the same path may be correlated. They are therefore averaged before the path-to-path uncertainty is evaluated and never counted as independent observations. Choose the settling time, sampling interval, sampling-window length, path count, and integration controls through explicit convergence studies. Strong symmetries or multiple stationary states can make the result depend on rho0.\n\nThe source may be a PIModel, CompiledPIModel, or reusable TrajectoryPlan, but it must be autonomous and use the fixed jump operators supported by PI trajectories. For a periodically driven stationary regime, use Floquet analysis. Call freeze only when the stationary state of one explicitly selected instantaneous generator is the intended question; freezing does not solve the driven problem. threaded=true uses task-owned workspaces and trajectory-indexed random streams. Pass a TrajectoryBatchWorkspace to reuse those buffers across calls.\n\nBy default, return the estimated PIState. Set return_info=true to receive a TrajectorySteadyStateResult, including the Hilbert–Schmidt sample spread and standard-error norm, the Liouvillian residual, relative residual, and trace error. These diagnostics are not a steady-state certificate and the state is never normalized, symmetrized, or positivity-repaired. With return_info=true, named Hermitian observables add means, unbiased variances, standard errors, and normal confidence intervals across the independent path means; requesting observables without the detailed result is rejected rather than computing and discarding their statistics.\n\nmemory_budget guards the retained prepared plan/workspace and the predictable sampling, accumulator, seed, and result arrays. For a raw PIModel, an assembly-free preparation preflight also runs before trajectory lowering. Inf is the explicit opt-out. Allocator metadata and RNG implementation storage remain outside the estimate.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.trajectory_steady_state"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.trajectory_average","page":"Dynamics and evolution","text":"Average equally sampled quantum trajectories into PI density matrices.\n\n\n\n\n\nAverage equally sampled composite quantum trajectories.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.trajectory_average"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.jump_statistics","page":"Dynamics and evolution","text":"jump_statistics(trajectories; nchannels=nothing)\n\nReturn total and channel-resolved jump-count statistics, empirical rates, Fano factors, no-jump probability, and pooled inter-jump waiting times. Unbiased sample variances are used when at least two trajectories are present.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.jump_statistics"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.trajectory_observable_statistics","page":"Dynamics and evolution","text":"trajectory_observable_statistics(trajectories, observables; confidence=0.95)\n\nCompute time-resolved Monte Carlo means, unbiased variances, standard errors, and normal confidence intervals. observables may be a named tuple, dictionary, pair collection, or a single Hermitian local matrix/PIOperator.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.trajectory_observable_statistics"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.trajectory_statistics","page":"Dynamics and evolution","text":"Return averaged states together with jump and optional observable statistics.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.trajectory_statistics"},{"category":"type","location":"api/dynamics/#PermutationalInvariantDynamics.WeakPIPseudoKet","page":"Dynamics and evolution","text":"WeakPIPseudoKet(basis, data; atol, rtol)\n\nNormalized pseudo-ket in the direct sum of the retained Schur irreps, directsum_nu U_nu.  Its sector slices are denoted psi_nu and map to a physical PI density operator through\n\nC_nu = psi_nu psi_nu^dagger  sqrt(f^nu).\n\nThis is an auxiliary weak-PI trajectory state, not a pure state of labeled particles and not a vector in the full d^N Hilbert space.  In particular, relative phases between different Schur sectors have no physical meaning. Construction checks dimensions, finiteness, and unit norm and never normalizes the supplied vector silently.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.WeakPIPseudoKet"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.weak_pi_dimension","page":"Dynamics and evolution","text":"Return sum_nu dim(U_nu), the direct-sum weak-PI pseudo-ket dimension.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.weak_pi_dimension"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.weak_pi_density","page":"Dynamics and evolution","text":"weak_pi_density(state)\n\nConvert a WeakPIPseudoKet into its physical PIState.  Only sector-diagonal outer products are retained, as required by the PI algebra; no full-Hilbert-space object is constructed.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.weak_pi_density"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.weak_pi_pseudoket","page":"Dynamics and evolution","text":"weak_pi_pseudoket(rho; atol, rtol)\n\nConstruct a direct-sum weak-PI pseudo-ket from a PIState whose multiplicity-weighted block in every occupied Schur sector has numerical rank one.  General mixed PI states are rejected.  Sector phases are fixed by the eigensolver and are physically irrelevant.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.weak_pi_pseudoket"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.weak_pi_expectation","page":"Dynamics and evolution","text":"weak_pi_expectation(state, observable)\n\nEvaluate an observable on a weak-PI pseudo-ket without first constructing a density matrix.  observable may be a compatible PIOperator, a local matrix (interpreted as its collective sum), or a prepared CollectiveObservablePlan.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.weak_pi_expectation"},{"category":"type","location":"api/dynamics/#PermutationalInvariantDynamics.WeakPIKrausBranch","page":"Dynamics and evolution","text":"One Kraus branch used by a weak-PI jump channel.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.WeakPIKrausBranch"},{"category":"type","location":"api/dynamics/#PermutationalInvariantDynamics.WeakPIJumpRecord","page":"Dynamics and evolution","text":"WeakPIJumpRecord\n\nMetadata for one sampled weak-PI Kraus branch.  source_partition and target_partition are the Schur sectors before and after the jump; child_partition identifies the one-box subduction branch of a local jump and is nothing for a sector-preserving collective/direct jump.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.WeakPIJumpRecord"},{"category":"type","location":"api/dynamics/#PermutationalInvariantDynamics.WeakPIQuantumTrajectory","page":"Dynamics and evolution","text":"One saved weak-PI pseudo-ket trajectory and its refined jump records.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.WeakPIQuantumTrajectory"},{"category":"type","location":"api/dynamics/#PermutationalInvariantDynamics.WeakPITrajectoryPlan","page":"Dynamics and evolution","text":"WeakPITrajectoryPlan(model; T=nothing)\nWeakPITrajectoryPlan(compiled; T=nothing)\n\nPrepare the opt-in direct-sum Schur-irrep pseudo-ket unraveling.  Fixed collective, direct-PI, and collective p-body jumps are split by source Schur sector.  A fixed one-body LocalJump is decomposed into sector-changing Kraus maps using one-box Schur subduction.  The decomposition is valid for qubits and qudits and is checked against every channel's prepared K^dagger K block at construction.\n\nOperator-valued schedules and LocalPBodyJump are rejected.  Scalar jump rates may depend on time and parameters but must evaluate to finite, nonnegative real values in the prepared precision.  This plan never constructs a d^N vector or matrix.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.WeakPITrajectoryPlan"},{"category":"type","location":"api/dynamics/#PermutationalInvariantDynamics.WeakPITrajectoryWorkspace","page":"Dynamics and evolution","text":"WeakPITrajectoryWorkspace(plan, state; mode=:full)\n\nCaller-owned integration and Kraus-selection scratch for one weak-PI trajectory.  Reuse sequentially; concurrent paths require distinct workspaces. The workspace caches the rate-weighted jump-loss blocks at the current integration node; driven-rate caches are invalidated before a new trajectory. The default mode=:full supports fixed-step and adaptive event-driven paths. Fixed-step RK4 uses three full-vector registers. Use mode=:fixed to omit k3, k4, and the six Dormand–Prince and event-root vectors when the workspace will only be used with algorithm=:fixed.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.WeakPITrajectoryWorkspace"},{"category":"type","location":"api/dynamics/#PermutationalInvariantDynamics.WeakPITrajectoryBatchWorkspace","page":"Dynamics and evolution","text":"WeakPITrajectoryBatchWorkspace(plan, state;\n                               workers=Threads.nthreads(), mode=:full)\n\nReusable task-owned workspace/RNG pool for weak_pi_quantum_trajectories and weak_pi_trajectory_steady_state.  The immutable Kraus plan is shared; every worker owns all mutable numerical scratch.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.WeakPITrajectoryBatchWorkspace"},{"category":"type","location":"api/dynamics/#PermutationalInvariantDynamics.WeakPIBatchMeansDiagnostics","page":"Dynamics and evolution","text":"WeakPIBatchMeansDiagnostics\n\nAutocorrelation-aware Hilbert–Schmidt uncertainty diagnostic optionally attached to weak_pi_trajectory_steady_state(...; batch_size=...) as result.metadata.batch_means.  batch_size consecutive post-settling time samples are averaged before the variance is formed, and effective_independent_samples is the number of resulting batch means.\n\nThe reported standard error assumes those batch means are approximately independent.  Increase batch_size and the total sampling window until the estimate stabilizes.  The diagnostic does not certify burn-in or finite-window bias and never changes the returned state or the primary independent-path standard error.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.WeakPIBatchMeansDiagnostics"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.weak_pi_quantum_trajectory","page":"Dynamics and evolution","text":"weak_pi_quantum_trajectory(source, state0, times; dt, rng,\n    parameters=nothing, max_jump_probability=0.05,\n    algorithm=:fixed, abstol=1e-9, reltol=1e-7,\n    dtmin=eps(R), dtmax=dt, event_time_tolerance=1e-10,\n    workspace=nothing)\n\nSimulate one fixed-step weak-PI pseudo-ket trajectory.  source is a PIModel, CompiledPIModel, or reusable WeakPITrajectoryPlan. Every local event samples a mathematically complete sector-changing Kraus branch, while jump_channels retains the model-level channel index.\n\nThe fixed step is shortened to control the maximum jump probability. All rate-weighted channel loss blocks are combined before one matrix-vector action per Schur sector; individual channel and Kraus-branch intensities are evaluated only when an event must be sampled. Set algorithm=:event (aliases :adaptive and :event_driven) to integrate the normalized pseudo-ket and accumulated jump hazard with the embedded Dormand–Prince 5(4) method.  Events are continuous hazard roots and sample the same sector-resolved Kraus branches as the fixed-step path. Bisection uses the quartic continuous extension of the accepted stages and performs no new RHS trials. dt is then the initial step; dtmax, dtmin, abstol, reltol, and event_time_tolerance control the adaptive solve.\n\nThis opt-in backend is distinct from quantum_trajectory, whose unresolved local event produces a density-valued conditional PI state.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.weak_pi_quantum_trajectory"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.weak_pi_quantum_trajectories","page":"Dynamics and evolution","text":"weak_pi_quantum_trajectories(source, state0, times, n;\n                             seed=0, threaded=false,\n                             workspace=nothing, algorithm=:fixed,\n                             trajectory_keywords...)\n\nGenerate an ensemble of weak-PI pseudo-ket trajectories.  A prepared Kraus plan is shared read-only, worker scratch is task-owned, and random streams are derived from the trajectory index so serial and threaded runs are reproducible for a fixed seed. memory_budget bounds the predictable pseudo-ket state/time history before Kraus-plan construction; jump records and worker scratch are additional data-dependent or reusable storage.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.weak_pi_quantum_trajectories"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.weak_pi_trajectory_steady_state","page":"Dynamics and evolution","text":"weak_pi_trajectory_steady_state(source, state0;\n    trajectories, settling_time, dt,\n    samples_per_trajectory=1, sampling_interval=nothing,\n    batch_size=nothing, algorithm=:fixed,\n    seed=0, threaded=false, workspace=nothing,\n    observables=nothing, confidence=0.95, return_info=false,\n    parameters=nothing, max_jump_probability=0.05)\n\nEstimate an autonomous stationary PI density operator with weak-PI pseudo-ket trajectories. For each independent path, the physical equation-(7) coefficient block is reconstructed at every selected time as\n\nC_nu(psi)=psi_nu psi_nu^daggersqrt(f^nu).\n\nThose density contributions are averaged within the path before independent path means are combined. Pseudo-ket amplitudes are never averaged directly: the density reconstruction is quadratic and relative phases between Schur sectors are unphysical. The returned object is consequently a generally mixed PIState, not a WeakPIPseudoKet.\n\nNo pseudo-ket history or jump record is constructed. Sector outer products are accumulated directly into preallocated PI coefficient vectors, so retained state and density storage scales with the worker count rather than with the number of trajectories or post-settling samples. Reproducible random streams retain one UInt64 seed per trajectory. Each density sample still requires sum_nu dim(U_nu)^2 work because the requested result is a full PI density operator.\n\nAt least two trajectories are required. Multiple post-settling samples are averaged within each path and never treated as independent observations. Choose the settling time, sampling interval and window, path count, dt, and max_jump_probability through separate convergence studies. Strong symmetries or multiple stationary states can make the estimate depend on state0; the unraveling also affects finite-sample variance.\n\nSet batch_size to an integer of at least two that exactly divides samples_per_trajectory to add an autocorrelation-aware batch-means report at result.metadata.batch_means; this requires return_info=true. Consecutive time samples are averaged in each batch before their Hilbert–Schmidt variance is evaluated. The reported effective independent sample count is the number of complete batch means and assumes batches are long enough to be approximately independent.  Refine the batch length and total window: this diagnostic does not certify burn-in or finite-window bias.  The primary standard_error remains the conservative independent-path estimate, so enabling batch means never silently changes its meaning.\n\nThe source may be a PIModel, CompiledPIModel, or reusable WeakPITrajectoryPlan, and must be autonomous.  Set algorithm=:event (or :adaptive/:event_driven) for continuous-hazard Dormand–Prince evolution. Current weak-PI operator restrictions still apply: fixed supported jump operators, no LocalPBodyJump, and an initial state representable by one weak-PI pseudo-ket. threaded=true uses task-owned workers and trajectory-indexed random streams. Pass a WeakPITrajectoryBatchWorkspace to reuse them across calls.\n\nBy default, return the estimated PIState. With return_info=true, return a TrajectorySteadyStateResult containing independent-path Hilbert–Schmidt uncertainty, optional named Hermitian-observable statistics, the density Liouvillian residual, relative residual, and trace error. These are diagnostics rather than a steady-state certificate. The averaged state is never normalized, symmetrized, or positivity-repaired. Requesting observables without the detailed result is rejected.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.weak_pi_trajectory_steady_state"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.weak_pi_trajectory_average","page":"Dynamics and evolution","text":"Average weak-PI pseudo-ket paths into physical PIState objects.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.weak_pi_trajectory_average"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.weak_pi_trajectory_statistics","page":"Dynamics and evolution","text":"weak_pi_trajectory_statistics(trajectories; observables=nothing,\n                              confidence=0.95, nchannels=nothing)\n\nReturn physical ensemble-average PI states, channel and Schur-sector-resolved jump statistics, and optional Hermitian-observable Monte Carlo statistics for weak-PI pseudo-ket trajectories.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.weak_pi_trajectory_statistics"},{"category":"type","location":"api/dynamics/#PermutationalInvariantDynamics.MeanFieldPlan","page":"Dynamics and evolution","text":"MeanFieldPlan(model::PIModel; limit=:finite)\nMeanFieldPlan(N, d, terms; limit=:finite)\n\nPrepare the finite-N product-state closure tr_2...N(L(sigma^otimes N)).  limit=:thermodynamic retains the leading N^(p-1)/(p-1)! subset count for local/Hamiltonian p-body terms.  For a collective p-body jump it keeps the leading disjoint-subset field with count N^(2p-1)/((p-1)!p!) and drops subleading overlap/self terms.  Rates are used exactly as supplied; no Kac normalization is inferred or inserted.\n\nOperators are copied into the immutable plan.  Fixed one- and symmetric p-body Hamiltonians, independent jumps, and collective p-body jumps are supported.  Direct PI terms, operator-valued time functions, and custom terms without an explicit mean-field lowering are rejected explicitly.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.MeanFieldPlan"},{"category":"type","location":"api/dynamics/#PermutationalInvariantDynamics.MeanFieldWorkspace","page":"Dynamics and evolution","text":"Mutable, per-task scratch for mean-field contractions and low-storage RK4 evolution. New workspaces allocate three full one-site integration matrices: stage, the current derivative k1, and the weighted accumulator k2. Legacy fields k3 and k4 remain as empty compatibility placeholders; field-constructed workspaces with full legacy matrices remain accepted.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.MeanFieldWorkspace"},{"category":"type","location":"api/dynamics/#PermutationalInvariantDynamics.MeanFieldResult","page":"Dynamics and evolution","text":"Saved fixed-step product-closure trajectory.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.MeanFieldResult"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.meanfield_rhs!","page":"Dynamics and evolution","text":"Apply the prepared product-state mean-field vector field without allocating scratch.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.meanfield_rhs!"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.meanfield_rhs","page":"Dynamics and evolution","text":"Allocate and return the one-particle mean-field derivative.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.meanfield_rhs"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.meanfield_problem","page":"Dynamics and evolution","text":"Construct an in-place SciML ODEProblem for a prepared mean-field plan.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.meanfield_problem"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.meanfield_evolve!","page":"Dynamics and evolution","text":"Propagate a one-particle density matrix with a preallocated three-scratch fixed-step RK4 kernel.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.meanfield_evolve!"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.solve_meanfield","page":"Dynamics and evolution","text":"Solve mean-field dynamics and retain matrices at the requested sampling times.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.solve_meanfield"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.meanfield_jacobian","page":"Dynamics and evolution","text":"Real Jacobian on the traceless-Hermitian one-particle tangent space.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.meanfield_jacobian"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.meanfield_stability","page":"Dynamics and evolution","text":"Linear-stability data for a candidate mean-field stationary state.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.meanfield_stability"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.meanfield_stationary_state","page":"Dynamics and evolution","text":"Find the stationary state in the basin of sigma0 by fixed-step relaxation.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.meanfield_stationary_state"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.meanfield_expectation","page":"Dynamics and evolution","text":"Product-state expectation tr(sigma*X) of a one-particle observable.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.meanfield_expectation"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.meanfield_collective_moments","page":"Dynamics and evolution","text":"Mean, second moment, and variance of sum_i X_i in a product state.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.meanfield_collective_moments"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.meanfield_pbody_expectation","page":"Dynamics and evolution","text":"Expectation of the unordered subset sum of a symmetric p-body observable.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.meanfield_pbody_expectation"},{"category":"type","location":"api/dynamics/#PermutationalInvariantDynamics.FloquetMap","page":"Dynamics and evolution","text":"FloquetMap\n\nReusable matrix-free one-period map.  Construct it with floquet_map or FloquetMap(source, period; ...).  The map stores immutable integration controls and a synchronized compatibility workspace; hot or parallel code should call apply! or apply_adjoint! with one explicit FloquetWorkspace per task.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.FloquetMap"},{"category":"type","location":"api/dynamics/#PermutationalInvariantDynamics.FloquetWorkspace","page":"Dynamics and evolution","text":"FloquetWorkspace(map)\n\nCaller-owned scratch for one-period applications of a FloquetMap. One workspace may be reused sequentially for forward and adjoint actions, but must not be shared by concurrent tasks.  The adjoint action reverses the actual discrete RK4 graph, so it is the numerical adjoint of the finite-step map rather than a separate integration of the continuous adjoint equation.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.FloquetWorkspace"},{"category":"type","location":"api/dynamics/#PermutationalInvariantDynamics.FloquetBatchWorkspace","page":"Dynamics and evolution","text":"FloquetBatchWorkspace(map, capacity; mode=:forward,\n                      memory_budget=512*1024^2)\n\nFixed-capacity caller-owned scratch for applying a FloquetMap to several vectors at once. mode=:forward stores only three n-by-capacity arrays and uses a low-storage classical RK4 schedule. mode=:full stores the four stage adjoints required by the exact reverse graph and additionally supports apply_adjoint!. Adjoint application through a forward-only workspace raises; it is never approximated or silently disabled.\n\nThe capacity is immutable. Calls with more columns raise before propagation instead of growing hidden storage. A prepared PI source evaluates every driven schedule once per RK stage and applies its sectorwise matrix–matrix kernels to the whole block. A custom MatrixFreeLiouvillian uses its batched callback when present and otherwise retains the established safe column fallback.\n\nBecause the caller's destination is also the low-storage RK4 accumulator, its element type must exactly match the map element type. Inputs may be narrower when they are representable and are copied into the map-precision current buffer before propagation.\n\nOne workspace may be reused sequentially, never concurrently. Construction checks the predictable stage and source-action storage against memory_budget; memory_budget=Inf is the explicit opt-out.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.FloquetBatchWorkspace"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.floquet_map","page":"Dynamics and evolution","text":"floquet_map(source, period; steps=256, t0=0, parameters=nothing,\n            basis=nothing, trace_vector=nothing,\n            memory_budget=512*1024^2)\n\nPrepare a matrix-free one-period propagator without constructing an n_PI-by-n_PI matrix.  One application integrates a single PI coefficient vector with the same fixed-step RK4 rule as floquet_propagator. basis or trace_vector supplies the physical trace functional for a raw operator; PI models and compiled PI sources provide it automatically.\n\nThe returned map is a fixed linear operator even when its underlying Liouvillian is periodically time dependent: t0, period, parameters, and the RK grid are part of the prepared object.  Converge steps independently of Krylov spectral or fixed-point tolerances.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.floquet_map"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.restricted_floquet_map","page":"Dynamics and evolution","text":"restricted_floquet_map(map, restriction; atol=0, rtol=nothing)\n\nExhaustively certify and prepare a symmetry-coordinate restriction of a matrix-free period map.  The returned RestrictedLiouvillian uses compressed solver vectors and an embedded caller-owned Floquet action; it does not materialize the one-period matrix.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.restricted_floquet_map"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.selected_floquet_multipliers","page":"Dynamics and evolution","text":"selected_floquet_multipliers(map; nev=min(6, size(map,1)),\n    method=:iram, which=:LM, vectors=false, block_size=min(nev,4),\n    operator_workspace=nothing, kwargs...)\n\nCompute selected Floquet multipliers without constructing the one-period matrix. method=:arnoldi uses ordinary Arnoldi, :iram uses exact-shift implicitly restarted Arnoldi, and :jd uses Jacobi–Davidson near target (one by default). method=:block_arnoldi uses the explicitly named thick-restarted block method. For a FloquetMap, it automatically prepares a forward-only FloquetBatchWorkspace of block_size; pass a compatible operator_workspace to reuse that bounded period-action storage.\n\nThe returned named tuple always contains Ritz residuals, converged, the selected values, the full operator dimension, and partial_scope; request vectors=true to retain right Floquet modes.\n\nLargest-modulus Arnoldi/IRAM selections are appropriate for slow decays. Jacobi–Davidson is a near-target calculation and is never labeled a global spectral-radius search. Converge the period map's RK steps separately from the eigensolver tolerances.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.selected_floquet_multipliers"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.floquet_propagator","page":"Dynamics and evolution","text":"floquet_propagator(model_or_L, period; steps=256, t0=0,\n                   parameters=nothing, memory_budget=512*1024^2)\n\nIntegrate one period of a time-dependent PI Liouvillian with a fixed-step RK4 scheme. All stage matrices and matrix-free sector workspaces are preallocated. Fixed operator terms with time-dependent scalar rate=(t,p)->... never assemble an instantaneous sparse Liouvillian. An InPlaceTimeOperator is evaluated once per RK stage and its dynamic blocks are reused for every propagator column.\n\nThis convenience necessarily retains several dense n_PI-by-n_PI RK arrays. Their conservative peak estimate is checked before allocation. For large problems use floquet_map, which applies one period to selected vectors matrix free. memory_budget=Inf is an explicit opt-in to the dense route.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.floquet_propagator"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.floquet_multipliers","page":"Dynamics and evolution","text":"Floquet multipliers, sorted by decreasing modulus for an explicit map.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.floquet_multipliers"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.floquet_exponents","page":"Dynamics and evolution","text":"Principal-branch Floquet exponents log(lambda)/period.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.floquet_exponents"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.floquet_gap","page":"Dynamics and evolution","text":"floquet_gap(F, period; atol=1e-10)\n\nReturn the nonnegative asymptotic decay rate from the subleading Floquet multiplier. The map must have a fixed multiplier within atol of one and no remaining multiplier with modulus larger than 1 + atol; otherwise an ArgumentError is thrown. A one-dimensional map has no subleading mode and returns a precision-matched NaN.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.floquet_gap"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.floquet_steady_state","page":"Dynamics and evolution","text":"floquet_steady_state(map::FloquetMap; method=:krylov,\n                     return_info=false, ...)\n\nSolve the periodic fixed-point equation (F-I)rho=0 with the physical trace constraint. method=:krylov (the default) uses restarted matrix-free GMRES and never constructs the period map. method=:dense materializes the map and retains the legacy SVD route for manageable PI dimensions.\n\nAn explicitly restricted Floquet map is also accepted. Its result is embedded back into the ambient PIState, and return_info=true reports the full-space period residual and restriction leakage. map_workspace owns one-period application scratch; workspace is the independent GMRES workspace. For the Krylov route both are included in the memory_budget preflight. The check uses the actual capacity and scalar type of supplied reusable workspaces.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.floquet_steady_state"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.stroboscopic_evolution","page":"Dynamics and evolution","text":"Return PI states after 0:nperiods applications of a Floquet propagator.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.stroboscopic_evolution"},{"category":"function","location":"api/dynamics/#PermutationalInvariantDynamics.floquet_evolve","page":"Dynamics and evolution","text":"State after an integer number of periods.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.floquet_evolve"},{"category":"section","location":"api/representation/#Representations,-states,-and-models","page":"Representations, states, and models","text":"","title":"Representations, states, and models"},{"category":"section","location":"api/representation/#Partitions,-exact-combinatorics,-and-representation-theory","page":"Representations, states, and models","text":"exact_binomial and exact_multinomial return BigInt values and are the public route when a machine integer could overflow.  They do not choose a floating approximation or a logarithmic representation.  PI construction routines keep related ratios exact through cancellation and apply a binary-scaled square root when needed; if the final nonzero factor is not representable in the selected scalar type, the operation raises and the caller must select a wider type.","title":"Partitions, exact combinatorics, and representation theory"},{"category":"section","location":"api/representation/#PI-bases,-states,-and-one-body-operators","page":"Representations, states, and models","text":"","title":"PI bases, states, and one-body operators"},{"category":"section","location":"api/representation/#Appendix-D-processes","page":"Representations, states, and models","text":"","title":"Appendix-D processes"},{"category":"section","location":"api/representation/#Supersites-and-pseudomode-specifications","page":"Representations, states, and models","text":"PISupersite groups the physical system and its local auxiliaries before applying permutation symmetry. It is not a tensor product of global PI operator spaces. The complete workflow, factor-ordering convention, cutoff checks, and matrix-free examples are given in Local pseudomodes and PI supersites. One mode shared by the complete ensemble instead uses a finite composite factor; see Global pseudomodes and shared cavities.","title":"Supersites and pseudomode specifications"},{"category":"section","location":"api/representation/#Physical-terms-and-models","page":"Representations, states, and models","text":"","title":"Physical terms and models"},{"category":"section","location":"api/representation/#Vectorized-superoperators-and-PI-tests","page":"Representations, states, and models","text":"","title":"Vectorized superoperators and PI tests"},{"category":"type","location":"api/representation/#PermutationalInvariantDynamics.Partition","page":"Representations, states, and models","text":"A partition padded to exactly D nonnegative parts.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.Partition"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.partitions","page":"Representations, states, and models","text":"partitions(N, d)\n\nEnumerate the partitions of N into exactly d nonnegative, nonincreasing parts, padding with zeros. The result uses the package's descending lexicographic sector order.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.partitions"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.weight","page":"Representations, states, and models","text":"Return the number of boxes (the sum of all parts) of p.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.weight"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.length_nonzero","page":"Representations, states, and models","text":"Return the number of nonzero rows of the Young diagram p.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.length_nonzero"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.removable_corners","page":"Representations, states, and models","text":"removable_corners(p)\n\nReturn the one-based row indices from which one box can be removed while leaving a valid partition.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.removable_corners"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.addable_corners","page":"Representations, states, and models","text":"addable_corners(p)\n\nReturn the one-based row indices to which one box can be added while leaving a valid length-D partition.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.addable_corners"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.remove_corner","page":"Representations, states, and models","text":"remove_corner(p, row)\n\nRemove one box from the one-based row. Throw ArgumentError if that row is not a removable corner.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.remove_corner"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.add_corner","page":"Representations, states, and models","text":"add_corner(p, row)\n\nAdd one box to the one-based row. Throw ArgumentError if that row is not an addable corner.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.add_corner"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.minus_plus_neighbors","page":"Representations, states, and models","text":"minus_plus_neighbors(p)\n\nReturn, in sector order, the distinct partitions obtained by removing one box from p and adding one box back. These are the sectors coupled to p by a general local one-particle process.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.minus_plus_neighbors"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.reachable_sectors","page":"Representations, states, and models","text":"reachable_sectors(p, n)\n\nReturn the sectors reachable from p after exactly n successive remove-then-add neighbor steps. The returned partitions remain in descending lexicographic sector order.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.reachable_sectors"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.symmetric_group_dimension","page":"Representations, states, and models","text":"symmetric_group_dimension(p)\n\nReturn the exact BigInt dimension f^p of the symmetric-group irrep with Young shape p, evaluated with the hook-length formula.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.symmetric_group_dimension"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.unitary_group_dimension","page":"Representations, states, and models","text":"unitary_group_dimension(p)\n\nReturn the exact BigInt dimension of the U(D) irrep labeled by the length-D partition p, evaluated with the Weyl dimension formula.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.unitary_group_dimension"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.commutant_dimension","page":"Representations, states, and models","text":"commutant_dimension(N, d)\n\nReturn the exact number of PI operator coordinates for N identical d-level systems, binomial(N+d^2-1N). This is the dimension of a complete PIBasis, not the Hilbert-space dimension.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.commutant_dimension"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.exact_binomial","page":"Representations, states, and models","text":"exact_binomial(n, k)\n\nReturn binomial(n,k) as an exact BigInt, without machine-integer overflow. n must be nonnegative; an out-of-range k returns zero, following Julia's binomial convention.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.exact_binomial"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.exact_multinomial","page":"Representations, states, and models","text":"exact_multinomial(counts)\n\nReturn (sum_i n_i)prod_i n_i as an exact BigInt. Sequential exact binomials are used, so no factorial intermediate exceeds the final multinomial coefficient. Numerical PI kernels combine or scale such factors before floating conversion rather than converting this potentially enormous integer directly.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.exact_multinomial"},{"category":"type","location":"api/representation/#PermutationalInvariantDynamics.GTPattern","page":"Representations, states, and models","text":"Immutable Gelfand–Tsetlin pattern, stored row-by-row for rows 1 through D.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.GTPattern"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.gt_patterns","page":"Representations, states, and models","text":"gt_patterns(p)\n\nEnumerate all Gelfand–Tsetlin patterns with top row p. Patterns are sorted by the ascending lexicographic order of their stored entry tuples, which is the ordering used inside every PI Schur block.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.gt_patterns"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.isvalid","page":"Representations, states, and models","text":"isvalid(g)\n\nReturn whether all entries of g are nonnegative and satisfy the GT interlacing inequalities.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.isvalid"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.shape","page":"Representations, states, and models","text":"Return the top-row partition labeling the irrep of g.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.shape"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.content","page":"Representations, states, and models","text":"content(g)\n\nReturn the length-D weight-content tuple of g, obtained from successive row-sum differences. Its entries give the local-label occupations and sum to weight(shape(g)).\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.content"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.gt_entry","page":"Representations, states, and models","text":"gt_entry(g, i, k)\n\nReturn entry i of row k of the Gelfand–Tsetlin pattern g. Both indices are one-based; row k contains k entries.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.gt_entry"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.triangular_shift","page":"Representations, states, and models","text":"Return shift row positions (tau_i,...,tau_D), or nothing if selection fails.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.triangular_shift"},{"category":"type","location":"api/representation/#PermutationalInvariantDynamics.OneBoxCGCache","page":"Representations, states, and models","text":"OneBoxCGCache(basis; max_depth=min(1, basis.N), T=Float64,\n              memory_budget=64 * 1024^2)\n\nPrecompute the nonzero one-box Gelfand–Tsetlin Clebsch–Gordan transitions reachable from the sectors of basis through at most max_depth successive box removals.  Compatible parent candidates are selected by their content; the cache retains packed sparse transition tuples, never dense Cartesian tables of structural zeros.\n\nThe cache is immutable in use, belongs to the exact basis object supplied at construction, and may be shared by read-only geometry constructors.  Its floating type is fixed by T.  A BigFloat cache also records the active precision and rounding mode and refuses reuse under different arithmetic settings, preventing an apparently high-precision computation from consuming incompatibly rounded stored coefficients.  max_depth=0 is valid and stores no transitions.\n\nConstruction is guarded by memory_budget (64 MiB by default); pass Inf to opt out explicitly.  candidate_count, coefficient_count, and estimated_bytes report respectively the compatible candidates evaluated, the retained nonzeros, and a conservative retained-storage estimate.  No global mutable cache is used.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.OneBoxCGCache"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.cgc","page":"Representations, states, and models","text":"cgc(mu, j, lam; T=nothing, cache=nothing)\n\nReturn the real GT Clebsch–Gordan coefficient of equations (B.10)–(B.11). Without a cache, omitting T preserves the historical Float64 result.  A OneBoxCGCache supplies precomputed one-box coefficients and, when T is omitted, selects its stored floating-point type.  An explicit T must match the cache type; cached requests outside its exact basis-owned domain or its configured depth raise instead of silently falling back to recomputation.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.cgc"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.partition_triangle","page":"Representations, states, and models","text":"partition_triangle(l, m, r)\n\nReturn whether l and r are both obtained from m by adding one box. This is the partition selection rule for a one-body three-sector contraction.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.partition_triangle"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.three_nu_symbol","page":"Representations, states, and models","text":"three_nu_symbol(WL, Wm, WR; T=nothing, cache=nothing)\n\nReturn the D x D dyadic of real one-box U(D) Clebsch–Gordan coefficients associated with the patterns Wm -> WL and Wm -> WR. The local labels use the paper's zero-based convention internally, and the returned matrix uses ordinary one-based Julia indices.\n\nWith cache=OneBoxCGCache(...), both coefficient vectors are read from the precomputed sparse one-box transitions.  The returned matrix always owns detached storage and may therefore be modified without changing the cache.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.three_nu_symbol"},{"category":"type","location":"api/representation/#PermutationalInvariantDynamics.PIBasis","page":"Representations, states, and models","text":"PIBasis(N, d; sectors=nothing)\n\nConstruct the equation-(7) PI operator basis for N identical d-level systems. By default all partitions of N into d padded parts are retained; sectors may select a restricted set. Sectors use descending lexicographic order, GT patterns use ascending stored-entry order, and each sector matrix is flattened in Julia column-major order. length(basis) is the retained PI coordinate dimension sum_nu dim(U_nu)^2.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.PIBasis"},{"category":"type","location":"api/representation/#PermutationalInvariantDynamics.PIOperator","page":"Representations, states, and models","text":"PIOperator(basis, data)\nPIOperator(basis; T=Float64)\n\nAn operator in orthonormal PI coefficient coordinates. data is partitioned into column-major stored blocks C_nu; use physical_block for the corresponding Schur block C_nusqrt(f^nu). The zero constructor selects the real scalar type with T.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.PIOperator"},{"category":"type","location":"api/representation/#PermutationalInvariantDynamics.PIState","page":"Representations, states, and models","text":"PIState(basis, data)\nPIState(basis; T=Float64)\n\nA density-state container in the same PI coefficient convention as PIOperator. Construction checks only basis-coordinate dimensions; it does not normalize, symmetrize, or impose positivity. Use validate_state when physical-state validation is required.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.PIState"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.coefficient_block","page":"Representations, states, and models","text":"coefficient_block(A, partition)\n\nReturn a mutable matrix view of the stored equation-(7) coefficient block C_nu. Mutating the view mutates A.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.coefficient_block"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.physical_block","page":"Representations, states, and models","text":"physical_block(A, partition)\n\nReturn the physical Schur block C_nusqrt(f^nu) corresponding to the stored coefficient block of A.  The returned matrix is detached from A; the exact multiplicity is square-rooted with binary scaling, and an unrepresentable final scale raises rather than producing a nonfinite value.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.physical_block"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.each_schur_block","page":"Representations, states, and models","text":"each_schur_block(A; representation=:physical)\n\nIterate lazily over partition => block pairs in the retained sector order of A.basis.  With the default representation=:physical, each returned block is detached storage for the physical Schur matrix C_nusqrt(f^nu); mutating it does not modify A.  With representation=:coefficient, each block is the mutable stored coefficient_block view, so mutating it does modify A.\n\nThe iterator can be passed directly to operator_from_schur_blocks or state_from_schur_blocks for a round trip, using the same representation keyword on both sides.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.each_schur_block"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.operator_from_schur_blocks","page":"Representations, states, and models","text":"operator_from_schur_blocks(basis, blocks; representation=:physical,\n                           T=nothing)\n\nConstruct a PIOperator from an iterable of partition => matrix pairs. Input order is irrelevant and unspecified retained sectors are exactly zero. By default each matrix is interpreted as a physical Schur block and is stored as sqrt(f^nu) times that matrix.  With representation=:coefficient, the matrices are interpreted directly as stored equation-(7) coefficient blocks.\n\nEvery label must belong to basis, duplicate labels and wrong block sizes raise, and input matrices are copied rather than aliased.  The output scalar type is the promotion of the input blocks' real floating component types; an empty collection produces a zero ComplexF64 operator. Pass an AbstractFloat type as T to select the output type explicitly; conversion that would narrow any supplied block raises. Tuple and integer-vector partition labels are accepted as conveniences. No full d^N operator is constructed.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.operator_from_schur_blocks"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.state_from_schur_blocks","page":"Representations, states, and models","text":"state_from_schur_blocks(basis, blocks; representation=:physical,\n                        T=nothing, validate=false,\n                        validation_keywords...)\n\nConstruct a PIState from an iterable of partition => matrix pairs, using the copying, sector-membership, dimension, representation, and scalar- promotion rules of operator_from_schur_blocks.  Unspecified retained sectors are exactly zero.  The function never normalizes, symmetrizes, clips, or otherwise repairs its input.\n\nWith validate=true, validate_state is called on the completed state and remaining keywords such as atol, rtol, trace_one, or positivity_method are forwarded to it.  Validation keywords supplied while validate=false raise instead of being silently ignored.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.state_from_schur_blocks"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.sector_metadata","page":"Representations, states, and models","text":"sector_metadata(basis)\n\nReturn one named tuple per retained Schur sector, in basis order.  Each entry contains the one-based sector index, partition, block_dimension dim(U_nu), coordinate_dimension, flattened coordinate_range, exact BigInt symmetric-group multiplicity f^nu, and exact retained hilbert_dimension = f^nu dim(U_nu).  For qubits,spinis the exact total-spin labelj=(nu_1-nu_2)/2; it ismissing` for other local dimensions.\n\nNo full d^N object is constructed.  For a complete basis, summing the hilbert_dimension fields gives exactly d^N; for a restricted basis it gives the retained Hilbert-space dimension.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.sector_metadata"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.sector_view","page":"Representations, states, and models","text":"Alias for coefficient_block.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.sector_view"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.identity_operator","page":"Representations, states, and models","text":"identity_operator(basis; T=Float64)\n\nConstruct the identity on the Hilbert space represented by basis. Its stored block in sector nu is sqrt(f^nu) I.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.identity_operator"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.maximally_mixed_state","page":"Representations, states, and models","text":"maximally_mixed_state(basis; T=Float64)\n\nConstruct the normalized maximally mixed state on the retained Hilbert-space sectors. For a complete basis this is Id^N.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.maximally_mixed_state"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.trace","page":"Representations, states, and models","text":"trace(A)\n\nReturn the physical trace sum_nu sqrt(f^nu) tr(C_nu) from the stored PI coefficient blocks.\n\n\n\n\n\nReturn the physical trace of a composite state or operator.\n\nThe implementation contracts only joint diagonal coordinates and fuses the exact product of Schur multiplicities with each sector-tuple trace.  It does not allocate a full composite trace vector.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.trace"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.purity","page":"Representations, states, and models","text":"Return the density-operator purity tr(rho^2) in orthonormal PI coordinates.\n\n\n\n\n\nReturn tr(rho^2) from the orthonormal composite coordinates.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.purity"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.normalize!","page":"Representations, states, and models","text":"normalize!(rho)\n\nDivide the stored coordinates of rho by its physical trace and return rho. A zero trace raises; no Hermiticity or positivity repair is performed.\n\n\n\n\n\nNormalize a composite state by its physical trace, without other repair.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.normalize!"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.ispositive","page":"Representations, states, and models","text":"ispositive(A; atol=_analysis_atol(A), rtol=0,\n           method=:auto, dense_threshold=256)\n\nReturn whether every physical Schur block is Hermitian and positive semidefinite within tolerance. See positivity_diagnostics for the eigenvalue and scalable shifted-Cholesky backends and their detailed report.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.ispositive"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.isphysical","page":"Representations, states, and models","text":"isphysical(rho; atol=_analysis_atol(rho), rtol=_state_rtol(rho))\n\nReturn whether rho is trace one, Hermitian, and positive within the requested tolerances. The input is never modified.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.isphysical"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.positivity_diagnostics","page":"Representations, states, and models","text":"positivity_diagnostics(A; method=:auto, dense_threshold=256,\n                       atol=_analysis_atol(A), rtol=0)\n\nCheck positivity sector by sector without constructing a full d^N matrix. :eigen returns the exact numerical minimum of every stored Schur block. :cholesky instead factorizes each block after the requested tolerance shift; it avoids a full eigenspectrum for accepted states and falls back to eigmin only when a factorization fails on LAPACK-supported scalar types. Other scalar types use a pivoted PSD decision without narrowing precision. :auto selects the Cholesky path when the largest irrep block exceeds dense_threshold or a Hermitian eigensolver is unavailable.\n\nOn the Cholesky path minimum_eigenvalue is missing, because a successful factorization is a lower-bound certificate rather than an eigenvalue calculation. For LAPACK-supported scalar types, a negative direction stores the failed block's numerical minimum in witness_eigenvalue; a generic pivoted-PSD failure is reported with a missing numerical witness. The input is never modified.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.positivity_diagnostics"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.state_diagnostics","page":"Representations, states, and models","text":"state_diagnostics(rho; atol=_analysis_atol(rho), rtol=_state_rtol(rho),\n                  positivity_method=:auto, dense_threshold=256)\n\nReturn trace, Hermiticity, and positivity diagnostics for rho.  Invalid inputs are reported but never normalized, symmetrized, or eigenvalue-clipped. The minimum eigenvalue is missing when the state is not Hermitian within the requested tolerance, because positivity is then not mathematically defined, or when the scalable Cholesky certificate succeeds without diagonalization. In the latter case positivity_method and positivity_witness distinguish the certificate from a failed spectral check.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.state_diagnostics"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.validate_state","page":"Representations, states, and models","text":"validate_state(rho; trace_one=true, hermitian=true, positive=true,\n               atol=_analysis_atol(rho), rtol=_state_rtol(rho),\n               positivity_method=:auto, dense_threshold=256)\n\nValidate selected density-state conditions and return rho.  A failed check throws ArgumentError; this function never silently repairs its argument. Set individual Boolean keywords to false only when a subnormalized or operator-like PIState is intentionally being analysed.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.validate_state"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.sector_population","page":"Representations, states, and models","text":"sector_population(A, partition)\n\nReturn sqrt(f^nu) tr(C_nu), the physical trace contribution of one Schur sector. For a valid state this is its sector probability.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.sector_population"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.sector_populations","page":"Representations, states, and models","text":"Return a dictionary of sector_population values for all retained sectors.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.sector_populations"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.basis_state","page":"Representations, states, and models","text":"basis_state(basis, partition, W; T=Float64)\n\nConstruct the trace-one PI state supported on GT pattern W in partition, uniformly over the sector's symmetric-group multiplicity copies.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.basis_state"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.sector_density_matrix","page":"Representations, states, and models","text":"sector_density_matrix(basis, partition, B)\n\nEmbed B as the physical Schur block of partition, with every other sector zero. The stored block is sqrt(f^nu) B; the caller is responsible for the desired global trace and positivity.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.sector_density_matrix"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.iid_pure_state","page":"Representations, states, and models","text":"iid_pure_state(basis, psi; atol=0, rtol=sqrt(eps(...)))\n\nConstruct the product state psipsi^otimes N directly in the fully symmetric Schur sector. psi must be normalized and that sector must be present in basis; no full d^N vector is formed. Occupation amplitudes are evaluated by normalized conditional-binomial recurrences, so large binomial or multinomial coefficients are never converted to the state scalar type.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.iid_pure_state"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.iid_state","page":"Representations, states, and models","text":"iid_state(basis, rho; atol=0, rtol=sqrt(eps(...)))\n\nConstruct the PI representation of rho⊗basis.N for a normalized one-particle density matrix. Each physical Schur block is evaluated by the one-box Clebsch–Gordan recurrence\n\nD_lambda(rho) = C' * (rho ⊗ D_mu(rho)) * C,\n\nwhere mu is any partition obtained by removing one corner from lambda. The recurrence is polynomial in N at fixed local dimension and only forms sector-sized matrices. It therefore also supports rank-deficient (including pure) density matrices without a matrix logarithm, an eigendecomposition, or a numerical regularizer.\n\nAll Schur sectors carrying nonzero weight must be present in basis; an incomplete restricted basis is rejected instead of silently projecting and renormalizing the product state. The input scalar precision is retained, and the input is validated without normalization or eigenvalue clipping.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.iid_state"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.thermal_state","page":"Representations, states, and models","text":"Construct exp(-beta*H)/Z, rejecting a non-Hermitian PI Hamiltonian.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.thermal_state"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.computational_product_state","page":"Representations, states, and models","text":"computational_product_state(basis, level; T=Float64)\n\nConstruct the tensor-power state levellevel^otimes N directly in the fully symmetric Schur sector. level is a one-based Julia local-state index in 1:basis.d; no full d^N vector is formed.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.computational_product_state"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.dicke_state","page":"Representations, states, and models","text":"dicke_state(basis, j, m; T=Float64)\n\nConstruct the normalized qubit PI state with total-spin labels (j,m).  With the package ordering (|g>,|e>), m=(n_e-n_g)2 and the corresponding partition is (N2+jN2-j).\n\nFor a sector of symmetric-group multiplicity f, this state is uniform over the f indistinguishable multiplicity copies. Consequently its full Hilbert-space purity is 1/f; it is pure only when f == 1. The requested sector must be present in basis.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.dicke_state"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.dicke_operator","page":"Representations, states, and models","text":"dicke_operator(basis, j, m, mp; T=Float64)\n\nConstruct the qubit PI operator corresponding to jmjmp, uniformly over the symmetric-group multiplicity copies of its Schur sector. If that multiplicity is f, the selected physical Schur-block entry is 1/f and the stored equation-(7) coefficient is 1/sqrt(f). Consequently, the diagonal case has the same coefficient data as dicke_state.\n\nBoth magnetic labels must belong to the requested j ladder, and its sector must be present in basis.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.dicke_operator"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.ghz_state","page":"Representations, states, and models","text":"ghz_state(basis; phase=0, T=nothing)\n\nConstruct the qubit GHZ state (g^otimes N + exp(im*phase)e^otimes N)sqrt(2) directly in the fully symmetric Schur sector. basis must contain that sector and have N >= 1; no full 2^N vector is formed. The scalar type is inferred from a floating phase (or defaults to Float64 for the exact default phase); pass T explicitly to select a non-narrowing output type.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.ghz_state"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.spin_coherent_state","page":"Representations, states, and models","text":"spin_coherent_state(basis, theta, phi=0; T=nothing)\n\nConstruct a qubit coherent-spin product state directly in the fully symmetric Schur sector. In the local ordering (|g>,|e>), the tensor-power factor is\n\nexp(im*phi) sin(theta2)g + cos(theta2)e.\n\nThus the collective-spin mean points along (sin(theta)cos(phi) sin(theta)sin(phi) cos(theta)). Angles are used as supplied and are not wrapped into a principal interval. The output type is inferred from floating angle inputs; an explicit T must represent both angles without narrowing.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.spin_coherent_state"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.spin_matrices","page":"Representations, states, and models","text":"spin_matrices(d=2; T=Float64)\n\nReturn the standard spin-j matrices with j=(d-1)/2 and hbar=1 in the ascending magnetic ordering -jj.  The result is the named tuple (j, jx, jy, jz, jp, jm), where jp and jm are the raising and lowering operators.  In particular, for d == 2, jx, jy, and jz are Pauli matrices divided by two, while jm = |g><e| in the package's local ordering (|g>, |e>).\n\nT selects the real floating-point type retained by all returned matrices.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.spin_matrices"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.collective_spin","page":"Representations, states, and models","text":"collective_spin(basis, component; cache=nothing)\n\nConstruct one collective spin operator J_alpha=sum_i j_alpha^(i) without forming the full computational-space matrix. component may be :x, :y, :z, :plus, or :minus. The one-particle matrices use the ascending -jj convention of spin_matrices, with j=(basis.d-1)/2.\n\nThe sole fully symmetric sector uses a lightweight occupation-number lift. Pass a shared OneBodyGeometry when constructing several components on a general Schur basis.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.collective_spin"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.collective_block","page":"Representations, states, and models","text":"collective_block(basis, X, partition; cache=nothing)\n\nReturn the physical Schur block in partition of the collective one-body operator sum_i X_i. A sole fully symmetric sector uses a lightweight occupation-number lift. General Schur sectors use OneBodyGeometry; pass a shared cache when evaluating several blocks or operators on the same exact basis.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.collective_block"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.collective_operator","page":"Representations, states, and models","text":"collective_operator(basis, X; cache=nothing)\n\nConstruct the PI operator representing sum_i X_i without forming a d^N computational-space matrix. A sole fully symmetric sector uses a lightweight occupation-number lift; otherwise pass a shared OneBodyGeometry to amortize general Schur recoupling setup.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.collective_operator"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.mean_local_operator","page":"Representations, states, and models","text":"mean_local_operator(basis, X; cache=nothing)\n\nConstruct the particle-averaged one-body observable (1N) sum_i X_i. Its equation-(7) coefficients are scaled directly by sqrt(f^nu)N; the extensive stored collective operator is never formed. The sole fully symmetric sector uses the occupation-number lift.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.mean_local_operator"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.local_kernel_element","page":"Representations, states, and models","text":"local_kernel_element(cache, X, Y, l, a, b, n, c, d)\n\nReturn the PI-coordinate matrix element from input block coordinate (ncd) to output coordinate (lab) of the local map rho - sum_i X_i rho Y_i^dagger. Schur-block indices are one-based.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.local_kernel_element"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.local_kernel_operator","page":"Representations, states, and models","text":"local_kernel_operator(basis, X, Y; representation=:sparse,\n                      cache=OneBodyGeometry(basis))\n\nConstruct the sparse PI-coordinate matrix of the local map rho - sum_i X_i rho Y_i^dagger. Reusing cache avoids rebuilding the one-body CG geometry. The representation keyword is retained for API compatibility; the current result is sparse.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.local_kernel_operator"},{"category":"type","location":"api/representation/#PermutationalInvariantDynamics.OneBodyGeometry","page":"Representations, states, and models","text":"OneBodyGeometry(basis; T=Float64, coefficient_cache=nothing)\n\nPrecompute the one-box Clebsch–Gordan contractions, multiplicity scales, and sector connections shared by local and collective one-particle operations. Contraction tables retain one contiguous tuple array and column-major offsets; logical empty cells and absent sector pairs therefore require no individual heap vectors or dictionary entries. The cache is read-only after construction and is tied to the exact PIBasis object supplied at construction; it may be reused to prepare many operators. Pass a basis-owned OneBoxCGCache with max_depth >= 1 for N > 0 through coefficient_cache to reuse precomputed CG coefficients across several geometry constructions. A zero-particle basis requires depth zero. The default preserves call-local construction and does not create global state.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.OneBodyGeometry"},{"category":"type","location":"api/representation/#PermutationalInvariantDynamics.PBodyGeometry","page":"Representations, states, and models","text":"PBodyGeometry(basis, p; T=Float64, coefficient_cache=nothing)\n\nCache Appendix-D removal paths, exact path weights, and successive one-box CG isometries for symmetric p-body processes. The retained path isometries store only exact nonzero support in CSC form over the combined (centre pattern, local word) column. Pass a basis-owned OneBoxCGCache whose max_depth >= p through coefficient_cache to reuse precomputed sparse coefficients. Without it, construction still uses content-indexed compatible candidates and shares each one-box edge within the geometry; it never evaluates the dense Cartesian set of structural zeros.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.PBodyGeometry"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.pbody_collective_block","page":"Representations, states, and models","text":"Appendix-D collective sum sum_{n1<...<np} Xp^(n1,...,np) in one Schur block.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.pbody_collective_block"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.pbody_collective_operator","page":"Representations, states, and models","text":"Construct the collective sum of a permutation-symmetric p-particle operator.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.pbody_collective_operator"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.pbody_kernel_element","page":"Representations, states, and models","text":"pbody_kernel_element(cache, X, Y, l, a, b, n, c, d)\n\nReturn one PI-coordinate matrix element of the Appendix-D local p-body map rho - sum_S X_S rho Y_S^dagger, where S runs over unordered p-particle subsets. The input coordinate is (ncd) and the output coordinate is (lab). Both X and Y must be permutation invariant on their p tensor factors.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.pbody_kernel_element"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.pbody_kernel_operator","page":"Representations, states, and models","text":"Sparse PI-coordinate representation of the Appendix-D superoperator K_X,Y.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.pbody_kernel_operator"},{"category":"type","location":"api/representation/#PermutationalInvariantDynamics.PISupersite","page":"Representations, states, and models","text":"PISupersite(N, factor_dimensions;\n            labels=nothing, specifications=nothing,\n            sectors=nothing, T=Float64,\n            memory_budget=512*1024^2)\nPISupersite(basis, factor_dimensions; kwargs...)\n\nDescribe one identical composite particle and construct its exact PI basis. factor_dimensions=(d1,d2,...) follows Julia's kron(factor1,factor2,...) ordering, so the last internal factor is fastest. The local dimension is D=prod(factor_dimensions) and the complete PI coordinate count is binomial(N+D^2-1,N), not D^(2N).\n\nlabels selects internal factors in lift_supersite_operator. specifications may retain immutable factor metadata; ordinary generic supersites use nothing. The default memory_budget checks at least the basis metadata, internal identities, and one complex PI coordinate vector before returning. Pass Inf only as an explicit opt-out.\n\nFor identical systems with local bosonic modes, prefer pseudomode_supersite, which installs a :system factor followed by one or more BosonicPseudomode specifications.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.PISupersite"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.supersite_tensor_operator","page":"Representations, states, and models","text":"supersite_tensor_operator(site, components...;\n                          memory_budget=512*1024^2)\n\nForm one local operator kron(components...) in the internal ordering recorded by site. Exactly one square component is required per factor. This allocates only the site.basis.d-dimensional local matrix; it never constructs an N-particle Hilbert-space operator.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.supersite_tensor_operator"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.lift_supersite_operator","page":"Representations, states, and models","text":"lift_supersite_operator(site, operator; factor,\n                        memory_budget=512*1024^2)\n\nLift a matrix acting on one internal factor to the whole local supersite, inserting identities on every other factor. factor is an integer or one of site.factor_labels.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.lift_supersite_operator"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.lift_system_operator","page":"Representations, states, and models","text":"lift_system_operator(site, operator; memory_budget=512*1024^2)\n\nLift a one-system operator to a pseudomode supersite. The system must be the factor labeled :system.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.lift_system_operator"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.lift_system_pbody_operator","page":"Representations, states, and models","text":"lift_system_pbody_operator(site, operator, p;\n                           memory_budget=512*1024^2)\n\nLift a system-only p-particle matrix to the paired supersite ordering by tensoring the identity on every particle's local auxiliaries. The input order is system_1 ⊗ ... ⊗ system_p; the output order is (system_1⊗aux_1) ⊗ ... ⊗ (system_p⊗aux_p).\n\nThe result is sparse and has exactly nnz(operator)*d_aux^p structural entries. This avoids the incorrect grouped ordering produced by kron(operator, I_aux^⊗p) and never constructs an N-particle operator.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.lift_system_pbody_operator"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.lift_system_term","page":"Representations, states, and models","text":"lift_system_term(site, term; memory_budget=512*1024^2)\n\nLift a fixed microscopic system term to a paired supersite while preserving its local/collective scope, body order, rate, and Hamiltonian hbar. One-body and symmetric p-body built-in terms are supported. Direct Schur-space terms and operator schedules are rejected because they do not define a fixed microscopic system matrix to lift.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.lift_system_term"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.supersite_iid_state","page":"Representations, states, and models","text":"supersite_iid_state(site, local_state;\n                    memory_budget=512*1024^2, kwargs...)\n\nConstruct N identical copies of an arbitrary local supersite ket or density matrix. Unlike supersite_product_state, local_state may already contain correlations or entanglement between the system and its local auxiliaries. The recurrence acts directly in PI coordinates.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.supersite_iid_state"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.supersite_product_state","page":"Representations, states, and models","text":"supersite_product_state(site, factor_states...;\n                        memory_budget=512*1024^2, kwargs...)\n\nConstruct the iid N-supersite product state from one local state per internal factor. If every factor is a ket, iid_pure_state is used; otherwise kets are converted to rank-one density matrices and iid_state is used. Only a local vector or density matrix is tensored—never a site.basis.d^N object.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.supersite_product_state"},{"category":"type","location":"api/representation/#PermutationalInvariantDynamics.BosonicPseudomode","page":"Representations, states, and models","text":"BosonicPseudomode(nmax;\n                  frequency=0, damping=0,\n                  thermal_occupation=0, label=:mode, T=nothing,\n                  memory_budget=512*1024^2)\n\nDescribe one identical truncated bosonic mode with occupations 0:nmax. The immutable specification retains its identity, annihilation, creation, number, parity, top-level projector, and vacuum ket. frequency is allowed to be signed for rotating-frame models; damping and thermal_occupation must be finite and nonnegative. All fixed mode matrices use exact sparse support, and memory_budget guards their aggregate retained storage plus sparse-assembly temporaries before allocation.\n\nThe package dissipator convention is D[a](rho)=a*rho*a' - {a'a,rho}/2, so damping=kappa gives an amplitude pole with real decay kappa/2.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.BosonicPseudomode"},{"category":"type","location":"api/representation/#PermutationalInvariantDynamics.PseudomodeCoupling","page":"Representations, states, and models","text":"PseudomodeCoupling(operator; mode=1, strength=1,\n                   counterrotating_strength=0,\n                   memory_budget=512*1024^2)\n\nSpecify a system–pseudomode interaction. The rotating-wave part is\n\ng * L⊗a' + conj(g) * L'⊗a,\n\nwith g=strength. The optional counter-rotating part is\n\nh * L⊗a + conj(h) * L'⊗a'.\n\nL may be non-Hermitian and g,h may be complex. The constructor copies the finite square system matrix. pseudomode_coupling_terms decomposes each complex coefficient into real scalar rates multiplying Hermitian local operators, making the result compatible with prepared PI model families.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.PseudomodeCoupling"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.pseudomode_supersite","page":"Representations, states, and models","text":"pseudomode_supersite(N, system_dimension, modes...;\n                     sectors=nothing, T=nothing,\n                     memory_budget=512*1024^2)\npseudomode_supersite(basis, system_dimension, modes...; kwargs...)\n\nConstruct one exact PI supersite containing a system_dimension-level system followed by one or more identical-per-site truncated pseudomodes. Modes are local to each system: the returned basis is PIBasis(N, system_dimension*prod(mode.levels)), which fully exploits permutation symmetry of the N system+mode tuples.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.pseudomode_supersite"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.lift_pseudomode_operator","page":"Representations, states, and models","text":"lift_pseudomode_operator(site, operator; mode=1,\n                         memory_budget=512*1024^2)\n\nLift an operator on one local truncated mode to the complete supersite. mode is a one-based mode number or its symbolic label.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.lift_pseudomode_operator"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.pseudomode_operators","page":"Representations, states, and models","text":"pseudomode_operators(site, mode=1; memory_budget=512*1024^2)\n\nReturn lifted supersite operators (annihilation, creation, number_operator, parity, top_projector) for one local pseudomode.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.pseudomode_operators"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.pseudomode_coupling_terms","page":"Representations, states, and models","text":"pseudomode_coupling_terms(site, coupling;\n                          retain_zero_components=false,\n                          memory_budget=512*1024^2)\n\nBuild the Hermitian local Hamiltonian terms for one PseudomodeCoupling. Fixed local tensor matrices are prepared once; the real and imaginary coupling strengths remain scalar term rates. Set retain_zero_components=true when a later compile_family specialization must vary a component whose prototype rate is zero.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.pseudomode_coupling_terms"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.pseudomode_damping_terms","page":"Representations, states, and models","text":"pseudomode_damping_terms(site, mode=1;\n                         retain_zero_terms=false,\n                         memory_budget=512*1024^2)\n\nReturn independent local loss and, when needed, thermal-gain terms for one pseudomode. Rates are kappa*(nbar+1) and kappa*nbar in the package's standard dissipator convention. A zero damping returns an empty tuple. retain_zero_terms=true keeps both loss and gain prototypes for a prepared scalar-rate family.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.pseudomode_damping_terms"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.pseudomode_model","page":"Representations, states, and models","text":"pseudomode_model(site, system_hamiltonian;\n                 couplings=(), system_terms=(), supersite_terms=(),\n                 system_rate=1, frequencies=nothing,\n                 dampings=nothing, thermal_occupations=nothing,\n                 retain_zero_terms=false,\n                 memory_budget=512*1024^2)\npseudomode_model(N, system_hamiltonian, modes;\n                 couplings=(), system_terms=(), supersite_terms=(),\n                 system_rate=1, frequencies=nothing,\n                 dampings=nothing, thermal_occupations=nothing,\n                 retain_zero_terms=false, sectors=nothing,\n                 memory_budget=512*1024^2)\n\nBuild a time-local PI model for N identical quantum systems, each coupled to the same finite collection of local truncated pseudomodes.\n\nThe returned named tuple contains the reusable PISupersite, exact PI basis, validated model, lifted system and mode operators, separated model term groups, the base one-supersite Hamiltonian, and cutoff metadata. system_terms are fixed built-in microscopic terms in the bare-system dimension and are lifted automatically; this includes uniform all-to-all PBodyHamiltonian interactions. supersite_terms are already in the combined local dimension.\n\nsite_hamiltonian (also returned as base_site_hamiltonian) combines only system_hamiltonian, mode frequencies, and pseudomode couplings. It cannot include genuinely collective or p-body system_terms; those remain in lifted_system_terms. Supplied supersite_terms are returned unchanged.\n\nLocal Hamiltonians, couplings, frequencies, and damping channels remain separate terms so compile, compile_family, and matrix-free solvers can reuse their exact Schur geometry. Prepare site once and call the first method throughout a scan to preserve exact basis identity. The N method is a convenience constructor. Independent local damping normally requires the complete Schur-sector basis; an incompatible sectors restriction is rejected by PIModel. Set retain_zero_terms=true before compile_family when a scan must vary rates that are zero in the prototype.\n\n\n\n\n\npseudomode_model(system_model::PIModel, mode::BosonicPseudomode; kwargs...)\n\nBuild the global/shared-mode workflow from an existing PI system model. The N, system_hamiltonian, mode signature remains local by default; pass topology=:global there or call global_pseudomode_model explicitly.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.pseudomode_model"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.independent_local_pseudomode_model","page":"Representations, states, and models","text":"independent_local_pseudomode_model(N, system_hamiltonian, modes;\n                                   kwargs...)\n\nGeneral multi-mode alias for pseudomode_model. The historical single-mode matrix signature remains available with keywords nmax, frequency, coupling_strength, and damping.\n\n\n\n\n\nindependent_local_pseudomode_model(N, system_hamiltonian, coupling;\n    nmax, frequency, coupling_strength, damping,\n    thermal_occupation=0, memory_budget=512*1024^2)\n\nBuild a finite-cutoff PI supersite model for N identical, independent local damped pseudomodes. Each supersite contains one d-level system and one oscillator truncated to occupations 0:nmax, in system ⊗ mode basis order. Its local Hamiltonian is\n\nH_mathrmsite=H_S+omega a^dagger a\n +gleft(L a^dagger+L^dagger aright)\n\nand its independent Lindblad rates are kappa(bar n+1)mathcal Da and kappabar nmathcal Da^dagger. At zero occupation this realizes the physical pole c=g^2 and nu=kappa2+iomega for channel L. Hermitian L=Q reduces to gQ(a+a^dagger). The returned named tuple contains basis, model, the lifted local operators, and immutable cutoff metadata so additional PI interactions can be built on the exact same basis. For several local modes, automatic lifting of system-only p-body terms, and prepared local-mode tracing, use the generalized pseudomode_model workflow.\n\nThis is a time-local pseudomode embedding, not a collective-coupling HEOMPlan. It is the safe existing-coordinate route for identical independent local environments, but only represents pole decompositions that admit such a positive damped-mode realization. The oscillator cutoff is an explicit approximation and must be converged in nmax.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.independent_local_pseudomode_model"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.pseudomode_product_state","page":"Representations, states, and models","text":"pseudomode_product_state(site, system_state;\n                         mode_states=nothing,\n                         memory_budget=512*1024^2, kwargs...)\n\nConstruct an iid state of identical system+pseudomode supersites. By default every mode starts in its vacuum ket. Supply one mode state for a single mode or a tuple of mode kets/density matrices for multiple modes.\n\n\n\n\n\npseudomode_product_state(model::GlobalPseudomodeModel, system_state;\n                         mode_state=nothing,\n                         memory_budget=512*1024^2)\n\nConstruct a factorized initial state for a shared-mode model. system_state may be a complete PIState, a one-particle ket, or a one-particle density matrix.  A one-particle input is lifted to an iid PI state.  The mode defaults to its vacuum. Inputs wider than the prepared model are rejected with rebuild guidance; narrower inputs are converted to the model precision. Inputs are not normalized or positivity-repaired.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.pseudomode_product_state"},{"category":"type","location":"api/representation/#PermutationalInvariantDynamics.AbstractPITerm","page":"Representations, states, and models","text":"AbstractPITerm\n\nSupertype for physical contributions to a PIModel. A custom subtype must implement the qualified extension hooks term_operator, term_rate, body_order, term_scope, term_process, validate_term, and compile_term(term, context). The supported compilation pattern is to build an equivalent built-in term and delegate to its compile_term method; compiled kernel types are internal. Sector-changing terms should also implement required_sectors, and driven terms should implement rebuild_term when they must support freeze.\n\nterm_scope returns Val(:local), Val(:collective), or Val(:direct), and term_process returns Val(:hamiltonian) or Val(:jump). Missing hooks raise explicitly rather than being inferred from field names.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.AbstractPITerm"},{"category":"type","location":"api/representation/#PermutationalInvariantDynamics.InPlaceTimeOperator","page":"Representations, states, and models","text":"InPlaceTimeOperator(prototype, update!)\n\nDescribe an operator-valued time dependence without allocating a new operator at every Liouvillian application. prototype fixes the operator's shape, basis, and scalar type. Before each evaluation the caller-owned destination is reset to prototype, then update!(destination, time, parameters) must mutate it in place and return either destination or nothing.\n\nAll built-in Hamiltonian and jump terms lower this schedule into mutable buffers owned by LiouvillianWorkspace, including Appendix-D p-body terms; the schedule itself and the compiled plan remain read-only and may be shared between tasks. Use one workspace per task. Calling the schedule directly, as done by freeze, intentionally returns a fresh evaluated operator.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.InPlaceTimeOperator"},{"category":"type","location":"api/representation/#PermutationalInvariantDynamics.LocalHamiltonian","page":"Representations, states, and models","text":"LocalHamiltonian(H; rate=1, hbar=1, check=true)\n\nHamiltonian term -(im/hbar) * rate * [sum_i H^(i), rho] generated by a one-particle operator. A callable rate or operator follows the package's (time, parameters) convention; check=true verifies a fixed matrix is Hermitian.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.LocalHamiltonian"},{"category":"type","location":"api/representation/#PermutationalInvariantDynamics.CollectiveHamiltonian","page":"Representations, states, and models","text":"CollectiveHamiltonian(H; rate=1, hbar=1, check=true)\n\nHamiltonian term generated by the collective one-body operator sum_i H^(i). It has the same microscopic Hamiltonian action as LocalHamiltonian and is marked as a collective term for model classification. check=true verifies a fixed matrix is Hermitian.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.CollectiveHamiltonian"},{"category":"type","location":"api/representation/#PermutationalInvariantDynamics.LocalJump","page":"Representations, states, and models","text":"LocalJump(L; rate=1)\n\nIndependent one-particle dissipative channels rate * sum_i D[L^(i)], with D[L](rho) = L*rho*L' - {L'*L,rho}/2.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.LocalJump"},{"category":"type","location":"api/representation/#PermutationalInvariantDynamics.CollectiveJump","page":"Representations, states, and models","text":"CollectiveJump(L; rate=1)\n\nCollective dissipative channel rate * D[sum_i L^(i)].\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.CollectiveJump"},{"category":"type","location":"api/representation/#PermutationalInvariantDynamics.CorrelatedLocalJumps","page":"Representations, states, and models","text":"CorrelatedLocalJumps(operators, kossakowski; rate=1,\n                     atol=nothing, rtol=nothing)\n\nCorrelated independent one-particle Lindblad channels\n\nmathrmratesum_isum_abGamma_ab\nleft(L_a^(i)rho L_b^(i)dagger\n-frac12L_b^dagger L_arho^(i)right)\n\nwhere operators is a nonempty collection of equally sized one-particle matrices and kossakowski is a Hermitian positive-semidefinite matrix Γ with one row and column per operator. A fixed Γ is copied, validated, and factorized once at construction. A raw (time, parameters) -> Γ function uses the allocating compatibility path; wrap a matrix prototype and mutating callback in InPlaceTimeOperator for task-local, preallocated evaluation.\n\natol and rtol control explicit Hermiticity and positivity acceptance. Their defaults are exact zero; only working-precision roundoff generated inside the factorization is tolerated. Positive eigenchannels are not dropped by a numerical rank cutoff. The common scalar rate must be real, finite, and representable in the prepared precision when evaluated, but may be negative for deliberately non-CP time-local generators.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.CorrelatedLocalJumps"},{"category":"type","location":"api/representation/#PermutationalInvariantDynamics.CorrelatedCollectiveJumps","page":"Representations, states, and models","text":"CorrelatedCollectiveJumps(operators, kossakowski; rate=1,\n                          atol=nothing, rtol=nothing)\n\nCorrelated collective one-body Lindblad channels\n\nmathrmratesum_abGamma_ab\nleft(J_arho J_b^dagger\n-frac12J_b^dagger J_arhoright)qquad\nJ_a=sum_i L_a^(i)\n\nThe operator, Kossakowski-matrix, tolerance, and time-dependence conventions are the same as for CorrelatedLocalJumps. Collective channels stay within each retained Schur sector, whereas correlated local channels may couple sectors.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.CorrelatedCollectiveJumps"},{"category":"type","location":"api/representation/#PermutationalInvariantDynamics.DirectPIHamiltonian","page":"Representations, states, and models","text":"DirectPIHamiltonian(H; rate=1, hbar=1, check=true)\n\nHamiltonian commutator generated directly by a PIOperator on the model's exact PIBasis. Use this when the Schur blocks are already known rather than from a one- or p-body microscopic decomposition. check=true verifies the fixed PIOperator prototype is Hermitian.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.DirectPIHamiltonian"},{"category":"type","location":"api/representation/#PermutationalInvariantDynamics.DirectPIJump","page":"Representations, states, and models","text":"DirectPIJump(L; rate=1)\n\nDissipative channel rate * D[L] for a PIOperator on the model's exact PIBasis.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.DirectPIJump"},{"category":"type","location":"api/representation/#PermutationalInvariantDynamics.PIModel","page":"Representations, states, and models","text":"PIModel(basis, terms)\n\nValidated immutable PI model. Terms are stored as a concrete tuple and must subtype AbstractPITerm. Construction checks operator dimensions, basis ownership, body orders, and whether a restricted basis contains every sector required by local processes.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.PIModel"},{"category":"type","location":"api/representation/#PermutationalInvariantDynamics.PBodyHamiltonian","page":"Representations, states, and models","text":"PBodyHamiltonian(H, p; rate=1, hbar=1, check=true)\n\nHamiltonian generated by the symmetric sum of H over all unordered p-particle subsets. H is a permutation-symmetric d^p x d^p operator; check=true verifies a fixed matrix is Hermitian.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.PBodyHamiltonian"},{"category":"type","location":"api/representation/#PermutationalInvariantDynamics.LocalPBodyJump","page":"Representations, states, and models","text":"LocalPBodyJump(L, p; rate=1)\n\nSum of independent channels rate * sum_S D[L_S] over unordered p-particle subsets S.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.LocalPBodyJump"},{"category":"type","location":"api/representation/#PermutationalInvariantDynamics.CollectivePBodyJump","page":"Representations, states, and models","text":"CollectivePBodyJump(L, p; rate=1)\n\nCollective channel rate * D[sum_S L_S], where the coherent sum runs over all unordered p-particle subsets.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.CollectivePBodyJump"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.qubit_ensemble_model","page":"Representations, states, and models","text":"qubit_ensemble_model(basis; hamiltonian=nothing,\n                     emission=0, dephasing=0, pumping=0,\n                     collective_emission=0, collective_dephasing=0,\n                     collective_pumping=0, T=nothing)\nqubit_ensemble_model(N; sectors=nothing, kwargs...)\n\nConstruct the standard six-rate PI model for identical qubits. In the package ordering (|g>,|e>), emission uses jm=|g><e|, pumping uses jp=jm', and dephasing uses the spin operator jz=Diagonal(-1/2,1/2). The contribution of each keyword rate is\n\nemission              * sum_i D[jm_i]\ndephasing             * sum_i D[jz_i]\npumping               * sum_i D[jp_i]\ncollective_emission   * D[sum_i jm_i]\ncollective_dephasing  * D[sum_i jz_i]\ncollective_pumping    * D[sum_i jp_i]\n\nwhere DL(rho)=L*rho*L - L*Lrho2. Therefore the local dephasing keyword damps a one-qubit off-diagonal element at dephasing/2; it is not the same normalization as using a Pauli sigma_z jump.\n\nhamiltonian may be a Hermitian 2x2 one-particle matrix, interpreted as sum_i h_i, or a PIOperator on the exact model basis. An InPlaceTimeOperator with either prototype is also accepted. Fixed numerical zero rates are omitted, which permits collective-only models on a restricted Schur basis. Callable rates are retained, and negative rates are passed through unchanged. By default the spin-matrix precision is inferred from the fixed nonzero numerical rates and Hamiltonian prototype; an all-zero or callable-only model defaults to Float64. Pass T explicitly when a callable rate needs another working precision. Wider rates or a wider Hamiltonian may promote the compiled generator.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.qubit_ensemble_model"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.left_superoperator","page":"Representations, states, and models","text":"Matrix representing X -> A*X under Julia column-major vectorization.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.left_superoperator"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.right_superoperator","page":"Representations, states, and models","text":"Matrix representing X -> X*A under Julia column-major vectorization.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.right_superoperator"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.sandwich_superoperator","page":"Representations, states, and models","text":"Matrix representing X -> A*X*B' under column-major vectorization.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.sandwich_superoperator"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.commutator_superoperator","page":"Representations, states, and models","text":"Vectorized generator X -> -im*(H*X-X*H).\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.commutator_superoperator"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.dissipator_superoperator","page":"Representations, states, and models","text":"Vectorized Lindblad dissipator D[L](X).\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.dissipator_superoperator"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.is_pi_operator","page":"Representations, states, and models","text":"is_pi_operator(A, N, d; atol=1e-12, rtol=sqrt(eps()))\n\nTest whether a full Hilbert-space operator commutes with every particle permutation. Adjacent transpositions generate the symmetric group, so only N-1 tensor-index permutations are tested and no permutation matrices are constructed.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.is_pi_operator"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.is_pi_superoperator","page":"Representations, states, and models","text":"is_pi_superoperator(S, N, d; atol=1e-12, rtol=sqrt(eps()))\n\nTest permutation covariance of a full Liouville-space matrix S, namely S C_pi = C_pi S with C_pi vec(rho)=vec(P_pi*rho*P_pi'). The test uses the induced index permutations directly and follows the package's column-major vectorization convention.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.is_pi_superoperator"},{"category":"function","location":"api/representation/#PermutationalInvariantDynamics.is_permutationally_invariant","page":"Representations, states, and models","text":"Alias for is_pi_operator.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.is_permutationally_invariant"},{"category":"section","location":"parameter_scans/#Prepared-parameter-scans-and-continuation","page":"Prepared parameter scans","text":"Phase diagrams and finite-size studies usually solve a sequence of closely related PI models. Reconstructing solver scratch at every point wastes memory bandwidth, while retaining all states merely to continue the next solve can consume far more memory than the Liouvillian application itself. The prepared scan layer separates the immutable experiment description from task-owned numerical scratch.","title":"Prepared parameter scans and continuation"},{"category":"section","location":"parameter_scans/#Workflow","page":"Prepared parameter scans","text":"basis = PIBasis(20, 2)\nsm = ComplexF64[0 1; 0 0]\nsp = Matrix(adjoint(sm))\n\nbuilder = pump -> PIModel(basis, (\n    LocalJump(sm; rate=1.0),\n    LocalJump(sp; rate=pump),\n))\n\nplan = ParameterScanPlan(0.05:0.05:0.5, builder;\n    algorithm=GMRESAlgorithm(krylovdim=30),\n    compile_options=(backend=:matrixfree,),\n    continuation=true)\n\nresult = parameter_scan(plan)\n\nWhen only scalar rates vary, compile the fixed representation geometry once:\n\nprototype = builder(0.05)\nfamily = compile_family(prototype)\n\nfamily_plan = ParameterScanPlan(0.05:0.05:0.5, family;\n    rate_builder=pump -> (1.0, pump),\n    algorithm=RecycledGMRESAlgorithm(\n        krylovdim=30, recycle_dim=8),\n    continuation=true)\n\nfamily_result = parameter_scan(family_plan)\n\nrate_builder follows family.rate_indices order. Each point binds its rates with specialize; immutable Schur geometry is shared, while each task owns its operator compatibility workspace and solver scratch.\n\nThe builder may accept either (parameter) or (parameter, index). A second constructor accepts (parameters, prototype, remaker) for code organized around an immutable prototype. In both cases the callable must return a fresh PIModel, an already CompiledPIModel, or a SpecializedPIModel. Ordinary models are compiled once per parameter when necessary: prepared kernel coefficients may depend on that parameter and therefore cannot in general be shared safely. Use compile_family only under its stricter fixed-operator, scalar-rate contract.","title":"Workflow"},{"category":"section","location":"parameter_scans/#Resource-preflight-and-materialization-guard","page":"Prepared parameter scans","text":"ParameterScanPlan has a 512 MiB memory_budget by default. Before allocating the Krylov/Arnoldi workspace at each point, the scan records a conservative resource report in point.diagnostics.resources. Its known peak includes:\n\nevery active compiled operator and task-owned solver workspace;\ntransient selected Ritz vectors and continuation copies;\none live output per worker;\nall outputs requested by save_outputs=true; and\nthe final restart state or Ritz seed.\n\nFor threaded scans the active per-worker terms are multiplied by the bounded worker-pool size; immutable family geometry is counted once in the process. Distributed scans apply the budget independently on every worker process. The report exposes known_peak_bytes, known_budget_fits, budget_status, and the component bounds. A model builder and the value returned by an arbitrary diagnostic can allocate or retain unbounded user objects, so they appear in unknown_components; consequently safe_to_run remains missing even when all known structural bounds fit.\n\nThe same budget is reserved before compiling a point. An explicit sparse backend whose conservative live assembly bound does not fit is rejected before the sparse PI-coordinate matrix is allocated. A family specialization applies the same rule without recomputing its shared Schur geometry estimate. A builder that constructs and returns an already compiled model has necessarily allocated it before the scan sees it; construct such an object once outside the builder, or return a PIModel/use compile_family, when pre-allocation guarding is required.\n\nalgorithm=:auto is budget aware at the aggregate scan level. Small steady problems prefer the direct route and small complete spectra prefer dense diagonalization only when their full point peak fits; otherwise the scan uses GMRES or ordinary Arnoldi. An explicitly requested direct or dense algorithm still raises instead of changing methods. Output accounting follows the solver route: Float32 matrix-free results retain 8-byte ComplexF32 coordinates, while factorizing steady-state and dense-spectrum guards conservatively reserve 16-byte ComplexF64 output, continuation-copy, and saved-history storage.\n\nFor a large scan, stream reduced diagnostics instead of density operators:\n\nexcited_population = ComplexF64[0 0; 0 1]\nsafe_plan = ParameterScanPlan(parameters, builder;\n    compile_options=(backend=:matrixfree,),\n    memory_budget=2 * 1024^3,\n    save_outputs=false,\n    diagnostic=(rho, parameter) ->\n        (parameter=parameter,\n         excitation=collective_expectation(rho, excited_population)))\n\nReduce krylovdim, the number of threaded workers, retained eigenvectors, or saved outputs when the known peak exceeds the budget. memory_budget=Inf disables the guard explicitly; it is not an automatic fallback.\n\nParameterScanPlan contains no numerical scratch. A ParameterScanWorkspace owns the previous continuation seed and compatible GMRES or Arnoldi arrays. One workspace may be reused sequentially; concurrent tasks require one workspace each. A fresh parameter_scan call clears any old continuation seed while retaining compatible solver arrays. Continue a specific path only through resume_parameter_scan, which reloads the validated checkpoint seed explicitly.","title":"Resource preflight and materialization guard"},{"category":"section","location":"parameter_scans/#Continuation-and-compatibility","page":"Prepared parameter scans","text":"Serial steady-state scans pass the preceding successful state to Krylov GMRES or shift-invert. RecycledGMRESAlgorithm additionally retains a bounded near-zero Ritz space in its task-owned workspace. At every point it recomputes the images of those directions under the new Liouvillian and still validates the complete unpreconditioned residual.\n\nHarmonic-Arnoldi spectral scans retain the selected slow subspace as a thick matrix seed. Block-Arnoldi scans retain up to block_size selected Ritz vectors and pass them back as the next block seed. Other iterative spectral methods use a deterministic combination of the previous selected Ritz vectors, avoiding a one-dimensional Arnoldi breakdown on a single exact eigenvector. The preferred spellings are :arnoldi, :block_arnoldi, :harmonic, :iram, and :jd; retained aliases are normalized before workspace sizing and resource preflight.\n\nContinuation is used only when all of the following match:\n\nparticle number and local dimension;\nretained Schur partitions and PI coordinate dimension;\nnumerical scalar type;\nsteady-state versus spectrum task;\nthe immediately preceding scan path.\n\nSolver storage is independently reused when its task, dimension, scalar type, and Krylov size match. A change in any of these properties rebuilds the workspace instead of converting or truncating it.\n\nOrdinary GMRESAlgorithm continues to reuse only the initial state and compatible arrays. Select RecycledGMRESAlgorithm for true GCRO-style subspace recycling, or use RecycledGMRESWorkspace directly for custom linear systems.","title":"Continuation and compatibility"},{"category":"section","location":"parameter_scans/#Streaming-and-restart","page":"Prepared parameter scans","text":"Set save_outputs=false to avoid retaining every state or spectrum. A callback still receives the complete live ParameterScanPoint:\n\ncallback = point -> begin\n    point.status === :success && println(point.parameter, \" \", point.residual)\n    nothing\nend\n\nprefix = parameter_scan(plan; callback, max_points=25)\nresult = resume_parameter_scan(plan, prefix; callback)\n\nA callback must return nothing to continue or :stop to produce a resumable prefix. With save_restart=true, the result retains at most one state or Ritz seed. Restart seeds are copied when they cross workspace, result, resume, or merge ownership boundaries, so callback-side changes to a streamed state and later changes to a checkpoint cannot corrupt the next continuation point. The result otherwise contains only copied parameters, point records, outputs selected by the user, and plain metadata. It does not retain a builder, compiled generator, workspace, callback, random generator, lock, or exception backtrace.\n\nResume validates the task, complete parameter sequence, every stored index, every indexed parameter, and the continuation/output/restart retention flags before building a model. This prevents a resumed result from mixing retained and discarded histories under misleading metadata. Failed points are retried by default. A continuation result must be a successful prefix because warming across an uncomputed branch would not represent the requested path. When an extension evaluates no point or fails before producing a newer checkpoint, the last validated prefix checkpoint remains available. A result made with save_restart=false resumes cold, even if the supplied workspace was used by another compatible-looking scan. Results without a saved seed report restart_index=0; a nonzero restart index therefore always identifies an owned usable checkpoint.","title":"Streaming and restart"},{"category":"section","location":"parameter_scans/#Failure-policy","page":"Prepared parameter scans","text":"Every failure creates a point with status=:failed, converged=false, and a printable error type and message.\n\non_error=:stop is the default and returns at the failed point;\non_error=:record continues, but clears the continuation seed;\non_error=:throw invokes the callback and then rethrows in serial mode.\n\nNo policy silently drops a parameter. Solver residual, trace error, convergence, iteration count, compilation time, solution time, warm-start status, workspace-reuse status, and nested solver diagnostics are recorded per successful point.","title":"Failure policy"},{"category":"section","location":"parameter_scans/#Threaded-and-distributed-scans","page":"Prepared parameter scans","text":"Path-dependent continuation is deliberately incompatible with threaded execution. Independent points use\n\nindependent = ParameterScanPlan(parameters, builder;\n    continuation=false, algorithm=GMRESAlgorithm())\nresult = parameter_scan(independent; execution=:threads)\n\nEach worker owns one workspace reused across its independent points, and every point receives an index-derived deterministic random stream. An acknowledgement protocol bounds live unsaved outputs and out-of-order callback records by the worker count. Results and callbacks remain in parameter-index order. The builder, remaker, and user diagnostic must themselves be thread safe.\n\nMulti-process schedulers can call parameter_scan with disjoint indices and then use merge_parameter_scan_results when continuation=false. Merging verifies the task, parameter sequence, stored index/value pairs, and absence of duplicate indexes. Independently computed continuation chunks are rejected because they do not prove a common restart path; use resume_parameter_scan for that case. This explicit chunking keeps Distributed.jl or cluster scheduling out of the package's core dependency set.\n\nAfter loading the Distributed stdlib, the optional package extension performs that partitioning directly:\n\nusing Distributed\naddprocs(4; exeflags=\"--project=$(dirname(Base.active_project()))\")\n\ndistributed = distributed_parameter_scan(independent)\n\nThe function deterministically assigns contiguous, balanced index chunks, loads the package on every selected worker, evaluates all chunk failures as records, and applies on_error plus callbacks on the master in global index order. It rejects continuation=true. A master callback causes live numerical outputs to cross process boundaries and is accepted only when save_outputs=true, making that retained-memory cost explicit. For large states, put the scalar reduction in plan.diagnostic and omit the callback. All worker chunks finish before master-side callback stopping or on_error selection is applied; these policies shorten the returned ordered prefix but do not cancel already dispatched remote computation. The plan and its captured builder/remaker must be serializable, and every worker must activate an environment containing a compatible package version.","title":"Threaded and distributed scans"},{"category":"section","location":"parameter_scans/#Spectrum-scans","page":"Prepared parameter scans","text":"Set task=:spectrum, select a high-level spectral algorithm, and optionally retain eigenvectors:\n\nplan = ParameterScanPlan(parameters, builder;\n    task=:spectrum,\n    algorithm=:block_arnoldi,\n    spectrum_target=:largest_real,\n    nev=8,\n    solver_options=(krylovdim=48, block_size=4, maxrestarts=20),\n    save_vectors=false)\n\nThe continuation seed is retained even when the returned spectra omit their vectors. Block-Arnoldi resource estimates include the block workspace and bounded restart matrix; changing block_size, dimension, or scalar type rebuilds that task-owned scratch. Dense spectra accept the same scan interface but do not claim a warm start or Krylov-workspace reuse.\n\nFor steady-state scans with GMRESAlgorithm or RecycledGMRESAlgorithm, the algorithm fields are the single source of workspace sizing. Repeating krylovdim or recycle_dim in solver_options is rejected instead of silently selecting one value.","title":"Spectrum scans"},{"category":"section","location":"parameter_scans/#Dependency-free-rows-and-columns","page":"Prepared parameter scans","text":"parameter_scan_rows returns named-tuple rows and parameter_scan_columns returns a named tuple of vectors. They intentionally exclude large outputs and nested diagnostics by default. Pass include_output=true when those objects are required. These representations are straightforward to adapt to Tables.jl without imposing it on core users.\n\nLoading Tables.jl activates a lazy row-table interface directly on ParameterScanResult:\n\nusing Tables\nrows = Tables.rows(result)\ncolumns = Tables.columntable(result)\n\nIts schema is the same scalar metadata returned by parameter_scan_rows; it never exposes output or nested diagnostics implicitly. Iterating rows is lazy, while a column-table sink naturally allocates its output columns.","title":"Dependency-free rows and columns"},{"category":"section","location":"parameter_scans/#API","page":"Prepared parameter scans","text":"","title":"API"},{"category":"type","location":"parameter_scans/#PermutationalInvariantDynamics.ParameterScanPlan","page":"Prepared parameter scans","text":"ParameterScanPlan(parameters, model_builder; task=:steady_state, ...)\n\nImmutable, shareable description of a parameter scan. model_builder is called as either model_builder(parameter) or model_builder(parameter, index) and must return a PIModel, an already CompiledPIModel, or a SpecializedPIModel. Models are compiled point by point because their prepared kernels may depend on the scanned parameter; a returned compiled model is used directly. The default compilation backend is :matrixfree; override compile_options explicitly for a materialized scan.\n\ntask=:steady_state calls stationary_state, while task=:spectrum calls liouvillian_spectrum. The corresponding solver is selected by algorithm. compile_options and solver_options must be named tuples. Internally managed keywords such as initial_state, workspace, and return_info are rejected instead of being silently overridden.\n\nmemory_budget defaults to 512 MiB and is enforced before each solver workspace is allocated. Its conservative scan peak includes active compiled operators, solver storage, one live output per worker, every output requested by save_outputs=true, and the final restart seed. The builder and arbitrary user diagnostics are not size-predictable and are reported as exclusions. Explicit sparse compilation or family specialization is rejected before materialization when its own bound exceeds the same budget. Pass memory_budget=Inf only as an explicit opt-out after checking available RAM.\n\nWith continuation=true, a serial scan uses the preceding successful state or Ritz vector as the next initial condition and reuses a compatible Krylov workspace. Plans contain no mutable numerical scratch and may be shared among tasks; every concurrent caller must own a separate ParameterScanWorkspace. The plan owns a copy of the parameter container and never mutates it.\n\nFor spectral scans, spectrum_target accepts the same values as liouvillian_spectrum, and nev is the requested number of modes. save_outputs=false supports streaming scans with bounded state-history storage. save_restart=true still retains one final continuation seed in the result so a prefix scan can be resumed. diagnostic may be a callable of (output, parameter, index), (output, parameter), or (output) and its return value is stored alongside the solver diagnostics.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.ParameterScanPlan"},{"category":"type","location":"parameter_scans/#PermutationalInvariantDynamics.ParameterScanWorkspace","page":"Prepared parameter scans","text":"ParameterScanWorkspace()\n\nTask-owned mutable continuation, Liouvillian, and Krylov scratch for parameter_scan. Reuse it sequentially across compatible scans, but never from concurrent tasks. Family scans reuse one LiouvillianWorkspace while the immutable family plan is unchanged. Krylov storage is discarded and rebuilt whenever the task, PI coordinate dimension, scalar type, or requested subspace size changes.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.ParameterScanWorkspace"},{"category":"function","location":"parameter_scans/#PermutationalInvariantDynamics.clear_parameter_scan_workspace!","page":"Prepared parameter scans","text":"Clear all continuation and solver scratch retained by workspace.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.clear_parameter_scan_workspace!"},{"category":"type","location":"parameter_scans/#PermutationalInvariantDynamics.ParameterScanPoint","page":"Prepared parameter scans","text":"ParameterScanPoint\n\nOne explicit parameter-scan record. status is :success or :failed; failed records retain a printable error type and message and are never treated as converged. Residual and trace-error values preserve the solver's scalar precision. Wall-clock fields are measured in seconds.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.ParameterScanPoint"},{"category":"type","location":"parameter_scans/#PermutationalInvariantDynamics.ParameterScanResult","page":"Prepared parameter scans","text":"ParameterScanResult\n\nCheckpoint-neutral parameter-scan output. It contains parameter values, plain point records, optional saved numerical outputs, and at most one final continuation seed; it never retains the model builder, compiled models, solver workspaces, locks, random generators, callbacks, or exception backtraces. Use resume_parameter_scan with the original plan to continue a prefix, and merge_parameter_scan_results to join disjoint chunks.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.ParameterScanResult"},{"category":"function","location":"parameter_scans/#PermutationalInvariantDynamics.parameter_scan","page":"Prepared parameter scans","text":"parameter_scan(plan; workspace=ParameterScanWorkspace(), callback=nothing,\n               indices=nothing, execution=:serial, on_error=:stop,\n               max_points=nothing)\n\nExecute a prepared parameter scan. Serial execution reuses compatible Krylov storage and, within this invocation, warm-starts each continuation point from the preceding success. A fresh public call clears any continuation seed left in workspace; use resume_parameter_scan to continue a validated checkpoint path. Threaded execution requires continuation=false.\n\nThe callback receives points in increasing index order and may return :stop. Failures follow the explicit on_error policy, and max_points creates a bounded resumable invocation. See ParameterScanPlan for output and restart retention controls.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.parameter_scan"},{"category":"function","location":"parameter_scans/#PermutationalInvariantDynamics.resume_parameter_scan","page":"Prepared parameter scans","text":"resume_parameter_scan(plan, previous; retry_failed=true, kwargs...)\n\nResume missing points of a checkpoint-neutral ParameterScanResult. The parameter sequence, task, indexes, and stored parameter values are checked strictly before any model is built. The continuation and output/restart retention settings must also match the checkpoint-producing plan, preventing mixed-history results. Failed points are retried by default.\n\nContinuation plans require the retained successful records to form a prefix; the stored final restart seed is used when available. A result created with save_restart=false remains resumable, but the first new point starts cold. Additional keywords are forwarded to parameter_scan; execution must remain serial for continuation.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.resume_parameter_scan"},{"category":"function","location":"parameter_scans/#PermutationalInvariantDynamics.merge_parameter_scan_results","page":"Prepared parameter scans","text":"merge_parameter_scan_results(plan, results...)\n\nJoin nonoverlapping checkpoint-neutral scan chunks in parameter-index order. Every result's task, parameter sequence, stored indexes, and indexed values are validated, and duplicate indexes raise. Multiple chunks are accepted only when plan.continuation == false; use resume_parameter_scan for a path-dependent continuation.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.merge_parameter_scan_results"},{"category":"function","location":"parameter_scans/#PermutationalInvariantDynamics.distributed_parameter_scan","page":"Prepared parameter scans","text":"distributed_parameter_scan(plan; workers=Distributed.workers(),\n                           indices=nothing, callback=nothing,\n                           on_error=:stop)\n\nExecute independent parameter points on deterministic, disjoint worker chunks. This optional method becomes available after loading the Distributed stdlib. The plan must have continuation=false: a path-dependent continuation cannot be partitioned across workers without changing its initial states.\n\nWorkers load PermutationalInvariantDynamics in their active project, run serial chunks with index-derived random streams, and return checkpoint-neutral records. Failure policy and callbacks are then applied on the master in global index order, and duplicate or inconsistent records are rejected by merge_parameter_scan_results. A master callback requires the live point outputs to cross the process boundary and is therefore accepted only when save_outputs=true. Prefer a scalar diagnostic in the plan when large states should remain off the master.\n\nEach worker must use an environment containing a compatible package version, and the builder, remaker, parameter values, algorithm, and diagnostic must be serializable. Loading Distributed does not add it to the core dependency set.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.distributed_parameter_scan"},{"category":"function","location":"parameter_scans/#PermutationalInvariantDynamics.parameter_scan_rows","page":"Prepared parameter scans","text":"parameter_scan_rows(result; include_output=false)\n\nReturn a stable vector of named-tuple rows suitable for display, serialization, or an optional Tables.rowtable-style adapter. Core code does not depend on Tables.jl. Set include_output=true to include potentially large states, eigenvectors, and nested diagnostics.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.parameter_scan_rows"},{"category":"function","location":"parameter_scans/#PermutationalInvariantDynamics.parameter_scan_columns","page":"Prepared parameter scans","text":"parameter_scan_columns(result; include_output=false)\n\nReturn the same scalar metadata as parameter_scan_rows in a named tuple of columns. This dependency-free representation can be passed directly to many data and plotting tools. Potentially large numerical outputs are excluded unless include_output=true.\n\n\n\n\n\n","title":"PermutationalInvariantDynamics.parameter_scan_columns"},{"category":"section","location":"getting_started/#Getting-started:-from-a-model-to-a-solution","page":"Getting started","text":"This page walks through one complete calculation and explains what each object does. If this is your first use of the package, follow the steps in order. The same pattern applies to qubits, qudits, closed dynamics, open dynamics, and most of the advanced workflows:\n\nlocal basis and matrices\n          ↓\nphysical PI terms → PIModel → compile once → solve → analyse → converge\n\nThe runnable version is examples/getting_started.jl.","title":"Getting started: from a model to a solution"},{"category":"section","location":"getting_started/#Before-starting:-does-PI-symmetry-apply?","page":"Getting started","text":"The exact PI representation is appropriate when all three statements hold:\n\nEvery constituent has the same local dimension d.\nThe initial density operator is invariant under relabelling constituents.\nThe generator treats constituent labels identically, so it preserves that invariance.\n\nThe constituents need not be bosons, and the state need not remain in the fully symmetric Hilbert-space sector. PIBasis(N, d) retains every Schur sector by default. That default is the safe choice for local incoherent processes such as independent spontaneous emission.\n\nIf individual constituents have different frequencies, rates, or controls, the model is generally not PI. Several separately PI ensembles can instead be combined through the composite-system workflow.","title":"Before starting: does PI symmetry apply?"},{"category":"section","location":"getting_started/#The-example-model","page":"Getting started","text":"Consider N identical qubits in the local basis\n\ngrangle = (10)^mathsf Tqquad\nerangle = (01)^mathsf T\n\nThey experience a coherent transverse drive, independent emission, and independent pumping:\n\nfracdrhodt\n=-ileftfracOmega2sum_isigma_i^xrhoright\n+gamma_downarrowsum_imathcal Dsigma_i^-rho\n+gamma_uparrowsum_imathcal Dsigma_i^+rho\n\nHere \\mathcal D[L]\\rho=L\\rho L^\\dagger-\\{L^\\dagger L,\\rho\\}/2. The model is PI because every site receives the same Hamiltonian and the same local rates.","title":"The example model"},{"category":"section","location":"getting_started/#Step-1:-create-the-PI-basis","page":"Getting started","text":"using PermutationalInvariantDynamics\n\nN = 8\nd = 2\nbasis = PIBasis(N, d)\n\nPIBasis defines the Schur sectors, Gel'fand–Tsetlin patterns, and coordinate offsets used by every state, operator, and model built from it. Construct it once and reuse the exact same basis object.\n\nThe number of retained PI operator coordinates is\n\nnpi = pi_dimension(basis)  # 165 for N=8 qubits\n\nBy comparison, a general full density operator has d^(2N) = 65_536 coordinates here. Production PI algorithms never construct that full object.","title":"Step 1: create the PI basis"},{"category":"section","location":"getting_started/#Step-2:-define-the-local-matrices","page":"Getting started","text":"spin = spin_matrices()       # local order: (|g>, |e>)\nsx = 2 * spin.jx\nsm = spin.jm                 # |g><e|\nsp = spin.jp                 # |e><g|\nnumber = sp * sm             # |e><e|\n\nThese are only d-by-d one-particle matrices. The package lifts them into the PI representation; do not construct an N-particle Kronecker product.\n\nMatrix columns are input states and rows are output states. Therefore sm[1, 2] = 1 maps the second local basis vector, |e>, to the first, |g>. Julia uses one-based indices even when a paper labels the same states 0 and 1. The helper also fixes the package spin convention: spin.jz = diag(-1/2, 1/2) in this order.","title":"Step 2: define the local matrices"},{"category":"section","location":"getting_started/#Step-3:-translate-the-master-equation-into-terms","page":"Getting started","text":"Omega = 0.7\ngamma_down = 0.12\ngamma_up = 0.02\n\nterms = (\n    LocalHamiltonian(spin.jx; rate=Omega),\n    LocalJump(sm; rate=gamma_down),\n    LocalJump(sp; rate=gamma_up),\n)\n\nChoose constructors from the physical meaning of the process:\n\nPhysical contribution Constructor\nIdentical one-particle Hamiltonian, sum_i h_i LocalHamiltonian(h)\nIndependent identical jumps, sum_i D[l_i] LocalJump(l)\nOne coherent summed jump, D[sum_i L_i] CollectiveJump(L)\nA Hamiltonian already stored as a PIOperator DirectPIHamiltonian(H)\nA permutation-symmetric p-particle process PBodyHamiltonian, LocalPBodyJump, or CollectivePBodyJump\n\nThe difference between local and collective jumps is essential. LocalJump sums probabilities from unresolved independent channels and can move population between Schur sectors. CollectiveJump sums amplitudes first and then forms one dissipator.\n\nThe rate keyword multiplies the term exactly as supplied. The package does not insert a factor of N, a Kac normalization, or a unit conversion.","title":"Step 3: translate the master equation into terms"},{"category":"section","location":"getting_started/#Step-4:-build-the-declarative-model","page":"Getting started","text":"model = PIModel(basis, terms)\n\n@assert isautonomous(model)\nmodel_report = diagnostics(model)\n\nPIModel records the physics but owns no mutable solver scratch. An autonomous model has no explicit time dependence; it can be used for dynamics, stationary states, and ordinary Liouvillian spectra.\n\ndiagnostics(model) reports the PI dimension, term information, autonomy, and memory estimates. Model constructors also reject incompatible matrix sizes, non-Hermitian Hamiltonians, and unsupported restricted-sector transitions before a solve begins. For a large model, recommend_solver is the assembly-free preflight; detailed model diagnostics may perform generator validation work.","title":"Step 4: build the declarative model"},{"category":"section","location":"getting_started/#Step-5:-construct-and-validate-the-initial-state","page":"Getting started","text":"Start from the fully excited product state:\n\nrho0 = computational_product_state(basis, 2)\nvalidate_state(rho0)\n\ncomputational_product_state(basis, 2) uses Julia's one-based local level and constructs (eranglelangle e)^otimes N directly in PI coordinates. It does not form a 2^N state vector. validate_state returns the state when its trace, Hermiticity, and positivity checks pass; it throws instead of repairing an invalid state.\n\nCommon alternatives are:\n\nrho_same = iid_pure_state(basis, ComplexF64[0, 1])\n\nmixed_local = ComplexF64[0.7 0; 0 0.3]\nrho_mixed = iid_state(basis, mixed_local)\n\nrho_dicke = dicke_state(basis, N / 2, 0)\nrho_ghz = ghz_state(basis)\nrho_coherent = spin_coherent_state(basis, pi / 3, pi / 4)\n\nAn identical mixed product state generally occupies several Schur sectors; that is still a PI state and is handled by the complete basis.","title":"Step 5: construct and validate the initial state"},{"category":"section","location":"getting_started/#Step-6:-compile-once","page":"Getting started","text":"prepared = compile(model; backend=:auto)\nprepared_report = diagnostics(prepared)\n\nCompilation lowers the local matrices into reusable Schur-block geometry and Liouvillian kernels. It does not evolve a state. Reuse prepared for every initial state, time interval, stationary solve, or spectrum with the same model.\n\nbackend=:auto is the recommended first choice. It selects a conservative sparse or matrix-free representation from the actual compiled problem. For a large calculation, inspect the recommendation before compiling:\n\nadvice = recommend_solver(model; task=:dynamics)\n\nInspect advice.resources to distinguish setup, retained plan, solver workspace, and output storage. advice.budget_status is :fits, :exceeds, :unknown, or :disabled; safe_to_run is intentionally missing when an estimate cannot certify a fit. The high-level stationary, spectral, and dynamics commands use a 512 MiB default budget, select a matrix-free route when an automatic dense/direct choice is too large, and reject an explicitly requested over-budget materialization before allocating it. Use memory_budget=Inf only to opt out deliberately.\n\nThe recommendation is a transparent resource preflight, not a convergence guarantee. See Architecture and efficient workflows for explicit backend and workspace control.","title":"Step 6: compile once"},{"category":"section","location":"getting_started/#Step-7A:-compute-time-evolution-and-retain-states","page":"Getting started","text":"times = collect(0.0:0.1:4.0)\n\nsolution = solve_dynamics(\n    prepared,\n    rho0,\n    (first(times), last(times));\n    saveat=times,\n    steps_per_interval=16,\n    observables=(excited=number,),\n    save_states=true,\n)\n\nThe important arguments are:\n\nArgument Meaning\ntspan=(t0,t1) Physical integration interval\nsaveat Times returned to the user; explicit times must include both endpoints\nsteps_per_interval Fixed RK4 steps taken between two consecutive saved times\nobservables Named local matrices or PIOperators sampled during propagation\nsave_states Whether every sampled PIState is retained\n\nFor an autonomous model, an adaptive matrix-free exponential action is also available:\n\nsolution_expv = solve_dynamics(\n    prepared,\n    rho0,\n    (first(times), last(times));\n    saveat=times,\n    algorithm=ExpvAlgorithm(\n        krylovdim=30,\n        atol=1e-11,\n        rtol=1e-9,\n    ),\n    observables=(excited=number,),\n    save_states=true,\n)\n\nThis reuses one Arnoldi workspace and task-owned Liouvillian workspace between saved times. It is often useful when output times are sparse relative to the RK4 step required for accuracy. It is not a time-ordered exponential: ExpvAlgorithm therefore rejects driven generators and nonempty parameters. Use RK4 or dynamics_problem for those cases.\n\nA local matrix passed as an observable denotes its collective sum. Thus the stored excitation series is sum_i <e_i|rho(t)|e_i>:\n\nexcited_fraction = real.(solution.observables[:excited]) ./ N\nrho_final = solution[end]\nrho_at_two = state_at(solution, 2.0)\n\nFor a saved-grid result, physical-time lookup requires a stored time. Iterating over solution iterates over the retained states when save_states=true. Use state(solution, i) for a saved index and state_at(solution, t) for a physical time; this distinction remains explicit when t is an integer.","title":"Step 7A: compute time evolution and retain states"},{"category":"section","location":"getting_started/#Step-7B:-retain-only-observables","page":"Getting started","text":"If the state history is not needed, use the same prepared model with save_states=false:\n\nseries = solve_dynamics(\n    prepared,\n    rho0,\n    (first(times), last(times));\n    saveat=times,\n    steps_per_interval=16,\n    observables=(excited=number,),\n    save_states=false,\n)\n\nexcited_fraction = real.(series.observables[:excited]) ./ N\n@assert series.states === nothing\n\nThis keeps one evolving PI state plus the requested scalar arrays. It is the preferred output policy for long evolutions or large parameter scans when no later state-level analysis is planned.\n\nThe two result forms are:\n\nCall Result contents\nNo observables DynamicsResult with times and saved states\nobservables=..., save_states=true DynamicsStreamResult with times, states, and observable arrays\nobservables=..., save_states=false DynamicsStreamResult with times and observable arrays; states === nothing","title":"Step 7B: retain only observables"},{"category":"section","location":"getting_started/#Step-7C:-compute-the-stationary-state","page":"Getting started","text":"Because this model is autonomous, its stationary state can be solved from the same prepared object:\n\nsteady = stationary_state(prepared; return_info=true)\nrho_ss = steady.state\n\n@assert steady.info.converged\nprintln(\"stationary residual = \", steady.info.residual)\nprintln(\"stationary trace error = \", steady.info.trace_error)\nvalidate_state(rho_ss)\n\nWithout return_info=true, stationary_state(prepared) returns the PIState directly. Retain the result information when reporting numerical accuracy. Solver convergence and state validity are separate checks, and neither proves that the stationary state is unique. Study near-zero modes or use an explicit uniqueness certificate when uniqueness is part of the conclusion.\n\nStationary-state and ordinary spectrum commands reject a genuinely driven model. For a periodic generator, use Floquet dynamics. freeze(model; time=t, parameters=p) answers only the instantaneous stationary question at that chosen time; it is not the steady state of the driven evolution.","title":"Step 7C: compute the stationary state"},{"category":"section","location":"getting_started/#Step-8:-analyse-the-result","page":"Getting started","text":"The same state can be inspected at several levels:\n\nreport = diagnostics(rho_final)\nexcited_final = collective_expectation(rho_final, number) / N\nrho_one = one_body_rdm(rho_final)\npurity_one = reduced_purity(rho_final, 1)\n\nFor a valid density operator, report.valid is true and the trace, Hermiticity, and positivity errors lie within their reported tolerances. collective_expectation(rho, number) returns the same total one-body excitation measured during propagation. one_body_rdm instead returns the normalized single-particle marginal.\n\nOther analysis starts from the same PIState:\n\nentropy = von_neumann_entropy(rho_ss)\nfisher = qfi(rho_ss, spin.jx)\nblocks = schur_block_structure(rho_ss; metric=:population)\n\nPrepare CollectiveObservablePlan or ReductionPlan when repeating the same analysis for many states. If one PI particle is itself a tensor product, such as spin tensor local_mode, prepare LocalFactorTracePlan to remove the same internal factor from every supersite before applying the ordinary particle-bipartition tools.","title":"Step 8: analyse the result"},{"category":"section","location":"getting_started/#Step-9:-check-numerical-convergence","page":"Getting started","text":"PI compression is exact, but a fixed-step integrator still has a time-step error. Repeat the observable calculation with more RK4 steps between saved times:\n\ncoarse = solve_dynamics(\n    prepared, rho0, (first(times), last(times));\n    saveat=times, steps_per_interval=8,\n    observables=(excited=number,), save_states=false)\n\nfine = solve_dynamics(\n    prepared, rho0, (first(times), last(times));\n    saveat=times, steps_per_interval=16,\n    observables=(excited=number,), save_states=false)\n\nstep_error = maximum(abs.(\n    coarse.observables[:excited] .- fine.observables[:excited])) / N\n\nIncrease the resolution again until step_error is small compared with the accuracy required by the physical conclusion. A small stationary residual, a converged time step, a converged Krylov space, and a sufficiently large trajectory ensemble are different checks; one never substitutes for another. The numerical-convergence guide provides reusable refinement reports.","title":"Step 9: check numerical convergence"},{"category":"section","location":"getting_started/#Complete-runnable-script","page":"Getting started","text":"Putting the central steps together gives:\n\nusing PermutationalInvariantDynamics\n\nN = 8\nbasis = PIBasis(N, 2)\nspin = spin_matrices()\nsm = spin.jm\nsp = spin.jp\nnumber = sp * sm\n\nmodel = PIModel(basis, (\n    LocalHamiltonian(spin.jx; rate=0.7),\n    LocalJump(sm; rate=0.12),\n    LocalJump(sp; rate=0.02),\n))\nrho0 = computational_product_state(basis, 2)\nprepared = compile(model; backend=:auto)\n\ntimes = collect(0.0:0.1:4.0)\nsolution = solve_dynamics(\n    prepared, rho0, (first(times), last(times));\n    saveat=times, steps_per_interval=16,\n    observables=(excited=number,), save_states=true)\n\nexcited_fraction = real.(solution.observables[:excited]) ./ N\nsteady = stationary_state(prepared; return_info=true)\n\n@assert diagnostics(solution[end]).valid\n@assert steady.info.converged\n@assert diagnostics(steady.state).valid\nprintln(\"PI coordinates: \", pi_dimension(basis))\nprintln(\"final excitation fraction: \", last(excited_fraction))\nprintln(\"stationary residual: \", steady.info.residual)","title":"Complete runnable script"},{"category":"section","location":"getting_started/#Common-modifications","page":"Getting started","text":"","title":"Common modifications"},{"category":"section","location":"getting_started/#Time-dependent-scalar-rates","page":"Getting started","text":"A scalar schedule receives the current time and the user-supplied parameter object:\n\ndrive = (t, p) -> p.Omega * cos(p.omega * t)\ndriven_model = PIModel(basis, (\n    LocalHamiltonian(spin.jx; rate=drive),\n    LocalJump(sm; rate=0.12),\n))\ndriven = compile(driven_model; backend=:matrixfree)\n\ndriven_solution = solve_dynamics(\n    driven, rho0, (0.0, 4.0);\n    saveat=0.1, parameters=(Omega=0.7, omega=2.0))\n\nUse InPlaceTimeOperator when the matrix itself, rather than only its scalar coefficient, changes with time.","title":"Time-dependent scalar rates"},{"category":"section","location":"getting_started/#Collective-decay","page":"Getting started","text":"Replace\n\nLocalJump(sm; rate=gamma_down)\n\nwith\n\nCollectiveJump(sm; rate=gamma_collective)\n\nonly when the physical collapse operator is J_- = sum_i sigma_i^-. The two models have different interference and Schur-sector structure.","title":"Collective decay"},{"category":"section","location":"getting_started/#Qudits","page":"Getting started","text":"For d > 2, use PIBasis(N, d), d-component local states, and d-by-d local matrices. The construction and solver sequence is unchanged, but PI dimension grows more rapidly with N.","title":"Qudits"},{"category":"section","location":"getting_started/#Adaptive-or-stiff-integration","page":"Getting started","text":"Install a compatible SciML solver and use:\n\nproblem = dynamics_problem(prepared, rho0, (0.0, 4.0))\n\nThen solve problem with the selected external algorithm. The package does not add a heavy ODE solver dependency to its core.","title":"Adaptive or stiff integration"},{"category":"section","location":"getting_started/#Other-solution-types","page":"Getting started","text":"Goal Continue with\nQuantum-jump records quantum_trajectories and streaming output\nHomodyne or heterodyne records Diffusive monitoring\nPeriodically driven stationary response floquet_steady_state and spectral visualization\nMany related parameter points Prepared parameter scans\nVery large product-state prediction Mean-field predictions\nFinite-memory bosonic environment PI–HEOM\nOne cavity or pseudomode shared by the ensemble Global pseudomodes and shared cavities\nIdentical independent local pseudomodes Local pseudomodes and PI supersites\nSeveral PI ensembles or a finite ancilla Composite systems","title":"Other solution types"},{"category":"section","location":"getting_started/#Common-errors-and-their-fixes","page":"Getting started","text":"Symptom Likely cause Fix\nLocal operator has the wrong size Matrix dimension differs from basis.d Supply a d-by-d matrix\nA lowering operator raises the state Row/column or local-level order is reversed Write its action on each basis column explicitly\nRestricted-sector compilation fails A local process leaves the selected sectors Start with the complete PIBasis(N,d)\nBasis-ownership error State, operator, plan, and model were built from different basis objects Reuse the exact same basis instance\nHamiltonian construction fails The supplied matrix is not Hermitian Correct the matrix or use an explicitly documented unchecked constructor only for diagnostics\nStationary or spectral solve rejects the model The generator is time dependent Evolve it, use Floquet tools, or explicitly freeze one instant\nA trajectory rejects a rate accepted by deterministic evolution Jump probabilities require nonnegative rates Use a nonnegative unraveling or deterministic evolution\nseries[i] throws after observable-only evolution save_states=false deliberately discarded states Read series.observables, or rerun with save_states=true\nResults change when steps_per_interval increases The fixed-step integration is not converged Refine until the quantity of interest stabilizes\nA SciML algorithm name is undefined The corresponding solver package is not installed/imported Add and import a compatible OrdinaryDiffEq package","title":"Common errors and their fixes"},{"category":"section","location":"getting_started/#Where-to-go-next","page":"Getting started","text":"Read Framework and physical conventions to understand the Schur–Weyl representation and equation-(7) normalization.\nRead API tiers and prepared analysis before choosing lower- level workspaces or solver internals.\nUse Research examples to find a literature model or workflow close to your calculation.\nConsult the complete public API for signatures and interactive-help descriptions.","title":"Where to go next"}]
}
